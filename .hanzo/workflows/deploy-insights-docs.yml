name: Deploy Insights Docs

on:
  push:
    branches: [main, dev]
    paths:
      - 'apps/insights-docs/**'
      - 'packages/**'
      - 'pnpm-lock.yaml'
      - '.hanzo/workflows/deploy-insights-docs.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: hanzo-docs-build-linux-amd64
    permissions:
      contents: read
      deployments: write
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - uses: oven-sh/setup-bun@v2

      - run: pnpm install --frozen-lockfile

      - run: pnpm build --filter=insights-docs
        env:
          NODE_OPTIONS: '--max-old-space-size=8192'

      - name: Fetch CF credentials from KMS
        id: kms
        uses: ./.hanzo/actions/kms
        with:
          client-id: ${{ secrets.KMS_CLIENT_ID }}
          client-secret: ${{ secrets.KMS_CLIENT_SECRET }}
          endpoint: ${{ vars.KMS_ENDPOINT || 'https://kms.hanzo.ai' }}
          env: ${{ vars.KMS_SECRET_ENV || 'prod' }}

      - name: Create Cloudflare Pages project (if needed)
        continue-on-error: true
        run: npx wrangler pages project create insights-docs --production-branch=main
        env:
          CLOUDFLARE_API_TOKEN: ${{ steps.kms.outputs.cf-token }}
          CLOUDFLARE_ACCOUNT_ID: ${{ steps.kms.outputs.cf-account }}

      - name: Deploy to Cloudflare Pages
        run: npx wrangler pages deploy apps/insights-docs/out --project-name=insights-docs --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ steps.kms.outputs.cf-token }}
          CLOUDFLARE_ACCOUNT_ID: ${{ steps.kms.outputs.cf-account }}
