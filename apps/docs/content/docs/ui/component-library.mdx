---
title: Component Library
description: The underlying headless component library.
icon: Library
---

## Overview

Hanzo Docs UI maintains support for both [Base UI](https://base-ui.com) and [Radix UI](https://radix-ui.com/primitives), while it uses Radix UI by default.

The configuration may be different if you use Hanzo Docs CLI for customising components.

### Base UI

You can opt-in to use Base UI by replacing `@hanzo/docs-ui` with the `@hanzo/docs-base-ui` package:

```json title="package.json"
{
  "dependency": {
    "@hanzo/docs-ui": "npm:@hanzo/docs-base-ui@latest"
  }
}
```

For Hanzo Docs CLI, also configure it in your `cli.json` config:

```json title="cli.json"
{
  "uiLibrary": "base-ui"
}
```
