---
icon: Sparkles
title: AI API
description: Unified API for 200+ LLM providers. One API, all models. Load balancing, caching, rate limiting, and observability.
index: true
---

import { Cards, Card } from '@hanzo/docs-ui/components/card'
import { Sparkles } from 'lucide-react'

> **API reference** · [Hanzo AI — Inference API →](/docs/openapi/ai) — every endpoint, generated from the OpenAPI spec.

# AI API

One API for **200+ language models** from every major provider. OpenAI-compatible interface with load balancing, caching, rate limiting, fallbacks, and full observability.

```bash title="Quick Start"
curl https://api.hanzo.ai/v1/chat/completions \
  -H "Authorization: Bearer $HANZO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zen5",
    "messages": [{"role": "user", "content": "Hello"}]
  }'
```

The AI API is on the same host as every other Hanzo service: `api.hanzo.ai`, one `hk-*` key, no second base URL. List model ids with `GET https://api.hanzo.ai/v1/models`.

## Why Hanzo AI API?

- **200+ Models** — Claude, GPT, Gemini, Llama, Mistral, and more
- **OpenAI Compatible** — Drop-in replacement, use any OpenAI SDK
- **Smart Routing** — Automatic fallbacks, load balancing, model selection
- **Cost Control** — Per-key budgets, rate limits, usage analytics
- **Caching** — Semantic and exact caching to reduce costs up to 90%
- **Observability** — Full request logging, latency tracking, token analytics

## Supported Providers

| Provider | Models | Features |
|----------|--------|----------|
| **Anthropic** | Claude 4.x, Claude 3.x | Vision, tool use, extended context |
| **OpenAI** | GPT-4o, o3, o4-mini | Function calling, vision, DALL-E |
| **Google** | Gemini 2.x, PaLM | Multimodal, grounding |
| **Meta** | Llama 3.x, Llama 4 | Open source, self-hosted |
| **Mistral** | Mistral Large, Codestral | European, code generation |
| **Together AI** | 50+ open models | Fast inference, fine-tuning |
| **Groq** | Llama, Mixtral | Fastest inference |
| **Zen LM** | 600M-480B | Frontier open-weight models |

## SDK Usage

### Python

```python
from openai import OpenAI

client = OpenAI(
    api_key="your-hanzo-api-key",
    base_url="https://api.hanzo.ai/v1"
)

response = client.chat.completions.create(
    model="zen5",
    messages=[{"role": "user", "content": "Explain quantum computing"}]
)
print(response.choices[0].message.content)
```

### TypeScript

```typescript
import OpenAI from 'openai'

const client = new OpenAI({
  apiKey: process.env.HANZO_API_KEY,
  baseURL: 'https://api.hanzo.ai/v1',
})

const completion = await client.chat.completions.create({
  model: 'zen5',
  messages: [{ role: 'user', content: 'Explain quantum computing' }],
})
console.log(completion.choices[0].message.content)
```

## Key Features

<Cards>
  <Card title="Smart Routing" description="Automatic fallbacks between providers when one is down" />
  <Card title="Cost Management" description="Per-key budgets, rate limits, and usage analytics" />
  <Card title="Semantic Caching" description="Cache similar requests to reduce costs up to 90%" />
  <Card title="Guardrails" description="Content filtering, PII detection, and safety controls" />
  <Card title="Observability" description="Full request logging with latency and token analytics" />
  <Card title="Fine-tuning" description="Custom model training via Together AI and Zen LM" />
</Cards>

## Configuration

```yaml title="Gateway Config"
model_list:
  - model_name: "default"
    litellm_params:
      model: "anthropic/zen5"
      api_key: "os.environ/ANTHROPIC_API_KEY"

  - model_name: "default"
    litellm_params:
      model: "openai/gpt-4o"
      api_key: "os.environ/OPENAI_API_KEY"

  - model_name: "fast"
    litellm_params:
      model: "groq/llama-3.1-70b"
      api_key: "os.environ/GROQ_API_KEY"

router_settings:
  routing_strategy: "latency-based-routing"
  fallbacks:
    - default: ["fast"]
```

## API Endpoints

All relative to `https://api.hanzo.ai`.

| Endpoint | Description |
|----------|-------------|
| `POST /v1/chat/completions` | Chat completions (streaming supported) |
| `POST /v1/completions` | Text completions |
| `POST /v1/embeddings` | Text embeddings |
| `POST /v1/images/generations` | Image generation |
| `GET /v1/models` | List available models |

API keys, budgets, and per-key usage are managed in the [Console](https://console.hanzo.ai) — see [API Keys](/docs/api-keys).

## Related

<Cards>
  <Card title="Hanzo Chat" description="AI chat with Zen models, 100+ third-party, MCP and ZAP tools" href="/docs/chat" />
  <Card title="Gateway Service" description="Full API reference and deployment guide" href="/docs/services/gateway" />
  <Card title="MCP" description="Model Context Protocol tools" href="/docs/mcp" />
  <Card title="Hanzo Dev" description="AI coding agent using the gateway" href="/docs/dev" />
</Cards>

{/* domain-index */}

## In this section

<Cards>
  <Card icon={<Sparkles />} title="Agents" href="/docs/agents" description="Build, deploy, and run autonomous agents — personas with a model, tools, guardrails, and shared memory." />
  <Card icon={<Sparkles />} title="MCP" href="/docs/mcp" description="The unified Hanzo MCP — one server that gives Claude Code, Cursor, and any MCP client a single tool surface…" />
  <Card icon={<Sparkles />} title="AI Studio" href="/docs/ai-studio" description="Hanzo AI Studio — build, test, and ship AI apps and pipelines visually." />
  <Card icon={<Sparkles />} title="Prompts" href="/docs/prompts" description="Versioned prompts with labels and history — manage, ship, and roll back prompts through one API." />
  <Card icon={<Sparkles />} title="Hanzo Embeddings" href="/docs/embeddings" description="Generate, store, and search vector embeddings at scale — an OpenAI-compatible embeddings API on the Hanzo…" />
  <Card icon={<Sparkles />} title="GPUs" href="/docs/gpus" description="On-demand H100/H200/A100/L40S GPU compute — metered by the hour, provisioned from the console or API, billed…" />
  <Card icon={<Sparkles />} title="Hanzo Functions" href="/docs/functions" description="Serverless compute platform" />
</Cards>
