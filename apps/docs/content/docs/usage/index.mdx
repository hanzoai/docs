---
title: Usage
description: One usage plane for every AI account you have — rate limits, credits, and spend across Codex, Claude, Hanzo, and 50+ providers.
index: true
---

import { Card, Cards } from '@hanzo/docs-base-ui/components/card';

# Hanzo Usage

**Hanzo Usage** is the universal AI connector and usage plane. Link your AI
provider accounts once, then see unified usage — rate limits, resets, credits,
and spend — everywhere Hanzo runs: the `dev` CLI, Hanzo Desktop, hanzo.app,
hanzo.chat, and [console.hanzo.ai](https://console.hanzo.ai).

## The story

1. **Connect once.** Link OpenAI (Codex), Anthropic (Claude), Hanzo, and 50+
   providers — plus GPU, cloud, and integration accounts (payments, ecommerce,
   marketing, analytics) — using logins you already have. No passwords stored.
2. **See unified usage.** One view across every account: limits, resets,
   credits, and spend, native in every Hanzo surface.
3. **Add your org.** Optionally share verified logins org-wide and route AI
   usage through Hanzo's backend for a comprehensive view, analytics, and
   per-member reporting. A 1% fee applies to routed usage.
4. **Pay OSS AI devs.** Hanzo is an AI-native cloud designed to route spend back
   to the open-source AI ecosystem it runs on.

## How it works

The engine is [`@hanzo/usage`](/docs/usage/quickstart) — a headless TypeScript
core (a fork of the MIT CodexBar app). Each provider is a descriptor with an
ordered pipeline of fetch strategies (`oauth` → `web` → `cli` → `apiToken` →
`localProbe`) that reuse your existing logins — CLI OAuth token files, browser
sessions, API keys. Strategies fall back in priority order and never ask for
passwords. The same core drives the macOS menu-bar app, the CLI, the desktop
and web apps, and the console.

<Cards>
  <Card
    title="Quickstart"
    description="Install @hanzo/usage and read usage in ~15 lines"
    href="/docs/usage/quickstart"
  />
  <Card
    title="Console onboarding"
    description="Connect accounts at console.hanzo.ai → AI Accounts"
    href="/docs/usage/console"
  />
  <Card
    title="Teams & orgs"
    description="Login sharing, verified routing, and per-member analytics"
    href="/docs/usage/teams"
  />
</Cards>
