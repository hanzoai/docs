---
title: Migrate
description: Fifteen vendors, each mapped to the Hanzo capability that answers it — nouns, the call before and after, and what does not carry.
icon: ArrowRightLeft
---

Fifteen guides, one per vendor. Each names the capability that answers it, maps
the vendor's nouns onto ours, shows the same call both ways, and says plainly
what you lose.

Every capability lives at one address — `/v1/<name>` — and the operation counts
below are read from the API document this site is built from.

| From | What it does | Capability |
|---|---|---|
| [Composio](/docs/migrate/composio) | Authenticated third-party tools for agents | `/v1/integrations` (40) · `/v1/connectors` (8) |
| [Portkey](/docs/migrate/portkey) | An LLM gateway with routing and fallbacks | `/v1/ai` (208) · `/v1/router` (30) |
| [Replicate](/docs/migrate/replicate) | Run and host models behind an HTTP call | `/v1/ml` (7) · the model catalog |
| [E2B](/docs/migrate/e2b) | Sandboxes for code an agent wrote | `/v1/sandboxes` (19) · `/v1/exec` (2) |
| [Amazon SQS](/docs/migrate/sqs) | Queues between services | `/v1/mq` (15) · `/v1/pubsub` (8) |
| [Twilio](/docs/migrate/twilio) | Phone numbers, SMS and calls | `/v1/tel` (10) · `/v1/channels` (7) · `/v1/notify` (4) |
| [AgentMail](/docs/migrate/agentmail) | Mailboxes an agent can send and read | `/v1/notify` (4) · `/v1/channels` (7) |
| [ElevenLabs](/docs/migrate/elevenlabs) | Speech synthesis and transcription | `/v1/audio` (5) |
| [Tavily](/docs/migrate/tavily) | Web search and page extraction for agents | `/v1/websearch` (6) · `/v1/crawl` (1) · `/v1/scrape` (1) |
| [GoDaddy](/docs/migrate/godaddy) | Buying and holding domains | `/v1/domain` (7) |
| [Doppler](/docs/migrate/doppler) | Secrets, held for an org | `/v1/kms` (5) |
| [Lago](/docs/migrate/lago) | Metered billing and invoices | `/v1/billing` (45) · `/v1/usage` (11) · `/v1/pricing` (25) |
| [Persona](/docs/migrate/persona) | Identity verification, KYC and KYB | `/v1/compliance` (17) |
| [Doola](/docs/migrate/doola) | Forming a US company | `/v1/company` (22) |
| [Ayrshare](/docs/migrate/ayrshare) | Publishing to social networks | `/v1/social` (13) |

## The key carries the tenant

Every call below authenticates with one header:

```bash
-H "Authorization: Bearer $HANZO_API_KEY"
```

A secret key (`sk-`) resolves to a user; a publishable key (`pk-`) resolves to an
org. There is no org, user or project header to set — the gateway derives all
three from the validated key. Whatever your old vendor did with an account SID, a
workspace id, an entity or a project slug, here it is the key.

See [API keys](/docs/api-keys) for how to mint one, and [Errors](/docs/errors)
for the shape every failure takes.
