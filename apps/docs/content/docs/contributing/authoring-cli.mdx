---
title: User Guide
description: The CLI tool that automates setups and installs components.
---

import { Cards, Card } from '@hanzo/docs-base-ui/components/card'
import { Terminal } from 'lucide-react'

## Installation

Initialize a config for CLI:

```package-install
npx @hanzo/docs-cli
```

You can change the output paths of components in the config.

### Components

Select and install components.

```package-install
npx @hanzo/docs-cli add
```

You can pass component names directly.

```package-install
npx @hanzo/docs-cli add banner files
```

#### How the magic works?

The CLI fetches the latest version of component from the GitHub repository of Hanzo Docs.
When you install a component, it is guaranteed to be up-to-date.

In addition, it also transforms import paths.
Make sure to use the latest version of CLI

> This is highly inspired by Shadcn UI.

### Customize

A simple way to customize Hanzo Docs layouts.

```package-install
npx @hanzo/docs-cli customize
```

### Tree

Generate files tree for Hanzo Docs UI `Files` component, using the `tree` command from your terminal.

```package-install
npx @hanzo/docs-cli tree ./my-dir ./output.tsx
```

You can output MDX files too:

```package-install
npx @hanzo/docs-cli tree ./my-dir ./output.mdx
```

See help for further details:

```package-install
npx @hanzo/docs-cli tree -h
```

#### Example Output

```tsx title="output.tsx"
import { File, Folder, Files } from '@hanzo/docs-base-ui/components/files';

export default (
  <Files>
    <Folder name="app">
      <File name="layout.tsx" />
      <File name="page.tsx" />
      <File name="global.css" />
    </Folder>
    <Folder name="components">
      <File name="button.tsx" />
      <File name="tabs.tsx" />
      <File name="dialog.tsx" />
    </Folder>
    <File name="package.json" />
  </Files>
);
```

{/* domain-index */}

## In this section

<Cards>
  <Card icon={<Terminal />} title="SDKs" href="/docs/sdks" description="Official Hanzo SDKs for every language — Python, TypeScript, Go, Rust, C++, Swift, Kotlin. Two lines per…" />
  <Card icon={<Terminal />} title="ZAP Protocol" href="/docs/zap" description="Zero-Copy App Proto — High-performance Cap'n Proto RPC for AI agent communication. 10-100x faster than MCP…" />
  <Card icon={<Terminal />} title="Hanzo Skills Reference" href="/docs/skills" description="Comprehensive reference documentation for all 49 Hanzo skills — deep-dive guides for AI agents and developers." />
  <Card icon={<Terminal />} title="Usage" href="/docs/usage" description="One usage plane for every AI account you have — rate limits, credits, and spend across Codex, Claude, Hanzo…" />
  <Card icon={<Terminal />} title="Organizations" href="/docs/organizations" description="Multi-org setup, resource scoping, team management, and white-label login for Hanzo organizations." />
</Cards>
