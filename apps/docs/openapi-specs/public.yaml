components:
  schemas:
    Accounts:
      properties:
        account:
          $ref: '#/components/schemas/SourceState'
          description: Account is the state of the caller's own linked-account side.
        hanzo:
          $ref: '#/components/schemas/SourceState'
          description: Hanzo is the state of the org's Hanzo-routed side.
        rows:
          description: |-
            Rows is the two row sets CONCATENATED, never summed — each row says which
            side it came from. A percent is not money and a provider's own spend is not
            a Hanzo charge, so adding them would produce a number that means nothing.
          items:
            $ref: '#/components/schemas/TotalView'
          type: array
      type: object
    AccountsTotal:
      properties:
        accounts:
          description: Accounts is how many linked accounts the total folds.
          type: integer
        completionTokens:
          description: CompletionTokens is the total completion-token count.
          type: integer
        costCents:
          description: CostCents is the total cost in cents.
          type: integer
        promptTokens:
          description: PromptTokens is the total prompt-token count.
          type: integer
        requests:
          description: Requests is the total request count the gateway routed.
          type: integer
        totalTokens:
          description: TotalTokens is the total token count.
          type: integer
      type: object
    ActivityPoint:
      properties:
        costCents:
          description: |-
            CostCents is the day's spend in whole US cents. A series is only ever returned
            for a subject the caller is authorized to see, so this is never withheld: 0 means
            no spend that day.
          type: integer
        day:
          description: Day is the UTC calendar day this point covers, "2006-01-02".
          type: string
        requests:
          description: |-
            Requests is the subject's request count on this day. 0 is a real, quiet day: the
            series is gap-filled, so every day in the range is present whether or not
            anything happened.
          type: integer
        tokens:
          description: |-
            Tokens is prompt+completion tokens on this day — normally the heatmap's
            intensity, scaled against ActivityTotals.MaxTokens.
          type: integer
      type: object
    ActivityRow:
      properties:
        action:
          type: string
        actor:
          type: string
        at:
          type: string
        detail:
          type: string
        id:
          type: integer
        key:
          type: string
      type: object
    ActivityTotals:
      properties:
        activeDays:
          description: |-
            ActiveDays counts the days with any usage at all — the streak/consistency number.
            Compare it against len(days) for the share of days the subject showed up.
          type: integer
        costCents:
          description: CostCents is the window's spend in whole US cents, the sum
            of Days[].CostCents.
          type: integer
        maxRequests:
          description: |-
            MaxRequests is the same ceiling for a request-based heatmap — the busiest single
            day's request count, 0 for an idle window.
          type: integer
        maxTokens:
          description: |-
            MaxTokens is the busiest single day's token count: the ceiling to normalize a
            token heatmap against, so the darkest cell is that day. 0 for an idle window,
            which a client must not divide by.
          type: integer
        requests:
          description: Requests is the sum of Days[].Requests over the whole window.
          type: integer
        tokens:
          description: Tokens is the sum of Days[].Tokens over the whole window.
          type: integer
      type: object
    ActivityView:
      properties:
        available:
          description: |-
            Available is false when nothing could be read: the warehouse is not connected,
            the rollup is not ready, or the subject is one the ledger cannot attribute (see
            Note). Days is then empty because there is no answer, not because there was no
            activity.
          type: boolean
        days:
          description: |-
            Days is the gap-filled series, one point per calendar day from From up to (not
            including) To, in ascending order, zero-valued days included. Always a list,
            never null.
          items:
            $ref: '#/components/schemas/ActivityPoint'
          type: array
        from:
          description: From is the first day in Days, "2006-01-02" inclusive.
          type: string
        id:
          description: |-
            ID is the subject the server actually read, after resolving "me"/empty to the
            caller and bounding it to what they may see — a ledger "owner/name" for a user,
            an org id for an org. Echoed so a client can confirm whose series it holds.
          type: string
        note:
          description: |-
            Note explains an empty-but-not-broken answer in plain words — today only
            subject=project, which the usage ledger records no column for. Present only when
            there is something to say; show it instead of an empty chart.
          type: string
        source:
          description: |-
            Source names the table the series was aggregated from (the derived daily rollup,
            hanzo.usage_rollup_daily).
          type: string
        subject:
          description: 'Subject echoes what the series is about: user|org|project.'
          type: string
        to:
          description: |-
            To is the EXCLUSIVE upper bound, "2006-01-02" — the day AFTER the last point in
            Days. A request for to=2026-03-31 answers to=2026-04-01 with 2026-03-31 last.
          type: string
        totals:
          $ref: '#/components/schemas/ActivityTotals'
          description: |-
            Totals are the window's sums plus the busiest-day ceilings a heatmap scales
            against. Derived from Days — nothing here is read separately.
      type: object
    Allowance:
      properties:
        limit:
          description: calls the plan allows per period; 0 = unbounded
          type: integer
        plan:
          description: the tier the limit came from
          type: string
        resets:
          description: unix seconds; when the count starts again
          type: integer
        spent:
          description: the subject is at the limit
          type: boolean
        used:
          type: integer
      type: object
    Answer:
      properties:
        cold:
          description: |-
            Cold reports that this request paid to PREPARE the revision — the tree
            write, the dependency fetch and the language server's first index. It is
            the billed event, surfaced so a caller can see what it was charged for.
          type: boolean
        completions:
          items:
            $ref: '#/components/schemas/Completion'
          type: array
        diagnostics:
          items:
            $ref: '#/components/schemas/Diagnostic'
          type: array
        hover:
          type: string
        lang:
          type: string
        locations:
          items:
            $ref: '#/components/schemas/Location'
          type: array
        op:
          type: string
        path:
          type: string
        repo:
          type: string
        rev:
          type: string
        symbols:
          items:
            $ref: '#/components/schemas/Symbol'
          type: array
      type: object
    AppView:
      properties:
        app:
          description: service / CR name, e.g. iam
          type: string
        cluster:
          type: string
        declaredTag:
          type: string
        drift:
          $ref: '#/components/schemas/Verdict'
        endpoints:
          items:
            type: string
          type: array
        env:
          description: main|test|dev
          type: string
        health:
          description: green|yellow|red|"" (unknown)
          type: string
        id:
          description: <org>/<app>/<env>, e.g. hanzoai/iam/main
          type: string
        latestTag:
          type: string
        namespace:
          type: string
        org:
          description: image namespace, e.g. hanzoai
          type: string
        phase:
          description: operator status.phase (Running/…)
          type: string
        registry:
          type: string
        repo:
          description: owner/repo, e.g. hanzoai/iam
          type: string
        role:
          description: operator spec.role (sql|kv|generic|ingress|…) or "" — the one
            declared class field
          type: string
        runningTag:
          type: string
      type: object
    AskAnswer:
      properties:
        answer:
          type: string
        citations:
          items:
            $ref: '#/components/schemas/Citation'
          type: array
        degraded:
          type: boolean
        question:
          type: string
      type: object
    AuthoredPlugin:
      properties:
        createdAt:
          description: CreatedAt is when the plugin was last built, Unix seconds.
          type: integer
        id:
          description: ID is the plugin's id within the org, and the id a delete addresses.
          type: string
        name:
          description: 'Name is the plugin''s name: one lowercase path segment, the
            id it runs by.'
          type: string
        org:
          description: Org is the org that built the plugin — the validated caller's.
          type: string
        provider:
          description: |-
            Provider is the connectors provider whose credential this plugin uses at
            run time. Absent for a plugin that needs none. The credential itself is
            never here — it stays under KMS custody in the connectors plane.
          type: string
        source:
          description: Source is the TypeScript as authored (or as generated from
            a spec).
          type: string
      type: object
    Backend:
      properties:
        url:
          description: URL is the upstream server, http(s)://host[:port].
          type: string
        weight:
          description: Weight is this member's share of the round-robin; must be >=
            0.
          type: integer
      type: object
    BackendStatus:
      properties:
        error:
          type: string
        hits:
          type: integer
        name:
          type: string
        status:
          type: string
        took_ms:
          type: integer
      type: object
    Blob:
      properties:
        data:
          contentEncoding: base64
          type: string
        dir:
          type: boolean
        entries:
          items:
            type: string
          type: array
        path:
          type: string
      type: object
    Board:
      properties:
        byModel:
          description: the top models by spend
          items:
            $ref: '#/components/schemas/ModelStat'
          type: array
        latency:
          $ref: '#/components/schemas/LatencyStat'
          description: overall latency percentiles from the GenAI spans
        other:
          $ref: '#/components/schemas/ModelStat'
          description: the long tail beyond the top models, folded into one row
        range:
          $ref: '#/components/schemas/BoardRange'
          description: the window they were computed over, echoed back
        scope:
          $ref: '#/components/schemas/BoardScope'
          description: whose numbers these are
        series:
          description: one gap-filled bucket per interval, so a chart never breaks
          items:
            $ref: '#/components/schemas/BoardPoint'
          type: array
        totals:
          $ref: '#/components/schemas/BoardTotals'
          description: the window's headline numbers
      type: object
    BoardPoint:
      properties:
        costCents:
          description: what this bucket cost, in cents
          type: integer
        errors:
          description: calls in this bucket that did not succeed
          type: integer
        generations:
          description: model calls in this bucket
          type: integer
        t:
          description: RFC3339 (UTC) bucket start
          type: string
        totalTokens:
          description: tokens in this bucket
          type: integer
      type: object
    BoardRange:
      properties:
        end:
          description: RFC3339 (UTC)
          type: string
        interval:
          description: hour | day
          type: string
        range:
          description: echoed label (24h | 7d | 30d | custom)
          type: string
        start:
          description: RFC3339 (UTC)
          type: string
      type: object
    BoardScope:
      properties:
        allOrgs:
          description: true when a platform admin is seeing every org at once
          type: boolean
        org:
          description: the org the board covers; "" when it covers all of them
          type: string
        project:
          description: the sub-scope within the org; "" is the whole org
          type: string
      type: object
    BoardTotals:
      properties:
        completionTokens:
          description: tokens the models answered with
          type: integer
        costCents:
          description: what the window cost, in cents
          type: integer
        errors:
          description: calls that did not succeed
          type: integer
        generations:
          description: how many model calls the window holds
          type: integer
        models:
          description: how many distinct models were called
          type: integer
        promptTokens:
          description: tokens sent to the models
          type: integer
        successRate:
          description: share of calls that succeeded, 0..1
          type: number
        totalTokens:
          description: prompt plus completion
          type: integer
        users:
          description: how many distinct users called them
          type: integer
      type: object
    Breakdown:
      properties:
        available:
          description: Available is false when the product-event table could not be
            read.
          type: boolean
        items:
          description: Items is the ranked buckets, most pageviews first. Empty rather
            than absent.
          items:
            $ref: '#/components/schemas/BreakdownRow'
          type: array
        reason:
          description: Reason says why the lens is unavailable. Omitted when it is
            available.
          type: string
        source:
          description: Source is the warehouse table the lens read.
          type: string
      type: object
    BreakdownRow:
      properties:
        key:
          description: |-
            Key is the bucket: a requested path, a referrer domain ("(direct)" for none or
            a same-origin one), or a utm_source ("(none)" when absent).
          type: string
        pageviews:
          description: Pageviews is how many $pageview events fell in this bucket.
          type: integer
        pct:
          description: |-
            Pct is this bucket's share of ALL in-window pageviews, 0..100, one decimal —
            not of the returned rows, so a top-N shows the long tail honestly.
          type: number
        visitors:
          description: Visitors is how many distinct people they came from.
          type: integer
      type: object
    Call:
      properties:
        agent:
          type: string
        from:
          type: string
        id:
          type: string
        org:
          type: string
        status:
          description: queued | ringing | answered | completed | failed
          type: string
        to:
          type: string
      type: object
    CaptureBatch:
      properties:
        batch:
          items:
            $ref: '#/components/schemas/CaptureEvent'
          type: array
        events:
          items:
            $ref: '#/components/schemas/CaptureEvent'
          type: array
      type: object
    CaptureEvent:
      properties:
        anonymousId:
          type: string
        channel:
          type: string
        clip:
          $ref: '#/components/schemas/ClipBody'
        currency:
          type: string
        distinctId:
          type: string
        environment:
          type: string
        error:
          $ref: '#/components/schemas/Exception'
        event:
          type: string
        groupId:
          type: string
        groupType:
          type: string
        kind:
          type: string
        level:
          type: string
        library:
          type: string
        libraryVersion:
          type: string
        log:
          $ref: '#/components/schemas/LogBody'
        messageId:
          type: string
        metric:
          $ref: '#/components/schemas/MetricBody'
        path:
          type: string
        personId:
          type: string
        product:
          type: string
        productId:
          type: string
        properties:
          additionalProperties: {}
          type: object
        quantity:
          type: integer
        refCode:
          type: string
        referrer:
          type: string
        release:
          type: string
        resource:
          type: string
        revenue:
          type: number
        service:
          type: string
        sessionId:
          type: string
        signupWeek:
          type: string
        site:
          type: string
        span:
          $ref: '#/components/schemas/SpanBody'
        spanId:
          type: string
        timestamp:
          type: string
        traceId:
          type: string
        type:
          type: string
        url:
          type: string
        utm:
          $ref: '#/components/schemas/UTM'
      type: object
    CaptureResult:
      properties:
        accepted:
          type: integer
        dropped:
          type: integer
      type: object
    Cart:
      properties:
        createdAt:
          description: CreatedAt is when the cart was opened, RFC3339.
          type: string
        currency:
          description: Currency is the ISO 4217 code every amount below is denominated
            in.
          type: string
        discountCents:
          description: DiscountCents is what coupons and promotions took off, in whole
            cents.
          type: integer
        email:
          description: Email is the shopper's address, when the cart carries one.
          type: string
        id:
          description: |-
            ID is the cart's id — what every other cart op addresses it by, and what a
            storefront persists against the browser session.
          type: string
        items:
          description: Items are the cart's lines, in the order they were added.
          items:
            $ref: '#/components/schemas/CartItem'
          type: array
        lineTotalCents:
          description: LineTotalCents is the sum of the lines before any discount,
            in whole cents.
          type: integer
        order:
          description: |-
            Order is the order this cart became, once checkout completed it. Empty
            until then, and its presence is what makes a cart final.
          type: string
        shippingCents:
          description: |-
            ShippingCents is the shipping charge, in whole cents. It stays zero until a
            shipping option is priced at checkout.
          type: integer
        status:
          description: |-
            Status is "active" for a cart still being filled, "ordered" once checkout
            turned it into an order, and "discarded" when the shopper abandoned it.
          type: string
        store:
          description: Store is the storefront the cart is being filled on.
          type: string
        subtotalCents:
          description: SubtotalCents is LineTotalCents less DiscountCents, in whole
            cents.
          type: integer
        taxCents:
          description: |-
            TaxCents is the sales tax, in whole cents. It stays zero until checkout
            resolves the shopper's tax region.
          type: integer
        totalCents:
          description: |-
            TotalCents is what the shopper pays: subtotal plus shipping plus tax, in
            whole cents.
          type: integer
        updatedAt:
          description: UpdatedAt is when the cart was last amended, RFC3339.
          type: string
        user:
          description: User is the signed-in shopper this cart belongs to, empty for
            a guest cart.
          type: string
      type: object
    CartItem:
      properties:
        free:
          description: |-
            Free reports a line that costs nothing because a coupon or a promotion made
            it so, rather than because its price is zero.
          type: boolean
        id:
          description: |-
            ID is the line's identity — the variant id when the line is a variant,
            otherwise the product id. It is what a subsequent set call addresses.
          type: string
        kind:
          description: |-
            Kind is "variant" when this line is a specific sellable variant and
            "product" when it is the product itself.
          type: string
        name:
          description: |-
            Name is the item's display name, cached onto the line when it was added so
            a cart renders without a second read.
          type: string
        priceCents:
          description: |-
            PriceCents is the unit price in whole cents, cached at the moment the line
            was added. The line's contribution to the cart is this times Quantity.
          type: integer
        quantity:
          description: Quantity is how many units of this item the cart holds.
          type: integer
        sku:
          description: |-
            SKU is the line's stock-keeping unit — the variant's when it has one,
            otherwise the product's. Empty when neither carries one.
          type: string
      type: object
    CartItemSet:
      properties:
        id:
          description: ID is the cart to amend, from the path.
          type: string
        product:
          description: |-
            Product names the catalog product to set, by its id or its URL slug. Give
            this or Variant, never both; a request naming neither is refused.
          type: string
        quantity:
          description: |-
            Quantity is how many of that item the cart should hold AFTER this call — it
            is the resulting count, not a delta, so sending 3 twice leaves 3 and not 6.
            ZERO REMOVES the line, which is the only way to take an item out.
          type: integer
        variant:
          description: |-
            Variant names the specific sellable variant to set, by its id or its SKU.
            Prefer it over Product for anything sold in sizes, colours or tiers — the
            price and the stock are the variant's, not the product's.
          type: string
      type: object
    CartOpen:
      properties:
        currency:
          description: |-
            Currency is the ISO 4217 code the cart is priced in, lower-cased. Empty
            means usd.
          type: string
        email:
          description: |-
            Email is the shopper's address, for a cart that belongs to someone who has
            not signed in. It is what a guest checkout and an abandoned-cart follow-up
            key on. Empty is fine.
          type: string
        store:
          description: |-
            Store is the storefront this cart is being filled on. Empty uses the org's
            default store, which is what a single-storefront merchant always wants.
          type: string
        user:
          description: |-
            User is the id of the signed-in shopper this cart belongs to, when there is
            one. Empty means a guest cart identified only by its own id.
          type: string
      type: object
    Catalog:
      properties:
        connectorCount:
          type: integer
        connectors:
          items:
            $ref: '#/components/schemas/ConnectorMetadata'
          type: array
      type: object
    CatalogEntry:
      properties:
        labels:
          description: Labels is the starter's second suggested taxonomy, same treatment
            as Tags.
          items:
            type: string
          type: array
        name:
          description: |-
            Name is the starter's suggested handle. It is NOT taken in your org: the
            catalog is shared reference content, so this name is free until you import it,
            and posting it under a name you already use appends a version to yours.
          type: string
        prompt:
          description: |-
            Prompt is the starter's full template body, ready to POST as-is. Entries too
            large to create (over 64 KiB) are dropped from this list rather than offered.
          type: string
        tags:
          description: |-
            Tags is the starter's suggested taxonomy, carried through unchanged if you
            import it.
          items:
            type: string
          type: array
        type:
          description: |-
            Type labels the template's kind, defaulted to "text" for entries that declare
            none.
          type: string
      type: object
    CategorySpend:
      properties:
        category:
          description: |-
            Category is the bucket the ledger's own tag mapped to. An untagged or
            unrecognised line gets its own honest bucket rather than being folded away.
          type: string
        cents:
          description: Cents is what the org spent in that bucket over the window,
            in US cents.
          type: integer
        count:
          description: Count is how many ledger lines rolled up into it.
          type: integer
      type: object
    Citation:
      properties:
        endLine:
          type: integer
        file:
          type: string
        line:
          type: integer
        repo:
          type: string
        symbol:
          type: string
      type: object
    ClipBody:
      properties:
        bytes:
          type: integer
        duration:
          type: integer
        object:
          type: string
      type: object
    CodeFile:
      properties:
        id:
          description: |-
            ID is the file's path RELATIVE to its session's artifact directory, which is
            also how it is fetched: GET /v1/exec/download/{session}/{id}.
          type: string
        name:
          description: |-
            Name is the display name. On an ANSWER it carries the `{session}/{id}`
            identifier whole, because the client matches on that prefix.
          type: string
        session_id:
          description: |-
            SessionID is the other accepted spelling of the same fact on the way IN. Both
            are read; whichever is set wins.
          type: string
        storage_session_id:
          description: |-
            StorageSessionID names the session holding the bytes, and is the spelling the
            answer always uses.
          type: string
      type: object
    CodeResult:
      properties:
        files:
          description: |-
            Files are what this run CREATED OR CHANGED, decided by mtime against a marker
            taken before the program started — so it is the run's output, not a listing of
            the directory. Fetch each from GET /v1/exec/download/{session}/{id}.
          items:
            $ref: '#/components/schemas/CodeFile'
          type: array
        session_id:
          description: |-
            SessionID is the sandbox this run used — the one that was passed in, or the
            fresh one that was leased. Pass it to the next run to keep the filesystem.
          type: string
        stderr:
          description: |-
            Stderr is what the program wrote to standard error, INCLUDING a compiler's
            diagnostics and the trace of a program that exited non-zero. Its presence is
            not a failed call.
          type: string
        stdout:
          description: Stdout is what the program wrote to standard output.
          type: string
      type: object
    CodeRun:
      properties:
        args:
          description: |-
            Args become the PROGRAM's argv, never the compiler's. For the compiled
            languages the toolchain builds first and these are passed to the binary it
            produced.
          items:
            type: string
          type: array
        code:
          description: |-
            Code is the WHOLE program, not a fragment: it is written to a single file and
            that file is what runs, so a compiled language needs its entry point and an
            interpreted one runs top to bottom.
          type: string
        files:
          description: |-
            Files are inputs the host already put in some session. Each names the session
            its bytes live in, which is usually — and ideally — the session this run wants.
          items:
            $ref: '#/components/schemas/CodeFile'
          type: array
        lang:
          description: |-
            Lang selects the toolchain, and with it the filename the code is written to
            and the line that runs it: py, js, ts, bash, r, php, go, rs, c, cpp, java, d,
            f90. Anything else is refused rather than guessed at — a run in the wrong
            language fails somewhere deep in a compiler, which reads as an outage.
          type: string
        runtime_session_hint:
          description: |-
            RuntimeSessionHint is the stateful-session hint. It is carried so a client
            that sends it is not silently misread, and it selects nothing here: every
            session in this implementation is already a warm sandbox, so there is no
            second kind of runtime for a hint to choose between.
          type: string
        session_id:
          description: |-
            SessionID continues an EXISTING sandbox, which is what makes runs stateful:
            the same filesystem, so one run's output file is the next run's input. Empty
            leases a fresh sandbox and the id it got comes back on the result.
          type: string
        user_id:
          description: |-
            UserID attributes the run inside the caller's org. It is a label, never a
            tenant: the org is resolved from the validated principal and a value here
            cannot widen what the run may reach.
          type: string
      required:
      - lang
      - code
      type: object
    CodingStartIn:
      properties:
        after:
          description: |-
            After names a previous run's session, and starts this one from where that
            one stopped instead of from the repository's default. It is how a follow-up
            instruction — "now add tests for it" — builds on work already done rather
            than beginning again on a fresh clone.

            It sets the base and nothing else, so this run still writes its OWN branch.
            One run, one branch: a run that wrote back onto an earlier run's branch
            would break the rule the forge's ref policy is built on, and would leave
            two turns of work with one name to review.

            A caller who already knows the branch may pass Base directly; this exists
            because the branch is derived from a session id and nobody should have to
            know how. Base wins if both are given.
          type: string
        agentRef:
          description: |-
            AgentRef names a configured agent to run as, which is how an org pins a
            harness, a model and a prompt to a name. Empty runs the default agent.
          type: string
        base:
          description: |-
            Base is the branch to start from. Empty takes the repository's default. The
            run never writes here — it writes the agent branch it answers with.
          type: string
        desktop:
          description: |-
            Desktop asks for a run with a SCREEN — an image carrying an X server — for a
            task that has to drive a browser or another windowed program. False, the
            default, is a headless checkout, which is what writing code needs.
          type: boolean
        project:
          description: |-
            Project scopes the run to one board's work when the org keeps more than one.
            Empty is the org's default.
          type: string
        prompt:
          description: |-
            Prompt is the task, in the words you would use with a colleague who has the
            checkout open. It is the whole instruction: there is no second field for
            context, and a prompt that names files and the outcome it wants gets a run
            that does not have to guess either.
          type: string
        replyChannel:
          description: |-
            ReplyChannel / ReplyThread are WHERE THE RUN NARRATES ITSELF, when the door
            that started it has somewhere for it to talk. Empty means nobody is
            listening and the run simply does not narrate — which is the app door's
            case, because /v1/agents/coding hands back a session id and the session stream is
            a better progress feed than any message could be.

            It is an ADDRESS and not a token: the engine says "put this text there",
            and the process that owns the workspace's bot credential is the one that
            actually posts. So a run reports into a Slack thread without the engine
            ever holding the token that could post anywhere else in that workspace.
          type: string
        replyThread:
          type: string
        repo:
          description: |-
            Repo is what to work on, as `owner/name` in the caller's own org. The engine
            resolves the clone URL and the push credential from the org itself, so this
            says WHICH repository and never how to reach it.
          type: string
        targetId:
          description: |-
            TargetID routes the run to a registered machine the org has claimed instead
            of to a sandbox in our cluster. Empty runs it here, which is the usual case.
          type: string
        timeoutSeconds:
          description: |-
            TimeoutSeconds bounds the whole run. Unset takes the default budget; a run
            that hits the bound is stopped and reports what it had done by then.
          type: integer
        tool:
          description: |-
            Tool is which harness runs the prompt — dev | claude | codex | python |
            node — and Desktop is whether the run needs a screen. Both are empty by
            default, which is `dev` with no screen, and that default is what every
            caller gets until it says otherwise.

            They are two fields because they are two questions. The harness decides
            what argv starts; the screen decides which image carries an X server. A
            caller may want claude WITH a browser it can see, and a single enum would
            have made that combination unsayable.
          type: string
      type: object
    CodingStarted:
      properties:
        branch:
          description: |-
            Branch is the ref the run will push its work to, and the ONLY ref it is
            permitted to write. It exists before the work does, so it is safe to tell
            somebody where to look while the run is still going.
          type: string
        repo:
          description: |-
            Repo is the repository the run was admitted against, echoed back as the
            engine resolved it.
          type: string
        routed:
          description: |-
            Routed says the run went to one of the org's own registered machines rather
            than to a sandbox in our cluster. False is the ordinary case.
          type: boolean
        sessionId:
          description: |-
            SessionID is the run's handle: its durable record, and the id its live
            progress streams under at /v1/agents/sessions/{sessionId}/stream. Every
            later question about this run is asked with it.
          type: string
        targetId:
          description: TargetID names that machine when Routed is true, and is empty
            otherwise.
          type: string
      type: object
    CollectOut:
      properties:
        balanceUsedCents:
          description: BalanceUsedCents is how much was covered by prepaid balance.
          type: integer
        cardChargedCents:
          description: CardChargedCents is how much was charged to the card on file.
          type: integer
        creditUsedCents:
          description: CreditUsedCents is how much was covered by credit grants.
          type: integer
        invoice:
          $ref: '#/components/schemas/InvoiceOut'
          description: |-
            Invoice is the invoice AFTER the attempt — its status is the authority on
            what happened, not this struct's other fields.
        paid:
          description: |-
            Paid reports whether the invoice is now settled in full. A false here with
            no error is a DECLINE: the invoice stays open and may be collected again.
          type: boolean
        processorRef:
          description: |-
            ProcessorRef is the processor's reference for any card charge — the field
            that proves money moved at the gateway rather than only in our ledger.
          type: string
        reason:
          description: Reason explains a decline or partial collection. Empty on success.
          type: string
      type: object
    CommerceOverview:
      properties:
        aov:
          description: |-
            AOV is average order value — Revenue/Orders, rounded to two places. Zero when
            there were no orders.
          type: number
        available:
          description: |-
            Available is false when the product-event table could not be read — the lens is
            reported missing rather than as zeros that look like no sales.
          type: boolean
        orders:
          description: Orders is how many order_completed events landed in the window.
          type: integer
        reason:
          description: Reason says why the lens is unavailable. Omitted when it is
            available.
          type: string
        revenue:
          description: Revenue is the total those orders carried, in the events' own
            currency unit.
          type: number
        source:
          description: Source is the warehouse table the lens read.
          type: string
      type: object
    Completion:
      properties:
        detail:
          type: string
        kind:
          type: integer
        label:
          type: string
      type: object
    Config:
      properties:
        max_age:
          description: MaxAge caps message age, e.g. "24h" or "7d"; "0" (default)
            is unlimited.
          type: string
        max_bytes:
          description: MaxBytes caps the stream's total stored bytes; -1 (default)
            is unlimited.
          type: integer
        max_msg_size:
          description: MaxMsgSize caps one message's size in bytes; -1 (default) is
            the broker's limit.
          type: integer
        max_msgs:
          description: MaxMsgs caps the number of stored messages; -1 (default) is
            unlimited.
          type: integer
        name:
          description: Name is the stream name, unique within the org (alphanumeric,
            hyphens, underscores).
          type: string
        num_replicas:
          description: Replicas is the number of stream replicas (1–5); this plane
            runs 1.
          type: integer
        retention:
          description: 'Retention is the retention policy: limits (default), interest,
            or workqueue.'
          type: string
        storage:
          description: 'Storage is the storage backend: file (default) or memory.'
          type: string
        subjects:
          description: 'Subjects are the org-relative subjects bound to this stream
            (wildcards supported). Default: the stream name.'
          items:
            type: string
          type: array
      type: object
    ConnectorAction:
      properties:
        description:
          type: string
        displayName:
          type: string
        name:
          type: string
        props:
          items:
            $ref: '#/components/schemas/PropSpec'
          type: array
      type: object
    ConnectorAuth:
      properties:
        required:
          type: boolean
        type:
          type: string
      type: object
    ConnectorMetadata:
      properties:
        actions:
          items:
            $ref: '#/components/schemas/ConnectorAction'
          type: array
        auth:
          $ref: '#/components/schemas/ConnectorAuth'
        categories:
          items:
            type: string
          type: array
        description:
          type: string
        displayName:
          type: string
        logoUrl:
          type: string
        name:
          type: string
        triggers:
          items:
            $ref: '#/components/schemas/ConnectorTrigger'
          type: array
        version:
          type: string
      type: object
    ConnectorTrigger:
      properties:
        description:
          type: string
        displayName:
          type: string
        name:
          type: string
        props:
          items:
            $ref: '#/components/schemas/PropSpec'
          type: array
        strategy:
          type: string
      type: object
    Consumer:
      properties:
        ack_floor:
          $ref: '#/components/schemas/Sequences'
          description: AckFloor is the highest contiguously acknowledged sequence
            pair.
        config:
          $ref: '#/components/schemas/Durable'
          description: Config is the consumer's configuration.
        created:
          description: Created is when the consumer was created.
          format: date-time
          type: string
        delivered:
          $ref: '#/components/schemas/Sequences'
          description: Delivered is the highest delivered sequence pair.
        name:
          description: Name is the consumer name.
          type: string
        num_ack_pending:
          description: AckPending is the number of delivered, not yet acknowledged
            messages.
          type: integer
        num_pending:
          description: Pending is the number of messages yet to be delivered.
          type: integer
        num_redelivered:
          description: Redelivered is the number of messages currently being redelivered.
          type: integer
        num_waiting:
          description: Waiting is the number of pull requests waiting for messages.
          type: integer
        stream_name:
          description: Stream is the stream this consumer reads.
          type: string
      type: object
    Contacts:
      properties:
        admin:
          $ref: '#/components/schemas/Registrant'
          description: who administers it
        billing:
          $ref: '#/components/schemas/Registrant'
          description: who is reached about payment
        registrant:
          $ref: '#/components/schemas/Registrant'
          description: who owns the domain
        tech:
          $ref: '#/components/schemas/Registrant'
          description: who is reached about technical matters
      type: object
    ContextBundle:
      properties:
        budgetTokens:
          type: integer
        query:
          type: string
        repo:
          type: string
        spans:
          items:
            $ref: '#/components/schemas/Span'
          type: array
        usedTokens:
          type: integer
      type: object
    D1Query:
      properties:
        params:
          items: {}
          type: array
        sql:
          type: string
      type: object
    DefRow:
      properties:
        definition: {}
        key:
          type: string
        updated_at:
          type: string
        updated_by:
          type: string
        version:
          type: integer
      type: object
    Delivery:
      properties:
        data:
          description: Data is the payload, base64-encoded.
          type: string
        headers:
          additionalProperties:
            items:
              type: string
            type: array
          description: Headers are the message headers, when any were published.
          type: object
        num_delivered:
          description: Delivered is how many times a consumer has been handed this
            message (pulls only).
          type: integer
        num_pending:
          description: Remaining is how many messages follow this one for the consumer
            (pulls only).
          type: integer
        sequence:
          description: Sequence is the message's stream sequence.
          type: integer
        subject:
          description: Subject is the org-relative subject the message was stored
            under.
          type: string
        timestamp:
          description: Timestamp is when the broker stored the message.
          format: date-time
          type: string
      type: object
    DeliveryRow:
      properties:
        attempt:
          type: integer
        created:
          type: string
        delivery:
          type: string
        durationMs:
          type: integer
        endpoint:
          type: string
        error:
          type: string
        httpStatus:
          type: integer
        status:
          type: string
        subject:
          type: string
      type: object
    DestinationField:
      properties:
        example:
          description: a sample value of the right shape ("G-XXXXXXX"), when one helps
          type: string
        key:
          description: the camelCase key on both the connect body and the stored config
          type: string
        label:
          description: human label for the console card's input
          type: string
        required:
          description: when true, a connect that leaves it empty is refused 400
          type: boolean
      type: object
    DestinationStatus:
      properties:
        account:
          description: |-
            Account is the operator's own label for the connected account, as supplied on
            connect. Absent when unset.
          type: string
        category:
          description: 'groups the card: Analytics | Advertising'
          type: string
        config:
          additionalProperties:
            type: string
          description: |-
            Config is the org's stored NON-SECRET configuration — the measurement/pixel
            ids keyed by DestinationField.Key. A secret is never in here; secrets live in
            KMS and only their names are published, in Secrets.
          type: object
        connected:
          description: |-
            Connected is true when this org has a stored row for the platform — it has
            been configured here at least once. It says nothing about whether a
            credential still resolves; that is Live.
          type: boolean
        enabled:
          description: |-
            Enabled is whether the fan-out forwards to this destination. False on a
            destination that is connected but paused, and on one never connected.
          type: boolean
        fields:
          description: |-
            Fields are the non-secret inputs this platform needs, which the console card
            renders and the connect body fills.
          items:
            $ref: '#/components/schemas/DestinationField'
          type: array
        live:
          description: |-
            Live is whether a credential resolves RIGHT NOW: a KMS-sealed secret for this
            org, else the integrations connection named by the platform's Fallback, else
            no credential needed at all (a public-ingest sink like Umami). False on a
            connected destination whose secret has gone missing — Connected && !Live is
            exactly the "reconnect me" state.
          type: boolean
        name:
          description: the platform's display name ("Google Analytics 4")
          type: string
        platform:
          description: the platform slug, and the path segment every route addresses
            it by
          type: string
        secrets:
          description: |-
            Secrets are the KMS secret NAMES this platform custodies for the org — names
            only, never values. The connect body accepts each under its camelCase form.
          items:
            type: string
          type: array
      type: object
    Diagnostic:
      properties:
        code:
          type: object
        message:
          type: string
        range:
          $ref: '#/components/schemas/Range'
        severity:
          type: integer
        source:
          type: string
      type: object
    DriftFlag:
      properties:
        kind:
          type: string
        message:
          type: string
        severity:
          type: string
      type: object
    Durable:
      properties:
        ack_policy:
          description: 'Ack is the acknowledgment policy: explicit (default), all,
            or none.'
          type: string
        ack_wait:
          description: AckWait is how long the broker waits for an ack before redelivering,
            e.g. "30s" (default).
          type: string
        deliver_policy:
          description: 'Deliver is where delivery starts: all (default), last, new,
            by_start_sequence, by_start_time, or last_per_subject.'
          type: string
        description:
          description: Description says what this consumer is for.
          type: string
        durable_name:
          description: Name is the durable consumer name (alphanumeric, hyphens, underscores).
          type: string
        filter_subject:
          description: Filter delivers only messages on this org-relative subject
            (wildcards supported).
          type: string
        max_ack_pending:
          description: MaxAckPending caps unacknowledged messages in flight (default
            1000).
          type: integer
        max_deliver:
          description: MaxDeliver caps delivery attempts per message; -1 (default)
            is unlimited.
          type: integer
        opt_start_seq:
          description: StartSeq is the starting sequence for deliver_policy by_start_sequence.
          type: integer
        opt_start_time:
          description: StartTime is the starting instant for deliver_policy by_start_time.
          format: date-time
          type: string
        replay_policy:
          description: 'Replay is the replay pacing: instant (default) or original.'
          type: string
      type: object
    EndIn:
      properties:
        id:
          description: ID is the sandbox whose lease ends, from an earlier lease.
          type: string
        purge:
          description: |-
            Purge deletes the project's DISK as well. It is opt-in because the disk holds
            the only copy of the checkout: ending a lease is cheap and reversible,
            deleting someone's uncommitted work is neither.
          type: boolean
      type: object
    Endpoint:
      properties:
        created:
          type: string
        deliveries7d:
          description: |-
            Deliveries7d / Failures7d are cheap usage counters computed from the delivery log
            over usageWindow (not stored columns) and populated ONLY on list/get. They are 0
            when there is no delivery history — never omitempty, so the console always sees them.
          type: integer
        description:
          type: string
        events:
          items:
            type: string
          type: array
        failures7d:
          type: integer
        id:
          type: string
        org:
          type: string
        secret:
          type: string
        status:
          type: string
        updated:
          type: string
        url:
          type: string
      type: object
    Entry:
      properties:
        archetype:
          type: string
        description:
          type: string
        forkable:
          description: |-
            Forkable is NOT omitempty: false is an answer here, not a missing field.
            Omitted, a client could not tell "you cannot fork this" from "nobody said".
          type: boolean
        id:
          type: string
        kind:
          description: repo | site
          type: string
        language:
          type: string
        license:
          type: string
        name:
          type: string
        note:
          description: |-
            Note is why a row is NOT in the published catalog, set by the admission gate
            (gate.go) on the sites it holds back. It is the difference between a demo
            that silently vanished from the public lens and one whose owner can read the
            reason and fix it. A published row never carries one.
          type: string
        org:
          description: hanzo | lux | zoo
          type: string
        origin:
          description: |-
            Origin is WHAT THIS IS TO YOU: template | community | third-party | product
            (origin.go owns the four nouns and derives them). Not omitempty, for the
            same reason Forkable is not: every row has an answer, and a missing one is
            exactly the flattening this field exists to end.
          type: string
        repo:
          description: source
          type: string
        scope:
          description: |-
            Scope is provenance, not storage: "public" for a row from the published
            corpus, "org" for one only this caller can see. A UI that cannot tell them
            apart cannot warn before sharing a link.
          type: string
        stars:
          type: integer
        template:
          description: lineage, if forked from one
          type: string
        title:
          type: string
        updated:
          type: string
        upstream:
          description: |-
            Upstream/License credit the third-party work an entry was published from:
            the difference between "this org built it" and "somebody else built it and
            we are showing it to you".

            WHO built it is Org, above — the account that paid for the project. There
            was once a separate admin-gated `official` boolean here claiming the same
            thing, and because it was gated it disagreed: apps Hanzo wrote and hosts
            were published by a script holding an ordinary org token, so it stayed
            false on all of them and this directory filed our own work as somebody
            else's. A field that restates an unforgeable fact can only ever be the
            wrong copy of it.
          type: string
        url:
          description: live, if it is deployed
          type: string
      type: object
    EnvVarJSON:
      properties:
        key:
          type: string
        secret:
          type: boolean
        value:
          type: string
      type: object
    Event:
      properties:
        distinctId:
          type: string
        event:
          type: string
        properties:
          additionalProperties: {}
          type: object
        time:
          type: string
        type:
          type: string
      type: object
    Exception:
      properties:
        frames:
          items:
            $ref: '#/components/schemas/Frame'
          type: array
        handled:
          type: boolean
        message:
          type: string
        stack:
          type: string
        type:
          type: string
      type: object
    Flow:
      properties:
        created:
          type: integer
        externalId:
          type: string
        folderId:
          type: string
        id:
          type: string
        metadata: {}
        projectId:
          description: projectId == org (server-derived)
          type: string
        publishedVersionId:
          type: string
        status:
          type: string
        updated:
          type: integer
      type: object
    FlowAction:
      properties:
        displayName:
          type: string
        name:
          type: string
        nextAction:
          $ref: '#/components/schemas/FlowAction'
        settings:
          $ref: '#/components/schemas/StepSettings'
        skip:
          type: boolean
        type:
          description: PIECE | CODE | ROUTER | LOOP_ON_ITEMS
          type: string
        valid:
          type: boolean
      type: object
    FlowRun:
      properties:
        created:
          type: integer
        finishTime:
          type: integer
        flowId:
          type: string
        flowVersionId:
          type: string
        id:
          type: string
        startTime:
          type: integer
        status:
          type: string
        updated:
          type: integer
      type: object
    FlowTrigger:
      properties:
        displayName:
          type: string
        name:
          type: string
        nextAction:
          $ref: '#/components/schemas/FlowAction'
        settings:
          $ref: '#/components/schemas/StepSettings'
        strategy:
          type: string
        type:
          description: PIECE_TRIGGER | EMPTY
          type: string
        valid:
          type: boolean
      type: object
    FlowVersion:
      properties:
        created:
          type: integer
        displayName:
          type: string
        flowId:
          type: string
        id:
          type: string
        schemaVersion:
          type: string
        state:
          type: string
        trigger:
          $ref: '#/components/schemas/FlowTrigger'
        updated:
          type: integer
        valid:
          type: boolean
      type: object
    Frame:
      properties:
        column:
          type: integer
        file:
          type: string
        function:
          type: string
        line:
          type: integer
      type: object
    GPU:
      properties:
        memory:
          description: VRAM bytes, 0 = unknown
          type: integer
        model:
          description: '"GB10", "8060S", "RTX 4090"'
          type: string
        vendor:
          description: nvidia | amd | apple | intel | ...
          type: string
      type: object
    GitOpsApp:
      properties:
        automated:
          type: boolean
        health:
          description: Healthy|Degraded|Progressing|…
          type: string
        history:
          items:
            $ref: '#/components/schemas/GitOpsDeploy'
          type: array
        name:
          type: string
        namespace:
          type: string
        operation:
          $ref: '#/components/schemas/GitOpsOperation'
        path:
          type: string
        project:
          type: string
        reconciledAt:
          type: string
        repoURL:
          type: string
        resources:
          type: integer
        revision:
          description: the commit last applied
          type: string
        selfHeal:
          type: boolean
        sync:
          description: Synced|OutOfSync|Unknown
          type: string
        targetRevision:
          type: string
      type: object
    GitOpsDeploy:
      properties:
        automated:
          type: boolean
        deployedAt:
          type: string
        id:
          type: integer
        revision:
          type: string
        startedAt:
          type: string
      type: object
    GitOpsOperation:
      properties:
        finishedAt:
          type: string
        message:
          type: string
        phase:
          type: string
        revision:
          type: string
        startedAt:
          type: string
      type: object
    GitOpsPlane:
      properties:
        applications:
          items:
            $ref: '#/components/schemas/GitOpsApp'
          type: array
        installed:
          type: boolean
        reason:
          type: string
      type: object
    Health:
      properties:
        status:
          description: Status is ok when the message plane answers, degraded otherwise.
          type: string
        uptime:
          description: Uptime is how long this surface has been mounted.
          type: string
        version:
          description: Version is the connected broker's server version; empty while
            degraded.
          type: string
      type: object
    Hit:
      properties:
        corpus:
          type: string
        doctype:
          type: string
        id:
          type: string
        matched:
          items:
            $ref: '#/components/schemas/Match'
          type: array
        project:
          type: string
        score:
          type: number
        title:
          type: string
        url:
          type: string
      type: object
    Holding:
      properties:
        costCents:
          description: wholesale cost
          type: integer
        domain:
          description: the name owned
          type: string
        expiresAt:
          description: when the registration lapses, RFC3339
          type: string
        nameservers:
          description: the authoritative nameservers the name points at
          items:
            type: string
          type: array
        order:
          description: registrar order id
          type: integer
        org:
          description: the org that owns the domain
          type: string
        priceCents:
          description: what the customer paid (sell)
          type: integer
        registeredAt:
          description: unix seconds
          type: integer
      type: object
    InvoiceLineIn:
      properties:
        amount:
          description: Amount is the line total in whole cents (250000 is $2,500.00).
          type: integer
        description:
          description: Description is the human-readable line, e.g. "Advisory retainer
            — August".
          type: string
        quantity:
          description: Quantity is the number of units, when the line is metered.
            Optional.
          type: integer
        unitPrice:
          description: UnitPrice is the per-unit price in cents, when the line is
            metered. Optional.
          type: integer
      type: object
    InvoiceOut:
      properties:
        amountDueCents:
          description: AmountDueCents is what remains collectible.
          type: integer
        amountPaidCents:
          description: AmountPaidCents is what has been collected so far.
          type: integer
        createdAt:
          description: CreatedAt is when the draft was raised, RFC3339.
          type: string
        currency:
          description: Currency is the ISO 4217 code.
          type: string
        customerEmail:
          description: CustomerEmail is where it is sent.
          type: string
        id:
          description: ID is the invoice id — what the issue, collect and void ops
            address.
          type: string
        lines:
          description: Lines are the charges on the invoice.
          items:
            $ref: '#/components/schemas/InvoiceLineIn'
          type: array
        number:
          description: |-
            Number is the human-facing invoice number, e.g. "INV-0042". A draft has
            none; issuing assigns it.
          type: string
        paymentRef:
          description: PaymentRef is the processor reference for the collection, once
            paid.
          type: string
        status:
          description: |-
            Status is draft, open, paid, void or uncollectible. A draft is not
            collectible; issuing moves it to open.
          type: string
        subtotalCents:
          description: SubtotalCents is the sum of the lines.
          type: integer
        userId:
          description: UserID is the customer billed.
          type: string
      type: object
    LLM:
      properties:
        available:
          description: |-
            Available is false when the warehouse was not connected or a query blipped.
            The totals below are then honest zeros, NOT measured ones.
          type: boolean
        completionTokens:
          description: CompletionTokens is the output half.
          type: integer
        costCents:
          description: CostCents is what they cost the org, in US cents. This IS a
            Hanzo charge.
          type: integer
        models:
          description: Models is how many distinct models were used.
          type: integer
        promptTokens:
          description: PromptTokens is the input half of that total.
          type: integer
        requests:
          description: Requests is how many completions the org made in the window.
          type: integer
        source:
          description: Source names the warehouse table the totals came from.
          type: string
        tokens:
          description: Tokens is the total tokens those completions consumed.
          type: integer
      type: object
    LLMOverview:
      properties:
        available:
          description: |-
            Available is true whenever the ledger answered — including with no usage in the
            window, which is honest zeros rather than a missing lens.
          type: boolean
        completionTokens:
          description: CompletionTokens is the output half of Tokens.
          type: integer
        errorRate:
          description: |-
            ErrorRate is Errors/Requests, 0..1, rounded to three places. Zero when there
            were no requests.
          type: number
        errors:
          description: Errors is how many of Requests failed.
          type: integer
        models:
          description: Models is how many distinct models the org called.
          type: integer
        promptTokens:
          description: PromptTokens is the input half of Tokens.
          type: integer
        providers:
          description: Providers is how many distinct providers served them.
          type: integer
        requests:
          description: Requests is how many LLM calls the org made in the window.
          type: integer
        source:
          description: Source is the warehouse table the lens read.
          type: string
        spendCents:
          description: SpendCents is what those calls cost, in cents.
          type: integer
        tokens:
          description: Tokens is prompt plus completion tokens over those calls.
          type: integer
      type: object
    LatencyStat:
      properties:
        available:
          description: false when no GenAI spans carry timing; the percentiles are
            then null
          type: boolean
        p50Ms:
          description: median latency over the window
          type: number
        p95Ms:
          description: 95th-percentile latency
          type: number
        p99Ms:
          description: 99th-percentile latency
          type: number
      type: object
    LeaderboardRow:
      properties:
        anonymous:
          description: |-
            Anonymous is true when this subject's identity was withheld and Handle is the
            "Anonymous" placeholder: the metric is real, the name is not. Render it as an
            unnamed row, never as someone actually called Anonymous.
          type: boolean
        costCents:
          description: |-
            CostCents is this subject's spend in whole US cents (not dollars, not
            millicents). It is 0 unless the viewer is entitled to this subject's spend —
            their own row, an admin looking at their own org, an explicitly cost-ranked
            board, or a platform admin on the global board — so 0 means "withheld or zero",
            and the two are deliberately indistinguishable.
          type: integer
        handle:
          description: |-
            Handle is the display identity to render: the peer's chosen handle if they opted
            into public listing, their username if the viewer is an admin of their org, the
            org's chosen display name (or its id) on an org board, and the literal
            "Anonymous" when the identity is withheld. Never an email or a raw ledger id.
          type: string
        metric:
          description: |-
            Metric is the value the board was ranked by, copied from Requests, Tokens or
            CostCents according to the request's metric. It is what a client sizes bars
            against without having to know which metric was asked for.
          type: integer
        rank:
          description: |-
            Rank is this subject's 1-based standing in the window, 1 being the top. Rows are
            ordered by the ranked metric descending (ties broken by request count), and a
            board is always the top of the list — there is no offset paging — so the first
            row is always rank 1 and rank is also the row's index + 1.
          type: integer
        requests:
          description: |-
            Requests is how many AI requests this subject made in the window. Volume is not
            sensitive, so it is reported for every row including anonymized ones.
          type: integer
        self:
          description: |-
            Self marks the caller's own row so a client can highlight it in place. At most
            one row carries it, and it is never set on an org board — org rows carry no user
            identity, so the caller's own org standing arrives in LeaderboardView.Self.
          type: boolean
        tokens:
          description: |-
            Tokens is prompt+completion tokens this subject spent in the window. Like
            Requests it is reported for every row.
          type: integer
      type: object
    LeaderboardView:
      properties:
        available:
          description: |-
            Available is false when the usage warehouse is not connected or its rollup is not
            ready. Rows is then empty because nothing could be read — not because nobody used
            anything. Show that difference; never render an unavailable board as a real one.
          type: boolean
        end:
          description: |-
            End is the EXCLUSIVE upper bound of the window, "2006-01-02" — the day after the
            last one counted. A board through today reports tomorrow's date here.
          type: string
        metric:
          description: 'Metric echoes the value ranked: tokens|requests|cost.'
          type: string
        period:
          description: |-
            Period is the window's canonical label: day|week|month|all. The server resolves
            aliases (7d, 30d, today, …) to these, so this may differ from what was sent.
          type: string
        rows:
          description: |-
            Rows are the ranked subjects, best first, at most the requested limit of them.
            Always a list, never null: an empty one means nothing was read, not an error.
          items:
            $ref: '#/components/schemas/LeaderboardRow'
          type: array
        scope:
          description: 'Scope echoes the board that was served: personal|org|global.'
          type: string
        self:
          $ref: '#/components/schemas/SelfRank'
          description: |-
            Self is the caller's own standing, reported even when they fall outside Rows.
            Absent when the caller's ledger identity cannot be resolved, or when the query
            behind it failed — never faked to keep the shape tidy.
        source:
          description: |-
            Source names the table these numbers were aggregated from (the derived daily
            rollup, hanzo.usage_rollup_daily), so an operator can tell exactly what was read.
          type: string
        start:
          description: |-
            Start is the first day counted, "2006-01-02" inclusive. Empty for period=all,
            which has no lower bound at all.
          type: string
        subject:
          description: |-
            Subject is what the rows stand for — "user" on a personal or org board, "org" on
            the global one. It tells a client whether Handle names a person or a company.
          type: string
        total:
          description: |-
            Total is how many subjects were ranked in the window — the org's active users, or
            the active/opted-in orgs on the global board. It is the universe the ranks are
            out of, so it is normally larger than len(rows).
          type: integer
      type: object
    LeaseIn:
      properties:
        class:
          description: "Class is what KIND of computer to lease, and the set is closed:\n\n\texec
            \    a throwaway one that keeps nothing. Seconds to minutes.\n\tdev      a
            coding one, with the project's own disk attached. Hours.\n\tdesktop  a
            dev one that also has a screen.\n\tandroid  a desktop with a phone running
            on that screen.\n\nEmpty leases an `exec`, which is the right answer for
            running a program and\nthe wrong one for working on a repository, because
            it keeps nothing.\n\nAn `android` needs a node that can virtualise a CPU,
            so it is the one class\na deployment may not be able to place. Where the
            fleet has none, the lease\nsucceeds and the pod stays Pending naming the
            device it is waiting for —\nwhich is the honest answer, because the alternative
            is an emulator running\non an interpreted CPU and never finishing its
            boot."
          type: string
        id:
          description: |-
            ID names a sandbox to RESUME, and is the id an earlier lease answered with.
            Empty asks for a new one. A caller that holds an id and omits it does not get
            a second view of the same computer, it gets a second computer.
          type: string
        project:
          description: |-
            Project names the disk to attach, and is REQUIRED for every class but `exec`.

            One live sandbox per project: the disk attaches to one computer at a time, so
            a second lease over a project that already has one is refused by name rather
            than handed a silently empty disk.
          type: string
        runtime:
          description: |-
            Runtime is the isolation boundary asked for: `gvisor` shares a filesystem
            and holds a project volume, `kata-fc` is a microVM that boots slower and
            reads files faster but has no shared filesystem at all. Empty asks for the
            fleet's default, which is the right answer unless you are measuring.

            It is a REQUEST. The owner decides, and refuses a combination it cannot
            honour — a volume under a runtime with no shared filesystem would write
            into a tmpfs and lose the bytes at exit. Read Leased.Runtime for what the
            sandbox actually got.
          type: string
        ttlSec:
          description: |-
            TTLSec bounds the lease in seconds. Unset takes the class default. Nothing
            runs forever, because a sandbox is somebody else's code on our nodes.
          type: integer
      type: object
    Leased:
      properties:
        class:
          type: string
        id:
          type: string
        runtime:
          description: |-
            Runtime is the boundary this sandbox GOT, which need not be the one asked
            for — carried for the same reason Workdir is, that it is a fact only the
            owner knows and a caller assuming it would be holding a second copy. Empty
            is the node's default runtime, and a real answer.
          type: string
        status:
          type: string
        workdir:
          type: string
      type: object
    Location:
      properties:
        external:
          type: boolean
        path:
          type: string
        range:
          $ref: '#/components/schemas/Range'
      type: object
    LogBody:
      properties:
        body:
          type: string
        number:
          type: integer
        severity:
          type: string
      type: object
    MCPError:
      properties:
        code:
          type: integer
        data: {}
        message:
          type: string
      type: object
    MCPListing:
      properties:
        description:
          description: Description is the publisher's one-line summary.
          type: string
        featured:
          description: Featured puts the listing on the front of the shelf. Curation.
          type: boolean
        hidden:
          description: |-
            Hidden keeps the listing out of the org-visible catalog. Curation: a sync
            never changes it. Only a SuperAdmin sets it, and only a SuperAdmin sees a
            hidden entry listed.
          type: boolean
        id:
          description: |-
            ID addresses the listing in a URL. It is the reverse-DNS NAME with its one
            slash written as an underscore — reversible, because a namespace never
            contains an underscore — so the id is readable and stable rather than a
            hash that means nothing to whoever reads a link.
          type: string
        logo:
          description: |-
            Logo is the brand mark to render for the listing — the publisher's icon when
            the entry carries one, or the one an admin set. Curation.
          type: string
        name:
          description: Name is the publisher's reverse-DNS name, e.g. "com.stripe/mcp".
          type: string
        official:
          description: |-
            Official is whether this is the vendor's OWN server rather than someone
            else's copy of it. Derived on every sync (see isOfficial) until a
            SuperAdmin sets it explicitly, after which the admin's answer stands.
          type: boolean
        packages:
          description: |-
            Packages are the runnable package forms — npm, pypi, oci — each with the
            runtime that launches it and the transport it then speaks.
          items:
            $ref: '#/components/schemas/MCPPackage'
          type: array
        registry:
          description: Registry is the upstream this row was synced from.
          type: string
        remotes:
          description: Remotes are the hosted endpoints the publisher serves the server
            at.
          items:
            $ref: '#/components/schemas/MCPRemote'
          type: array
        repo:
          description: Repo is the source repository URL, when the entry names one.
          type: string
        site:
          description: Site is the project's homepage, when the entry names one.
          type: string
        synced:
          description: Synced is when this row was last confirmed against upstream,
            Unix seconds.
          type: integer
        title:
          description: Title is the human-readable display name, when the entry carries
            one.
          type: string
        transports:
          description: |-
            Transports are the distinct transports this server can be reached over,
            sorted: some of "stdio", "streamable-http", "sse". A listing with
            "streamable-http" is one an org can enable here and now; a listing that is
            only "stdio" needs a process to run it.
          items:
            type: string
          type: array
        vendor:
          description: Vendor is the namespace half of Name — the publisher, e.g.
            "com.stripe".
          type: string
        version:
          description: Version is the published version of this listing.
          type: string
      type: object
    MCPPackage:
      properties:
        identifier:
          description: Identifier is the package name or download URL.
          type: string
        registry:
          description: 'Registry is where the package is fetched from: npm, pypi,
            oci, nuget, mcpb.'
          type: string
        runtime:
          description: 'Runtime is the publisher''s hint for what launches it: npx,
            uvx, docker.'
          type: string
        transport:
          description: 'Transport is what the launched process speaks: usually "stdio".'
          type: string
        version:
          description: Version is the exact published package version.
          type: string
      type: object
    MCPRemote:
      properties:
        transport:
          description: Transport is "streamable-http" or "sse".
          type: string
        url:
          description: URL is the endpoint.
          type: string
      type: object
    MCPRequest:
      properties:
        id: {}
        jsonrpc:
          type: string
        method:
          type: string
        params:
          additionalProperties: {}
          type: object
      type: object
    MCPResponse:
      properties:
        error:
          $ref: '#/components/schemas/MCPError'
        id: {}
        jsonrpc:
          type: string
        result:
          additionalProperties: {}
          type: object
      type: object
    MCPServer:
      properties:
        authHeader:
          description: |-
            AuthHeader is the request header the KMS-held credential is injected into,
            e.g. "Authorization". Absent when the server needs no credential.
          type: string
        createdAt:
          description: CreatedAt is when the server was registered, Unix seconds.
          type: integer
        hasSecret:
          description: |-
            HasSecret is whether a credential is sealed in KMS for this server. The
            VALUE is never returned by any route.
          type: boolean
        id:
          description: |-
            ID is the server's id within the org. It also PREFIXES every tool name the
            server contributes, which is what keeps two servers' "search" apart.
          type: string
        listing:
          description: |-
            Listing is the catalog entry this server was enabled from, when it was.
            Empty means the org typed the URL in itself.
          type: string
        name:
          description: Name is the org's label for the server.
          type: string
        org:
          description: Org is the org that registered the server — the validated caller's.
          type: string
        source:
          description: |-
            Source is where the registration came from: "catalog" when it was enabled
            off the shelf, "org" when the org registered the URL itself. It is DERIVED
            from Listing rather than stored, because two columns for one fact is two
            chances to disagree.
          type: string
        url:
          description: |-
            URL is the server's JSON-RPC endpoint. Always a public http(s) host: the
            registration boundary and the dialer both refuse anything else.
          type: string
      type: object
    Match:
      properties:
        backend:
          type: string
        rank:
          type: integer
        score:
          type: number
      type: object
    MetricBody:
      properties:
        labels:
          additionalProperties: {}
          type: object
        name:
          type: string
        value:
          type: number
      type: object
    Metrics:
      properties:
        at:
          description: unix seconds, server-stamped
          type: integer
        gpuUtil:
          description: 0..1 aggregate utilization
          type: number
        load1:
          type: number
        load5:
          type: number
        load15:
          type: number
        memFree:
          description: bytes
          type: integer
        memUsed:
          description: bytes
          type: integer
      type: object
    Middleware:
      properties:
        config:
          additionalProperties:
            type: string
          description: |-
            Config is the transform's parameters: redirectScheme takes scheme (default
            https) and permanent ("true" ⇒ 301, else 302); stripPrefix REQUIRES
            prefixes (comma-separated, first match wins); addPrefix REQUIRES prefix;
            headers is a header→value map set on the response.
          type: object
        id:
          description: |-
            ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128
            chars. A create that omits it gets a generated one. Routes reference it by
            this id.
          type: string
        type:
          description: 'Type is the transform: redirectScheme, stripPrefix, addPrefix
            or headers.'
          type: string
      type: object
    ModelRow:
      properties:
        model:
          description: Model is the model id, e.g. zen5-coder.
          type: string
        pct:
          description: Pct is this model's share of the window's returned spend, 0..100,
            one decimal.
          type: number
        provider:
          description: Provider is who served it.
          type: string
        requests:
          description: Requests is how many calls went to this model.
          type: integer
        spendCents:
          description: SpendCents is what they cost, in cents.
          type: integer
        tokens:
          description: Tokens is prompt plus completion tokens over those calls.
          type: integer
      type: object
    ModelStat:
      properties:
        completionTokens:
          description: tokens it answered with
          type: integer
        costCents:
          description: what this model cost, in cents
          type: integer
        costPct:
          description: share of total spend, 0..100
          type: number
        errorRate:
          description: share of its calls that failed, 0..1
          type: number
        errors:
          description: calls to it that did not succeed
          type: integer
        model:
          description: the model this row is about, or "other" for the fold
          type: string
        modelCount:
          description: '>0 only on the "other" fold'
          type: integer
        p50Ms:
          description: median latency, null when no spans carry it
          type: number
        p95Ms:
          description: 95th-percentile latency, null when unknown
          type: number
        p99Ms:
          description: 99th-percentile latency, null when unknown
          type: number
        promptTokens:
          description: tokens sent to it
          type: integer
        provider:
          description: who serves it
          type: string
        requests:
          description: calls to this model in the window
          type: integer
        totalTokens:
          description: prompt plus completion
          type: integer
      type: object
    Number:
      properties:
        capable:
          description: voice | sms | mms | fax
          items:
            type: string
          type: array
        country:
          type: string
        currency:
          type: string
        e164:
          type: string
        id:
          type: string
        monthly:
          description: minor units, as the carrier quoted it
          type: integer
        org:
          type: string
        type:
          type: string
      type: object
    Offer:
      properties:
        available:
          description: whether it can be bought right now
          type: boolean
        currency:
          description: the currency both prices are in
          type: string
        domain:
          description: the name this quote prices
          type: string
        premium:
          description: whether the registry prices it above the standard rate
          type: boolean
        priceCents:
          description: sell (first-term registration)
          type: integer
        renewalPriceCents:
          description: sell (renewal)
          type: integer
        tld:
          description: the top-level domain the name sits under
          type: string
      type: object
    Overview:
      properties:
        commerce:
          $ref: '#/components/schemas/CommerceOverview'
          description: Commerce is the orders/revenue lens over product events.
        end:
          description: End is the window's exclusive upper bound, RFC3339 UTC.
          type: string
        interval:
          description: 'Interval is the bucket width the window implies: hour or day.'
          type: string
        llm:
          $ref: '#/components/schemas/LLMOverview'
          description: LLM is the LLM usage lens — real per-org data.
        range:
          description: 'Range is the window that was actually applied: 24h, 7d, 30d
            or custom.'
          type: string
        scope:
          $ref: '#/components/schemas/Scope'
          description: Scope names the tenant these numbers belong to.
        start:
          description: Start is the window's inclusive lower bound, RFC3339 UTC.
          type: string
        web:
          $ref: '#/components/schemas/WebOverview'
          description: Web is the web-traffic lens over product events.
      type: object
    PagesBuildConfig:
      properties:
        build_command:
          type: string
        destination_dir:
          type: string
        root_dir:
          type: string
      type: object
    PagesD1Binding:
      properties:
        id:
          type: string
      type: object
    PagesDeploy:
      properties:
        branch:
          type: string
      type: object
    PagesDeploymentConfig:
      properties:
        compatibility_date:
          type: string
        compatibility_flags:
          items:
            type: string
          type: array
        d1_databases:
          additionalProperties:
            $ref: '#/components/schemas/PagesD1Binding'
          type: object
        env_vars:
          additionalProperties:
            $ref: '#/components/schemas/PagesEnvVar'
          type: object
        kv_namespaces:
          additionalProperties:
            $ref: '#/components/schemas/PagesKVBinding'
          type: object
        r2_buckets:
          additionalProperties:
            $ref: '#/components/schemas/PagesR2Binding'
          type: object
      type: object
    PagesDeploymentConfigs:
      properties:
        preview:
          $ref: '#/components/schemas/PagesDeploymentConfig'
        production:
          $ref: '#/components/schemas/PagesDeploymentConfig'
      type: object
    PagesEnvVar:
      properties:
        type:
          type: string
        value:
          type: string
      type: object
    PagesKVBinding:
      properties:
        namespace_id:
          type: string
      type: object
    PagesProjectCreate:
      properties:
        build_config:
          $ref: '#/components/schemas/PagesBuildConfig'
        deployment_configs:
          $ref: '#/components/schemas/PagesDeploymentConfigs'
        name:
          type: string
        production_branch:
          type: string
      type: object
    PagesR2Binding:
      properties:
        name:
          type: string
      type: object
    PathIn:
      properties:
        id:
          description: ID is the sandbox to read from, from an earlier lease.
          type: string
        path:
          description: |-
            Path is read relative to the sandbox's working directory unless it is
            absolute, and a path that climbs out of it is refused rather than rewritten.
            Empty names the working directory itself, which lists it.
          type: string
      type: object
    PaymentIn:
      properties:
        amountCents:
          description: |-
            AmountCents is the amount to charge, in whole cents (5000 is $50.00).
            Server-side bounds apply and are authoritative — the default floor is $1
            and the ceiling $5,000, so a fat-fingered or hostile amount is refused
            before any money moves.
          type: integer
        currency:
          description: Currency is the ISO 4217 code, lower-cased. Empty means usd.
          type: string
        idempotencyKey:
          description: |-
            IdempotencyKey makes a retry safe: the same key never charges twice, it
            replays the first result. Sending one is strongly recommended for an agent,
            which retries by construction. Empty falls back to a windowed key derived
            from the amount and currency, so a double-submit inside 15 minutes still
            collapses onto one charge.
          type: string
        sourceId:
          description: |-
            SourceID is the single-use payment token that stands in for the card: a
            Square Web Payments SDK nonce minted in the browser, or a Square sandbox
            test nonce when the org's credentials are sandbox ones. The card number
            itself never reaches this process, which is what keeps it out of PCI scope.
          type: string
      type: object
    PaymentOut:
      properties:
        balanceCents:
          description: |-
            BalanceCents is the org's balance AFTER this payment, read back from the
            same key just credited so it matches what the balance endpoint reports.
          type: integer
        id:
          description: |-
            ID is the ledger transaction id for the credit. It is what getPayment
            reads back, and the customer-visible receipt for the money.
          type: string
        processorRef:
          description: |-
            ProcessorRef is the payment processor's own reference for the charge
            (Square's payment id). It is the field that proves money actually moved at
            the gateway rather than only in our ledger — the thing to quote when
            reconciling against a processor dashboard.
          type: string
        status:
          description: |-
            Status is "ok" on a settled charge. A charge that did not settle is an
            error with the processor's reason, never a status field to inspect.
          type: string
        test:
          description: |-
            Test reports which bucket this credited: true is a SANDBOX charge crediting
            the test balance, false is live money. It is always stated so a receipt can
            never be mistaken for the other kind.
          type: boolean
      type: object
    PaymentRecord:
      properties:
        amountCents:
          description: AmountCents is the credited amount in whole cents.
          type: integer
        createdAt:
          description: CreatedAt is when the credit was written, RFC3339.
          type: string
        currency:
          description: Currency is the ISO 4217 code.
          type: string
        id:
          description: ID is the ledger transaction id.
          type: string
        notes:
          description: Notes is the ledger memo, carrying the processor and its reference.
          type: string
        status:
          description: |-
            Status is the payment's state. This ledger writes a deposit only AFTER the
            processor settled, so a payment that can be read is one that succeeded.
          type: string
        subject:
          description: Subject is the billing key this payment credited.
          type: string
        test:
          description: Test reports whether this was a sandbox charge (test balance)
            or live money.
          type: boolean
      type: object
    Policy:
      properties:
        cache_paths:
          additionalProperties:
            type: integer
          description: |-
            CachePaths overrides CacheTTLSec per path PREFIX (key "/v1/models" → seconds).
            The longest matching prefix wins.
          type: object
        cache_ttl_sec:
          description: |-
            CacheTTLSec is the org's default edge-cache TTL for its responses, in seconds;
            0 means no caching. Unset inherits the platform default.
          type: integer
        cors_origins:
          description: |-
            CORSOrigins is the PLATFORM-scope CORS allowlist EdgeCORS admits: an exact
            origin, a bare host, or a "*.host" wildcard. Writable only by a SuperAdmin —
            CORS is evaluated before identity, so it has no tenant to scope to.
          items:
            type: string
          type: array
        methods:
          description: |-
            Methods is the allowlist of HTTP methods the edge accepts for this org. Empty
            means all are accepted.
          items:
            type: string
          type: array
        mode:
          description: |-
            Mode is the abuse gate's posture for THIS scope: "shadow" scores traffic and
            records the verdict without acting on it, "live" enforces it. Unset means
            shadow.

            It is the one per-org field that does NOT inherit. Every other field here
            layers a platform default under the org's own value, which is right for a
            default: a tenant that sets no rate ceiling should get the platform's. Mode
            is not a default, it is an ARMING DECISION — it is what makes a statistical
            judgement start refusing real traffic — and inheriting it means arming one
            scope arms every tenant that never asked for it, without a write to their
            row and without anything in their config changing. So a tenant is live only
            if that tenant's OWN row says live, and the platform row's mode governs
            exactly one scope: the anonymous lane, which has no tenant of its own.

            It is also not self-service. Writing it requires SuperAdmin (see the
            /v1/gateway config op): the subject of an abuse control does not get to
            switch the control off.
          type: string
        org_rpm:
          description: |-
            OrgRPM is the org's OWN authenticated rate ceiling, requests per minute, as
            ScopeRateLimit enforces it. Unset inherits the platform default, then the
            static boot default.
          type: integer
        per_ip_rpm:
          description: |-
            PerIPRPM is the PLATFORM-scope pre-auth flood cap: requests EdgeRateLimit
            admits per WindowSec from one client IP. SuperAdmin-only, same reason.
          type: integer
        updated_at:
          description: |-
            UpdatedAt is the unix second this policy row was last written. Server-stamped;
            a client-supplied value is ignored.
          type: integer
        updated_by:
          description: |-
            UpdatedBy is the validated user id that wrote this policy row. Server-stamped;
            a client-supplied value is ignored.
          type: string
        window_sec:
          description: WindowSec is the window PerIPRPM is counted over, in seconds.
            SuperAdmin-only.
          type: integer
      type: object
    Position:
      properties:
        character:
          type: integer
        line:
          type: integer
      type: object
    Price:
      properties:
        amount:
          description: |-
            Amount is what ONE call costs, EXACTLY: an 18-decimal USD value, so a
            per-call price of $0.0025 is $0.0025 and not a cent-floored zero. Cents
            cannot hold a per-token price, and a tool plane is where per-token prices
            live.
        currency:
          description: Currency is the ISO 4217 code, e.g. "USD". Empty means USD.
          type: string
        recipient:
          description: Recipient is the payout wallet ref the marketplace seller is
            paid at.
          type: string
      type: object
    ProductRow:
      properties:
        orders:
          description: Orders is how many order_completed events carried it.
          type: integer
        productId:
          description: ProductID is the product the order events named.
          type: string
        revenue:
          description: Revenue is the total they carried, in the events' own currency
            unit.
          type: number
        units:
          description: Units is the summed quantity sold.
          type: integer
      type: object
    PropSpec:
      properties:
        description:
          type: string
        displayName:
          type: string
        name:
          type: string
        required:
          type: boolean
        type:
          description: string|number|boolean|object|array
          type: string
      type: object
    ProviderBreakdown:
      properties:
        available:
          description: |-
            Available is false when the warehouse could not be read, which means "no
            answer" and NOT "no usage" — Items is then empty for a reason.
          type: boolean
        items:
          description: Items is one row per provider, most tokens first.
          items:
            $ref: '#/components/schemas/ProviderRow'
          type: array
        source:
          description: Source names the warehouse table the rows came from.
          type: string
      type: object
    ProviderInfo:
      properties:
        displayName:
          description: |-
            DisplayName is the human label for the sign-in button; this deployment
            sends "Hanzo". Omitted from the body when empty.
          type: string
        name:
          description: |-
            Name is the provider id, and it is the value that goes back in the URL to
            start a login: GET /v1/team/account/auth/{provider}. This deployment
            surfaces exactly one, "openid" — the hanzo.id door.
          type: string
      type: object
    ProviderRow:
      properties:
        costCents:
          description: CostCents is what they cost the org, in US cents.
          type: integer
        provider:
          description: Provider is the upstream the requests were routed to, e.g.
            anthropic.
          type: string
        requests:
          description: Requests is how many completions the org made against that
            provider.
          type: integer
        tokens:
          description: |-
            Tokens is the total tokens those completions consumed, prompt plus
            completion.
          type: integer
      type: object
    Purge:
      properties:
        filter:
          description: Filter purges only messages on this org-relative subject (wildcards
            supported).
          type: string
        keep:
          description: Keep retains that many newest messages.
          type: integer
        name:
          description: Name is the stream name, from the path.
          type: string
      type: object
    Query:
      properties:
        character:
          description: |-
            Character is a 0-based UTF-16 code-unit offset within Line, per the LSP
            specification — not a byte offset and not a rune index.
          type: integer
        line:
          description: Line is 0-based, per the LSP specification.
          type: integer
        path:
          description: Path is the repo-relative file, e.g. "apps/lsp/lsp.go".
          type: string
        relation:
          description: |-
            Relation refines locate: definition, reference, type or implementation.
            Empty means definition. Every other op ignores it.
          type: string
        repo:
          description: |-
            Repo is the repository NAME within the caller's own org, e.g. "cloud".
            Not a URL and not an owner/name pair: the owner is the validated
            principal's org, so this names a repository the caller already owns.
          type: string
        rev:
          description: |-
            Rev is a branch, tag or commit sha. Empty means the default branch. It is
            resolved to a commit before anything else happens, so an answer is always
            about one immutable tree.
          type: string
      type: object
    RaiseInvoiceIn:
      properties:
        currency:
          description: Currency is the ISO 4217 code, lower-cased. Empty means usd.
          type: string
        customerEmail:
          description: CustomerEmail is where the invoice is sent. Optional.
          type: string
        lines:
          description: |-
            Lines are the charges. The invoice subtotal and amount due are COMPUTED
            from these — there is no total field to send, because a total that
            disagreed with its own lines would bill a number nobody could derive.
          items:
            $ref: '#/components/schemas/InvoiceLineIn'
          type: array
        userId:
          description: |-
            UserID identifies the customer being billed, within the caller's own org.
            Required — an invoice with no addressee is not an invoice.
          type: string
      type: object
    Ran:
      properties:
        exitCode:
          type: integer
        stderr:
          type: string
        stdout:
          type: string
      type: object
    Range:
      properties:
        end:
          $ref: '#/components/schemas/Position'
        start:
          $ref: '#/components/schemas/Position'
      type: object
    RateCard:
      properties:
        basis:
          description: |-
            Basis names where the rates come from, so a published price can be
            explained rather than merely asserted.
          type: string
        microUsdPerGbHour:
          description: |-
            MicroUSDPerGBHour is the price of one GiB of memory for one hour, in
            millionths of a US dollar.
          type: integer
        microUsdPerVcpuHour:
          description: |-
            MicroUSDPerVCPUHour is the price of one vCPU for one hour, in millionths of
            a US dollar.
          type: integer
      type: object
    Record:
      properties:
        name:
          description: the record name the customer creates
          type: string
        type:
          description: TXT | CNAME
          type: string
        value:
          description: the record value
          type: string
      type: object
    RegisterResult:
      properties:
        quote:
          $ref: '#/components/schemas/Offer'
          description: the price it was bought at
        record:
          $ref: '#/components/schemas/Holding'
          description: the ownership row this purchase issued
      type: object
    Registrant:
      properties:
        address1:
          description: street address
          type: string
        address2:
          description: second address line
          type: string
        city:
          description: city or locality
          type: string
        companyName:
          description: the organisation the contact acts for
          type: string
        country:
          description: ISO-3166 alpha-2, e.g. "US"
          type: string
        email:
          description: where WHOIS correspondence is sent
          type: string
        fax:
          description: fax number, in the same form as phone
          type: string
        firstName:
          description: the contact's given name
          type: string
        lastName:
          description: the contact's family name
          type: string
        phone:
          description: +NN.NNNNNNN
          type: string
        state:
          description: state, province or region
          type: string
        zip:
          description: postal code
          type: string
      type: object
    RenewResult:
      properties:
        paidCents:
          description: what this renewal cost, in cents
          type: integer
        record:
          $ref: '#/components/schemas/Holding'
          description: the ownership row with its new expiry
      type: object
    Report:
      properties:
        answer:
          description: |-
            Answer is the grounded prose, with inline markdown citations. Every link in
            it points at a page in Sources: the citation check runs on the text before
            it leaves the engine, so a cited URL is one THIS call fetched.
          type: string
        follow_ups:
          description: |-
            FollowUps are the questions worth asking next. Best-effort — an empty list
            is a normal outcome, not a fault.
          items:
            type: string
          type: array
        mode:
          description: 'Mode is the profile that ran: search, news, research or deep.'
          type: string
        model:
          description: Model is the model that synthesized the answer.
          type: string
        sources:
          description: |-
            Sources are the pages the answer was written from, deduplicated and ranked.
            Always an array, never null.
          items:
            $ref: '#/components/schemas/Source'
          type: array
      type: object
    Request:
      properties:
        doctypes:
          description: DocTypes restricts the semantic leg to a subset of indexed
            knowledge types.
          items:
            type: string
          type: array
        index:
          description: Index names the lexical index to query. Defaults to "kb".
          type: string
        limit:
          description: Limit bounds the FUSED result set (default 10, max 50).
          type: integer
        mode:
          description: 'Mode selects the legs: auto (default) | text | semantic |
            hybrid.'
          type: string
        offset:
          description: Offset pages the fused result set.
          type: integer
        project:
          description: Project narrows to one project scope within the org.
          type: string
        query:
          description: Query is the natural-language or keyword query. Required.
          type: string
      type: object
    Response:
      properties:
        backends:
          description: Backends is the per-leg report. Always populated.
          items:
            $ref: '#/components/schemas/BackendStatus'
          type: array
        hits:
          description: Hits is the fused, ranked result set.
          items:
            $ref: '#/components/schemas/Hit'
          type: array
        mode:
          description: Mode is the mode actually used after `auto` resolution.
          type: string
        status:
          description: |-
            Status is the query's overall honesty signal:
              ok          every consulted leg answered.
              partial     at least one leg failed; Hits holds the survivors' results.
              unavailable every consulted leg failed; Hits is empty AND that is stated.
          type: string
        took_ms:
          type: integer
      type: object
    Route:
      properties:
        host:
          description: |-
            Host is the exact hostname this route matches, lowercased with any trailing
            dot stripped. It is a GLOBALLY unique claim — one route across the whole
            edge may hold a host, so no tenant can hijack another's.
          type: string
        id:
          description: |-
            ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.
            A create that omits it gets a generated one.
          type: string
        middlewares:
          description: |-
            Middlewares are the ids of the edge transforms to apply, in this order,
            before the request reaches the service. At most 16.
          items:
            type: string
          type: array
        pathPrefix:
          description: |-
            PathPrefix narrows the match to requests under this path; it must start
            with "/". Empty matches every path on the host.
          type: string
        priority:
          description: |-
            Priority orders routes that share a host: higher wins, and equal priorities
            fall back to the longer PathPrefix.
          type: integer
        service:
          description: |-
            Service is the id of the backend pool this route dispatches to. A route
            naming a service that does not exist is skipped at compile, not served.
          type: string
        tls:
          description: TLS asks the edge to terminate TLS for Host with an ACME-managed
            certificate.
          type: boolean
      type: object
    RoutedUsage:
      properties:
        account:
          description: Account is the provider-side account identifier.
          type: string
        billing:
          description: 'Billing is how the routed inference bills: plan or commerce.'
          type: string
        completionTokens:
          description: CompletionTokens is the routed completion-token count.
          type: integer
        costCents:
          description: CostCents is the routed cost in cents.
          type: integer
        kind:
          description: 'Kind is how the account authenticates: subscription or apikey.'
          type: string
        promptTokens:
          description: PromptTokens is the routed prompt-token count.
          type: integer
        provider:
          description: Provider is the AI provider the row's account belongs to.
          type: string
        requests:
          description: Requests is how many requests the gateway routed through this
            account.
          type: integer
        totalTokens:
          description: TotalTokens is the routed total token count.
          type: integer
      type: object
    RunIn:
      properties:
        argv:
          description: |-
            Argv is the program and its arguments, already split — the form no shell can
            misread. Give this or Command, not both.
          items:
            type: string
          type: array
        blind:
          description: |-
            Blind is the set of secrets this command must never publish.

            It exists because output is redacted where it is PRODUCED or not at all. A
            caller that scrubbed the returned result would still have streamed the
            unredacted bytes into the session as they were written — to a durable event
            store, an SSE feed and a chat thread — because the narration leaves the
            sandbox by a different door from the result. Nothing downstream can take a
            secret back out of a message that has already been delivered.

            The sandbox holds these only for the life of the one command, applies them
            to every stream leaving it, and never logs or stores them.
          items:
            type: string
          type: array
        command:
          description: |-
            Command is a shell line, run by `sh -c`. Use it when a pipeline or a
            redirection is the point, and Argv when it is not.
          type: string
        dir:
          description: |-
            Dir runs the command somewhere other than the sandbox's working directory,
            which Leased.Workdir names.
          type: string
        id:
          description: ID is the sandbox to run in, from an earlier lease.
          type: string
        session:
          description: |-
            Session is the live agent session this command narrates into: its output is
            appended there AS IT IS PRODUCED, so every surface watching that session
            watches the command work instead of a blank pause. A long run is otherwise a
            silence with a verdict at the end.

            It names a SESSION and never a tenant. The org is the one the caller already
            proved, so a session belonging to somebody else is simply absent from the org
            this call acts for and the append is refused there. Empty means nothing is
            watching, and then nothing is sent.
          type: string
        stdin:
          description: |-
            Stdin is fed to the program on standard input. This is how bytes reach a file
            without being quoted into a shell line: `cat > path` with the contents here
            writes them exactly.
          type: string
        timeoutSec:
          description: |-
            TimeoutSec bounds this ONE command, so a wedged program holds the caller for
            its own timeout rather than for the whole lease.
          type: integer
      type: object
    SMS:
      properties:
        from:
          type: string
        id:
          type: string
        org:
          type: string
        status:
          description: queued | sent | delivered | failed
          type: string
        text:
          type: string
        to:
          type: string
      type: object
    Scope:
      properties:
        org:
          description: |-
            Org is the IAM org slug the rows were read under: the validated principal's,
            resolved server-side.
          type: string
      type: object
    SelfRank:
      properties:
        costCents:
          description: |-
            CostCents is the caller's own spend in whole US cents. Always populated — your
            own spend is never withheld from you — so here 0 really does mean zero.
          type: integer
        handle:
          description: |-
            Handle is how the caller appears on this board: their chosen handle, falling back
            to their username, on a user board; their org id on the global board. Present
            even when unlisted — this is the caller looking at themselves.
          type: string
        listed:
          description: |-
            Listed says whether the caller is publicly visible on this board: opted in on a
            user board, org opted in (or the viewer is a platform admin) on the global one.
            False is the prompt to offer the opt-in, and explains an unranked global self.
          type: boolean
        metric:
          description: |-
            Metric is whichever of the three values above the board was ranked by, so a
            client can compare the caller against the rows without re-reading the request.
            Metric <= 0 is exactly the case that leaves Ranked false.
          type: integer
        ofTotal:
          description: |-
            OfTotal is the size of the universe Rank is out of — "rank N of OfTotal". On a
            user board that is the org's users with any usage in the window; on the global
            board it is every active org for a platform admin, and the count of opted-in
            orgs for everyone else.
          type: integer
        rank:
          description: |-
            Rank is the caller's 1-based standing, computed as (subjects whose windowed
            metric strictly exceeds the caller's) + 1. It is exact against the whole ranked
            universe, not just the returned page, so it can far exceed len(rows). Read it
            only when Ranked.
          type: integer
        ranked:
          description: |-
            Ranked is false when the caller holds no position: they had no usage in the
            window, or (on the global board) their org has not opted into public listing and
            so is not ranked against a set it never joined. Rank is then 0 and means nothing.
          type: boolean
        requests:
          description: Requests is the caller's own request count in the window, 0
            if they were idle.
          type: integer
        tokens:
          description: Tokens is the caller's own prompt+completion tokens in the
            window.
          type: integer
      type: object
    Sequences:
      properties:
        consumer_seq:
          description: Consumer is the consumer's own sequence.
          type: integer
        stream_seq:
          description: Stream is the corresponding stream sequence.
          type: integer
      type: object
    SharePolicy:
      properties:
        revenueShareBps:
          type: integer
        updatedAt:
          type: integer
      type: object
    Skill:
      properties:
        content:
          description: Content is the SKILL.md body, markdown.
          type: string
        createdAt:
          description: CreatedAt is when the skill was last written, Unix seconds.
          type: integer
        description:
          description: Description is the one-line summary discovery shows for the
            skill.
          type: string
        id:
          description: |-
            ID is the skill's id within the org. It is DERIVED from Name, so writing
            the same name again revises that skill rather than adding another.
          type: string
        name:
          description: 'Name is the skill''s name: one lowercase path segment (a-z0-9,
            _ or -).'
          type: string
        org:
          description: |-
            Org is the org that authored the skill — the validated caller's, never a
            value the body supplied.
          type: string
      type: object
    Source:
      properties:
        engine:
          type: string
        favicon:
          type: string
        snippet:
          type: string
        title:
          type: string
        url:
          type: string
      type: object
    SourceState:
      properties:
        available:
          description: |-
            Available is whether this side's ledger answered. False means its rows are
            missing, not that there were none.
          type: boolean
        note:
          description: |-
            Note is the human sentence that says what this side's numbers mean, so a
            board cannot present a plan percentage as a Hanzo charge.
          type: string
        scope:
          description: 'Scope is whose rows this side carries: "user" or "org".'
          type: string
        source:
          description: Source is the table of record the rows came from.
          type: string
      type: object
    Sources:
      properties:
        commerce:
          description: Commerce is whether the billing ledger answered the spend block.
          type: boolean
        warehouse:
          description: Warehouse is whether the usage warehouse answered the LLM block.
          type: boolean
      type: object
    Span:
      properties:
        endLine:
          type: integer
        file:
          type: string
        kind:
          type: string
        line:
          type: integer
        repo:
          type: string
        role:
          description: 'context: match | definition | caller'
          type: string
        score:
          type: number
        snippet:
          type: string
        symbol:
          type: string
        tier:
          type: string
      type: object
    SpanBody:
      properties:
        duration:
          type: integer
        id:
          type: string
        kind:
          type: string
        parent:
          type: string
        status:
          type: string
        trace:
          type: string
      type: object
    Spec:
      properties:
        arch:
          description: amd64 | arm64 | ...
          type: string
        cpus:
          description: logical cores
          type: integer
        gpus:
          items:
            $ref: '#/components/schemas/GPU'
          type: array
        memory:
          description: total RAM, bytes
          type: integer
        os:
          description: linux | darwin | windows
          type: string
      type: object
    Spend:
      properties:
        available:
          description: |-
            Available is false when the commerce ledger was unconfigured or
            unreachable. Every number below is then an honest zero, NOT a measured one.
          type: boolean
        availableCents:
          description: AvailableCents is what of that balance is still spendable.
          type: integer
        balanceCents:
          description: BalanceCents is the prepaid wallet's balance, in US cents.
          type: integer
        byCategory:
          description: ByCategory is the window's spend split by ledger category,
            largest first.
          items:
            $ref: '#/components/schemas/CategorySpend'
          type: array
        mtdCents:
          description: |-
            MTDCents is commerce's authoritative month-to-date consumed figure, which
            is a different period from the window and is not derived from it.
          type: integer
        overageCents:
          description: OverageCents is month-to-date consumption beyond the plan's
            allowance.
          type: integer
        series:
          description: |-
            Series is the window's spend over time, gap-filled at the window's
            interval.
          items:
            $ref: '#/components/schemas/SpendPoint'
          type: array
        source:
          description: Source names where the roll-up came from.
          type: string
        totalCents:
          description: |-
            TotalCents is consumption over the requested window, in US cents. It is
            self-consistent with ByCategory and Series.
          type: integer
      type: object
    SpendPoint:
      properties:
        cents:
          description: Cents is the consumption recorded in that bucket, in US cents.
          type: integer
        t:
          description: |-
            T is the bucket's start instant, RFC3339 UTC. Buckets are gap-filled, so a
            window with no spend still has its points.
          type: string
      type: object
    StarterKit:
      properties:
        category:
          description: groups the kit in the gallery browser ("Portfolio", "SaaS")
          type: string
        demo:
          description: live demo (<slug>.hanzo.app), when deployed
          type: string
        description:
          description: the browse-card blurb
          type: string
        features:
          description: the highlights the card lists, at most 32
          items:
            type: string
          type: array
        framework:
          description: the stack the kit is built on ("Next.js 14.2 + TS")
          type: string
        org:
          description: owner of a PRIVATE template; empty in the public catalog
          type: string
        preview:
          description: the still image the browse card renders
          type: string
        rating:
          description: |-
            Rating is public-gallery curation, on the same terms as Tier: catalog-only,
            never accepted from a request, absent on a customer's own kit.
          type: number
        slug:
          description: the kit's identity — lowercase alphanumeric with dashes, max
            40
          type: string
        source:
          description: the repository the kit is forked from
          type: string
        tier:
          description: |-
            Tier is public-gallery curation, carried verbatim from the embedded catalog.
            No request can set it — neither write body has the field and neither builds a
            kit carrying one — so it is absent on every customer-published kit.
          type: integer
        title:
          description: display name
          type: string
        useCase:
          description: what the kit is for, in a phrase
          type: string
        variants:
          description: the shapes this template ships in
          items:
            $ref: '#/components/schemas/Variant'
          type: array
      type: object
    State:
      properties:
        bytes:
          description: Bytes is the total stored size.
          type: integer
        consumer_count:
          description: Consumers is the number of consumers attached to this stream.
          type: integer
        first_seq:
          description: FirstSeq is the sequence of the first stored message.
          type: integer
        first_ts:
          description: FirstTS is the timestamp of the first stored message.
          format: date-time
          type: string
        last_seq:
          description: LastSeq is the sequence of the last stored message.
          type: integer
        last_ts:
          description: LastTS is the timestamp of the last stored message.
          format: date-time
          type: string
        messages:
          description: Messages is the number of messages currently stored.
          type: integer
        num_deleted:
          description: Deleted is the number of deleted messages (sequence gaps).
          type: integer
        num_subjects:
          description: Subjects is the number of distinct subjects stored.
          type: integer
      type: object
    StepSettings:
      properties:
        actionName:
          type: string
        input:
          additionalProperties:
            type: object
          type: object
        pieceName:
          type: string
        pieceVersion:
          type: string
        triggerName:
          type: string
      type: object
    StopIn:
      properties:
        id:
          type: string
      type: object
    Stopped:
      properties:
        stopped:
          type: integer
      type: object
    Stream:
      properties:
        config:
          $ref: '#/components/schemas/Config'
          description: Config is the stream's configuration.
        created:
          description: Created is when the stream was created.
          format: date-time
          type: string
        name:
          description: Name is the stream name within the org.
          type: string
        state:
          $ref: '#/components/schemas/State'
          description: State is the stream's current state.
      type: object
    Streams:
      properties:
        streams:
          description: Streams is the page, ordered by name.
          items:
            $ref: '#/components/schemas/Stream'
          type: array
        total:
          description: Total is the org's stream count before paging.
          type: integer
      type: object
    Symbol:
      properties:
        detail:
          type: string
        kind:
          type: integer
        name:
          type: string
        range:
          $ref: '#/components/schemas/Range'
      type: object
    TLSConfig:
      properties:
        acmeEmail:
          description: |-
            ACMEEmail is the ACME account email. It binds an account for the lifetime
            of an edge process, so it applies only when the edge (re)starts.
          type: string
        extraHosts:
          description: |-
            ExtraHosts get certificates without owning a route — at most 256. They feed
            the ACME HostPolicy and hot-apply on the next reload.
          items:
            type: string
          type: array
        staging:
          description: |-
            Staging issues from Let's Encrypt's staging directory (untrusted certs, high
            rate limits). Like ACMEEmail it applies only when the edge (re)starts.
          type: boolean
      type: object
    Timeseries:
      properties:
        end:
          description: End is the window's exclusive upper bound, RFC3339 UTC.
          type: string
        interval:
          description: 'Interval is the bucket width: hour or day.'
          type: string
        range:
          description: 'Range is the window that was actually applied: 24h, 7d, 30d
            or custom.'
          type: string
        scope:
          $ref: '#/components/schemas/Scope'
          description: Scope names the tenant these numbers belong to.
        series:
          description: Series is one point per bucket, oldest first, with empty buckets
            zero-filled.
          items:
            $ref: '#/components/schemas/UsagePoint'
          type: array
        source:
          description: Source is the warehouse table the series read.
          type: string
        start:
          description: Start is the window's inclusive lower bound, RFC3339 UTC.
          type: string
      type: object
    Tool:
      properties:
        activated:
          description: |-
            Activated is filled by the registry from the activation store for the
            requesting (org,project); providers leave it zero. An unactivated tool is
            discoverable but refused 403 at dispatch.
          type: boolean
        description:
          description: Description is the prose a model reads to decide whether to
            call the tool.
          type: string
        dispatchable:
          description: |-
            Dispatchable is whether the tool can be CALLED. False for a listing-only
            entry: a skill is activated and attached to an agent, never called.
          type: boolean
        inputSchema:
          description: |-
            Schema is the JSON Schema of the call arguments — the MCP inputSchema.
            Absent for a tool that takes none.
        name:
          description: |-
            Name is the tool's id in the flat, fleet-wide tool namespace — the value a
            tools/call passes. Unique across sources: a collision is resolved by source
            precedence before the caller ever sees it.
          type: string
        price:
          $ref: '#/components/schemas/Price'
          description: |-
            Price is what a call costs and who is paid, absent for a free tool.
            Enforcement is the x402 settlement seam; this is the declaration.
        source:
          description: |-
            Source is where the tool comes from: connector, function, zap-service,
            agent, skill or mcp.
          type: string
      type: object
    Top:
      properties:
        end:
          description: End is the window's exclusive upper bound, RFC3339 UTC.
          type: string
        models:
          $ref: '#/components/schemas/TopModels'
          description: Models ranks the window's LLM models by spend — real per-org
            data.
        products:
          $ref: '#/components/schemas/TopProducts'
          description: Products ranks the window's products by revenue.
        range:
          description: 'Range is the window that was actually applied: 24h, 7d, 30d
            or custom.'
          type: string
        scope:
          $ref: '#/components/schemas/Scope'
          description: Scope names the tenant these rankings belong to.
        start:
          description: Start is the window's inclusive lower bound, RFC3339 UTC.
          type: string
        topPages:
          $ref: '#/components/schemas/Breakdown'
          description: Pages ranks the paths visitors requested, by pageviews.
        topReferrers:
          $ref: '#/components/schemas/Breakdown'
          description: Referrers ranks the external domains visitors arrived from,
            by pageviews.
        topSources:
          $ref: '#/components/schemas/Breakdown'
          description: Sources ranks the utm_source campaigns visitors arrived on,
            by pageviews.
      type: object
    TopModels:
      properties:
        available:
          description: Available is true whenever the ledger answered, including with
            no rows.
          type: boolean
        items:
          description: Items is the ranked models, highest spend first.
          items:
            $ref: '#/components/schemas/ModelRow'
          type: array
        source:
          description: Source is the warehouse table the lens read.
          type: string
      type: object
    TopProducts:
      properties:
        available:
          description: Available is false when the product-event table could not be
            read.
          type: boolean
        items:
          description: Items is the ranked products, highest revenue first. Empty
            rather than absent.
          items:
            $ref: '#/components/schemas/ProductRow'
          type: array
        reason:
          description: Reason says why the lens is unavailable. Omitted when it is
            available.
          type: string
        source:
          description: Source is the warehouse table the lens read.
          type: string
      type: object
    TotalView:
      properties:
        confidence:
          description: |-
            Confidence says how much the counters mean; a percentage-only meter leaves
            them at zero.
          type: string
        costCents:
          description: |-
            CostCents is the row's cost in US cents. For an "account" row this is the
            PROVIDER's own charge, not a Hanzo one.
          type: integer
        provider:
          description: Provider is the upstream the usage was measured against.
          type: string
        requests:
          description: Requests is how many requests the row covers.
          type: integer
        scope:
          description: |-
            Scope is whose row it is: "user" for the caller's own linked accounts,
            "org" for the whole tenant's Hanzo-routed usage.
          type: string
        source:
          description: |-
            Source is where the row came from: "account" is the provider's own meter
            on the caller's linked account, "hanzo" is Hanzo-routed inference. The two
            are never summed.
          type: string
        tokens:
          description: Tokens is the total tokens the row covers.
          type: integer
        usedPct:
          description: |-
            UsedPct is how much of a plan window the row consumed, 0–100. It is a
            share, never money.
          type: number
        window:
          description: Window is the meter window class the row rolls up, when it
            has one.
          type: string
        windows:
          description: Windows is how many window instances rolled up into the row.
          type: integer
      type: object
    TrafficCaller:
      properties:
        action:
          description: Action is the verdict currently held against it, if any.
          type: string
        cred:
          description: |-
            Cred is the caller's key: a credential fingerprint (a per-process one-way
            digest, not a key) for a validated caller, and "ip:<addr>" for one that
            presented no credential we could validate.
          type: string
        failures:
          description: Failures is how many ended 401 or 403.
          type: integer
        held_until:
          description: HeldUntil is when the held verdict lapses, unix seconds.
          type: integer
        paths:
          description: Paths is the approximate number of distinct paths it touched
            (max 64).
          type: integer
        reason:
          description: Reason is why that verdict was reached.
          type: string
        requests:
          description: Requests is its request count in the window.
          type: integer
      type: object
    TrafficView:
      properties:
        blind:
          description: |-
            Blind is how many requests in the window carried no identity to attribute
            them to — no validated credential and no client address. Non-zero on a
            public plane means the client address is not reaching this process (a TCP
            load balancer with no PROXY protocol in front of it, typically), so this
            scope's callers cannot be told apart and nothing can be held against them.
          type: integer
        callers:
          description: |-
            Callers is the scope's busiest callers this window. A credentialed caller
            appears as a FINGERPRINT — a per-process one-way digest: enough to recognise
            the same caller across requests, never enough to reconstruct the credential.
          items:
            $ref: '#/components/schemas/TrafficCaller'
          type: array
        ceiling:
          description: Ceiling is the most callers this scope may hold at once.
          type: integer
        denied:
          description: Denied is how many of them the gate refused.
          type: integer
        lanes:
          additionalProperties:
            type: integer
          description: |-
            Lanes is the request count per lane — agent, human, bot, unknown. This is
            the split that separates a customer's automation from a scraper.
          type: object
        mode:
          description: |-
            Mode is the abuse gate's posture for this scope: "shadow" records the scorer's
            action without enforcing it, "live" enforces it.
          type: string
        org:
          description: |-
            Org is the scope this view was taken for — the validated principal's own,
            never a value the caller supplied. Empty names the anonymous lane, the one
            scope that has no tenant.
          type: string
        refused:
          description: Refused is how many callers this scope's ceilings turned away
            in the window.
          type: integer
        requests:
          description: Requests is how many requests this scope made in the window.
          type: integer
        screens:
          description: |-
            Screens is how many of them were put to the scorer — the billable unit of
            the risk product. Counted from the first request, whatever the SKU costs.
          type: integer
        strain:
          description: |-
            Strain is what this scope's ceilings are doing: "clear" below them, "full"
            at them, "refuse" once a caller has been turned away inside this window —
            which means that caller is UNMEASURED and the numbers here are a sample
            rather than a census. It is reported rather than logged because the
            alternative — a bound that degrades a scope silently — is the failure this
            design exists to rule out. No other scope can move it.
          type: string
        tracked:
          description: |-
            Tracked is how many callers this scope holds state for right now, and
            Ceiling is the most it may hold. Tracked == Ceiling is the fact a bound
            that binds cannot hide.
          type: integer
        unscored:
          description: |-
            Unscored is how many of those screens got NO answer — the scorer was absent,
            stuck, slow, erroring or silent. An unanswered screen allows ordinary
            traffic, so this is the number that separates "a quiet day" from "the judge
            stopped answering and nothing said so".
          type: integer
        window_sec:
          description: WindowSec is the span the counts cover, in seconds.
          type: integer
      type: object
    TreasuryReport:
      properties:
        accruedCents:
          description: lifetime revenue-share into the fund
          type: integer
        byProgramCents:
          additionalProperties:
            type: integer
          description: program → lifetime paid
          type: object
        paidCents:
          description: lifetime backed payouts out of the fund
          type: integer
        policy:
          $ref: '#/components/schemas/SharePolicy'
          description: current revenue-share policy
        reserveCents:
          description: fund:reserve balance (available now)
          type: integer
        solventForPayout:
          description: 'reserve > 0: at least some payout is backable'
          type: boolean
      type: object
    TreeEntry:
      properties:
        lang:
          type: string
        path:
          type: string
        symbols:
          type: integer
      type: object
    UTM:
      properties:
        campaign:
          type: string
        content:
          type: string
        medium:
          type: string
        source:
          type: string
        term:
          type: string
      type: object
    Upstream:
      properties:
        backends:
          description: 'Backends are the upstream servers to balance across: 1..32
            of them.'
          items:
            $ref: '#/components/schemas/Backend'
          type: array
        id:
          description: |-
            ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.
            A create that omits it gets a generated one. Routes reference it by this id.
          type: string
        passHostHeader:
          description: |-
            PassHostHeader forwards the client's original Host header upstream instead
            of rewriting it to the backend's.
          type: boolean
      type: object
    UsagePoint:
      properties:
        requests:
          description: Requests is how many LLM calls fell in this bucket.
          type: integer
        spendCents:
          description: SpendCents is what they cost, in cents.
          type: integer
        t:
          description: T is the bucket's start, RFC3339 UTC, aligned to the interval.
          type: string
        tokens:
          description: Tokens is prompt plus completion tokens over those calls.
          type: integer
      type: object
    Variant:
      properties:
        framework:
          description: only when it differs from the template's
          type: string
        id:
          description: selector, unique within the template ("react", "grid-3-fluid")
          type: string
        kind:
          description: 'the axis it varies: format | page | theme'
          type: string
        label:
          description: human label for the picker
          type: string
        source:
          description: the repository this shape is forked from; the synthesized default
            shape carries the template's own
          type: string
      type: object
    Verdict:
      properties:
        flags:
          items:
            $ref: '#/components/schemas/DriftFlag'
          type: array
        severity:
          type: string
      type: object
    WebOverview:
      properties:
        available:
          description: |-
            Available is false when the product-event table could not be read — the lens is
            reported missing rather than as zeros that look like real traffic.
          type: boolean
        pageviews:
          description: Pageviews is how many $pageview events landed in the window.
          type: integer
        reason:
          description: Reason says why the lens is unavailable. Omitted when it is
            available.
          type: string
        sessions:
          description: Sessions is how many distinct visits they span.
          type: integer
        source:
          description: Source is the warehouse table the lens read.
          type: string
        visitors:
          description: Visitors is how many distinct people those pageviews came from.
          type: integer
      type: object
    Wire:
      properties:
        action:
          type: string
        authMethod:
          type: string
        email:
          type: string
        hash:
          type: string
        home:
          description: |-
            Home is present ONLY on a cross-org action: the org the actor came FROM,
            while Org is the org they acted IN. A console row carrying `home` is a
            platform-admin impersonation and should be rendered as one.
          type: string
        isAdmin:
          type: boolean
        method:
          type: string
        org:
          type: string
        path:
          type: string
        prevHash:
          type: string
        reason:
          type: string
        requestId:
          type: string
        resource:
          type: string
        resourceId:
          type: string
        result:
          type: string
        seq:
          type: integer
        sourceIp:
          type: string
        status:
          type: integer
        sub:
          type: string
        time:
          type: string
        userAgent:
          type: string
      type: object
    WorkerScriptPut:
      properties:
        bindings: {}
        compatibilityDate:
          type: string
        compatibilityFlags:
          items:
            type: string
          type: array
        mainModule:
          type: string
        script:
          type: string
      type: object
    WriteIn:
      properties:
        data:
          contentEncoding: base64
          description: Data is the file's bytes, and replaces whatever was there.
          type: string
        id:
          description: ID is the sandbox to write into, from an earlier lease.
          type: string
        path:
          description: |-
            Path is confined the same way PathIn.Path is. Missing parent directories are
            created.
          type: string
      type: object
    Wrote:
      properties:
        bytes:
          type: integer
        path:
          type: string
      type: object
    accountView:
      properties:
        address:
          description: Address is the ledger account address ("org:acme:wallet", "fund:reserve",
            …).
          type: string
        balanceCents:
          description: BalanceCents is that account's signed balance in minor units.
          type: integer
      type: object
    accounts:
      properties:
        accounts:
          items:
            $ref: '#/components/schemas/RoutedUsage'
          type: array
        scope:
          type: string
        source:
          type: string
        total:
          $ref: '#/components/schemas/AccountsTotal'
      type: object
    accountsOut:
      properties:
        accounts:
          description: Accounts are the ledger accounts in scope with their balances.
          items:
            $ref: '#/components/schemas/accountView'
          type: array
        scope:
          description: 'Scope is the scope actually served: "org" or "house".'
          type: string
        tenant:
          description: Tenant is the org whose accounts these are (empty for the house
            scope's own rows).
          type: string
      type: object
    activationReq:
      properties:
        activate:
          description: Activate switches these tool names on for the caller's org
            and project.
          items:
            type: string
          type: array
        deactivate:
          description: Deactivate switches these tool names off.
          items:
            type: string
          type: array
      type: object
    activationSet:
      properties:
        enabled:
          description: Enabled is every tool name activated for the caller's org and
            project.
          items:
            type: string
          type: array
      type: object
    activityFeed:
      properties:
        activity:
          description: Activity is the merged run/create/update events, newest first,
            capped at 50.
          items:
            $ref: '#/components/schemas/activityView'
          type: array
      type: object
    activityOut:
      properties:
        data:
          description: 'Data is the change log newest-first: who created, updated
            or deleted which key, when.'
          items:
            $ref: '#/components/schemas/ActivityRow'
          type: array
      type: object
    activityView:
      properties:
        agent:
          description: agent name
          type: string
        at:
          description: RFC3339 UTC
          type: string
        id:
          type: string
        kind:
          description: invoked|failed|created|updated (from real events)
          type: string
        message:
          type: string
      type: object
    addDomainReq:
      properties:
        app:
          description: App is the application's slug, from the path.
          type: string
        host:
          description: Host is the hostname to attach. Required, and must be a valid
            DNS hostname.
          type: string
        project:
          description: Project is the project the application lives under, from the
            path.
          type: string
      type: object
    agentBinding:
      properties:
        agentName:
          type: string
        botVersion:
          type: string
        createdTime:
          type: string
        machineId:
          type: string
        message:
          type: string
        name:
          type: string
        org:
          type: string
        owner:
          type: string
        provider:
          type: string
        publicIp:
          type: string
        status:
          type: string
        updatedTime:
          type: string
      type: object
    agentDetail:
      properties:
        computeRef:
          type: string
        createdAt:
          type: string
        description:
          type: string
        executionMode:
          type: string
        id:
          type: string
        instructions:
          type: string
        model:
          type: string
        name:
          type: string
        recentRuns:
          items:
            $ref: '#/components/schemas/agentRunView'
          type: array
        runs:
          type: integer
        schedule:
          type: string
        serviceAccountId:
          type: string
        status:
          type: string
        tools:
          items:
            type: string
          type: array
        updatedAt:
          type: string
      type: object
    agentList:
      properties:
        agents:
          description: Agents is the org's agents, each carrying its recorded run
            count.
          items:
            $ref: '#/components/schemas/agentView'
          type: array
      type: object
    agentRunView:
      properties:
        actor:
          type: string
        agent:
          description: |-
            What an operator needs to answer "what ran, for whom, and what did it do" —
            and, through traceId, to leave this record for the waterfall of the very
            same run rather than a search that hopefully lands near it.

            Agent is on the row because the org-wide feed lists runs across agents, and
            a run that cannot name its agent is an orphan in exactly the view built to
            make sense of many of them. Every field is omitempty: a run recorded before
            these columns existed reports absence rather than a zero it never measured.
          type: string
        completionTokens:
          type: integer
        createdAt:
          type: string
        durationMs:
          type: integer
        error:
          type: string
        id:
          type: string
        input:
          type: string
        model:
          type: string
        output:
          type: string
        promptTokens:
          type: integer
        status:
          type: string
        toolCalls:
          type: integer
        traceId:
          type: string
      type: object
    agentView:
      properties:
        computeRef:
          type: string
        createdAt:
          type: string
        description:
          type: string
        executionMode:
          type: string
        id:
          type: string
        model:
          type: string
        name:
          type: string
        runs:
          type: integer
        schedule:
          type: string
        serviceAccountId:
          type: string
        status:
          type: string
        tools:
          items:
            type: string
          type: array
        updatedAt:
          type: string
      type: object
    aiMCPApp:
      properties:
        name:
          description: Name is the subsystem, as the manifest names it.
          type: string
        served:
          description: |-
            Served reports that THIS process mounted it, so its tools are on this
            process's door rather than behind a sibling this process only knows the name
            of.
          type: boolean
      type: object
    aiMCPSurface:
      properties:
        apps:
          description: Apps is one row per subsystem this deployment composes, in
            manifest order.
          items:
            $ref: '#/components/schemas/aiMCPApp'
          type: array
        names:
          description: |-
            Names are this process's own tool names, present only when the query asked
            for them.
          items:
            type: string
          type: array
        tools:
          description: |-
            Tools is how many tools THIS PROCESS's door carries: its own typed-op
            registry, projected. It is the only number a subsystem can state honestly —
            what the FLEET's door carries is a question only the host can ask, and it
            asks it by asking every subsystem (POST /v1/mcp, tools/list).
          type: integer
      type: object
    allowlistPutIn:
      properties:
        accessGroups:
          additionalProperties:
            additionalProperties:
              items:
                type: string
              type: array
            type: object
          description: |-
            AccessGroups REPLACES the org's named access groups, as
            group name -> channel -> entries. Absent or null leaves them alone.
          type: object
        channel:
          description: |-
            Channel is the transport to edit: discord, slack, teams or telegram.
            Required; an unknown value is a 404.
          type: string
        dm:
          description: |-
            DM REPLACES the config-managed DM allow entries. Absent or null leaves them
            alone; an empty list clears them. It never touches senders approved through
            pairing — a policy edit cannot revoke an approved pairing.
          items:
            type: string
          type: array
        dmPolicy:
          description: |-
            DMPolicy sets how direct messages are admitted: "pairing" (a person must be
            approved first), "allowlist" (only listed senders) or "open". Empty leaves
            it unchanged.
          type: string
        group:
          description: |-
            Group REPLACES the config-managed group allow entries. Absent or null
            leaves them alone; an empty list clears them.
          items:
            type: string
          type: array
        groupPolicy:
          description: |-
            GroupPolicy sets how group and thread rooms are admitted: "open",
            "allowlist" or "disabled". Empty leaves it unchanged.
          type: string
      type: object
    allowlistView:
      properties:
        accessGroups:
          additionalProperties:
            additionalProperties:
              items:
                type: string
              type: array
            type: object
          description: |-
            AccessGroups is the org's named sender sets, as group name -> channel ->
            member entries, held once for the whole org. A DM or Group entry written
            `accessGroup:<name>` admits any sender listed under that name for THIS
            channel, or under the channel `*`, which is how one set covers all four
            transports. Replaced wholesale by the PUT.
          type: object
        dm:
          description: |-
            DM is the CONFIG-managed DM allow entries — the list PUT
            /v1/channels/allowlist owns and replaces wholesale. An entry matches a sender
            either EXACTLY, as the transport-native id inbox messages carry, or as
            `accessGroup:<name>` resolved through AccessGroups. A bare `*` admits
            everyone, but only while DMPolicy is "open": it is gate syntax, not an
            identity, so under "allowlist" it matches nobody.
          items:
            type: string
          type: array
        dmPolicy:
          description: |-
            DMPolicy decides every inbound DIRECT message, defaulting to "pairing" when
            the org has never set one. "pairing": a sender with no entry is sent a
            pairing code and the message is DROPPED — it never reaches the inbox — and
            they are admitted only once an admin approves. "allowlist": only DM admits,
            and Paired senders are suspended, since a pairing grant counts under
            "pairing" alone. "open" is not unconditional either — it still requires `*`
            or a matching entry in DM.
          type: string
        group:
          description: |-
            Group is the CONFIG-managed group allow entries, consulted only while
            GroupPolicy is "allowlist". Entries match the same two ways as DM, and here a
            bare `*` admits every sender in the room.
          items:
            type: string
          type: array
        groupPolicy:
          description: |-
            GroupPolicy decides every inbound GROUP or THREAD message — a thread is a
            group surface — defaulting to "open". "open" admits every sender in the room.
            "allowlist" admits only what Group lists, so an EMPTY Group blocks the
            channel's group rooms outright. "disabled" drops all of them.
          type: string
        paired:
          description: |-
            Paired is the senders admitted by PAIRING — the entries POST
            /v1/channels/pairing/approve minted, DM scope only. READ-ONLY on this
            endpoint: the PUT writes config entries and can never revoke one of these
            (listing a paired sender under DM instead promotes that entry to config,
            which the admin then owns). They admit only while DMPolicy is "pairing".
          items:
            type: string
          type: array
      type: object
    apiKey:
      properties:
        createdAt:
          description: CreatedAt is when the key last changed, as IAM records it.
          type: string
        key:
          description: |-
            Key is the FULL value, and is present for a publishable key only: it is
            public by construction and useless to its holder if it cannot be read back.
          type: string
        limit:
          description: |-
            Limit is what this key may reach, as `kind:name` entries — `model:zen5`,
            `project:acme`, `product:commerce`. Absent means the key reaches whatever
            its holder does, which is what every key minted before limits existed does
            and must keep doing.
          items:
            type: string
          type: array
        prefix:
          description: |-
            Prefix is the recognizable, non-secret head of the key — enough to tell two
            keys apart, never enough to use one.
          type: string
        type:
          description: 'Type is the key class: secret (sk-) or publishable (pk-).'
          type: string
      type: object
    apiKeyList:
      properties:
        keys:
          description: Keys is every key the caller holds, at most one per type.
          items:
            $ref: '#/components/schemas/apiKey'
          type: array
      type: object
    appView:
      properties:
        buildType:
          type: string
        createdAt:
          type: integer
        currentDeploymentId:
          type: string
        description:
          type: string
        dockerfile:
          type: string
        domains:
          items:
            type: string
          type: array
        env:
          items:
            $ref: '#/components/schemas/EnvVarJSON'
          type: array
        environment:
          type: string
        health:
          type: string
        id:
          type: string
        image:
          $ref: '#/components/schemas/imageView'
        name:
          type: string
        namespace:
          type: string
        org:
          type: string
        phase:
          type: string
        port:
          type: integer
        projectId:
          type: string
        replicas:
          type: integer
        repo:
          $ref: '#/components/schemas/gitSource'
        secretSync:
          description: '""|pending|syncing|ready|failed (secrets.go)'
          type: string
        secretSyncDetail:
          description: honest reason when not ready
          type: string
        slug:
          type: string
        source:
          type: string
        status:
          type: string
        storageGb:
          description: GiB; absent means stateless
          type: integer
        updatedAt:
          type: integer
      type: object
    appearance:
      properties:
        accent:
          description: |-
            Accent is the one hue — a CSS colour token (a hex, or a bounded functional
            colour like rgb()/oklch()). Anything else is dropped rather than stored.
          type: string
        density:
          description: 'Density is the spacing step: "compact", "default" or "comfortable".'
          type: string
        type:
          description: |-
            Type is the text-size multiplier, clamped to the ramp window [0.85, 1.4].
            Absent (0) leaves the published default.
          type: number
      type: object
    approvePairingIn:
      properties:
        channel:
          description: 'Channel is the transport the request came in on: discord,
            slack, teams or telegram.'
          type: string
        code:
          description: |-
            Code is the pairing code from GET /v1/channels/pairing. It is a capability:
            holding it is what authorises the approval, alongside org admin.
          type: string
      type: object
    argoApp:
      properties:
        apiVersion:
          type: string
        kind:
          type: string
        metadata:
          $ref: '#/components/schemas/argoMeta'
        spec:
          $ref: '#/components/schemas/argoSpec'
        status:
          $ref: '#/components/schemas/argoStatus'
      type: object
    argoAppList:
      properties:
        apiVersion:
          type: string
        items:
          items:
            $ref: '#/components/schemas/argoApp'
          type: array
        kind:
          type: string
        metadata:
          $ref: '#/components/schemas/argoListMeta'
      type: object
    argoCluster:
      properties:
        connectionState:
          $ref: '#/components/schemas/argoConnectionState'
        info:
          $ref: '#/components/schemas/argoClusterInfo'
        name:
          type: string
        server:
          type: string
      type: object
    argoClusterInfo:
      properties:
        applicationsCount:
          type: integer
        connectionState:
          $ref: '#/components/schemas/argoConnectionState'
        serverVersion:
          type: string
      type: object
    argoClusterList:
      properties:
        items:
          items:
            $ref: '#/components/schemas/argoCluster'
          type: array
        metadata:
          $ref: '#/components/schemas/argoListMeta'
      type: object
    argoConnectionState:
      properties:
        attemptedAt:
          type: string
        message:
          type: string
        status:
          type: string
      type: object
    argoDestination:
      properties:
        name:
          description: ArgoCD allows a destination by cluster name; omitted for the
            in-cluster projection.
          type: string
        namespace:
          type: string
        server:
          type: string
      type: object
    argoGroupKind:
      properties:
        group:
          type: string
        kind:
          type: string
      type: object
    argoHealth:
      properties:
        message:
          type: string
        status:
          type: string
      type: object
    argoInfoItem:
      properties:
        name:
          type: string
        value:
          type: string
      type: object
    argoListMeta:
      properties:
        resourceVersion:
          type: string
      type: object
    argoMeta:
      properties:
        creationTimestamp:
          type: string
        labels:
          additionalProperties:
            type: string
          type: object
        name:
          type: string
        namespace:
          type: string
        uid:
          type: string
      type: object
    argoNode:
      properties:
        createdAt:
          type: string
        group:
          type: string
        health:
          $ref: '#/components/schemas/argoHealth'
        images:
          items:
            type: string
          type: array
        info:
          items:
            $ref: '#/components/schemas/argoInfoItem'
          type: array
        kind:
          type: string
        name:
          type: string
        namespace:
          type: string
        parentRefs:
          items:
            $ref: '#/components/schemas/argoResourceRef'
          type: array
        resourceVersion:
          type: string
        uid:
          type: string
        version:
          type: string
      type: object
    argoProject:
      properties:
        apiVersion:
          type: string
        kind:
          type: string
        metadata:
          $ref: '#/components/schemas/argoMeta'
        spec:
          $ref: '#/components/schemas/argoProjectSpec'
        status:
          $ref: '#/components/schemas/argoProjectStat'
      type: object
    argoProjectList:
      properties:
        items:
          items:
            $ref: '#/components/schemas/argoProject'
          type: array
        metadata:
          $ref: '#/components/schemas/argoListMeta'
      type: object
    argoProjectSpec:
      properties:
        clusterResourceWhitelist:
          items:
            $ref: '#/components/schemas/argoGroupKind'
          type: array
        description:
          type: string
        destinations:
          items:
            $ref: '#/components/schemas/argoDestination'
          type: array
        sourceRepos:
          items:
            type: string
          type: array
      type: object
    argoProjectStat:
      properties: {}
      type: object
    argoResourceRef:
      properties:
        group:
          type: string
        kind:
          type: string
        name:
          type: string
        namespace:
          type: string
        uid:
          type: string
        version:
          type: string
      type: object
    argoResourceStatus:
      properties:
        group:
          type: string
        health:
          $ref: '#/components/schemas/argoHealth'
        kind:
          type: string
        name:
          type: string
        namespace:
          type: string
        status:
          type: string
        version:
          type: string
      type: object
    argoRevisionMetadata:
      properties:
        author:
          type: string
        date:
          type: string
        message:
          type: string
        signatureInfo:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    argoSource:
      properties:
        path:
          type: string
        repoURL:
          type: string
        targetRevision:
          type: string
      type: object
    argoSpec:
      properties:
        destination:
          $ref: '#/components/schemas/argoDestination'
        project:
          type: string
        source:
          $ref: '#/components/schemas/argoSource'
      type: object
    argoStatus:
      properties:
        health:
          $ref: '#/components/schemas/argoHealth'
        reconciledAt:
          type: string
        resources:
          items:
            $ref: '#/components/schemas/argoResourceStatus'
          type: array
        summary:
          $ref: '#/components/schemas/argoSummary'
        sync:
          $ref: '#/components/schemas/argoSyncStatus'
      type: object
    argoSummary:
      properties:
        images:
          items:
            type: string
          type: array
      type: object
    argoSyncStatus:
      properties:
        revision:
          type: string
        status:
          type: string
      type: object
    argoSyncWindows:
      properties:
        activeWindows:
          items:
            type: object
          type: array
        assignedWindows:
          items:
            type: object
          type: array
        canSync:
          type: boolean
      type: object
    argoTree:
      properties:
        hosts:
          items:
            type: object
          type: array
        nodes:
          items:
            $ref: '#/components/schemas/argoNode'
          type: array
        orphanedNodes:
          items:
            $ref: '#/components/schemas/argoNode'
          type: array
      type: object
    askPostIn:
      properties:
        query:
          description: Query is the question, from the BODY. Takes precedence over
            `?q=`.
          type: string
        repo:
          description: Repo is the repository narrowing, from the BODY. Takes precedence
            over `?repo=`.
          type: string
      type: object
    askRequest:
      properties:
        followUps:
          type: boolean
        language:
          type: string
        maxQueries:
          type: integer
        maxSources:
          type: integer
        mode:
          type: string
        model:
          type: string
        q:
          type: string
        question:
          type: string
        sources:
          items:
            type: string
          type: array
        stream:
          type: boolean
        system:
          type: string
      type: object
    authoredPluginList:
      properties:
        plugins:
          description: |-
            Plugins is every plugin this org built, newest first, each carrying the
            TypeScript as authored. The bundled artifact is never rendered.
          items:
            $ref: '#/components/schemas/AuthoredPlugin'
          type: array
      type: object
    authoredSkillList:
      properties:
        skills:
          description: Skills is every skill this org authored, each with its SKILL.md
            content.
          items:
            $ref: '#/components/schemas/Skill'
          type: array
      type: object
    authorizeOut:
      properties:
        authorizeUrl:
          description: AuthorizeURL is the provider consent (or bot deep-link) URL.
          type: string
      type: object
    baseHealth:
      properties:
        service:
          description: Service is "base" — which subsystem answered.
          type: string
        status:
          description: Status is "ok" when the subsystem is serving.
          type: string
      type: object
    binarySpec:
      properties:
        image:
          type: string
        ldflags:
          type: string
        main:
          type: string
        name:
          type: string
        out:
          type: string
        platforms:
          items:
            type: string
          type: array
        run:
          type: string
      type: object
    bindAgentReq:
      properties:
        agentName:
          description: AgentName is the cloud Agent (/v1/agents) the machine will
            run. Required.
          type: string
        botVersion:
          description: BotVersion pins the @hanzo/bot runtime version; empty takes
            the default.
          type: string
        id:
          description: ID is the machine to bind, from the URL path.
          type: string
      type: object
    bindingList:
      properties:
        agentBindings:
          description: |-
            AgentBindings is one row per bound machine, emitted verbatim as vm reports
            it.
          items:
            $ref: '#/components/schemas/agentBinding'
          type: array
      type: object
    blobJSON:
      properties:
        binary:
          description: Binary marks content git could not treat as text; it comes
            back base64.
          type: boolean
        content:
          description: Content is the file's bytes, empty when Truncated.
          type: string
        encoding:
          description: 'Encoding is how Content is carried: "utf8" verbatim, or "base64".'
          type: string
        path:
          description: Path is the file's repo-relative path.
          type: string
        size:
          description: Size is the file's byte length in the repo, whatever was returned
            below.
          type: integer
        truncated:
          description: |-
            Truncated marks a file past the 1 MiB view cap. No content is sent —
            clone the repo for it.
          type: boolean
      type: object
    blueprintHealth:
      properties:
        blueprints:
          description: Blueprints is how many blueprints this build has embedded and
            priced.
          type: integer
        rateCard:
          $ref: '#/components/schemas/RateCard'
          description: |-
            RateCard is the rate card actually in force after the operator env overlay
            (CLOUD_BLUEPRINT_UCPU_HR / CLOUD_BLUEPRINT_UGB_HR), not the shipped default.
        service:
          description: Service names the subsystem answering — always "blueprint".
          type: string
        status:
          description: Status is "ok"; the route answers 200 whenever the subsystem
            is mounted.
          type: string
      type: object
    blueprintIndex:
      properties:
        data:
          description: Data is one row per embedded blueprint, sorted by template
            id.
          items:
            $ref: '#/components/schemas/blueprintRow'
          type: array
      type: object
    blueprintRow:
      properties:
        estCentsPerMonth:
          description: |-
            CentsPerMonth is the estimated compute cost of running the whole stack for
            one month, in USD cents, from the rate card GET /v1/blueprint/health echoes.
          type: integer
        services:
          description: Services is how many compose services the stack runs.
          type: integer
        templateId:
          description: |-
            TemplateID is the blueprint slug — the id GET /v1/blueprint/sbom takes as
            ?template= and the path under templates.hanzo.ai/blueprints/<id>/.
          type: string
      type: object
    botList:
      properties:
        bots:
          description: |-
            Bots is one row per kind=bot machine, each joined with its agent binding
            when it has one.
          items:
            $ref: '#/components/schemas/botView'
          type: array
      type: object
    botMember:
      properties:
        active:
          description: |-
            Active is whether the agent projects as a LIVE workspace member, derived
            from its registry status: empty, "active" and "ready" are live, anything
            else (archived/retired) is not. An inactive bot drops out of the Team list
            while its past authorship survives.
          type: boolean
        id:
          description: the agent id
          type: string
        name:
          description: display name
          type: string
        personRef:
          description: the projected Person _id
          type: string
        userId:
          description: derived member account uuid (personUuid)
          type: string
      type: object
    botRoster:
      properties:
        bots:
          description: Bots is every agent of the caller's org, projected as a workspace
            member.
          items:
            $ref: '#/components/schemas/botMember'
          type: array
      type: object
    botSync:
      properties:
        projected:
          description: Projected is how many roster entries the reconcile touched.
          type: integer
        synced:
          description: Synced is true when the reconcile ran.
          type: boolean
      type: object
    botView:
      properties:
        agent:
          type: string
        binding:
          $ref: '#/components/schemas/agentBinding'
        createdTime:
          type: string
        gpu:
          type: string
        id:
          type: string
        image:
          type: string
        mem:
          type: string
        name:
          type: string
        os:
          type: string
        privateIp:
          type: string
        provider:
          type: string
        publicIp:
          type: string
        region:
          type: string
        status:
          type: string
        type:
          type: string
        vcpu:
          type: integer
      type: object
    browserTagOut:
      properties:
        id:
          type: string
        platform:
          type: string
        type:
          type: string
      type: object
    bucketCreateIn:
      properties:
        name:
          description: Name is the bucket name to create.
          type: string
      type: object
    bucketRecord:
      properties:
        bucket:
          description: Bucket is the bucket's name within the org.
          type: string
        history:
          description: History is how many revisions each key keeps.
          type: integer
        ttl:
          description: TTL is the entry expiry in seconds; 0 means none.
          type: integer
        values:
          description: Values is how many values the bucket holds right now.
          type: integer
      type: object
    bucketWrite:
      properties:
        bucket:
          description: |-
            Bucket is the bucket's name within the org, from the path: 1–64 of
            [A-Za-z0-9_], no dash.
          type: string
        history:
          description: History is how many revisions each key keeps, 1–64. 0 means
            1.
          type: integer
        maxValue:
          description: |-
            MaxValue caps one value's size in bytes. 0 or less means the server's
            ceiling.
          type: integer
        ttl:
          description: TTL expires entries after this many SECONDS. 0 means no expiry.
          type: integer
      type: object
    buildBoard:
      properties:
        builds:
          description: Builds are the org's real BuildKit build records, newest first.
          items:
            $ref: '#/components/schemas/buildRow'
          type: array
      type: object
    buildList:
      properties:
        builds:
          description: Builds is every published build, most recently updated first.
          items:
            $ref: '#/components/schemas/buildSummary'
          type: array
      type: object
    buildOut:
      properties:
        bytes:
          type: integer
        generated:
          type: boolean
        plugin:
          $ref: '#/components/schemas/AuthoredPlugin'
      type: object
    buildRequest:
      properties:
        name:
          type: string
        provider:
          type: string
        source:
          type: string
        spec:
          type: string
      type: object
    buildRow:
      properties:
        commit:
          description: Commit is the short git ref the build pinned.
          type: string
        duration:
          description: Duration is the wall time of a TERMINAL build; empty while
            it still runs.
          type: string
        id:
          description: ID is the build record's id.
          type: string
        repo:
          description: Repo is the repo the build built, or the image it produced.
          type: string
        startedAt:
          description: StartedAt is when the build was recorded, RFC3339 UTC.
          type: string
        status:
          description: 'Status is the build''s real state: queued, building, succeeded
            or failed.'
          type: string
      type: object
    buildSummary:
      properties:
        agent:
          type: string
        endedAt:
          type: string
        org:
          type: string
        project:
          type: string
        repo:
          type: string
        session:
          type: string
        startedAt:
          type: string
        status:
          type: string
        title:
          type: string
        turns:
          type: integer
      type: object
    buildTurn:
      properties:
        actor:
          type: string
        at:
          type: string
        body:
          type: string
        commit:
          type: string
        kind:
          type: string
        subject:
          type: string
        turn:
          type: integer
      type: object
    buildView:
      properties:
        agent:
          type: string
        endedAt:
          type: string
        model:
          type: string
        org:
          type: string
        project:
          type: string
        repo:
          type: string
        session:
          type: string
        startedAt:
          type: string
        status:
          type: string
        title:
          type: string
        turns:
          items:
            $ref: '#/components/schemas/buildTurn'
          type: array
        verify:
          description: |-
            Verify is the exact command that re-derives every commit binding below
            straight from git, so nothing here has to be taken on trust.
          type: string
      type: object
    busAck:
      properties:
        duplicate:
          description: |-
            Duplicate is true when JetStream deduplicated the message by its
            Nats-Msg-Id instead of storing it again.
          type: boolean
        ok:
          description: OK is true when the bus accepted the message.
          type: boolean
        seq:
          description: Seq is the message's sequence in that stream.
          type: integer
        stream:
          description: |-
            Stream is the stream that stored the message — absent when no stream
            captures the subject and the message went out core (fire-and-forget).
          type: string
      type: object
    busMessage:
      properties:
        data:
          description: Data is the payload as UTF-8 text.
          type: string
        headers:
          additionalProperties:
            items:
              type: string
            type: array
          description: Headers are the message's headers, when it carries any.
          type: object
        seq:
          description: Seq is the message's stream sequence — fetched messages only.
          type: integer
        subject:
          description: Subject is the message's subject in the org's own namespace.
          type: string
        time:
          description: |-
            Time is when the stream stored the message, RFC3339 — fetched messages
            only.
          type: string
      type: object
    busPublish:
      properties:
        data:
          description: |-
            Data is the payload, carried verbatim as UTF-8 text (typically JSON).
            Binary payloads belong on the NATS port.
          type: string
        headers:
          additionalProperties:
            type: string
          description: |-
            Headers are optional message headers, one value per name. A Nats-Msg-Id
            header is JetStream's deduplication key: a repeat within the stream's
            dedup window is acknowledged as duplicate rather than stored twice.
          type: object
        subject:
          description: |-
            Subject is the subject to publish to, in the org's own namespace — e.g.
            orders.created. No wildcards.
          type: string
      type: object
    busRequest:
      properties:
        data:
          description: Data is the request payload, carried verbatim as UTF-8 text.
          type: string
        headers:
          additionalProperties:
            type: string
          description: Headers are optional request headers, one value per name.
          type: object
        subject:
          description: Subject is the subject a responder listens on, in the org's
            namespace.
          type: string
        timeoutMs:
          description: |-
            TimeoutMs bounds the wait for a reply. 0 or less means the default of
            5000; anything above 30000 is clamped to 30000.
          type: integer
      type: object
    buyInput:
      properties:
        e164:
          type: string
      type: object
    byoGPU:
      properties:
        arch:
          description: native target, e.g. "gfx1151"
          type: string
        memoryTotal:
          description: VRAM (or unified pool), e.g. "122880 MiB"
          type: string
        name:
          type: string
        unified:
          description: unified CPU/GPU memory pool (APU / SoC)
          type: boolean
      type: object
    byoWorker:
      properties:
        arch:
          description: |-
            Arch/CPUs/Memory are the connecting host's static CPU spec, mirrored from the
            registration: Arch is runtime.GOARCH (amd64 | arm64), Memory is total RAM in
            BYTES — the same fields a code-linked run-target carries, so the /v1/visor/fleet
            board renders a linked node's arch + cores + RAM like any other unit.
          type: string
        capabilities:
          description: |-
            Capabilities the worker advertises ("studio.render", "engine.serve"); Engine
            is present when it runs a hanzo-engine model server. Both additive + omitempty.
          items:
            type: string
          type: array
        cpuModel:
          type: string
        cpus:
          type: integer
        cuda:
          type: string
        driver:
          type: string
        engine:
          $ref: '#/components/schemas/engineAdvertisement'
        firstSeen:
          type: string
        gpus:
          items:
            $ref: '#/components/schemas/byoGPU'
          type: array
        hip:
          type: string
        hostname:
          type: string
        id:
          type: string
        jobQueue:
          type: string
        lastHeartbeat:
          type: string
        location:
          description: '"on-prem" (BYO has no cloud region)'
          type: string
        memory:
          type: integer
        os:
          type: string
        provider:
          description: always "byo"
          type: string
        rocm:
          type: string
        status:
          description: online | offline
          type: string
        version:
          type: string
      type: object
    callInput:
      properties:
        agent:
          type: string
        from:
          type: string
        record:
          description: |-
            Record is a per-call flag rather than a product. Where a recording lands and
            how long it is kept is the org's retention policy, not this call's.
          type: boolean
        to:
          type: string
        webhook:
          type: string
      type: object
    callList:
      properties:
        data:
          items:
            $ref: '#/components/schemas/Call'
          type: array
      type: object
    capabilities:
      properties:
        actions:
          description: "Actions is whether the transport renders an INTERACTIVE control
            natively, and\nit is the flag to read before composing one. The vocabulary
            is a closed\nkind-tagged union (envelope.go), exactly four kinds, each
            carrying only its\nown field plus an optional label:\n\n\tcommand  — a
            bot command to run (`command`), rendered as a button that\n\t           invokes
            it.\n\turl      — an external link (`url`), rendered as a link button.\n\tselect
            \  — a menu (`options`, each a label and the value choosing it\n\t           returns),
            rendered as a picker.\n\tapproval — a reference to an approval request
            (`approval.id`), rendered as\n\t           approve/deny controls bound
            to that id.\n\nFalse on all four transports this pass, and nothing refuses
            a send for it:\nactions are accepted, validated per kind, and flattened
            by renderText to one\nline each after the text — `[label] command`, `[label]
            url`,\n`[label] opt | opt`, `[label] approval requested: <id>`. So a caller
            that\nneeds a real control must read this flag and degrade itself; a caller
            that\nonly needs the choice communicated can send actions and take the
            text form."
          type: boolean
        dm:
          description: |-
            DM is whether the transport carries a DIRECT message at all. True for slack,
            teams and telegram. False for discord, honestly: that ingress is guild-scoped
            slash commands — an interaction without a guild id is refused at the door —
            so nothing ever arrives classified as a DM, no reply route is ever learned
            for one, and a send addressed at a Discord DM is refused 409.
          type: boolean
        group:
          description: |-
            Group is whether the transport carries multi-person rooms — a Discord guild
            channel, a Slack channel, a Teams channel or group chat, a Telegram group or
            supergroup. True on all four.
          type: boolean
        media:
          description: |-
            Media is whether the transport renders an ATTACHMENT natively. False on all
            four this pass, and a send is not refused for it: renderText flattens each
            attachment to one `kind: url (mime)` line after the text rather than dropping
            it.
          type: boolean
        thread:
          description: |-
            Thread is whether a reply can be threaded UNDER a specific message. True for
            slack alone: it is the only transport whose ingress reports a thread
            (thread_ts, published as the envelope's replyTo) and whose door posts back
            into it. Discord's replyTo makes an inline reply rather than a thread,
            Telegram's answers one message id, and Teams carries no reply target at all —
            a replyTo sent to it is ignored.
          type: boolean
      type: object
    capturedError:
      properties:
        distinctId:
          description: |-
            DistinctID is the person/visitor the error is attributed to. Omitted when the
            row carries none.
          type: string
        event:
          description: Event is the event name the error was stored under, e.g. $error.
          type: string
        exception:
          description: |-
            Exception is the captured error itself — the {type, message, stack, handled}
            object, already redacted at the fold point. Omitted when the row has none.
        id:
          description: |-
            ID is the row's stable event id — the client's own idempotency id when it sent
            one, else the server-minted one.
          type: string
        library:
          description: Library is the client SDK that reported the error. Omitted
            when absent.
          type: string
        libraryVersion:
          description: LibraryVer is that SDK's version. Omitted when absent.
          type: string
        path:
          description: Path is the URL's path component. Omitted when absent.
          type: string
        product:
          description: Product is the surface that emitted the error. Omitted when
            absent.
          type: string
        properties:
          description: |-
            Properties is the row's whole property bag, returned verbatim as stored — any
            JSON object, $exception included. Omitted when the row carries none or it did
            not parse.
        sessionId:
          description: SessionID groups the events of one visit. Omitted when the
            client sent none.
          type: string
        timestamp:
          description: Timestamp is when the error was captured, RFC3339 UTC.
          type: string
        url:
          description: URL is the full page address the error fired on. Omitted when
            absent.
          type: string
      type: object
    catalogEntry:
      properties:
        configured:
          type: boolean
        description:
          type: string
        displayName:
          type: string
        kind:
          description: '"native" | "piece"'
          type: string
        provider:
          type: string
      type: object
    catalogList:
      properties:
        data:
          description: Data is every starter prompt, each importable as-is with POST
            /v1/prompts.
          items:
            $ref: '#/components/schemas/CatalogEntry'
          type: array
      type: object
    catalogOut:
      properties:
        connectors:
          description: Connectors is every connectable source, sorted by provider.
          items:
            $ref: '#/components/schemas/catalogEntry'
          type: array
      type: object
    catalogPage:
      properties:
        data:
          description: Data is the page of matching entries, most recently updated
            first.
          items:
            $ref: '#/components/schemas/Entry'
          type: array
        facets:
          additionalProperties:
            additionalProperties:
              type: integer
            type: object
          description: |-
            Facets counts the whole matching set along every browse axis, so a rail a
            client renders is a rail that has results behind it. Keyed axis → value → count.
          type: object
        total:
          description: Total is how many entries matched BEFORE paging — what a pager
            sizes itself on.
          type: integer
      type: object
    channelView:
      properties:
        account:
          description: |-
            Account is the id-shaped fact about that connection: the lowercased external
            id integrations custodies for it — a Discord guild id, a Slack team
            (workspace) id, a Teams AAD tenant id, or the Telegram chat the org bound.
            Empty when not connected. Informational: the access policy keys on
            (org, channel), so exactly one account is representable per pair.
          type: string
        accountLabel:
          description: |-
            AccountLabel is the human label of that same account — the Discord guild
            name, the Slack team name, the Teams tenant name (falling back to the tenant
            id), the Telegram chat title. DISPLAY ONLY: never a key, and never swapped
            with Account, on any surface.
          type: string
        capabilities:
          $ref: '#/components/schemas/capabilities'
          description: |-
            Capabilities is what this transport renders natively — read it before
            composing a message that needs threading, media or interactive actions.
        connected:
          description: |-
            Connected is whether integrations holds a connection for (this org, this
            transport) — whether someone finished its connect flow. False leaves Account
            and AccountLabel empty, and a send is then refused downstream rather than
            here: by the transport's own binding check (403 for a Telegram chat this org
            has not bound, 409 for a Discord or Teams room with no inbound-learned
            route), or on Slack by the absent per-org bot token, which surfaces as 502.
          type: boolean
        dmPolicy:
          description: |-
            DMPolicy is how this org admits direct messages here: "pairing", "allowlist"
            or "open", defaulting to "pairing" when the org has never set one.
          type: string
        groupPolicy:
          description: |-
            GroupPolicy is how this org admits group and thread rooms here: "open",
            "allowlist" or "disabled", defaulting to "open". Both policy fields come
            back EMPTY — rather than the listing failing — when the policy cannot be
            read; GET /v1/channels/allowlist carries the same two with the entries they
            consult.
          type: string
        id:
          description: |-
            ID is the fixed transport identifier — discord, slack, teams or telegram —
            and the value every route on this surface names a channel by, including the
            `:channel` segment of the send path. The listing is always in that order.
          type: string
        pendingPairing:
          description: |-
            PendingPairing counts the org's UNEXPIRED pairing requests on this channel:
            exactly the rows GET /v1/channels/pairing returns for it, one per person
            waiting on an admin. It never exceeds three — the pending cap per
            (org, channel) — and expired requests are not counted.
          type: integer
      type: object
    chatChannels:
      properties:
        channels:
          description: |-
            Channels is every chat transport this deployment supports, in a fixed
            order, each carrying whether the org has connected it, the account behind
            the connection, what the transport can do, the org's DM/group access
            policies for it, and how many pairing requests are waiting.
          items:
            $ref: '#/components/schemas/channelView'
          type: array
      type: object
    claimKeyOut:
      properties:
        claimKey:
          description: |-
            ClaimKey is the capability itself. It is returned ONCE and never again — only
            its SHA-256 hash is stored — so a daemon that loses it mints a new one.
          type: string
        targetId:
          description: TargetID is the machine the key authenticates.
          type: string
      type: object
    clusterAttach:
      properties:
        default:
          description: Default marks this the org's default cluster for scheduling.
          type: boolean
        kubeconfig:
          description: |-
            Kubeconfig is the cluster's kubeconfig, verbatim. Required — a body without
            one is not an attach.
          type: string
        name:
          description: |-
            Name is the fleet-local name for the cluster; lower-cased, and the key the
            detach route addresses it by. Required.
          type: string
        provider:
          description: |-
            Provider is a free-form label for where the cluster runs ("gke", "on-prem");
            it is display only, not a routing key.
          type: string
      type: object
    clusterDetached:
      properties:
        detached:
          description: Detached is the lower-cased fleet name that was removed.
          type: string
      type: object
    clusterDetailView:
      properties:
        amdGpu:
          type: integer
        createdAt:
          type: string
        doClusterId:
          type: string
        doksClusterId:
          type: string
        kind:
          type: string
        name:
          type: string
        nodeCount:
          type: integer
        nodePools:
          items:
            $ref: '#/components/schemas/nodePoolView'
          type: array
        nodeSize:
          type: string
        nodes:
          items:
            $ref: '#/components/schemas/machineView'
          type: array
        nvidiaGpu:
          type: integer
        region:
          type: string
        status:
          type: string
      type: object
    clusterList:
      properties:
        clusters:
          description: |-
            Clusters is the merged fleet — kind "managed" for Visor-provisioned, "byo"
            for an attached kubeconfig.
          items:
            $ref: '#/components/schemas/clusterView'
          type: array
        degraded:
          description: |-
            Degraded names any source that did not answer, so an empty Clusters means
            "you have none" only when this is absent. Omitted when everything answered,
            so a healthy response is unchanged. See degraded.go.
          items:
            $ref: '#/components/schemas/sourceFailure'
          type: array
      type: object
    clusterView:
      properties:
        amdGpu:
          type: integer
        createdAt:
          type: string
        doClusterId:
          type: string
        doksClusterId:
          type: string
        kind:
          description: |-
            Fleet fields (additive): "managed" (Visor-provisioned) vs "byo" (attached
            kubeconfig), and the live GPU inventory a BYO cluster reports.
          type: string
        name:
          type: string
        nodeCount:
          type: integer
        nodePools:
          items:
            $ref: '#/components/schemas/nodePoolView'
          type: array
        nodeSize:
          type: string
        nvidiaGpu:
          type: integer
        region:
          type: string
        status:
          type: string
      type: object
    collabPayload:
      properties:
        content:
          additionalProperties:
            type: string
          description: Content maps a document field to its ProseMirror markup JSON.
          type: object
        source:
          description: |-
            Source is the blob ref a getContent reads the snapshot from. Absent means
            there is no snapshot to read, which answers empty content.
          type: string
        updates:
          additionalProperties:
            type: string
          description: |-
            Updates carries, per field, a base64 Y.js state update encoding the SAME
            markup — the front computes it (markupToYDoc → encodeStateAsUpdate) so a
            createContent seeds the live-editing lane's update log, not just the
            snapshot blob. Without it a dialog-created description is invisible in the
            collaborative editor, which replays the ydoc log, never the snapshot.
          type: object
      type: object
    collabRequest:
      properties:
        documentId:
          description: |-
            DocumentID addresses the document field, as
            "<workspaceUuid>|<objectClass>|<objectId>|<objectAttr>" — the
            collaborator-client encodeDocumentId shape, from the path.
          type: string
        method:
          description: 'Method is the verb: createContent, updateContent or getContent.'
          type: string
        payload:
          $ref: '#/components/schemas/collabPayload'
          description: Payload is the verb's argument.
      type: object
    collabResult:
      properties:
        content:
          additionalProperties:
            type: string
          description: |-
            Content maps each document field to its value for the verb: the new blob
            ref after a createContent, the stored markup after a getContent.
          type: object
        error:
          description: |-
            Error carries a SEMANTIC refusal, which this RPC reports under 200 because
            the client throws on result.error — auth and tenancy failures are HTTP
            statuses instead.
          type: string
      type: object
    commitJSON:
      properties:
        authorEmail:
          description: AuthorEmail is the commit author's email.
          type: string
        authorName:
          description: AuthorName is the commit author's name.
          type: string
        date:
          description: Date is the author date, RFC 3339 UTC.
          type: string
        message:
          description: Message is the commit's SUBJECT — its first line only.
          type: string
        sha:
          description: SHA is the full commit hash.
          type: string
        shortSha:
          description: ShortSHA is the abbreviated hash a UI displays.
          type: string
      type: object
    commitsJSON:
      properties:
        commits:
          description: Commits are newest first.
          items:
            $ref: '#/components/schemas/commitJSON'
          type: array
      type: object
    connView:
      properties:
        account:
          description: Account is the provider's label for the connected account.
          type: string
        connectedAt:
          description: ConnectedAt is when the connector was last (re)established,
            RFC 3339 UTC.
          type: string
        expiresAt:
          description: |-
            ExpiresAt is when the access token expires, RFC 3339 UTC; empty for a
            non-expiring credential. Reading the token auto-rotates inside the window.
          type: string
        externalId:
          description: ExternalID is the provider's own id for that account.
          type: string
        id:
          description: ID is provider + ":" + label — what every other connector route
            addresses.
          type: string
        label:
          description: Label is the caller's name for this connection ("default",
            "work").
          type: string
        provider:
          description: Provider is the user-scoped provider's registry id.
          type: string
        scopes:
          description: Scopes are the permissions the credential carries. Never null;
            [] when none.
          items:
            type: string
          type: array
      type: object
    connectIn:
      properties:
        accountId:
          description: |-
            AccountID is the provider account the credential should be scoped to, for the
            providers whose Verify needs one (Cloudflare). Ignored by the OAuth path.
          type: string
        provider:
          description: Provider is the connector's registry id, from the :provider
            path segment.
          type: string
        token:
          description: |-
            Token is the customer's provider credential. Its PRESENCE — not its value —
            is what selects the apikey seal over the OAuth flow for a provider that
            offers both: {"token":"…"}, even empty, is an apikey attempt (→ verify, which
            answers the "token required" 400 on an empty value), while a body with no
            token key (the console Connect button, `hanzo connector add` with no --token)
            starts OAuth. Read on STDIN by the CLI, never argv; never logged or echoed.
          type: string
      type: object
    connectOut:
      properties:
        account:
          description: |-
            Account is the account label the provider reported for the credential.
            apikey path only; a pointer because "" is a real answer the provider gave.
          type: string
        authorizeUrl:
          description: AuthorizeURL is the provider consent URL to send the user to.
            OAuth path only.
          type: string
        connected:
          description: Connected is true on the apikey path once the credential verified
            and sealed.
          type: boolean
        externalId:
          description: ExternalID is the provider's account id for the credential.
            apikey path only.
          type: string
        provider:
          description: Provider is the connector's registry id. apikey path only.
          type: string
        scopes:
          description: |-
            Scopes are the permissions the credential carries. apikey path only; never
            null on that path ([] when the provider reported none).
          items:
            type: string
          type: array
      type: object
    connectionOut:
      properties:
        account:
          description: Account names the connected external account, when the provider
            reports one.
          type: string
        provider:
          description: Provider is the connector this answer is about.
          type: string
        status:
          description: 'Status is the connection state: connected or disconnected.'
          type: string
      type: object
    connectionView:
      properties:
        account:
          description: |-
            Account is the human label of the connected third-party account (the Slack
            team name, the GitHub org login). Provider-supplied and sanitized on ingest.
          type: string
        connectedAt:
          description: ConnectedAt is when the connection was last (re)established,
            RFC 3339 UTC.
          type: string
        externalId:
          description: |-
            ExternalID is the provider's own id for the account (Slack team.id, GitHub
            installation_id) — the value inbound webhooks are mapped back to this org by.
          type: string
        scopes:
          description: Scopes are the permissions the provider granted. Never null;
            [] when none.
          items:
            type: string
          type: array
      type: object
    connectorProviderView:
      properties:
        category:
          description: Category groups the card.
          type: string
        description:
          description: Description is the one-line pitch the console card shows.
          type: string
        id:
          description: ID is the provider's registry id and the :provider path segment.
          type: string
        methods:
          description: |-
            Methods are the intake paths this provider supports, derived from its
            capabilities: "device", "oauth" (adopt an externally obtained bundle) and
            "token" (a customer-held credential). At least one, always.
          items:
            type: string
          type: array
        name:
          description: Name is the provider's display name.
          type: string
        scopes:
          description: Scopes are the permissions a connection will ask for. Never
            null.
          items:
            type: string
          type: array
      type: object
    connectorProvidersOut:
      properties:
        providers:
          description: Providers is every USER-scoped provider, sorted by id. Never
            null.
          items:
            $ref: '#/components/schemas/connectorProviderView'
          type: array
      type: object
    connectorTokenOut:
      properties:
        expiresAt:
          description: ExpiresAt is when this token expires, RFC 3339 UTC; empty if
            non-expiring.
          type: string
        label:
          description: Label is the connector's label.
          type: string
        provider:
          description: Provider is the connector's provider id.
          type: string
        token:
          description: Token is the access token, rotated first if it was within the
            refresh window.
          type: string
      type: object
    connectorView:
      properties:
        account:
          description: Account names the connected external account. Absent until
            the org connects.
          type: string
        configured:
          description: Configured is true when this deployment holds OAuth credentials
            for the provider.
          type: boolean
        docCount:
          description: DocCount is the live count of this provider's documents in
            the org's store.
          type: integer
        error:
          description: Error is the last sync failure, if any. Absent until the org
            connects.
          type: string
        kind:
          description: Kind is "native" for a first-party Go connector, "piece" for
            a long-tail one.
          type: string
        lastSync:
          description: LastSync is when the last pull finished. Absent until the org
            connects.
          type: string
        provider:
          description: Provider is the connector's id.
          type: string
        status:
          description: Status is connected, disconnected, syncing or error.
          type: string
      type: object
    connectorsOut:
      properties:
        connectors:
          description: Connectors is the caller's own set. Never null; [] when they
            have none.
          items:
            $ref: '#/components/schemas/connView'
          type: array
      type: object
    consoleSettings:
      properties:
        appsInAnyNamespaceEnabled:
          description: |-
            AppsInAnyNamespaceEnabled is false: applications are projected from operator
            App CRs in the platform namespaces, never declared in an arbitrary one.
          type: boolean
        dexConfig:
          description: |-
            DexConfig carries no connectors. This console does not run Dex; its sign-in is
            GET /v1/deploy/login, which is an IAM authorization-code round trip.
          properties:
            connectors:
              items:
                $ref: '#/components/schemas/deployEmpty'
              type: array
          type: object
        execEnabled:
          description: 'ExecEnabled is false: this plane serves no container terminal.'
          type: boolean
        googleAnalytics:
          description: |-
            GoogleAnalytics carries no tracking id and anonymizes users — this console
            reports no analytics.
          properties:
            anonymizeUsers:
              type: boolean
            trackingID:
              type: string
          type: object
        help:
          description: Help carries no chat link and no binary download URLs.
          properties:
            binaryUrls:
              additionalProperties:
                type: string
              type: object
            chatText:
              type: string
            chatUrl:
              type: string
          type: object
        hydratorEnabled:
          description: 'HydratorEnabled is false: there is no manifest hydrator on
            this plane.'
          type: boolean
        kustomizeVersions:
          description: |-
            KustomizeVersions is always empty: an App CR is an image pin, not a kustomize
            build.
          items:
            type: string
          type: array
        oidcConfig:
          $ref: '#/components/schemas/deployEmpty'
          description: |-
            OidcConfig is always null. The SPA's own OIDC flow is deliberately not
            configured — identity is owned by Hanzo IAM at the edge and minted for this
            host by GET /v1/deploy/login.
        plugins:
          description: 'Plugins is always empty: this plane loads no argocd config-management
            plugins.'
          items:
            $ref: '#/components/schemas/deployEmpty'
          type: array
        statusBadgeEnabled:
          description: 'StatusBadgeEnabled is false: no badge endpoint is served.'
          type: boolean
        statusBadgeRootUrl:
          description: StatusBadgeRootUrl is always empty, for the same reason.
          type: string
        syncWithReplaceAllowed:
          description: |-
            SyncWithReplaceAllowed is false: a sync here asks the operator to reconcile an
            App CR, and never replaces an object.
          type: boolean
        uiBannerContent:
          description: 'UiBannerContent is always empty: this console shows no banner.'
          type: string
        uiCssURL:
          description: 'UiCssURL is always empty: no stylesheet is injected.'
          type: string
        url:
          description: Url is the console's public origin, https://cd.hanzo.ai.
          type: string
        userLoginsDisabled:
          description: |-
            UserLoginsDisabled is true: the SPA must not render its own username/password
            form. Signing in goes through IAM, at GET /v1/deploy/login.
          type: boolean
      type: object
    contextIn:
      properties:
        budgetTokens:
          description: |-
            BudgetTokens caps the bundle's size. Clamped to [256, 32000]; 0 or absent
            uses 4000.
          type: integer
        query:
          description: Query is what to retrieve context for. Required, max 4000 bytes.
          type: string
        repo:
          description: |-
            Repo narrows retrieval to one repository. Empty searches every repo the org
            has indexed.
          type: string
      type: object
    controlCommandView:
      properties:
        command:
          type: string
        message:
          type: string
        payload: {}
        seq:
          type: integer
      type: object
    controlDrain:
      properties:
        commands:
          description: Commands is the session's control commands newer than the cursor,
            oldest first.
          items:
            $ref: '#/components/schemas/controlCommandView'
          type: array
        cursor:
          description: |-
            Cursor is the seq to send as `after` on the next poll — the highest seq in
            this page, or the cursor sent in when the page is empty.
          type: integer
      type: object
    cookieAck:
      properties:
        result:
          description: Result is true when the cookie was written or cleared.
          type: boolean
      type: object
    costLine:
      properties:
        key:
          description: the function the line is about
          type: string
        points:
          description: one point per bucket, oldest first
          items:
            $ref: '#/components/schemas/pointView'
          type: array
      type: object
    crawlDocument:
      properties:
        markdown:
          description: |-
            Markdown is the page's content, extracted and rendered to markdown. This is
            the field to read.
          type: string
        metadata:
          description: |-
            Metadata is whatever the document said about itself — description, og:*,
            language — plus the response status, the final URL and the content type. It
            is an OPEN key space, so it is carried as raw JSON: `map[string]any`
            publishes `additionalProperties:{"type":"object"}`, which the integer
            `status` inside it refutes, while raw JSON publishes `{}` — "any JSON",
            which is true. The bytes are identical either way (encoding/json writes a
            map's keys sorted, and this IS that marshalling).
        title:
          description: Title is the document's title, when it carried one.
          type: string
        url:
          description: URL is the address actually read, after redirects.
          type: string
      type: object
    crawlRequest:
      properties:
        url:
          type: string
      type: object
    crawlResult:
      properties:
        data:
          $ref: '#/components/schemas/crawlDocument'
          description: Data is the page, present exactly when Success.
        error:
          description: |-
            Error says what stopped the fetch: the host was refused, unreachable, or
            served something that is not a document.
          type: string
        success:
          description: |-
            Success is whether the page was fetched and read. FALSE with an Error is a
            complete answer, not a fault — check this before reading Data.
          type: boolean
      type: object
    createAgentIn:
      properties:
        computeRef:
          type: string
        description:
          type: string
        executionMode:
          type: string
        instructions:
          type: string
        model:
          type: string
        name:
          type: string
        schedule:
          type: string
        serviceAccountId:
          type: string
        tools:
          items:
            type: string
          type: array
      type: object
    createAppReq:
      properties:
        buildType:
          description: |-
            BuildType is `pack` — the zero-config default that detects any project —
            or `dockerfile`, the explicit escape hatch. An image app never builds.
          type: string
        description:
          description: Description is free text about what the application is.
          type: string
        dockerfile:
          description: Dockerfile is the path to build from, for buildType `dockerfile`.
          type: string
        domains:
          description: |-
            Domains are extra ingress hosts. The canonical default host is always
            attached; a bare custom host is refused here and must go through
            add-domain → verify first.
          items:
            type: string
          type: array
        env:
          description: |-
            Env is the application's environment. Keys must match
            `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is sealed into
            KMS and its plaintext is never written to the database.
          items:
            $ref: '#/components/schemas/EnvVarJSON'
          type: array
        environment:
          description: Environment is the deploy target this app names ("production"
            by default).
          type: string
        image:
          $ref: '#/components/schemas/imageOrigin'
          description: Image is the container image to run, for source `image`.
        name:
          description: |-
            Name is the application's display name. Required; the slug is derived from
            it when none is given.
          type: string
        port:
          description: Port is the container port the app listens on.
          type: integer
        project:
          description: Project is the project to create the application under, from
            the path.
          type: string
        replicas:
          description: |-
            Replicas is how many copies to run; clamped to the deployment's limit
            rather than refused.
          type: integer
        repo:
          $ref: '#/components/schemas/gitOrigin'
          description: Repo is the git source to build from, for source `git`.
        slug:
          description: |-
            Slug is the app's identity in the cluster — its CR name and part of its
            host. Given or derived from Name, it must match
            `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and one already used in this
            project is 409.
          type: string
        source:
          description: |-
            Source is `git`, which requires repo.url, or `image`, which requires
            image.repository. Anything else is 400.
          type: string
        storageGb:
          description: |-
            StorageGB is the persistent volume size in GiB; absent means stateless.
            Clamped to the deployment's limit rather than refused.
          type: integer
      type: object
    createClusterReq:
      properties:
        name:
          description: Name is the cluster's name. Required.
          type: string
        nodePool:
          properties:
            count:
              type: integer
            name:
              type: string
            size:
              type: string
          type: object
        region:
          description: Region is the provider region slug (e.g. "nyc3"). Required.
          type: string
        version:
          description: Version is the Kubernetes version slug; empty takes the provider
            default.
          type: string
      type: object
    createEndpointIn:
      properties:
        description:
          description: Description is a free-text label for the console. Optional,
            clipped to 1024 bytes.
          type: string
        events:
          description: |-
            Events are NATS subject patterns to subscribe to (e.g. "commerce.order.>").
            An empty or omitted list means EVERY event on the platform bus. Max 64
            patterns, each max 256 bytes.
          items:
            type: string
          type: array
        status:
          description: |-
            Status is "active" or "disabled". Empty defaults to active. A disabled
            endpoint receives no bus deliveries, but can still be exercised with
            POST /v1/webhooks/{id}/test.
          type: string
        url:
          description: |-
            URL is the https:// address each matching event is POSTed to. Required,
            max 2048 bytes; http:// and every other scheme is refused, because a
            webhook carries signed event data and must not travel in the clear.
          type: string
      type: object
    createFlowReq:
      properties:
        displayName:
          description: DisplayName names the flow's initial draft version.
          type: string
        externalId:
          description: ExternalID is the caller's own id for this flow. Optional.
          type: string
        folderId:
          description: FolderID groups the flow in the builder's tree. Optional.
          type: string
        trigger:
          $ref: '#/components/schemas/FlowTrigger'
          description: |-
            Trigger is the root of the step tree — how the flow starts, and the action
            chain that follows. Optional: a flow may be created empty and edited later.
      type: object
    createReq:
      properties:
        description:
          description: Description is a free-form blurb, max 4KiB.
          type: string
        name:
          description: |-
            Name is the repo's handle, unique within the scope, and the last segment of
            both clone URLs. Must match ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$; a trailing
            ".git" is stripped first. Required.
          type: string
        project:
          description: |-
            Project narrows the repo to a sub-scope of the org. Omit it to use the
            caller's own X-Project-Id scope; it can never widen past the caller's org.
          type: string
        public:
          description: |-
            Public grants ANONYMOUS read (fetch) only; push and the whole control plane
            stay org-authed. Defaults to false.
          type: boolean
      type: object
    createServerReq:
      properties:
        authHeader:
          description: |-
            AuthHeader is the request header the credential is injected into, e.g.
            "Authorization". Empty means the server needs no credential.
          type: string
        listing:
          description: |-
            Listing enables a CATALOG entry instead — the id from GET /v1/tools/catalog.
            The endpoint is the listing's own streamable-http remote, so a listing that
            only ships a stdio package is refused: there is nothing to reach yet.
          type: string
        name:
          description: |-
            Name labels the server for the org. Required with URL; with Listing it
            defaults to the listing's own title.
          type: string
        secret:
          description: |-
            Secret is the credential VALUE. It is sealed into KMS under a per-org ref
            and never stored in SQLite, never listed, and never returned.
          type: string
        url:
          description: |-
            URL is the server's JSON-RPC endpoint. It must be an http(s) URL naming a
            PUBLIC host: loopback, link-local, private and cloud-metadata addresses are
            refused here and again when the dialer connects.
          type: string
      type: object
    createVersionIn:
      properties:
        displayName:
          description: DisplayName names the new version.
          type: string
        id:
          description: ID is the flow to add a version to, from the path.
          type: string
        trigger:
          $ref: '#/components/schemas/FlowTrigger'
          description: |-
            Trigger is the root of the version's step tree. Optional: a version with no
            trigger is created invalid, and cannot run until one is set.
      type: object
    credentialIn:
      properties:
        accountId:
          description: AccountID scopes the credential where the provider's Verify
            needs one.
          type: string
        label:
          description: Label names this connection; empty means "default".
          type: string
        oauth:
          $ref: '#/components/schemas/oauthBundleIn'
          description: |-
            OAuth is a bundle the CLI already obtained through its own local PKCE flow.
            Present ⇒ the Adopt path; absent ⇒ the Token path.
        provider:
          description: Provider is the user-scoped provider's registry id, from the
            path.
          type: string
        token:
          description: |-
            Token is the customer-held credential for the Verify path. Read on STDIN by
            the CLI, never argv; never logged, echoed, or stored outside KMS.
          type: string
      type: object
    credentialOut:
      properties:
        connected:
          description: Connected is always true — a failed verification is a 400 and
            stores nothing.
          type: boolean
        connector:
          $ref: '#/components/schemas/connView'
          description: Connection is the connector as it now stands.
      type: object
    csrfResp:
      properties:
        csrfToken:
          description: |-
            Token is the value to send back in the X-CSRF-Token header. It is bound to the
            caller's identity, so it authorizes writes as them and as nobody else.
          type: string
        expiresIn:
          description: |-
            ExpiresIn is the token's lifetime in seconds. Fetch a new one when it lapses;
            a write with an expired token is refused.
          type: integer
      type: object
    curateReq:
      properties:
        featured:
          description: Featured puts the listing on the front of the shelf, or takes
            it off.
          type: boolean
        hidden:
          description: Hidden takes the listing off the org-visible shelf, or puts
            it back.
          type: boolean
        id:
          description: ID is the listing to curate, from the path.
          type: string
        logo:
          description: |-
            Logo is the brand mark to render, an https URL. Empty clears ours and lets
            the next sync adopt the publisher's own icon again.
          type: string
        official:
          description: |-
            Official overrides the derivation: setting it makes this answer FINAL, so
            no later sync re-derives over it. That is the difference between a default
            and a decision — the derivation can only tell that a domain-verified
            publisher serves the endpoint, not that the product is theirs.
          type: boolean
      type: object
    dashResp:
      properties:
        account:
          description: Account is the linked account that was asked about, when one
            was named.
          type: string
        available:
          description: |-
            Available is false when the warehouse could not be read. That means "no
            answer", NOT "no usage" — the two lists below are then empty for a reason.
          type: boolean
        current:
          description: Current is the newest window instance of each lane — the dash
            headline.
          items:
            $ref: '#/components/schemas/usageWindowView'
          type: array
        from:
          description: From is the inclusive start of that window, RFC3339 UTC.
          type: string
        provider:
          description: Provider is the upstream that was asked about, echoed back.
          type: string
        range:
          description: 'Range is the window that was served: 1h, 24h, 7d or 30d.'
          type: string
        scope:
          description: 'Scope says whose rows these are: the caller''s own linked
            accounts.'
          type: string
        source:
          description: Source names the meter of record — the provider's own login,
            not Hanzo.
          type: string
        to:
          description: To is the exclusive end of that window, RFC3339 UTC.
          type: string
        windows:
          description: Windows is every instance in range, newest first — the history
            behind it.
          items:
            $ref: '#/components/schemas/usageWindowView'
          type: array
      type: object
    databaseCreateIn:
      properties:
        name:
          description: Name is the database name to create.
          type: string
      type: object
    datasetList:
      properties:
        data:
          description: Data is the caller org's datasets, newest first, bounded by
            limit.
          items:
            $ref: '#/components/schemas/datasetView'
          type: array
      type: object
    datasetReq:
      properties:
        description:
          description: Description is free text about what this set measures; over
            64 KiB is refused.
          type: string
        metadata:
          additionalProperties:
            type: object
          description: Metadata is a free-form object stored with the set and echoed
            back verbatim.
          type: object
        name:
          description: |-
            Name is the dataset's org-unique handle and the segment that will address
            it, so it must match ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$.
          type: string
      required:
      - name
      type: object
    datasetView:
      properties:
        createdAt:
          description: CreatedAt is when the name was first written, kept across later
            edits.
          type: string
        description:
          description: Description is free text the org wrote about what this set
            measures.
          type: string
        items:
          description: |-
            Items is how many examples the set holds. It is filled only by the single
            read — a listing does not count, so it is absent there rather than zero.
          type: integer
        metadata:
          additionalProperties:
            type: object
          description: Metadata is the free-form object stored with the set, echoed
            back verbatim.
          type: object
        name:
          description: Name is the dataset's org-unique handle and the segment that
            addresses it.
          type: string
        updatedAt:
          description: UpdatedAt is when the description or metadata last changed.
          type: string
      type: object
    dayCount:
      properties:
        count:
          description: Count is that day's total.
          type: integer
        date:
          description: Date is the day, YYYY-MM-DD.
          type: string
      type: object
    definition:
      properties:
        code:
          description: Code is the source to run, capped so one function cannot amplify
            the store.
          type: string
        envNames:
          description: |-
            EnvNames are the secret NAMES to mount. Values live in the secret store and
            are never carried here.
          items:
            type: string
          type: array
        environment:
          description: |-
            Environment is a second spelling of runtime, accepted so a console that says
            "environment" needs no translation.
          type: string
        handler:
          description: Handler is the entry point within the code.
          type: string
        image:
          description: Image names a prebuilt image to run instead of source.
          type: string
        memoryLimit:
          description: |-
            MemoryLimit is the memory the function runs with, defaulting to 256Mi. It is
            also the multiplier on the GB-seconds compute charge.
          type: string
        name:
          description: |-
            Name is the function's org-unique handle and the segment that addresses it,
            matching ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The names that would shadow a
            collection route are reserved.
          type: string
        namespace:
          description: |-
            Namespace groups functions for display. It is cosmetic — the org is the
            isolation key — and is normalised to a DNS-safe label.
          type: string
        runtime:
          description: 'Runtime is the language the code runs under: node, python
            or deno.'
          type: string
        target:
          description: |-
            Target is where the function runs: sandbox (the default) or fleet, the org's
            own GPU fleet. fleet supports runtime=python only.
          type: string
        timeoutSec:
          description: |-
            TimeoutSec is the per-invocation deadline, defaulting to 30 and clamped at
            900 — a larger value is capped rather than reset to the default.
          type: integer
      required:
      - name
      type: object
    defsOut:
      properties:
        data:
          description: Data is every definition in the caller's (org, project) store,
            by key.
          items:
            $ref: '#/components/schemas/DefRow'
          type: array
      type: object
    deletedOut:
      properties:
        deleted:
          description: Deleted is the key that no longer exists.
          type: string
      type: object
    deliveryList:
      properties:
        data:
          description: Data is the matching attempts, newest first.
          items:
            $ref: '#/components/schemas/DeliveryRow'
          type: array
      type: object
    deployEmpty:
      properties: {}
      type: object
    deployLogs:
      properties:
        deploymentId:
          description: DeploymentID is the deployment these logs belong to.
          type: string
        logs:
          description: |-
            Logs is the recorded status timeline followed by the streamed pod output,
            newline-separated.
          type: string
        source:
          description: |-
            Source says which pod the log body carries — `build`, `app`, or `none` when
            neither pod was reachable — so a console can label the pane honestly.
          type: string
      type: object
    deployReq:
      properties:
        app:
          description: App is the application's slug, from the path.
          type: string
        commit:
          description: |-
            Commit is the git commit or ref to build, for a git-source app. Defaults to
            the app's branch.
          type: string
        project:
          description: Project is the project the application lives under, from the
            path.
          type: string
        tag:
          description: |-
            Tag is the image tag to deploy, for an image-source app. Defaults to the
            app's tag, then `latest`.
          type: string
      type: object
    deploymentView:
      properties:
        applicationId:
          type: string
        buildId:
          type: string
        commit:
          type: string
        createdAt:
          type: integer
        id:
          type: string
        image:
          type: string
        message:
          type: string
        org:
          type: string
        source:
          type: string
        status:
          type: string
        updatedAt:
          type: integer
        version:
          type: integer
      type: object
    destinationDisconnected:
      properties:
        disconnected:
          description: Disconnected is true when the credentials and the row are gone.
          type: boolean
      type: object
    destinationList:
      properties:
        destinations:
          description: Destinations is one card per registered platform, in slug order.
          items:
            $ref: '#/components/schemas/DestinationStatus'
          type: array
      type: object
    destinationTest:
      properties:
        error:
          description: Error is the platform's rejection, present only on a failed
            send.
          type: string
        message:
          description: Message is the platform's own note about the send, present
            only on success.
          type: string
        ok:
          description: OK is true when the platform accepted the synthetic event.
          type: boolean
        sent:
          description: Sent is how many events the platform accepted, present only
            on success.
          type: integer
      type: object
    devicePollOut:
      properties:
        connector:
          $ref: '#/components/schemas/connView'
          description: Connection is the connected connector. Present only on "connected".
        interval:
          description: |-
            Interval is the seconds to wait before the next poll. Present only while
            pending, and it may rise when the provider asks the client to slow down.
          type: integer
        status:
          description: Status is the flow's state. "pending" means poll again after
            Interval.
          type: string
      type: object
    deviceStartIn:
      properties:
        label:
          description: |-
            Label names this connection so one user can hold several per provider
            ("work", "personal"). Empty means "default". 1-64 of [A-Za-z0-9._-].
          type: string
        provider:
          description: Provider is the user-scoped provider's registry id, from the
            path.
          type: string
      type: object
    deviceStartOut:
      properties:
        expiresAt:
          description: ExpiresAt is when the flow dies, RFC 3339 UTC.
          type: string
        flow:
          description: Flow is the id to poll with.
          type: string
        interval:
          description: Interval is the seconds to wait between polls.
          type: integer
        userCode:
          description: UserCode is the short code the user types at VerifyURL.
          type: string
        verifyUrl:
          description: VerifyURL is the page the user opens to enter UserCode.
          type: string
      type: object
    disconnectOut:
      properties:
        disconnected:
          description: Disconnected is always true — the org's secrets and connection
            row are gone.
          type: boolean
      type: object
    domainAddIn:
      properties:
        name:
          description: Name is the custom domain to attach, e.g. "www.acme.com".
          type: string
        project:
          description: Project is the Pages project name, from the path.
          type: string
      type: object
    domainView:
      properties:
        createdAt:
          description: CreatedAt is the unix second the custom claim was made.
          type: integer
        detail:
          description: Detail says why a claim is still pending, in the resolver's
            own words.
          type: string
        host:
          description: Host is the hostname itself.
          type: string
        kind:
          description: Kind is `default`, `subtree` or `custom` — how the org came
            to own it.
          type: string
        primary:
          description: Primary marks the app's permanent default host.
          type: boolean
        records:
          description: Records are the DNS records to publish while a custom claim
            is pending.
          items:
            $ref: '#/components/schemas/Record'
          type: array
        status:
          description: |-
            Status is `live`, `provisioning`, `pending_deploy` or `pending`, derived
            from the operator CR and never fabricated.
          type: string
        url:
          description: URL is the host as an HTTPS address.
          type: string
        verified:
          description: |-
            Verified is whether ownership is settled — always true for a host the org
            structurally owns.
          type: boolean
      type: object
    driftBoard:
      properties:
        apps:
          description: Apps are the service rows, ordered by org, then app, then env.
          items:
            $ref: '#/components/schemas/AppView'
          type: array
        summary:
          $ref: '#/components/schemas/fleetSummary'
          description: Summary counts the board by drift severity.
      type: object
    driftTally:
      properties:
        ok:
          description: OK is how many rows run what they declare.
          type: integer
        red:
          description: Red is how many have drifted badly.
          type: integer
        yellow:
          description: Yellow is how many have drifted within tolerance.
          type: integer
      type: object
    edgeState:
      properties:
        configured:
          description: |-
            Configured is whether the edge holds credentials to act at all. False means
            every purge is a no-op.
          type: boolean
        error:
          description: Error is the blocker, so an operator reads it instead of guessing
            at it.
          type: string
        freshness:
          description: |-
            Freshness says, in one phrase, how long after a publish a reader sees it.
            It is the sentence an operator actually wants; the booleans above are how
            a machine reads the same fact.
          type: string
        policy:
          additionalProperties:
            type: string
          description: |-
            Policy is the Cache-Control this edge serves each class of object with. It
            is DERIVED from the one canonical function, never a second copy: half the
            confusion when a publish looks stale is not knowing what the TTLs are, and
            reading them out of the source is not something an operator should have to
            do to answer "how long until this is live".
          type: object
        provider:
          description: |-
            Provider is the CDN behind this edge, or "none". It is the first thing an
            operator wants and the only vendor name this API returns.
          type: string
        reach:
          description: |-
            Reach is the apexes a publish is invalidated on. A site is served on more
            than one — the site plane's own and the first-party apex — and a purge that
            covers one of them looks identical from here to a purge that covers both.
          items:
            type: string
          type: array
        status:
          description: Status is "ok" when a publish reaches readers immediately,
            else "degraded".
          type: string
      type: object
    embedStatusResp:
      properties:
        app:
          description: App is the app this verdict is about.
          type: string
        embedUrl:
          description: |-
            EmbedURL is the in-app landing URL to frame. Empty when the caller is not
            entitled — a non-entitled caller never receives it.
          type: string
        entitled:
          description: Entitled is whether the caller's org may frame this brand-owned
            app.
          type: boolean
        origin:
          description: Origin is the app's origin on this deployment's own brand domain.
          type: string
        phase:
          description: 'Phase is the verdict in one word: not-entitled, not-provisioned
            or ready.'
          type: string
        reachable:
          description: Reachable is whether the app answered the liveness probe.
          type: boolean
      type: object
    enablementBoard:
      properties:
        betas:
          description: Betas are the subset of Items the caller's org may still opt
            into.
          items:
            $ref: '#/components/schemas/userEnablementItem'
          type: array
        items:
          description: Items is every managed item, each resolved for the caller's
            org.
          items:
            $ref: '#/components/schemas/userEnablementItem'
          type: array
        org:
          description: |-
            Org is the org this view was resolved for; empty for a caller with no
            validated principal, who sees only the generally-available items.
          type: string
      type: object
    enablementOptRef:
      properties:
        id:
          description: ID is the item within that namespace.
          type: string
        kind:
          description: 'Kind is the item''s namespace: "model", "provider" or "feature".'
          type: string
      type: object
    endpointList:
      properties:
        data:
          description: |-
            Data is the org's endpoints, newest first, each with its signing secret
            REDACTED — the secret leaves the server only on create and on rotate.
          items:
            $ref: '#/components/schemas/Endpoint'
          type: array
      type: object
    endpointReq:
      properties:
        connector:
          description: Connector names the stored credential to reach this endpoint
            with.
          type: string
        locator:
          description: |-
            Locator addresses the resource. For a git source it is the https clone URL on
            the provider's own host, with no embedded credentials; for a native target it
            is the repository name.
          type: string
        provider:
          description: |-
            Provider is the platform: github or gitlab for a source; a target defaults to
            the native Hanzo Git plane.
          type: string
      type: object
    endpointView:
      properties:
        connector:
          type: string
        locator:
          type: string
        provider:
          type: string
      type: object
    engineAdvertisement:
      properties:
        apis:
          description: '["openai","anthropic"]'
          items:
            type: string
          type: array
        models:
          description: ids from the node's GET /v1/models
          items:
            type: string
          type: array
        status:
          description: '"ready" | "unreachable"'
          type: string
        url:
          type: string
      type: object
    engineStatus:
      properties:
        reachable:
          description: Reachable is true when the engine answered its health probe.
          type: boolean
        revision:
          description: |-
            Revision is the engine build's git revision, present only when
            reachable (the server's own build identity — it publishes no semver).
          type: string
      type: object
    entitlementsView:
      properties:
        enabled:
          description: Enabled is the org's turned-on product ids, sorted. Always
            an array, never null.
          items:
            type: string
          type: array
      type: object
    environmentBoard:
      properties:
        environments:
          description: Environments are the org's deploy targets, in first-seen order.
          items:
            $ref: '#/components/schemas/environmentRow'
          type: array
      type: object
    environmentRow:
      properties:
        id:
          description: |-
            ID is the environment's name, which is also its identity — an environment
            is derived from the apps that target it, so it has no id of its own.
          type: string
        name:
          description: Name is the environment's name as an app declared it.
          type: string
        services:
          description: Services are the apps that target this environment, by name.
          items:
            type: string
          type: array
        status:
          description: |-
            Status rolls up the real states of this environment's apps: degraded,
            active, idle or empty.
          type: string
        type:
          description: |-
            Type buckets the name for display: production, staging, development or
            custom.
          type: string
        updatedAt:
          description: UpdatedAt is when any of them last changed, RFC3339 UTC; empty
            when unset.
          type: string
      type: object
    errorList:
      properties:
        data:
          description: Data is the errors, newest first. Empty rather than absent
            when there are none.
          items:
            $ref: '#/components/schemas/capturedError'
          type: array
      type: object
    evaluateIn:
      properties:
        distinct_id:
          description: DistinctID is the identity the flags are evaluated for. Required.
          type: string
        groups:
          description: Groups are the group-level properties, keyed by group type
            index.
        person_properties:
          description: PersonProperties are the person-level properties conditions
            match against.
      type: object
    evaluatorList:
      properties:
        data:
          description: Data is the caller org's judges, bounded by limit.
          items:
            $ref: '#/components/schemas/evaluatorView'
          type: array
      type: object
    evaluatorReq:
      properties:
        criteria:
          description: Criteria is the written standard the judge applies; over 64
            KiB is refused.
          type: string
        model:
          description: Model is the model that will do the grading.
          type: string
        name:
          description: |-
            Name is the judge's org-unique handle, matching
            ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. Re-posting a name edits that judge
            rather than adding a second one.
          type: string
        scoreName:
          description: |-
            ScoreName is the name the resulting scores are filed under. It defaults to
            the judge's own name and must match the same pattern.
          type: string
      required:
      - name
      type: object
    evaluatorView:
      properties:
        createdAt:
          description: CreatedAt is when the judge was first defined.
          type: string
        criteria:
          description: Criteria is the written standard the judge applies.
          type: string
        model:
          description: Model is the model that does the grading.
          type: string
        name:
          description: Name is the judge's org-unique handle.
          type: string
        scoreName:
          description: ScoreName is the name the resulting scores are filed under.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          type: string
      type: object
    eventList:
      properties:
        data:
          description: Data is the events, newest first. Empty rather than absent
            when there are none.
          items:
            $ref: '#/components/schemas/productEvent'
          type: array
      type: object
    eventView:
      properties:
        actor:
          type: string
        createdAt:
          type: string
        id:
          type: string
        kind:
          type: string
        payload: {}
        seq:
          type: integer
        sessionId:
          type: string
      type: object
    fileContent:
      properties:
        content:
          description: |-
            Content is the file's text as the index stored it. It is NOT guaranteed
            byte-verbatim — the git object plane is the source of record for exact
            bytes, history and blame.
          type: string
        lang:
          description: Lang is the detected language.
          type: string
        path:
          description: Path echoes the file that was read.
          type: string
        repo:
          description: Repo echoes the repository it came from.
          type: string
      type: object
    fileInput:
      properties:
        content:
          description: |-
            Content is the file's full text. Max 1 MiB per file; binary files should
            simply be omitted rather than sent.
          type: string
        path:
          description: Path is the file's repo-relative path, e.g. "internal/store/db.go".
          type: string
      type: object
    fileJSON:
      properties:
        content:
          description: Content is the file's bytes, empty when Truncated.
          type: string
        encoding:
          description: 'Encoding is how Content is carried: "utf8" verbatim, or "base64".'
          type: string
        path:
          description: Path is the file's repo-relative path.
          type: string
        size:
          description: Size is the file's byte length in the repo.
          type: integer
        truncated:
          description: |-
            Truncated marks a file past the read cap; no content is sent. A caller
            assembling a desired set must treat this as INCOMPLETE, never as empty.
          type: boolean
      type: object
    filesJSON:
      properties:
        files:
          description: |-
            Files are the selected files, sorted by path. Directories are never
            returned.
          items:
            $ref: '#/components/schemas/fileJSON'
          type: array
        rev:
          description: Rev is the full revision the ref resolved to — pin follow-up
            reads to it.
          type: string
      type: object
    financeLedgerEntry:
      properties:
        account:
          type: string
        balanceCents:
          type: integer
        cents:
          type: integer
        currency:
          type: string
        date:
          type: string
        description:
          type: string
        id:
          type: string
      type: object
    fleetBoard:
      properties:
        units:
          description: |-
            Units is the union across sources — agent run-targets, BYO workers, BYO
            clusters and Visor machines — each row naming the source it came from.
          items:
            $ref: '#/components/schemas/fleetUnit'
          type: array
      type: object
    fleetMetrics:
      properties:
        at:
          type: string
        gpuUtil:
          type: number
        load1:
          type: number
        memFree:
          type: integer
        memUsed:
          type: integer
      type: object
    fleetSpec:
      properties:
        arch:
          type: string
        cpus:
          type: integer
        gpuModel:
          type: string
        gpus:
          type: integer
        memory:
          type: integer
        os:
          type: string
      type: object
    fleetSummary:
      properties:
        byDrift:
          $ref: '#/components/schemas/driftTally'
          description: ByDrift counts those rows green, yellow and red.
        total:
          description: Total is how many rows the board returned, after filtering.
          type: integer
      type: object
    fleetUnit:
      properties:
        host:
          type: string
        kind:
          type: string
        label:
          type: string
        metrics:
          $ref: '#/components/schemas/fleetMetrics'
        queued:
          type: integer
        running:
          description: |-
            Running is what the unit is actively executing: agent sessions for a run-target,
            in-flight renders for a BYO GPU. Queued is the gpu-jobs backlog on this GPU's
            lane (BYO units only; an agent unit does not queue). Both come from the org's
            gpu-jobs queue for BYO units, overlaid in listFleet.
          type: integer
        sessions:
          type: integer
        source:
          type: string
        spec:
          $ref: '#/components/schemas/fleetSpec'
        status:
          type: string
        unit:
          type: string
      type: object
    flowCreate:
      properties:
        data:
          description: |-
            Data is the workflow graph (the product's nodes/edges document),
            verbatim. Omit it to create an empty workflow.
        description:
          description: Description says what the workflow does.
          type: string
        name:
          description: |-
            Name is the workflow's display name, unique within the org's project
            (the product de-duplicates by suffixing).
          type: string
      type: object
    flowPage:
      properties:
        data:
          description: Data is the page of flows, newest-updated first.
          items:
            $ref: '#/components/schemas/Flow'
          type: array
      type: object
    flowRun:
      properties:
        input:
          description: Input is the run's chat input value, handed to the graph's
            input node.
          type: string
        session:
          description: |-
            Session groups runs into one conversation; the product mints one when
            absent and returns it in the response.
          type: string
        tweaks:
          description: |-
            Tweaks override component fields for this run only (the product's
            tweaks document), verbatim.
        workflow:
          description: Workflow is the UUID of the workflow to run.
          type: string
      type: object
    flowStatus:
      properties:
        reachable:
          description: Reachable is true when the flow service answered its health
            probe.
          type: boolean
        version:
          description: Version is the flow service's own version, present only when
            reachable.
          type: string
      type: object
    flowUpdate:
      properties:
        data:
          description: Data replaces the workflow graph when present, verbatim.
        description:
          description: Description replaces the description when present.
          type: string
        locked:
          description: Locked freezes or unfreezes the workflow against edits when
            present.
          type: boolean
        name:
          description: Name renames the workflow when present.
          type: string
        workflow:
          description: Workflow is the workflow's UUID, taken from the path.
          type: string
      type: object
    fnList:
      properties:
        functions:
          description: Functions is one row per published function.
          items:
            $ref: '#/components/schemas/functionView'
          type: array
      type: object
    functionDetail:
      properties:
        avgDurationMs:
          type: number
        createdAt:
          type: string
        endpoint:
          type: string
        envCount:
          type: integer
        environment:
          type: string
        errors7d:
          type: integer
        image:
          type: string
        invocations7d:
          type: integer
        lastDeployedAt:
          type: string
        memoryLimit:
          type: string
        name:
          type: string
        namespace:
          type: string
        recentInvocations:
          description: RecentInvocations is its twenty most recent runs, newest first.
          items:
            $ref: '#/components/schemas/invocationView'
          type: array
        secrets:
          description: Secrets are the NAMES it mounts. Values are never read or returned.
          items:
            type: string
          type: array
        status:
          type: string
        successRate:
          type: number
        target:
          type: string
        timeoutSec:
          type: integer
        triggers:
          description: Triggers is how this function is reached.
          items:
            $ref: '#/components/schemas/triggerView'
          type: array
      type: object
    functionView:
      properties:
        avgDurationMs:
          description: mean wall-clock of those runs
          type: number
        createdAt:
          description: when it was first published
          type: string
        endpoint:
          description: the path that invokes it
          type: string
        envCount:
          description: how many secret NAMES it mounts; values are never carried
          type: integer
        environment:
          description: the language it runs under
          type: string
        errors7d:
          description: how many of those runs failed
          type: integer
        image:
          description: the prebuilt image it runs, when it runs one instead of source
          type: string
        invocations7d:
          description: runs in the last 7 days; ABSENT, never 0, when it has not run
          type: integer
        lastDeployedAt:
          description: when its code last changed
          type: string
        memoryLimit:
          description: the memory it runs with, and the multiplier on its compute
            charge
          type: string
        name:
          description: the function's org-unique handle
          type: string
        namespace:
          description: the display group it belongs to; the org is the isolation key
          type: string
        status:
          description: whether it is ready to serve
          type: string
        successRate:
          description: share of those runs that succeeded, 0..1
          type: number
        target:
          description: 'where it runs: empty for the sandbox, "fleet" for the org''s
            GPU fleet'
          type: string
        timeoutSec:
          description: its per-invocation deadline
          type: integer
      type: object
    gcOut:
      properties:
        maintained:
          description: Maintained is always true; the call fails rather than reporting
            false.
          type: boolean
        repo:
          description: Repo is the repo that was repacked.
          type: string
        sizeBytes:
          description: |-
            SizeBytes is the size measured AFTER the repack — usually smaller, since
            repacking drops the packs it supersedes.
          type: integer
      type: object
    gitOrigin:
      properties:
        branch:
          description: Branch is the branch to build; defaults to `main` for a git
            source.
          type: string
        url:
          description: |-
            URL is the repository clone URL. Required for source `git`, and validated
            against the same allowlist the privileged build enforces.
          type: string
      type: object
    gitSource:
      properties:
        branch:
          type: string
        provider:
          type: string
        url:
          type: string
      type: object
    githubBackfillIn:
      properties:
        state:
          description: |-
            State is the GitHub issue state to walk: "open" (the default), "closed" or
            "all". Anything else is a 400.
          type: string
      type: object
    githubBackfillResult:
      properties:
        created:
          description: Created is how many native issues this pass created.
          type: integer
        failed:
          description: Failed is how many repos or issues errored; the pass continues
            past each.
          type: integer
        issues:
          description: Issues is how many upstream issues were seen.
          type: integer
        repos:
          description: Repos is how many granted repos were walked (archived/disabled
            are skipped).
          type: integer
        truncated:
          description: |-
            Truncated is set when the time budget or the issue cap stopped the pass early.
            Re-run to continue — the mirror is idempotent by ExtRef, so nothing duplicates.
          type: boolean
        updated:
          description: Updated is how many existing native issues this pass refreshed.
          type: integer
      type: object
    githubClaimIn:
      properties:
        accounts:
          description: |-
            Accounts names GitHub logins the App is installed on ("hanzoai"). Matched
            case-insensitively, since GitHub logins are. Ignored when all is true.
          items:
            type: string
          type: array
        all:
          description: All binds every account the App holds, instead of naming them.
          type: boolean
      type: object
    githubClaimOut:
      properties:
        already:
          description: Already were bound before the call and are unchanged by it.
          items:
            type: string
          type: array
        claimed:
          description: Claimed are the accounts this call bound. Never null; [] when
            none.
          items:
            type: string
          type: array
      type: object
    githubForkOut:
      properties:
        clone_url:
          type: string
        default_branch:
          type: string
        existing:
          description: |-
            Existing reports that the fork was already there. GitHub answers 202 either
            way, so without this a caller cannot tell "made you one" from "you had one".
          type: boolean
        full_name:
          type: string
        html_url:
          type: string
      type: object
    githubForkReq:
      properties:
        org:
          description: |-
            Org is the GitHub account to fork INTO; empty forks to the installation's
            own account, which is the common case.
          type: string
        repo:
          description: Repo is a repository the org's installation was GRANTED, by
            name.
          type: string
      type: object
    githubImportIn:
      properties:
        all:
          description: |-
            All imports every repository the installation grants, instead of naming
            them. Archived and disabled repositories are skipped either way — they
            cannot be fetched.
          type: boolean
        repos:
          description: |-
            Repos names the repositories to import, either owner-qualified
            ("hanzo-apps/ai") or as a bare name ("ai"); a trailing ".git" is stripped.
            A bare name that matches more than one granted repository is an error
            rather than a guess, because one Hanzo org may hold several GitHub
            installations and a name is only unique within an owner.
            Ignored when all is true.
          items:
            type: string
          type: array
      type: object
    githubImportOut:
      properties:
        queued:
          description: Queued is how many repositories were handed to the background
            importer.
          type: integer
        repos:
          description: Repos names those repositories, in the installation's listing
            order.
          items:
            type: string
          type: array
      type: object
    githubInstallationView:
      properties:
        connected:
          description: Connected reports whether THIS org has already bound this account.
          type: boolean
        grant:
          description: |-
            Grant is "all" or "selected" — how many of the account's repositories the
            install covers. A reader deciding what to import needs the reach, not just
            the name.
          type: string
        htmlUrl:
          description: HTMLURL is the account's page on GitHub.
          type: string
        login:
          description: Login is the GitHub account name — the org or user the App
            is installed on.
          type: string
        type:
          description: Type is "Organization" or "User".
          type: string
      type: object
    githubInstallationsOut:
      properties:
        installUrl:
          description: |-
            InstallURL is where to grant a new account, so a UI with an empty list has
            somewhere to send the reader instead of a dead end.
          type: string
        installations:
          description: |-
            Installations is every account the caller may see: the ones its own org has
            bound, or — for a super admin — every account the App is installed on.
            Never null; [] when none.
          items:
            $ref: '#/components/schemas/githubInstallationView'
          type: array
      type: object
    githubPagesBuildOut:
      properties:
        repo:
          description: Repo is the repository the build was queued for.
          type: string
        status:
          description: Status is GitHub's build state at the moment it was queued
            ("queued").
          type: string
        url:
          description: URL is GitHub's API URL for the build, for polling it there.
          type: string
      type: object
    githubPagesDisabledOut:
      properties:
        disabled:
          description: Disabled is always true — a failure is an HTTP error, never
            this shape.
          type: boolean
        repo:
          description: Repo is the repository whose site was deleted.
          type: string
      type: object
    githubPagesEnableReq:
      properties:
        branch:
          description: |-
            Branch is the legacy source branch; empty defaults to the repo's own default
            branch. Ignored when buildType is "workflow".
          type: string
        buildType:
          description: |-
            BuildType selects the builder: "workflow" builds via GitHub Actions, anything
            else builds from the branch source above.
          type: string
        path:
          description: |-
            Path is the source directory within the branch: "/" (the default) or "/docs".
            GitHub allows no others.
          type: string
        repo:
          description: Repo is the repository, from the :repo path segment.
          type: string
      type: object
    githubPagesSource:
      properties:
        branch:
          description: Branch is the branch the site builds from.
          type: string
        path:
          description: 'Path is the directory within that branch: "/" or "/docs".'
          type: string
      type: object
    githubPagesUpdateReq:
      properties:
        branch:
          description: Branch switches the legacy source branch. Empty leaves the
            source alone.
          type: string
        buildType:
          description: 'BuildType switches the builder: "legacy" or "workflow". Empty
            leaves it.'
          type: string
        cname:
          description: |-
            CNAME is the custom domain. Omit to leave it alone, "" to clear it, or a
            valid FQDN to set it.
          type: string
        httpsEnforced:
          description: HTTPSEnforced toggles GitHub's enforce-HTTPS bit. Omit to leave
            it alone.
          type: boolean
        path:
          description: |-
            Path is the source directory to pair with Branch: "/" (the default) or
            "/docs". Read only when Branch is given.
          type: string
        repo:
          description: Repo is the repository, from the :repo path segment.
          type: string
      type: object
    githubPagesUpdatedOut:
      properties:
        repo:
          description: Repo is the repository that was updated.
          type: string
        updated:
          description: Updated is always true — a failure is an HTTP error, never
            this shape.
          type: boolean
      type: object
    githubPagesView:
      properties:
        buildType:
          description: 'BuildType is the builder in use: "legacy" (branch source)
            or "workflow".'
          type: string
        cname:
          description: CNAME is the custom domain, absent when none is set.
          type: string
        custom404:
          description: Custom404 is whether the repo ships its own 404 page.
          type: boolean
        httpsEnforced:
          description: HTTPSEnforced is GitHub's enforce-HTTPS bit.
          type: boolean
        repo:
          description: Repo is the repository the site belongs to.
          type: string
        source:
          $ref: '#/components/schemas/githubPagesSource'
          description: Source is the branch + path the site builds from. Absent under
            "workflow".
        status:
          description: |-
            Status is GitHub's build state: "built", "building" or "errored". Absent
            before the first build.
          type: string
        url:
          description: URL is the live site (GitHub's html_url).
          type: string
      type: object
    githubRepoView:
      properties:
        defaultBranch:
          description: DefaultBranch is the repo's default branch at GitHub.
          type: string
        fullName:
          description: FullName is GitHub's owner/name.
          type: string
        htmlUrl:
          description: HTMLURL is the repo's page at GitHub.
          type: string
        imported:
          description: Imported is whether this repo has been mirrored into git.hanzo.ai.
          type: boolean
        lastSyncedAt:
          description: LastSyncedAt is the last successful mirror, RFC 3339 UTC. Absent
            if never.
          type: string
        name:
          description: Name is the repository's short name within the installation.
          type: string
        private:
          description: Private is GitHub's visibility bit for the repo.
          type: boolean
        syncStatus:
          description: SyncStatus is "synced", "conflict", or "" when the repo is
            not imported.
          type: string
      type: object
    githubReposOut:
      properties:
        repos:
          description: Repos is every repo the installation grants. Never null; []
            when none.
          items:
            $ref: '#/components/schemas/githubRepoView'
          type: array
        unread:
          description: |-
            Unread names the connected accounts this answer could NOT read, so a short
            list is distinguishable from a complete one. Absent when the answer is whole.

            The fan-out is per installation, and one account failing used to be dropped
            in silence: the response stayed 200 and simply carried fewer repositories,
            erroring only when EVERY account failed. Measured, twice in a row, minutes
            apart: 1475 repositories, then 1157 — a whole installation missing with
            nothing in the answer to say so. Anything driven off the list then
            under-covers and reports success, which is the failure this field ends.
          items:
            type: string
          type: array
      type: object
    githubSearchHit:
      properties:
        clone_url:
          type: string
        default_branch:
          type: string
        description:
          type: string
        full_name:
          type: string
        html_url:
          type: string
        language:
          type: string
        private:
          type: boolean
        stars:
          type: integer
      type: object
    githubSearchOut:
      properties:
        count:
          type: integer
        repos:
          items:
            $ref: '#/components/schemas/githubSearchHit'
          type: array
      type: object
    githubSearchReq:
      properties:
        limit:
          description: |-
            Limit caps the answer; 0 takes the default and anything above the ceiling
            is clamped rather than refused.
          type: integer
        q:
          description: |-
            Q is GitHub's own search syntax, passed through: "tetris language:go",
            "org:hanzoai stars:>10". Passing it through rather than inventing a
            vocabulary means one thing to learn, and it is theirs.
          type: string
      type: object
    gitlabProjectView:
      properties:
        cloneUrl:
          description: CloneURL is the https remote to clone.
          type: string
        defaultBranch:
          description: |-
            DefaultBranch is the branch a clone lands on ("main" when GitLab names none,
            which is what an empty project reports).
          type: string
        description:
          description: Description is the project's own, empty when it has none.
          type: string
        fullName:
          description: |-
            FullName is the namespace path ("acme/widgets", "acme/team/widgets" for a
            subgroup) — the string GitLab calls path_with_namespace.
          type: string
        htmlUrl:
          description: HTMLURL is the project's page.
          type: string
        name:
          description: Name is the project's path segment ("widgets"), not its display
            name.
          type: string
        private:
          description: Private is true for anything not publicly visible (private
            or internal).
          type: boolean
        pushedAt:
          description: PushedAt is RFC3339 last activity, so a client can sort or
            say "2h ago".
          type: string
      type: object
    gitlabProjectsOut:
      properties:
        account:
          description: |-
            Account is the connected GitLab username, so a client can label the list
            without a second call. Empty when the connection recorded none.
          type: string
        projects:
          description: |-
            Projects is every project the token reaches, newest activity first. Never
            null; [] when the account has none.
          items:
            $ref: '#/components/schemas/gitlabProjectView'
          type: array
      type: object
    gpuAlertList:
      properties:
        alerts:
          description: |-
            Alerts is always empty, and typed as a raw list because Visor exposes no
            alert inventory for this surface to shape: there is nothing to describe
            until there is something to return.
          items:
            type: object
          type: array
      type: object
    gpuJob:
      properties:
        attempt:
          type: integer
        closeTime:
          type: string
        failureCause:
          type: string
        gpu:
          type: string
        id:
          type: string
        label:
          type: string
        lastHeartbeat:
          type: string
        leaseExpiry:
          type: string
        runId:
          type: string
        startTime:
          type: string
        status:
          description: queued|running|completed|failed|canceled
          type: string
        type:
          type: string
        worker:
          type: string
      type: object
    gpuList:
      properties:
        gpus:
          description: |-
            GPUs is every accelerator the org has, from Visor GPU droplets and from BYO
            workers alike.
          items:
            $ref: '#/components/schemas/gpuView'
          type: array
      type: object
    gpuView:
      properties:
        id:
          type: string
        location:
          type: string
        machine:
          type: string
        memory:
          type: string
        model:
          type: string
        name:
          type: string
        provider:
          description: |-
            Provider distinguishes a BYO accelerator ("byo") from a Visor-provisioned
            one (the machine's real provider). Memory is VRAM when known (BYO reports it
            from nvidia-smi; Visor's machine object carries none, so it stays empty and
            the UI renders "—"). Both are additive + omitempty: existing rows are
            unaffected and the console normalizer ignores fields it does not read.
          type: string
        region:
          type: string
        status:
          type: string
      type: object
    graphEdge:
      properties:
        from:
          type: string
        kind:
          description: parent | link | provenance
          type: string
        to:
          type: string
      type: object
    graphNode:
      properties:
        id:
          description: '"<doctype>:<name>" — globally unique, click-to-open key'
          type: string
        name:
          description: the document name (empty for synthetic nodes)
          type: string
        project:
          type: string
        title:
          description: display label
          type: string
        type:
          description: kb-page | kb-memory | kb-source | kb-connector | unresolved
          type: string
      type: object
    graphOut:
      properties:
        degraded:
          description: |-
            Degraded is true when the store was unreachable and this graph is honestly
            empty rather than wrong. Absent on a normal answer.
          type: boolean
        edges:
          description: Edges are the parent tree, the resolved wikilinks and the connector
            provenance.
          items:
            $ref: '#/components/schemas/graphEdge'
          type: array
        nodes:
          description: Nodes are the pages, memories, sources, connectors and unresolved
            link targets.
          items:
            $ref: '#/components/schemas/graphNode'
          type: array
      type: object
    healthLens:
      properties:
        available:
          description: Available reports whether that table exists in the warehouse
            right now.
          type: boolean
        table:
          description: Table is the fully-qualified warehouse table the lens reads.
          type: string
      type: object
    healthLenses:
      properties:
        events:
          $ref: '#/components/schemas/healthLens'
          description: |-
            Events is the web/commerce lens (event.fact, signal='act'), honest-empty until the
            collector emits.
        llm:
          $ref: '#/components/schemas/healthLens'
          description: LLM is the live per-org usage ledger lens (hanzo.cloud_usage).
      type: object
    healthOut:
      properties:
        engine:
          description: Engine names the evaluator this deployment runs.
          type: string
        ok:
          description: OK is true whenever the flag engine is serving.
          type: boolean
      type: object
    healthPlane:
      properties:
        bus:
          description: Bus is the address this process reaches the plane at.
          type: string
        ready:
          description: Ready reports whether an ingest would succeed right now. False
            is a 503.
          type: boolean
        reason:
          description: Reason is the plane's own failure text, present only when Ready
            is false.
          type: string
        stream:
          description: Stream is the JetStream stream every signal lands on.
          type: string
      type: object
    healthReport:
      properties:
        datastore:
          description: |-
            Datastore reports whether the shared warehouse client has a live connection.
            It is load-bearing for the READ path: false is one of the two ways this
            answers 503.
          type: boolean
        lenses:
          $ref: '#/components/schemas/healthLenses'
          description: |-
            Lenses is per-lens table availability, probed only when connected — so it is
            absent from a degraded report, which has nothing to say about tables it could
            not reach.
        lost:
          $ref: '#/components/schemas/loss'
          description: |-
            Lost is the count of facts the sink irrecoverably dropped since boot
            (warehouse.go). It is reported on the DEGRADED report too, and deliberately: a
            warehouse that is unreachable is exactly when facts start failing their
            deliveries, so suppressing the number here would hide it precisely when it
            moves. ANY NON-ZERO VALUE IS AN ALARM — it counts data the door already
            answered 200 for.
        plane:
          $ref: '#/components/schemas/healthPlane'
          description: |-
            Plane reports the event plane — the bus and the stream every accepted event is
            published to BEFORE any of it reaches the warehouse. It is load-bearing for the
            WRITE path, and it is here because its absence was a real outage: this endpoint
            answered 200/ok on warehouse connectivity alone while every POST /v1/event 503'd
            on a stream that could not bind, so 100% ingest loss was invisible to monitoring.
            A probe that cannot see the write path cannot report the write path.
        reason:
          description: Reason is the human-readable cause, present only on a degraded
            report.
          type: string
        service:
          description: |-
            Service names the subsystem answering, so a probe aggregating several health
            endpoints can attribute a degraded one.
          type: string
        status:
          description: |-
            Status is ok or degraded. Degraded is the 503 and means EITHER load-bearing
            dependency is down — the warehouse this subsystem reads, or the event plane it
            writes. It is not moved by a missing lens table, which is honest-empty.
          type: string
        warehouse:
          description: Warehouse names the datastore database every lens reads.
          type: string
      type: object
    helpArticle:
      properties:
        body:
          description: Body is the article's rich-text content as the author saved
            it.
          type: string
        category:
          description: |-
            Category is the name of the knowledge-base section the article sits in, or
            empty when it is filed under none.
          type: string
        excerpt:
          description: Excerpt is the short summary the author wrote for listings,
            or empty.
          type: string
        slug:
          description: |-
            Slug is the article's stable public identifier — the path segment it was
            addressed by.
          type: string
        title:
          description: Title is the article's headline.
          type: string
        updatedAt:
          description: UpdatedAt is the unix second the article was last written.
          type: integer
      type: object
    helpArticleCard:
      properties:
        category:
          description: |-
            Category is the name of the knowledge-base section the article sits in, or
            empty when it is filed under none.
          type: string
        excerpt:
          description: Excerpt is the short summary the author wrote for listings,
            or empty.
          type: string
        slug:
          description: |-
            Slug is the article's stable public identifier and the path segment
            /v1/help/articles/{slug} addresses it by.
          type: string
        title:
          description: Title is the article's headline.
          type: string
        updatedAt:
          description: |-
            UpdatedAt is the unix second the article was last written, in the help
            center's own store.
          type: integer
      type: object
    helpArticleList:
      properties:
        data:
          description: |-
            Data is the matching Published, public articles, newest write order last —
            the store's order, not a ranking. Empty when the center has none.
          items:
            $ref: '#/components/schemas/helpArticleCard'
          type: array
      type: object
    helpCategory:
      properties:
        description:
          description: Description is the section's blurb, or empty.
          type: string
        name:
          description: Name is the section's name, and the value an article's category
            matches.
          type: string
      type: object
    helpCategoryList:
      properties:
        data:
          description: |-
            Data is the sections that front at least one public article. Empty when the
            center publishes none.
          items:
            $ref: '#/components/schemas/helpCategory'
          type: array
      type: object
    helpTicketFiled:
      properties:
        status:
          description: Status is the lifecycle state the ticket was filed in — always
            "Open".
          type: string
        ticket:
          description: |-
            Ticket is the opaque, random customer-facing reference ("tkt_" + 24 hex
            characters). It is NOT the ticket's internal name: that name is sequential,
            and handing it out would disclose the center's ticket volume.
          type: string
      type: object
    helpTicketIntake:
      properties:
        description:
          description: |-
            Description is the customer's message. Optional; it becomes the ticket's
            description AND the opening entry of its conversation thread. Clipped at
            16 KiB.
          type: string
        email:
          description: |-
            Email is how the support team replies. Required; clipped at 320 characters
            (the RFC 5321 maximum). It is recorded as the ticket's customer, and it is
            not verified.
          type: string
        priority:
          description: |-
            Priority is Low, Medium, High or Urgent, case-insensitively. Anything else —
            including omitting it — is recorded as Medium rather than refused.
          type: string
        subject:
          description: |-
            Subject is the one-line summary of the problem. Required; longer than 300
            characters is clipped rather than refused.
          type: string
      type: object
    hit:
      properties:
        doctype:
          type: string
        name:
          type: string
        project:
          type: string
        provider:
          type: string
        score:
          type: number
        title:
          type: string
        url:
          type: string
      type: object
    holdings:
      properties:
        domains:
          description: Domains is the caller org's domains, newest registration first.
          items:
            $ref: '#/components/schemas/Holding'
          type: array
      type: object
    iam.AccountItem:
      properties:
        modifyRule:
          type: string
        name:
          type: string
        regex:
          type: string
        tab:
          type: string
        viewRule:
          type: string
        visible:
          type: boolean
      type: object
    iam.Address:
      properties:
        city:
          type: string
        line1:
          type: string
        line2:
          type: string
        region:
          type: string
        state:
          type: string
        tag:
          type: string
        zipCode:
          type: string
      type: object
    iam.Answer:
      properties:
        code:
          type: string
        data:
          type: object
        data2:
          type: object
        data3:
          type: object
        msg:
          type: string
        name:
          type: string
        status:
          type: string
        sub:
          type: string
      type: object
    iam.Application:
      properties:
        affiliationUrl:
          type: string
        category:
          type: string
        cert:
          type: string
        certObj:
          $ref: '#/components/schemas/iam.Cert'
        certPublicKey:
          type: string
        clientCert:
          type: string
        clientId:
          description: |-
            ClientId is the OAuth2/OIDC client identifier and the GLOBAL key every
            confidential-client resolver authenticates against (store.GetApplicationByClientId,
            the mint gates, Basic auth). It MUST be globally unique across ALL owners — a
            collision would let one app shadow another at that key. This store persists each
            entity as a JSON document in a shared table, so there is no per-field column to
            carry a DB UNIQUE index; uniqueness is enforced at the write in
            applications.Create/Update (ensureClientIdUnique), exactly as the (owner,name)
            natural key is, and store.GetApplicationByClientId resolves admin-preferring as
            defense-in-depth.
          type: string
        clientSecret:
          type: string
        codeResendTimeout:
          type: integer
        cookieExpireInHours:
          type: integer
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        customScopes:
          items:
            $ref: '#/components/schemas/iam.ScopeDescription'
          type: array
        defaultGroup:
          type: string
        deleted:
          type: boolean
        description:
          type: string
        disableSamlAttributes:
          type: boolean
        disableSignin:
          type: boolean
        displayName:
          type: string
        domain:
          type: string
        enableAutoSignin:
          type: boolean
        enableCodeSignin:
          type: boolean
        enableExclusiveSignin:
          type: boolean
        enableLinkWithEmail:
          type: boolean
        enablePassword:
          type: boolean
        enableSamlAssertionSignature:
          type: boolean
        enableSamlC14n10:
          type: boolean
        enableSamlCompress:
          type: boolean
        enableSamlPostBinding:
          type: boolean
        enableSignUp:
          type: boolean
        enableSigninSession:
          type: boolean
        enableWebAuthn:
          type: boolean
        environment:
          type: string
        expireInHours:
          type: number
        failedSigninFrozenTime:
          type: integer
        failedSigninLimit:
          type: integer
        favicon:
          type: string
        footerHtml:
          type: string
        forcedRedirectOrigin:
          type: string
        forgetUrl:
          type: string
        formBackgroundUrl:
          type: string
        formBackgroundUrlMobile:
          type: string
        formCss:
          type: string
        formCssMobile:
          type: string
        formOffset:
          type: integer
        formSideHtml:
          type: string
        grantTypes:
          items:
            type: string
          type: array
        headerHtml:
          type: string
        homepageUrl:
          type: string
        id:
          type: string
        ipRestriction:
          type: string
        ipWhitelist:
          type: string
        isShared:
          type: boolean
        logo:
          type: string
        name:
          type: string
        order:
          type: integer
        orgChoiceMode:
          type: string
        organization:
          type: string
        organizationObj:
          $ref: '#/components/schemas/iam.Organization'
        otherDomains:
          items:
            type: string
          type: array
        owner:
          type: string
        project:
          type: string
        providers:
          items:
            $ref: '#/components/schemas/iam.ProviderItem'
          type: array
        redirectUris:
          items:
            type: string
          type: array
        refreshExpireInHours:
          type: number
        samlAttributes:
          items:
            $ref: '#/components/schemas/iam.SamlItem'
          type: array
        samlHashAlgorithm:
          type: string
        samlReplyUrl:
          type: string
        scopes:
          items:
            $ref: '#/components/schemas/iam.ScopeItem'
          type: array
        signinHtml:
          type: string
        signinItems:
          items:
            $ref: '#/components/schemas/iam.SigninItem'
          type: array
        signinMethods:
          items:
            $ref: '#/components/schemas/iam.SigninMethod'
          type: array
        signinUrl:
          type: string
        signupHtml:
          type: string
        signupItems:
          items:
            $ref: '#/components/schemas/iam.SignupItem'
          type: array
        signupUrl:
          type: string
        sslCert:
          type: string
        sslMode:
          type: string
        tags:
          items:
            type: string
          type: array
        termsOfUse:
          type: string
        themeData:
          $ref: '#/components/schemas/iam.ThemeData'
        title:
          type: string
        tokenAttributes:
          items:
            $ref: '#/components/schemas/iam.JwtItem'
          type: array
        tokenFields:
          items:
            type: string
          type: array
        tokenFormat:
          type: string
        tokenSigningMethod:
          type: string
        type:
          type: string
        updatedAt:
          format: date-time
          type: string
        upstreamHost:
          type: string
        useEmailAsSamlNameId:
          type: boolean
      type: object
    iam.ApplicationListResult:
      properties:
        applications:
          items:
            $ref: '#/components/schemas/iam.Application'
          type: array
      type: object
    iam.ApplicationRef:
      properties:
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      type: object
    iam.AuditLog:
      properties:
        action:
          type: string
        clientIp:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        id:
          type: string
        isTriggered:
          type: boolean
        language:
          type: string
        method:
          type: string
        name:
          type: string
        object:
          type: string
        organization:
          type: string
        owner:
          type: string
        requestUri:
          type: string
        response:
          type: string
        statusCode:
          type: integer
        updatedAt:
          format: date-time
          type: string
        user:
          type: string
      type: object
    iam.CartItem:
      properties:
        displayName:
          type: string
        name:
          type: string
        owner:
          type: string
        price:
          type: number
        quantity:
          type: integer
      type: object
    iam.Cert:
      properties:
        accessKey:
          type: string
        accessSecret:
          type: string
        account:
          type: string
        bitSize:
          type: integer
        certificate:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        cryptoAlgorithm:
          type: string
        deleted:
          type: boolean
        displayName:
          type: string
        domainExpireTime:
          type: string
        expireInYears:
          type: integer
        expireTime:
          type: string
        id:
          type: string
        name:
          type: string
        owner:
          type: string
        privateKey:
          type: string
        provider:
          type: string
        scope:
          type: string
        type:
          type: string
        updatedAt:
          format: date-time
          type: string
      type: object
    iam.ConsentRecord:
      properties:
        application:
          type: string
        grantedScopes:
          items:
            type: string
          type: array
      type: object
    iam.CreateInput:
      properties:
        password:
          type: string
        user:
          $ref: '#/components/schemas/iam.User'
      type: object
    iam.CreateOrganizationInput:
      properties:
        accountItems:
          items:
            $ref: '#/components/schemas/iam.AccountItem'
          type: array
        accountMenu:
          type: string
        avatar:
          type: string
        balanceCredit:
          type: number
        balanceCurrency:
          type: string
        countryCodes:
          items:
            type: string
          type: array
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        dcrPolicy:
          type: string
        defaultApplication:
          type: string
        defaultAvatar:
          type: string
        defaultPassword:
          type: string
        deleted:
          type: boolean
        disableSignin:
          type: boolean
        displayName:
          type: string
        emoji:
          type: string
        enableSoftDeletion:
          type: boolean
        enableTour:
          type: boolean
        failedSigninFrozenTime:
          type: integer
        failedSigninLimit:
          type: integer
        favicon:
          type: string
        founder:
          type: string
        hasPrivilegeConsent:
          type: boolean
        id:
          type: string
        initScore:
          type: integer
        ipRestriction:
          type: string
        ipWhitelist:
          type: string
        isPersonal:
          type: boolean
        isProfilePublic:
          type: boolean
        kerberosKdcHost:
          type: string
        kerberosKeytab:
          type: string
        kerberosRealm:
          type: string
        kerberosServiceName:
          type: string
        languages:
          items:
            type: string
          type: array
        ldapAttributes:
          items:
            type: string
          type: array
        logo:
          type: string
        logoDark:
          type: string
        masterPassword:
          type: string
        masterVerificationCode:
          type: string
        mfaItems:
          items:
            $ref: '#/components/schemas/iam.MfaItem'
          type: array
        mfaRememberInHours:
          type: integer
        name:
          type: string
        navItems:
          items:
            type: string
          type: array
        orgBalance:
          type: number
        owner:
          type: string
        passwordExpireDays:
          type: integer
        passwordObfuscatorKey:
          type: string
        passwordObfuscatorType:
          type: string
        passwordOptions:
          items:
            type: string
          type: array
        passwordSalt:
          type: string
        passwordType:
          type: string
        tags:
          items:
            type: string
          type: array
        themeData:
          $ref: '#/components/schemas/iam.ThemeData'
        updatedAt:
          format: date-time
          type: string
        useEmailAsUsername:
          type: boolean
        usePermanentAvatar:
          type: boolean
        userBalance:
          type: number
        userNavItems:
          items:
            type: string
          type: array
        userTypes:
          items:
            type: string
          type: array
        websiteUrl:
          type: string
        widgetItems:
          items:
            type: string
          type: array
      type: object
    iam.CreateSessionIn:
      properties:
        application:
          type: string
        exclusiveSignin:
          type: boolean
        name:
          type: string
        owner:
          type: string
        sessionId:
          items:
            type: string
          type: array
      required:
      - owner
      - name
      - application
      type: object
    iam.DeleteOrganizationInput:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.DeleteOrganizationOutput:
      properties:
        affected:
          type: boolean
      type: object
    iam.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.DeleteResponse:
      properties:
        deleted:
          type: boolean
      type: object
    iam.DeleteResult:
      properties:
        deleted:
          type: boolean
      type: object
    iam.DeleteSessionOut:
      properties:
        deleted:
          type: boolean
      type: object
    iam.FaceId:
      properties:
        faceIdData:
          items:
            type: number
          type: array
        imageUrl:
          type: string
        name:
          type: string
      type: object
    iam.Input:
      properties:
        createdTime:
          type: string
        description:
          type: string
        displayName:
          type: string
        isDefault:
          type: boolean
        metadata:
          type: string
        name:
          type: string
        organization:
          type: string
        owner:
          type: string
        tags:
          items:
            type: string
          type: array
        workspace:
          type: string
      type: object
    iam.Invitation:
      properties:
        application:
          type: string
        code:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        defaultCode:
          type: string
        deleted:
          type: boolean
        displayName:
          type: string
        email:
          type: string
        id:
          type: string
        isRegexp:
          type: boolean
        name:
          type: string
        owner:
          type: string
        phone:
          type: string
        quota:
          type: integer
        signupGroup:
          type: string
        state:
          type: string
        updatedAt:
          format: date-time
          type: string
        updatedTime:
          type: string
        usedCount:
          type: integer
        username:
          type: string
      type: object
    iam.JwtItem:
      properties:
        category:
          type: string
        name:
          type: string
        type:
          type: string
        value:
          type: string
      type: object
    iam.Key:
      properties:
        accessKey:
          description: |-
            AccessKey (pk-*) is the publishable identifier and lookup index;
            AccessSecret (sk-*) is the confidential secret.
          type: string
        accessSecret:
          type: string
        accessSecretDigest:
          type: string
        application:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          description: |-
            CreatedTime and UpdatedTime are RFC3339 audit stamps carried as strings
            for byte-parity with the v1 row (orm.Model separately tracks CreatedAt /
            UpdatedAt as time.Time for the store's own lifecycle).
          type: string
        deleted:
          type: boolean
        displayName:
          description: DisplayName is the human-facing label.
          type: string
        expireTime:
          description: |-
            ExpireTime is when the key stops being honored (empty = never). State is
            the lifecycle flag ("Active", "test", …); "test" mints test-env
            credentials instead of live ones.
          type: string
        id:
          type: string
        name:
          type: string
        organization:
          type: string
        owner:
          description: Owner is the tenant that holds the key; Name is unique within
            Owner.
          type: string
        scope:
          description: |-
            Scope is the key's ACCESS CLASS, orthogonal to Type (which names the bound
            principal). Empty (the default, "secret") is a full key: a pk- publishable
            half AND a confidential sk- half, the sk- authenticating a server-side reader.
            KeyScopePublish is a WRITE-ONLY publishable key — a pk- half only, no secret —
            that resolves to just an ORG (never a principal) at the ingest door and is safe
            to ship in client JS. A missing value on an existing row reads as the default,
            so every pre-Scope key is a secret key unchanged.
          type: string
        state:
          type: string
        type:
          description: |-
            Type is the scope the key is bound to — "Organization", "Application",
            "User", or "General" — and Organization / Application / User name the
            concrete principal for whichever scope Type selects.
          type: string
        updatedAt:
          format: date-time
          type: string
        updatedTime:
          type: string
        user:
          type: string
      type: object
    iam.ListOrganizationsOutput:
      properties:
        count:
          type: integer
        organizations:
          items:
            $ref: '#/components/schemas/iam.Organization'
          type: array
      type: object
    iam.ListOutput:
      properties:
        auditLogs:
          items:
            $ref: '#/components/schemas/iam.AuditLog'
          type: array
        total:
          type: integer
      type: object
    iam.ListResponse:
      properties:
        keys:
          items:
            $ref: '#/components/schemas/iam.Key'
          type: array
      type: object
    iam.ListSessionsIn:
      properties:
        application:
          type: string
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      type: object
    iam.ListSessionsOut:
      properties:
        sessions:
          items:
            $ref: '#/components/schemas/iam.Session'
          type: array
      type: object
    iam.ManagedAccount:
      properties:
        application:
          type: string
        signinUrl:
          type: string
        username:
          type: string
      type: object
    iam.MfaAccount:
      properties:
        accountName:
          type: string
        issuer:
          type: string
        origin:
          type: string
      type: object
    iam.MfaItem:
      properties:
        name:
          type: string
        rule:
          type: string
      type: object
    iam.MfaProps:
      properties:
        countryCode:
          type: string
        enabled:
          type: boolean
        isPreferred:
          type: boolean
        mfaRememberInHours:
          type: integer
        mfaType:
          type: string
        url:
          type: string
      type: object
    iam.Organization:
      properties:
        accountItems:
          items:
            $ref: '#/components/schemas/iam.AccountItem'
          type: array
        accountMenu:
          type: string
        avatar:
          description: |-
            How the organization appears across Hanzo — the square mark beside its
            name — as an image or as one emoji, never both. It is the pair a person
            carries (User.Avatar) under the same names, resolved the same way, so a
            screen draws a subject without asking which kind of subject it has. Both
            halves live on the row: a mark that appears everywhere cannot be kept on
            one device. Written through schema.MarkOf; Logo and LogoDark above are a
            different thing, the wordmark a login screen draws.
          type: string
        balanceCredit:
          type: number
        balanceCurrency:
          type: string
        countryCodes:
          items:
            type: string
          type: array
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        dcrPolicy:
          type: string
        defaultApplication:
          type: string
        defaultAvatar:
          type: string
        defaultPassword:
          type: string
        deleted:
          type: boolean
        disableSignin:
          type: boolean
        displayName:
          type: string
        emoji:
          type: string
        enableSoftDeletion:
          type: boolean
        enableTour:
          type: boolean
        failedSigninFrozenTime:
          type: integer
        failedSigninLimit:
          description: |-
            Per-organization signin throttle. Zero means "inherit the application
            default"; a non-zero value overrides it. Safe bounds are clamped by the
            resource service before persistence.
          type: integer
        favicon:
          type: string
        founder:
          description: |-
            Founder is the stable storage id of the identity that provisioned this org
            (self-service onboarding). It is the resume token that makes provisioning
            converge on a backend where each write autocommits independently (no
            transaction rollback): after a partial failure that created the org but did
            not move the founder in, a retry recognises the org as the founder's own and
            completes it, instead of refusing it as "already taken". It also fences the
            org to ONE tenant — a different identity can never complete or join it.
          type: string
        hasPrivilegeConsent:
          type: boolean
        id:
          type: string
        initScore:
          type: integer
        ipRestriction:
          type: string
        ipWhitelist:
          type: string
        isPersonal:
          type: boolean
        isProfilePublic:
          type: boolean
        kerberosKdcHost:
          type: string
        kerberosKeytab:
          type: string
        kerberosRealm:
          type: string
        kerberosServiceName:
          type: string
        languages:
          items:
            type: string
          type: array
        ldapAttributes:
          items:
            type: string
          type: array
        logo:
          type: string
        logoDark:
          type: string
        masterPassword:
          type: string
        masterVerificationCode:
          type: string
        mfaItems:
          items:
            $ref: '#/components/schemas/iam.MfaItem'
          type: array
        mfaRememberInHours:
          type: integer
        name:
          type: string
        navItems:
          items:
            type: string
          type: array
        orgBalance:
          description: |-
            Balance fields are read-only mirrors; authoritative balances live in
            Commerce (billing.hanzo.ai). Carried for field-complete v1 parity.
          type: number
        owner:
          type: string
        passwordExpireDays:
          type: integer
        passwordObfuscatorKey:
          type: string
        passwordObfuscatorType:
          type: string
        passwordOptions:
          items:
            type: string
          type: array
        passwordSalt:
          type: string
        passwordType:
          type: string
        tags:
          items:
            type: string
          type: array
        themeData:
          $ref: '#/components/schemas/iam.ThemeData'
        updatedAt:
          format: date-time
          type: string
        useEmailAsUsername:
          type: boolean
        usePermanentAvatar:
          type: boolean
        userBalance:
          type: number
        userNavItems:
          items:
            type: string
          type: array
        userTypes:
          items:
            type: string
          type: array
        websiteUrl:
          type: string
        widgetItems:
          items:
            type: string
          type: array
      type: object
    iam.Permission:
      properties:
        actions:
          items:
            type: string
          type: array
        adapter:
          type: string
        approveTime:
          type: string
        approver:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          description: Descriptive metadata.
          type: string
        deleted:
          type: boolean
        description:
          type: string
        displayName:
          type: string
        domains:
          items:
            type: string
          type: array
        effect:
          type: string
        groups:
          items:
            type: string
          type: array
        id:
          type: string
        isEnabled:
          type: boolean
        model:
          description: |-
            Authorization model, targets, and decision. AuthzModel carries the v1
            `model` column (the named authz model); it is not the Go identifier
            `Model` because that name is taken by the embedded orm.Model[Permission]
            mixin. The HTTP contract is unchanged — json:"model".
          type: string
        name:
          type: string
        owner:
          description: Identity — the (owner, name) natural key.
          type: string
        resourceType:
          type: string
        resources:
          items:
            type: string
          type: array
        roles:
          items:
            type: string
          type: array
        state:
          type: string
        submitter:
          description: Submission / approval workflow.
          type: string
        updatedAt:
          format: date-time
          type: string
        users:
          description: Subjects the grant is evaluated for.
          items:
            type: string
          type: array
      type: object
    iam.Project:
      properties:
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        description:
          type: string
        displayName:
          type: string
        id:
          type: string
        isDefault:
          type: boolean
        metadata:
          type: string
        name:
          type: string
        organization:
          type: string
        owner:
          type: string
        tags:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        workspace:
          type: string
      type: object
    iam.Provider:
      properties:
        appId:
          type: string
        bucket:
          type: string
        category:
          type: string
        cert:
          type: string
        clientId:
          type: string
        clientId2:
          type: string
        clientSecret:
          type: string
        clientSecret2:
          type: string
        content:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        customAuthUrl:
          type: string
        customLogo:
          type: string
        customTokenUrl:
          type: string
        customUserInfoUrl:
          type: string
        deleted:
          type: boolean
        disableSsl:
          type: boolean
        displayName:
          type: string
        domain:
          type: string
        emailRegex:
          type: string
        enablePkce:
          type: boolean
        enableProxy:
          type: boolean
        enableSignAuthnRequest:
          type: boolean
        endpoint:
          type: string
        host:
          type: string
        httpHeaders:
          additionalProperties:
            type: string
          type: object
        id:
          type: string
        idP:
          type: string
        intranetEndpoint:
          type: string
        issuerUrl:
          type: string
        metadata:
          type: string
        method:
          type: string
        name:
          type: string
        owner:
          type: string
        pathPrefix:
          type: string
        port:
          type: integer
        providerUrl:
          type: string
        receiver:
          type: string
        regionId:
          type: string
        scopes:
          type: string
        signName:
          type: string
        sslMode:
          type: string
        subType:
          type: string
        templateCode:
          type: string
        title:
          type: string
        type:
          type: string
        updatedAt:
          format: date-time
          type: string
        userMapping:
          additionalProperties:
            type: string
          type: object
      type: object
    iam.ProviderItem:
      properties:
        bindingRule:
          items:
            type: string
          type: array
        canSignIn:
          type: boolean
        canSignUp:
          type: boolean
        canUnlink:
          type: boolean
        countryCodes:
          items:
            type: string
          type: array
        name:
          type: string
        owner:
          type: string
        prompted:
          type: boolean
        provider:
          $ref: '#/components/schemas/iam.Provider'
        rule:
          type: string
        signupGroup:
          type: string
      type: object
    iam.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.Role:
      properties:
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        description:
          type: string
        displayName:
          type: string
        domains:
          items:
            type: string
          type: array
        groups:
          items:
            type: string
          type: array
        id:
          type: string
        isEnabled:
          type: boolean
        name:
          type: string
        owner:
          type: string
        roles:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        users:
          items:
            type: string
          type: array
      type: object
    iam.SamlItem:
      properties:
        name:
          type: string
        nameFormat:
          type: string
        value:
          type: string
      type: object
    iam.ScopeDescription:
      properties:
        description:
          type: string
        displayName:
          type: string
        scope:
          type: string
      type: object
    iam.ScopeItem:
      properties:
        description:
          type: string
        displayName:
          type: string
        name:
          type: string
        tools:
          items:
            type: string
          type: array
      type: object
    iam.SearchOrganizationsOutput:
      properties:
        cursor:
          type: string
        organizations:
          items:
            $ref: '#/components/schemas/iam.Organization'
          type: array
      type: object
    iam.Session:
      properties:
        application:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        exclusiveSignin:
          description: |-
            ExclusiveSignin is a transient control flag (v1 xorm:"-"): a caller sets
            it on a create to collapse SessionId down to the single incoming cookie
            instead of appending. It is never stored — a persisted session always
            carries it false, so orm:"-" keeps it off the column backends and
            omitempty keeps it out of the SQLite JSON blob.
          type: boolean
        id:
          type: string
        name:
          type: string
        owner:
          type: string
        sessionId:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
      type: object
    iam.SessionRef:
      properties:
        application:
          type: string
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      - application
      type: object
    iam.SetAvatarInput:
      properties:
        avatar:
          type: string
        emoji:
          type: string
        name:
          type: string
        owner:
          type: string
      type: object
    iam.SigninItem:
      properties:
        customCss:
          type: string
        isCustom:
          type: boolean
        label:
          type: string
        name:
          type: string
        placeholder:
          type: string
        rule:
          type: string
        visible:
          type: boolean
      type: object
    iam.SigninMethod:
      properties:
        displayName:
          type: string
        name:
          type: string
        rule:
          type: string
      type: object
    iam.SignupItem:
      properties:
        customCss:
          type: string
        label:
          type: string
        name:
          type: string
        options:
          items:
            type: string
          type: array
        placeholder:
          type: string
        prompted:
          type: boolean
        regex:
          type: string
        required:
          type: boolean
        rule:
          type: string
        type:
          type: string
        visible:
          type: boolean
      type: object
    iam.ThemeData:
      properties:
        borderRadius:
          type: integer
        colorPrimary:
          type: string
        isCompact:
          type: boolean
        isEnabled:
          type: boolean
        themeType:
          type: string
      type: object
    iam.Token:
      properties:
        accessToken:
          type: string
        accessTokenHash:
          type: string
        application:
          type: string
        code:
          type: string
        codeChallenge:
          type: string
        codeChallengeMethod:
          type: string
        codeExpireIn:
          type: integer
        codeIsUsed:
          type: boolean
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        expiresIn:
          type: integer
        id:
          type: string
        name:
          type: string
        nonce:
          description: |-
            Nonce is the OIDC authorize nonce, stored on the code and echoed into the
            id_token minted at the exchange (OIDC Core §3.1.3.6) so a relying party
            binds the id_token to its own request and detects replay.
          type: string
        organization:
          type: string
        owner:
          type: string
        publicGrant:
          description: |-
            PublicGrant records that this grant was established WITHOUT client
            authentication — a PKCE code exchange from a client that presented no
            secret. Whether a client is confidential is a property of the GRANT, not
            only of the registration: `hanzo-cli` and every @hanzo/iam SPA keep a
            registered secret for a BACKEND path while the surface that actually signs
            in is a public PKCE client that cannot hold one. authorizationCodeGrant
            already makes exactly that bounded relaxation; this is the same fact,
            recorded so refreshTokenGrant can honour it instead of demanding a secret
            the client never had (which 401s invalid_client and kills the session at
            the access token's expiry). Carried across rotation, so the second refresh
            behaves like the first.
          type: boolean
        redirectUri:
          description: |-
            RedirectUri binds the authorization code to the exact redirect URI of the
            authorize request (RFC 6749 §4.1.3): the token endpoint refuses a code
            redeemed with a different redirect_uri, closing code-injection across a
            client's registered URIs.
          type: string
        refreshConsumed:
          type: boolean
        refreshExpireIn:
          type: integer
        refreshFamily:
          description: |-
            Refresh-token rotation state (v2). Each refresh belongs to a family (the
            grant); rotation mints a new row in the same family and marks the prior
            one consumed. Presenting a consumed refresh is reuse — the whole family is
            revoked (RFC 9700 §4.14.2). RefreshExpireIn is the refresh token's own
            absolute expiry (unix), independent of the access token's shorter life.
          type: string
        refreshToken:
          type: string
        refreshTokenHash:
          type: string
        resource:
          description: RFC 8707 resource indicator
          type: string
        scope:
          type: string
        tokenType:
          type: string
        updatedAt:
          format: date-time
          type: string
        user:
          type: string
        userCode:
          type: string
      type: object
    iam.UpdateInput:
      properties:
        password:
          type: string
        user:
          $ref: '#/components/schemas/iam.User'
      type: object
    iam.UpdateOrganizationInput:
      properties:
        accountItems:
          items:
            $ref: '#/components/schemas/iam.AccountItem'
          type: array
        accountMenu:
          type: string
        avatar:
          type: string
        balanceCredit:
          type: number
        balanceCurrency:
          type: string
        countryCodes:
          items:
            type: string
          type: array
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        dcrPolicy:
          type: string
        defaultApplication:
          type: string
        defaultAvatar:
          type: string
        defaultPassword:
          type: string
        deleted:
          type: boolean
        disableSignin:
          type: boolean
        displayName:
          type: string
        emoji:
          type: string
        enableSoftDeletion:
          type: boolean
        enableTour:
          type: boolean
        failedSigninFrozenTime:
          type: integer
        failedSigninLimit:
          type: integer
        favicon:
          type: string
        founder:
          type: string
        hasPrivilegeConsent:
          type: boolean
        id:
          type: string
        initScore:
          type: integer
        ipRestriction:
          type: string
        ipWhitelist:
          type: string
        isPersonal:
          type: boolean
        isProfilePublic:
          type: boolean
        kerberosKdcHost:
          type: string
        kerberosKeytab:
          type: string
        kerberosRealm:
          type: string
        kerberosServiceName:
          type: string
        languages:
          items:
            type: string
          type: array
        ldapAttributes:
          items:
            type: string
          type: array
        logo:
          type: string
        logoDark:
          type: string
        masterPassword:
          type: string
        masterVerificationCode:
          type: string
        mfaItems:
          items:
            $ref: '#/components/schemas/iam.MfaItem'
          type: array
        mfaRememberInHours:
          type: integer
        name:
          type: string
        navItems:
          items:
            type: string
          type: array
        orgBalance:
          type: number
        owner:
          type: string
        passwordExpireDays:
          type: integer
        passwordObfuscatorKey:
          type: string
        passwordObfuscatorType:
          type: string
        passwordOptions:
          items:
            type: string
          type: array
        passwordSalt:
          type: string
        passwordType:
          type: string
        tags:
          items:
            type: string
          type: array
        themeData:
          $ref: '#/components/schemas/iam.ThemeData'
        updatedAt:
          format: date-time
          type: string
        useEmailAsUsername:
          type: boolean
        usePermanentAvatar:
          type: boolean
        userBalance:
          type: number
        userNavItems:
          items:
            type: string
          type: array
        userTypes:
          items:
            type: string
          type: array
        websiteUrl:
          type: string
        widgetItems:
          items:
            type: string
          type: array
      type: object
    iam.UpdateSessionIn:
      properties:
        application:
          type: string
        name:
          type: string
        owner:
          type: string
        sessionId:
          items:
            type: string
          type: array
      required:
      - owner
      - name
      - application
      type: object
    iam.User:
      properties:
        accessKey:
          description: |-
            API credentials. AccessSecret / AccessSecretHash / the OAuth tokens are
            bearer material. AccessSecretHash MUST persist (orm stores via JSON; a
            json:"-" field is never saved), so it carries a real json tag and the
            handler's redact() strips it (and AccessSecret + the token fields) before
            responding.
          type: string
        accessSecret:
          type: string
        accessSecretHash:
          type: string
        accessToken:
          type: string
        address:
          items:
            type: string
          type: array
        addresses:
          items:
            $ref: '#/components/schemas/iam.Address'
          type: array
        adfs:
          type: string
        affiliation:
          type: string
        alipay:
          type: string
        amazon:
          type: string
        apple:
          type: string
        applicationScopes:
          items:
            $ref: '#/components/schemas/iam.ConsentRecord'
          type: array
        auth0:
          type: string
        avatar:
          type: string
        avatarType:
          type: string
        azuread:
          type: string
        azureadb2c:
          type: string
        baidu:
          type: string
        balance:
          description: |-
            Balance mirrors v1 for lossless migration but is authoritative in
            Commerce (billing.hanzo.ai), not here — do not write it from IAM.
          type: number
        balanceCredit:
          type: number
        balanceCurrency:
          type: string
        battlenet:
          type: string
        bilibili:
          type: string
        bio:
          type: string
        birthday:
          type: string
        bitbucket:
          type: string
        box:
          type: string
        cart:
          items:
            $ref: '#/components/schemas/iam.CartItem'
          type: array
        cloudfoundry:
          type: string
        countryCode:
          type: string
        createdAt:
          format: date-time
          type: string
        createdIp:
          description: Sign-in provenance.
          type: string
        createdTime:
          type: string
        currency:
          type: string
        custom:
          type: string
        custom2:
          type: string
        custom3:
          type: string
        custom4:
          type: string
        custom5:
          type: string
        custom6:
          type: string
        custom7:
          type: string
        custom8:
          type: string
        custom9:
          type: string
        custom10:
          type: string
        dailymotion:
          type: string
        deezer:
          type: string
        deleted:
          type: boolean
        deletedTime:
          type: string
        digitalocean:
          type: string
        dingtalk:
          type: string
        discord:
          type: string
        displayName:
          description: Profile.
          type: string
        douyin:
          type: string
        dropbox:
          type: string
        education:
          type: string
        email:
          type: string
        emailVerified:
          type: boolean
        eveonline:
          type: string
        externalId:
          type: string
        faceIds:
          items:
            $ref: '#/components/schemas/iam.FaceId'
          type: array
        facebook:
          type: string
        firstName:
          type: string
        fitbit:
          type: string
        gender:
          type: string
        gitea:
          type: string
        gitee:
          type: string
        github:
          description: Linked federated-identity subjects, one column per connector
            (v1 parity).
          type: string
        gitlab:
          type: string
        google:
          type: string
        groups:
          items:
            type: string
          type: array
        hash:
          type: string
        heroku:
          type: string
        homepage:
          type: string
        iam:
          type: string
        id:
          description: |-
            Id is the user's STABLE OPAQUE identifier — the value the OIDC `sub` claim
            carries. It is the v1 the legacy surface per-row UUID (e.g.
            "e7d7fda0-4c53-4508-9d35-7ec892b7e5d7"), migrated verbatim so a user's `sub`
            is byte-identical across the cutover: every live session, external reference,
            and the downstream money-path principal keyed on `sub` survive unchanged. A
            user minted natively in v2 is assigned a fresh UUID here on create, so the
            `sub` is ALWAYS a stable opaque id going forward — never the (Owner, Name)
            pair, which is mutable (a rename would otherwise silently reissue identity).

            It is distinct from the embedded orm.Model STORAGE KEY — the value the datastore
            locks and looks a row up by — which is NOT (Owner, Name) for every row: a MIGRATED
            legacy row is stamped "owner/name" (SetId in the migrator), but a v2-native
            users.Create'd row is NOT — Create allocates rather than pinning a key, so its
            storage key is a store-assigned surrogate id (a decimal string like
            "17847909129933610000001"). (Owner, Name) is therefore the natural/QUERY key
            (unique, indexed), not necessarily the storage key: resolve a row for a locked
            write by its REAL key (store.GetUserByName(...).Key().Encode(), which stamps both
            shapes — see internal/oidc updateUser), never by assuming "owner/name". This Id is
            a first-class, indexed DOMAIN field; its json tag "id" dominates the promoted
            orm.Model `Id_` (also "id") by shallower depth, so the persisted record's "id" is
            this UUID — exactly the v1 shape. A row that carries no Id (a not-yet-assigned
            pre-cutover user) falls back to the (Owner, Name) subject at mint; every other
            path resolves `sub`→user by Id.
          type: string
        idCard:
          type: string
        idCardType:
          type: string
        influxcloud:
          type: string
        infoflow:
          type: string
        instagram:
          type: string
        intercom:
          type: string
        invitation:
          type: string
        invitationCode:
          type: string
        ipWhitelist:
          type: string
        isAdmin:
          type: boolean
        isDefaultAvatar:
          description: State flags.
          type: boolean
        isDeleted:
          type: boolean
        isForbidden:
          type: boolean
        isOnline:
          type: boolean
        isVerified:
          type: boolean
        kakao:
          type: string
        karma:
          type: integer
        kwai:
          type: string
        language:
          type: string
        lark:
          type: string
        lastChangePasswordTime:
          type: string
        lastName:
          type: string
        lastSigninIp:
          type: string
        lastSigninTime:
          type: string
        lastSigninWrongTime:
          type: string
        lastfm:
          type: string
        ldap:
          type: string
        line:
          type: string
        linkedin:
          type: string
        location:
          type: string
        mailru:
          type: string
        managedAccounts:
          items:
            $ref: '#/components/schemas/iam.ManagedAccount'
          type: array
        meetup:
          type: string
        mfaAccounts:
          items:
            $ref: '#/components/schemas/iam.MfaAccount'
          type: array
        mfaEmailEnabled:
          type: boolean
        mfaItems:
          items:
            $ref: '#/components/schemas/iam.MfaItem'
          type: array
        mfaPhoneEnabled:
          type: boolean
        mfaPushEnabled:
          type: boolean
        mfaPushProvider:
          type: string
        mfaPushReceiver:
          type: string
        mfaRadiusEnabled:
          type: boolean
        mfaRadiusProvider:
          type: string
        mfaRadiusUsername:
          type: string
        mfaRememberDeadline:
          type: string
        mfaRememberDigest:
          description: |-
            MfaRememberDigest is the digest of the token held by the ONE browser the
            deadline above applies to. Without it the deadline is account-wide and "don't
            ask again on this browser" switches the second factor off everywhere. It is a
            digest, never the token, so a database dump yields nothing presentable — and it
            carries a REAL json tag because orm persists via json.Marshal, so `json:"-"`
            would never be stored (the trap PasswordHash documents above); Mask() strips it
            from every response instead.
          type: string
        microsoftonline:
          type: string
        multiFactorAuths:
          items:
            $ref: '#/components/schemas/iam.MfaProps'
          type: array
        name:
          type: string
        naver:
          type: string
        needUpdatePassword:
          type: boolean
        nextcloud:
          type: string
        okta:
          type: string
        onedrive:
          type: string
        originalRefreshToken:
          type: string
        originalToken:
          type: string
        oura:
          type: string
        owner:
          description: Identity / tenancy. (Owner, Name) is the natural key.
          type: string
        passwordHash:
          description: |-
            Credential material. PasswordHash is a one-way bcrypt digest and is
            verify-only. It MUST be persisted (orm serializes the entity to its JSON
            data column, so a json:"-" field would never be stored — that silently
            broke login), so it carries a real json tag; the users API redact() strips
            it (and every other secret) from every response. PasswordType and
            PasswordSalt describe the digest scheme so rows hashed under the legacy
            argon2id scheme can still be verified and lazily re-hashed to bcrypt.
          type: string
        passwordSalt:
          type: string
        passwordType:
          type: string
        patreon:
          type: string
        paypal:
          type: string
        permanentAvatar:
          type: string
        permissions:
          items:
            $ref: '#/components/schemas/iam.Permission'
          type: array
        phone:
          type: string
        preHash:
          type: string
        preferredMfaType:
          type: string
        properties:
          additionalProperties:
            type: string
          type: object
        qq:
          type: string
        ranking:
          type: integer
        realName:
          type: string
        recoveryCodes:
          items:
            type: string
          type: array
        region:
          type: string
        registerSource:
          type: string
        registerType:
          type: string
        roles:
          description: |-
            Authorization attachments. Roles and Permissions are computed on read
            from the authz store and carried here for API parity with v1.
          items:
            $ref: '#/components/schemas/iam.Role'
          type: array
        salesforce:
          type: string
        score:
          type: integer
        shopify:
          type: string
        signinWrongTimes:
          type: integer
        signupApplication:
          type: string
        slack:
          type: string
        soundcloud:
          type: string
        spotify:
          type: string
        steam:
          type: string
        strava:
          type: string
        stripe:
          type: string
        tag:
          type: string
        telegram:
          type: string
        tiktok:
          type: string
        title:
          type: string
        totpSecret:
          type: string
        tumblr:
          type: string
        twitch:
          type: string
        twitter:
          type: string
        type:
          type: string
        typetalk:
          type: string
        uber:
          type: string
        updatedAt:
          format: date-time
          type: string
        updatedTime:
          type: string
        verificationCode:
          type: string
        vk:
          type: string
        webauthnCredentials:
          description: |-
            Multi-factor authentication. TotpSecret and RecoveryCodes are secret
            verify-only material — the handler strips them from every response.
            WebauthnCredentials is carried as raw JSON here for lossless migration;
            the typed passkey model is the sibling WebauthnCredential entity.
          items: {}
          type: array
        wechat:
          type: string
        wecom:
          type: string
        weibo:
          type: string
        wepay:
          type: string
        xero:
          type: string
        yahoo:
          type: string
        yammer:
          type: string
        yandex:
          type: string
        zoom:
          type: string
      type: object
    iam.WebauthnCredential:
      properties:
        aaguid:
          contentEncoding: base64
          type: string
        attachment:
          type: string
        attestationFormat:
          description: |-
            AttestationFormat is the statement format the authenticator attested in
            ("packed", "apple", "none", …), which is a DIFFERENT value from the
            attestation type above. The library reads it back when resolving the FIDO
            AppID extension, so a row that dropped it would round-trip a credential the
            verifier no longer recognises as the one it stored.
          type: string
        attestationType:
          type: string
        backupEligible:
          type: boolean
        backupState:
          type: boolean
        cloneWarning:
          type: boolean
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        credentialId:
          contentEncoding: base64
          type: string
        deleted:
          type: boolean
        id:
          type: string
        name:
          type: string
        owner:
          type: string
        publicKey:
          contentEncoding: base64
          type: string
        signCount:
          type: integer
        transport:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        user:
          type: string
        userPresent:
          type: boolean
        userVerified:
          type: boolean
      type: object
    iam.Workspace:
      properties:
        bucket:
          type: string
        createdAt:
          format: date-time
          type: string
        createdTime:
          type: string
        deleted:
          type: boolean
        description:
          type: string
        displayName:
          type: string
        id:
          type: string
        isDefault:
          type: boolean
        metadata:
          type: string
        name:
          type: string
        organization:
          type: string
        owner:
          type: string
        tags:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
      type: object
    iam.accountBody:
      properties:
        avatar:
          type: string
        bio:
          type: string
        displayName:
          type: string
        homepage:
          type: string
      type: object
    iam.assumeBody:
      properties:
        org:
          type: string
      type: object
    iam.auditlogs.Input:
      properties:
        action:
          type: string
        clientIp:
          type: string
        createdTime:
          type: string
        isTriggered:
          type: boolean
        language:
          type: string
        method:
          type: string
        name:
          type: string
        object:
          type: string
        organization:
          type: string
        owner:
          type: string
        requestUri:
          type: string
        response:
          type: string
        statusCode:
          type: integer
        user:
          type: string
      type: object
    iam.bulk:
      properties:
        maxOperations:
          type: integer
        maxPayloadSize:
          type: integer
        supported:
          type: boolean
      type: object
    iam.certs.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.certs.ListOutput:
      properties:
        certs:
          items:
            $ref: '#/components/schemas/iam.Cert'
          type: array
        total:
          type: integer
      type: object
    iam.certs.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.config:
      properties:
        authenticationSchemes:
          items:
            $ref: '#/components/schemas/iam.scheme'
          type: array
        bulk:
          $ref: '#/components/schemas/iam.bulk'
        changePassword:
          $ref: '#/components/schemas/iam.toggle'
        documentationUri:
          type: string
        etag:
          $ref: '#/components/schemas/iam.toggle'
        filter:
          $ref: '#/components/schemas/iam.filter'
        patch:
          $ref: '#/components/schemas/iam.toggle'
        schemas:
          items:
            type: string
          type: array
        sort:
          $ref: '#/components/schemas/iam.toggle'
      type: object
    iam.filter:
      properties:
        maxResults:
          type: integer
        supported:
          type: boolean
      type: object
    iam.invitations.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.invitations.Input:
      properties:
        application:
          type: string
        code:
          type: string
        createdTime:
          type: string
        defaultCode:
          type: string
        displayName:
          type: string
        email:
          type: string
        isRegexp:
          type: boolean
        name:
          type: string
        owner:
          type: string
        phone:
          type: string
        quota:
          type: integer
        signupGroup:
          type: string
        state:
          type: string
        updatedTime:
          type: string
        usedCount:
          type: integer
        username:
          type: string
      type: object
    iam.invitations.ListOutput:
      properties:
        invitations:
          items:
            $ref: '#/components/schemas/iam.Invitation'
          type: array
        total:
          type: integer
      type: object
    iam.invitations.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.keys.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.listProvidersOut:
      properties:
        providers:
          items:
            $ref: '#/components/schemas/iam.Provider'
          type: array
      type: object
    iam.listResponse:
      properties:
        Resources:
          items:
            type: object
          type: array
        itemsPerPage:
          type: integer
        schemas:
          items:
            type: string
          type: array
        startIndex:
          type: integer
        totalResults:
          type: integer
      type: object
    iam.listTokensOut:
      properties:
        tokens:
          items:
            $ref: '#/components/schemas/iam.Token'
          type: array
      type: object
    iam.listWebauthnCredentialsOut:
      properties:
        webauthnCredentials:
          items:
            $ref: '#/components/schemas/iam.WebauthnCredential'
          type: array
      type: object
    iam.mutationResult:
      properties:
        affected:
          type: boolean
        provider:
          $ref: '#/components/schemas/iam.Provider'
      type: object
    iam.passwordBody:
      properties:
        code:
          description: Code is the one-time code delivered to the account's own address.
          type: string
        oldPassword:
          description: |-
            OldPassword is the credential being replaced, the proof a signed-in caller
            gives instead of a code.
          type: string
        organization:
          description: |-
            The account being recovered, for a caller who cannot be signed in. Read on
            the CODE arm only — a signed-in caller is resolved from its own session or
            token, never from these.
          type: string
        password:
          description: |-
            Password is the new credential. It must satisfy the platform floor and the
            organization's own complexity options.
          type: string
        username:
          description: email, username OR phone
          type: string
      type: object
    iam.permission.DeleteResponse:
      properties:
        deleted:
          type: boolean
      type: object
    iam.permission.ListResponse:
      properties:
        permissions:
          items:
            $ref: '#/components/schemas/iam.Permission'
          type: array
      type: object
    iam.permission.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.person:
      properties:
        displayName:
          type: string
        email:
          type: string
        isAdmin:
          type: boolean
        name:
          type: string
        owner:
          type: string
        password:
          type: string
        passwordType:
          type: string
        phone:
          type: string
      type: object
    iam.projects.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.projects.ListOutput:
      properties:
        projects:
          items:
            $ref: '#/components/schemas/iam.Project'
          type: array
        total:
          type: integer
      type: object
    iam.projects.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.providerKey:
      properties:
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      type: object
    iam.providerResult:
      properties:
        provider:
          $ref: '#/components/schemas/iam.Provider'
      type: object
    iam.registration:
      properties:
        cert:
          type: string
        clientId:
          type: string
        clientSecret:
          type: string
        displayName:
          type: string
        enableCodeSignin:
          description: |-
            EnableCodeSignin offers sign-in by an emailed or texted one-time code
            beside the password. A POINTER for the same reason as IsShared: a plain
            bool reads as false on every reconcile that says nothing and would switch
            the method off for every app whose caller never mentioned it.
          type: boolean
        expireInHours:
          description: |-
            ExpireInHours and RefreshExpireInHours are the application's token
            lifetimes. They are the ONLY declarative way to say that a refresh token
            must OUTLIVE its access token: with neither stated, oidc.refreshTTL clamps
            the refresh lifetime to the access lifetime, so the refresh_token grant the
            registration advertises expires at the same instant as the token it was
            meant to renew and can never be exercised. `hanzo-cli` sat in exactly that
            state — a browser re-login every hour, and a live refresh returning 401.

            POINTERS, for the same reason as IsShared: a plain float would read as 0 on
            every reconcile that says nothing and reset a deliberate lifetime back to
            the default. Nil means "not stated, leave it".
          type: number
        grantTypes:
          items:
            type: string
          type: array
        isShared:
          description: |-
            IsShared declares that this application serves EVERY organization, not only
            the one named in Organization. It is the honest description of a brand app —
            hanzo-id, hanzo-chat, a brand console — whose customers each live in their own
            tenant: self-service onboarding moves a founder OUT of the brand org, so
            `user.Owner != app.Organization` is the steady state and the app really does
            serve every org. Application.ServesOrg reads it as one of the three ways to
            say yes.

            A POINTER because omission must PRESERVE. This upsert is the operator's
            steady-state reconcile and most callers say nothing about sharing; a plain
            bool would read as false on every one of them and silently un-share an app —
            the same shape of accident that de-secreted apps through update-application.
            Nil means "not stated, leave it"; only an explicit true or false moves it.
          type: boolean
        name:
          type: string
        organization:
          type: string
        public:
          description: |-
            Public declares a client that CANNOT hold a credential — a browser SPA,
            a CLI, a desktop app. It proves itself with PKCE instead, and the token
            endpoint treats "no stored secret" as exactly that (token.go: a secret is
            verified only when one is stored). Without this flag every upsert minted
            a secret, so a public client could never be registered at all and its
            browser code->token exchange 401'd `invalid_client` forever.
          type: boolean
        redirectUris:
          items:
            type: string
          type: array
        refreshExpireInHours:
          type: number
      type: object
    iam.reply:
      properties:
        action:
          type: string
        data:
          type: object
        msg:
          type: string
        status:
          type: string
      type: object
    iam.roles.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.roles.Input:
      properties:
        createdTime:
          type: string
        description:
          type: string
        displayName:
          type: string
        domains:
          items:
            type: string
          type: array
        groups:
          items:
            type: string
          type: array
        isEnabled:
          type: boolean
        name:
          type: string
        owner:
          type: string
        roles:
          items:
            type: string
          type: array
        users:
          items:
            type: string
          type: array
      type: object
    iam.roles.ListOutput:
      properties:
        roles:
          items:
            $ref: '#/components/schemas/iam.Role'
          type: array
        total:
          type: integer
      type: object
    iam.roles.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    iam.scheme:
      properties:
        description:
          type: string
        name:
          type: string
        primary:
          type: boolean
        type:
          type: string
      type: object
    iam.toggle:
      properties:
        supported:
          type: boolean
      type: object
    iam.tokenKey:
      properties:
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      type: object
    iam.tokenMutation:
      properties:
        affected:
          type: boolean
        token:
          $ref: '#/components/schemas/iam.Token'
      type: object
    iam.tokenResult:
      properties:
        token:
          $ref: '#/components/schemas/iam.Token'
      type: object
    iam.users.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.users.ListOutput:
      properties:
        total:
          type: integer
        users:
          items:
            $ref: '#/components/schemas/iam.User'
          type: array
      type: object
    iam.users.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      type: object
    iam.webauthnCredentialKey:
      properties:
        name:
          type: string
        owner:
          type: string
      required:
      - owner
      - name
      type: object
    iam.webauthnCredentialMutationResult:
      properties:
        affected:
          type: boolean
        webauthnCredential:
          $ref: '#/components/schemas/iam.WebauthnCredential'
      type: object
    iam.webauthnCredentialResult:
      properties:
        webauthnCredential:
          $ref: '#/components/schemas/iam.WebauthnCredential'
      type: object
    iam.workspaces.DeleteOutput:
      properties:
        deleted:
          type: boolean
      type: object
    iam.workspaces.Input:
      properties:
        bucket:
          type: string
        createdTime:
          type: string
        description:
          type: string
        displayName:
          type: string
        isDefault:
          type: boolean
        metadata:
          type: string
        name:
          type: string
        organization:
          type: string
        owner:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    iam.workspaces.ListOutput:
      properties:
        total:
          type: integer
        workspaces:
          items:
            $ref: '#/components/schemas/iam.Workspace'
          type: array
      type: object
    iam.workspaces.Ref:
      properties:
        name:
          type: string
        owner:
          type: string
      type: object
    imageOrigin:
      properties:
        repository:
          description: Repository is the image repository. Required for source `image`.
          type: string
        tag:
          description: Tag is the image tag to deploy; `latest` when omitted.
          type: string
      type: object
    imageView:
      properties:
        repository:
          type: string
        tag:
          type: string
      type: object
    inboxPage:
      properties:
        cursor:
          description: |-
            Cursor is the row id to pass back as `since` for the next page. It is the
            last message's id, or the requested cursor when the page is empty.
          type: integer
        messages:
          description: Messages are the inbound messages, oldest first.
          items:
            $ref: '#/components/schemas/inboxView'
          type: array
      type: object
    inboxView:
      properties:
        account:
          description: |-
            Account is the lowercased external id of the org's connected account on that
            transport: the Discord guild id, the Slack team id, the Teams AAD tenant id,
            or the bound Telegram chat id. Informational only — the gate keys on
            (org, channel), never on the account.
          type: string
        channel:
          description: |-
            Channel is the transport this message arrived on — discord, slack, teams or
            telegram — and the `:channel` segment to reply through.
          type: string
        createdAt:
          description: |-
            CreatedAt is Unix SECONDS, stamped by the ingest goroutine when the message
            was accepted — not the transport's own send time. Rows are dropped 30 days
            after it.
          type: integer
        id:
          description: |-
            ID is the store's row id, assigned on insert — SERVER-SET, and the cursor:
            pass a page's last id back as `since`. It rises with arrival order but is
            not contiguous, because one sequence is shared by every org in the store and
            a caller reads only its own rows.
          type: integer
        replyTo:
          description: |-
            ReplyTo is the transport's reply target for this message: Slack's thread_ts,
            or the Telegram message id it arrived as. Send it back as the body's
            `replyTo` to answer in the SAME thread. Empty means the transport reported
            none — a top-level Slack message, and every Discord and Teams message, since
            neither carries one — and a reply then lands at the top level of the room.
          type: string
        roomId:
          description: |-
            RoomID is the conversation on the ORIGINATING transport, and the value to
            send back as `room.id`: a Discord channel snowflake, a Slack conversation id
            (D… IM, C… public channel, G… private or mpim), a Teams conversation id
            (19:…@thread.… for a channel or group chat, a:… for a personal chat), or a
            Telegram chat id in decimal (negative for a group, positive for a DM). It is
            stable for the life of the room, so every message from one conversation
            carries the same value.
          type: string
        roomKind:
          description: |-
            RoomKind is how ingest classified the room: "dm", "group" or "thread". It
            decides which policy gated the message — dmPolicy for "dm", groupPolicy for
            BOTH "group" and "thread". Only Slack ever reports "thread"; Telegram's
            reply-to id becomes ReplyTo instead, and Discord's ingress is guild-scoped
            so its rooms are always "group".
          type: string
        sender:
          description: |-
            Sender is the TRANSPORT-NATIVE user id of whoever wrote the message — a
            Discord member.user.id, a Slack U… user id, a Teams aadObjectId (falling
            back to from.id), a Telegram from.id in decimal. Stable per person per
            transport, and the identity the gate keys on: an allow entry, an access-group
            member and a pairing approval all name exactly this value.
          type: string
        senderUser:
          description: |-
            SenderUser is the HANZO account subject that chat identity is linked to,
            resolved at ingest through the org's user link. Best-effort and omitted when
            absent: a person who never linked their chat account — or a link store that
            could not be read — leaves it empty and is never blocked for it.
          type: string
        text:
          description: |-
            Text is the body as the transport delivered it, with the bot mention already
            stripped by the ingress adapter (on Discord it is the /hanzo prompt argument,
            since that ingress is slash commands only), truncated to 8 KiB on store.
            Inbound attachments are not stored — this is the whole of what was said.
          type: string
      type: object
    indexIn:
      properties:
        files:
          description: |-
            Files is the full set of files to index. Required and non-empty; max 20000
            files, 1 MiB per file and 1 GiB in total. Unchanged files are skipped by
            content hash, so re-sending the whole tree is cheap.
          items:
            $ref: '#/components/schemas/fileInput'
          type: array
        prune:
          description: |-
            Prune deletes indexed files that are NOT in this request — which makes the
            call a full sync of the repo rather than an upsert. Only pass it when Files
            is the complete tree.
          type: boolean
        repo:
          description: |-
            Repo is the repository label to index under. Required, max 200 bytes. It is
            a stored column value, not a filesystem path.
          type: string
      type: object
    indexResult:
      properties:
        chunks:
          description: Chunks is how many AST-boundary chunks the repo holds after
            this pass.
          type: integer
        files:
          description: Files is how many files the repo holds after this pass.
          type: integer
        indexed:
          description: Indexed is how many files were parsed and written on this pass.
          type: integer
        pruned:
          description: |-
            Pruned is how many stored files were deleted because prune was set and they
            were absent from the request.
          type: integer
        repo:
          description: Repo is the repository that was indexed.
          type: string
        semantic:
          description: |-
            Semantic reports whether the semantic tier was available for this pass. When
            false the index is lexical + symbolic only and hybrid search still works.
          type: boolean
        skipped:
          description: Skipped is how many files were unchanged by content hash and
            left alone.
          type: integer
        symbols:
          description: Symbols is how many symbol definitions the repo holds after
            this pass.
          type: integer
        vectors:
          description: Vectors is how many of those chunks carry an embedding.
          type: integer
      type: object
    infoOut:
      properties:
        jetstream:
          description: JetStream is true when durable streams are enabled.
          type: boolean
        max_payload:
          description: MaxPayload is the broker's message-size ceiling in bytes.
          type: integer
        server_id:
          description: Server is the broker's server id.
          type: string
        server_name:
          description: Name is the broker's server name.
          type: string
        streams:
          description: Streams is the org's stream count.
          type: integer
        version:
          description: Version is the broker's server version.
          type: string
      type: object
    ingressMiddlewares:
      properties:
        middlewares:
          description: Middlewares is the org's middlewares, ordered by id.
          items:
            $ref: '#/components/schemas/Middleware'
          type: array
      type: object
    ingressRoutes:
      properties:
        routes:
          description: Routes is the org's routes, ordered by id.
          items:
            $ref: '#/components/schemas/Route'
          type: array
      type: object
    ingressServices:
      properties:
        services:
          description: Services is the org's services, ordered by id.
          items:
            $ref: '#/components/schemas/Upstream'
          type: array
      type: object
    ingressStatus:
      properties:
        acmeCacheDir:
          description: ACMECacheDir is where autocert persists accounts and certificates.
          type: string
        acmeStaging:
          description: ACMEStaging is true when certificates are issued from Let's
            Encrypt staging.
          type: boolean
        edgeEnabled:
          description: EdgeEnabled is true when the edge listeners are actually bound.
          type: boolean
        httpAddr:
          description: HTTPAddr is the address the ACME HTTP-01 + HTTP router listens
            on.
          type: string
        httpsAddr:
          description: HTTPSAddr is the address the SNI TLS terminator listens on.
          type: string
        liveHosts:
          description: LiveHosts is how many hosts the compiled table routes.
          type: integer
        proxy:
          description: Proxy names the reverse-proxy implementation behind every route.
          type: string
        role:
          description: Role is "edge" when CLOUD_INGRESS_EDGE_ENABLED is set, else
            "app".
          type: string
        tlsHosts:
          description: |-
            TLSHosts is how many hosts the ACME HostPolicy will issue a certificate
            for. NOT a subset of LiveHosts: an extraHost owns no route, and a TLS route
            naming a missing service is skipped while its host still wants a cert.
          type: integer
      type: object
    ingressTLS:
      properties:
        acmeDirectory:
          description: |-
            ACMEDirectory is the ACME endpoint in use: the staging URL, or
            "letsencrypt-production".
          type: string
        acmeEmail:
          description: |-
            ACMEEmail is the account email the PROCESS was started with
            (CLOUD_INGRESS_ACME_EMAIL), not the stored config's.
          type: string
        config:
          $ref: '#/components/schemas/TLSConfig'
          description: Config is the caller org's stored ACME intent.
        edgeEnabled:
          description: EdgeEnabled is true when the edge listeners are actually bound.
          type: boolean
        managedHosts:
          description: |-
            ManagedHosts is every host the ACME HostPolicy will issue a certificate for
            — the union across ALL orgs of TLS-marked routes and configured extraHosts,
            because one process holds one certificate cache.
          items:
            type: string
          type: array
        note:
          description: Note states which fields hot-apply and which need an edge restart.
          type: string
        role:
          description: Role is "edge" when this instance binds the listeners, else
            "app".
          type: string
      type: object
    insightsBody:
      properties:
        batch:
          items:
            $ref: '#/components/schemas/insightsEvent'
          type: array
        distinct_id:
          type: string
        event:
          type: string
        properties:
          additionalProperties: {}
          type: object
        timestamp:
          type: string
        uuid:
          type: string
      type: object
    insightsEvent:
      properties:
        distinct_id:
          type: string
        event:
          type: string
        properties:
          additionalProperties: {}
          type: object
        timestamp:
          type: string
        uuid:
          type: string
      type: object
    insightsStatus:
      properties:
        engine:
          description: 'Engine names the engine serving the surface: hanzo-analytics.'
          type: string
        ok:
          description: OK is always true — reaching this route is the liveness fact
            it reports.
          type: boolean
        surface:
          description: 'Surface is the path prefix this status covers: /v1/insights.'
          type: string
      type: object
    invocationList:
      properties:
        invocations:
          description: Invocations is one row per past run, newest first.
          items:
            $ref: '#/components/schemas/invocationView'
          type: array
      type: object
    invocationView:
      properties:
        durationMs:
          description: how long it took
          type: integer
        id:
          description: the invocation's handle
          type: string
        method:
          description: the HTTP method that triggered it
          type: string
        status:
          description: 'how the run ended: ok, error or timeout'
          type: string
        statusCode:
          description: |-
            Code is the status the function's OWN code answered with, which is not the
            status of the reply — a program can answer 500 through a healthy sandbox.
          type: integer
        time:
          description: when it ran, RFC3339
          type: string
      type: object
    invokeReq:
      properties:
        input:
          description: Input is what the function is given on stdin. It is opaque
            to this surface.
          type: string
      type: object
    issueEdit:
      properties:
        description:
          description: Description rewrites the body.
          type: string
        key:
          description: Key is the board — the repository name, from the path.
          type: string
        num:
          description: Num is the issue number on that repository, from the path.
          type: integer
        priority:
          description: Priority re-prioritises it.
          type: string
        status:
          description: Status moves the card to another column.
          type: string
        title:
          description: Title renames the work item.
          type: string
      type: object
    issueHit:
      properties:
        assignee:
          type: string
        kind:
          type: string
        number:
          type: integer
        priority:
          type: string
        project:
          type: string
        repo:
          type: string
        source:
          type: string
        status:
          type: string
        title:
          type: string
        url:
          type: string
      type: object
    issueHits:
      properties:
        count:
          type: integer
        issues:
          items:
            $ref: '#/components/schemas/issueHit'
          type: array
      type: object
    issueView:
      properties:
        assignee:
          type: string
        createdAt:
          type: integer
        description:
          type: string
        dueAt:
          description: unix seconds; absent = no due date
          type: integer
        extRef:
          description: external anchor
          type: string
        id:
          type: string
        identifier:
          description: KEY-<number>, the human handle
          type: string
        kind:
          description: issue | pr | epic
          type: string
        labels:
          items:
            type: string
          type: array
        number:
          type: integer
        priority:
          type: string
        projectKey:
          type: string
        repo:
          description: git repo binding
          type: string
        source:
          description: team | git | crm | helpdesk | cms | agent
          type: string
        startAt:
          description: unix seconds; absent = unscheduled
          type: integer
        status:
          type: string
        title:
          type: string
        updatedAt:
          type: integer
      type: object
    itemList:
      properties:
        data:
          description: |-
            Data is the examples of the one dataset named in the path, archived ones
            included, so the caller sees the whole set rather than what a run would use.
          items:
            $ref: '#/components/schemas/itemView'
          type: array
      type: object
    itemReq:
      properties:
        expectedOutput:
          description: |-
            Expected is the answer a correct model produces, which the judge grades
            against, stored as raw JSON exactly as sent, up to 64 KiB.
        id:
          description: |-
            ID makes the write idempotent — re-posting the same id replaces that example
            in place. Omit it and one is generated. An id that already exists in a
            DIFFERENT dataset is 409 rather than a move.
          type: string
        input:
          description: |-
            Input is what the model under test will be given, stored as raw JSON exactly
            as sent, up to 64 KiB.
        metadata:
          additionalProperties:
            type: object
          description: Metadata is a free-form object stored with the example.
          type: object
        status:
          description: |-
            Status is ACTIVE (the default) or ARCHIVED. Only ACTIVE examples are fed to
            a run, which is how an example is retired without being deleted.
          type: string
      type: object
    itemResult:
      properties:
        error:
          description: |-
            Error is why this example produced no score — the model, the judge, or the
            run's deadline. A result carrying one is not counted in Scored.
          type: string
        itemId:
          description: ItemID is the example that was scored.
          type: string
        output:
          description: Output is what the model under test answered, truncated at
            2000 characters.
          type: string
        score:
          description: Score is the judge's grade.
          type: number
        traceId:
          description: TraceID is the model call this result came from.
          type: string
      type: object
    itemView:
      properties:
        createdAt:
          description: CreatedAt is when the example was first written.
          type: string
        datasetName:
          description: Dataset is the set this example belongs to.
          type: string
        expectedOutput:
          description: Expected is the answer a correct model produces, which the
            judge grades against.
          type: object
        id:
          description: ID is the example's handle, unique within the caller's org.
          type: string
        input:
          description: Input is what the model under test is given, as it was written.
          type: object
        metadata:
          additionalProperties:
            type: object
          description: Metadata is the free-form object stored with the example.
          type: object
        status:
          description: |-
            Status is ACTIVE or ARCHIVED. Only ACTIVE examples are fed to a run, which
            is how one is retired without being deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          type: string
      type: object
    jobCancel:
      properties:
        id:
          description: ID is the job (activity) id, from the URL path.
          type: string
        reason:
          description: Reason is recorded on the cancellation; empty records "canceled
            from console".
          type: string
        run:
          description: |-
            Run is the run id; empty defaults to the job id, which is what the dispatcher
            sets (runId == activityId == prompt_id), so the common case sends no body.
          type: string
      type: object
    jobCanceled:
      properties:
        canceled:
          description: Canceled is the job id that was canceled.
          type: string
        run:
          description: Run is the run id the cancel was applied to.
          type: string
      type: object
    jobList:
      properties:
        jobs:
          description: |-
            Jobs is the queue, most-recent-first. Every LIVE job is present; terminal
            history is capped, so a busy org's running work is never crowded out.
          items:
            $ref: '#/components/schemas/gpuJob'
          type: array
      type: object
    judgeSpec:
      properties:
        criteria:
          description: |-
            Criteria is the standard the judge applies, defaulting to a correctness
            criterion.
          type: string
        model:
          description: |-
            Model is the model that grades. It defaults to the model under test, so a
            run with no judge named has the model grade itself.
          type: string
        name:
          description: |-
            Name is the score name the judge's grades are filed under, "llm-judge" by
            default. A name that is not a legal handle falls back to that default
            rather than being stored as sent.
          type: string
      type: object
    kbAuthorizeOut:
      properties:
        authorizeUrl:
          description: AuthorizeURL is the provider's authorize endpoint with an org-bound
            signed state.
          type: string
      type: object
    kbConnectorsOut:
      properties:
        connectors:
          description: Connectors is every supported provider with this org's connection
            state.
          items:
            $ref: '#/components/schemas/connectorView'
          type: array
      type: object
    kbSyncOut:
      properties:
        ingested:
          description: Ingested is how many documents landed in the org's knowledge
            store.
          type: integer
        provider:
          description: Provider is the connector that was pulled.
          type: string
      type: object
    keyList:
      properties:
        data:
          description: Data holds the org's keys.
          items:
            $ref: '#/components/schemas/keyView'
          type: array
      type: object
    keyTypeIn:
      properties:
        limit:
          description: |-
            Limit narrows what the minted key may reach, as `kind:name` entries:
            `model:zen5`, `project:acme`, `product:commerce`, or `model:*` for a whole
            kind. It only ever NARROWS — a key can never reach further than the person
            who minted it — so an unrecognised kind costs availability, never privilege.

            Omitted mints an unrestricted key, because that is what every key in the
            estate is today and a default that restricted would revoke all of them.

            Example: {"type": "secret", "limit": ["model:zen5", "project:acme"]}
          items:
            type: string
          type: array
        type:
          description: |-
            Type is the key class to act on: "secret" (sk-, session-equivalent, belongs
            on a server) or "publishable" (pk-, org-identifying, safe in a browser
            bundle). Omitted means secret, which is what every existing caller means.
          type: string
      type: object
    keyView:
      properties:
        createdAt:
          description: CreatedAt is RFC 3339 UTC.
          type: string
        fingerprint:
          description: |-
            Fingerprint is the key's SHA256 fingerprint ("SHA256:…"), globally unique
            and the handle SSH auth resolves a presented key by.
          type: string
        id:
          description: ID is the key's identifier ("gitkey_…"), the handle to delete
            it by.
          type: string
        publicKey:
          description: PublicKey is the canonical OpenSSH authorized-key line as stored.
          type: string
        title:
          description: Title is the key's label — the caller's, or the comment on
            the key line.
          type: string
      type: object
    kitList:
      properties:
        data:
          description: Data is the public catalog followed by the caller org's own
            kits.
          items:
            $ref: '#/components/schemas/StarterKit'
          type: array
      type: object
    kvAck:
      properties:
        revision:
          description: Revision is the revision the write created.
          type: integer
      type: object
    kvEntry:
      properties:
        created:
          description: Created is when this revision was written, RFC3339.
          type: string
        key:
          description: Key is the entry's key.
          type: string
        operation:
          description: 'Operation is what wrote the revision: put, del or purge.'
          type: string
        revision:
          description: Revision is the entry's revision in the bucket.
          type: integer
        value:
          description: Value is the value as UTF-8 text; empty for delete and purge
            markers.
          type: string
      type: object
    kvPage:
      properties:
        data:
          description: Data are the key's retained revisions.
          items:
            $ref: '#/components/schemas/kvEntry'
          type: array
      type: object
    kvWrite:
      properties:
        bucket:
          description: Bucket is the bucket, from the path.
          type: string
        key:
          description: Key is the key, from the path.
          type: string
        value:
          description: Value is the value, carried verbatim as UTF-8 text (typically
            JSON).
          type: string
      type: object
    lastEventView:
      properties:
        actor:
          type: string
        at:
          type: string
        kind:
          type: string
        preview:
          type: string
        seq:
          type: integer
      type: object
    listOut:
      properties:
        providers:
          description: Providers is the whole catalog. Never null; [] when nothing
            is registered.
          items:
            $ref: '#/components/schemas/providerView'
          type: array
      type: object
    liveness:
      properties:
        service:
          description: Service names the answering subsystem; it is always commerce.
          type: string
        status:
          description: 'Status is always ok: mounted is the only state that can answer.'
          type: string
      type: object
    logLines:
      properties:
        logs:
          description: |-
            Logs is that run's error text when it failed, else its output. It is empty
            when the function has never run.
          type: string
      type: object
    loss:
      properties:
        exhausted:
          description: Exhausted counts facts the bus abandoned after maxDeliver failed
            inserts.
          type: integer
        undecodable:
          description: Undecodable counts messages acked without landing because they
            did not parse.
          type: integer
      type: object
    machineList:
      properties:
        machines:
          description: 'Machines is every machine the org has: Visor-provisioned and
            BYO together.'
          items:
            $ref: '#/components/schemas/machineView'
          type: array
      type: object
    machineView:
      properties:
        createdTime:
          type: string
        gpu:
          type: string
        id:
          type: string
        image:
          type: string
        mem:
          type: string
        name:
          type: string
        os:
          type: string
        privateIp:
          type: string
        provider:
          type: string
        publicIp:
          type: string
        region:
          type: string
        status:
          type: string
        type:
          type: string
        vcpu:
          type: integer
      type: object
    makeIn:
      properties:
        ack_policy:
          type: string
        ack_wait:
          type: string
        deliver_policy:
          type: string
        description:
          type: string
        durable_name:
          type: string
        filter_subject:
          type: string
        max_ack_pending:
          type: integer
        max_deliver:
          type: integer
        opt_start_seq:
          type: integer
        opt_start_time:
          format: date-time
          type: string
        replay_policy:
          type: string
        stream:
          description: Stream is the stream name, from the path.
          type: string
      type: object
    mcpCatalog:
      properties:
        catalog:
          description: Catalog is this page of listings, featured first, then by name.
          items:
            $ref: '#/components/schemas/MCPListing'
          type: array
        limit:
          description: |-
            Limit is the page size that was actually applied — the default or the clamp,
            when the request asked for neither or for too much.
          type: integer
        offset:
          description: |-
            Offset is where this page started, so a caller pages from what the server
            did rather than from what it asked for.
          type: integer
        total:
          description: |-
            Total is how many listings the filter matched, which is more than this page
            holds whenever there is a next one.
          type: integer
      type: object
    mcpCatalogSync:
      properties:
        added:
          description: Added is how many listings the catalog did not have before.
          type: integer
        registry:
          description: Registry is the upstream this pass read.
          type: string
        total:
          description: Total is how many listings the catalog holds now.
          type: integer
        updated:
          description: Updated is how many the publisher has changed since we last
            looked.
          type: integer
      type: object
    mcpServerList:
      properties:
        servers:
          description: |-
            Servers is every external MCP server this org has registered. No secret
            VALUE is ever included — only whether one is set.
          items:
            $ref: '#/components/schemas/MCPServer'
          type: array
      type: object
    meshServiceList:
      properties:
        services:
          description: Services is one row per ZT edge service tagged with the caller's
            org role.
          items:
            $ref: '#/components/schemas/meshView'
          type: array
      type: object
    meshView:
      properties:
        id:
          description: ID is the ZT edge service's id.
          type: string
        mtls:
          description: |-
            Mtls is "required" when the service mandates end-to-end encryption, else
            "enabled" — the fabric mutually authenticates every link, so it is never
            truly off.
          type: string
        service:
          description: Service is the edge service's name.
          type: string
        status:
          description: 'Status is "active": a listed service is a configured, dialable
            mesh entry.'
          type: string
      type: object
    messageInput:
      properties:
        from:
          type: string
        media:
          items:
            type: string
          type: array
        text:
          type: string
        to:
          type: string
      type: object
    messageList:
      properties:
        data:
          items:
            $ref: '#/components/schemas/SMS'
          type: array
      type: object
    metricList:
      properties:
        data:
          description: Data is one row per prompt the org owns.
          items:
            $ref: '#/components/schemas/metricRow'
          type: array
      type: object
    metricRow:
      properties:
        createdAt:
          description: CreatedAt is when version 1 was written, RFC 3339 UTC.
          type: string
        currentVersion:
          description: |-
            CurrentVer is the version number served as current. It always equals
            `versions`: numbering is dense from 1, and deleting a prompt takes its whole
            history with it rather than leaving a gap.
          type: integer
        lastUpdatedAt:
          description: |-
            LastUpdatedAt is when the newest version was appended, RFC 3339 UTC — the age
            of the template you would get today.
          type: string
        name:
          description: Name is the prompt this row is about — its org-unique handle.
          type: string
        type:
          description: Type is the current version's kind.
          type: string
        versions:
          description: |-
            Versions is how many revisions the prompt has, COUNTED in the store and
            uncapped — so it can exceed the 100 entries a list row or a detail response
            carries. Note the type: here `versions` is a number, while on a list row it is
            the list of version numbers.
          type: integer
      type: object
    metricsView:
      properties:
        range:
          description: echoes the requested window (24H|7D|30D)
          type: string
        resource:
          $ref: '#/components/schemas/resourceUsage'
        series:
          description: per-agent invocation histogram (real)
          items:
            $ref: '#/components/schemas/seriesLine'
          type: array
      type: object
    mintedKey:
      properties:
        accessKey:
          description: |-
            AccessKey is the same value under its predecessor name, carried so callers
            written against the older field keep working. One value, two names.
          type: string
        key:
          description: Key is the credential, returned ONCE — a secret key is unreadable
            afterwards.
          type: string
        limit:
          description: |-
            Limit is what the minted key may reach, echoed back so the caller can see
            the narrowing took. Absent means unrestricted.
          items:
            type: string
          type: array
        type:
          description: Type is the class of key that was minted.
          type: string
      type: object
    mirrorList:
      properties:
        data:
          description: Data holds the repo's outbound mirror targets.
          items:
            $ref: '#/components/schemas/mirrorTargetView'
          type: array
      type: object
    mirrorReq:
      properties:
        name:
          description: |-
            Name is the local repo to mirror into, from the :name path segment. It is
            CREATED on first use.
          type: string
        project:
          description: |-
            Project is the sub-scope to land the repo in; empty uses the caller's own,
            exactly as a create would.
          type: string
        source:
          description: |-
            Source is the http(s) git URL to fetch from. The host is SSRF-guarded, and a
            credential is sent only if we hold one NAMED FOR that host — so a
            tenant-supplied URL to anywhere else fetches anonymously.
          type: string
      type: object
    mirrorTargetReq:
      properties:
        host:
          description: |-
            Host is an optional assertion of the target's hostname. The authoritative
            host is the one in URL; a value that disagrees with it is refused.
          type: string
        name:
          description: |-
            Name is the repo whose advanced refs are pushed downstream, from the :name
            path segment.
          type: string
        url:
          description: |-
            URL is the downstream https git remote. Must be https to an allowlisted
            host (github.com / gitlab.com); any embedded credentials are stripped.
            Required.
          type: string
      type: object
    mirrorTargetView:
      properties:
        createdAt:
          description: CreatedAt is RFC 3339 UTC.
          type: string
        host:
          description: Host is the target's lowercased hostname, taken from URL and
            never the body.
          type: string
        id:
          description: ID is the target's identifier ("mir_…"), the handle to remove
            it by.
          type: string
        repo:
          description: Repo is the repo whose advanced refs are pushed downstream.
          type: string
        url:
          description: URL is the canonical https remote, with any embedded credentials
            stripped.
          type: string
      type: object
    mutateReq:
      properties:
        add:
          description: |-
            Add is the product ids to turn ON. Each must already be an ACTIVE entitlement
            of the org's plan, unless the caller is a platform super admin.
          items:
            type: string
          type: array
        remove:
          description: Remove is the product ids to turn OFF. Disabling is never gated.
          items:
            type: string
          type: array
      type: object
    namespaceCreateIn:
      properties:
        title:
          description: Title is the namespace's display title. Cloudflare mints the
            id.
          type: string
      type: object
    networkList:
      properties:
        networks:
          description: |-
            Networks holds the org's overlay network, or is empty when the org has no
            edge-routers on the fabric (no nodes → no network, never a fabricated one).
          items:
            $ref: '#/components/schemas/networkView'
          type: array
      type: object
    networkView:
      properties:
        id:
          description: |-
            ID is the org-derived id of the overlay network — the key
            GET /v1/networks/{id} addresses.
          type: string
        name:
          description: Name is the org the overlay belongs to.
          type: string
        nodes:
          description: Nodes is how many edge-routers the org has on the fabric.
          type: integer
        status:
          description: |-
            Status is "connected" once at least one of the org's edge-routers is
            online, else "provisioning" (routers exist but none has dialed home).
          type: string
      type: object
    newIssue:
      properties:
        description:
          description: Description becomes the issue body.
          type: string
        key:
          description: Key is the board — the repository name, from the path.
          type: string
        priority:
          description: Priority is one of none, urgent, high, medium or low.
          type: string
        status:
          description: |-
            Status is the board column to open into: backlog, todo, in_progress, done
            or canceled. Empty opens into backlog.
          type: string
        title:
          description: Title is required.
          type: string
      type: object
    nextIn:
      properties:
        batch:
          description: Batch is how many messages to pull (1–1000, default 1).
          type: integer
        expires:
          description: Expires is how long to wait for messages, e.g. "5s" (default
            "30s", max "60s").
          type: string
        name:
          description: Name is the consumer name, from the path.
          type: string
        no_wait:
          description: NoWait answers immediately with whatever is available instead
            of waiting.
          type: boolean
        stream:
          description: Stream is the stream name, from the path.
          type: string
      type: object
    noInput:
      properties: {}
      type: object
    nodeList:
      properties:
        nodes:
          description: |-
            Nodes is one row per worker node, in the SAME machineView shape the machines
            surface emits — a node IS a machine.
          items:
            $ref: '#/components/schemas/machineView'
          type: array
      type: object
    nodePoolView:
      properties:
        autoScale:
          type: boolean
        count:
          type: integer
        maxNodes:
          type: integer
        minNodes:
          type: integer
        name:
          type: string
        poolId:
          type: string
        size:
          type: string
      type: object
    none:
      properties: {}
      type: object
    notifyHealth:
      properties:
        service:
          description: Service names the subsystem answering — always "notify".
          type: string
        status:
          description: Status is "ok"; the route answers 200 whenever the subsystem
            is mounted.
          type: string
      type: object
    notifySend:
      properties:
        body:
          description: Body is the message text, sent verbatim when present — the
            no-template path.
          type: string
        channel:
          description: |-
            Channel selects the delivery channel, sms or email. The per-channel routes
            (/send/sms, /send/email) pin it, overriding whatever the body names; on the
            generic route it is required.
          type: string
        event:
          description: |-
            Event is the event name, which doubles as the template id when TemplateID is
            empty — the IAM OTP path sends event=iam.otp_sent and nothing else.
          type: string
        provider:
          description: |-
            Provider pins a provider service name (twilio, plivo, twilio_email, mail).
            Empty picks the one whose org credentials are actually configured in KMS.
          type: string
        subject:
          description: Subject is the message subject, carried on the email channel
            only.
          type: string
        sync:
          description: |-
            Sync must be exactly "true": delivery here is synchronous, and anything else
            answers 503 because the queue plane that would run an async dispatch is owned
            elsewhere. Over REST it rides as ?sync=true (the URL binds over the body); a
            by-name call states it in its arguments.
          type: string
        template_id:
          description: TemplateID selects a built-in template when Body is empty.
          type: string
        template_vars:
          description: |-
            TemplateVars carries the values the selected template renders against, as
            a raw JSON object. Raw on purpose: the by-name call plane computes an
            input's layout before reading any payload and refuses a map field
            outright, which would make every typed call to these ops fail — and that
            call is the reason they are typed at all (IAM's OTP sender). deliver
            decodes it right before the template renders, so REST bodies decode
            byte-identically to the map this replaced.
        to:
          description: |-
            To is the destination address per recipient — a phone number for sms, an
            email address for email. Several recipients fan out into one provider call
            each, and the response shape follows the count (see the items field).
          items:
            type: string
          type: array
      type: object
    numberList:
      properties:
        data:
          items:
            $ref: '#/components/schemas/Number'
          type: array
      type: object
    o11y.AWSAccountConfig:
      properties:
        regions:
          items:
            type: string
          type: array
      type: object
    o11y.AWSCloudWatchLogsSubscription:
      properties:
        filterPattern:
          description: |-
            https://docs.aws.amazon.com/AmazonCloudWatch/latest/logs/FilterAndPatternSyntax.html
            "" implies no filtering is required
          type: string
        logGroupNamePrefix:
          description: |-
            subscribe to all logs groups with specified prefix.
            eg: `/aws/rds/`
          type: string
      type: object
    o11y.AWSCloudWatchMetricStreamFilter:
      properties:
        metricNames:
          items:
            type: string
          type: array
        namespace:
          description: https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-properties-cloudwatch-metricstream-metricstreamfilter.html
          type: string
      type: object
    o11y.AWSConnectionArtifact:
      properties:
        connectionUrl:
          type: string
      type: object
    o11y.AWSIntegrationConfig:
      properties:
        enabledRegions:
          items:
            type: string
          type: array
        telemetryCollectionStrategy:
          $ref: '#/components/schemas/o11y.AWSTelemetryCollectionStrategy'
      type: object
    o11y.AWSLogsCollectionStrategy:
      properties:
        subscriptions:
          items:
            $ref: '#/components/schemas/o11y.AWSCloudWatchLogsSubscription'
          type: array
      type: object
    o11y.AWSMetricsCollectionStrategy:
      properties:
        streamFilters:
          description: to be used as https://docs.aws.amazon.com/AWSCloudFormation/latest/UserGuide/aws-resource-cloudwatch-metricstream.html#cfn-cloudwatch-metricstream-includefilters
          items:
            $ref: '#/components/schemas/o11y.AWSCloudWatchMetricStreamFilter'
          type: array
      type: object
    o11y.AWSPostableAccountConfig:
      properties:
        deploymentRegion:
          type: string
        regions:
          items:
            type: string
          type: array
      type: object
    o11y.AWSServiceConfig:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.AWSServiceLogsConfig'
        metrics:
          $ref: '#/components/schemas/o11y.AWSServiceMetricsConfig'
      type: object
    o11y.AWSServiceLogsConfig:
      properties:
        enabled:
          type: boolean
        s3Buckets:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
      type: object
    o11y.AWSServiceMetricsConfig:
      properties:
        enabled:
          type: boolean
      type: object
    o11y.AWSTelemetryCollectionStrategy:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.AWSLogsCollectionStrategy'
        metrics:
          $ref: '#/components/schemas/o11y.AWSMetricsCollectionStrategy'
        s3Buckets:
          additionalProperties:
            items:
              type: string
            type: array
          description: Only available in S3 Sync Service Type in AWS
          type: object
      type: object
    o11y.Account:
      properties:
        agentReport:
          $ref: '#/components/schemas/o11y.AgentReport'
        config:
          $ref: '#/components/schemas/o11y.AccountConfig'
        createdAt:
          format: date-time
          type: string
        id: {}
        orgId: {}
        provider: {}
        providerAccountId:
          type: string
        removedAt:
          format: date-time
          type: string
        updatedAt:
          format: date-time
          type: string
      type: object
    o11y.AccountConfig:
      properties:
        aws:
          $ref: '#/components/schemas/o11y.AWSAccountConfig'
        azure:
          $ref: '#/components/schemas/o11y.AzureAccountConfig'
        gcp:
          $ref: '#/components/schemas/o11y.GCPAccountConfig'
      type: object
    o11y.AgentReport:
      properties:
        data:
          additionalProperties:
            type: object
          type: object
        timestampMillis:
          type: integer
      type: object
    o11y.AggregateAttributeResponse:
      properties:
        attributeKeys:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
      type: object
    o11y.Alert:
      properties:
        annotations:
          additionalProperties:
            type: string
          type: object
        endsAt:
          format: date-time
          type: string
        generatorURL:
          type: string
        labels:
          additionalProperties:
            type: string
          type: object
        startsAt:
          format: date-time
          type: string
      type: object
    o11y.AlertStatus:
      properties:
        inhibitedBy:
          items:
            type: string
          type: array
        silencedBy:
          items:
            type: string
          type: array
        state:
          type: string
      type: object
    o11y.AssociatedComponent:
      properties:
        name:
          type: string
        type: {}
      type: object
    o11y.AttributeKey:
      properties:
        dataType:
          type: string
        isColumn:
          type: boolean
        isJSON:
          type: boolean
        key:
          type: string
        type:
          type: string
      type: object
    o11y.AttributesComponentEntry:
      properties:
        associatedComponent:
          $ref: '#/components/schemas/o11y.AssociatedComponent'
        attributes:
          items:
            type: string
          type: array
      type: object
    o11y.Authorization:
      properties:
        credentials: {}
        credentials_file:
          type: string
        credentials_ref:
          description: CredentialsRef is the name of the secret within the secret
            manager to use as credentials.
          type: string
        type:
          type: string
      type: object
    o11y.AzureAccountConfig:
      properties:
        deploymentRegion:
          type: string
        resourceGroups:
          items:
            type: string
          type: array
      type: object
    o11y.AzureConnectionArtifact:
      properties:
        cliCommand:
          type: string
        cloudPowerShellCommand:
          type: string
      type: object
    o11y.AzureIntegrationConfig:
      properties:
        deploymentRegion:
          type: string
        resourceGroups:
          items:
            type: string
          type: array
        telemetryCollectionStrategy:
          items:
            $ref: '#/components/schemas/o11y.AzureTelemetryCollectionStrategy'
          type: array
      type: object
    o11y.AzureLogsCollectionStrategy:
      properties:
        categoryGroups:
          description: List of categories to enable for diagnostic settings, to start
            with it will have 'allLogs' and no filtering.
          items:
            type: string
          type: array
      type: object
    o11y.AzureMetricsCollectionStrategy:
      properties: {}
      type: object
    o11y.AzureServiceConfig:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.AzureServiceLogsConfig'
        metrics:
          $ref: '#/components/schemas/o11y.AzureServiceMetricsConfig'
      type: object
    o11y.AzureServiceLogsConfig:
      properties:
        enabled:
          type: boolean
      type: object
    o11y.AzureServiceMetricsConfig:
      properties:
        enabled:
          type: boolean
      type: object
    o11y.AzureTelemetryCollectionStrategy:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.AzureLogsCollectionStrategy'
        metrics:
          $ref: '#/components/schemas/o11y.AzureMetricsCollectionStrategy'
        resourceProvider:
          description: https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/resource-providers-and-types
          type: string
        resourceType:
          type: string
      type: object
    o11y.BasicAuth:
      properties:
        password: {}
        password_file:
          type: string
        password_ref:
          description: PasswordRef is the name of the secret within the secret manager
            to use as the password.
          type: string
        username:
          type: string
        username_file:
          type: string
        username_ref:
          description: UsernameRef is the name of the secret within the secret manager
            to use as the username.
          type: string
      type: object
    o11y.BuilderQuery:
      properties:
        IsAnomaly:
          type: boolean
        QueriesUsedInFormula:
          items:
            type: string
          type: array
        ShiftBy:
          type: integer
        aggregateAttribute:
          $ref: '#/components/schemas/o11y.AttributeKey'
        aggregateOperator:
          type: string
        dataSource:
          type: string
        disabled:
          type: boolean
        expression:
          type: string
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        functions:
          items:
            $ref: '#/components/schemas/o11y.Function'
          type: array
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        having:
          items:
            $ref: '#/components/schemas/o11y.Having'
          type: array
        legend:
          type: string
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          items:
            $ref: '#/components/schemas/o11y.OrderBy'
          type: array
        pageSize:
          type: integer
        queryName:
          type: string
        reduceTo:
          type: string
        selectColumns:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        seriesAggregation:
          type: string
        spaceAggregation:
          type: string
        stepInterval:
          type: integer
        temporality:
          type: string
        timeAggregation:
          type: string
      type: object
    o11y.Channel:
      properties:
        createdAt:
          format: date-time
          type: string
        data:
          type: string
        id: {}
        name:
          type: string
        orgId:
          type: string
        type:
          type: string
        updatedAt:
          format: date-time
          type: string
      type: object
    o11y.Checks:
      properties:
        missingDefaultEnabledMetrics:
          items:
            $ref: '#/components/schemas/o11y.MissingMetricsComponentEntry'
          type: array
        missingOptionalMetrics:
          items:
            $ref: '#/components/schemas/o11y.MissingMetricsComponentEntry'
          type: array
        missingRequiredAttributes:
          items:
            $ref: '#/components/schemas/o11y.MissingAttributesComponentEntry'
          type: array
        presentDefaultEnabledMetrics:
          items:
            $ref: '#/components/schemas/o11y.MetricsComponentEntry'
          type: array
        presentOptionalMetrics:
          items:
            $ref: '#/components/schemas/o11y.MetricsComponentEntry'
          type: array
        presentRequiredAttributes:
          items:
            $ref: '#/components/schemas/o11y.AttributesComponentEntry'
          type: array
        ready:
          type: boolean
        type: {}
      type: object
    o11y.CloudIntegrationService:
      properties:
        cloudIntegrationId: {}
        config:
          $ref: '#/components/schemas/o11y.ServiceConfig'
        createdAt:
          format: date-time
          type: string
        id: {}
        type: {}
        updatedAt:
          format: date-time
          type: string
      type: object
    o11y.ClusterListRecord:
      properties:
        clusterUID:
          type: string
        cpuAllocatable:
          type: number
        cpuUsage:
          type: number
        memoryAllocatable:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
      type: object
    o11y.ClusterListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.ClusterListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.ClusterListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.ClusterRecord:
      properties:
        clusterCPU:
          type: number
        clusterCPUAllocatable:
          type: number
        clusterMemory:
          type: number
        clusterMemoryAllocatable:
          type: number
        clusterName:
          description: |-
            TODO(nikhilmantri0902): once the underlying attr key is migrated to
            k8s.cluster.uid (see ClusterNameAttrKey), surface ClusterUID alongside
            (or replace) ClusterName.
          type: string
        meta:
          additionalProperties:
            type: string
          type: object
        nodeCountsByReadiness:
          $ref: '#/components/schemas/o11y.NodeCountsByReadiness'
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
      type: object
    o11y.Clusters:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.ClusterRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.CollectedLogAttribute:
      properties:
        name:
          type: string
        path:
          type: string
        type:
          type: string
      type: object
    o11y.CollectedMetric:
      properties:
        description:
          type: string
        name:
          type: string
        type:
          type: string
        unit:
          type: string
      type: object
    o11y.CompositeQuery:
      properties:
        builderQueries:
          additionalProperties:
            $ref: '#/components/schemas/o11y.BuilderQuery'
          type: object
        chQueries:
          additionalProperties:
            $ref: '#/components/schemas/o11y.DatastoreQuery'
          type: object
        fillGaps:
          description: FillGaps is used to fill the gaps in the time series data
          type: boolean
        panelType:
          type: string
        promQueries:
          additionalProperties:
            $ref: '#/components/schemas/o11y.PromQuery'
          type: object
        queries:
          items:
            $ref: '#/components/schemas/o11y.QueryEnvelope'
          type: array
        queryType:
          type: string
        unit:
          description: |-
            Unit for the time series data shown in the graph
            This is used in alerts to format the value and threshold
          type: string
      type: object
    o11y.ConnectionArtifact:
      properties:
        aws:
          $ref: '#/components/schemas/o11y.AWSConnectionArtifact'
          description: required till new providers are added
        azure:
          $ref: '#/components/schemas/o11y.AzureConnectionArtifact'
        gcp:
          $ref: '#/components/schemas/o11y.GCPConnectionArtifact'
      type: object
    o11y.Credentials:
      properties:
        ingestionKey:
          type: string
        ingestionUrl:
          type: string
        o11yApiKey:
          description: PAT
          type: string
        o11yApiUrl:
          type: string
      type: object
    o11y.DaemonSetListRecord:
      properties:
        availableNodes:
          type: integer
        cpuLimit:
          type: number
        cpuRequest:
          type: number
        cpuUsage:
          type: number
        daemonSetName:
          type: string
        desiredNodes:
          type: integer
        memoryLimit:
          type: number
        memoryRequest:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        restarts:
          type: integer
      type: object
    o11y.DaemonSetListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.DaemonSetListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.DaemonSetListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.DaemonSetRecord:
      properties:
        currentNodes:
          type: integer
        daemonSetCPU:
          type: number
        daemonSetCPULimit:
          type: number
        daemonSetCPURequest:
          type: number
        daemonSetMemory:
          type: number
        daemonSetMemoryLimit:
          type: number
        daemonSetMemoryRequest:
          type: number
        daemonSetName:
          type: string
        desiredNodes:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
      type: object
    o11y.DaemonSets:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.DaemonSetRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.DataCollected:
      properties:
        logs:
          items:
            $ref: '#/components/schemas/o11y.CollectedLogAttribute'
          type: array
        metrics:
          items:
            $ref: '#/components/schemas/o11y.CollectedMetric'
          type: array
      type: object
    o11y.DataCollectedForIntegration:
      properties:
        logs:
          items:
            $ref: '#/components/schemas/o11y.integrations.CollectedLogAttribute'
          type: array
        metrics:
          items:
            $ref: '#/components/schemas/o11y.integrations.CollectedMetric'
          type: array
      type: object
    o11y.DatastoreQuery:
      properties:
        disabled:
          type: boolean
        legend:
          type: string
        query:
          type: string
      type: object
    o11y.DeploymentListRecord:
      properties:
        availablePods:
          type: integer
        cpuLimit:
          type: number
        cpuRequest:
          type: number
        cpuUsage:
          type: number
        deploymentName:
          type: string
        desiredPods:
          type: integer
        memoryLimit:
          type: number
        memoryRequest:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        restarts:
          type: integer
      type: object
    o11y.DeploymentListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.DeploymentListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.DeploymentListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.DeploymentRecord:
      properties:
        availablePods:
          type: integer
        deploymentCPU:
          type: number
        deploymentCPULimit:
          type: number
        deploymentCPURequest:
          type: number
        deploymentMemory:
          type: number
        deploymentMemoryLimit:
          type: number
        deploymentMemoryRequest:
          type: number
        deploymentName:
          type: string
        desiredPods:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
      type: object
    o11y.Deployments:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.DeploymentRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.DeprecatedGettableAlert:
      properties:
        Alert:
          $ref: '#/components/schemas/o11y.Alert'
        fingerprint:
          type: string
        receivers:
          items:
            type: string
          type: array
        status:
          $ref: '#/components/schemas/o11y.AlertStatus'
      type: object
    o11y.DiscordConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        avatar_url:
          type: string
        content:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        title:
          type: string
        username:
          type: string
        webhook_url: {}
        webhook_url_file:
          type: string
      type: object
    o11y.EmailConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        auth_identity:
          type: string
        auth_password: {}
        auth_password_file:
          type: string
        auth_secret: {}
        auth_secret_file:
          type: string
        auth_username:
          type: string
        force_implicit_tls:
          description: |-
            ForceImplicitTLS controls whether to use implicit TLS (direct TLS connection).
            true: force use of implicit TLS (direct TLS connection)
            false: force disable implicit TLS (use explicit TLS/STARTTLS if required)
            nil (default): auto-detect based on port (465=implicit, other=explicit) for backward compatibility
          type: boolean
        from:
          type: string
        headers:
          additionalProperties:
            type: string
          type: object
        hello:
          type: string
        html:
          type: string
        require_tls:
          type: boolean
        smarthost: {}
        text:
          type: string
        threading:
          $ref: '#/components/schemas/o11y.ThreadingConfig'
        tls_config:
          $ref: '#/components/schemas/o11y.TLSConfig'
        to:
          description: Email address to notify.
          type: string
      type: object
    o11y.Event:
      properties:
        attributeMap:
          additionalProperties:
            type: object
          type: object
        isError:
          type: boolean
        name:
          type: string
        timeUnixNano:
          type: integer
      type: object
    o11y.Filter:
      properties:
        expression:
          description: expression to filter by following the filter syntax
          type: string
      type: object
    o11y.FilterAttributeKeyResponse:
      properties:
        attributeKeys:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
      type: object
    o11y.FilterAttributeValueRequest:
      properties:
        aggregateAttribute:
          type: string
        aggregateOperator:
          type: string
        dataSource:
          type: string
        endTimeMillis:
          type: integer
        existingFilterItems:
          items:
            $ref: '#/components/schemas/o11y.FilterItem'
          type: array
        filterAttributeKey:
          type: string
        filterAttributeKeyDataType:
          type: string
        includeRelated:
          type: boolean
        limit:
          type: integer
        metricNames:
          items:
            type: string
          type: array
        searchText:
          type: string
        startTimeMillis:
          type: integer
        tagType:
          type: string
      type: object
    o11y.FilterAttributeValueResponse:
      properties:
        boolAttributeValues:
          items:
            type: boolean
          type: array
        numberAttributeValues:
          items:
            type: object
          type: array
        relatedValues:
          $ref: '#/components/schemas/o11y.FilterAttributeValueResponse'
        stringAttributeValues:
          items:
            type: string
          type: array
      type: object
    o11y.FilterItem:
      properties:
        key:
          $ref: '#/components/schemas/o11y.AttributeKey'
        op:
          type: string
        value:
          type: object
      type: object
    o11y.FilterSet:
      properties:
        items:
          items:
            $ref: '#/components/schemas/o11y.FilterItem'
          type: array
        op:
          type: string
      type: object
    o11y.FlamegraphSpan:
      properties:
        attributes:
          additionalProperties:
            type: object
          type: object
        durationNano:
          type: integer
        event:
          items:
            $ref: '#/components/schemas/o11y.Event'
          type: array
        hasError:
          type: boolean
        level:
          type: integer
        name:
          type: string
        parentSpanId:
          type: string
        resource:
          additionalProperties:
            type: string
          type: object
        spanId:
          type: string
        timestamp:
          type: integer
      type: object
    o11y.FormatOptions:
      properties:
        fillGaps:
          type: boolean
        formatTableResultForUI:
          type: boolean
      type: object
    o11y.Function:
      properties:
        args:
          items:
            type: object
          type: array
        name:
          type: string
        namedArgs:
          additionalProperties:
            type: object
          type: object
      type: object
    o11y.FunnelStep:
      properties:
        description:
          description: step description
          type: string
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        has_errors:
          type: boolean
        id: {}
        latency_pointer:
          type: string
        latency_type:
          type: string
        name:
          description: step name
          type: string
        service_name:
          type: string
        span_name:
          type: string
        step_order:
          type: integer
      type: object
    o11y.GCPAccountConfig:
      properties:
        deploymentProjectId:
          description: Project ID where central pub/sub for logs exist
          type: string
        deploymentRegion:
          description: Project ID where otel collector will be deployed
          type: string
        projectIds:
          description: List of project IDs to monitor
          items:
            type: string
          type: array
      type: object
    o11y.GCPConnectionArtifact:
      properties: {}
      type: object
    o11y.GCPIntegrationConfig:
      properties: {}
      type: object
    o11y.GCPServiceConfig:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.GCPServiceLogsConfig'
        metrics:
          $ref: '#/components/schemas/o11y.GCPServiceMetricsConfig'
      type: object
    o11y.GCPServiceLogsConfig:
      properties:
        enabled:
          type: boolean
      type: object
    o11y.GCPServiceMetricsConfig:
      properties:
        enabled:
          type: boolean
      type: object
    o11y.GettableAccountWithConnectionArtifact:
      properties:
        connectionArtifact:
          $ref: '#/components/schemas/o11y.ConnectionArtifact'
        id: {}
      type: object
    o11y.GettableAccounts:
      properties:
        accounts:
          items:
            $ref: '#/components/schemas/o11y.Account'
          type: array
      type: object
    o11y.GettableAgentCheckIn:
      properties:
        account_id:
          description: Older fields for backward compatibility with existing AWS agents
          type: string
        cloud_account_id:
          type: string
        cloudIntegrationId:
          type: string
        integration_config:
          $ref: '#/components/schemas/o11y.IntegrationConfig'
        integrationConfig:
          $ref: '#/components/schemas/o11y.ProviderIntegrationConfig'
        providerAccountId:
          type: string
        removed_at:
          format: date-time
          type: string
        removedAt:
          format: date-time
          type: string
      type: object
    o11y.GettableCreatedIngestionKey:
      properties:
        id:
          type: string
        value:
          type: string
      type: object
    o11y.GettableCreatedIngestionKeyLimit:
      properties:
        id:
          type: string
      type: object
    o11y.GettableFieldKeys:
      properties:
        complete:
          type: boolean
        keys:
          additionalProperties:
            items:
              $ref: '#/components/schemas/o11y.TelemetryFieldKey'
            type: array
          type: object
      type: object
    o11y.GettableFieldValues:
      properties:
        complete:
          type: boolean
        values:
          $ref: '#/components/schemas/o11y.TelemetryFieldValues'
      type: object
    o11y.GettableFlamegraphTrace:
      properties:
        endTimestampMillis:
          type: integer
        hasMore:
          type: boolean
        spans:
          items:
            items:
              $ref: '#/components/schemas/o11y.FlamegraphSpan'
            type: array
          type: array
        startTimestampMillis:
          type: integer
      type: object
    o11y.GettableFunnel:
      properties:
        created_at:
          type: integer
        created_by:
          type: string
        description:
          type: string
        funnel:
          $ref: '#/components/schemas/o11y.StorableFunnel'
        funnel_id:
          type: string
        funnel_name:
          type: string
        org_id:
          type: string
        steps:
          items:
            $ref: '#/components/schemas/o11y.FunnelStep'
          type: array
        updated_at:
          type: integer
        updated_by:
          type: string
        user_email:
          type: string
      type: object
    o11y.GettableHost:
      properties:
        hosts:
          items:
            $ref: '#/components/schemas/o11y.Host'
          type: array
        name:
          type: string
        state:
          type: string
        tier:
          type: string
      type: object
    o11y.GettableIngestionKeys:
      properties:
        _pagination:
          $ref: '#/components/schemas/o11y.Pagination'
        keys:
          items:
            $ref: '#/components/schemas/o11y.IngestionKey'
          type: array
      type: object
    o11y.GettableRoutePolicy:
      properties:
        channels:
          items:
            type: string
          type: array
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        description:
          type: string
        expression:
          type: string
        id:
          type: string
        kind: {}
        name:
          type: string
        tags:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
      type: object
    o11y.GettableRuleStateHistory:
      properties:
        fingerprint:
          type: integer
        labels:
          items:
            $ref: '#/components/schemas/o11y.Label'
          type: array
        overallState: {}
        overallStateChanged:
          type: boolean
        ruleId:
          type: string
        ruleName:
          type: string
        state: {}
        stateChanged:
          type: boolean
        unixMilli:
          type: integer
        value:
          type: number
      type: object
    o11y.GettableRuleStateHistoryContributor:
      properties:
        count:
          type: integer
        fingerprint:
          type: integer
        labels:
          items:
            $ref: '#/components/schemas/o11y.Label'
          type: array
        relatedLogsLink:
          type: string
        relatedTracesLink:
          type: string
      type: object
    o11y.GettableRuleStateHistoryStats:
      properties:
        currentAvgResolutionTime:
          type: number
        currentAvgResolutionTimeSeries:
          $ref: '#/components/schemas/o11y.TimeSeries'
        currentTriggersSeries:
          $ref: '#/components/schemas/o11y.TimeSeries'
        pastAvgResolutionTime:
          type: number
        pastAvgResolutionTimeSeries:
          $ref: '#/components/schemas/o11y.TimeSeries'
        pastTriggersSeries:
          $ref: '#/components/schemas/o11y.TimeSeries'
        totalCurrentTriggers:
          type: integer
        totalPastTriggers:
          type: integer
      type: object
    o11y.GettableRuleStateTimeline:
      properties:
        items:
          items:
            $ref: '#/components/schemas/o11y.GettableRuleStateHistory'
          type: array
        nextCursor:
          type: string
        total:
          type: integer
      type: object
    o11y.GettableRuleStateWindow:
      properties:
        end:
          type: integer
        start:
          type: integer
        state: {}
      type: object
    o11y.GettableServicesMetadata:
      properties:
        services:
          items:
            $ref: '#/components/schemas/o11y.ServiceMetadata'
          type: array
      type: object
    o11y.GettableSpanMapperGroups:
      properties:
        items:
          items:
            $ref: '#/components/schemas/o11y.SpanMapperGroup'
          type: array
      type: object
    o11y.GettableSpanMappers:
      properties:
        items:
          items:
            $ref: '#/components/schemas/o11y.SpanMapper'
          type: array
      type: object
    o11y.GettableTestRule:
      properties:
        alertCount:
          type: integer
        message:
          type: string
      type: object
    o11y.GettableTraceAggregations:
      properties:
        aggregations:
          items:
            $ref: '#/components/schemas/o11y.SpanAggregationResult'
          type: array
      type: object
    o11y.GettableWaterfallTrace:
      properties:
        endTimestampMillis:
          type: integer
        hasMissingSpans:
          type: boolean
        hasMore:
          type: boolean
        rootServiceEntryPoint:
          type: string
        rootServiceName:
          type: string
        spans:
          items:
            $ref: '#/components/schemas/o11y.WaterfallSpan'
          type: array
        startTimestampMillis:
          type: integer
        totalErrorSpansCount:
          type: integer
        totalSpansCount:
          type: integer
        uncollapsedSpans:
          items:
            type: string
          type: array
      type: object
    o11y.GoogleChatReceiverConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        text:
          type: string
        title:
          type: string
        webhook_url: {}
      type: object
    o11y.GroupByKey:
      properties:
        description:
          type: string
        fieldContext: {}
        fieldDataType: {}
        name:
          type: string
        signal: {}
        unit:
          type: string
      required:
      - name
      type: object
    o11y.HTTPClientConfig:
      properties:
        authorization:
          $ref: '#/components/schemas/o11y.Authorization'
          description: The HTTP authorization credentials for the targets.
        basic_auth:
          $ref: '#/components/schemas/o11y.BasicAuth'
          description: The HTTP basic authentication credentials for the targets.
        bearer_token:
          description: |-
            The bearer token for the targets. Deprecated in favour of
            Authorization.Credentials.
        bearer_token_file:
          description: |-
            The bearer token file for the targets. Deprecated in favour of
            Authorization.CredentialsFile.
          type: string
        enable_http2:
          description: |-
            EnableHTTP2 specifies whether the client should configure HTTP2.
            The omitempty flag is not set, because it would be hidden from the
            marshalled configuration when set to false.
          type: boolean
        follow_redirects:
          description: |-
            FollowRedirects specifies whether the client should follow HTTP 3xx redirects.
            The omitempty flag is not set, because it would be hidden from the
            marshalled configuration when set to false.
          type: boolean
        http_headers:
          description: |-
            HTTPHeaders specify headers to inject in the requests. Those headers
            could be marshalled back to the users.
        no_proxy:
          type: string
        oauth2:
          $ref: '#/components/schemas/o11y.OAuth2'
          description: The OAuth2 client credentials used to fetch a token for the
            targets.
        proxy_connect_header:
          additionalProperties:
            items: {}
            type: array
          type: object
        proxy_from_environment:
          type: boolean
        proxy_url: {}
        tls_config:
          $ref: '#/components/schemas/o11y.TLSConfig'
          description: TLSConfig to use to connect to the targets.
      type: object
    o11y.Having:
      properties:
        columnName:
          type: string
        op:
          type: string
        value:
          type: object
      type: object
    o11y.Host:
      properties:
        is_default:
          type: boolean
        name:
          type: string
        url:
          type: string
      type: object
    o11y.HostFilter:
      properties:
        Filter:
          $ref: '#/components/schemas/o11y.Filter'
        filterByStatus: {}
      type: object
    o11y.HostListRecord:
      properties:
        active:
          type: boolean
        cpu:
          type: number
        hostName:
          type: string
        load15:
          type: number
        memory:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        os:
          type: string
        wait:
          type: number
      type: object
    o11y.HostListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.HostListResponse:
      properties:
        clusterNames:
          items:
            type: string
          type: array
        endTimeBeforeRetention:
          type: boolean
        isSendingK8SAgentMetrics:
          type: boolean
        nodeNames:
          items:
            type: string
          type: array
        records:
          items:
            $ref: '#/components/schemas/o11y.HostListRecord'
          type: array
        sentAnyHostMetricsData:
          type: boolean
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.HostRecord:
      properties:
        activeHostCount:
          type: integer
        cpu:
          type: number
        diskUsage:
          type: number
        hostName:
          type: string
        inactiveHostCount:
          type: integer
        load15:
          type: number
        memory:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        status: {}
        wait:
          type: number
      type: object
    o11y.Hosts:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.HostRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.IncidentioConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        alert_source_token:
          description: AlertSourceToken is the key used to authenticate with the alert
            source in incident.io.
        alert_source_token_file:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        max_alerts:
          description: |-
            MaxAlerts is the maximum number of alerts to be sent per incident.io message.
            Alerts exceeding this threshold will be truncated. Setting this to 0
            allows an unlimited number of alerts. Note that if the payload exceeds
            incident.io's size limits, you will receive a 429 response and alerts
            will not be ingested.
          type: integer
        timeout:
          description: |-
            Timeout is the maximum time allowed to invoke incident.io. Setting this to 0
            does not impose a timeout.
          type: integer
        url:
          description: URL to send POST request to.
        url_file:
          type: string
      type: object
    o11y.IngestionKey:
      properties:
        created_at:
          format: date-time
          type: string
        expires_at:
          format: date-time
          type: string
        id:
          type: string
        limits:
          items:
            $ref: '#/components/schemas/o11y.Limit'
          type: array
        name:
          type: string
        tags:
          items:
            type: string
          type: array
        updated_at:
          format: date-time
          type: string
        value:
          type: string
        workspace_id:
          type: string
      type: object
    o11y.InstallIntegrationRequest:
      properties:
        config:
          additionalProperties:
            type: object
          type: object
        integration_id:
          type: string
      type: object
    o11y.InstalledIntegration:
      properties:
        config:
          additionalProperties:
            type: object
          type: object
        id: {}
        installed_at:
          format: date-time
          type: string
        org_id:
          type: string
        type:
          type: string
      type: object
    o11y.Integration:
      properties:
        assets:
          $ref: '#/components/schemas/o11y.IntegrationAssets'
        author:
          $ref: '#/components/schemas/o11y.IntegrationAuthor'
        categories:
          items:
            type: string
          type: array
        configuration:
          items:
            $ref: '#/components/schemas/o11y.IntegrationConfigStep'
          type: array
        connection_tests:
          $ref: '#/components/schemas/o11y.IntegrationConnectionTests'
        data_collected:
          $ref: '#/components/schemas/o11y.DataCollectedForIntegration'
        description:
          type: string
        icon:
          type: string
        id:
          type: string
        installation:
          $ref: '#/components/schemas/o11y.InstalledIntegration'
        overview:
          type: string
        title:
          type: string
      type: object
    o11y.IntegrationAssets:
      properties:
        alerts:
          items: {}
          type: array
        dashboards:
          items:
            additionalProperties:
              type: object
            type: object
          type: array
        logs:
          $ref: '#/components/schemas/o11y.LogsAssets'
      type: object
    o11y.IntegrationAuthor:
      properties:
        email:
          type: string
        homepage:
          type: string
        name:
          type: string
      type: object
    o11y.IntegrationConfig:
      properties:
        enabled_regions:
          description: backward compatible
          items:
            type: string
          type: array
        telemetry:
          $ref: '#/components/schemas/o11y.OldAWSCollectionStrategy'
          description: backward compatible
      type: object
    o11y.IntegrationConfigStep:
      properties:
        instructions:
          type: string
        title:
          type: string
      type: object
    o11y.IntegrationConnectionStatus:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.SignalConnectionStatus'
        metrics:
          $ref: '#/components/schemas/o11y.SignalConnectionStatus'
      type: object
    o11y.IntegrationConnectionTests:
      properties:
        logs:
          $ref: '#/components/schemas/o11y.LogsConnectionTest'
        metrics:
          description: Metric names expected to have been received for the integration.
          items:
            type: string
          type: array
      type: object
    o11y.IntegrationsListItem:
      properties:
        author:
          $ref: '#/components/schemas/o11y.IntegrationAuthor'
        description:
          type: string
        icon:
          type: string
        id:
          type: string
        is_installed:
          type: boolean
        title:
          type: string
      type: object
    o11y.IntegrationsListResponse:
      properties:
        integrations:
          items:
            $ref: '#/components/schemas/o11y.IntegrationsListItem'
          type: array
      type: object
    o11y.JiraConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        api_type:
          type: string
        api_url: {}
        custom_fields:
          additionalProperties:
            type: object
          type: object
        description:
          $ref: '#/components/schemas/o11y.JiraFieldConfig'
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        issue_type:
          type: string
        labels:
          items:
            type: string
          type: array
        priority:
          type: string
        project:
          type: string
        reopen_duration: {}
        reopen_transition:
          type: string
        resolve_transition:
          type: string
        summary:
          $ref: '#/components/schemas/o11y.JiraFieldConfig'
        wont_fix_resolution:
          type: string
      type: object
    o11y.JiraFieldConfig:
      properties:
        enable_update:
          description: EnableUpdate indicates whether this field should be omitted
            when updating an existing issue.
          type: boolean
        template:
          description: Template is the template string used to render the field.
          type: string
      type: object
    o11y.JobListRecord:
      properties:
        activePods:
          type: integer
        cpuLimit:
          type: number
        cpuRequest:
          type: number
        cpuUsage:
          type: number
        desiredSuccessfulPods:
          type: integer
        failedPods:
          type: integer
        jobName:
          type: string
        memoryLimit:
          type: number
        memoryRequest:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        restarts:
          type: integer
        successfulPods:
          type: integer
      type: object
    o11y.JobListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.JobListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.JobListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.JobRecord:
      properties:
        activePods:
          type: integer
        desiredSuccessfulPods:
          type: integer
        failedPods:
          type: integer
        jobCPU:
          type: number
        jobCPULimit:
          type: number
        jobCPURequest:
          type: number
        jobMemory:
          type: number
        jobMemoryLimit:
          type: number
        jobMemoryRequest:
          type: number
        jobName:
          type: string
        meta:
          additionalProperties:
            type: string
          type: object
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
        successfulPods:
          type: integer
      type: object
    o11y.Jobs:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.JobRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.Label:
      properties:
        key:
          $ref: '#/components/schemas/o11y.TelemetryFieldKey'
        value:
          type: object
      type: object
    o11y.Limit:
      properties:
        config:
          $ref: '#/components/schemas/o11y.LimitConfig'
        created_at:
          format: date-time
          type: string
        id:
          type: string
        key_id:
          type: string
        metric:
          $ref: '#/components/schemas/o11y.LimitMetric'
        signal:
          description: '"logs", "traces", "metrics"'
          type: string
        tags:
          items:
            type: string
          type: array
        updated_at:
          format: date-time
          type: string
      type: object
    o11y.LimitConfig:
      properties:
        day:
          $ref: '#/components/schemas/o11y.LimitValue'
        second:
          $ref: '#/components/schemas/o11y.LimitValue'
      type: object
    o11y.LimitMetric:
      properties:
        day:
          $ref: '#/components/schemas/o11y.LimitMetricValue'
        second:
          $ref: '#/components/schemas/o11y.LimitMetricValue'
      type: object
    o11y.LimitMetricValue:
      properties:
        count:
          type: integer
        size:
          type: integer
      type: object
    o11y.LimitValue:
      properties:
        count:
          type: integer
        size:
          type: integer
      type: object
    o11y.LogsAssets:
      properties:
        pipelines:
          items:
            $ref: '#/components/schemas/o11y.PostablePipeline'
          type: array
      type: object
    o11y.LogsConnectionTest:
      properties:
        attribute_key:
          type: string
        attribute_value:
          type: string
      type: object
    o11y.MSTeamsConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        summary:
          type: string
        text:
          type: string
        title:
          type: string
        webhook_url: {}
        webhook_url_file:
          type: string
      type: object
    o11y.MSTeamsV2Config:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        text:
          type: string
        title:
          type: string
        webhook_url: {}
        webhook_url_file:
          type: string
      type: object
    o11y.MattermostAttachment:
      properties:
        author_icon:
          type: string
        author_link:
          type: string
        author_name:
          type: string
        color:
          type: string
        fallback:
          type: string
        fields:
          items:
            $ref: '#/components/schemas/o11y.MattermostField'
          type: array
        footer:
          type: string
        footer_icon:
          type: string
        image_url:
          type: string
        pretext:
          type: string
        text:
          type: string
        thumb_url:
          type: string
        title:
          type: string
        title_link:
          type: string
      type: object
    o11y.MattermostConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        attachments:
          items:
            $ref: '#/components/schemas/o11y.MattermostAttachment'
          type: array
        channel:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        icon_emoji:
          type: string
        icon_url:
          type: string
        priority:
          $ref: '#/components/schemas/o11y.MattermostPriority'
        props:
          $ref: '#/components/schemas/o11y.MattermostProps'
        text:
          type: string
        type:
          type: string
        username:
          type: string
        webhook_url: {}
        webhook_url_file:
          type: string
      type: object
    o11y.MattermostField:
      properties:
        short:
          type: boolean
        title:
          type: string
        value:
          type: string
      type: object
    o11y.MattermostPriority:
      properties:
        persistent_notifications:
          type: boolean
        priority:
          type: string
        requested_ack:
          type: boolean
      type: object
    o11y.MattermostProps:
      properties:
        card:
          type: string
      type: object
    o11y.MetricsComponentEntry:
      properties:
        associatedComponent:
          $ref: '#/components/schemas/o11y.AssociatedComponent'
        metrics:
          items:
            type: string
          type: array
      type: object
    o11y.MissingAttributesComponentEntry:
      properties:
        associatedComponent:
          $ref: '#/components/schemas/o11y.AssociatedComponent'
        attributes:
          items:
            type: string
          type: array
        documentationLink:
          type: string
        message:
          type: string
      type: object
    o11y.MissingMetricsComponentEntry:
      properties:
        associatedComponent:
          $ref: '#/components/schemas/o11y.AssociatedComponent'
        documentationLink:
          type: string
        message:
          type: string
        metrics:
          items:
            type: string
          type: array
      type: object
    o11y.NamespaceListRecord:
      properties:
        countByPhase:
          $ref: '#/components/schemas/o11y.PodCountByPhase'
        cpuUsage:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        namespaceName:
          type: string
      type: object
    o11y.NamespaceListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.NamespaceListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.NamespaceListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.NamespaceRecord:
      properties:
        meta:
          additionalProperties:
            type: string
          type: object
        namespaceCPU:
          type: number
        namespaceMemory:
          type: number
        namespaceName:
          type: string
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
      type: object
    o11y.Namespaces:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.NamespaceRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.NodeCountByCondition:
      properties:
        notReady:
          type: integer
        ready:
          type: integer
        unknown:
          type: integer
      type: object
    o11y.NodeCountsByReadiness:
      properties:
        notReady:
          type: integer
        ready:
          type: integer
      type: object
    o11y.NodeListRecord:
      properties:
        countByCondition:
          $ref: '#/components/schemas/o11y.NodeCountByCondition'
        meta:
          additionalProperties:
            type: string
          type: object
        nodeCPUAllocatable:
          type: number
        nodeCPUUsage:
          type: number
        nodeMemoryAllocatable:
          type: number
        nodeMemoryUsage:
          type: number
        nodeUID:
          type: string
      type: object
    o11y.NodeListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.NodeListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.NodeListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.NodeRecord:
      properties:
        condition: {}
        meta:
          additionalProperties:
            type: string
          type: object
        nodeCPU:
          type: number
        nodeCPUAllocatable:
          type: number
        nodeCountsByReadiness:
          $ref: '#/components/schemas/o11y.NodeCountsByReadiness'
        nodeMemory:
          type: number
        nodeMemoryAllocatable:
          type: number
        nodeName:
          type: string
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
      type: object
    o11y.Nodes:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.NodeRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.NotifierConfig:
      properties:
        send_resolved:
          type: boolean
      type: object
    o11y.O11yAPIKey:
      properties:
        createdAt:
          description: CreatedAt is when the key was minted.
          format: date-time
          type: string
        expiresAt:
          description: |-
            ExpiresAt is when the key stops working, as a unix timestamp in seconds;
            zero means never.
          type: integer
        id:
          description: ID is the key id.
          type: string
        lastObservedAt:
          description: LastObservedAt is when the key was last seen authenticating.
          format: date-time
          type: string
        name:
          description: Name is the key's name.
          type: string
        serviceAccountId:
          description: ServiceAccountID is the account the key belongs to.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yAPIKeyCreateIn:
      properties:
        expiresAt:
          description: |-
            ExpiresAt is when the key stops working, as a unix timestamp in seconds.
            Zero means it never expires; a past timestamp is refused.
          type: integer
        name:
          description: |-
            Name is the key's name: a lowercase letter followed by lowercase letters,
            digits or hyphens, at most 80 characters. Required.
          type: string
      type: object
    o11y.O11yAPIKeyCreateOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yAPIKeySecret'
          description: Data carries the key's id and secret.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAPIKeySecret:
      properties:
        id:
          description: ID is the key id.
          type: string
        key:
          description: Key is the secret the service account authenticates with.
          type: string
      type: object
    o11y.O11yAPIKeyUpdateIn:
      properties:
        expiresAt:
          description: |-
            ExpiresAt is when the key stops working, as a unix timestamp in seconds.
            Zero means it never expires; a past timestamp is refused.
          type: integer
        name:
          description: |-
            Name is the key's new name, under the same rules it was created with.
            Required.
          type: string
      type: object
    o11y.O11yAPIKeysOut:
      properties:
        data:
          description: Data holds the keys.
          items:
            $ref: '#/components/schemas/o11y.O11yAPIKey'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAccountOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Account'
          description: Data holds the account.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAccountsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableAccounts'
          description: Data holds the connected accounts.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAck:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAffectedAsset:
      properties:
        id:
          description: ID is the asset's id.
          type: string
        impactedLabels:
          description: ImpactedLabels are the rule labels the asset uses.
          items:
            type: string
          type: array
        name:
          description: Name is the asset's name.
          type: string
        type:
          description: Type is dashboard or alert_rule.
          type: string
        widget:
          $ref: '#/components/schemas/o11y.O11yAffectedWidget'
          description: Widget is the affected panel, for a dashboard.
      type: object
    o11y.O11yAffectedWidget:
      properties:
        id:
          description: ID is the panel's id.
          type: string
        name:
          description: Name is the panel's name.
          type: string
      type: object
    o11y.O11yAgentCheckInIn:
      properties:
        account_id:
          type: string
        cloud_account_id:
          type: string
        cloudIntegrationId: {}
        data:
          additionalProperties:
            type: object
          type: object
        providerAccountId:
          type: string
      type: object
    o11y.O11yAgentCheckInOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableAgentCheckIn'
          description: Data holds the check-in result.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAggregateAttributesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.AggregateAttributeResponse'
          description: Data holds the attribute keys.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAggregation:
      properties:
        alias:
          description: Alias is the aggregation's alias.
          type: string
        anomalyScores:
          description: AnomalyScores are anomaly overlays.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
        index:
          description: Index is the aggregation's position in the query.
          type: integer
        lowerBoundSeries:
          description: LowerBoundSeries are forecast lower bounds.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
        meta:
          $ref: '#/components/schemas/o11y.O11yAggregationMeta'
          description: Meta describes the aggregation.
        predictedSeries:
          description: PredictedSeries are forecast overlays, when the query asked
            for them.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
        series:
          description: Series are the aggregated time series.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
        upperBoundSeries:
          description: UpperBoundSeries are forecast upper bounds.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
      type: object
    o11y.O11yAggregationMeta:
      properties:
        unit:
          description: Unit is the aggregation's unit.
          type: string
      type: object
    o11y.O11yAlertsOut:
      properties:
        data:
          description: Data holds the alerts.
          items:
            $ref: '#/components/schemas/o11y.DeprecatedGettableAlert'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAnalyzeIn:
      properties:
        query:
          description: Query is the query text. Required.
          type: string
        queryType:
          description: |-
            QueryType says which language the query is in — promql or the datastore's
            SQL dialect. Required.
          type: string
      required:
      - query
      - queryType
      type: object
    o11y.O11yAnalyzeOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yQueryFilterAnalysis'
          description: Data is the analysis.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yApdexOut:
      properties:
        data:
          description: Data holds one settings row per service.
          items:
            $ref: '#/components/schemas/o11y.O11yApdexSettings'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yApdexSetIn:
      properties:
        excludeStatusCodes:
          description: |-
            ExcludeStatusCodes are status codes excluded from the score, comma
            separated.
          type: string
        serviceName:
          description: ServiceName is the service the threshold applies to.
          type: string
        threshold:
          description: Threshold is the satisfied-response time in seconds.
          type: number
      type: object
    o11y.O11yApdexSetOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMessage'
          description: Data is the acknowledgment.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yApdexSettings:
      properties:
        excludeStatusCodes:
          description: |-
            ExcludeStatusCodes are status codes excluded from the score, comma
            separated.
          type: string
        id:
          description: ID is the settings row's id.
          type: string
        orgId:
          description: OrgID is the org the settings belong to.
          type: string
        serviceName:
          description: ServiceName is the service.
          type: string
        threshold:
          description: Threshold is the satisfied-response time in seconds.
          type: number
      type: object
    o11y.O11yAttributeKey:
      properties:
        dataType:
          description: DataType is the attribute's value type — string, int64, float64
            or bool.
          type: string
        isColumn:
          description: IsColumn marks an attribute materialized as its own column.
          type: boolean
        isJSON:
          description: IsJSON marks an attribute extracted from a JSON body.
          type: boolean
        key:
          description: Key is the attribute's name.
          type: string
        type:
          description: 'Type says where the attribute lives: tag or resource.'
          type: string
      type: object
    o11y.O11yAttributeKeysOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.FilterAttributeKeyResponse'
          description: Data holds the attribute keys.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAttributeMapping:
      properties:
        email:
          description: Email is the key carrying the email; defaults to "email".
          type: string
        groups:
          description: Groups is the key carrying the group list; defaults to "groups".
          type: string
        name:
          description: Name is the key carrying the display name; defaults to "name".
          type: string
        role:
          description: Role is the key carrying the role; defaults to "role".
          type: string
      type: object
    o11y.O11yAttributeValuesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.FilterAttributeValueResponse'
          description: Data holds the values, split by data type.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAuthDomain:
      properties:
        authNProviderInfo:
          $ref: '#/components/schemas/o11y.O11yAuthNProviderInfo'
          description: AuthNProviderInfo is provider detail the console needs to finish
            setup.
        config:
          $ref: '#/components/schemas/o11y.O11yAuthDomainConfig'
          description: Config is the domain's SSO configuration.
        createdAt:
          description: CreatedAt is when it was claimed.
          format: date-time
          type: string
        id:
          description: ID is the auth domain id.
          type: string
        name:
          description: Name is the email domain, e.g. example.com.
          type: string
        orgId:
          description: OrgID is the org that claimed it.
          type: string
        updatedAt:
          description: UpdatedAt is when its configuration last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yAuthDomainConfig:
      properties:
        googleAuthConfig:
          $ref: '#/components/schemas/o11y.O11yGoogleConfig'
          description: Google is the Google provider's settings, when SSOType is google_auth.
        oidcConfig:
          $ref: '#/components/schemas/o11y.O11yOIDCConfig'
          description: OIDC is the OIDC provider's settings, when SSOType is oidc.
        roleMapping:
          $ref: '#/components/schemas/o11y.O11yRoleMapping'
          description: RoleMapping maps the provider's groups onto roles for new users.
        samlConfig:
          $ref: '#/components/schemas/o11y.O11ySAMLConfig'
          description: SAML is the SAML provider's settings, when SSOType is saml.
        ssoEnabled:
          description: SSOEnabled turns enforced SSO on for the domain.
          type: boolean
        ssoType:
          description: SSOType picks the provider — saml, google_auth or oidc.
          type: string
      type: object
    o11y.O11yAuthDomainOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yAuthDomain'
          description: Data is the domain.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAuthDomainsOut:
      properties:
        data:
          description: Data holds the domains.
          items:
            $ref: '#/components/schemas/o11y.O11yAuthDomain'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yAuthNProviderInfo:
      properties:
        relayStatePath:
          description: |-
            RelayStatePath is the relay-state path a SAML IdP must be configured
            with, when the provider needs one.
          type: string
      type: object
    o11y.O11yAuthNSupport:
      properties:
        callback:
          description: Callback are the SSO routes; each is begun by visiting its
            URL.
          items:
            $ref: '#/components/schemas/o11y.O11yCallbackAuthN'
          type: array
        password:
          description: Password are the password routes.
          items:
            $ref: '#/components/schemas/o11y.O11yPasswordAuthN'
          type: array
      type: object
    o11y.O11yBulkInviteIn:
      properties:
        invites:
          description: Invites are the invitations to create; an email may appear
            only once.
          items:
            $ref: '#/components/schemas/o11y.O11yInviteIn'
          type: array
      type: object
    o11y.O11yCallbackAuthN:
      properties:
        provider:
          description: Provider is the route's provider — google_auth, saml or oidc.
          type: string
        url:
          description: URL is where the browser goes to begin the flow.
          type: string
      type: object
    o11y.O11yChangePasswordIn:
      properties:
        newPassword:
          description: NewPassword is the password to set.
          type: string
        oldPassword:
          description: |-
            OldPassword is the current password; the change is refused when it does
            not match.
          type: string
      type: object
    o11y.O11yChannelOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Channel'
          description: Data holds the channel.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yChannelUpdateIn:
      properties:
        Receiver:
          $ref: '#/components/schemas/o11y.Receiver'
        googlechat_configs:
          items:
            $ref: '#/components/schemas/o11y.GoogleChatReceiverConfig'
          type: array
      type: object
    o11y.O11yChannelsOut:
      properties:
        data:
          description: Data holds the channels.
          items:
            $ref: '#/components/schemas/o11y.Channel'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCheckOut:
      properties:
        data:
          description: Data holds the verdicts.
          items:
            $ref: '#/components/schemas/o11y.O11yTransactionResult'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yClusterListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.ClusterListResponse'
          description: Data holds the cluster records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yColumnInfo:
      properties:
        columnAlias:
          description: Alias is the column's alias in the query, when it has one.
          type: string
        columnName:
          description: Name is the column's name.
          type: string
      type: object
    o11y.O11yConnectionStatusOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.IntegrationConnectionStatus'
          description: Data holds the logs and metrics connection status.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCreateAccountIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.PostableAccountConfig'
        credentials:
          $ref: '#/components/schemas/o11y.Credentials'
      type: object
    o11y.O11yCreateAccountOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableAccountWithConnectionArtifact'
          description: Data holds the account and the connection artifact.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCreateLimitIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.LimitConfig'
        signal:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.O11yCreated:
      properties:
        id:
          description: ID is the new record's id.
          type: string
      type: object
    o11y.O11yCreatedIngestionKeyOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableCreatedIngestionKey'
          description: Data holds the created ingestion key.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCreatedLimitOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableCreatedIngestionKeyLimit'
          description: Data holds the created limit.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCreatedOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yCreated'
          description: Data carries the id.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yCredentialsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Credentials'
          description: Data holds the connection credentials.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDaemonSetListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.DaemonSetListResponse'
          description: Data holds the daemonset records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboard:
      properties:
        createdAt:
          description: CreatedAt is when the dashboard was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created it.
          type: string
        id:
          description: ID is the dashboard's id.
          type: string
        image:
          description: Image is an optional cover image reference.
          type: string
        locked:
          description: Locked reports whether the dashboard is locked against edits.
          type: boolean
        name:
          description: Name is the dashboard's unique internal name.
          type: string
        orgId:
          description: OrgID is the org the dashboard belongs to.
          type: string
        schemaVersion:
          description: SchemaVersion is the dashboard schema version.
          type: string
        source:
          description: 'Source is where the dashboard came from: user, system or integration.'
          type: string
        spec:
          description: Spec is the Perses dashboard spec, carried verbatim as open
            JSON.
        tags:
          description: Tags are the dashboard's tags.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardTag'
          type: array
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is who last changed it.
          type: string
      type: object
    o11y.O11yDashboardDisplay:
      properties:
        description:
          description: Description says what the dashboard is for.
          type: string
        name:
          description: Name is the human-facing dashboard name.
          type: string
      type: object
    o11y.O11yDashboardList:
      properties:
        dashboards:
          description: Dashboards are the rows for this page.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardListItem'
          type: array
        tags:
          description: Tags are all tags in use across the org's dashboards.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardTag'
          type: array
        total:
          description: Total is the count across all pages.
          type: integer
      type: object
    o11y.O11yDashboardListForUser:
      properties:
        dashboards:
          description: Dashboards are the rows for this page, each with the caller's
            pin state.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardListItemForUser'
          type: array
        tags:
          description: Tags are all tags in use across the org's dashboards.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardTag'
          type: array
        total:
          description: Total is the count across all pages.
          type: integer
      type: object
    o11y.O11yDashboardListForUserOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardListForUser'
          description: Data is the list page with pins.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardListItem:
      properties:
        createdAt:
          description: CreatedAt is when the dashboard was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created it.
          type: string
        id:
          description: ID is the dashboard's id.
          type: string
        image:
          description: Image is an optional cover image reference.
          type: string
        locked:
          description: Locked reports whether the dashboard is locked against edits.
          type: boolean
        name:
          description: Name is the dashboard's unique internal name.
          type: string
        orgId:
          description: OrgID is the org the dashboard belongs to.
          type: string
        schemaVersion:
          description: SchemaVersion is the dashboard schema version.
          type: string
        source:
          description: 'Source is where the dashboard came from: user, system or integration.'
          type: string
        spec:
          $ref: '#/components/schemas/o11y.O11yDashboardListSpec'
          description: Spec is the display-only slice of the dashboard spec.
        tags:
          description: Tags are the dashboard's tags.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardTag'
          type: array
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is who last changed it.
          type: string
      type: object
    o11y.O11yDashboardListItemForUser:
      properties:
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        id:
          type: string
        image:
          type: string
        locked:
          type: boolean
        name:
          type: string
        orgId:
          type: string
        pinned:
          description: Pinned reports whether the calling user has pinned this dashboard.
          type: boolean
        schemaVersion:
          type: string
        source:
          type: string
        spec:
          $ref: '#/components/schemas/o11y.O11yDashboardListSpec'
        tags:
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardTag'
          type: array
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
      type: object
    o11y.O11yDashboardListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardList'
          description: Data is the list page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardListSpec:
      properties:
        display:
          $ref: '#/components/schemas/o11y.O11yDashboardDisplay'
          description: Display is the dashboard's display metadata.
      type: object
    o11y.O11yDashboardOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboard'
          description: Data is the dashboard.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardPatchIn:
      properties:
        id:
          description: ID is the dashboard id from the path.
          type: string
        ops:
          description: |-
            Ops are the JSON Patch operations, applied in order. On the wire this IS the
            request body — a bare array, not an object.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardPatchOp'
          type: array
      type: object
    o11y.O11yDashboardPatchOp:
      properties:
        from:
          description: From is the source JSON Pointer for move and copy; ignored
            otherwise.
          type: string
        op:
          description: 'Op is the verb: add, remove, replace, move, copy or test.'
          type: string
        path:
          description: |-
            Path is a JSON Pointer into the postable dashboard, e.g.
            /spec/display/name, /spec/panels/<id>, /tags/-.
          type: string
        value:
          description: |-
            Value is the value to add/replace/test; its type depends on Path. Required
            for add, replace and test; ignored otherwise.
      type: object
    o11y.O11yDashboardPostable:
      properties:
        generateName:
          description: |-
            GenerateName derives a fresh unique name from spec.display.name instead of
            taking Name.
          type: boolean
        image:
          description: Image is an optional cover image reference.
          type: string
        name:
          description: |-
            Name is the dashboard's unique internal name (a DNS-1123 label). Omit it
            with generateName to derive one from the display name.
          type: string
        schemaVersion:
          description: SchemaVersion is the dashboard schema version; must be the
            current v6.
          type: string
        spec:
          description: |-
            Spec is the Perses dashboard spec (display, variables, panels, layouts,
            datasources). Its plugin-defined leaves are open JSON, carried verbatim.
        tags:
          description: |-
            Tags are the dashboard's tags; at most ten, and none may use a reserved DSL
            key.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardPostableTag'
          type: array
      type: object
    o11y.O11yDashboardPostableTag:
      properties:
        key:
          description: Key is the tag key.
          type: string
        value:
          description: Value is the tag value.
          type: string
      type: object
    o11y.O11yDashboardTag:
      properties:
        key:
          description: Key is the tag key.
          type: string
        value:
          description: Value is the tag value.
          type: string
      type: object
    o11y.O11yDashboardUpdateIn:
      properties:
        id:
          description: ID is the dashboard id from the path.
          type: string
        image:
          type: string
        name:
          type: string
        schemaVersion:
          type: string
        spec: {}
        tags:
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardPostableTag'
          type: array
      type: object
    o11y.O11yDashboardVarValues:
      properties:
        variableValues:
          description: VariableValues are the values, in the order the query produced
            them.
          items:
            type: object
          type: array
      type: object
    o11y.O11yDashboardVarsIn:
      properties:
        query:
          description: Query is the variable query to evaluate. Required.
          type: string
        variables:
          additionalProperties:
            type: object
          description: |-
            Variables are the current values of the other dashboard variables, for
            queries that reference them.
          type: object
      required:
      - query
      type: object
    o11y.O11yDashboardVarsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardVarValues'
          description: Data holds the values.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardView:
      properties:
        createdAt:
          description: CreatedAt is when the view was created.
          format: date-time
          type: string
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardViewData'
          description: Data is the listing state the view captures.
        id:
          description: ID is the saved view's id.
          type: string
        name:
          description: Name is the saved view's name.
          type: string
        orgId:
          description: OrgID is the org the view belongs to.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yDashboardViewData:
      properties:
        order:
          description: Order is the captured sort direction.
          type: string
        query:
          description: Query is the captured filter DSL.
          type: string
        sort:
          description: Sort is the captured sort field.
          type: string
        version:
          description: Version is the saved-view schema version; must be v1.
          type: string
      type: object
    o11y.O11yDashboardViewList:
      properties:
        views:
          description: Views are the saved views, shared org-wide.
          items:
            $ref: '#/components/schemas/o11y.O11yDashboardView'
          type: array
      type: object
    o11y.O11yDashboardViewListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardViewList'
          description: Data is the saved views.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardViewOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardView'
          description: Data is the saved view.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDashboardViewPostable:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardViewData'
          description: Data is the listing state the view captures.
        name:
          description: Name is the saved view's name; at most 32 characters, no surrounding
            space.
          type: string
      type: object
    o11y.O11yDashboardViewUpdateIn:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDashboardViewData'
        id:
          description: ID is the saved view id from the path.
          type: string
        name:
          type: string
      type: object
    o11y.O11yDependency:
      properties:
        callCount:
          description: CallCount is how many calls crossed the edge in the window.
          type: integer
        callRate:
          description: CallRate is calls per second.
          type: number
        child:
          description: Child is the called service.
          type: string
        errorRate:
          description: ErrorRate is the percentage of calls that erred.
          type: number
        p50:
          description: P50 is the median call duration, in nanoseconds.
          type: number
        p75:
          description: P75 is the 75th-percentile call duration, in nanoseconds.
          type: number
        p90:
          description: P90 is the 90th-percentile call duration, in nanoseconds.
          type: number
        p95:
          description: P95 is the 95th-percentile call duration, in nanoseconds.
          type: number
        p99:
          description: P99 is the 99th-percentile call duration, in nanoseconds.
          type: number
        parent:
          description: Parent is the calling service.
          type: string
      type: object
    o11y.O11yDependencyGraphIn:
      properties:
        end:
          description: End is the window end, as epoch nanoseconds. Required.
          type: string
        start:
          description: Start is the window start, as epoch nanoseconds. Required.
          type: string
        tags:
          description: Tags narrow the graph to spans matching every condition.
          items:
            $ref: '#/components/schemas/o11y.O11yTagFilter'
          type: array
      required:
      - start
      - end
      type: object
    o11y.O11yDeploymentListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.DeploymentListResponse'
          description: Data holds the deployment records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDeprecatedUser:
      properties:
        createdAt:
          description: CreatedAt is when they joined.
          format: date-time
          type: string
        displayName:
          description: DisplayName is what the console shows for them.
          type: string
        email:
          description: Email is their address.
          type: string
        id:
          description: ID is the user id.
          type: string
        isRoot:
          description: IsRoot marks the org's root user.
          type: boolean
        orgId:
          description: OrgID is the org they belong to.
          type: string
        role:
          description: Role is their legacy role — ADMIN, EDITOR or VIEWER.
          type: string
        status:
          description: Status is their lifecycle state — active, pending_invite or
            deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when their record last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yDeprecatedUserOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDeprecatedUser'
          description: Data is the member.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDeprecatedUserUpdate:
      properties:
        displayName:
          description: DisplayName is the new display name; empty leaves it unchanged.
          type: string
        role:
          description: |-
            Role is the legacy role to move to — ADMIN, EDITOR or VIEWER; empty
            leaves it unchanged.
          type: string
      type: object
    o11y.O11yDeprecatedUsersOut:
      properties:
        data:
          description: Data holds the members.
          items:
            $ref: '#/components/schemas/o11y.O11yDeprecatedUser'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDiscoverIn:
      properties:
        aggregations:
          description: Aggregations are the measures to compute per group. Empty means
            a count.
          items:
            type: string
          type: array
        filters:
          description: |-
            Filters narrow the scan; each is a field, an operator (eq, neq, like) and
            a value, and they combine with AND.
          items:
            $ref: '#/components/schemas/o11y.O11yFilter'
          type: array
        groupBy:
          description: GroupBy are the columns to group the rows by.
          items:
            type: string
          type: array
        limit:
          description: Limit caps how many rows come back.
          type: integer
        orderBy:
          description: OrderBy is the column or aggregation to sort the rows on.
          type: string
        orderDir:
          description: OrderDir is asc or desc.
          type: string
        period:
          description: Period is the window to read, relative to now — 1h, 24h, 7d,
            14d, 30d.
          type: string
        project:
          description: Project is the project to read, as its id. Required.
          type: string
      required:
      - project
      type: object
    o11y.O11yDiscoverOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yTable'
          description: Data is the table.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDisk:
      properties:
        name:
          description: Name is the disk's name.
          type: string
        type:
          description: Type is the disk's type, e.g. local or s3.
          type: string
      type: object
    o11y.O11yDomainFilter:
      properties:
        expression:
          description: Expression is the predicate, e.g. `http.status_code >= 500`.
          type: string
      type: object
    o11y.O11yDomainGroupBy:
      properties:
        description:
          description: Description describes the field, when known.
          type: string
        fieldContext:
          description: |-
            FieldContext says which plane the field lives on, e.g. attribute,
            resource, span.
          type: string
        fieldDataType:
          description: 'FieldDataType is the field''s type: string, int64, float64
            or bool.'
          type: string
        name:
          description: Name is the field's name. Required.
          type: string
        signal:
          description: Signal is the telemetry signal the field belongs to, e.g. traces.
          type: string
        unit:
          description: Unit is the field's unit, when known.
          type: string
      type: object
    o11y.O11yDomainsAnswer:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDomainsData'
          description: |-
            Data holds the per-query results, emitted LAST — see the field-order note
            on the type.
        meta:
          $ref: '#/components/schemas/o11y.O11yQueryStats'
          description: Meta reports what the read cost.
        type:
          description: 'Type names the result shape: scalar, time_series or raw.'
          type: string
        warning:
          $ref: '#/components/schemas/o11y.O11yQueryWarning'
          description: Warning carries the store's warning for this read, when it
            raised one.
      type: object
    o11y.O11yDomainsData:
      properties:
        results:
          description: |-
            Results is one entry per query. Each entry's shape follows the answer's
            type — time-series data, scalar data or raw rows — so the bytes pass
            through verbatim.
          items: {}
          type: array
      type: object
    o11y.O11yDomainsIn:
      properties:
        domain:
          description: |-
            Domain narrows the read to one external domain (the domain view requires
            it).
          type: string
        end:
          description: End is the window's end, epoch milliseconds.
          type: integer
        endpoint:
          description: Endpoint narrows the domain view to one endpoint.
          type: string
        filter:
          $ref: '#/components/schemas/o11y.O11yDomainFilter'
          description: Filter is an additional predicate in the query-builder filter
            syntax.
        groupBy:
          description: GroupBy adds grouping columns to the result.
          items:
            $ref: '#/components/schemas/o11y.O11yDomainGroupBy'
          type: array
        show_ip:
          description: |-
            ShowIP keeps rows whose domain is a bare IP address; they are dropped
            otherwise.
          type: boolean
        start:
          description: Start is the window's start, epoch milliseconds.
          type: integer
      type: object
    o11y.O11yDomainsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yDomainsAnswer'
          description: Data holds the result.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDowntimeScheduleOut:
      properties:
        data:
          description: Data holds the schedule.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDowntimeSchedulesOut:
      properties:
        data:
          description: Data holds the schedules.
          items: {}
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yDowntimeUpdateIn:
      properties:
        alertIds:
          items:
            type: string
          type: array
        description:
          type: string
        name:
          type: string
        schedule: {}
        scope:
          type: string
      type: object
    o11y.O11yDraftFunnelIn:
      properties:
        end_time:
          description: EndTime is the end of the window, as a millisecond epoch.
          type: integer
        start_time:
          description: StartTime is the start of the window, as a millisecond epoch.
          type: integer
        step_end:
          description: StepEnd is the step the transition runs to, 1-based.
          type: integer
        step_start:
          description: |-
            StepStart is the step the transition runs from, 1-based. Ignored by the
            reads that span the whole funnel.
          type: integer
        steps:
          description: Steps are the funnel's steps, in order. At least two are needed.
          items:
            $ref: '#/components/schemas/o11y.FunnelStep'
          type: array
      type: object
    o11y.O11yEmailPasswordSessionIn:
      properties:
        email:
          description: Email is the account's address. Required.
          type: string
        orgId:
          description: OrgID picks the org to sign into when the address belongs to
            several.
          type: string
        password:
          description: Password is the account's password. Required.
          type: string
      type: object
    o11y.O11yErrorDetail:
      properties:
        code:
          description: Code is the machine-readable code.
          type: string
        errors:
          description: Errors are further details, one message and its suggestions
            each.
          items:
            $ref: '#/components/schemas/o11y.O11yErrorItem'
          type: array
        message:
          description: Message is the human-readable reason.
          type: string
        retry:
          $ref: '#/components/schemas/o11y.O11yRetry'
          description: Retry says when it is worth trying again, for errors that pass.
        suggestions:
          description: Suggestions say what to try instead.
          items:
            type: string
          type: array
        type:
          description: Type is the error's category, e.g. invalid_input, not_found.
          type: string
        url:
          description: Url points at documentation for the error, when there is any.
          type: string
      type: object
    o11y.O11yErrorGettableIssue:
      properties:
        issue:
          $ref: '#/components/schemas/o11y.O11yErrorIssue'
          description: Issue is the lifecycle row.
        latestEvent:
          $ref: '#/components/schemas/o11y.O11yOccurrence'
          description: LatestEvent is the most recent occurrence that landed on the
            issue.
      type: object
    o11y.O11yErrorGettableIssueOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yErrorGettableIssue'
          description: Data is the issue and its latest occurrence.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yErrorIssue:
      properties:
        assignee:
          description: Assignee is who the issue is assigned to.
          type: string
        count:
          description: Count is how many occurrences have landed on the issue.
          type: integer
        createdAt:
          description: CreatedAt is when the issue was first recorded.
          format: date-time
          type: string
        culprit:
          description: Culprit is where it came from — the function or route blamed
            for it.
          type: string
        environment:
          description: Environment is the deployment the issue was seen in.
          type: string
        fingerprint:
          description: Fingerprint is the grouping key that puts like errors in one
            issue.
          type: string
        firstSeen:
          description: FirstSeen is when the earliest occurrence was recorded.
          format: date-time
          type: string
        id:
          description: ID is the issue id.
          type: string
        lastSeen:
          description: LastSeen is when the latest was.
          format: date-time
          type: string
        level:
          description: Level is the issue's severity, e.g. error, warning, info.
          type: string
        platform:
          description: Platform is the reporting runtime, e.g. go, python, javascript.
          type: string
        regressed:
          description: Regressed marks an issue that reopened after being resolved.
          type: boolean
        release:
          description: Release is the version that produced it.
          type: string
        resolvedAt:
          description: ResolvedAt is when the issue was resolved, if it is.
          format: date-time
          type: string
        serviceName:
          description: ServiceName is the service that reported it.
          type: string
        status:
          description: 'Status is the lifecycle state: unresolved, resolved or ignored.'
          type: string
        type:
          description: Type is the exception type.
          type: string
        updatedAt:
          description: UpdatedAt is when the issue last changed.
          format: date-time
          type: string
        value:
          description: Value is the exception value.
          type: string
      type: object
    o11y.O11yErrorIssueOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yErrorIssue'
          description: Data is the issue.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yErrorIssues:
      properties:
        items:
          description: Items are the issues.
          items:
            $ref: '#/components/schemas/o11y.O11yErrorIssue'
          type: array
        limit:
          description: Limit is the page cap that was applied.
          type: integer
        offset:
          description: Offset is how many were skipped.
          type: integer
        total:
          description: Total is how many matched the filter.
          type: integer
      type: object
    o11y.O11yErrorIssuesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yErrorIssues'
          description: Data holds the issues.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yErrorItem:
      properties:
        message:
          description: Message is the detail.
          type: string
        suggestions:
          description: Suggestions say what to try about this detail.
          items:
            type: string
          type: array
      type: object
    o11y.O11yErrorUpdateIssueIn:
      properties:
        assignee:
          description: Assignee is who the issue is assigned to.
          type: string
        id:
          description: ID is the issue id.
          type: string
        status:
          description: 'Status is the new lifecycle state: unresolved, resolved or
            ignored.'
          type: string
      required:
      - id
      type: object
    o11y.O11yErrorWithSpan:
      properties:
        errorId:
          description: ErrorID is the exception instance id.
          type: string
        exceptionEscaped:
          description: ExceptionEscaped marks an exception that escaped its span uncaught.
          type: boolean
        exceptionMessage:
          description: ExceptionMsg is the exception's message.
          type: string
        exceptionStacktrace:
          description: ExceptionStacktrace is the captured stack trace.
          type: string
        exceptionType:
          description: ExceptionType is the exception's type.
          type: string
        groupID:
          description: GroupID is the exception group it belongs to.
          type: string
        serviceName:
          description: ServiceName is the service that reported it.
          type: string
        spanID:
          description: SpanID is the span it happened on.
          type: string
        timestamp:
          description: Timestamp is when it happened.
          format: date-time
          type: string
        traceID:
          description: TraceID is the trace the span belonged to.
          type: string
      type: object
    o11y.O11yErrorsCountIn:
      properties:
        end:
          description: End is the window end, as a nanosecond epoch spelled as a string.
          type: string
        exceptionType:
          description: ExceptionType narrows to one exception type.
          type: string
        serviceName:
          description: ServiceName narrows to one reporting service.
          type: string
        start:
          description: Start is the window start, as a nanosecond epoch spelled as
            a string.
          type: string
        tags:
          description: Tags narrow the scan to spans carrying the given tag values.
          items:
            $ref: '#/components/schemas/o11y.O11yTagQuery'
          type: array
      type: object
    o11y.O11yErrorsListIn:
      properties:
        end:
          description: End is the window end, as a nanosecond epoch spelled as a string.
          type: string
        exceptionType:
          description: ExceptionType narrows to one exception type.
          type: string
        limit:
          description: Limit caps how many exception groups come back. Required, non-zero.
          type: integer
        offset:
          description: Offset is how many groups to skip.
          type: integer
        order:
          description: 'Order is the direction: ascending or descending.'
          type: string
        orderParam:
          description: OrderParam is the column to order by, e.g. exceptionCount,
            lastSeen.
          type: string
        serviceName:
          description: ServiceName narrows to one reporting service.
          type: string
        start:
          description: Start is the window start, as a nanosecond epoch spelled as
            a string.
          type: string
        tags:
          description: Tags narrow the scan to spans carrying the given tag values.
          items:
            $ref: '#/components/schemas/o11y.O11yTagQuery'
          type: array
      type: object
    o11y.O11yEvent:
      properties:
        culprit:
          description: Culprit is where it came from — the function or route blamed
            for it.
          type: string
        environment:
          description: Environment is the deployment it happened in.
          type: string
        eventId:
          description: EventID is its own id.
          type: string
        fingerprint:
          description: Fingerprint is the grouping key that puts like errors in one
            issue.
          type: string
        frames:
          description: Frames are the stack, innermost first.
          items:
            $ref: '#/components/schemas/o11y.O11yFrame'
          type: array
        handled:
          description: Handled says whether the application caught it.
          type: boolean
        level:
          description: Level is its severity, e.g. error, warning, info.
          type: string
        message:
          description: Message is the human-readable message.
          type: string
        orgId:
          description: OrgID is the org that owns it.
          type: string
        platform:
          description: Platform is the reporting runtime, e.g. go, python, javascript.
          type: string
        projectId:
          description: ProjectID is the project it was captured for.
          type: string
        receivedAt:
          description: ReceivedAt is when it arrived here.
          format: date-time
          type: string
        release:
          description: Release is the version that produced it.
          type: string
        serverName:
          description: ServerName is the host that reported it.
          type: string
        serviceName:
          description: ServiceName is the service that reported it.
          type: string
        spanId:
          description: SpanID is the span it belonged to.
          type: string
        tags:
          additionalProperties:
            type: string
          description: Tags are the reporter's own key/value labels.
          type: object
        timestamp:
          description: Timestamp is when the error happened, as the reporter recorded
            it.
          format: date-time
          type: string
        traceId:
          description: TraceID is the trace it belonged to.
          type: string
        transaction:
          description: Transaction is the operation it happened in.
          type: string
        type:
          description: Type is the exception type.
          type: string
        userEmail:
          description: UserEmail is that user's email, when attached.
          type: string
        userId:
          description: UserID identifies the affected end user, when the reporter
            attached one.
          type: string
        userIp:
          description: UserIP is that user's address, when attached.
          type: string
        value:
          description: Value is the exception value.
          type: string
      type: object
    o11y.O11yEventIn:
      properties:
        attributes:
          additionalProperties:
            type: object
          description: Attributes are free-form event properties.
          type: object
        eventName:
          description: EventName names the event; required for track events.
          type: string
        eventType:
          description: EventType is the kind of event — track, identify or group.
            Required.
          type: string
        rateLimited:
          description: RateLimited marks an event the reporting client rate-limited.
          type: boolean
      required:
      - eventType
      type: object
    o11y.O11yEventUser:
      properties:
        email:
          description: Email is that user's email.
          type: string
        id:
          description: ID identifies the affected end user.
          type: string
        ipAddress:
          description: IP is that user's address.
          type: string
        username:
          description: Username is that user's name.
          type: string
      type: object
    o11y.O11yEvents:
      properties:
        items:
          description: Items are the events, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yEvent'
          type: array
      type: object
    o11y.O11yFeature:
      properties:
        defaultVariant:
          description: DefaultVariant is the variant used when nothing overrides it.
          type: string
        description:
          description: Description says what the flag gates.
          type: string
        kind:
          description: Kind is the flag's value kind, e.g. boolean.
          type: string
        name:
          description: Name is the flag's name.
          type: string
        resolvedValue:
          description: ResolvedValue is the value resolved for the caller's org.
          type: object
        stage:
          description: Stage is the flag's lifecycle stage, e.g. stable.
          type: string
        variants:
          additionalProperties:
            type: object
          description: Variants are the flag's possible values, by variant name.
          type: object
      type: object
    o11y.O11yFeaturesOut:
      properties:
        data:
          description: Data are the features.
          items:
            $ref: '#/components/schemas/o11y.O11yFeature'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFieldCatalogOut:
      properties:
        interesting:
          description: Interesting are fields seen in the data that could be selected.
          items:
            $ref: '#/components/schemas/o11y.O11yTelemetryField'
          type: array
        selected:
          description: Selected are the fields materialized as their own columns.
          items:
            $ref: '#/components/schemas/o11y.O11yTelemetryField'
          type: array
      type: object
    o11y.O11yFieldKeysOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFieldKeys'
          description: |-
            Data holds the field keys grouped by name, and whether the catalog is
            complete.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFieldSetting:
      properties:
        dataType:
          description: |-
            DataType is the field's data type, e.g. string, int64, float64, bool.
            Required.
          type: string
        index:
          description: |-
            Index is the index expression to put on the column, e.g. minmax,
            set(N), bloom_filter(P), tokenbf_v1(S,H,SEED). Empty keeps the default.
          type: string
        indexGranularity:
          description: IndexGranularity is the index granularity in rows.
          type: integer
        name:
          description: Name is the field to tune. Required.
          type: string
        selected:
          description: Selected materializes the field as its own column when true.
          type: boolean
        type:
          description: 'Type is where the field lives: attributes or resources. Required.'
          type: string
      required:
      - name
      - dataType
      - type
      type: object
    o11y.O11yFieldValuesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFieldValues'
          description: Data holds the values by data type, and whether the value list
            is complete.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFilter:
      properties:
        field:
          description: Field is the column to test.
          type: string
        op:
          description: 'Op is how to test it: eq, neq or like.'
          type: string
        value:
          description: Value is what to test it against.
          type: string
      type: object
    o11y.O11yFilterItem:
      properties:
        key:
          $ref: '#/components/schemas/o11y.O11yAttributeKey'
          description: Key is the attribute tested.
        op:
          description: Operator is the comparison, e.g. =, !=, in, contains.
          type: string
        value:
          description: Value is what it is tested against; its type follows the attribute's.
          type: object
      type: object
    o11y.O11yFilterKey:
      properties:
        dataType:
          description: DataType is the attribute's value type — string, int64, float64
            or bool.
          type: string
        isColumn:
          description: IsColumn marks an attribute stored as its own column.
          type: boolean
        isJSON:
          description: IsJSON marks an attribute read out of a JSON body.
          type: boolean
        key:
          description: Key is the attribute name.
          type: string
        type:
          description: Type says where the attribute lives — tag or resource.
          type: string
      type: object
    o11y.O11yFilterSet:
      properties:
        items:
          description: Items are the conditions.
          items:
            $ref: '#/components/schemas/o11y.O11yFilterItem'
          type: array
        op:
          description: Operator combines the items — AND or OR.
          type: string
      type: object
    o11y.O11yFilterSuggestions:
      properties:
        attributes:
          description: Attributes are the suggested attribute keys.
          items:
            $ref: '#/components/schemas/o11y.O11yAttributeKey'
          type: array
        example_queries:
          description: ExampleQueries are ready-to-run filter sets.
          items:
            $ref: '#/components/schemas/o11y.O11yFilterSet'
          type: array
      type: object
    o11y.O11yFilterSuggestionsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yFilterSuggestions'
          description: Data holds the suggestions.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yForgotPasswordIn:
      properties:
        email:
          description: Email is the address to mail the reset link to. Required.
          type: string
        frontendBaseURL:
          description: FrontendBaseURL is the console origin the reset link is built
            on.
          type: string
        orgId:
          description: OrgID is the org the address belongs to. Required.
          type: string
      type: object
    o11y.O11yFrame:
      properties:
        column:
          description: Column is the column number.
          type: integer
        file:
          description: File is the file it is in.
          type: string
        function:
          description: Function is the function the frame is in.
          type: string
        line:
          description: Line is the line number.
          type: integer
        own:
          description: |-
            Own marks a frame in the reporting application's own code rather than in a
            dependency or the runtime.
          type: boolean
      type: object
    o11y.O11yFunnelCreateIn:
      properties:
        funnel_name:
          description: Name is the funnel's name.
          type: string
        timestamp:
          description: |-
            Timestamp is when the funnel was created, as a millisecond epoch. Zero
            takes the runtime's own clock.
          type: integer
      type: object
    o11y.O11yFunnelDeleteOut:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFunnelOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFunnel'
          description: Data is the funnel with its steps.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFunnelRow:
      properties:
        data:
          additionalProperties:
            type: object
          description: Data are the row's columns, keyed by column name.
          type: object
        timestamp:
          description: Timestamp is the row's time.
          type: string
      type: object
    o11y.O11yFunnelRowsOut:
      properties:
        data:
          description: Data are the rows.
          items:
            $ref: '#/components/schemas/o11y.O11yFunnelRow'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yFunnelStepWindowIn:
      properties:
        end_time:
          type: integer
        start_time:
          type: integer
        step_end:
          type: integer
        step_start:
          type: integer
      type: object
    o11y.O11yFunnelStepsUpdateIn:
      properties:
        description:
          description: Description replaces the funnel's description. Empty leaves
            it as it was.
          type: string
        funnel_id:
          description: FunnelID is the funnel to update.
          type: string
        funnel_name:
          description: Name replaces the funnel's name. Empty leaves it as it was.
          type: string
        steps:
          description: |-
            Steps are the funnel's steps, in order. At least two are needed before
            any analytics read will answer.
          items:
            $ref: '#/components/schemas/o11y.FunnelStep'
          type: array
        timestamp:
          description: Timestamp is when the change was made, as a millisecond epoch.
          type: integer
      type: object
    o11y.O11yFunnelUpdateIn:
      properties:
        description:
          description: Description replaces the funnel's description. Empty leaves
            it as it was.
          type: string
        funnel_name:
          description: Name replaces the funnel's name. Empty leaves it as it was.
          type: string
        timestamp:
          description: Timestamp is when the change was made, as a millisecond epoch.
          type: integer
      type: object
    o11y.O11yFunnelWindowIn:
      properties:
        end_time:
          type: integer
        start_time:
          type: integer
      type: object
    o11y.O11yFunnelsOut:
      properties:
        data:
          description: Data are the funnels.
          items:
            $ref: '#/components/schemas/o11y.GettableFunnel'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yGettableHostOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableHost'
          description: Data holds the host info.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yGlobalConfig:
      properties:
        ai_assistant_url:
          description: AIAssistantURL is the AI assistant endpoint, when one is exposed.
          type: string
        external_url:
          description: ExternalURL is the deployment's public URL.
          type: string
        identN:
          $ref: '#/components/schemas/o11y.O11yIdentN'
          description: IdentN says which identity providers are enabled.
        ingestion_url:
          description: IngestionURL is where telemetry is sent.
          type: string
        mcp_url:
          description: MCPURL is the MCP endpoint, when one is exposed.
          type: string
      type: object
    o11y.O11yGlobalConfigOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yGlobalConfig'
          description: Data is the configuration.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yGoogleConfig:
      properties:
        allowedGroups:
          description: AllowedGroups, when set, admits only members of these groups.
          items:
            type: string
          type: array
        clientId:
          description: ClientID is the OAuth application's id.
          type: string
        clientSecret:
          description: ClientSecret is the OAuth application's secret.
          type: string
        domainToAdminEmail:
          additionalProperties:
            type: string
          description: |-
            DomainToAdminEmail maps each Workspace domain to the admin the service
            account impersonates; "*" is the fallback.
          type: object
        fetchGroups:
          description: FetchGroups reads the user's Workspace groups for role mapping.
          type: boolean
        fetchTransitiveGroupMembership:
          description: |-
            FetchTransitiveGroupMembership also reads groups held through other
            groups.
          type: boolean
        insecureSkipEmailVerified:
          description: InsecureSkipEmailVerified admits addresses Google has not verified.
          type: boolean
        redirectURI:
          description: RedirectURI is the callback the flow returns to.
          type: string
        serviceAccountJson:
          description: |-
            ServiceAccountJSON is the service-account credential used to read
            groups, when FetchGroups is on.
          type: string
      type: object
    o11y.O11yHealthOut:
      properties:
        status:
          description: Status is "ok".
          type: string
      type: object
    o11y.O11yHostListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.HostListResponse'
          description: Data holds the host records and the fleet's reporting state.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yIdentN:
      properties:
        apikey:
          $ref: '#/components/schemas/o11y.O11yToggle'
          description: APIKey is the API-key provider.
        impersonation:
          $ref: '#/components/schemas/o11y.O11yToggle'
          description: Impersonation is the impersonation provider.
        tokenizer:
          $ref: '#/components/schemas/o11y.O11yToggle'
          description: Tokenizer is the token-based provider.
      type: object
    o11y.O11yIdentifiable:
      properties:
        id:
          description: ID is the created resource's id.
          type: string
      type: object
    o11y.O11yIdentifiableOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yIdentifiable'
          description: Data is the created resource's id.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraAttributeKeysOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.FilterAttributeKeyResponse'
          description: Data holds the keys.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraAttributeValuesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.FilterAttributeValueResponse'
          description: Data holds the values, split by data type.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraChecksOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Checks'
          description: |-
            Data holds what is present, what is missing and whether the section is
            ready.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraClustersOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Clusters'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraDaemonSetsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.DaemonSets'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraDeploymentsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Deployments'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraHostsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Hosts'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraJobsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Jobs'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraNamespacesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Namespaces'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraNodesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Nodes'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraPodsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Pods'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraStatefulSetsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.StatefulSets'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInfraVolumesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Volumes'
          description: Data holds the rows.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yIngestionKeysOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableIngestionKeys'
          description: Data holds the ingestion keys.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInstallOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.IntegrationsListItem'
          description: Data holds the installed integration.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yIntegrationAck:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yIntegrationOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Integration'
          description: Data holds the integration and its installation record.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yIntegrationsListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.IntegrationsListResponse'
          description: Data holds the available integrations and their installed state.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yInvite:
      properties:
        createdAt:
          description: CreatedAt is when it was created.
          format: date-time
          type: string
        email:
          description: Email is the address it was sent to.
          type: string
        id:
          description: ID is the invitation's id.
          type: string
        inviteLink:
          description: InviteLink is the full link mailed to them.
          type: string
        name:
          description: Name is the invitee's display name.
          type: string
        orgId:
          description: OrgID is the org they are invited into.
          type: string
        role:
          description: Role is the role the invitee will hold.
          type: string
        token:
          description: Token is the secret that redeems it.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yInviteIn:
      properties:
        email:
          description: Email is the address the invitation goes to.
          type: string
        frontendBaseUrl:
          description: FrontendBaseUrl is the console origin the invite link is built
            on.
          type: string
        name:
          description: Name is the invitee's display name.
          type: string
        role:
          description: Role is the role they will hold on accepting — ADMIN, EDITOR
            or VIEWER.
          type: string
      type: object
    o11y.O11yInviteOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yInvite'
          description: Data is the invitation.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yJobListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.JobListResponse'
          description: Data holds the job records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yK8sOnboarding:
      properties:
        didSendClusterMetrics:
          description: DidSendClusterMetrics says whether cluster metrics have arrived.
          type: boolean
        didSendNodeMetrics:
          description: DidSendNodeMetrics says whether node metrics have arrived.
          type: boolean
        didSendPodMetrics:
          description: DidSendPodMetrics says whether pod metrics have arrived.
          type: boolean
        isSendingOptionalPodMetrics:
          description: |-
            IsSendingOptionalPodMetrics says whether optional pod metrics are
            flowing.
          type: boolean
        isSendingRequiredMetadata:
          description: |-
            IsSendingRequiredMetadata reports, per pod, which required metadata
            labels are present.
          items:
            $ref: '#/components/schemas/o11y.O11yPodOnboarding'
          type: array
      type: object
    o11y.O11yLLMAnnotation:
      properties:
        author:
          description: Author is who wrote it.
          type: string
        content:
          description: Content is the note itself.
          type: string
        createdAt:
          description: CreatedAt is when the annotation was stored.
          format: date-time
          type: string
        id:
          description: ID is the annotation's id.
          type: string
        observationId:
          description: ObservationID is the observation the annotation attaches to,
            when narrowed.
          type: string
        queue:
          description: Queue is the review queue the annotation sits in, when queued.
          type: string
        status:
          description: Status is the annotation's review status, e.g. PENDING.
          type: string
        traceId:
          description: TraceID is the trace the annotation attaches to.
          type: string
        updatedAt:
          description: UpdatedAt is when the annotation last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yLLMAnnotationOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMAnnotation'
          description: Data is the annotation.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMAnnotationsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMAnnotationsPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMAnnotationsPage:
      properties:
        items:
          description: Items are the annotations, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMAnnotation'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
        total:
          description: Total is how many annotations match, across all pages.
          type: integer
      type: object
    o11y.O11yLLMIngestAnnotation:
      properties:
        content:
          description: Content is the note itself. Required.
          type: string
        observationId:
          description: |-
            ObservationID is the single observation the annotation attaches to, when
            narrowed to one.
          type: string
        queue:
          description: Queue is the review queue to file the annotation in.
          type: string
        status:
          description: Status is the annotation's initial review status. Defaults
            to PENDING.
          type: string
        traceId:
          description: TraceID is the trace the annotation attaches to. Required.
          type: string
      type: object
    o11y.O11yLLMIngestScore:
      properties:
        comment:
          description: Comment is a free-text note.
          type: string
        dataType:
          description: |-
            DataType is the score's kind — NUMERIC or CATEGORICAL. Defaults from the
            value when empty.
          type: string
        name:
          description: Name is the score's name, e.g. helpfulness. Required.
          type: string
        observationId:
          description: |-
            ObservationID is the single observation the score attaches to, when
            narrowed to one.
          type: string
        source:
          description: Source is where the score came from, e.g. API, EVAL. Defaults
            to API.
          type: string
        stringValue:
          description: StringValue is the categorical score, when the score is categorical.
          type: string
        traceId:
          description: TraceID is the trace the score attaches to. Required.
          type: string
        value:
          description: Value is the numeric score.
          type: number
      type: object
    o11y.O11yLLMObservation:
      properties:
        completionTokens:
          description: CompletionTokens is the output token count.
          type: integer
        id:
          description: ID is the observation's id (the span id).
          type: string
        latencyMs:
          description: LatencyMs is how long it took, in milliseconds.
          type: number
        model:
          description: Model is the model that served it.
          type: string
        name:
          description: Name is the observation's name.
          type: string
        parentObservationId:
          description: ParentID is the parent observation, when the span has one.
          type: string
        promptTokens:
          description: PromptTokens is the input token count.
          type: integer
        provider:
          description: Provider is the model's provider.
          type: string
        serviceName:
          description: ServiceName is the app that emitted it.
          type: string
        sessionId:
          description: SessionID is the conversation the observation belongs to.
          type: string
        startTime:
          description: StartTime is when the observation started.
          format: date-time
          type: string
        statusCode:
          description: StatusCode is the observation's status, e.g. OK, ERROR.
          type: string
        totalCost:
          description: TotalCost is the observation's cost.
          type: number
        totalTokens:
          description: TotalTokens is the sum of prompt and completion tokens.
          type: integer
        traceId:
          description: TraceID is the trace the observation belongs to.
          type: string
        type:
          description: Type is the observation kind, e.g. chat, embeddings, tool.
          type: string
        userId:
          description: UserID is the end user the observation is attributed to.
          type: string
      type: object
    o11y.O11yLLMObservationsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMObservationsPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMObservationsPage:
      properties:
        items:
          description: Items are the observations, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMObservation'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
      type: object
    o11y.O11yLLMPricingCacheCosts:
      properties:
        mode:
          description: |-
            Mode is how cached tokens are counted — subtract (inside input_tokens,
            OpenAI-style), additive (reported separately, Anthropic-style) or unknown.
          type: string
        read:
          description: Read is the cost per unit of cache-read tokens.
          type: number
        write:
          description: Write is the cost per unit of cache-write tokens.
          type: number
      type: object
    o11y.O11yLLMPricingRule:
      properties:
        createdAt:
          description: CreatedAt is when the rule was stored.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created the rule.
          type: string
        enabled:
          description: Enabled says whether the rule is on.
          type: boolean
        id:
          description: ID is the rule's id.
          type: string
        isOverride:
          description: IsOverride marks the rule user-pinned; when true the sync job
            skips it.
          type: boolean
        modelName:
          description: Model is the model the rule prices.
          type: string
        modelPattern:
          description: ModelPattern are the model-name globs the rule matches.
          items:
            type: string
          type: array
        orgId:
          description: OrgID is the org the rule belongs to.
          type: string
        pricing:
          $ref: '#/components/schemas/o11y.O11yLLMRulePricing'
          description: Pricing is the per-unit cost.
        provider:
          description: Provider is the model's provider.
          type: string
        sourceId:
          description: SourceID is the upstream source the rule was synced from, when
            synced.
          type: string
        syncedAt:
          description: SyncedAt is when the rule was last synced, when it is synced.
          format: date-time
          type: string
        unit:
          description: Unit is the pricing unit, e.g. per_million_tokens.
          type: string
        updatedAt:
          description: UpdatedAt is when the rule last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is who last changed it.
          type: string
      type: object
    o11y.O11yLLMPricingRuleOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMPricingRule'
          description: Data is the rule.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMPricingRulesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMPricingRulesPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMPricingRulesPage:
      properties:
        items:
          description: Items are the rules.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMPricingRule'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
        total:
          description: Total is how many rules match, across all pages.
          type: integer
      type: object
    o11y.O11yLLMRulePricing:
      properties:
        cache:
          $ref: '#/components/schemas/o11y.O11yLLMPricingCacheCosts'
          description: Cache is the cost of cached tokens, when the model prices them.
        input:
          description: Input is the cost per unit of input tokens.
          type: number
        output:
          description: Output is the cost per unit of output tokens.
          type: number
      type: object
    o11y.O11yLLMScore:
      properties:
        comment:
          description: Comment is a free-text note.
          type: string
        createdAt:
          description: CreatedAt is when the score was stored.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created the score.
          type: string
        dataType:
          description: DataType is the score's kind — NUMERIC or CATEGORICAL.
          type: string
        id:
          description: ID is the score's id.
          type: string
        name:
          description: Name is the score's name.
          type: string
        observationId:
          description: ObservationID is the observation the score attaches to, when
            narrowed.
          type: string
        source:
          description: Source is where the score came from, e.g. API, EVAL.
          type: string
        stringValue:
          description: StringValue is the categorical score, when the score is categorical.
          type: string
        timestamp:
          description: Timestamp is the score's own event time.
          format: date-time
          type: string
        traceId:
          description: TraceID is the trace the score attaches to.
          type: string
        updatedAt:
          description: UpdatedAt is when the score last changed.
          format: date-time
          type: string
        value:
          description: Value is the numeric score.
          type: number
      type: object
    o11y.O11yLLMScoreOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMScore'
          description: Data is the score.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMScoresOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMScoresPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMScoresPage:
      properties:
        items:
          description: Items are the scores, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMScore'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
        total:
          description: Total is how many scores match, across all pages.
          type: integer
      type: object
    o11y.O11yLLMSession:
      properties:
        completionTokens:
          description: CompletionTokens is the conversation's total output tokens.
          type: integer
        id:
          description: ID is the session id.
          type: string
        observations:
          description: Observations is how many observations the conversation holds.
          type: integer
        promptTokens:
          description: PromptTokens is the conversation's total input tokens.
          type: integer
        totalCost:
          description: TotalCost is the conversation's total cost.
          type: number
        totalTokens:
          description: TotalTokens is the conversation's total tokens.
          type: integer
        traces:
          description: Traces is how many traces the conversation holds.
          type: integer
        userId:
          description: UserID is the end user the conversation is attributed to.
          type: string
      type: object
    o11y.O11yLLMSessionsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMSessionsPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMSessionsPage:
      properties:
        items:
          description: Items are the conversations, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMSession'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
      type: object
    o11y.O11yLLMTrace:
      properties:
        completionTokens:
          description: CompletionTokens is the trace's total output tokens.
          type: integer
        id:
          description: ID is the trace id.
          type: string
        latencyMs:
          description: LatencyMs is the trace's span, in milliseconds.
          type: number
        observations:
          description: Observations is how many observations the trace holds.
          type: integer
        promptTokens:
          description: PromptTokens is the trace's total input tokens.
          type: integer
        serviceName:
          description: ServiceName is the app that emitted it.
          type: string
        sessionId:
          description: SessionID is the conversation the trace belongs to.
          type: string
        totalCost:
          description: TotalCost is the trace's total cost.
          type: number
        totalTokens:
          description: TotalTokens is the trace's total tokens.
          type: integer
        userId:
          description: UserID is the end user the trace is attributed to.
          type: string
      type: object
    o11y.O11yLLMTracesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMTracesPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMTracesPage:
      properties:
        items:
          description: Items are the traces, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMTrace'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
      type: object
    o11y.O11yLLMUpdatablePricingRule:
      properties:
        enabled:
          description: Enabled turns the rule on.
          type: boolean
        id:
          description: ID matches an existing rule by its id.
          type: string
        isOverride:
          description: |-
            IsOverride pins the rule so the sync job skips it. Omit to leave a matched
            override untouched.
          type: boolean
        modelName:
          description: Model is the model the rule prices. Required.
          type: string
        modelPattern:
          description: ModelPattern are the model-name globs the rule matches. Required.
          items:
            type: string
          type: array
        pricing:
          $ref: '#/components/schemas/o11y.O11yLLMRulePricing'
          description: Pricing is the per-unit cost. Required.
        provider:
          description: Provider is the model's provider. Required.
          type: string
        sourceId:
          description: SourceID matches an existing rule by its upstream source id.
          type: string
        unit:
          description: Unit is the pricing unit, e.g. per_million_tokens. Required.
          type: string
      type: object
    o11y.O11yLLMUpdatablePricingRules:
      properties:
        rules:
          description: Rules are the rules to create or update, matched per rule.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMUpdatablePricingRule'
          type: array
      type: object
    o11y.O11yLLMUser:
      properties:
        completionTokens:
          description: CompletionTokens is their total output tokens.
          type: integer
        id:
          description: ID is the end user's id (user.id).
          type: string
        observations:
          description: Observations is how many observations they produced.
          type: integer
        promptTokens:
          description: PromptTokens is their total input tokens.
          type: integer
        sessions:
          description: Sessions is how many conversations they had.
          type: integer
        totalCost:
          description: TotalCost is their total cost.
          type: number
        totalTokens:
          description: TotalTokens is their total tokens.
          type: integer
        traces:
          description: Traces is how many traces they produced.
          type: integer
      type: object
    o11y.O11yLLMUsersOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLLMUsersPage'
          description: Data is the page.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLLMUsersPage:
      properties:
        items:
          description: Items are the end users, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLLMUser'
          type: array
        limit:
          description: Limit is the page cap the read ran with.
          type: integer
        offset:
          description: Offset is the row offset this page started at.
          type: integer
      type: object
    o11y.O11yLicenseActiveOut:
      properties:
        data:
          description: Data is the license.
          type: object
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLicensesOut:
      properties:
        data:
          description: Data are the licenses.
          items:
            type: object
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yListError:
      properties:
        exceptionCount:
          description: ExceptionCount is how many instances the group holds in the
            window.
          type: integer
        exceptionMessage:
          description: ExceptionMsg is its message.
          type: string
        exceptionType:
          description: ExceptionType is the exception's type.
          type: string
        firstSeen:
          description: FirstSeen is when the earliest was.
          format: date-time
          type: string
        groupID:
          description: GroupID is the group's id.
          type: string
        lastSeen:
          description: LastSeen is when the latest instance was recorded.
          format: date-time
          type: string
        serviceName:
          description: ServiceName is the service that reported them.
          type: string
      type: object
    o11y.O11yLogAggregateBucket:
      properties:
        groupBy:
          additionalProperties: {}
          description: GroupBy carries the group's key values when the aggregate grouped.
          type: object
        timestamp:
          description: Timestamp is the start of the bucket.
          type: integer
        value:
          description: Value is the bucket's aggregated value.
      type: object
    o11y.O11yLogAggregateOut:
      properties:
        items:
          additionalProperties:
            $ref: '#/components/schemas/o11y.O11yLogAggregateBucket'
          description: Items are the buckets, keyed by bucket timestamp.
          type: object
      type: object
    o11y.O11yLogConfigVersion:
      properties:
        config:
          description: Config is the rendered collector config the version deployed.
          type: string
        createdAt:
          description: CreatedAt is when the version was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is the id of who created the version.
          type: string
        createdByName:
          description: CreatedByName is the display name of who created the version.
          type: string
        deployResult:
          description: DeployResult is the deployment's outcome message.
          type: string
        deploySequence:
          description: DeploySequence orders this deployment among the version's deployments.
          type: integer
        deployStatus:
          description: |-
            DeployStatus is where the deployment stands, e.g. dirty, deploying,
            deployed, in_progress, failed, unknown.
          type: string
        elementType:
          description: ElementType is the config element the version carries — log_pipelines.
          type: string
        id:
          description: ID is the version record's id.
          type: string
        lastHash:
          description: LastHash is the deployed config's hash.
          type: string
        orgId:
          description: OrgID is the org the version belongs to.
          type: string
        updatedAt:
          description: UpdatedAt is when the version last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is the id of who last changed it.
          type: string
        version:
          description: Version is the config version number.
          type: integer
      type: object
    o11y.O11yLogFilter:
      properties:
        items:
          description: Items are the predicates.
          items:
            $ref: '#/components/schemas/o11y.O11yLogFilterItem'
          type: array
        op:
          description: 'Op combines the items: AND or OR.'
          type: string
      type: object
    o11y.O11yLogFilterItem:
      properties:
        key:
          $ref: '#/components/schemas/o11y.O11yLogFilterKey'
          description: Key is the field the predicate tests.
        op:
          description: Op is the comparison, e.g. =, !=, in, contains.
          type: string
        value:
          description: Value is what it tests against, in the value's own JSON type.
      type: object
    o11y.O11yLogFilterKey:
      properties:
        dataType:
          description: DataType is the field's data type, e.g. string, int64, float64,
            bool.
          type: string
        isColumn:
          description: IsColumn marks a field materialized as its own column.
          type: boolean
        isJSON:
          description: IsJSON marks a path into the record's JSON body.
          type: boolean
        key:
          description: Key is the field's name.
          type: string
        type:
          description: 'Type is where the field lives: tag or resource.'
          type: string
      type: object
    o11y.O11yLogParseFrom:
      properties:
        parse_from:
          description: ParseFrom is the field to read.
          type: string
      type: object
    o11y.O11yLogPipeline:
      properties:
        alias:
          description: Alias is the pipeline's short name.
          type: string
        config:
          description: Config is the pipeline's processors, in order.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPipelineOperator'
          type: array
        createdAt:
          description: CreatedAt is when the pipeline was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is the id of who created the pipeline.
          type: string
        description:
          description: Description says what the pipeline is for.
          type: string
        enabled:
          description: Enabled says whether the pipeline is on.
          type: boolean
        filter:
          $ref: '#/components/schemas/o11y.O11yLogFilter'
          description: Filter selects which records the pipeline processes.
        id:
          description: ID is the pipeline's id.
          type: string
        name:
          description: Name is the pipeline's display name.
          type: string
        orderId:
          description: OrderID is the pipeline's 1-based position in the set.
          type: integer
        updatedAt:
          description: UpdatedAt is when the pipeline last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is the id of who last changed it.
          type: string
      type: object
    o11y.O11yLogPipelineCreateIn:
      properties:
        pipelines:
          description: Pipelines are the pipelines the new version holds, in order.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPostablePipeline'
          type: array
      type: object
    o11y.O11yLogPipelineOperator:
      properties:
        default:
          description: Default is the id of the processor a router falls through to.
          type: string
        enable_flattening:
          description: EnableFlattening flattens parsed JSON one level when true.
          type: boolean
        enable_paths:
          description: EnablePaths keeps the JSON path in flattened keys when true.
          type: boolean
        enabled:
          description: Enabled turns the processor on.
          type: boolean
        expr:
          description: Expr is a router route's expression.
          type: string
        field:
          description: Field is the field an add/remove processor works on.
          type: string
        fields:
          description: Fields are the fields a retain processor keeps.
          items:
            type: string
          type: array
        from:
          description: From is the source field of a move or copy.
          type: string
        id:
          description: ID is the processor's id, unique within the pipeline.
          type: string
        if:
          description: If gates the processor on an expression.
          type: string
        layout:
          description: Layout is a time parser's layout.
          type: string
        layout_type:
          description: LayoutType is the layout's kind, e.g. strptime, gotime, epoch.
          type: string
        mapping:
          additionalProperties:
            items:
              type: string
            type: array
          description: |-
            Mapping maps severity levels (or flattened keys) to the values that
            mean them.
          type: object
        name:
          description: Name is the processor's display name.
          type: string
        on_error:
          description: OnError says what to do when the processor fails, e.g. send,
            drop.
          type: string
        orderId:
          description: OrderID is the processor's 1-based position in the pipeline.
          type: integer
        output:
          description: Output is the id of the processor that runs next.
          type: string
        overwrite_text:
          description: |-
            OverwriteSeverityText rewrites the severity text alongside the number
            when true.
          type: boolean
        parse_from:
          description: ParseFrom is where a parser reads from.
          type: string
        parse_to:
          description: ParseTo is where a parser writes its result.
          type: string
        path_prefix:
          description: PathPrefix prefixes flattened keys.
          type: string
        pattern:
          description: Pattern is a grok parser's pattern.
          type: string
        regex:
          description: Regex is a regex parser's expression.
          type: string
        routes:
          description: Routes are a router processor's routes.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPipelineRoute'
          type: array
        span_id:
          $ref: '#/components/schemas/o11y.O11yLogParseFrom'
          description: SpanID says where a trace parser reads the span id from.
        to:
          description: To is the destination field of a move or copy.
          type: string
        trace_flags:
          $ref: '#/components/schemas/o11y.O11yLogParseFrom'
          description: TraceFlags says where a trace parser reads the trace flags
            from.
        trace_id:
          $ref: '#/components/schemas/o11y.O11yLogParseFrom'
          description: TraceID says where a trace parser reads the trace id from.
        type:
          description: |-
            Type is the processor type, e.g. grok_parser, regex_parser, json_parser,
            trace_parser, time_parser, severity_parser, add, remove, move, copy.
          type: string
        value:
          description: Value is the value an add processor writes.
          type: string
      type: object
    o11y.O11yLogPipelinePreview:
      properties:
        collectorLogs:
          description: CollectorLogs is what the collector logged while simulating.
          items:
            type: string
          type: array
        logs:
          description: Logs are the sample records after the pipelines ran over them.
          items:
            $ref: '#/components/schemas/o11y.O11yLogRecord'
          type: array
      type: object
    o11y.O11yLogPipelinePreviewIn:
      properties:
        logs:
          description: Logs are the sample records to transform.
          items:
            $ref: '#/components/schemas/o11y.O11yLogRecord'
          type: array
        pipelines:
          description: Pipelines are the pipelines to simulate, in order.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPipeline'
          type: array
      type: object
    o11y.O11yLogPipelinePreviewOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLogPipelinePreview'
          description: Data is the dry run's result.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLogPipelineRoute:
      properties:
        expr:
          description: Expr is the expression that selects the route.
          type: string
        output:
          description: Output is the id of the processor the route sends to.
          type: string
      type: object
    o11y.O11yLogPipelines:
      properties:
        config:
          description: Config is the rendered collector config the version deployed.
          type: string
        createdAt:
          description: CreatedAt is when the version was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is the id of who created the version.
          type: string
        createdByName:
          description: CreatedByName is the display name of who created the version.
          type: string
        deployResult:
          description: DeployResult is the deployment's outcome message.
          type: string
        deploySequence:
          description: DeploySequence orders this deployment among the version's deployments.
          type: integer
        deployStatus:
          description: |-
            DeployStatus is where the deployment stands, e.g. dirty, deploying,
            deployed, in_progress, failed, unknown.
          type: string
        elementType:
          description: ElementType is the config element this version carries — log_pipelines.
          type: string
        history:
          description: History is the recent version history, newest first.
          items:
            $ref: '#/components/schemas/o11y.O11yLogConfigVersion'
          type: array
        id:
          description: ID is the version record's id.
          type: string
        lastHash:
          description: LastHash is the deployed config's hash.
          type: string
        orgId:
          description: OrgID is the org the version belongs to.
          type: string
        pipelines:
          description: Pipelines are the version's pipelines, in order.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPipeline'
          type: array
        updatedAt:
          description: UpdatedAt is when the version last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is the id of who last changed it.
          type: string
        version:
          description: Version is the config version number.
          type: integer
      type: object
    o11y.O11yLogPipelinesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yLogPipelines'
          description: Data is the config version.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLogPostablePipeline:
      properties:
        alias:
          description: Alias is the pipeline's short name.
          type: string
        config:
          description: Config is the pipeline's processors, in order.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPipelineOperator'
          type: array
        description:
          description: Description says what the pipeline is for.
          type: string
        enabled:
          description: Enabled turns the pipeline on.
          type: boolean
        filter:
          $ref: '#/components/schemas/o11y.O11yLogFilter'
          description: Filter selects which records the pipeline processes.
        id:
          description: |-
            ID is the pipeline's id. Empty on a new pipeline; the id it was listed
            with to keep an existing one.
          type: string
        name:
          description: Name is the pipeline's display name.
          type: string
        orderId:
          description: OrderID is the pipeline's 1-based position in the set.
          type: integer
      type: object
    o11y.O11yLogPromoteIndex:
      properties:
        fieldDataType:
          description: FieldDataType is the path's data type, e.g. string, number,
            bool.
          type: string
        granularity:
          description: Granularity is the index granularity in rows.
          type: integer
        type:
          description: Type is the index type, e.g. minmax, set(N), bloom_filter(P).
          type: string
      type: object
    o11y.O11yLogPromoteOut:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLogPromotePath:
      properties:
        indexes:
          description: Indexes are the indexes to put on the path.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPromoteIndex'
          type: array
        path:
          description: |-
            Path is the body path, e.g. body.user.id on the way in; listed without
            the body. prefix.
          type: string
        promote:
          description: Promote lifts the path into its own column when true.
          type: boolean
      type: object
    o11y.O11yLogPromotedOut:
      properties:
        data:
          description: Data holds the paths.
          items:
            $ref: '#/components/schemas/o11y.O11yLogPromotePath'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yLogRecord:
      properties:
        attributes_bool:
          additionalProperties:
            type: boolean
          description: AttributesBool are the record's boolean attributes.
          type: object
        attributes_float:
          additionalProperties:
            type: number
          description: AttributesFloat are the record's float attributes.
          type: object
        attributes_int:
          additionalProperties:
            type: integer
          description: AttributesInt are the record's integer attributes.
          type: object
        attributes_string:
          additionalProperties:
            type: string
          description: AttributesString are the record's string attributes.
          type: object
        body:
          description: Body is the record's body.
          type: string
        id:
          description: ID is the record's id.
          type: string
        resources_string:
          additionalProperties:
            type: string
          description: ResourcesString are the record's string resource attributes.
          type: object
        severity_number:
          description: SeverityNumber is the record's severity as a number.
          type: integer
        severity_text:
          description: SeverityText is the record's severity as text, e.g. ERROR.
          type: string
        span_id:
          description: SpanID is the span the record belongs to.
          type: string
        timestamp:
          description: Timestamp is the record's time as a nanosecond epoch.
          type: integer
        trace_flags:
          description: TraceFlags are the record's trace flags.
          type: integer
        trace_id:
          description: TraceID is the trace the record belongs to.
          type: string
      type: object
    o11y.O11yLogRecordsOut:
      properties:
        results:
          description: Results are the records, newest first.
          items:
            additionalProperties: {}
            type: object
          type: array
      type: object
    o11y.O11yLogsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yEvents'
          description: Data holds the events.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMessage:
      properties:
        data:
          description: Data is the acknowledgment message.
          type: string
      type: object
    o11y.O11yMetricAckOut:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricAlert:
      properties:
        alertId:
          description: AlertID is the alert rule's id.
          type: string
        alertName:
          description: AlertName is the alert rule's name.
          type: string
      type: object
    o11y.O11yMetricAlerts:
      properties:
        alerts:
          description: Alerts are the alert rules referencing the metric.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricAlert'
          type: array
      type: object
    o11y.O11yMetricAlertsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricAlerts'
          description: Data holds the alerts.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricAttribute:
      properties:
        key:
          description: Key is the attribute's name.
          type: string
        valueCount:
          description: ValueCount is how many distinct values the attribute has.
          type: integer
        values:
          description: Values are the attribute's distinct values.
          items:
            type: string
          type: array
      type: object
    o11y.O11yMetricAttributes:
      properties:
        attributes:
          description: Attributes are the keys, each with its values.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricAttribute'
          type: array
        totalKeys:
          description: TotalKeys is how many keys the metric has.
          type: integer
      type: object
    o11y.O11yMetricAttributesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricAttributes'
          description: Data holds the attributes.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricDashboards:
      properties:
        dashboards:
          description: Dashboards are the panels referencing the metric.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricPanel'
          type: array
      type: object
    o11y.O11yMetricDashboardsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricDashboards'
          description: Data holds the panels.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricField:
      properties:
        description:
          description: Description describes the field.
          type: string
        fieldContext:
          description: FieldContext is the context the field lives in, e.g. resource,
            attribute.
          type: string
        fieldDataType:
          description: FieldDataType is the field's data type.
          type: string
        name:
          description: Name is the field's name.
          type: string
        signal:
          description: Signal is the telemetry signal the field belongs to.
          type: string
        unit:
          description: Unit is the field's unit.
          type: string
      type: object
    o11y.O11yMetricFilter:
      properties:
        expression:
          description: Expression is the filter, in the query-builder filter syntax.
          type: string
      type: object
    o11y.O11yMetricHighlights:
      properties:
        activeTimeSeries:
          description: ActiveTimeSeries is how many of them are active.
          type: integer
        dataPoints:
          description: DataPoints is how many data points the metric has.
          type: integer
        lastReceived:
          description: |-
            LastReceived is when the metric last arrived, as a Unix timestamp in
            milliseconds.
          type: integer
        totalTimeSeries:
          description: TotalTimeSeries is how many time series the metric has ever
            had.
          type: integer
      type: object
    o11y.O11yMetricHighlightsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricHighlights'
          description: Data holds the highlights.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricInspectIn:
      properties:
        end:
          description: |-
            End is the end of the window as a Unix timestamp in milliseconds, at most
            thirty minutes after start. Required.
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.O11yMetricFilter'
          description: Filter narrows the series returned.
        metricName:
          description: MetricName is the metric to inspect. Required.
          type: string
        start:
          description: Start is the start of the window as a Unix timestamp in milliseconds.
            Required.
          type: integer
      required:
      - metricName
      - start
      - end
      type: object
    o11y.O11yMetricInspectOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricSeriesSet'
          description: Data holds the series.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricLabel:
      properties:
        key:
          $ref: '#/components/schemas/o11y.O11yMetricField'
          description: Key is the label's field.
        value:
          description: Value is the label's value.
          type: object
      type: object
    o11y.O11yMetricList:
      properties:
        metrics:
          description: Metrics are the metrics, with their metadata.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSummary'
          type: array
      type: object
    o11y.O11yMetricListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricList'
          description: Data holds the metrics.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricMetadata:
      properties:
        description:
          description: Description describes the metric.
          type: string
        isMonotonic:
          description: IsMonotonic marks a sum that only ever increases.
          type: boolean
        temporality:
          description: Temporality is delta or cumulative.
          type: string
        type:
          description: Type is the metric type, e.g. gauge, sum, histogram.
          type: string
        unit:
          description: Unit is the metric's unit.
          type: string
      type: object
    o11y.O11yMetricMetadataOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricMetadata'
          description: Data holds the metadata.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricMetadataSaveIn:
      properties:
        description:
          description: Description describes the metric.
          type: string
        isMonotonic:
          description: IsMonotonic marks a sum that only ever increases.
          type: boolean
        metricName:
          description: MetricName is the metric to update. Required.
          type: string
        temporality:
          description: Temporality is delta or cumulative.
          type: string
        type:
          description: Type is the metric type, e.g. gauge, sum, histogram.
          type: string
        unit:
          description: Unit is the metric's unit.
          type: string
      required:
      - metricName
      type: object
    o11y.O11yMetricOnboarding:
      properties:
        hasMetrics:
          description: HasMetrics is true once any non-O11y metric has been ingested.
          type: boolean
      type: object
    o11y.O11yMetricOnboardingOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricOnboarding'
          description: Data holds the flag.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricOrder:
      properties:
        direction:
          description: Direction is asc or desc.
          type: string
        key:
          $ref: '#/components/schemas/o11y.O11yMetricField'
          description: Key is the field to order by.
      type: object
    o11y.O11yMetricPanel:
      properties:
        dashboardId:
          description: DashboardID is the dashboard's id.
          type: string
        dashboardName:
          description: DashboardName is the dashboard's name.
          type: string
        filterBy:
          description: FilterBy are the labels the panel filters the metric by.
          items:
            type: string
          type: array
        groupBy:
          description: GroupBy are the labels the panel groups the metric by.
          items:
            type: string
          type: array
        panelId:
          description: PanelID is the panel's id.
          type: string
        panelName:
          description: PanelName is the panel's name.
          type: string
      type: object
    o11y.O11yMetricPoint:
      properties:
        partial:
          description: Partial marks a point whose bucket the window only partly covers.
          type: boolean
        timestamp:
          description: Timestamp is the point's time as a Unix timestamp in milliseconds.
          type: integer
        value:
          description: Value is the point's value.
          type: number
        values:
          description: Values carries the bucket values of a heatmap point.
          items:
            type: number
          type: array
      type: object
    o11y.O11yMetricSeries:
      properties:
        labels:
          description: Labels identify the series.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricLabel'
          type: array
        values:
          description: Values are the series' points, in time order.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricPoint'
          type: array
      type: object
    o11y.O11yMetricSeriesSet:
      properties:
        series:
          description: Series are the time series.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricSeries'
          type: array
      type: object
    o11y.O11yMetricStat:
      properties:
        description:
          description: Description describes the metric.
          type: string
        metricName:
          description: MetricName is the metric's name.
          type: string
        samples:
          description: Samples is how many samples the metric had.
          type: integer
        timeseries:
          description: TimeSeries is how many time series the metric had.
          type: integer
        type:
          description: Type is the metric type.
          type: string
        unit:
          description: Unit is the metric's unit.
          type: string
      type: object
    o11y.O11yMetricStats:
      properties:
        metrics:
          description: Metrics are the counted metrics.
          items:
            $ref: '#/components/schemas/o11y.O11yMetricStat'
          type: array
        total:
          description: Total is how many metrics matched, across all pages.
          type: integer
      type: object
    o11y.O11yMetricStatsIn:
      properties:
        end:
          description: End is the end of the window as a Unix timestamp in milliseconds.
            Required.
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.O11yMetricFilter'
          description: Filter narrows the metrics counted.
        limit:
          description: Limit caps how many metrics come back, between 1 and 5000.
            Required.
          type: integer
        offset:
          description: Offset is how many metrics to skip, for paging.
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.O11yMetricOrder'
          description: OrderBy sorts the page, by samples or timeseries.
        start:
          description: Start is the start of the window as a Unix timestamp in milliseconds.
            Required.
          type: integer
      required:
      - start
      - end
      - limit
      type: object
    o11y.O11yMetricStatsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricStats'
          description: Data holds the statistics.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricSummary:
      properties:
        description:
          description: Description describes the metric.
          type: string
        isMonotonic:
          description: IsMonotonic marks a sum that only ever increases.
          type: boolean
        metricName:
          description: MetricName is the metric's name.
          type: string
        temporality:
          description: Temporality is delta or cumulative.
          type: string
        type:
          description: Type is the metric type, e.g. gauge, sum, histogram.
          type: string
        unit:
          description: Unit is the metric's unit.
          type: string
      type: object
    o11y.O11yMetricTreemap:
      properties:
        samples:
          description: Samples are the entries when measuring by sample count.
          items:
            $ref: '#/components/schemas/o11y.O11yTreemapEntry'
          type: array
        timeseries:
          description: TimeSeries are the entries when measuring by time-series count.
          items:
            $ref: '#/components/schemas/o11y.O11yTreemapEntry'
          type: array
      type: object
    o11y.O11yMetricTreemapIn:
      properties:
        end:
          description: End is the end of the window as a Unix timestamp in milliseconds.
            Required.
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.O11yMetricFilter'
          description: Filter narrows the metrics counted.
        limit:
          description: Limit caps how many entries come back, between 1 and 5000.
            Required.
          type: integer
        mode:
          description: 'Mode picks the measure: timeseries or samples. Required.'
          type: string
        start:
          description: Start is the start of the window as a Unix timestamp in milliseconds.
            Required.
          type: integer
      required:
      - start
      - end
      - limit
      - mode
      type: object
    o11y.O11yMetricTreemapOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yMetricTreemap'
          description: Data holds the treemap.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMetricsQueryRangeOut:
      properties:
        data:
          description: |-
            Data is the result — its result type, the value, and query stats when
            asked.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yMyServiceAccountUpdateIn:
      properties:
        name:
          description: |-
            Name is the account's new name, under the same rules it was created with.
            Required.
          type: string
      type: object
    o11y.O11yNamespaceListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.NamespaceListResponse'
          description: Data holds the namespace records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yNextPrevErrorIDs:
      properties:
        groupID:
          description: GroupID is the group both belong to.
          type: string
        nextErrorID:
          description: NextErrorID is the id of the instance immediately after this
            one.
          type: string
        nextTimestamp:
          description: NextTimestamp is that instance's time.
          format: date-time
          type: string
        prevErrorID:
          description: PrevErrorID is the id of the instance immediately before this
            one.
          type: string
        prevTimestamp:
          description: PrevTimestamp is that instance's time.
          format: date-time
          type: string
      type: object
    o11y.O11yNodeListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.NodeListResponse'
          description: Data holds the node records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yOIDCConfig:
      properties:
        claimMapping:
          $ref: '#/components/schemas/o11y.O11yAttributeMapping'
          description: ClaimMapping names the token claims to read identity from.
        clientId:
          description: ClientID is the OAuth application's id.
          type: string
        clientSecret:
          description: ClientSecret is the OAuth application's secret.
          type: string
        getUserInfo:
          description: |-
            GetUserInfo also queries the userinfo endpoint, for providers whose id
            tokens are thin.
          type: boolean
        insecureSkipEmailVerified:
          description: |-
            InsecureSkipEmailVerified admits addresses the provider has not
            verified.
          type: boolean
        issuer:
          description: Issuer is the provider's issuer URL.
          type: string
        issuerAlias:
          description: |-
            IssuerAlias overrides the issuer for providers whose discovery document
            disagrees with their issuer URL.
          type: string
      type: object
    o11y.O11yObject:
      properties:
        resource:
          $ref: '#/components/schemas/o11y.O11yResourceRef'
          description: Resource is the resource's type and kind.
        selector:
          description: Selector picks the instance — an FGA object string, wildcard
            allowed.
          type: string
      type: object
    o11y.O11yObjectGroup:
      properties:
        resource:
          $ref: '#/components/schemas/o11y.O11yResourceRef'
          description: Resource is the objects' type and kind.
        selectors:
          description: Selectors pick the instances; a wildcard selects them all.
          items:
            type: string
          type: array
      type: object
    o11y.O11yOccurrence:
      properties:
        culprit:
          description: Culprit is where it came from.
          type: string
        environment:
          description: Environment is the deployment it happened in.
          type: string
        eventId:
          description: EventID is the occurrence's id.
          type: string
        fingerprint:
          description: Fingerprint is the grouping key it was bucketed by.
          type: string
        frames:
          description: Frames are the stack, innermost first.
          items:
            $ref: '#/components/schemas/o11y.O11yOccurrenceFrame'
          type: array
        level:
          description: Level is its severity, e.g. error, warning, info.
          type: string
        platform:
          description: Platform is the reporting runtime.
          type: string
        release:
          description: Release is the version that produced it.
          type: string
        serverName:
          description: ServerName is the host that reported it.
          type: string
        serviceName:
          description: ServiceName is the service that reported it.
          type: string
        spanId:
          description: SpanID is the span it belonged to.
          type: string
        tags:
          additionalProperties:
            type: string
          description: Tags are the reporter's own key/value labels.
          type: object
        timestamp:
          description: Timestamp is when the error happened.
          format: date-time
          type: string
        traceId:
          description: TraceID is the trace it belonged to.
          type: string
        transaction:
          description: Transaction is the operation it happened in.
          type: string
        type:
          description: Type is the exception type.
          type: string
        user:
          $ref: '#/components/schemas/o11y.O11yEventUser'
          description: User is the affected end-user context, when the reporter attached
            one.
        value:
          description: Value is the exception value.
          type: string
      type: object
    o11y.O11yOccurrenceFrame:
      properties:
        absPath:
          description: AbsPath is the file's absolute path.
          type: string
        colno:
          description: Colno is the column number.
          type: integer
        filename:
          description: Filename is the file it is in.
          type: string
        function:
          description: Function is the function the frame is in.
          type: string
        inApp:
          description: InApp marks a frame in the reporting application's own code.
          type: boolean
        lineno:
          description: Lineno is the line number.
          type: integer
        module:
          description: Module is the module the function is in.
          type: string
      type: object
    o11y.O11yOnboardingOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yK8sOnboarding'
          description: Data is the progress report.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yOperation:
      properties:
        errorCount:
          description: ErrorCount is how many of those runs errored.
          type: integer
        name:
          description: Name is the operation (span name).
          type: string
        numCalls:
          description: NumCalls is how many times it ran in the window.
          type: integer
        p50:
          description: P50 is its median latency, nanoseconds.
          type: number
        p95:
          description: P95 is its p95 latency, nanoseconds.
          type: number
        p99:
          description: P99 is its p99 latency, nanoseconds.
          type: number
      type: object
    o11y.O11yOperationsIn:
      properties:
        end:
          description: End is the window's end, epoch nanoseconds as a string.
          type: string
        limit:
          description: Limit caps how many operations come back.
          type: integer
        service:
          description: Service is the service whose operations are read.
          type: string
        start:
          description: Start is the window's start, epoch nanoseconds as a string.
          type: string
        tags:
          description: Tags narrow the spans counted, each a span-attribute predicate.
          items:
            $ref: '#/components/schemas/o11y.O11yServiceTag'
          type: array
      type: object
    o11y.O11yOperationsOut:
      properties:
        data:
          description: Data holds one entry per operation.
          items:
            $ref: '#/components/schemas/o11y.O11yOperation'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yOrgStatsOut:
      properties:
        data:
          additionalProperties:
            type: object
          description: Data are the statistics, keyed by the reporter's own counter
            names.
          type: object
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yOrganization:
      properties:
        alias:
          description: Alias is an alternate name the org also answers to.
          type: string
        createdAt:
          description: CreatedAt is when the org was created.
          format: date-time
          type: string
        displayName:
          description: DisplayName is what the console shows for the org.
          type: string
        id:
          description: |-
            ID is the org id. On update it is ignored: the call always addresses the
            caller's own org.
          type: string
        key:
          description: Key is the org's stable numeric key, derived from its id.
          type: integer
        name:
          description: Name is the org's short name.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yOrganizationOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yOrganization'
          description: Data is the organization.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yOverallStateTransitionsOut:
      properties:
        data:
          description: Data holds the windows.
          items:
            $ref: '#/components/schemas/o11y.ReleStateItem'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPasswordAuthN:
      properties:
        provider:
          description: Provider is the route's provider, e.g. email_password.
          type: string
      type: object
    o11y.O11yPercentilePosition:
      properties:
        description:
          description: Description says the same in words.
          type: string
        percentile:
          description: Percentile is the percentile the duration lands at.
          type: number
      type: object
    o11y.O11yPercentiles:
      properties:
        p50:
          description: P50 is the median.
          type: number
        p90:
          description: P90 is the 90th percentile.
          type: number
        p99:
          description: P99 is the 99th percentile.
          type: number
      type: object
    o11y.O11yPodListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.PodListResponse'
          description: Data holds the pod records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPodOnboarding:
      properties:
        clusterName:
          description: ClusterName is the pod's cluster.
          type: string
        hasClusterName:
          description: HasClusterName says whether the cluster label is present.
          type: boolean
        hasCronjobName:
          description: HasCronjobName says whether the cronjob label is present.
          type: boolean
        hasDaemonsetName:
          description: HasDaemonsetName says whether the daemonset label is present.
          type: boolean
        hasDeploymentName:
          description: HasDeploymentName says whether the deployment label is present.
          type: boolean
        hasJobName:
          description: HasJobName says whether the job label is present.
          type: boolean
        hasNamespaceName:
          description: HasNamespaceName says whether the namespace label is present.
          type: boolean
        hasNodeName:
          description: HasNodeName says whether the node label is present.
          type: boolean
        hasStatefulsetName:
          description: HasStatefulsetName says whether the statefulset label is present.
          type: boolean
        namespaceName:
          description: NamespaceName is the pod's namespace.
          type: string
        nodeName:
          description: NodeName is the pod's node.
          type: string
        podName:
          description: PodName is the pod.
          type: string
      type: object
    o11y.O11yPostableAuthDomain:
      properties:
        config:
          $ref: '#/components/schemas/o11y.O11yAuthDomainConfig'
          description: Config is the domain's SSO configuration.
        name:
          description: Name is the email domain being claimed, e.g. example.com.
          type: string
      type: object
    o11y.O11yPostableUser:
      properties:
        displayName:
          description: DisplayName is the new member's display name.
          type: string
        email:
          description: Email is the new member's address. Required.
          type: string
        frontendBaseUrl:
          description: FrontendBaseUrl is the console origin the invite link is built
            on.
          type: string
        userRoles:
          description: UserRoles are the roles the member starts with, each by id.
          items:
            $ref: '#/components/schemas/o11y.O11yRoleID'
          type: array
      type: object
    o11y.O11yPreference:
      properties:
        allowedScopes:
          description: AllowedScopes are the scopes the preference may be set at —
            org, user.
          items:
            type: string
          type: array
        allowedValues:
          description: AllowedValues restricts a string preference to these values.
          items:
            type: string
          type: array
        defaultValue:
          description: DefaultValue is the value before anyone set one.
          type: object
        description:
          description: Description says what the preference does.
          type: string
        name:
          description: Name is the preference name.
          type: string
        value:
          description: Value is the current value.
          type: object
        valueType:
          description: |-
            ValueType is the JSON type a value must have — string, integer, float or
            boolean.
          type: string
      type: object
    o11y.O11yPreferenceOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yPreference'
          description: Data is the preference.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPreferencesOut:
      properties:
        data:
          description: Data holds the preferences.
          items:
            $ref: '#/components/schemas/o11y.O11yPreference'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yProcessListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.ProcessListResponse'
          description: Data holds the process records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPromQueryOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yPromResult'
          description: Data is the evaluation result.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPromResult:
      properties:
        result:
          description: |-
            Result is the result in PromQL's own wire shape for ResultType, carried
            verbatim.
        resultType:
          description: 'ResultType discriminates Result: matrix, vector, scalar or
            string.'
          type: string
        stats:
          description: Stats holds query statistics when the caller asked for them.
      type: object
    o11y.O11yPublicDashboard:
      properties:
        defaultTimeRange:
          description: DefaultTimeRange is the fixed window when the range is not
            caller-chosen.
          type: string
        publicPath:
          description: PublicPath is the public URL path the share is reachable at.
          type: string
        timeRangeEnabled:
          description: TimeRangeEnabled reports whether the public page may pick its
            own range.
          type: boolean
      type: object
    o11y.O11yPublicDashboardData:
      properties:
        dashboard:
          $ref: '#/components/schemas/o11y.O11yPublicDashboardV1'
          description: Dashboard is the sanitized dashboard.
        publicDashboard:
          $ref: '#/components/schemas/o11y.O11yPublicDashboard'
          description: PublicDashboard is the public-sharing config.
      type: object
    o11y.O11yPublicDashboardDataOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yPublicDashboardData'
          description: Data is the sanitized dashboard and its share config.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPublicDashboardOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yPublicDashboard'
          description: Data is the public-sharing config.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yPublicDashboardV1:
      properties:
        createdAt:
          description: CreatedAt is when the dashboard was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created it.
          type: string
        data:
          description: Data is the sanitized widget data as stored, carried verbatim
            as open JSON.
        id:
          description: ID is the dashboard's id.
          type: string
        locked:
          description: Locked reports whether the dashboard is locked.
          type: boolean
        org_id:
          description: OrgID is the org the dashboard belongs to.
          type: string
        source:
          description: Source is where the dashboard came from.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is who last changed it.
          type: string
      type: object
    o11y.O11yPublicDashboardWriteIn:
      properties:
        defaultTimeRange:
          type: string
        id:
          description: ID is the dashboard id from the path.
          type: string
        timeRangeEnabled:
          type: boolean
      type: object
    o11y.O11yPvcListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.VolumeListResponse'
          description: Data holds the volume records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQueryFilterAnalysis:
      properties:
        groups:
          description: Groups are the columns the query groups by.
          items:
            $ref: '#/components/schemas/o11y.O11yColumnInfo'
          type: array
        metricNames:
          description: MetricNames are the metrics the query reads.
          items:
            type: string
          type: array
      type: object
    o11y.O11yQueryRange:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yQueryRangeData'
          description: Data holds the results.
        meta:
          $ref: '#/components/schemas/o11y.O11yQueryStats'
          description: Meta reports what the query scanned.
        type:
          description: Type is the result kind; time_series here.
          type: string
        warning:
          $ref: '#/components/schemas/o11y.O11yQueryWarning'
          description: Warning carries a non-fatal warning, when the query raised
            one.
      type: object
    o11y.O11yQueryRangeData:
      properties:
        results:
          description: Results are the per-query results, each a set of aggregated
            series.
          items:
            $ref: '#/components/schemas/o11y.O11yReductionSeriesResult'
          type: array
      type: object
    o11y.O11yQueryRangeFormatOut:
      properties:
        data:
          description: Data is the query, normalized to the v3 shape.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQueryRangeOut:
      properties:
        data:
          description: |-
            Data is the query result — the result type, the result set, execution stats
            and any warning — as the engine rendered it.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQueryRangePreviewIn:
      properties:
        compositeQuery:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.CompositeQuery'
        end:
          type: integer
        formatOptions:
          $ref: '#/components/schemas/o11y.FormatOptions'
        noCache:
          type: boolean
        requestType: {}
        schemaVersion:
          type: string
        start:
          type: integer
        variables:
          additionalProperties:
            $ref: '#/components/schemas/o11y.VariableItem'
          type: object
        verbose:
          description: |-
            Verbose selects the answer's depth. Empty or "true" renders the underlying
            Datastore SQL with EXPLAIN and granule analysis; "false" returns only the
            per-query valid/error/warnings verdict with no Datastore round trips.
          type: string
      type: object
    o11y.O11yQueryRangePreviewOut:
      properties:
        data:
          description: |-
            Data holds one preview per query, keyed by query name — the rendered
            statements with optional EXPLAIN and granule analysis, or the per-query
            verdict.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQueryStats:
      properties:
        bytesScanned:
          description: BytesScanned is how many bytes the query read.
          type: integer
        durationMs:
          description: DurationMS is how long the query took, in milliseconds.
          type: integer
        rowsScanned:
          description: RowsScanned is how many rows the query read.
          type: integer
        stepIntervals:
          additionalProperties:
            type: integer
          description: StepIntervals is the step used per query, in seconds.
          type: object
      type: object
    o11y.O11yQueryWarning:
      properties:
        message:
          description: Message is the warning.
          type: string
        url:
          description: URL points at the relevant documentation.
          type: string
        warnings:
          description: Warnings carries additional notes.
          items:
            $ref: '#/components/schemas/o11y.O11yQueryWarningNote'
          type: array
      type: object
    o11y.O11yQueryWarningNote:
      properties:
        message:
          description: Message is the note.
          type: string
      type: object
    o11y.O11yQueueCheck:
      properties:
        attribute:
          description: Attribute is the span attribute or telemetry the check looked
            for.
          type: string
        error_message:
          description: |-
            Message says what is missing when the check fails; empty on a pass. Its
            wire key is error_message.
          type: string
        status:
          description: Status is "1" when the telemetry is present, "0" when it is
            not.
          type: string
      type: object
    o11y.O11yQueueChecksOut:
      properties:
        data:
          description: Data holds one check per required attribute, sorted by attribute.
          items:
            $ref: '#/components/schemas/o11y.O11yQueueCheck'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQueueFilterKey:
      properties:
        dataType:
          description: 'DataType is the attribute''s type: string, int64, float64
            or bool.'
          type: string
        isColumn:
          description: IsColumn marks an attribute materialized as its own store column.
          type: boolean
        isJSON:
          description: IsJSON marks an attribute read out of the span's JSON body.
          type: boolean
        key:
          description: Key is the attribute name.
          type: string
        type:
          description: 'Type says which plane the attribute lives on: tag or resource.'
          type: string
      type: object
    o11y.O11yQueueFilterRule:
      properties:
        key:
          $ref: '#/components/schemas/o11y.O11yQueueFilterKey'
          description: Key names the attribute the predicate tests.
        op:
          description: Op is the comparison, e.g. =, !=, in, contains.
          type: string
        value:
          description: Value is the operand; its JSON type follows the attribute's
            dataType.
          type: object
      type: object
    o11y.O11yQueueFilterSet:
      properties:
        items:
          description: Items are the predicates.
          items:
            $ref: '#/components/schemas/o11y.O11yQueueFilterRule'
          type: array
        op:
          description: 'Op combines the items: AND or OR.'
          type: string
      type: object
    o11y.O11yQueueIn:
      properties:
        end:
          description: End is the window's end, epoch nanoseconds.
          type: integer
        eval_time:
          description: |-
            EvalTime bounds the span-evaluation scan, nanoseconds; only the
            span/evaluation view reads it.
          type: integer
        start:
          description: Start is the window's start, epoch nanoseconds.
          type: integer
        variables:
          additionalProperties:
            type: string
          description: |-
            Variables name what the view drills into — topic, partition, service,
            consumer_group — keyed by the name the view expects.
          type: object
      type: object
    o11y.O11yQueueListIn:
      properties:
        end:
          description: End is the window's end, epoch nanoseconds.
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.O11yQueueFilterSet'
          description: Filters narrow the rows by span attribute; null means all rows.
        limit:
          description: Limit caps how many rows come back.
          type: integer
        start:
          description: Start is the window's start, epoch nanoseconds.
          type: integer
      type: object
    o11y.O11yQueueRow:
      properties:
        data:
          additionalProperties: {}
          description: |-
            Data holds the row's cells keyed by column name; each cell's JSON type is
            the column's own, so the bytes pass through verbatim.
          type: object
        timestamp:
          description: Timestamp anchors the row in time.
          format: date-time
          type: string
      type: object
    o11y.O11yQueueRowsOut:
      properties:
        data:
          description: Data holds one row per messaging destination.
          items:
            $ref: '#/components/schemas/o11y.O11yQueueRow'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yQuickFiltersOut:
      properties:
        data:
          description: Data holds one entry per signal.
          items:
            $ref: '#/components/schemas/o11y.O11ySignalFilters'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yReductionRule:
      properties:
        active:
          description: Active says whether the rule is in force.
          type: boolean
        createdAt:
          description: CreatedAt is when the rule was created.
          format: date-time
          type: string
        createdBy:
          description: CreatedBy is who created it.
          type: string
        effectiveFrom:
          description: EffectiveFrom is when the rule took effect.
          format: date-time
          type: string
        id:
          description: ID is the rule's id.
          type: string
        ingestedSamples:
          description: IngestedSamples is how many samples arrived while the rule
            was active.
          type: integer
        ingestedSeries:
          description: IngestedSeries is how many series arrived while the rule was
            active.
          type: integer
        labels:
          description: Labels are the label names the rule matches.
          items:
            type: string
          type: array
        matchType:
          description: MatchType is drop or keep.
          type: string
        metricName:
          description: MetricName is the metric the rule governs.
          type: string
        retainedSamples:
          description: RetainedSamples is how many of them were kept.
          type: integer
        retainedSeries:
          description: RetainedSeries is how many of them were kept.
          type: integer
        updatedAt:
          description: UpdatedAt is when the rule last changed.
          format: date-time
          type: string
        updatedBy:
          description: UpdatedBy is who last changed it.
          type: string
      type: object
    o11y.O11yReductionRuleCreateIn:
      properties:
        labels:
          description: Labels are the label names the rule matches. Required, at least
            one.
          items:
            type: string
          type: array
        matchType:
          description: 'MatchType is drop or keep: drop the named labels, or keep
            only them. Required.'
          type: string
        metricName:
          description: MetricName is the metric the rule governs; one rule per metric.
            Required.
          type: string
      required:
      - metricName
      - matchType
      - labels
      type: object
    o11y.O11yReductionRuleListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yReductionRules'
          description: Data holds the rules.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yReductionRuleOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yReductionRule'
          description: Data holds the rule.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yReductionRulePreview:
      properties:
        affectedAssets:
          description: AffectedAssets are the dashboards and alerts the rule would
            touch.
          items:
            $ref: '#/components/schemas/o11y.O11yAffectedAsset'
          type: array
        currentRetainedSeries:
          description: CurrentRetainedSeries is how many survive the rules in force
            today.
          type: integer
        droppedLabels:
          description: DroppedLabels are the labels the rule would drop.
          items:
            type: string
          type: array
        effectiveFrom:
          description: EffectiveFrom is when the rule would take effect.
          format: date-time
          type: string
        ingestedSeries:
          description: IngestedSeries is how many series the metric ingests today.
          type: integer
        reductionPercent:
          description: ReductionPercent is the estimated reduction, in percent.
          type: number
        retainedSeries:
          description: RetainedSeries is how many would survive with the candidate
            rule.
          type: integer
      type: object
    o11y.O11yReductionRulePreviewIn:
      properties:
        labels:
          description: Labels are the label names the rule would match. Required,
            at least one.
          items:
            type: string
          type: array
        lookbackMs:
          description: LookbackMs is how far back to sample when estimating.
          type: integer
        matchType:
          description: MatchType is drop or keep. Required.
          type: string
        metricName:
          description: MetricName is the metric the rule would govern. Required.
          type: string
      required:
      - metricName
      - matchType
      - labels
      type: object
    o11y.O11yReductionRulePreviewOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yReductionRulePreview'
          description: Data holds the estimate.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yReductionRuleSaveIn:
      properties:
        id:
          description: ID is the rule's id.
          type: string
        labels:
          description: Labels are the label names the rule matches. Required, at least
            one.
          items:
            type: string
          type: array
        matchType:
          description: MatchType is drop or keep. Required.
          type: string
      required:
      - id
      - matchType
      - labels
      type: object
    o11y.O11yReductionRules:
      properties:
        rules:
          description: Rules are the rules.
          items:
            $ref: '#/components/schemas/o11y.O11yReductionRule'
          type: array
        total:
          description: Total is how many rules matched, across all pages.
          type: integer
      type: object
    o11y.O11yReductionSeriesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yQueryRange'
          description: Data holds the query-range answer.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yReductionSeriesResult:
      properties:
        aggregations:
          description: Aggregations are the query's aggregation buckets.
          items:
            $ref: '#/components/schemas/o11y.O11yAggregation'
          type: array
        queryName:
          description: QueryName names the query the result answers.
          type: string
      type: object
    o11y.O11yReductionStats:
      properties:
        estimatedMonthlySavingsUsd:
          description: EstimatedMonthlySavingsUsd is the estimated monthly savings,
            in USD.
          type: number
        ingestedSamples:
          description: IngestedSamples is how many samples arrived across all rules.
          type: integer
        ingestedSeries:
          description: IngestedSeries is how many series arrived across all rules.
          type: integer
        retainedSamples:
          description: RetainedSamples is how many of them were kept.
          type: integer
        retainedSeries:
          description: RetainedSeries is how many of them were kept.
          type: integer
      type: object
    o11y.O11yReductionStatsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yReductionStats'
          description: Data holds the totals.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRegisterIn:
      properties:
        email:
          description: Email is the admin's email. Required.
          type: string
        name:
          description: Name is the admin's display name.
          type: string
        orgDisplayName:
          description: OrgDisplayName is the organization's display name.
          type: string
        orgName:
          description: OrgName is the organization's name.
          type: string
        password:
          description: Password is the admin's password.
          type: string
      required:
      - email
      type: object
    o11y.O11yRegisterOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yUser'
          description: |-
            Data is the user. The runtime answers register with the same user shape
            the identity face reads, so it is the ONE O11yUser (identity.go) — a
            created user is a user, and the document names it once.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yResetPasswordIn:
      properties:
        password:
          description: Password is the new password.
          type: string
        token:
          description: Token is the reset-password token authorizing the change.
          type: string
      type: object
    o11y.O11yResetToken:
      properties:
        expiresAt:
          description: ExpiresAt is when it stops working.
          format: date-time
          type: string
        id:
          description: ID is the grant's id.
          type: string
        passwordId:
          description: PasswordID is the password record it resets.
          type: string
        token:
          description: Token is the secret that redeems it.
          type: string
      type: object
    o11y.O11yResetTokenOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yResetToken'
          description: Data is the token.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yResetTokenRef:
      properties:
        token:
          description: Token is the reset-password token.
          type: string
      type: object
    o11y.O11yResourceRef:
      properties:
        kind:
          description: Kind is the resource kind the type belongs to.
          type: string
        type:
          description: Type is the resource type, e.g. role, dashboard, serviceaccount.
          type: string
      type: object
    o11y.O11yRetentionMatch:
      properties:
        key:
          description: Key is the label to test.
          type: string
        values:
          description: Values are the label values the rule matches.
          items:
            type: string
          type: array
      type: object
    o11y.O11yRetentionOut:
      properties:
        cold_storage_ttl_days:
          description: ColdStorageTTLDays is how old data must be before it moves,
            in days.
          type: integer
        cold_storage_volume:
          description: ColdStorageVolume names the volume aged data moves to.
          type: string
        default_ttl_days:
          description: DefaultTTLDays is the retention for data no rule matches, in
            days.
          type: integer
        expected_logs_move_ttl_duration_hrs:
          description: |-
            ExpectedLogsMoveTTLHours is the pending logs cold-storage move TTL, in
            hours.
          type: integer
        expected_logs_ttl_duration_hrs:
          description: ExpectedLogsTTLHours is the pending logs TTL, in hours.
          type: integer
        status:
          description: Status is the last TTL operation's state.
          type: string
        ttl_conditions:
          description: TTLConditions are the ordered per-label rules; the first match
            wins.
          items:
            $ref: '#/components/schemas/o11y.O11yRetentionRule'
          type: array
        version:
          description: Version is the policy format version.
          type: string
      type: object
    o11y.O11yRetentionRule:
      properties:
        conditions:
          description: Conditions all have to hold for the rule to match.
          items:
            $ref: '#/components/schemas/o11y.O11yRetentionMatch'
          type: array
        ttlDays:
          description: TTLDays is the retention applied when it does, in days.
          type: integer
      type: object
    o11y.O11yRetentionSetIn:
      properties:
        coldStorageDurationDays:
          description: ColdStorageDurationDays is how old data must be before it moves,
            in days.
          type: integer
        coldStorageVolume:
          description: ColdStorageVolume names the volume aged data moves to, when
            set.
          type: string
        defaultTTLDays:
          description: DefaultTTLDays is the retention for data no rule matches, in
            days.
          type: integer
        ttlConditions:
          description: TTLConditions are ordered per-label rules; the first matching
            rule wins.
          items:
            $ref: '#/components/schemas/o11y.O11yRetentionRule'
          type: array
        type:
          description: Type is the signal the policy applies to — traces, metrics
            or logs.
          type: string
      type: object
    o11y.O11yRetentionSetOut:
      properties:
        message:
          description: Message says what was done.
          type: string
      type: object
    o11y.O11yRetry:
      properties:
        delay:
          description: Delay is how long to wait before retrying, in nanoseconds.
          type: integer
      type: object
    o11y.O11yRole:
      properties:
        createdAt:
          description: CreatedAt is when the role was created.
          format: date-time
          type: string
        description:
          description: Description says what the role is for.
          type: string
        id:
          description: ID is the role id.
          type: string
        name:
          description: Name is the role's name.
          type: string
        orgId:
          description: OrgID is the org the role belongs to.
          type: string
        type:
          description: Type is how the role came to be — managed by the platform or
            custom.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yRoleCreateIn:
      properties:
        description:
          description: Description says what the role is for.
          type: string
        name:
          description: |-
            Name is the role's name: lowercase letters and hyphens, at most 50
            characters, not starting with the reserved managed-role prefix. Required.
          type: string
        transactionGroups:
          description: TransactionGroups are the grants the role carries.
          items:
            $ref: '#/components/schemas/o11y.O11yTransactionGroup'
          type: array
      type: object
    o11y.O11yRoleCreateOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yCreated'
          description: Data carries the new role's id.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRoleDetail:
      properties:
        createdAt:
          description: CreatedAt is when the role was created.
          format: date-time
          type: string
        description:
          description: Description says what the role is for.
          type: string
        id:
          description: ID is the role id.
          type: string
        name:
          description: Name is the role's name.
          type: string
        orgId:
          description: OrgID is the org the role belongs to.
          type: string
        transactionGroups:
          description: TransactionGroups are the grants the role carries.
          items:
            $ref: '#/components/schemas/o11y.O11yTransactionGroup'
          type: array
        type:
          description: Type is custom or managed.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yRoleID:
      properties:
        id:
          description: ID is the role id.
          type: string
      type: object
    o11y.O11yRoleMapping:
      properties:
        defaultRole:
          description: DefaultRole is the role when no group mapping applies.
          type: string
        groupMappings:
          additionalProperties:
            type: string
          description: GroupMappings maps a provider group name to a role name.
          type: object
        useRoleAttribute:
          description: |-
            UseRoleAttribute reads the role straight from the provider's role claim
            instead of the group mappings.
          type: boolean
      type: object
    o11y.O11yRoleOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yRoleDetail'
          description: Data holds the role and its transaction groups.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRoleUpdateIn:
      properties:
        description:
          description: |-
            Description says what the role is for. Required — send an empty string to
            clear it.
          type: string
        transactionGroups:
          description: |-
            TransactionGroups are the grants the role carries. Required — send an
            empty array to clear them.
          items:
            $ref: '#/components/schemas/o11y.O11yTransactionGroup'
          type: array
      type: object
    o11y.O11yRolesOut:
      properties:
        data:
          description: Data holds the roles.
          items:
            $ref: '#/components/schemas/o11y.O11yRole'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRotateSessionIn:
      properties:
        refreshToken:
          description: RefreshToken is the refresh token being redeemed.
          type: string
      type: object
    o11y.O11yRoutePoliciesOut:
      properties:
        data:
          description: Data holds the policies.
          items:
            $ref: '#/components/schemas/o11y.GettableRoutePolicy'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRoutePolicyOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableRoutePolicy'
          description: Data holds the policy.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRoutePolicyUpdateIn:
      properties:
        channels:
          items:
            type: string
          type: array
        description:
          type: string
        expression:
          type: string
        kind: {}
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.O11yRuleHistoryContributorsOut:
      properties:
        data:
          description: Data holds the contributors.
          items:
            $ref: '#/components/schemas/o11y.GettableRuleStateHistoryContributor'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleHistoryFilterKeysOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFieldKeys'
          description: Data holds the keys.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleHistoryFilterValuesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFieldValues'
          description: Data holds the values.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleHistoryOverallStatusOut:
      properties:
        data:
          description: Data holds the windows.
          items:
            $ref: '#/components/schemas/o11y.GettableRuleStateWindow'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleHistoryQueryIn:
      properties:
        end:
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        limit:
          type: integer
        offset:
          type: integer
        order:
          type: string
        start:
          type: integer
        state:
          type: string
      type: object
    o11y.O11yRuleHistoryStatsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableRuleStateHistoryStats'
          description: Data holds the statistics.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleHistoryTimelineOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableRuleStateTimeline'
          description: Data holds the timeline and its paging cursor.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleOut:
      properties:
        data:
          description: Data holds the rule.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleStateContributorsOut:
      properties:
        data:
          description: Data holds the contributors.
          items:
            $ref: '#/components/schemas/o11y.RuleStateHistoryContributor'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleStateTimelineOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.RuleStateTimeline'
          description: Data holds the timeline.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRuleStatsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Stats'
          description: Data holds the statistics.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yRulesOut:
      properties:
        data:
          description: Data holds the rules.
          items: {}
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySAMLConfig:
      properties:
        attributeMapping:
          $ref: '#/components/schemas/o11y.O11yAttributeMapping'
          description: AttributeMapping names the assertion attributes to read identity
            from.
        insecureSkipAuthNRequestsSigned:
          description: |-
            InsecureSkipAuthNRequestsSigned skips signing outgoing AuthN requests,
            for IdPs that refuse signed ones.
          type: boolean
        samlCert:
          description: SamlCert is the IdP's signing certificate.
          type: string
        samlEntity:
          description: SamlEntity is the IdP's entityID.
          type: string
        samlIdp:
          description: SamlIdp is the IdP's single-sign-on endpoint.
          type: string
      type: object
    o11y.O11ySavedViewCreateOut:
      properties:
        data:
          description: Data is the new view's id.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySavedViewDeleteOut:
      properties:
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySavedViewListOut:
      properties:
        data:
          description: Data holds the views.
          items:
            $ref: '#/components/schemas/o11y.SavedView'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySavedViewOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.SavedView'
          description: Data holds the view.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySavedViewUpdateIn:
      properties:
        category:
          type: string
        compositeQuery:
          $ref: '#/components/schemas/o11y.CompositeQuery'
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        extraData:
          type: string
        id: {}
        name:
          type: string
        sourcePage:
          type: string
        tags:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
        viewId:
          description: ViewID is the id of the view to replace, taken from the URL.
          type: string
      type: object
    o11y.O11ySentryEventOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yEvent'
          description: Data is the event.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySentryIssueEventsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yEvents'
          description: Data holds the occurrences.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySentryPostableProject:
      properties:
        name:
          description: Name is the project's display name. Required.
          type: string
        platform:
          description: Platform is the reporting runtime, e.g. go, python, javascript.
          type: string
        slug:
          description: Slug is the project's short name. Server-assigned from Name
            when empty.
          type: string
      required:
      - name
      type: object
    o11y.O11ySentryProject:
      properties:
        createdAt:
          description: CreatedAt is when the project was created.
          format: date-time
          type: string
        dsn:
          description: DSN is the project's freshly-derived ingest DSN.
          type: string
        id:
          description: ID is the project id.
          type: string
        name:
          description: Name is the project's display name.
          type: string
        platform:
          description: Platform is the reporting runtime, e.g. go, python, javascript.
          type: string
        slug:
          description: Slug is the project's short name.
          type: string
        status:
          description: 'Status is the project''s lifecycle state: active or disabled.'
          type: string
        updatedAt:
          description: UpdatedAt is when the project last changed.
          format: date-time
          type: string
      type: object
    o11y.O11ySentryProjectOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11ySentryProject'
          description: Data is the project.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySentryProjects:
      properties:
        items:
          description: Items are the projects.
          items:
            $ref: '#/components/schemas/o11y.O11ySentryProject'
          type: array
        total:
          description: Total is how many the org has.
          type: integer
      type: object
    o11y.O11ySentryProjectsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11ySentryProjects'
          description: Data holds the projects.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySentryUpdateIssueIn:
      properties:
        assignee:
          description: Assignee is who the issue is assigned to.
          type: string
        id:
          description: ID is the issue id.
          type: string
        status:
          description: 'Status is the new lifecycle state: unresolved, resolved or
            ignored.'
          type: string
      required:
      - id
      type: object
    o11y.O11yService:
      properties:
        avgDuration:
          description: AvgDuration is their average latency, nanoseconds.
          type: number
        callRate:
          description: CallRate is calls per second over the window.
          type: number
        dataWarning:
          $ref: '#/components/schemas/o11y.O11yServiceWarning'
          description: |-
            DataWarning carries the entry-point operations the numbers were computed
            over.
        errorRate:
          description: ErrorRate is the percentage of calls that errored.
          type: number
        fourXXRate:
          description: FourXXRate is the percentage of calls that answered 4xx.
          type: number
        num4XX:
          description: Num4XX is how many of the calls answered 4xx.
          type: integer
        numCalls:
          description: NumCalls is how many entry-point spans landed in the window.
          type: integer
        numErrors:
          description: NumErrors is how many of the calls errored.
          type: integer
        p99:
          description: Percentile99 is the p99 latency of its entry-point spans, nanoseconds.
          type: number
        serviceName:
          description: ServiceName is the service.
          type: string
      type: object
    o11y.O11yServiceAccount:
      properties:
        createdAt:
          description: CreatedAt is when the account was created.
          format: date-time
          type: string
        email:
          description: Email is the address the account authenticates as.
          type: string
        id:
          description: ID is the service account id.
          type: string
        name:
          description: Name is the account's name.
          type: string
        orgId:
          description: OrgID is the org the account belongs to.
          type: string
        status:
          description: Status is active or deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yServiceAccountCreateIn:
      properties:
        name:
          description: |-
            Name is the account's name: a lowercase letter followed by lowercase
            letters, digits or hyphens, at most 50 characters. Required.
          type: string
      type: object
    o11y.O11yServiceAccountCreateOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yCreated'
          description: Data carries the new account's id.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServiceAccountDetail:
      properties:
        createdAt:
          description: CreatedAt is when the account was created.
          format: date-time
          type: string
        email:
          description: Email is the address the account authenticates as.
          type: string
        id:
          description: ID is the service account id.
          type: string
        name:
          description: Name is the account's name.
          type: string
        orgId:
          description: OrgID is the org the account belongs to.
          type: string
        serviceAccountRoles:
          description: ServiceAccountRoles are the account's role assignments.
          items:
            $ref: '#/components/schemas/o11y.O11yServiceAccountRole'
          type: array
        status:
          description: Status is active or deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yServiceAccountOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yServiceAccountDetail'
          description: Data holds the account and its role assignments.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServiceAccountRole:
      properties:
        createdAt:
          description: CreatedAt is when the role was assigned.
          format: date-time
          type: string
        id:
          description: ID is the assignment's own id.
          type: string
        role:
          $ref: '#/components/schemas/o11y.O11yRole'
          description: Role is the role itself.
        roleId:
          description: RoleID is the role held.
          type: string
        serviceAccountId:
          description: ServiceAccountID is the account holding the role.
          type: string
        updatedAt:
          description: UpdatedAt is when the assignment last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yServiceAccountRoleGrantIn:
      properties:
        id:
          description: RoleID is the id of the role to assign. Required.
          type: string
      type: object
    o11y.O11yServiceAccountRolesOut:
      properties:
        data:
          description: Data holds the roles.
          items:
            $ref: '#/components/schemas/o11y.O11yRole'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServiceAccountUpdateIn:
      properties:
        name:
          description: |-
            Name is the account's new name, under the same rules it was created with.
            Required.
          type: string
      type: object
    o11y.O11yServiceAccountsOut:
      properties:
        data:
          description: Data holds the service accounts.
          items:
            $ref: '#/components/schemas/o11y.O11yServiceAccount'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServiceOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.Service'
          description: Data holds the service.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServiceTag:
      properties:
        BoolValues:
          description: BoolValues are the boolean operands, when the attribute is
            a bool.
          items:
            type: boolean
          type: array
        Key:
          description: Key is the span attribute to test.
          type: string
        NumberValues:
          description: NumberValues are the numeric operands, when the attribute is
            a number.
          items:
            type: number
          type: array
        Operator:
          description: Operator is how to test it, e.g. in, not_in.
          type: string
        StringValues:
          description: StringValues are the string operands, when the attribute is
            a string.
          items:
            type: string
          type: array
        TagType:
          description: TagType says which plane the attribute lives on, e.g. tag or
            resource.
          type: string
      type: object
    o11y.O11yServiceWarning:
      properties:
        topLevelOps:
          description: TopLevelOps are the entry-point operations the profile was
            computed over.
          items:
            type: string
          type: array
      type: object
    o11y.O11yServicesIn:
      properties:
        end:
          description: End is the window's end, epoch nanoseconds as a string.
          type: string
        start:
          description: Start is the window's start, epoch nanoseconds as a string.
          type: string
        tags:
          description: Tags narrow the spans counted, each a span-attribute predicate.
          items:
            $ref: '#/components/schemas/o11y.O11yServiceTag'
          type: array
      type: object
    o11y.O11yServicesMetadataOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableServicesMetadata'
          description: Data holds the services metadata.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yServicesOut:
      properties:
        data:
          description: Data holds one entry per service.
          items:
            $ref: '#/components/schemas/o11y.O11yService'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySessionContext:
      properties:
        exists:
          description: Exists says whether any account carries the address.
          type: boolean
        orgs:
          description: Orgs are the orgs the address belongs to, each with its sign-in
            routes.
          items:
            $ref: '#/components/schemas/o11y.O11ySessionOrg'
          type: array
      type: object
    o11y.O11ySessionContextOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11ySessionContext'
          description: Data is the context.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySessionOrg:
      properties:
        authNSupport:
          $ref: '#/components/schemas/o11y.O11yAuthNSupport'
          description: AuthNSupport lists the org's open sign-in routes.
        id:
          description: ID is the org id.
          type: string
        name:
          description: Name is the org's display name.
          type: string
        warning:
          $ref: '#/components/schemas/o11y.O11yErrorDetail'
          description: |-
            Warning reports an org whose SSO is configured but not currently usable,
            in the platform's error shape.
      type: object
    o11y.O11ySetRoleIn:
      properties:
        name:
          description: Name is the role name to assign.
          type: string
      type: object
    o11y.O11ySignalFilters:
      properties:
        filters:
          description: Filters are the attributes offered, in display order.
          items:
            $ref: '#/components/schemas/o11y.O11yFilterKey'
          type: array
        signal:
          description: Signal is the signal the filters belong to.
          type: string
      type: object
    o11y.O11ySignalFiltersOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11ySignalFilters'
          description: Data is the signal's entry.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySpanMapperCreateIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.SpanMapperConfig'
        enabled:
          type: boolean
        fieldContext: {}
        name:
          type: string
      type: object
    o11y.O11ySpanMapperGroupOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.SpanMapperGroup'
          description: Data is the group.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySpanMapperGroupUpdateIn:
      properties:
        condition:
          $ref: '#/components/schemas/o11y.SpanMapperGroupCondition'
        enabled:
          type: boolean
        name:
          type: string
      type: object
    o11y.O11ySpanMapperGroupsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableSpanMapperGroups'
          description: Data holds the groups.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySpanMapperOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.SpanMapper'
          description: Data is the mapper.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySpanMapperUpdateIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.SpanMapperConfig'
        enabled:
          type: boolean
        fieldContext: {}
      type: object
    o11y.O11ySpanMappersOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableSpanMappers'
          description: Data holds the mappers.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySpanPercentile:
      properties:
        percentiles:
          $ref: '#/components/schemas/o11y.O11yPercentiles'
          description: Percentiles are the peer group's duration percentiles.
        position:
          $ref: '#/components/schemas/o11y.O11yPercentilePosition'
          description: Position is where the given duration lands.
      type: object
    o11y.O11ySpanPercentileIn:
      properties:
        end:
          description: End is the window end, as epoch nanoseconds.
          type: integer
        name:
          description: Name is the span name whose peers are compared. Required.
          type: string
        resourceAttributes:
          additionalProperties:
            type: string
          description: ResourceAttributes narrow the peer group to spans carrying
            them all.
          type: object
        serviceName:
          description: ServiceName is the service the span belongs to. Required.
          type: string
        spanDuration:
          description: SpanDuration is the span's duration in nanoseconds.
          type: integer
        start:
          description: Start is the window start, as epoch nanoseconds.
          type: integer
      required:
      - name
      - serviceName
      type: object
    o11y.O11ySpanPercentileOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11ySpanPercentile'
          description: Data is the placement.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yStat:
      properties:
        time:
          description: Time is the start of the bucket.
          format: date-time
          type: string
        value:
          description: Value is how many events fell in it.
          type: integer
      type: object
    o11y.O11yStatefulSetListOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.StatefulSetListResponse'
          description: Data holds the statefulset records.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yStats:
      properties:
        items:
          description: Items are the buckets, oldest first.
          items:
            $ref: '#/components/schemas/o11y.O11yStat'
          type: array
      type: object
    o11y.O11yStatsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yStats'
          description: Data holds the buckets.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11ySubstituteVarsOut:
      properties:
        data:
          description: Data is the resolved composite query, with its variables substituted.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTable:
      properties:
        columns:
          description: Columns names each position in a row.
          items:
            type: string
          type: array
        rows:
          description: Rows are the result rows, each as long as Columns.
          items:
            items:
              type: object
            type: array
          type: array
      type: object
    o11y.O11yTagFilter:
      properties:
        boolValues:
          description: BoolValues are the values matched when the tag holds booleans.
          items:
            type: boolean
          type: array
        key:
          description: Key is the tag to test.
          type: string
        numberValues:
          description: NumberValues are the values matched when the tag holds numbers.
          items:
            type: number
          type: array
        operator:
          description: |-
            Operator is the comparison to apply — in, not_in, equals, contains and
            the other operators the trace filter grammar names.
          type: string
        stringValues:
          description: StringValues are the values matched when the tag holds strings.
          items:
            type: string
          type: array
        tagType:
          description: 'TagType says which kind of value the tag holds: string, number
            or bool.'
          type: string
      type: object
    o11y.O11yTagQuery:
      properties:
        boolValues:
          description: BoolValues are the boolean values to test against.
          items:
            type: boolean
          type: array
        key:
          description: Key is the tag to test.
          type: string
        numberValues:
          description: NumberValues are the numeric values to test against.
          items:
            type: number
          type: array
        operator:
          description: Operator is the comparison, e.g. in, nin, contains, exists.
          type: string
        stringValues:
          description: StringValues are the string values to test against.
          items:
            type: string
          type: array
        tagType:
          description: TagType is where the tag lives, e.g. ResourceAttribute, SpanAttribute.
          type: string
      type: object
    o11y.O11yTelemetryField:
      properties:
        dataType:
          description: DataType is the field's data type, e.g. string, int64, float64,
            bool.
          type: string
        name:
          description: Name is the field's name.
          type: string
        type:
          description: 'Type is where the field lives: attributes or resources.'
          type: string
      type: object
    o11y.O11yTestNotificationOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yTestNotificationResult'
          description: Data holds the fired-series count and a status message.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTestNotificationResult:
      properties:
        alertCount:
          description: AlertCount is how many series would alert for the tested rule.
          type: integer
        message:
          description: Message is a human-readable status, e.g. "notification sent".
          type: string
      type: object
    o11y.O11yTestRuleOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableTestRule'
          description: Data holds how many series would alert.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yToggle:
      properties:
        enabled:
          description: Enabled says whether the feature is on.
          type: boolean
      type: object
    o11y.O11yToken:
      properties:
        accessToken:
          description: AccessToken authenticates requests until it expires.
          type: string
        expiresIn:
          description: ExpiresIn is the access token's lifetime in seconds.
          type: integer
        refreshToken:
          description: RefreshToken buys the next pair via rotateSession.
          type: string
        tokenType:
          description: TokenType is how to present the access token, e.g. bearer.
          type: string
      type: object
    o11y.O11yTokenOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yToken'
          description: Data is the pair.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTopLevelOpsIn:
      properties:
        end:
          description: |-
            End is the window's end, epoch nanoseconds as a string; empty means
            unbounded.
          type: string
        service:
          description: Service narrows the map to one service when set.
          type: string
        start:
          description: |-
            Start is the window's start, epoch nanoseconds as a string; empty means
            unbounded.
          type: string
      type: object
    o11y.O11yTrace:
      properties:
        count:
          description: Count is how many captured errors carried it.
          type: integer
        firstSeen:
          description: FirstSeen is when the earliest of them was recorded.
          format: date-time
          type: string
        lastSeen:
          description: LastSeen is when the latest was.
          format: date-time
          type: string
        message:
          description: Message is the latest error message seen on the trace.
          type: string
        traceId:
          description: TraceID is the trace id.
          type: string
      type: object
    o11y.O11yTraceAggregationsIn:
      properties:
        aggregations:
          items:
            $ref: '#/components/schemas/o11y.SpanAggregation'
          type: array
      type: object
    o11y.O11yTraceAggregationsOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableTraceAggregations'
          description: Data holds one result per aggregation asked for.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTraceDetail:
      properties:
        events:
          description: Events are the error events carrying the trace id.
          items:
            $ref: '#/components/schemas/o11y.O11yEvent'
          type: array
        traceId:
          description: TraceID is the trace that was read.
          type: string
      type: object
    o11y.O11yTraceFlamegraphIn:
      properties:
        selectFields:
          items:
            $ref: '#/components/schemas/o11y.TelemetryFieldKey'
          type: array
        selectedSpanId:
          type: string
      type: object
    o11y.O11yTraceFlamegraphOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableFlamegraphTrace'
          description: Data holds the flamegraph.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTraceOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yTraceDetail'
          description: Data holds the trace and its events.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTraceSpanWindow:
      properties:
        columns:
          description: Columns names the fields each row carries, in row order.
          items:
            type: string
          type: array
        endTimestampMillis:
          description: EndTimestampMillis is when it closes.
          type: integer
        events:
          description: Events are the rows, each positionally matching Columns.
          items:
            items:
              type: object
            type: array
          type: array
        isSubTree:
          description: |-
            IsSubTree says the window is a subtree of the trace rather than the whole
            of it.
          type: boolean
        startTimestampMillis:
          description: StartTimestampMillis is when the window opens.
          type: integer
      type: object
    o11y.O11yTraceWaterfallIn:
      properties:
        selectedSpanId:
          type: string
        uncollapsedSpans:
          items:
            type: string
          type: array
      type: object
    o11y.O11yTraceWaterfallOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.GettableWaterfallTrace'
          description: Data holds the waterfall.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTraces:
      properties:
        items:
          description: Items are the traces, most recent first.
          items:
            $ref: '#/components/schemas/o11y.O11yTrace'
          type: array
      type: object
    o11y.O11yTracesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yTraces'
          description: Data holds the traces.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yTransaction:
      properties:
        object:
          $ref: '#/components/schemas/o11y.O11yObject'
          description: Object is the resource the verb would act on.
        relation:
          description: Relation is the verb being asked about, e.g. read, create,
            update, delete.
          type: string
      type: object
    o11y.O11yTransactionGroup:
      properties:
        objectGroup:
          $ref: '#/components/schemas/o11y.O11yObjectGroup'
          description: ObjectGroup is the set of objects it allows the verb on.
        relation:
          description: Relation is the verb the grant allows.
          type: string
      type: object
    o11y.O11yTransactionResult:
      properties:
        authorized:
          description: Authorized says whether the caller may do it.
          type: boolean
        object:
          $ref: '#/components/schemas/o11y.O11yObject'
          description: Object is the resource it would act on.
        relation:
          description: Relation is the verb that was asked about.
          type: string
      type: object
    o11y.O11yTreemapEntry:
      properties:
        metricName:
          description: MetricName is the metric's name.
          type: string
        percentage:
          description: Percentage is the metric's share, in percent.
          type: number
        totalValue:
          description: TotalValue is the metric's absolute count.
          type: integer
      type: object
    o11y.O11yUpdatableAuthDomain:
      properties:
        config:
          $ref: '#/components/schemas/o11y.O11yAuthDomainConfig'
          description: Config is the SSO configuration to store.
      type: object
    o11y.O11yUpdatablePreference:
      properties:
        value:
          description: |-
            Value is the value to set; its JSON type must match the preference's
            declared value type.
          type: object
      type: object
    o11y.O11yUpdatableQuickFilters:
      properties:
        filters:
          description: Filters are the attributes to offer, in the order to offer
            them.
          items:
            $ref: '#/components/schemas/o11y.O11yFilterKey'
          type: array
        signal:
          description: Signal is the signal whose filters are being replaced.
          type: string
      type: object
    o11y.O11yUpdatableUser:
      properties:
        displayName:
          description: DisplayName is the new display name.
          type: string
      type: object
    o11y.O11yUpdateAccountIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.UpdatableAccountConfig'
      type: object
    o11y.O11yUpdateIngestionKeyIn:
      properties:
        expires_at:
          format: date-time
          type: string
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.O11yUpdateLimitIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.LimitConfig'
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.O11yUpdateServiceIn:
      properties:
        config:
          $ref: '#/components/schemas/o11y.ServiceConfig'
      type: object
    o11y.O11yUsageItem:
      properties:
        count:
          description: Count is how many spans were ingested in the bucket.
          type: integer
        time:
          description: Time is the bucket start.
          format: date-time
          type: string
        timestamp:
          description: Timestamp is the bucket start, as epoch nanoseconds.
          type: integer
      type: object
    o11y.O11yUser:
      properties:
        createdAt:
          description: CreatedAt is when they joined.
          format: date-time
          type: string
        displayName:
          description: DisplayName is what the console shows for them.
          type: string
        email:
          description: Email is their address.
          type: string
        id:
          description: ID is the user id.
          type: string
        isRoot:
          description: IsRoot marks the org's root user, which cannot be deleted or
            demoted.
          type: boolean
        orgId:
          description: OrgID is the org they belong to.
          type: string
        status:
          description: Status is their lifecycle state — active, pending_invite or
            deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when their record last changed.
          format: date-time
          type: string
      type: object
    o11y.O11yUserRole:
      properties:
        createdAt:
          description: CreatedAt is when it was assigned.
          format: date-time
          type: string
        id:
          description: ID is the assignment's own id.
          type: string
        role:
          $ref: '#/components/schemas/o11y.O11yRole'
          description: Role is the role itself.
        roleId:
          description: RoleID is the role held.
          type: string
        updatedAt:
          description: UpdatedAt is when the assignment last changed.
          format: date-time
          type: string
        userId:
          description: UserID is the user holding the role.
          type: string
      type: object
    o11y.O11yUserUpdate:
      properties:
        displayName:
          description: DisplayName is the new display name.
          type: string
      type: object
    o11y.O11yUserWithRoles:
      properties:
        createdAt:
          description: CreatedAt is when they joined.
          format: date-time
          type: string
        displayName:
          description: DisplayName is what the console shows for them.
          type: string
        email:
          description: Email is their address.
          type: string
        id:
          description: ID is the user id.
          type: string
        isRoot:
          description: IsRoot marks the org's root user.
          type: boolean
        orgId:
          description: OrgID is the org they belong to.
          type: string
        status:
          description: Status is their lifecycle state — active, pending_invite or
            deleted.
          type: string
        updatedAt:
          description: UpdatedAt is when their record last changed.
          format: date-time
          type: string
        userRoles:
          description: UserRoles are their role assignments.
          items:
            $ref: '#/components/schemas/o11y.O11yUserRole'
          type: array
      type: object
    o11y.O11yUserWithRolesOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yUserWithRoles'
          description: Data is the member.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yUsersOut:
      properties:
        data:
          description: Data holds the members.
          items:
            $ref: '#/components/schemas/o11y.O11yUser'
          type: array
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.O11yVersionOut:
      properties:
        ee:
          description: EE says whether an enterprise edition is present; "N" in this
            build.
          type: string
        setupCompleted:
          description: SetupCompleted says whether the first user has been created.
          type: boolean
        version:
          description: Version is the build version.
          type: string
      type: object
    o11y.O11yWidgetQueryRange:
      properties:
        data:
          description: Data is the query result payload, carried verbatim as open
            JSON.
        meta:
          description: Meta is the execution stats, carried verbatim as open JSON.
        type:
          description: Type is the request type the result answers, e.g. time_series,
            scalar.
          type: string
        warning:
          description: Warning is any query warning, carried verbatim as open JSON
            when present.
      type: object
    o11y.O11yWidgetQueryRangeOut:
      properties:
        data:
          $ref: '#/components/schemas/o11y.O11yWidgetQueryRange'
          description: Data is the query-range result.
        status:
          description: Status is "success".
          type: string
      type: object
    o11y.OAuth2:
      properties:
        TLSConfig:
          $ref: '#/components/schemas/o11y.TLSConfig'
        audience:
          description: |-
            Audience optionally specifies the intended audience of the
            request.  If empty, the value of TokenURL is used as the
            intended audience. Only used if
            GrantType is set to "urn:ietf:params:oauth:grant-type:jwt-bearer".
          type: string
        claims:
          additionalProperties:
            type: object
          description: |-
            Claims is a map of claims to be added to the JWT token. Only used if
            GrantType is set to "urn:ietf:params:oauth:grant-type:jwt-bearer".
          type: object
        client_certificate_key: {}
        client_certificate_key_file:
          type: string
        client_certificate_key_id:
          type: string
        client_certificate_key_ref:
          description: |-
            ClientCertificateKeyRef is the name of the secret within the secret manager to use as the client
            secret.
          type: string
        client_id:
          type: string
        client_secret: {}
        client_secret_file:
          type: string
        client_secret_ref:
          description: |-
            ClientSecretRef is the name of the secret within the secret manager to use as the client
            secret.
          type: string
        endpoint_params:
          additionalProperties:
            type: string
          type: object
        grant_type:
          description: |-
            GrantType is the OAuth2 grant type to use. It can be one of
            "client_credentials" or "urn:ietf:params:oauth:grant-type:jwt-bearer" (RFC 7523).
            Default value is "client_credentials"
          type: string
        iss:
          description: |-
            Iss is the OAuth client identifier used when communicating with
            the configured OAuth provider. Default value is client_id. Only used if
            GrantType is set to "urn:ietf:params:oauth:grant-type:jwt-bearer".
          type: string
        no_proxy:
          type: string
        proxy_connect_header:
          additionalProperties:
            items: {}
            type: array
          type: object
        proxy_from_environment:
          type: boolean
        proxy_url: {}
        scopes:
          items:
            type: string
          type: array
        signature_algorithm:
          description: |-
            SignatureAlgorithm is the RSA algorithm used to sign JWT token. Only used if
            GrantType is set to "urn:ietf:params:oauth:grant-type:jwt-bearer".
            Default value is RS256 and valid values RS256, RS384, RS512
          type: string
        token_url:
          type: string
      type: object
    o11y.OldAWSCollectionStrategy:
      properties:
        aws_logs:
          $ref: '#/components/schemas/o11y.OldAWSLogsStrategy'
        aws_metrics:
          $ref: '#/components/schemas/o11y.OldAWSMetricsStrategy'
        provider:
          type: string
        s3_buckets:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
      type: object
    o11y.OldAWSLogsStrategy:
      properties:
        cloudwatch_logs_subscriptions:
          items:
            properties:
              filter_pattern:
                type: string
              log_group_name_prefix:
                type: string
            type: object
          type: array
      type: object
    o11y.OldAWSMetricsStrategy:
      properties:
        cloudwatch_metric_stream_filters:
          items:
            properties:
              MetricNames:
                items:
                  type: string
                type: array
              Namespace:
                type: string
            type: object
          type: array
      type: object
    o11y.OpsGenieConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        actions:
          type: string
        api_key: {}
        api_key_file:
          type: string
        api_url: {}
        description:
          type: string
        details:
          additionalProperties:
            type: string
          type: object
        entity:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        note:
          type: string
        priority:
          type: string
        responders:
          items:
            $ref: '#/components/schemas/o11y.OpsGenieConfigResponder'
          type: array
        source:
          type: string
        tags:
          type: string
        update_alerts:
          type: boolean
      type: object
    o11y.OpsGenieConfigResponder:
      properties:
        id:
          description: One of those 3 should be filled.
          type: string
        name:
          type: string
        type:
          description: team, user, escalation, schedule etc.
          type: string
        username:
          type: string
      type: object
    o11y.OrderBy:
      properties:
        columnName:
          type: string
        order:
          type: string
      type: object
    o11y.OrderByKey:
      properties:
        description:
          type: string
        fieldContext: {}
        fieldDataType: {}
        name:
          type: string
        signal: {}
        unit:
          type: string
      required:
      - name
      type: object
    o11y.OtelSpanRef:
      properties:
        refType:
          type: string
        spanId:
          type: string
        traceId:
          type: string
      type: object
    o11y.PagerdutyConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        class:
          type: string
        client:
          type: string
        client_url:
          type: string
        component:
          type: string
        description:
          type: string
        details:
          additionalProperties:
            type: object
          type: object
        group:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        images:
          items:
            $ref: '#/components/schemas/o11y.PagerdutyImage'
          type: array
        links:
          items:
            $ref: '#/components/schemas/o11y.PagerdutyLink'
          type: array
        routing_key: {}
        routing_key_file:
          type: string
        service_key: {}
        service_key_file:
          type: string
        severity:
          type: string
        source:
          type: string
        timeout:
          description: |-
            Timeout is the maximum time allowed to invoke the pagerduty. Setting this to 0
            does not impose a timeout.
          type: integer
        url: {}
      type: object
    o11y.PagerdutyImage:
      properties:
        alt:
          type: string
        href:
          type: string
        src:
          type: string
      type: object
    o11y.PagerdutyLink:
      properties:
        href:
          type: string
        text:
          type: string
      type: object
    o11y.Pagination:
      properties:
        page:
          type: integer
        pages:
          type: integer
        per_page:
          type: integer
        total:
          type: integer
      type: object
    o11y.PodCountByPhase:
      properties:
        failed:
          type: integer
        pending:
          type: integer
        running:
          type: integer
        succeeded:
          type: integer
        unknown:
          type: integer
      type: object
    o11y.PodCountsByPhase:
      properties:
        failed:
          type: integer
        pending:
          type: integer
        running:
          type: integer
        succeeded:
          type: integer
        unknown:
          type: integer
      type: object
    o11y.PodListRecord:
      properties:
        countByPhase:
          $ref: '#/components/schemas/o11y.PodCountByPhase'
        meta:
          additionalProperties:
            type: string
          type: object
        podCPU:
          type: number
        podCPULimit:
          type: number
        podCPURequest:
          type: number
        podMemory:
          type: number
        podMemoryLimit:
          type: number
        podMemoryRequest:
          type: number
        podUID:
          type: string
        restartCount:
          type: integer
      type: object
    o11y.PodListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.PodListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.PodListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.PodRecord:
      properties:
        meta:
          additionalProperties:
            type: string
          type: object
        podAge:
          type: integer
        podCPU:
          type: number
        podCPULimit:
          type: number
        podCPURequest:
          type: number
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
        podMemory:
          type: number
        podMemoryLimit:
          type: number
        podMemoryRequest:
          type: number
        podPhase: {}
        podUID:
          type: string
      type: object
    o11y.Pods:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.PodRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.PostableAccountConfig:
      properties:
        AgentVersion:
          description: as agent version is common for all providers, we can keep it
            at top level of this struct
          type: string
        aws:
          $ref: '#/components/schemas/o11y.AWSPostableAccountConfig'
        azure:
          $ref: '#/components/schemas/o11y.AzureAccountConfig'
        gcp:
          $ref: '#/components/schemas/o11y.GCPAccountConfig'
      type: object
    o11y.PostableChannel:
      properties:
        Receiver:
          $ref: '#/components/schemas/o11y.Receiver'
        googlechat_configs:
          items:
            $ref: '#/components/schemas/o11y.GoogleChatReceiverConfig'
          type: array
      type: object
    o11y.PostableClusters:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableDaemonSets:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableDeployments:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableHost:
      properties:
        name:
          type: string
      type: object
    o11y.PostableHosts:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.HostFilter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableIngestionKey:
      properties:
        expires_at:
          format: date-time
          type: string
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.PostableJobs:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableNamespaces:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableNodes:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostablePipeline:
      properties:
        alias:
          type: string
        config:
          items: {}
          type: array
        description:
          type: string
        enabled:
          type: boolean
        filter:
          $ref: '#/components/schemas/o11y.FilterSet'
        id:
          type: string
        name:
          type: string
        orderId:
          type: integer
      type: object
    o11y.PostablePlannedMaintenance:
      properties:
        alertIds:
          items:
            type: string
          type: array
        description:
          type: string
        name:
          type: string
        schedule: {}
        scope:
          type: string
      type: object
    o11y.PostablePods:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableProfile:
      properties:
        existing_observability_tool:
          type: string
        has_existing_observability_tool:
          type: boolean
        logs_scale_per_day_in_gb:
          type: integer
        number_of_hosts:
          type: integer
        number_of_services:
          type: integer
        reasons_for_interest_in_o11y:
          items:
            type: string
          type: array
        timeline_for_migrating_to_o11y:
          type: string
        uses_otel:
          type: boolean
        where_did_you_discover_o11y:
          type: string
      type: object
    o11y.PostableRoutePolicy:
      properties:
        channels:
          items:
            type: string
          type: array
        description:
          type: string
        expression:
          type: string
        kind: {}
        name:
          type: string
        tags:
          items:
            type: string
          type: array
      type: object
    o11y.PostableSpanMapperGroup:
      properties:
        condition:
          $ref: '#/components/schemas/o11y.SpanMapperGroupCondition'
        enabled:
          type: boolean
        name:
          type: string
      type: object
    o11y.PostableStatefulSets:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.PostableVolumes:
      properties:
        end:
          type: integer
        filter:
          $ref: '#/components/schemas/o11y.Filter'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.GroupByKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.OrderBy'
        start:
          type: integer
      type: object
    o11y.ProcessListRecord:
      properties:
        meta:
          additionalProperties:
            type: string
          type: object
        processCMD:
          type: string
        processCMDLine:
          type: string
        processCPU:
          type: number
        processID:
          type: string
        processMemory:
          type: number
        processName:
          type: string
      type: object
    o11y.ProcessListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.ProcessListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.ProcessListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.PromQuery:
      properties:
        disabled:
          type: boolean
        legend:
          type: string
        query:
          type: string
        stats:
          type: string
      type: object
    o11y.ProviderIntegrationConfig:
      properties:
        aws:
          $ref: '#/components/schemas/o11y.AWSIntegrationConfig'
        azure:
          $ref: '#/components/schemas/o11y.AzureIntegrationConfig'
        gcp:
          $ref: '#/components/schemas/o11y.GCPIntegrationConfig'
      type: object
    o11y.PushoverConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        device:
          type: string
        expire:
          type: integer
        html:
          type: boolean
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        monospace:
          type: boolean
        priority:
          type: string
        retry:
          type: integer
        sound:
          type: string
        title:
          type: string
        token: {}
        token_file:
          type: string
        ttl:
          type: integer
        url:
          type: string
        url_title:
          type: string
        user_key: {}
        user_key_file:
          type: string
      type: object
    o11y.QueryEnvelope:
      properties:
        spec:
          description: Spec is the deferred decoding of the query if any.
          type: object
        type:
          description: Type is the type of the query.
      type: object
    o11y.QueryRangeParamsV3:
      properties:
        compositeQuery:
          $ref: '#/components/schemas/o11y.CompositeQuery'
        end:
          type: integer
        formatForWeb:
          type: boolean
        noCache:
          type: boolean
        start:
          type: integer
        step:
          description: step is in seconds; used for prometheus queries
          type: integer
        variables:
          additionalProperties:
            type: object
          type: object
      type: object
    o11y.QueryRangeRequest:
      properties:
        compositeQuery:
          $ref: '#/components/schemas/o11y.querybuildertypesv5.CompositeQuery'
          description: CompositeQuery is the composite query to use for the request.
        end:
          description: End is the end time of the query in epoch milliseconds.
          type: integer
        formatOptions:
          $ref: '#/components/schemas/o11y.FormatOptions'
        noCache:
          description: NoCache is a flag to disable caching for the request.
          type: boolean
        requestType:
          description: RequestType is the type of the request.
        schemaVersion:
          description: SchemaVersion is the version of the schema to use for the request
            payload.
          type: string
        start:
          description: Start is the start time of the query in epoch milliseconds.
          type: integer
        variables:
          additionalProperties:
            $ref: '#/components/schemas/o11y.VariableItem'
          description: Variables is the variables to use for the request.
          type: object
      type: object
    o11y.QueryWarnData:
      properties:
        message:
          type: string
        url:
          type: string
        warnings:
          items:
            $ref: '#/components/schemas/o11y.QueryWarnDataAdditional'
          type: array
      type: object
    o11y.QueryWarnDataAdditional:
      properties:
        message:
          type: string
      type: object
    o11y.Receiver:
      properties:
        discord_configs:
          items:
            $ref: '#/components/schemas/o11y.DiscordConfig'
          type: array
        email_configs:
          items:
            $ref: '#/components/schemas/o11y.EmailConfig'
          type: array
        incidentio_configs:
          items:
            $ref: '#/components/schemas/o11y.IncidentioConfig'
          type: array
        jira_configs:
          items:
            $ref: '#/components/schemas/o11y.JiraConfig'
          type: array
        mattermost_configs:
          items:
            $ref: '#/components/schemas/o11y.MattermostConfig'
          type: array
        msteams_configs:
          items:
            $ref: '#/components/schemas/o11y.MSTeamsConfig'
          type: array
        msteamsv2_configs:
          items:
            $ref: '#/components/schemas/o11y.MSTeamsV2Config'
          type: array
        name:
          description: A unique identifier for this receiver.
          type: string
        opsgenie_configs:
          items:
            $ref: '#/components/schemas/o11y.OpsGenieConfig'
          type: array
        pagerduty_configs:
          items:
            $ref: '#/components/schemas/o11y.PagerdutyConfig'
          type: array
        pushover_configs:
          items:
            $ref: '#/components/schemas/o11y.PushoverConfig'
          type: array
        rocketchat_configs:
          items:
            $ref: '#/components/schemas/o11y.RocketchatConfig'
          type: array
        slack_configs:
          items:
            $ref: '#/components/schemas/o11y.SlackConfig'
          type: array
        sns_configs:
          items:
            $ref: '#/components/schemas/o11y.SNSConfig'
          type: array
        telegram_configs:
          items:
            $ref: '#/components/schemas/o11y.TelegramConfig'
          type: array
        victorops_configs:
          items:
            $ref: '#/components/schemas/o11y.VictorOpsConfig'
          type: array
        webex_configs:
          items:
            $ref: '#/components/schemas/o11y.WebexConfig'
          type: array
        webhook_configs:
          items:
            $ref: '#/components/schemas/o11y.WebhookConfig'
          type: array
        wechat_configs:
          items:
            $ref: '#/components/schemas/o11y.WechatConfig'
          type: array
      type: object
    o11y.ReleStateItem:
      properties:
        end:
          type: integer
        start:
          type: integer
        state: {}
      type: object
    o11y.RocketchatAttachmentAction:
      properties:
        image_url:
          type: string
        is_webview:
          type: boolean
        msg:
          type: string
        msg_in_chat_window:
          type: boolean
        msg_processing_type:
          type: string
        text:
          type: string
        type:
          type: string
        url:
          type: string
        webview_height_ratio:
          type: string
      type: object
    o11y.RocketchatAttachmentField:
      properties:
        short:
          type: boolean
        title:
          type: string
        value:
          type: string
      type: object
    o11y.RocketchatConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        actions:
          items:
            $ref: '#/components/schemas/o11y.RocketchatAttachmentAction'
          type: array
        api_url: {}
        channel:
          description: 'RocketChat channel override, (like #other-channel or @username).'
          type: string
        color:
          type: string
        emoji:
          type: string
        fields:
          items:
            $ref: '#/components/schemas/o11y.RocketchatAttachmentField'
          type: array
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        icon_url:
          type: string
        image_url:
          type: string
        link_names:
          type: boolean
        short_fields:
          type: boolean
        text:
          type: string
        thumb_url:
          type: string
        title:
          type: string
        title_link:
          type: string
        token: {}
        token_file:
          type: string
        token_id: {}
        token_id_file:
          type: string
      type: object
    o11y.RuleStateHistory:
      properties:
        fingerprint:
          type: integer
        labels: {}
        overallState:
          description: One of ["normal", "firing"]
        overallStateChanged:
          type: boolean
        relatedLogsLink:
          type: string
        relatedTracesLink:
          type: string
        ruleID:
          type: string
        ruleName:
          type: string
        state:
          description: One of ["normal", "firing", "nodata", "muted"]
        stateChanged:
          type: boolean
        unixMilli:
          type: integer
        value:
          type: number
      type: object
    o11y.RuleStateHistoryContributor:
      properties:
        count:
          type: integer
        fingerprint:
          type: integer
        labels: {}
        relatedLogsLink:
          type: string
        relatedTracesLink:
          type: string
      type: object
    o11y.RuleStateTimeline:
      properties:
        items:
          items:
            $ref: '#/components/schemas/o11y.RuleStateHistory'
          type: array
        labels:
          additionalProperties:
            items:
              type: string
            type: array
          type: object
        total:
          type: integer
      type: object
    o11y.SNSConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        api_url:
          type: string
        attributes:
          additionalProperties:
            type: string
          type: object
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        phone_number:
          type: string
        sigv4:
          $ref: '#/components/schemas/o11y.SigV4Config'
        subject:
          type: string
        target_arn:
          type: string
        topic_arn:
          type: string
      type: object
    o11y.SavedView:
      properties:
        category:
          type: string
        compositeQuery:
          $ref: '#/components/schemas/o11y.CompositeQuery'
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        extraData:
          description: ExtraData is JSON encoded data used by frontend to store additional
            data
          type: string
        id: {}
        name:
          type: string
        sourcePage:
          type: string
        tags:
          items:
            type: string
          type: array
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
      type: object
    o11y.Series:
      properties:
        labels:
          additionalProperties:
            type: string
          type: object
        labelsArray:
          items:
            additionalProperties:
              type: string
            type: object
          type: array
        values:
          items: {}
          type: array
      type: object
    o11y.Service:
      properties:
        assets:
          $ref: '#/components/schemas/o11y.ServiceAssets'
        cloudIntegrationService:
          $ref: '#/components/schemas/o11y.CloudIntegrationService'
        dataCollected:
          $ref: '#/components/schemas/o11y.DataCollected'
        icon:
          type: string
        id:
          type: string
        overview:
          description: markdown
          type: string
        supportedSignals:
          $ref: '#/components/schemas/o11y.SupportedSignals'
        title:
          type: string
      type: object
    o11y.ServiceAssets:
      properties:
        dashboards:
          items:
            $ref: '#/components/schemas/o11y.ServiceDashboard'
          type: array
      type: object
    o11y.ServiceConfig:
      properties:
        aws:
          $ref: '#/components/schemas/o11y.AWSServiceConfig'
        azure:
          $ref: '#/components/schemas/o11y.AzureServiceConfig'
        gcp:
          $ref: '#/components/schemas/o11y.GCPServiceConfig'
      type: object
    o11y.ServiceDashboard:
      properties:
        description:
          type: string
        integrationDashboard:
          $ref: '#/components/schemas/o11y.StorableIntegrationDashboard'
        title:
          type: string
      type: object
    o11y.ServiceMetadata:
      properties:
        enabled:
          description: if the service is enabled for the account
          type: boolean
        icon:
          type: string
        id:
          type: string
        title:
          type: string
      type: object
    o11y.SigV4Config:
      properties:
        AccessKey:
          type: string
        ExternalID:
          type: string
        Profile:
          type: string
        Region:
          type: string
        RoleARN:
          type: string
        SecretKey: {}
        ServiceName:
          type: string
        UseFIPSSTSEndpoint:
          type: boolean
      type: object
    o11y.SignalConnectionStatus:
      properties:
        last_received_from:
          description: resource identifier
          type: string
        last_received_ts_ms:
          description: epoch milliseconds
          type: integer
      type: object
    o11y.SlackAction:
      properties:
        confirm:
          $ref: '#/components/schemas/o11y.SlackConfirmationField'
        name:
          type: string
        style:
          type: string
        text:
          type: string
        type:
          type: string
        url:
          type: string
        value:
          type: string
      type: object
    o11y.SlackConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        actions:
          items:
            $ref: '#/components/schemas/o11y.SlackAction'
          type: array
        api_url: {}
        api_url_file:
          type: string
        app_token: {}
        app_token_file:
          type: string
        app_url: {}
        callback_id:
          type: string
        channel:
          description: 'Slack channel override, (like #other-channel or @username).'
          type: string
        color:
          type: string
        fallback:
          type: string
        fields:
          items:
            $ref: '#/components/schemas/o11y.SlackField'
          type: array
        footer:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        icon_emoji:
          type: string
        icon_url:
          type: string
        image_url:
          type: string
        link_names:
          type: boolean
        message_text:
          type: string
        mrkdwn_in:
          items:
            type: string
          type: array
        pretext:
          type: string
        short_fields:
          type: boolean
        text:
          type: string
        thumb_url:
          type: string
        timeout:
          description: |-
            Timeout is the maximum time allowed to invoke the slack. Setting this to 0
            does not impose a timeout.
          type: integer
        title:
          type: string
        title_link:
          type: string
        username:
          type: string
      type: object
    o11y.SlackConfirmationField:
      properties:
        dismiss_text:
          type: string
        ok_text:
          type: string
        text:
          type: string
        title:
          type: string
      type: object
    o11y.SlackField:
      properties:
        short:
          type: boolean
        title:
          type: string
        value:
          type: string
      type: object
    o11y.SpanAggregation:
      properties:
        aggregation: {}
        field:
          $ref: '#/components/schemas/o11y.TelemetryFieldKey'
      type: object
    o11y.SpanAggregationResult:
      properties:
        aggregation: {}
        field:
          $ref: '#/components/schemas/o11y.TelemetryFieldKey'
        value:
          additionalProperties:
            type: integer
          type: object
      type: object
    o11y.SpanMapper:
      properties:
        config:
          $ref: '#/components/schemas/o11y.SpanMapperConfig'
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        enabled:
          type: boolean
        fieldContext: {}
        group_id: {}
        id: {}
        name:
          type: string
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
      type: object
    o11y.SpanMapperConfig:
      properties:
        sources:
          items:
            $ref: '#/components/schemas/o11y.SpanMapperSource'
          type: array
      type: object
    o11y.SpanMapperGroup:
      properties:
        condition:
          $ref: '#/components/schemas/o11y.SpanMapperGroupCondition'
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        enabled:
          type: boolean
        id: {}
        name:
          type: string
        orgId: {}
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
      type: object
    o11y.SpanMapperGroupCondition:
      properties:
        attributes:
          items:
            type: string
          type: array
        resource:
          items:
            type: string
          type: array
      type: object
    o11y.SpanMapperSource:
      properties:
        context: {}
        key:
          type: string
        operation: {}
        priority:
          type: integer
      type: object
    o11y.StatefulSetListRecord:
      properties:
        availablePods:
          type: integer
        cpuLimit:
          type: number
        cpuRequest:
          type: number
        cpuUsage:
          type: number
        desiredPods:
          type: integer
        memoryLimit:
          type: number
        memoryRequest:
          type: number
        memoryUsage:
          type: number
        meta:
          additionalProperties:
            type: string
          type: object
        restarts:
          type: integer
        statefulSetName:
          type: string
      type: object
    o11y.StatefulSetListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.StatefulSetListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.StatefulSetListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.StatefulSetRecord:
      properties:
        currentPods:
          type: integer
        desiredPods:
          type: integer
        meta:
          additionalProperties:
            type: string
          type: object
        podCountsByPhase:
          $ref: '#/components/schemas/o11y.PodCountsByPhase'
        statefulSetCPU:
          type: number
        statefulSetCPULimit:
          type: number
        statefulSetCPURequest:
          type: number
        statefulSetMemory:
          type: number
        statefulSetMemoryLimit:
          type: number
        statefulSetMemoryRequest:
          type: number
        statefulSetName:
          type: string
      type: object
    o11y.StatefulSets:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.StatefulSetRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.Stats:
      properties:
        currentAvgResolutionTime:
          type: string
        currentAvgResolutionTimeSeries:
          $ref: '#/components/schemas/o11y.Series'
        currentTriggersSeries:
          $ref: '#/components/schemas/o11y.Series'
        pastAvgResolutionTime:
          type: string
        pastAvgResolutionTimeSeries:
          $ref: '#/components/schemas/o11y.Series'
        pastTriggersSeries:
          $ref: '#/components/schemas/o11y.Series'
        totalCurrentTriggers:
          type: integer
        totalPastTriggers:
          type: integer
      type: object
    o11y.StatusComponent:
      properties:
        current_status:
          description: |-
            CurrentStatus is this component's own condition: "full_outage" for a
            service that did not answer its health probe at all.
          type: string
        id:
          type: string
        name:
          type: string
      type: object
    o11y.StatusIncident:
      properties:
        affected_components:
          items:
            $ref: '#/components/schemas/o11y.StatusComponent'
          type: array
        current_worst_impact:
          description: |-
            CurrentWorstImpact is the incident's impact on the PLATFORM, which is not
            the same question as the component's own condition above.
          type: string
        id:
          type: string
        last_update_at:
          description: |-
            LastUpdateAt is when the failing measurement this incident reports was
            read, RFC3339 UTC.
          type: string
        last_update_message:
          type: string
        name:
          type: string
        status:
          type: string
        url:
          type: string
      type: object
    o11y.StatusMaintenance:
      properties:
        affected_components:
          items:
            $ref: '#/components/schemas/o11y.StatusComponent'
          type: array
        ends_at:
          type: string
        id:
          type: string
        last_update_at:
          type: string
        last_update_message:
          type: string
        name:
          type: string
        starts_at:
          type: string
        status:
          type: string
        url:
          type: string
      type: object
    o11y.StatusSummary:
      properties:
        checked_at:
          description: |-
            CheckedAt is when the underlying availability read was taken, RFC3339 UTC.
            Not part of the status-page schema the panel parses (which ignores unknown
            fields); it is here because a status document with no timestamp cannot be
            told apart from a stale one.
          type: string
        in_progress_maintenances:
          items:
            $ref: '#/components/schemas/o11y.StatusMaintenance'
          type: array
        ongoing_incidents:
          items:
            $ref: '#/components/schemas/o11y.StatusIncident'
          type: array
        page_title:
          type: string
        page_url:
          description: |-
            PageURL is the HUMAN status page — an HTML page for people, distinct from
            this JSON endpoint. Every link in this document points there.
          type: string
        scheduled_maintenances:
          items:
            $ref: '#/components/schemas/o11y.StatusMaintenance'
          type: array
      type: object
    o11y.StorableFunnel:
      properties:
        createdAt:
          format: date-time
          type: string
        createdBy:
          type: string
        description:
          type: string
        funnel_name:
          type: string
        id: {}
        org_id: {}
        steps:
          items:
            $ref: '#/components/schemas/o11y.FunnelStep'
          type: array
        tags:
          type: string
        updatedAt:
          format: date-time
          type: string
        updatedBy:
          type: string
        user:
          $ref: '#/components/schemas/o11y.User'
      type: object
    o11y.StorableIntegrationDashboard:
      properties:
        createdAt:
          format: date-time
          type: string
        dashboardId:
          type: string
        id:
          type: string
        provider: {}
        slug:
          type: string
        updatedAt:
          format: date-time
          type: string
      type: object
    o11y.SupportedSignals:
      properties:
        logs:
          type: boolean
        metrics:
          type: boolean
      type: object
    o11y.TLSConfig:
      properties:
        ca:
          description: Text of the CA cert to use for the targets.
          type: string
        ca_file:
          description: The CA cert to use for the targets.
          type: string
        ca_ref:
          description: |-
            CARef is the name of the secret within the secret manager to use as the CA cert for the
            targets.
          type: string
        cert:
          description: Text of the client cert file for the targets.
          type: string
        cert_file:
          description: The client cert file for the targets.
          type: string
        cert_ref:
          description: |-
            CertRef is the name of the secret within the secret manager to use as the client cert for
            the targets.
          type: string
        insecure_skip_verify:
          description: Disable target certificate validation.
          type: boolean
        key:
          description: Text of the client key file for the targets.
        key_file:
          description: The client key file for the targets.
          type: string
        key_ref:
          description: |-
            KeyRef is the name of the secret within the secret manager to use as the client key for
            the targets.
          type: string
        max_version:
          description: Maximum TLS version.
        min_version:
          description: Minimum TLS version.
        server_name:
          description: Used to verify the hostname for the targets.
          type: string
      type: object
    o11y.TelegramConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        api_url: {}
        chat:
          type: integer
        chat_file:
          type: string
        disable_notifications:
          type: boolean
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        message_thread_id:
          type: integer
        parse_mode:
          type: string
        token: {}
        token_file:
          type: string
      type: object
    o11y.TelemetryFieldKey:
      properties:
        description:
          type: string
        fieldContext: {}
        fieldDataType: {}
        name:
          type: string
        signal: {}
        unit:
          type: string
      required:
      - name
      type: object
    o11y.TelemetryFieldValues:
      properties:
        boolValues:
          items:
            type: boolean
          type: array
        numberValues:
          items:
            type: number
          type: array
        relatedValues:
          items:
            type: string
          type: array
        stringValues:
          items:
            type: string
          type: array
      type: object
    o11y.ThreadingConfig:
      properties:
        enabled:
          type: boolean
        thread_by_date:
          type: string
      type: object
    o11y.TimeSeries:
      properties:
        labels:
          items:
            $ref: '#/components/schemas/o11y.Label'
          type: array
        values:
          items: {}
          type: array
      type: object
    o11y.UninstallIntegrationRequest:
      properties:
        integration_id:
          type: string
      type: object
    o11y.UpdatableAccountConfig:
      properties:
        aws:
          $ref: '#/components/schemas/o11y.AWSAccountConfig'
        azure:
          $ref: '#/components/schemas/o11y.UpdatableAzureAccountConfig'
        gcp:
          $ref: '#/components/schemas/o11y.UpdatableGCPAccountConfig'
      type: object
    o11y.UpdatableAzureAccountConfig:
      properties:
        resourceGroups:
          items:
            type: string
          type: array
      type: object
    o11y.UpdatableGCPAccountConfig:
      properties:
        deploymentProjectId:
          description: Project ID where central pub/sub for logs exist
          type: string
        deploymentRegion:
          description: Compute service region where otel collector will be deployed
          type: string
        projectIds:
          description: List of project IDs to monitor
          items:
            type: string
          type: array
      type: object
    o11y.User:
      properties:
        createdAt:
          format: date-time
          type: string
        displayName:
          type: string
        email: {}
        id: {}
        isRoot:
          type: boolean
        orgId: {}
        status: {}
        updatedAt:
          format: date-time
          type: string
      type: object
    o11y.VariableItem:
      properties:
        type: {}
        value:
          type: object
      type: object
    o11y.VictorOpsConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        api_key: {}
        api_key_file:
          type: string
        api_url: {}
        custom_fields:
          additionalProperties:
            type: string
          type: object
        entity_display_name:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message_type:
          type: string
        monitoring_tool:
          type: string
        routing_key:
          type: string
        state_message:
          type: string
      type: object
    o11y.VolumeListRecord:
      properties:
        meta:
          additionalProperties:
            type: string
          type: object
        persistentVolumeClaimName:
          type: string
        volumeAvailable:
          type: number
        volumeCapacity:
          type: number
        volumeInodes:
          type: number
        volumeInodesFree:
          type: number
        volumeInodesUsed:
          type: number
        volumeUsage:
          type: number
      type: object
    o11y.VolumeListRequest:
      properties:
        end:
          description: epoch time in ms
          type: integer
        filters:
          $ref: '#/components/schemas/o11y.FilterSet'
        groupBy:
          items:
            $ref: '#/components/schemas/o11y.AttributeKey'
          type: array
        limit:
          type: integer
        offset:
          type: integer
        orderBy:
          $ref: '#/components/schemas/o11y.OrderBy'
        start:
          description: epoch time in ms
          type: integer
      type: object
    o11y.VolumeListResponse:
      properties:
        records:
          items:
            $ref: '#/components/schemas/o11y.VolumeListRecord'
          type: array
        total:
          type: integer
        type:
          type: string
      type: object
    o11y.VolumeRecord:
      properties:
        meta:
          additionalProperties:
            type: string
          type: object
        persistentVolumeClaimName:
          type: string
        volumeAvailable:
          type: number
        volumeCapacity:
          type: number
        volumeInodes:
          type: number
        volumeInodesFree:
          type: number
        volumeInodesUsed:
          type: number
        volumeUsage:
          type: number
      type: object
    o11y.Volumes:
      properties:
        endTimeBeforeRetention:
          type: boolean
        records:
          items:
            $ref: '#/components/schemas/o11y.VolumeRecord'
          type: array
        total:
          type: integer
        type: {}
        warning:
          $ref: '#/components/schemas/o11y.QueryWarnData'
      type: object
    o11y.WaterfallSpan:
      properties:
        attributes:
          additionalProperties:
            type: object
          type: object
        db_name:
          description: Calculated fields https://o11y.io/docs/traces-management/guides/derived-fields-spans
          type: string
        db_operation:
          type: string
        duration_nano:
          type: integer
        events:
          items:
            $ref: '#/components/schemas/o11y.Event'
          type: array
        external_http_method:
          type: string
        external_http_url:
          type: string
        flags:
          type: integer
        has_children:
          type: boolean
        has_error:
          type: boolean
        http_host:
          type: string
        http_method:
          type: string
        http_url:
          type: string
        is_remote:
          type: string
        kind_string:
          type: string
        level:
          type: integer
        name:
          type: string
        parent_span_id:
          type: string
        references:
          items:
            $ref: '#/components/schemas/o11y.OtelSpanRef'
          type: array
        resource:
          additionalProperties:
            type: string
          type: object
        response_status_code:
          type: string
        span_id:
          type: string
        status_code:
          type: integer
        status_code_string:
          type: string
        status_message:
          type: string
        sub_tree_node_count:
          type: integer
        time_unix:
          type: integer
        trace_id:
          type: string
        trace_state:
          type: string
      type: object
    o11y.WebexConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        api_url: {}
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        room_id:
          type: string
      type: object
    o11y.WebhookConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        max_alerts:
          description: |-
            MaxAlerts is the maximum number of alerts to be sent per webhook message.
            Alerts exceeding this threshold will be truncated. Setting this to 0
            allows an unlimited number of alerts.
          type: integer
        timeout:
          description: |-
            Timeout is the maximum time allowed to invoke the webhook. Setting this to 0
            does not impose a timeout.
          type: integer
        url:
          description: URL to send POST request to.
        url_file:
          type: string
      type: object
    o11y.WechatConfig:
      properties:
        NotifierConfig:
          $ref: '#/components/schemas/o11y.NotifierConfig'
        agent_id:
          type: string
        api_secret: {}
        api_secret_file:
          type: string
        api_url: {}
        corp_id:
          type: string
        http_config:
          $ref: '#/components/schemas/o11y.HTTPClientConfig'
        message:
          type: string
        message_type:
          type: string
        to_party:
          type: string
        to_tag:
          type: string
        to_user:
          type: string
      type: object
    o11y.addItemsIn:
      properties:
        id:
          description: ID is the annotation queue to add to, from the path.
          type: string
        items:
          description: |-
            Items are the objects to enqueue for review, 1–200 per request. Each names
            exactly one object.
          items:
            $ref: '#/components/schemas/o11y.itemInput'
          type: array
      type: object
    o11y.alertmanagertypes.Receiver:
      properties:
        Receiver:
          $ref: '#/components/schemas/o11y.Receiver'
        googlechat_configs:
          items:
            $ref: '#/components/schemas/o11y.GoogleChatReceiverConfig'
          type: array
      type: object
    o11y.annItemList:
      properties:
        data:
          description: Data is the page of items.
          items:
            $ref: '#/components/schemas/o11y.annItemView'
          type: array
        meta:
          $ref: '#/components/schemas/o11y.listMeta'
          description: Meta is the paging that produced it.
      type: object
    o11y.annItemView:
      properties:
        assignee:
          description: Assignee is the reviewer it is for, omitted when unassigned.
          type: string
        completedAt:
          description: CompletedAt is when it was reviewed, omitted while pending.
          type: string
        createdAt:
          description: CreatedAt is when it was enqueued, RFC3339 in UTC.
          type: string
        id:
          description: ID is the item's id.
          type: string
        objectId:
          description: ObjectID is the referenced object's id.
          type: string
        objectType:
          description: 'ObjectType is what it references: TRACE, OBSERVATION or SESSION.'
          type: string
        observationId:
          description: ObservationID echoes objectId when objectType is OBSERVATION.
          type: string
        queueId:
          description: QueueID is the queue it belongs to.
          type: string
        sessionId:
          description: SessionID echoes objectId when objectType is SESSION.
          type: string
        status:
          description: Status is PENDING or COMPLETED.
          type: string
        traceId:
          description: TraceID echoes objectId when objectType is TRACE.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed, RFC3339 in UTC.
          type: string
      type: object
    o11y.annItemsCreated:
      properties:
        data:
          description: Data is every item created by this request, in request order.
          items:
            $ref: '#/components/schemas/o11y.annItemView'
          type: array
      type: object
    o11y.annQueueDeleted:
      properties:
        deleted:
          description: Deleted is true when the queue (and its items) were removed.
          type: boolean
      type: object
    o11y.annQueueDetailView:
      properties:
        completedCount:
          description: CompletedCount is how many have been reviewed.
          type: integer
        createdAt:
          description: CreatedAt is when it was created, RFC3339 in UTC.
          type: string
        description:
          description: Description is its free text, omitted when empty.
          type: string
        id:
          description: ID is the queue's id.
          type: string
        items:
          description: Items is the queue's first page of items (up to 100).
          items:
            $ref: '#/components/schemas/o11y.annItemView'
          type: array
        name:
          description: Name is its display handle.
          type: string
        pendingCount:
          description: PendingCount is how many of its items are still awaiting review.
          type: integer
        scoreConfigIds:
          description: ScoreConfigIDs are the eval score-configs reviewers grade against.
          items:
            type: string
          type: array
        updatedAt:
          description: UpdatedAt is when it last changed, RFC3339 in UTC.
          type: string
      type: object
    o11y.annQueueList:
      properties:
        data:
          description: Data is the page of queues.
          items:
            $ref: '#/components/schemas/o11y.annQueueView'
          type: array
        meta:
          $ref: '#/components/schemas/o11y.listMeta'
          description: Meta is the paging that produced it.
      type: object
    o11y.annQueueView:
      properties:
        createdAt:
          description: CreatedAt is when it was created, RFC3339 in UTC.
          type: string
        description:
          description: Description is its free text, omitted when empty.
          type: string
        id:
          description: ID is the queue's id.
          type: string
        name:
          description: Name is its display handle.
          type: string
        scoreConfigIds:
          description: ScoreConfigIDs are the eval score-configs reviewers grade against.
          items:
            type: string
          type: array
        updatedAt:
          description: UpdatedAt is when it last changed, RFC3339 in UTC.
          type: string
      type: object
    o11y.availabilityPoint:
      properties:
        t:
          description: T is the bucket start, RFC3339 in UTC.
          type: string
        total:
          description: |-
            Total is how many services reported at all inside the bucket. It can be
            lower than the current total: a target added last week reported nothing
            the week before, and saying so is the point.
          type: integer
        up:
          description: Up is how many services were up at the end of the bucket.
          type: integer
      type: object
    o11y.availabilityResponse:
      properties:
        range:
          properties:
            sinceSec:
              type: integer
            stepSec:
              type: integer
          type: object
        series:
          description: Series is the trend, oldest bucket first.
          items:
            $ref: '#/components/schemas/o11y.availabilityPoint'
          type: array
        services:
          description: |-
            Services is the current inventory, sorted by name so two reads of an
            unchanged fleet are byte-identical.
          items:
            $ref: '#/components/schemas/o11y.serviceUp'
          type: array
        total:
          description: Total is how many services the prober currently watches.
          type: integer
        up:
          description: Up is how many services are up right now.
          type: integer
      type: object
    o11y.createQueueReq:
      properties:
        description:
          description: Description is optional free text, up to 512 characters.
          type: string
        name:
          description: |-
            Name is the queue's display handle, 1–128 printable characters. It must be
            unique within the org's project. Required.
          type: string
        scoreConfigIds:
          description: ScoreConfigIDs are the eval score-configs reviewers grade against.
          items:
            type: string
          type: array
      type: object
    o11y.deployment:
      properties:
        instance:
          type: string
        up:
          type: boolean
      type: object
    o11y.integrations.CollectedLogAttribute:
      properties:
        name:
          type: string
        path:
          type: string
        type:
          type: string
      type: object
    o11y.integrations.CollectedMetric:
      properties:
        description:
          type: string
        name:
          type: string
        type:
          type: string
        unit:
          type: string
      type: object
    o11y.itemInput:
      properties:
        assignee:
          description: Assignee is the reviewer this item is for, up to 512 characters.
          type: string
        objectId:
          description: ObjectID is the referenced object's id, paired with objectType.
          type: string
        objectType:
          description: |-
            ObjectType is TRACE, OBSERVATION or SESSION — the generic form, paired
            with objectId.
          type: string
        observationId:
          description: |-
            ObservationID references an observation — the console-friendly form of
            objectType=OBSERVATION.
          type: string
        sessionId:
          description: |-
            SessionID references a session — the console-friendly form of
            objectType=SESSION.
          type: string
        traceId:
          description: |-
            TraceID references a trace — the console-friendly form of
            objectType=TRACE.
          type: string
      type: object
    o11y.listMeta:
      properties:
        limit:
          description: Limit is how many rows one page holds.
          type: integer
        page:
          description: Page is the 1-based page this response is.
          type: integer
        totalItems:
          description: TotalItems is how many rows match in total.
          type: integer
        totalPages:
          description: TotalPages is ceil(totalItems/limit), at least 1.
          type: integer
      type: object
    o11y.metricsResponse:
      properties:
        product:
          type: string
        range:
          properties:
            sinceSec:
              type: integer
            stepSec:
              type: integer
          type: object
        series:
          properties:
            errors:
              items:
                $ref: '#/components/schemas/o11y.point'
              type: array
            latencyP50Ms:
              items:
                $ref: '#/components/schemas/o11y.point'
              type: array
            latencyP95Ms:
              items:
                $ref: '#/components/schemas/o11y.point'
              type: array
            requests:
              items:
                $ref: '#/components/schemas/o11y.point'
              type: array
          type: object
        summary:
          properties:
            errorRate:
              type: number
            errors:
              type: integer
            p95Ms:
              type: number
            requests:
              type: integer
          type: object
        usage:
          properties:
            calls:
              type: integer
            costCents:
              type: integer
            series:
              items:
                $ref: '#/components/schemas/o11y.usageBucket'
              type: array
            tokens:
              type: integer
          type: object
      type: object
    o11y.point:
      properties:
        t:
          description: T is the bucket start, RFC3339 in UTC.
          type: string
        v:
          description: V is the bucket's value.
          type: number
      type: object
    o11y.querybuildertypesv5.CompositeQuery:
      properties:
        queries:
          description: Queries is the queries to use for the request.
          items:
            $ref: '#/components/schemas/o11y.QueryEnvelope'
          type: array
      type: object
    o11y.querybuildertypesv5.OrderBy:
      properties:
        direction:
          description: direction to order by
        key:
          $ref: '#/components/schemas/o11y.OrderByKey'
          description: key to order by
      type: object
    o11y.serviceUp:
      properties:
        name:
          description: |-
            Name is the service as the fleet prober knows it (probes.go's target name,
            which is the `service` label on hanzo_service_up).
          type: string
        up:
          description: Up is true when the service answered its own health URL on
            the last cycle.
          type: boolean
      type: object
    o11y.statusResult:
      properties:
        checkedAt:
          type: string
        deployments:
          items:
            $ref: '#/components/schemas/o11y.deployment'
          type: array
        latencyMs:
          type: integer
        product:
          type: string
        source:
          type: string
        up:
          type: boolean
      type: object
    o11y.traceRow:
      properties:
        durationMs:
          description: |-
            DurationMs is End minus Start in milliseconds: the trace's wall clock,
            not the sum of its spans, which double-counts everything concurrent.
          type: number
        end:
          description: End is the latest span end, RFC3339 with nanoseconds, in UTC.
          type: string
        numSpans:
          description: NumSpans is how many spans the trace carries.
          type: integer
        start:
          description: Start is the earliest span start, RFC3339 with nanoseconds,
            in UTC.
          type: string
        traceId:
          description: TraceID is the trace's id — the {traceId} of the detail read.
          type: string
      type: object
    o11y.tracesOut:
      properties:
        count:
          description: Count is how many traces this page carries.
          type: integer
        limit:
          description: Limit is the page cap actually applied, after clamping.
          type: integer
        sinceSec:
          description: SinceSec is the window actually read, in seconds, after clamping.
          type: integer
        traces:
          description: Traces are the caller org's traces, most recently active first.
          items:
            $ref: '#/components/schemas/o11y.traceRow'
          type: array
      type: object
    o11y.updateItemIn:
      properties:
        assignee:
          description: Assignee replaces the reviewer this item is for, up to 512
            characters.
          type: string
        id:
          description: ID is the annotation queue the item belongs to, from the path.
          type: string
        itemId:
          description: ItemID is the item to update, from the path.
          type: string
        status:
          description: 'Status is the item''s new review state: PENDING or COMPLETED.
            Required.'
          type: string
      type: object
    o11y.updateQueueIn:
      properties:
        description:
          description: Description replaces the free text when present, up to 512
            characters.
          type: string
        id:
          description: ID is the annotation queue to update, from the path.
          type: string
        name:
          description: |-
            Name replaces the queue's display handle when present, 1–128 printable
            characters and unique within the project.
          type: string
        scoreConfigIds:
          description: ScoreConfigIDs replaces the whole score-config set when present.
          items:
            type: string
          type: array
      type: object
    o11y.usageBucket:
      properties:
        calls:
          description: Calls is how many LLM calls landed in the bucket.
          type: integer
        costCents:
          description: CostCents is what they cost, in cents.
          type: integer
        t:
          description: T is the bucket start, RFC3339 in UTC.
          type: string
        tokens:
          description: Tokens is how many tokens they consumed.
          type: integer
      type: object
    oauthBundleIn:
      properties:
        access:
          description: Access is the access token.
          type: string
        account:
          description: Account is the account label the flow reported; sanitized on
            ingest.
          type: string
        refresh:
          description: Refresh is the refresh token. It is sealed and NEVER handed
            back out.
          type: string
      type: object
    onboardReq:
      properties:
        name:
          description: |-
            Name is the organization's display name. Ignored when personal is true, which
            derives the name from the caller's own username instead.
          type: string
        personal:
          description: |-
            Personal asks for the caller's own workspace: the name is derived from their
            username and the slug auto-suffixes to stay unique. Meaningless — and refused
            — for a caller who already has an organization.
          type: boolean
      type: object
    onboardResp:
      properties:
        accessKey:
          description: |-
            AccessKey is the identifier of the org-scoped credential provisioning minted
            with the organization. Present on a first run that actually minted one.
          type: string
        accessSecret:
          description: |-
            AccessSecret is that credential's confidential half, returned ONCE — on the
            response that mints it and never again. IAM keeps only its argon2id digest
            and blanks the plaintext, so this is the single moment it exists in a form
            its owner can read; a replay of the same provision re-reveals nothing.
          type: string
        additional:
          description: |-
            Additional is true when the caller already had an organization and this one
            was created WITHOUT moving them into it — they reach it via the org switcher.
          type: boolean
        displayName:
          description: DisplayName is the organization's human name.
          type: string
        org:
          description: Org is the created organization's slug, which is what X-Org-Id
            carries.
          type: string
      type: object
    openReq:
      properties:
        base:
          description: |-
            Base is the branch the work is proposed INTO, by short name. Defaults to
            the repo's default branch, which is where a proposal goes when nobody says
            otherwise.
          type: string
        body:
          description: Body is the longer description. Optional.
          type: string
        head:
          description: |-
            Head is the branch holding the work, by short name (agent/fix-503).
            Required, and must already exist.
          type: string
        name:
          description: Name is the repo the proposal belongs to, from the :name path
            segment.
          type: string
        title:
          description: Title is the one-line summary of what is being proposed. Required.
          type: string
      type: object
    optinView:
      properties:
        org:
          $ref: '#/components/schemas/orgOptinView'
          description: |-
            Org is the caller's org's listing preference on the cross-org board, and whether
            this caller is allowed to change it. It is read for every caller — a member sees
            where their org stands even though only an admin may edit it.
        user:
          $ref: '#/components/schemas/userOptinView'
          description: User is the caller's OWN listing preference, and whether they
            may change it.
      type: object
    order:
      properties:
        contacts:
          $ref: '#/components/schemas/Contacts'
          description: |-
            Contacts is the WHOIS contact set. Omit it and the registrar uses the
            reseller account's default contacts.
        domain:
          description: Domain is the name to buy. It is required.
          type: string
        years:
          description: Years is the term to buy, defaulting to 1.
          type: integer
      required:
      - domain
      type: object
    orgOptinReq:
      properties:
        display:
          description: |-
            Display is the name shown for the org on that board: 1-40 characters of
            letters, digits, space, dot, underscore, apostrophe or hyphen. Left empty on a
            listing opt-in it defaults to the org id.
          type: string
        listed:
          description: |-
            Listed publishes the org on the cross-org global board when true, and withdraws
            it when false.
          type: boolean
      type: object
    orgOptinView:
      properties:
        canManage:
          description: |-
            CanManage is true only for an admin of this org (or a platform SuperAdmin) — the
            callers whose write of the org preference will be accepted.
          type: boolean
        display:
          description: |-
            Display is the name shown for the org on that board. Empty when none was chosen;
            opting in without one defaults it to the org id.
          type: string
        listed:
          description: |-
            Listed is true when the org has opted onto the cross-org global board. False —
            the default — keeps the org off it entirely; the org's own members still see
            their own board. Listing consents to publishing usage VOLUME, never spend.
          type: boolean
      type: object
    pairingApproved:
      properties:
        ownerBootstrapped:
          description: |-
            OwnerBootstrapped is true when this approval was the org's FIRST on the
            channel and therefore also made the sender its owner.
          type: boolean
        sender:
          description: Sender is the external chat identity that is now allowed to
            DM the org's bot.
          type: string
      type: object
    pairingQueue:
      properties:
        pending:
          description: |-
            Pending is every unexpired pairing request waiting on an org admin, each
            carrying the channel, the requesting sender and the code to approve it with.
          items:
            $ref: '#/components/schemas/pairingView'
          type: array
      type: object
    pairingView:
      properties:
        channel:
          description: |-
            Channel is the transport the request arrived on — discord, slack, teams or
            telegram — and half of what approval names. The cap of three unapproved
            requests applies per (org, channel); while it is full no further code is
            minted until one is approved or expires.
          type: string
        code:
          description: |-
            Code is the CAPABILITY that authorises the approval: eight characters from a
            32-symbol uppercase alphabet (A-Z0-9 minus the confusables 0, O, 1 and I),
            minted with crypto/rand and also sent to the requester in chat. An org admin
            passes it with the channel to POST /v1/channels/pairing/approve, which
            CONSUMES it — the request row is deleted, so a code approves once — and which
            takes org admin as well as the code. It lives ONE HOUR from CreatedAt;
            expired requests are not listed here, and approving one is a 404. It is shown
            on this admin surface and NEVER logged.
          type: string
        createdAt:
          description: |-
            CreatedAt is Unix SECONDS of FIRST contact: when the request was minted and
            the code sent. Expiry is measured from here and from nowhere else.
          type: integer
        lastSeen:
          description: |-
            LastSeen is Unix SECONDS of the MOST RECENT message from this sender while
            the request has been pending. It moves as they keep writing, which is how an
            admin tells a live request from an abandoned one — but it does not extend the
            hour and does not re-send the code, since one request sends exactly one chat
            reply.
          type: integer
        sender:
          description: |-
            Sender is the transport-native user id waiting for access — the same
            identity inbox messages carry. Approving mints a DM allow entry for exactly
            this value and nothing wider: pairing never grants group access.
          type: string
      type: object
    patchFlowIn:
      properties:
        externalId:
          description: ExternalID sets the caller's own id for this flow.
          type: string
        folderId:
          description: FolderID moves the flow in the builder's tree.
          type: string
        id:
          description: ID is the flow to update, from the path.
          type: string
        metadata:
          description: Metadata replaces the caller's opaque JSON.
        publishedVersionId:
          description: |-
            PublishedVersionID pins the version runs execute. It must name a version OF
            THIS FLOW; empty clears the pin, so runs take the latest version again.
          type: string
      type: object
    patchIn:
      properties:
        name:
          description: Name is the repo to update, from the :name path segment.
          type: string
        public:
          description: |-
            Public flips anonymous read access. Omit it and the request is refused —
            there is nothing else to update yet.
          type: boolean
      type: object
    patchSessionIn:
      properties:
        cwd:
          description: |-
            Cwd is where the session is working NOW.

            It was write-once — captured at register and never again — which is right
            for a run that starts in a directory and stays there, and wrong for a linked
            shell, which is a place a person moves around in. The console showed the
            directory `hanzo link` happened to be run from and kept showing it after the
            shell had walked away, so the field answered "which work is this" with an
            answer that was true once. A pointer, so an unchanged path is an omitted
            field rather than a repeated write.
          type: string
        id:
          description: ID is the session to update, from the path.
          type: string
        project:
          description: |-
            Project tags the product this session built; Published is the author's
            decision to let anyone read the story (provenance.go). Both are pointers so
            "absent" and "cleared" are different requests.
          type: string
        published:
          type: boolean
        status:
          type: string
        target:
          description: 'Target re-dispatches a session to a run-target (the #48 association).
            "" detaches.'
          type: string
        terminal:
          description: |-
            Terminal publishes (or, with "", withdraws) the URL this session's live
            terminal can be watched at. A pointer so "absent" and "withdrawn" are
            different requests: a session that stops sharing must be able to say so.
          type: string
        title:
          type: string
      type: object
    patchSyncIn:
      properties:
        actor:
          description: |-
            Actor is the loop-guard identity the sync writes as. Omitted, the stored actor
            stands.
          type: string
        direction:
          description: Direction is both, pull, push or off. Omitted, the stored direction
            stands.
          type: string
        id:
          description: ID is the sync to update, from the path.
          type: string
        kind:
          description: Kind names a different kind of sync, and is refused, for the
            same reason.
          type: string
        source:
          $ref: '#/components/schemas/endpointReq'
          description: |-
            Source, Target and Kind are DECLARED HERE IN ORDER TO BE REFUSED.

            They are immutable by design — re-pointing a sync is a delete and a create, so
            a link can never silently start syncing somewhere else — but an UNDECLARED
            field is dropped by the binder before the handler sees it, so a request asking
            to repoint answered 200, changed nothing, and said nothing. The operator then
            believes a moved repository has been repointed and it has not.

            Live: a sync still naming github.com/hanzoai/cloud after the repository moved
            to hanzo-inc/cloud failed every reconcile with "Repository not found", and the
            PATCH that appeared to fix it did nothing at all. Declaring the fields is what
            lets the documented immutability actually answer.
            Source names a new upstream, and is refused. Delete this sync and create the
            one you want.
        target:
          $ref: '#/components/schemas/endpointReq'
          description: Target names a new native repository, and is refused, for the
            same reason.
        trigger:
          description: Trigger is webhook, poll or manual. Omitted, the stored trigger
            stands.
          type: string
      type: object
    patchTargetIn:
      properties:
        capacity:
          type: string
        host:
          type: string
        id:
          description: ID is the target to update, from the path.
          type: string
        kind:
          type: string
        label:
          type: string
        metrics:
          $ref: '#/components/schemas/Metrics'
          description: present => a heartbeat; the server stamps its time
        spec:
          $ref: '#/components/schemas/Spec'
        status:
          type: string
      type: object
    pickOut:
      properties:
        consumers:
          description: Consumers is the page, ordered by name.
          items:
            $ref: '#/components/schemas/Consumer'
          type: array
        total:
          description: Total is the stream's consumer count before paging.
          type: integer
      type: object
    pipelineBoard:
      properties:
        pipelines:
          description: Pipelines are one per application in the caller's org.
          items:
            $ref: '#/components/schemas/pipelineRow'
          type: array
      type: object
    pipelineRow:
      properties:
        duration:
          description: |-
            Duration is how long that run took; empty while it is still queued or
            building.
          type: string
        id:
          description: ID is the application id — one pipeline is one application.
          type: string
        lastRun:
          description: LastRun is when the most recent deployment started, RFC3339
            UTC.
          type: string
        name:
          description: Name is the application's name.
          type: string
        repo:
          description: Repo is the git repo or image the pipeline builds from.
          type: string
        status:
          description: Status is the latest deployment's status, or the app's when
            it has none.
          type: string
      type: object
    planEntitlements:
      properties:
        entitlements:
          description: |-
            Entitlements is the canonical namespaced entitlement block derived from the
            plan's limits and addons, where -1 means unlimited.
        id:
          description: ID is the plan id or slug that was resolved, as it was requested.
          type: string
        license_features:
          description: |-
            LicenseFeatures is the flat, sorted feature list a signed license carries,
            derived from the entitlements.
          items:
            type: string
          type: array
      type: object
    planHealth:
      properties:
        service:
          description: Service names the subsystem that answered.
          type: string
        status:
          description: Status is "ok" whenever this subsystem is mounted.
          type: string
      type: object
    planInfo:
      properties:
        active:
          description: Active is whether that plan's entitlement is live.
          type: boolean
        guestLimit:
          description: GuestLimit is the plan's team.guests cap, when the plan carries
            one.
          type: integer
        guests:
          description: Guests is how many of those seats are guests.
          type: integer
        plan:
          description: |-
            Plan is the licensed plan id, empty when it cannot be resolved here — an
            honest dash on the page, never a fabricated tier.
          type: string
        seats:
          description: Seats is the org's distinct active human members.
          type: integer
        upgradeUrl:
          description: UpgradeURL is where the page sends a caller who wants a bigger
            plan.
          type: string
      type: object
    planList:
      properties:
        plans:
          description: |-
            Plans are the plans in this section, each an opaque object exactly as the
            @hanzo/plans catalog emits it — typically id, name, description,
            priceMonthly, category, a feature list, a limits block and a price_ref.
          items: {}
          type: array
      type: object
    planRegionList:
      properties:
        regions:
          description: |-
            Regions are the regions cloud capacity is offered in, each an opaque object
            exactly as the catalog emits it — typically id, name, location and flag.
          items: {}
          type: array
      type: object
    planResolution:
      properties:
        entitlements:
          description: |-
            Entitlements is the canonical namespaced entitlement block derived from the
            plan's limits and addons — keys like "ai.tokens_per_min" and
            "world.api_rate_limit", where -1 means unlimited.
        id:
          description: ID is the plan's catalog id.
          type: string
        license_features:
          description: |-
            LicenseFeatures is the flat, sorted feature list a signed license carries,
            derived from the entitlements — "ai.premium", "licensing.product:team".
          items:
            type: string
          type: array
        price_ref:
          description: |-
            PriceRef is the plan's billing reference — currency, the recurring monthly
            and annual amounts, whether it prices per seat, its Stripe lookup key and
            its metered components.
        tenant_id:
          description: |-
            TenantID is the catalog the record came from: "hanzo" for the canonical
            catalog, a reseller org for that reseller's override.
          type: string
      type: object
    planSchemas:
      properties:
        entitlements:
          description: |-
            Entitlements is entitlements.schema.json — the JSON Schema every
            entitlement key is declared in, including its type, unit and enum.
        plan:
          description: |-
            Plan is plan.schema.json — the JSON Schema a catalog plan record conforms
            to.
      type: object
    planTierList:
      properties:
        tiers:
          description: |-
            Tiers are the rentable GPU configurations, each an opaque object exactly as
            the catalog emits it — typically id, name, GPU count and model, VRAM, vCPUs,
            host memory and hourly price.
          items: {}
          type: array
      type: object
    planToolList:
      properties:
        tools:
          description: |-
            Tools are the metered tools, each an opaque object exactly as the catalog
            emits it — typically name, billing unit and price.
          items: {}
          type: array
      type: object
    planVocab:
      properties:
        engine_features:
          description: |-
            EngineFeatures are the inference-engine capabilities a license can grant:
            inference, embeddings, rerank, training, vision, audio, tools.
          items:
            type: string
          type: array
        keys:
          additionalProperties: {}
          description: |-
            Keys maps every entitlement key to its descriptor — key, namespace, JSON
            type(s), nullability, unit, enum and title, as the schema declares them.
          type: object
        namespaces:
          description: |-
            Namespaces are the entitlement key namespaces: the prefix before the dot in
            "ai.tokens_per_min".
          items:
            type: string
          type: array
      type: object
    pluginDeleted:
      properties:
        deleted:
          description: Deleted is the plugin id that is now gone.
          type: string
      type: object
    pluginMount:
      properties:
        enabled:
          description: Enabled is whether this subsystem is switched on in this deployment.
          type: boolean
        name:
          description: Name is the subsystem's name, the same label a traced request
            resolves to.
          type: string
        prefixes:
          description: Prefixes are the URL prefixes this subsystem serves.
          items:
            type: string
          type: array
      type: object
    pluginMountList:
      properties:
        plugins:
          description: |-
            Plugins is every subsystem the composition root declared, filtered to the
            enabled ones unless all=true.
          items:
            $ref: '#/components/schemas/pluginMount'
          type: array
      type: object
    pointView:
      properties:
        t:
          description: the bucket's start, RFC3339 (UTC)
          type: string
        v:
          description: how many invocations fell in it — a real count, never interpolated
          type: integer
      type: object
    poolCreate:
      properties:
        autoScale:
          description: AutoScale turns the provider's cluster autoscaler on for this
            pool.
          type: boolean
        clusterId:
          description: ClusterID is the cluster to add the pool to, from the URL path.
          type: string
        count:
          description: Count is how many nodes the pool starts with.
          type: integer
        maxNodes:
          type: integer
        minNodes:
          description: |-
            MinNodes and MaxNodes bound the autoscaler; they are ignored unless
            AutoScale is set.
          type: integer
        name:
          description: Name is the pool's name.
          type: string
        provider:
          description: |-
            Provider is the cloud the cluster lives on (e.g. "digitalocean"). Required —
            Visor routes the create by it. Accepted from the body or ?provider=.
          type: string
        size:
          description: Size is the provider size slug for each node (e.g. "s-4vcpu-8gb").
          type: string
      type: object
    poolScale:
      properties:
        clusterId:
          description: ClusterID and PoolID address the pool, from the URL path.
          type: string
        count:
          description: |-
            Count is the node count to scale TO — an absolute target, not a delta, and
            never negative.
          type: integer
        poolId:
          type: string
        provider:
          description: Provider is the cloud the cluster lives on. Required; body
            or ?provider=.
          type: string
      type: object
    populatedFlow:
      properties:
        created:
          description: Created and Updated are unix milliseconds.
          type: integer
        externalId:
          description: ExternalID is the caller's own id for this flow, if it set
            one.
          type: string
        folderId:
          description: FolderID groups the flow in the builder's tree.
          type: string
        id:
          description: ID is the flow's id.
          type: string
        metadata:
          description: Metadata is the caller's opaque JSON, stored and returned verbatim.
        projectId:
          description: |-
            Org is the owning org, which this surface names projectId. Server-derived from
            the validated principal — never read from a request.
          type: string
        publishedVersionId:
          description: |-
            PublishedVersionID is the version a run executes when set; empty means the
            latest version runs.
          type: string
        status:
          description: Status is ENABLED or DISABLED — whether the flow's trigger
            is armed.
          type: string
        updated:
          type: integer
        version:
          $ref: '#/components/schemas/FlowVersion'
          description: Version is the flow's latest version — its display name and
            step tree.
      type: object
    prefsView:
      properties:
        prefs:
          description: |-
            Prefs is the caller's preference document: an opaque JSON object whose keys
            the surfaces own, returned verbatim. `{}` when nothing has been saved.
        updatedAt:
          description: |-
            UpdatedAt is when the document was last written, unix seconds. Absent when
            nothing has been saved.
          type: integer
      type: object
    previewReq:
      properties:
        app:
          description: App is the parent application's slug, from the path.
          type: string
        branch:
          description: Branch is the branch to preview; defaults to the parent app's
            branch.
          type: string
        image:
          description: |-
            Image is the already-built image ref to deploy. Required — a preview never
            builds.
          type: string
        project:
          description: Project is the project the parent application lives under,
            from the path.
          type: string
      type: object
    previewView:
      properties:
        app:
          description: App is the preview application's own slug, `<app>-<branch>`.
          type: string
        branch:
          description: Branch is the branch this preview maps.
          type: string
        deployment:
          $ref: '#/components/schemas/deploymentView'
          description: Deployment is the deployment the preview recorded.
        url:
          description: URL is the preview's live HTTPS address.
          type: string
      type: object
    pricingHealth:
      properties:
        service:
          description: Service names the subsystem that answered.
          type: string
        status:
          description: Status is "ok" whenever this subsystem is mounted.
          type: string
      type: object
    pricingModelList:
      properties:
        models:
          description: |-
            Models are the catalog entries visible to the caller, each an opaque
            object exactly as the pricing source emits it, with any admin override
            merged on top. An admin additionally sees hidden entries, each annotated
            under "_overlay".
          items:
            additionalProperties:
              type: object
            type: object
          type: array
        total:
          description: |-
            Total is how many models this answer carries — recounted over the visible
            set, not the catalog's own total.
          type: integer
        updated:
          description: |-
            Updated is when the catalog was last refreshed, as the pricing source
            recorded it.
          type: object
      type: object
    pricingPlanList:
      properties:
        plans:
          description: |-
            Plans are the plans in this section, each an opaque object exactly as the
            pricing source emits it — typically id, name, description, price and a
            feature list.
          items:
            additionalProperties:
              type: object
            type: object
          type: array
      type: object
    pricingPresetList:
      properties:
        presets:
          description: |-
            Presets are the named compute sizes, each an opaque object exactly as the
            pricing source emits it — typically id, name, provider slug, vCPU, memory,
            disk and price.
          items:
            additionalProperties:
              type: object
            type: object
          type: array
      type: object
    pricingProviderList:
      properties:
        providers:
          additionalProperties:
            type: object
          description: |-
            Providers maps a provider name to its opaque info object. A provider
            hidden for the caller's org is absent entirely.
          type: object
        updated:
          description: |-
            Updated is when the catalog was last refreshed, as the pricing source
            recorded it.
          type: object
      type: object
    pricingRegionList:
      properties:
        regions:
          description: |-
            Regions are the regions cloud instances can be placed in, each an opaque
            object exactly as the pricing source emits it — typically id, name and
            location.
          items:
            additionalProperties:
              type: object
            type: object
          type: array
      type: object
    pricingSyncOut:
      properties:
        status:
          description: Status is "ok" when the sync completed.
          type: string
        updated:
          description: Updated is the RFC 3339 time the refreshed catalog was stamped
            with.
          type: string
      type: object
    pricingTierList:
      properties:
        tiers:
          description: |-
            Tiers are the rentable GPU configurations, each an opaque object exactly
            as the pricing source emits it — typically id, name, accelerator count and
            model, VRAM, vCPU, memory and hourly price.
          items:
            additionalProperties:
              type: object
            type: object
          type: array
      type: object
    pricingToolList:
      properties:
        tools:
          description: |-
            Tools are the metered tools, each an opaque object exactly as the pricing
            source emits it — typically name, billing unit and price.
          items:
            additionalProperties:
              type: object
            type: object
          type: array
      type: object
    productEvent:
      properties:
        distinctId:
          description: DistinctID is the person/visitor the event is attributed to.
          type: string
        event:
          description: Event is the event name, e.g. page_viewed or signup_completed.
          type: string
        id:
          description: |-
            ID is the row's stable event id — the client's own idempotency id when it sent
            one, else the server-minted one.
          type: string
        path:
          description: Path is the URL's path component, the key the topPages lens
            groups by.
          type: string
        product:
          description: Product is the surface that emitted the event. Omitted when
            absent.
          type: string
        properties:
          description: |-
            Properties is the row's attributes map as a JSON object (string values — the
            plane stores Map(String,String), so a nested value the caller sent is a
            JSON-encoded string). Omitted when the row carries none.
        sessionId:
          description: SessionID groups the events of one visit. Omitted when the
            client sent none.
          type: string
        timestamp:
          description: Timestamp is when the event happened, RFC3339 UTC.
          type: string
        type:
          description: |-
            Type is the row's kind — the plane's discriminator: page, track, identify or
            group. (Errors are not here at all: they land on event.error and are read at
            /v1/errors.)
          type: string
        url:
          description: URL is the full page address the event fired on. Omitted when
            absent.
          type: string
      type: object
    projectView:
      properties:
        applications:
          type: integer
        createdAt:
          type: integer
        description:
          type: string
        name:
          type: string
        org:
          type: string
        slug:
          type: string
      type: object
    projectionView:
      properties:
        apps:
          additionalProperties:
            type: boolean
          description: |-
            Apps says, per console app, whether the org may open it. The SAME six keys are
            always present (studio, bot, world, platform, team, admin), so a client maps
            over it unconditionally; a key is false both when the plan does not grant the
            app and when commerce could not be reached, because a read that decides what to
            SHOW fails to LOCKED rather than to an error.
          type: object
        tier:
          description: |-
            Tier is the plan slug commerce resolved for the org, or "" when the org has no
            active licensing subscription — which the console treats as its free default.
          type: string
      type: object
    projectsBoundDomains:
      properties:
        bound:
          description: |-
            Bound is the result of THIS call, one row per host in the request: live for
            an already-vouched host, pending with the DNS records to publish otherwise.
          items:
            $ref: '#/components/schemas/projectsDomain'
          type: array
        domains:
          description: |-
            Domains are the hostnames that are VERIFIED and routing right now, after
            this bind.
          items:
            type: string
          type: array
        org:
          description: Org is the organisation that owns the site.
          type: string
        slug:
          description: Slug is the site the hosts were bound to.
          type: string
      type: object
    projectsBuildSite:
      properties:
        brief:
          description: |-
            Brief is what the site should be, in plain language. It is the whole input the
            model gets and it is size-bounded.
          type: string
        model:
          description: |-
            Model names which model writes the site. Absent takes the deployment's
            default — this route spends inference on the caller's org either way.
          type: string
        name:
          description: Name is the site's display name. Taken from what the model
            writes when omitted.
          type: string
        slug:
          description: |-
            Slug is the handle and public host label to publish under. Derived from the
            name, or from the brief, when omitted.
          type: string
      type: object
    projectsComplete:
      properties:
        bytes:
          description: Bytes is their total size in bytes.
          type: integer
        commit:
          description: Commit is the revision that was built, recorded on the deployment.
          type: string
        files:
          description: Files is how many objects CI published.
          type: integer
        id:
          description: ID is the queued deployment to complete, from the path.
          type: string
        keys:
          description: |-
            Keys is the manifest CI just uploaded, RELATIVE to the deployment prefix. It
            is what replaces `aws s3 sync --delete`: an upload grant authorizes writes
            only, so CI cannot remove a file, and cloud reconciles the prefix against
            this list instead (grant.go). Omit it and nothing is deleted — the prefix
            only grows, which is the old pre-grant behaviour and a safe default.
          items:
            type: string
          type: array
        liveUrl:
          description: |-
            LiveURL is a HINT at the address the site should serve at. The public host is
            claimed by cloud first, so this can refine the URL a deployment reports but
            can never assert a subdomain another tenant holds.
          type: string
        message:
          description: Message is what happened, in words — on an error completion,
            why it failed.
          type: string
        slug:
          description: Slug is the project the deployment belongs to, from the path.
          type: string
        status:
          description: |-
            Status is how the build ended: `live` if it succeeded, `error` if it did not.
            Nothing else is accepted.
          type: string
      type: object
    projectsCreate:
      properties:
        analytics:
          description: |-
            Analytics is the opt-OUT for the wired-by-default analytics beacon: absent
            (nil) ⇒ ON (the default); explicit false ⇒ off. A pointer so "unset" is
            distinguishable from "false" — the only way to turn the default off.
          type: boolean
        description:
          description: |-
            Description is the one-line summary, copied onto anything forked from this
            project.
          type: string
        framework:
          description: |-
            Framework is a BUILD HINT from a closed set, defaulting to static. It tells CI
            how to build a linked repo and never gates a deploy.
          type: string
        license:
          description: License is the terms that upstream work carries.
          type: string
        name:
          description: |-
            Name is the project's display name and the only REQUIRED field. When slug is
            omitted it is also what the slug is derived from.
          type: string
        repo:
          description: |-
            Repo links a git source, so pushes to it rebuild this project. Omit it for a
            project deployed by uploading an artifact.
          properties:
            branch:
              type: string
            url:
              type: string
          type: object
        slug:
          description: |-
            Slug is the handle everything else addresses this project by: the public host
            `<slug>.hanzo.app`, the object-store key segment, and the path parameter of
            every later call. Derived from the name when omitted. It is a hostname label,
            so it is constrained and reserved labels such as `api` or `admin` are refused.
          type: string
        upstream:
          description: |-
            Upstream credits the third-party work this project was published from. It is
            accepted from any caller: giving away credit can only cost the publisher, so
            it needs no gate.
          type: string
        visibility:
          description: |-
            Visibility is "public" (the default when absent) or "private". Publishing
            publicly is ungated — that is the point of a community. Going PRIVATE is
            the paid feature, so an unfunded org asking for it is refused rather than
            silently downgraded (see resolve).
          type: string
      type: object
    projectsDeploySite:
      properties:
        files:
          description: |-
            Files is the whole site, inline — every file it consists of. It REPLACES what
            is there rather than merging, so an omitted file is a deleted one.
          items:
            $ref: '#/components/schemas/projectsFile'
          type: array
        name:
          description: Name is the site's display name.
          type: string
        slug:
          description: Slug is the handle and public host label to publish under.
          type: string
      type: object
    projectsDeployStart:
      properties:
        commit:
          description: |-
            Commit is the git sha this build was produced from, recorded on the
            deployment so a released site can be traced back to its source. Optional.

            It is the ONLY field here, and deliberately: the predecessor also accepted
            `source` and `branch`. `source` was the Content-Type discriminator this
            split removed. `branch` was accepted and DISCARDED — there is no branch
            column on a deployment, and the lifecycle event derives the branch from the
            project's own linked one — so declaring it would publish a settable field
            that does nothing into the document, every generated SDK and the MCP input
            schema. A field that is read by nothing is not described as if it were.

            `url:"-"` because zip binds the query string OVER a decoded body, so
            without it a `?commit=` the caller never sent would outrank the one it did.
          type: string
        slug:
          description: Slug is the site to deploy, from the path.
          type: string
      type: object
    projectsDeployment:
      properties:
        bucket:
          description: Bucket is the object-store bucket its files were written to.
          type: string
        bytes:
          description: Bytes is their total size in bytes.
          type: integer
        commit:
          description: |-
            Commit is the revision that was built, for a deployment that came from a
            repository. Absent for an uploaded artifact, which has no revision.
          type: string
        createdAt:
          description: CreatedAt is when the deployment was queued, as Unix seconds.
          type: integer
        files:
          description: Files is how many objects the deployment published.
          type: integer
        id:
          description: |-
            ID identifies this one deployment attempt, and is what CI quotes back to
            complete it.
          type: string
        liveUrl:
          description: LiveURL is where this deployment serves, once it is live.
          type: string
        message:
          description: |-
            Message is what happened, in words — the build's own note, or on a failure
            why it failed.
          type: string
        prefix:
          description: |-
            Prefix is the key prefix within that bucket holding EXACTLY this deployment's
            objects — the unit an upload grant is scoped to, so a grant for one deployment
            cannot write over another.
          type: string
        projectId:
          description: ProjectID is the project this deployment belongs to.
          type: string
        source:
          description: |-
            Source is what caused the deployment — a git push, an uploaded artifact, a
            generated site.
          type: string
        status:
          description: |-
            Status is where the attempt got to — queued, live, or failed. A deployment
            that is live is not necessarily the one SERVING: the project's own
            currentDeploymentId says which is.
          type: string
        updatedAt:
          description: |-
            UpdatedAt is when it last changed state, as Unix seconds — so the gap between
            the two is how long the build took.
          type: integer
        upload:
          $ref: '#/components/schemas/projectsUploadGrant'
          description: |-
            Upload is the prefix-scoped, short-lived S3 write grant handed to CI with a
            queued git deployment, so it needs no bucket credential (grant.go). Present
            ONLY on the 202 that creates the deployment — it is never stored and never
            replayed on a later read, so a grant cannot outlive the build it was minted
            for by being fetched again.
        version:
          description: |-
            Version counts deployments of this project from 1, so the history reads as an
            ordered sequence rather than by timestamp. It is per project, not global.
          type: integer
      type: object
    projectsDomain:
      properties:
        createdAt:
          description: CreatedAt is when the host was claimed, as Unix seconds — not
            when it went live.
          type: integer
        detail:
          description: Detail is what is holding the claim up, in words a person can
            act on.
          type: string
        host:
          description: Host is the custom hostname claimed for this site.
          type: string
        records:
          description: |-
            Records are EXACTLY the DNS records to publish to prove ownership and route
            the host. Present only while pending, because a live host has already proved
            it; absent is therefore "nothing left to do", not "we cannot say what to do".
          items:
            $ref: '#/components/schemas/Record'
          type: array
        status:
          description: |-
            Status is `live` when the edge answers for this host now, `pending` while the
            claim is waiting on DNS proof of ownership. A pending host is claimed but
            serves nothing.
          type: string
        url:
          description: |-
            URL is where the host will serve once it is live — present on a pending claim
            too, so a console can show the destination before it works.
          type: string
        verified:
          description: |-
            Verified is the same fact as a boolean, for a caller that only needs the yes
            or no. It cannot disagree with status.
          type: boolean
      type: object
    projectsDomains:
      properties:
        claims:
          description: |-
            Claims is one row per host — live, or pending with the DNS records it still
            owes.
          items:
            $ref: '#/components/schemas/projectsDomain'
          type: array
        domains:
          description: Domains are the hostnames that are VERIFIED and routing right
            now.
          items:
            type: string
          type: array
        org:
          description: Org is the organisation that owns the site.
          type: string
        slug:
          description: Slug is the site the panel belongs to.
          type: string
      type: object
    projectsDomainsBind:
      properties:
        domains:
          description: |-
            Domains are the custom hostnames to attach, in order. An empty list is a
            400 rather than a clear — releasing a host is its own call.
          items:
            type: string
          type: array
        slug:
          description: Slug is the site the hosts attach to, from the path.
          type: string
      type: object
    projectsFile:
      properties:
        content:
          description: |-
            Content is the file's whole text, inline. There is no upload step and no
            reference to fetch: a site is sent as its bytes, and each file and the site as
            a whole are size-bounded.
          type: string
        path:
          description: |-
            Path is where the file lands in the site, RELATIVE to its root — so
            "index.html" is the page served at /. Leading slashes and any attempt to
            escape the root are refused.
          type: string
      type: object
    projectsFork:
      properties:
        name:
          description: target project name (optional; defaults to the parent's title)
          type: string
        slug:
          description: parent slug to fork — catalog template or published project
            (required)
          type: string
        target:
          description: |-
            Target overrides the derived project slug (optional; defaults to the
            parent slug). Kept distinct from Slug so callers can rename on fork.
          type: string
        variant:
          description: |-
            Variant picks a template's format/page/theme (optional; defaults to the
            template's first shape). This is the axis the catalog used to spend
            sibling slugs on, so it is expressed here, where the user's preference is.
          type: string
      type: object
    projectsProject:
      properties:
        analytics:
          description: |-
            Analytics is whether the web-analytics beacon is injected into this site's
            pages. It is ON by default — a project has to opt out — and it is what the
            static builder reads to decide whether to inject at all.
          type: boolean
        bucket:
          description: Bucket is the object-store bucket the site's files are served
            out of.
          type: string
        cacheControl:
          description: |-
            CacheControl is the Cache-Control policy the edge serves this site's HTML
            under — how long a reader may hold a stale page before asking again. Assets
            are content-addressed and are not governed by it.
          type: string
        createdAt:
          description: CreatedAt is when the project was created, as Unix seconds.
          type: integer
        currentDeploymentId:
          description: |-
            CurrentDeploymentID names the deployment currently serving, so a caller can
            ask what is live without scanning the history.
          type: string
        description:
          description: |-
            Description is the one-line summary, which is copied onto forks of this
            project and shown on a gallery card.
          type: string
        forkedFrom:
          description: |-
            ForkedFrom is the parent this project was forked from ("<org>/<slug>" of a
            published project, or a catalog template slug) — the attribution edge a
            gallery credits.
          type: string
        framework:
          description: |-
            Framework is a BUILD HINT from a closed set, defaulting to static. It tells CI
            how to build a linked repo and never gates a deploy, so a wrong value costs a
            build rather than access.
          type: string
        hidden:
          description: |-
            Hidden is PLATFORM MODERATION, and it is a different axis from visibility: it
            pulls a public project out of the catalogue without editing the publisher's
            own choice, so un-hiding restores exactly what they asked for. A project is
            listed only when it is public AND not hidden. Always present, never omitted,
            for the same reason as visibility.
          type: boolean
        hiddenReason:
          description: HiddenReason is why moderation hid it. Absent when it is not
            hidden.
          type: string
        id:
          description: |-
            ID is the project's internal identifier. It is stable across a rename, but it
            is not what the API addresses this project by — `slug` is.
          type: string
        key:
          description: |-
            Key is the project's publishable ingest key, minted at create. It is the
            value the injected beacon carries and the ONE thing that attributes this
            site's events; the static-builder reads it beside analytics.

            Publishable means it belongs in a page's source: it names a write scope and
            mints no principal, so it is returned in full rather than masked. Masking it
            would only mean every caller needed a second endpoint to get the thing the
            page already ships.
          type: string
        lastPurgeAt:
          description: |-
            LastPurgeAt is when the edge cache was last cleared, as Unix seconds, so a
            console can say how fresh what readers see actually is. Absent means never.
          type: integer
        license:
          description: |-
            License is the terms that upstream work carries. Absent has the same reading:
            undeclared, not unencumbered.
          type: string
        liveUrl:
          description: |-
            LiveURL is where the site answers today. Absent until something has been
            deployed.
          type: string
        name:
          description: Name is the project's display name, free text a person chose.
          type: string
        org:
          description: |-
            Org is the organisation that owns the project, and therefore who pays for it
            and who may change it. It is also the AUTHORSHIP line a gallery credits;
            there is no separate author field.
          type: string
        repo:
          $ref: '#/components/schemas/projectsRepo'
          description: |-
            Repo is the git source this project builds from, empty when it is deployed by
            uploading an artifact instead.
        slug:
          description: |-
            Slug is the identifier that MATTERS: the handle every later call addresses,
            the S3 key segment the site's objects live under, and the label of the public
            host `<slug>.hanzo.app`. Because it is a hostname it is constrained and
            reserved labels such as `api` are refused.
          type: string
        space:
          description: |-
            Space is the project's Base data space, which is where a deployed site's form,
            forum and data submissions land. Absent means the site stores nothing.
          type: string
        status:
          description: |-
            Status is where the project stands — whether a build has ever succeeded and
            whether anything is serving right now.
          type: string
        tags:
          additionalProperties:
            type: string
          description: |-
            Tags is the site's browser tag config: platform slug → non-secret pixel id (GA
            measurement, Meta pixel, …) — what track.js injects and the server CAPI reads,
            per site. Omitted when none are set. The API SECRET is never here (KMS).
          type: object
        updatedAt:
          description: |-
            UpdatedAt is when the project's own record last changed, as Unix seconds. A
            deploy is not an edit of the project, so this does not move on every publish.
          type: integer
        upstream:
          description: |-
            Upstream credits the third-party work this project was published from — a
            free-text line, because the honest answer is a name and a title that no enum
            could hold. Absent means NOBODY HAS SAID, not that there is nothing to say.
          type: string
        visibility:
          description: |-
            Visibility is "public" or "private", and Hidden reports platform
            moderation. Both are always present (never omitempty) so a consumer can
            tell a real answer from "this API is too old to say" — and so a console
            never renders a project as public because a field was missing.

            Authorship is deliberately absent: it is Org, above.
          type: string
      type: object
    projectsPublish:
      properties:
        slug:
          description: Slug is the site to publish, from the path.
          type: string
        source:
          description: |-
            Source is the build output to promote, as a path RELATIVE to your org's own
            storage space — never a URL and never a bucket. The org segment is prepended
            server-side from the validated principal, so the worst a hostile source can
            address is something your own org already owns.
          type: string
      type: object
    projectsRelease:
      properties:
        active:
          description: |-
            Active is whether this is the release the site is SERVING right now. Exactly
            one release of a site is active; the others are kept so they can be activated
            again, until retention reclaims them.
          type: boolean
        bytes:
          description: Bytes is their total size in bytes.
          type: integer
        createdAt:
          description: |-
            CreatedAt is when the release was cut, as Unix seconds — not when it was last
            activated.
          type: integer
        objects:
          description: Objects is how many files the release holds.
          type: integer
        releaseId:
          description: |-
            ReleaseID is derived from a DIGEST of the release's own manifest, so identical
            content is the same release and a release can never be confused with another
            one. Activating an older id IS the rollback.
          type: string
        slug:
          description: Slug is the site this release belongs to.
          type: string
        source:
          description: |-
            Source is what the release was cut from — the build output or upload it was
            promoted out of.
          type: string
        url:
          description: |-
            URL is where the site serves. Present only on the ACTIVE release, since an
            inactive one is not answering anywhere.
          type: string
      type: object
    projectsRepo:
      properties:
        branch:
          description: |-
            Branch is the ref a push has to touch for this project to rebuild. Pushes to
            any other branch are ignored.
          type: string
        provider:
          description: |-
            Provider is the forge the URL was recognised as — it decides which webhook
            and which credential reach the repository, and is DERIVED from the URL rather
            than chosen by the caller.
          type: string
        url:
          description: URL is the clone address of the repository this project builds
            from.
          type: string
      type: object
    projectsSite:
      properties:
        name:
          description: Name is the site's display name.
          type: string
        slug:
          description: Slug is the site's handle — also the label of the host it serves
            at.
          type: string
        status:
          description: |-
            Status is the project's state behind the site — whether it is serving, still
            building, or failed its last build. A site that is listed is not necessarily
            one that answers.
          type: string
        updatedAt:
          description: UpdatedAt is when the project last changed, as Unix seconds.
          type: integer
        url:
          description: URL is the pretty address readers use, not the object-store
            path behind it.
          type: string
      type: object
    projectsSiteDeploy:
      properties:
        deploymentId:
          description: DeploymentID is the deployment this publish recorded, for the
            history.
          type: string
        files:
          description: Files are the site-relative paths that were uploaded, sorted.
          items:
            type: string
          type: array
        name:
          description: Name is the project's display name.
          type: string
        slug:
          description: |-
            Slug is the project the site was published into, created on the fly when
            the slug was free.
          type: string
        status:
          description: Status is the deployment status, "live" on success.
          type: string
        url:
          description: |-
            URL is the canonical live URL, https://<slug>.<apex> — empty when the
            subdomain belongs to another tenant and this site has none.
          type: string
      type: object
    projectsUpdate:
      properties:
        cacheControl:
          description: |-
            CacheControl replaces the Cache-Control policy the edge serves this site's
            HTML under. Absent leaves it.
          type: string
        description:
          description: Description replaces the one-line summary. Absent leaves it.
          type: string
        framework:
          description: |-
            Framework replaces the build hint. It affects the NEXT build only — nothing
            already deployed is rebuilt.
          type: string
        hidden:
          description: |-
            Hidden is MODERATION, and the only admin-gated field on this body: it pulls
            a public project out of the catalogue from admin.hanzo.ai without editing
            the publisher's own visibility choice, so un-hiding restores exactly what
            they asked for. A tenant sending it is ignored.
          type: boolean
        hiddenReason:
          description: |-
            HiddenReason records WHY moderation hid it, so the action can be explained
            and reviewed later. Admin-gated like hidden itself.
          type: string
        license:
          description: |-
            License is the terms that upstream work carries, with the same clear-versus-
            leave rule.
          type: string
        name:
          description: Name replaces the display name. Absent leaves it; the slug
            never moves with it.
          type: string
        repo:
          description: Repo relinks the git source. Absent leaves the existing link.
          properties:
            branch:
              type: string
            url:
              type: string
          type: object
        slug:
          description: |-
            Slug is the project to update, from the path. The URL is the addressing
            authority — a `slug` in the body cannot move the write to another project.
          type: string
        tags:
          additionalProperties:
            type: string
          description: |-
            Tags sets the site's browser tag config: platform slug → non-secret pixel id
            (e.g. {"ga4":"G-…","meta":"…"}). track.js injects these first-party and the
            server CAPI reads them, per site. Absent LEAVES them; a present object REPLACES
            the set (send {} to clear). The ids are public — they ship in the page — so this
            is not the SECRET path (a CAPI token is sealed via POST /v1/destinations).
          type: object
        upstream:
          description: |-
            Upstream credits the third-party work this project was published from, and is
            settable after the fact because the live demos are the ones that most need
            crediting. An explicit empty string CLEARS the credit; absent leaves it.
          type: string
        visibility:
          description: |-
            Visibility flips an existing project between "public" and "private". Same
            ONE rule as at create: public is free, private needs a paid plan.
          type: string
      type: object
    projectsUploadGrant:
      properties:
        expiresAt:
          description: |-
            ExpiresAt is when the grant stops being accepted, as Unix seconds. It is
            short-lived by design and is handed out ONCE, on the response that queues the
            deployment — a later read of that deployment does not carry it, so a grant
            cannot be fetched again after the build it was minted for.
          type: integer
        fields:
          additionalProperties:
            type: string
          description: |-
            Fields are form values every POST must carry VERBATIM, alongside `key` and
            `file`. The signature covers them, so altering any one of them — including
            widening the key to reach outside the prefix — invalidates the grant rather
            than extending it.
          type: object
        maxBytes:
          description: MaxBytes bounds ONE object, not the upload as a whole.
          type: integer
        prefix:
          description: |-
            Prefix is the only place this grant can write: the deployment's own key
            prefix. It authorizes WRITES ONLY, which is why completing a deployment
            reconciles the prefix against a manifest instead of letting CI delete.
          type: string
        url:
          description: |-
            URL is the address to POST each object to. It is signed for the PUBLIC
            endpoint, because the signature covers the host and CI posts from outside the
            cluster.
          type: string
      type: object
    promoteReq:
      properties:
        app:
          description: App is the application's slug, from the path.
          type: string
        deploymentId:
          description: |-
            DeploymentID promotes that deployment's exact built image. One of this and
            Tag is required.
          type: string
        project:
          description: Project is the project the application lives under, from the
            path.
          type: string
        tag:
          description: Tag promotes an image tag, resolved the same way a deploy resolves
            one.
          type: string
      type: object
    promptDetail:
      properties:
        createdAt:
          description: |-
            CreatedAt is when version 1 was written, RFC 3339 UTC. Appending a version
            does not move it.
          type: string
        labels:
          description: |-
            Labels is the current version's free-form taxonomy. `[]` when none, never
            null.
          items:
            type: string
          type: array
        lastUpdatedAt:
          description: |-
            UpdatedAt is when the current version was appended, RFC 3339 UTC. Equal to
            createdAt for a prompt that has only ever had one version.
          type: string
        name:
          description: Name is the prompt's org-unique handle and the URL segment
            it is addressed by.
          type: string
        prompt:
          description: |-
            Prompt is the CURRENT version's template body — the only content this service
            returns. Earlier versions are listed in versionHistory by number and date, and
            their bodies are not served in bulk.
          type: string
        tags:
          description: Tags is the second free-form taxonomy, same rules as Labels.
          items:
            type: string
          type: array
        type:
          description: |-
            Type labels the current version's kind; "text" unless the creator said
            otherwise.
          type: string
        version:
          description: |-
            Version is the current version number, starting at 1 and incremented by one on
            every create against an existing name.
          type: integer
        versionHistory:
          description: |-
            Versions is the history METADATA, newest first, capped at the last 100 — no
            bodies, so a long history cannot inflate this response. It always includes the
            current version as its first entry.
          items:
            $ref: '#/components/schemas/versionView'
          type: array
      type: object
    promptList:
      properties:
        data:
          description: |-
            Data is one row per prompt the org owns, each with its version numbers and
            taxonomy — never the template bodies.
          items:
            $ref: '#/components/schemas/promptMeta'
          type: array
      type: object
    promptMeta:
      properties:
        labels:
          description: |-
            Labels is the creator's free-form taxonomy, stored as given after trimming and
            de-duplication. Always present, `[]` when none — never null.
          items:
            type: string
          type: array
        lastUpdatedAt:
          description: |-
            LastUpdatedAt is when the newest version was appended, RFC 3339 UTC. Empty
            only if the record carries no timestamp at all.
          type: string
        name:
          description: |-
            Name is the prompt's org-unique handle and the URL segment it is fetched by:
            GET /v1/prompts/<name>.
          type: string
        tags:
          description: |-
            Tags is the second free-form taxonomy under the same rules as Labels. Nothing
            in this service interprets either; they are yours to organize by.
          items:
            type: string
          type: array
        type:
          description: |-
            Type labels the template's kind, "text" unless the creator said otherwise. It
            is the CURRENT version's type; earlier versions may carry a different one.
          type: string
        versions:
          description: |-
            Versions lists every version NUMBER this prompt has, newest first, capped at
            the last 100. The highest is the current one. (On a metrics row the same key
            is a count, not a list.)
          items:
            type: integer
          type: array
      type: object
    promptReq:
      properties:
        labels:
          description: Labels is free-form taxonomy, each up to 64 characters, capped
            at 32 entries.
          items:
            type: string
          type: array
        name:
          description: |-
            Name is the org-unique handle AND the URL segment the prompt is addressed by:
            1-64 characters matching ^[A-Za-z0-9][A-Za-z0-9._-]*$. "metrics", "new" and
            "catalog" are reserved. A name that already exists appends a new version.
          type: string
        prompt:
          description: |-
            Prompt is the template body, capped at 64 KiB. It holds template text only —
            never a secret.
          type: string
        tags:
          description: Tags is free-form taxonomy under the same bounds as Labels.
          items:
            type: string
          type: array
        type:
          description: Type labels the template's kind; defaults to "text".
          type: string
      type: object
    providerView:
      properties:
        available:
          description: |-
            Available is whether THIS DEPLOYMENT has the provider's app credentials, so
            connect can succeed. False renders the card without a working Connect button.
          type: boolean
        category:
          description: Category groups the card ("Communication", "Developer", "Marketing").
          type: string
        connected:
          description: Connected is whether this org has a live connection to the
            provider.
          type: boolean
        connection:
          $ref: '#/components/schemas/connectionView'
          description: |-
            Connection is the connected account's non-secret detail. Absent when the org
            has no connection; tokens NEVER appear here (they live only in KMS).
        description:
          description: Description is the one-line pitch the console card shows.
          type: string
        id:
          description: ID is the provider's registry id and the :provider path segment
            ("slack").
          type: string
        name:
          description: Name is the provider's display name ("Slack").
          type: string
      type: object
    provisionRequest:
      properties:
        instance:
          type: string
        name:
          type: string
      type: object
    provisionResult:
      properties:
        connectionString:
          type: string
        database:
          type: string
        host:
          type: string
        id:
          type: string
        kind:
          type: string
        name:
          type: string
        password:
          type: string
        port:
          type: integer
        status:
          type: string
        username:
          type: string
      type: object
    provisionedResource:
      properties:
        database:
          description: |-
            Database is the logical database, collection, index or bucket this
            resource resolves to on its backend.
          type: string
        host:
          description: |-
            Host is the address that actually routes to this resource — a dedicated
            instance's own in-cluster Service, or the public gateway for a shared one.
          type: string
        id:
          description: ID is the resource's server-minted handle, "rs_"-prefixed.
          type: string
        kind:
          description: 'Kind is the product: sql, vector, datastore, kv, search, s3
            or docdb.'
          type: string
        name:
          description: Name is the org-unique slug the caller provisioned the resource
            under.
          type: string
        port:
          description: Port is the port a client connects to on Host.
          type: integer
        status:
          description: |-
            Status is "ready", or "provisioning" while a dedicated instance is still
            being materialized. A dedicated resource's status is reconciled from the
            operator's live CR before this is answered, so it is never a stale ready.
          type: string
        username:
          description: |-
            Username is the credential's user, for the kinds that mint one per
            resource. Absent for a kind whose backend authenticates with a shared,
            out-of-band key.
          type: string
      type: object
    provisionedSummary:
      properties:
        createdAt:
          description: CreatedAt is when the resource was provisioned, in unix seconds.
          type: integer
        host:
          description: |-
            Host is the address that actually routes to this resource — a dedicated
            instance's own in-cluster Service, or the public gateway for a shared one.
            Never the internal admin address of a shared backend.
          type: string
        id:
          description: ID is the resource's server-minted handle, "rs_"-prefixed.
          type: string
        kind:
          description: 'Kind is the product: sql, vector, datastore, kv, search, s3
            or docdb.'
          type: string
        name:
          description: Name is the org-unique slug the caller provisioned the resource
            under.
          type: string
        port:
          description: Port is the port a client connects to on Host.
          type: integer
        status:
          description: |-
            Status is "ready", or "provisioning" while a dedicated instance is still
            being materialized by the operator.
          type: string
      type: object
    publishKitIn:
      properties:
        category:
          description: Category groups the kit in the gallery browser.
          type: string
        demo:
          description: Demo is the deployed site itself, when there is one.
          type: string
        description:
          description: Description is the browse-card blurb, max 4096 characters.
          type: string
        features:
          description: Features are the highlights the card lists, at most 32.
          items:
            type: string
          type: array
        framework:
          description: Framework is the stack the kit is built on ("Next.js 14").
          type: string
        preview:
          description: Preview is the still image the browse card renders, max 4096
            characters.
          type: string
        slug:
          description: Slug is the kit's identity — lowercase alphanumeric with dashes,
            max 40.
          type: string
        source:
          description: Source is the repository the kit is forked from, max 4096 characters.
          type: string
        title:
          description: Title is the display name. Required, max 200 characters.
          type: string
        useCase:
          description: UseCase is what the kit is for, in a phrase.
          type: string
        variants:
          description: Variants are the shapes this kit ships in, at most 32; the
            fork picks one.
          items:
            $ref: '#/components/schemas/Variant'
          type: array
      type: object
    pullList:
      properties:
        data:
          description: Data holds the repo's pull requests, newest number first.
          items:
            $ref: '#/components/schemas/pullView'
          type: array
      type: object
    pullView:
      properties:
        author:
          description: Author is the user who opened it; empty for a caller with no
            user.
          type: string
        base:
          description: Base is the branch the work is proposed into.
          type: string
        body:
          description: Body is the longer description; empty when none was given.
          type: string
        createdAt:
          description: CreatedAt is RFC 3339 UTC.
          type: string
        head:
          description: Head is the branch holding the work.
          type: string
        mergedRev:
          description: |-
            MergedRev is what base points at now that the merge landed. Empty while
            the proposal is open.
          type: string
        number:
          description: Number is the proposal's per-repo handle, dense from 1.
          type: integer
        repo:
          description: Repo is the repository the proposal belongs to.
          type: string
        state:
          description: State is "open" or "merged".
          type: string
        title:
          description: Title is the one-line summary.
          type: string
        updatedAt:
          description: UpdatedAt is RFC 3339 UTC.
          type: string
      type: object
    purgeIn:
      properties:
        files:
          description: Files purges exactly the listed URLs — at most 30, Cloudflare's
            per-request cap.
          items:
            type: string
          type: array
        purge_everything:
          description: Everything drops the zone's entire edge cache.
          type: boolean
        zone:
          description: Zone is the 32-hex Cloudflare zone id, from the path.
          type: string
      type: object
    purgeOut:
      properties:
        purged:
          description: Purged is the number of messages removed.
          type: integer
      type: object
    push:
      properties:
        after:
          type: string
        before:
          type: string
        pusher:
          properties:
            login:
              type: string
            username:
              type: string
          type: object
        ref:
          type: string
        repository:
          properties:
            name:
              type: string
            owner:
              properties:
                login:
                  type: string
                username:
                  type: string
              type: object
          type: object
      type: object
    pushFile:
      properties:
        content:
          description: Content is the file's bytes, carried per Encoding.
          type: string
        encoding:
          description: Encoding is "base64", or "utf-8" (the default, also "utf8"
            / "text").
          type: string
        path:
          description: Path is repo-relative. Absolute or traversing paths are refused.
          type: string
      type: object
    pushReq:
      properties:
        branch:
          description: |-
            Branch to advance; empty means "main". A fresh branch that is the repo's
            first also becomes HEAD.
          type: string
        files:
          description: |-
            Files are added to or overwritten on the branch tip — files already there
            and not listed SURVIVE. At least one, at most 5000, 32 MiB each.
          items:
            $ref: '#/components/schemas/pushFile'
          type: array
        message:
          description: Message is the commit message; empty gets a generated one.
          type: string
        name:
          description: |-
            Name is the repo to push into, from the :name path segment. It is CREATED
            on first push if it does not exist.
          type: string
      type: object
    pushResp:
      properties:
        branch:
          description: Branch is the branch that was advanced, resolved (never empty).
          type: string
        cloneUrl:
          description: CloneURL is the repo's HTTPS remote.
          type: string
        commit:
          description: Commit is the new commit's full hash.
          type: string
        sshUrl:
          description: SSHURL is the repo's scp-style SSH remote.
          type: string
      type: object
    quoteList:
      properties:
        results:
          description: |-
            Results is one quote per name, priced RETAIL — this deployment's markup is
            already applied and the wholesale cost is never on the wire.
          items:
            $ref: '#/components/schemas/Offer'
          type: array
      type: object
    reachability:
      properties:
        configured:
          description: Configured is whether the wholesale credentials are present
            at all.
          type: boolean
        env:
          description: |-
            Env is the registrar environment. It is the fact that decides whether money
            moves: only "prod" reaches the live, billable registrar — anything else,
            including unset, is the sandbox.
          type: string
        error:
          description: Error is the blocker, so an operator reads it instead of guessing
            at it.
          type: string
        reachable:
          description: |-
            Reachable is whether the registrar accepted those credentials on a live call
            made while the caller waited.
          type: boolean
        registrar:
          description: Registrar names the wholesale registrar behind it.
          type: string
        service:
          description: Service names the subsystem answering.
          type: string
        status:
          description: Status is "ok" when a live call succeeded, else "degraded".
          type: string
      type: object
    readOut:
      properties:
        messages:
          description: Messages is what was read, stream-ordered.
          items:
            $ref: '#/components/schemas/Delivery'
          type: array
      type: object
    readiness:
      properties:
        crd:
          description: |-
            CRD is whether the operator App CRD was found, and is absent when no
            cluster client resolved and the question could not be asked.
          type: boolean
        error:
          description: Error is the real reason the plane is degraded; absent when
            it is not.
          type: string
        k8s:
          description: K8s is whether a cluster client resolved at all. False means
            no kubeconfig.
          type: boolean
        service:
          description: Service is always "platform" — which control plane answered.
          type: string
        status:
          description: Status is "ok" when this plane can deploy, "degraded" when
            it cannot.
          type: string
      type: object
    readmeJSON:
      properties:
        content:
          description: Content is the file's text, verbatim and unrendered.
          type: string
        encoding:
          description: Encoding is always "utf8" — a README is text by definition.
          type: string
        path:
          description: Path is the file the README was found at (README.md, README,
            …).
          type: string
      type: object
    refJSON:
      properties:
        name:
          description: Name is the short ref name ("main", "v1.2.0"), not the full
            refs/… path.
          type: string
        sha:
          description: SHA is the full commit hash the ref resolves to.
          type: string
      type: object
    refreshOut:
      properties:
        connector:
          $ref: '#/components/schemas/connView'
          description: Connection is the connector with its new expiry.
        refreshed:
          description: Refreshed is always true — a failed rotation is an HTTP error.
          type: boolean
      type: object
    refsJSON:
      properties:
        branches:
          description: Branches are the repo's heads; empty on a repo with no commits.
          items:
            $ref: '#/components/schemas/refJSON'
          type: array
        default:
          description: Default is the branch name a caller gets when it asks for no
            ref.
          type: string
        tags:
          description: Tags are the repo's tags; empty when there are none.
          items:
            $ref: '#/components/schemas/refJSON'
          type: array
      type: object
    registerKeyReq:
      properties:
        publicKey:
          description: |-
            PublicKey is one OpenSSH authorized-key line ("ssh-ed25519 AAAA… you@host").
            Required; a line that does not parse is refused and never stored.
          type: string
        title:
          description: |-
            Title labels the key in the console. Max 256 chars; when omitted the
            comment on the key line is used.
          type: string
      type: object
    registerReq:
      properties:
        account:
          type: string
        actor:
          type: string
        agent:
          type: string
        cwd:
          type: string
        host:
          description: Execution context — where this session runs (all optional).
          type: string
        parentSessionId:
          type: string
        project:
          description: |-
            The readable build (provenance.go): which product this session builds, and
            whether its story may be read by the world.
          type: string
        provider:
          description: Account tag — the linked AI account this session ran under
            (login manager).
          type: string
        published:
          type: boolean
        repo:
          type: string
        status:
          type: string
        target:
          type: string
        taskRunId:
          type: string
        taskWorkflowId:
          type: string
        terminal:
          description: |-
            Terminal is the URL this session's live terminal is published at, so the
            console can watch it. Optional — a session that publishes nothing is still
            a session.
          type: string
        title:
          type: string
      type: object
    registryImage:
      properties:
        name:
          description: Name is the repository name inside the org's namespace (e.g.
            "cloud").
          type: string
        ref:
          description: |-
            Ref is the full pullable reference (host + org + name) the docker CLI
            takes verbatim.
          type: string
      type: object
    registryImageList:
      properties:
        data:
          description: Data is the org's repositories.
          items:
            $ref: '#/components/schemas/registryImage'
          type: array
        truncated:
          description: |-
            Truncated is true when the catalog walk hit its page bound before the
            registry was exhausted — the list is a prefix, not the whole.
          type: boolean
      type: object
    registryMint:
      properties:
        image:
          description: Image is the repository name inside the org's namespace (e.g.
            "cloud").
          type: string
      type: object
    registryPackage:
      properties:
        description:
          description: Description says what the package is, as published.
          type: string
        name:
          description: Name is the package name (`<org>` or `@<org>/…`).
          type: string
        updated:
          description: Updated is when the package last changed, as the registry reports
            it.
          type: string
        version:
          description: Version is the latest published version.
          type: string
      type: object
    registryPackageList:
      properties:
        data:
          description: Data is the packages in the org's scope.
          items:
            $ref: '#/components/schemas/registryPackage'
          type: array
      type: object
    registryProject:
      properties:
        images:
          description: Images is how many of the org's repositories the OCI catalog
            holds.
          type: integer
        packages:
          description: Packages is how many of the org's packages the npm registry
            reports.
          type: integer
        project:
          description: |-
            Project is the namespace: the org's slug, which prefixes its image names
            and scopes its npm packages.
          type: string
      type: object
    registryProjectList:
      properties:
        data:
          description: Data is the namespaces visible to the caller.
          items:
            $ref: '#/components/schemas/registryProject'
          type: array
      type: object
    registryStatus:
      properties:
        host:
          description: Host is the OCI registry host clients push to and pull from.
          type: string
        oci:
          description: Oci is true when the OCI registry answered its /v2/ probe.
          type: boolean
        pkg:
          description: Pkg is true when the npm registry answered its ping.
          type: boolean
        pkgHost:
          description: PkgHost is the npm registry host.
          type: string
        realm:
          description: |-
            Realm is the token endpoint the OCI registry's challenge advertises,
            present only when the registry is reachable and auth-gated.
          type: string
        service:
          description: Service is the token service name from the same challenge.
          type: string
      type: object
    registryTagList:
      properties:
        data:
          description: Data is the tag names, as the registry reports them.
          items:
            type: string
          type: array
        image:
          description: Image is the repository name inside the org's namespace.
          type: string
        ref:
          description: Ref is the full repository reference the tags belong to.
          type: string
      type: object
    registryToken:
      properties:
        expires:
          description: Expires is the token's lifetime in seconds.
          type: integer
        ref:
          description: Ref is the one repository reference the token can pull.
          type: string
        token:
          description: |-
            Token is the bearer to present on the OCI wire
            (`Authorization: Bearer …` against the host's /v2/ routes).
          type: string
      type: object
    releaseBoard:
      properties:
        releases:
          description: Releases are the deployments that genuinely reached the cluster.
          items:
            $ref: '#/components/schemas/releaseRow'
          type: array
      type: object
    releaseRow:
      properties:
        environment:
          description: Environment is the deploy target the application names.
          type: string
        id:
          description: |-
            ID is the deployment's id — a release IS a deployment that reached the
            cluster.
          type: string
        name:
          description: Name is the application the release belongs to.
          type: string
        releasedAt:
          description: ReleasedAt is when the deployment last changed, RFC3339 UTC.
          type: string
        status:
          description: Status is deploying or live — the two states that mean released.
          type: string
        version:
          description: Version is the released image tag, or v<n> when the image carries
            none.
          type: string
      type: object
    renewReq:
      properties:
        domain:
          description: Domain is the name to extend. It is required, and the caller's
            org must hold it.
          type: string
        years:
          description: Years is how much longer to hold it, defaulting to 1.
          type: integer
      required:
      - domain
      type: object
    replaceKitIn:
      properties:
        category:
          description: Category groups the kit in the gallery browser.
          type: string
        demo:
          description: Demo is the deployed site itself, when there is one.
          type: string
        description:
          description: Description is the browse-card blurb, max 4096 characters.
          type: string
        features:
          description: Features are the highlights the card lists, at most 32.
          items:
            type: string
          type: array
        framework:
          description: Framework is the stack the kit is built on ("Next.js 14").
          type: string
        preview:
          description: Preview is the still image the browse card renders, max 4096
            characters.
          type: string
        slug:
          description: Slug is the kit to replace, from the path.
          type: string
        source:
          description: Source is the repository the kit is forked from, max 4096 characters.
          type: string
        title:
          description: Title is the display name. Required, max 200 characters.
          type: string
        useCase:
          description: UseCase is what the kit is for, in a phrase.
          type: string
        variants:
          description: Variants are the shapes this kit ships in, at most 32; the
            fork picks one.
          items:
            $ref: '#/components/schemas/Variant'
          type: array
      type: object
    replayBody:
      properties:
        distinctId:
          type: string
        events:
          items: {}
          type: array
        sessionId:
          type: string
        windowId:
          type: string
      type: object
    repoList:
      properties:
        data:
          description: Data holds the repos in scope, most recently updated first.
          items:
            $ref: '#/components/schemas/repoView'
          type: array
      type: object
    repoTree:
      properties:
        files:
          description: |-
            Files are the repo's indexed files in path order, each with its language
            and how many symbols it defines. Never null.
          items:
            $ref: '#/components/schemas/TreeEntry'
          type: array
        repo:
          description: Repo echoes the repository that was walked.
          type: string
      type: object
    repoView:
      properties:
        branches:
          description: |-
            Branches are the repo's branch names. Read live, so the detail view carries
            them and a list row does not.
          items:
            type: string
          type: array
        cloneUrl:
          description: CloneURL is the HTTPS smart-HTTP remote `git clone` takes.
          type: string
        createdAt:
          description: CreatedAt is RFC 3339 UTC.
          type: string
        defaultBranch:
          description: DefaultBranch is where HEAD points on a fresh repo ("main").
          type: string
        description:
          description: Description is the caller-supplied blurb (max 4KiB).
          type: string
        head:
          description: Head is the resolved HEAD commit, empty on an empty repo.
          type: string
        id:
          description: ID is the repo's stable, prefixed identifier ("repo_" + 128
            random bits).
          type: string
        name:
          description: Name is the org-unique handle, and the last path segment of
            both URLs below.
          type: string
        org:
          description: Org owns the repo — the gateway-minted X-Org-Id, and the isolation
            key.
          type: string
        project:
          description: |-
            Project is the optional sub-scope the repo lives in; absent for the org's
            default scope.
          type: string
        public:
          description: |-
            Public grants ANONYMOUS read (fetch) only; push and the whole control plane
            stay org-authed.
          type: boolean
        sizeBytes:
          description: |-
            SizeBytes is the repo's measured on-disk size, re-measured on create, after
            each push, and after a gc. This is the number billing meters.
          type: integer
        sshUrl:
          description: SSHURL is the scp-style SSH remote (git@host:org/repo.git).
          type: string
        updatedAt:
          description: UpdatedAt is RFC 3339 UTC, empty until the first write.
          type: string
      type: object
    reportOut:
      properties:
        delivered:
          description: |-
            Delivered is true when a waiting durable owner received this result. False
            means there was none to deliver to — an unknown or already-finished run — which
            is a clean no-op, not an error.
          type: boolean
      type: object
    reportReq:
      properties:
        account:
          description: Account is the linked account the window was metered from.
          type: string
        cachedInputTokens:
          description: CachedInputTokens is the prompt tokens the provider served
            from cache.
          type: integer
        confidence:
          description: Confidence says how much the counters below mean.
          type: string
        costCents:
          description: CostCents is what the window cost on the PROVIDER's own plan,
            in US cents.
          type: integer
        costLimitCents:
          description: CostLimitCents is the plan's spend ceiling for the window,
            in US cents.
          type: integer
        currency:
          description: Currency is the provider's currency when it is not US cents.
          type: string
        inputTokens:
          description: InputTokens is prompt tokens consumed in the window.
          type: integer
        kind:
          description: |-
            Kind is subscription or apikey. Empty is accepted; anything else is
            refused.
          type: string
        lane:
          description: Lane is the meter lane within the account.
          type: string
        machine:
          description: Machine is the host whose meter read the window. Required on
            every sample.
          type: string
        outputTokens:
          description: OutputTokens is completion tokens produced in the window.
          type: integer
        plan:
          description: Plan is the subscription plan the account is on, as the provider
            names it.
          type: string
        provider:
          description: |-
            Provider is the upstream the account belongs to, e.g. anthropic. Required
            on every sample.
          type: string
        requests:
          description: Requests is how many requests the window covers.
          type: integer
        resetsAt:
          description: ResetsAt is when the measured window rolls over, RFC3339.
          type: string
        samples:
          description: |-
            Samples is the batch form: every lane a poller measured, in one call. When
            it is non-empty the top-level sample fields are ignored.
          items:
            $ref: '#/components/schemas/sampleReq'
          type: array
        synthetic:
          description: Synthetic marks a window the meter inferred rather than read.
          type: boolean
        totalTokens:
          description: TotalTokens is the window's total tokens.
          type: integer
        usedPct:
          description: UsedPct is how much of the window's allowance is consumed,
            0–100.
          type: number
        window:
          description: |-
            Window is the window class: 6h, day, week or month. Required, and a class
            this surface does not know is refused rather than rewritten.
          type: string
        windowMinutes:
          description: |-
            WindowMinutes is the window's real length in minutes, as the meter reports
            it.
          type: integer
        windowStart:
          description: |-
            WindowStart is when the measured window opened, RFC3339. This is how a
            backfill states WHICH window it measured; the server always owns the
            observation clock, so there is no timestamp field.
          type: string
      type: object
    reportResp:
      properties:
        accepted:
          description: |-
            Accepted is how many samples passed validation. Every one of them was
            accepted, or the whole report was refused — there is no partial success.
          type: integer
        stored:
          description: |-
            Stored is whether the warehouse actually persisted them. False means the
            datastore was unavailable and the poll of history was lost; the request
            still succeeded, so a device retries without being blocked.
          type: boolean
      type: object
    reportRunIn:
      properties:
        branch:
          description: |-
            Branch, CommitSha and Diffstat describe what the run produced; Error is the
            failure when OK is false. Each is clamped, never rejected.
          type: string
        changed:
          type: boolean
        commitSha:
          type: string
        diffstat:
          type: string
        error:
          type: string
        id:
          description: ID is the machine reporting, from the path.
          type: string
        ok:
          description: OK is whether the run succeeded; Changed whether it produced
            any commit.
          type: boolean
        runId:
          description: RunID is the routed run being completed, from the path.
          type: string
      type: object
    resourceUsage:
      properties:
        costCents:
          type: number
        cpuVcpuHours:
          type: number
        memGbHours:
          type: number
        storageIoBytes:
          type: number
      type: object
    restartRef:
      properties:
        app:
          description: App is the service's CR name, from the path. It must be a DNS-1123
            label.
          type: string
        env:
          description: |-
            Env is REQUIRED and must be main, test or dev. A bare call does not default
            to production, which is what closes the fat-finger and confused-deputy
            hazard.

            It carries no `validate:"required"`: the handler already refuses an empty env
            with the sentence that names the three values, and a validator tag would
            replace that sentence with a generic one. The requirement is stated here and
            enforced there, once.
          type: string
      type: object
    restarted:
      properties:
        app:
          description: App is the service that was restarted.
          type: string
        env:
          description: Env is that namespace's lifecycle env.
          type: string
        namespace:
          description: Namespace is the namespace its Deployment was patched in.
          type: string
        ok:
          description: OK is always true — a failure is an error, not a false here.
          type: boolean
        restartedAt:
          description: RestartedAt is the timestamp stamped onto the pod template,
            RFC3339 UTC.
          type: string
      type: object
    revokedKey:
      properties:
        ok:
          description: |-
            OK is true when the key was revoked. A failure is an error status, never a
            false here.
          type: boolean
        type:
          description: |-
            Type is the key class that was revoked, resolved — so a caller that named
            nothing can see it revoked the secret key.
          type: string
      type: object
    rollbackReq:
      properties:
        app:
          description: App is the application's slug, from the path.
          type: string
        deploymentId:
          description: |-
            DeploymentID is the deployment to redeploy. Omit it to return to the
            previous release.
          type: string
        project:
          description: Project is the project the application lives under, from the
            path.
          type: string
      type: object
    routeCreateIn:
      properties:
        pattern:
          description: Pattern is the URL pattern to bind, e.g. "acme.com/api/*".
          type: string
        script:
          description: |-
            Script is the Worker script to dispatch to. Omit it to leave the pattern
            bound to no script, which is how Cloudflare expresses "bypass the Worker here".
          type: string
        zone:
          description: Zone is the 32-hex Cloudflare zone id, from the path.
          type: string
      type: object
    routedRunOut:
      properties:
        base:
          type: string
        branch:
          type: string
        cloneUrl:
          type: string
        project:
          type: string
        prompt:
          type: string
        repo:
          description: Repo is the repository to work in and CloneURL is how to fetch
            it.
          type: string
        sessionId:
          description: |-
            SessionID is the live session opened at dispatch; the machine streams its
            turns into it.
          type: string
        timeoutSeconds:
          description: TimeoutSeconds bounds the run on the machine; 0 means the machine's
            own default.
          type: integer
      type: object
    routerList:
      properties:
        routers:
          description: Routers is one row per ZT edge-router tagged with the caller's
            org role.
          items:
            $ref: '#/components/schemas/routerView'
          type: array
      type: object
    routerView:
      properties:
        id:
          description: ID is the ZT edge-router's id.
          type: string
        name:
          description: Name is the edge-router's name, falling back to its id when
            it has none.
          type: string
        region:
          description: |-
            Region comes from a "region-<slug>" role attribute and is omitted when the
            router carries none, so the column renders "—" rather than a guess.
          type: string
        status:
          description: |-
            Status is the controller's own health signal: "online" when connected,
            "disabled" when administratively disabled, "offline" otherwise.
          type: string
      type: object
    runList:
      properties:
        runs:
          description: Runs is the agent's executions, newest first.
          items:
            $ref: '#/components/schemas/agentRunView'
          type: array
      type: object
    runPage:
      properties:
        data:
          description: Data is the page of runs, newest first.
          items:
            $ref: '#/components/schemas/FlowRun'
          type: array
      type: object
    runRecord:
      properties:
        avgScore:
          description: AvgScore is the mean over the scored examples.
          type: number
        createdAt:
          description: CreatedAt is when the run first landed.
          type: string
        dataset:
          description: Dataset is the set that was scored.
          type: string
        items:
          description: Items is how many examples were attempted.
          type: integer
        judgeModel:
          description: JudgeModel is the model that graded.
          type: string
        model:
          description: Model is the model under test.
          type: string
        runName:
          description: RunName is the run's label.
          type: string
        scored:
          description: Scored is how many produced a real score.
          type: integer
        updatedAt:
          description: UpdatedAt is when the record last changed.
          type: string
      type: object
    runReq:
      properties:
        env:
          description: |-
            Env is the run's environment. Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`;
            a variable marked `secret: true` is sealed into KMS.
          items:
            $ref: '#/components/schemas/EnvVarJSON'
          type: array
        gpu:
          description: GPU is how many GPUs the run asks for; a negative value is
            400.
          type: integer
        image:
          description: Image is the container image to run. Required.
          type: string
        maxScale:
          description: |-
            MaxScale above the floor declares an autoscaling ceiling; 0 means no
            autoscaler at all — a fixed run at the floor.
          type: integer
        minScale:
          description: MinScale is the replica floor, clamped to the deployment's
            limit.
          type: integer
        name:
          description: |-
            Name is the run's name, and the slug is derived from it. Required, and it
            must resolve to `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`. Re-running the same
            name updates that run in place.
          type: string
        port:
          description: Port is the container port the run listens on.
          type: integer
        runtime:
          description: |-
            Runtime is accepted for the client contract and echoed nowhere: the image
            IS the runtime unit.
          type: string
        shape:
          description: |-
            Shape is a compute size label, echoed back; sizing is the operator's
            default. Defaults to "auto".
          type: string
      type: object
    runRequest:
      properties:
        dataset:
          description: |-
            Dataset is the set to score, which must belong to the caller's org and hold
            at least one ACTIVE example.
          type: string
        judge:
          $ref: '#/components/schemas/judgeSpec'
          description: |-
            Judge is the judge to grade with. Omitted, the model under test grades
            itself against a default correctness criterion under the score name
            "llm-judge".
        limit:
          description: |-
            Limit caps how many examples this run scores. It defaults to 20, and
            anything above 100 falls back to that default.
          type: integer
        model:
          description: Model is the model under test.
          type: string
        runName:
          description: |-
            RunName labels the run and is generated from the clock when omitted. It
            must match ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$.
          type: string
      required:
      - dataset
      - model
      type: object
    runSummary:
      properties:
        avgScore:
          description: AvgScore is the mean over the scored examples, 0 when none
            scored.
          type: number
        dataset:
          description: Dataset is the set that was scored.
          type: string
        items:
          description: Items is how many examples the run attempted.
          type: integer
        judgeModel:
          description: JudgeModel is the model that graded.
          type: string
        model:
          description: Model is the model under test.
          type: string
        results:
          description: Results is one row per attempted example.
          items:
            $ref: '#/components/schemas/itemResult'
          type: array
        runName:
          description: RunName is the run's label, which scores and traces are filed
            under.
          type: string
        scored:
          description: |-
            Scored is how many produced a real score. It counts successes only, so a
            partial run is honest about what it achieved.
          type: integer
      type: object
    runView:
      properties:
        id:
          description: ID is the application id the run created or converged.
          type: string
        name:
          description: Name is the run's name, as stored.
          type: string
        shape:
          description: Shape is the compute size label the request asked for, or "auto".
          type: string
        status:
          description: Status is the application's state — `deploying` on a fresh
            accept.
          type: string
        url:
          description: URL is the run's live HTTPS address.
          type: string
      type: object
    runnerBuildReq:
      properties:
        arch:
          description: Arch is the target architecture for the artifact lane.
          type: string
        args:
          additionalProperties:
            type: string
          description: |-
            Args are --build-arg values. They are what lets several images off ONE
            Dockerfile mean different things — the sandbox classes are three entries
            differing only by STAGE. Validated at the k8s choke point, with VERSION and
            REVISION taking precedence: those are receipts the builder derives from the
            tag and the commit, and a caller that could overwrite them could make an
            image lie about which commit it is.
          type: object
        binaries:
          description: |-
            Binaries selects the ARTIFACT lane (artifact.go): build what the repo's
            hanzo.yml `binaries:` block declares — a Go binary, an npm tarball, a Rust
            binary — and publish it to hanzoai/s3 instead of pushing an image. It is the
            same recipe hanzoai/ci reads, sent verbatim, so `image` is meaningless here
            and must be absent.
          items:
            $ref: '#/components/schemas/binarySpec'
          type: array
        branch:
          description: Branch is the branch to build when no SHA or Ref is given.
          type: string
        bucket:
          description: Bucket mirrors hanzo.yml's `bucket:` — where the artifact lane
            publishes.
          type: string
        context:
          description: Context is the build context path within the repo.
          type: string
        dockerTarget:
          description: DockerTarget is the multi-stage build target to stop at.
          type: string
        dockerfile:
          description: Dockerfile is the path to build from; empty uses the zero-config
            frontend.
          type: string
        image:
          description: |-
            Image is the output image ref to push. Required on the image lane, and it
            must target a registry namespace the caller's org owns.
          type: string
        organizationId:
          description: |-
            OrgID attributes the build to an org. On the IAM path it defaults to the
            caller's own validated org, and a foreign one is refused unless the caller
            is a platform SuperAdmin.
          type: string
        os:
          description: OS is the target operating system for the artifact lane.
          type: string
        ref:
          description: Ref is the git ref to build when no SHA is given.
          type: string
        repo:
          description: Repo is the repository clone URL to build. Required on the
            image lane.
          type: string
        sha:
          description: SHA is the commit to pin; it wins over Ref and Branch.
          type: string
        tag:
          description: |-
            Tag is the publish path segment, so both front doors write ONE index at ONE
            URL. It defaults to the pinned ref, and must be named explicitly for a
            branch.
          type: string
      type: object
    runnerBuildResp:
      properties:
        buildJobId:
          description: BuildJobID is the queued build's id, and what its progress
            is read by.
          type: string
        image:
          description: Image is the ref the image lane will push.
          type: string
        index:
          description: Index is the binaries.json URL the artifact lane will publish.
          type: string
        runnerPool:
          description: RunnerPool is the runner class the build was placed on.
          type: string
        status:
          description: Status is `queued` — the build was accepted and has not finished.
          type: string
        target:
          description: Target is the multi-stage build target, echoed back.
          type: string
      type: object
    runs:
      properties:
        data:
          description: Data is the caller org's runs, bounded by limit.
          items:
            $ref: '#/components/schemas/runRecord'
          type: array
      type: object
    sampleAccepted:
      properties:
        recorded:
          description: |-
            Recorded is always true: the response is an acknowledgement, and the
            warehouse write is detached, so it reports acceptance, not durability.
          type: boolean
      type: object
    sampleIngest:
      properties:
        gpuModel:
          type: string
        gpuUtil:
          description: |-
            GPUUtil is accelerator utilization as a fraction 0..1; the warehouse clamps
            anything outside that.
          type: number
        gpus:
          description: |-
            GPUs is how many accelerators the reading covers, GPUModel the representative
            model name.
          type: integer
        host:
          description: Host is the node's hostname, for display.
          type: string
        memFree:
          type: integer
        memUsed:
          description: MemUsed and MemFree are host memory in bytes.
          type: integer
        unit:
          description: |-
            Unit is the reporting node's own id — the same id it registered under, and
            the key the board joins this series onto. Required.
          type: string
      type: object
    sampleList:
      properties:
        samples:
          description: Samples are the readings, OLDEST first — the order a chart
            plots.
          items:
            $ref: '#/components/schemas/sampleView'
          type: array
      type: object
    sampleReq:
      properties:
        account:
          description: Account is the linked account the window was metered from.
          type: string
        cachedInputTokens:
          description: CachedInputTokens is the prompt tokens the provider served
            from cache.
          type: integer
        confidence:
          description: Confidence says how much the counters below mean.
          type: string
        costCents:
          description: CostCents is what the window cost on the PROVIDER's own plan,
            in US cents.
          type: integer
        costLimitCents:
          description: CostLimitCents is the plan's spend ceiling for the window,
            in US cents.
          type: integer
        currency:
          description: Currency is the provider's currency when it is not US cents.
          type: string
        inputTokens:
          description: InputTokens is prompt tokens consumed in the window.
          type: integer
        kind:
          description: |-
            Kind is subscription or apikey. Empty is accepted; anything else is
            refused.
          type: string
        lane:
          description: Lane is the meter lane within the account.
          type: string
        machine:
          description: Machine is the host whose meter read the window. Required.
          type: string
        outputTokens:
          description: OutputTokens is completion tokens produced in the window.
          type: integer
        plan:
          description: Plan is the subscription plan the account is on, as the provider
            names it.
          type: string
        provider:
          description: Provider is the upstream the account belongs to, e.g. anthropic.
            Required.
          type: string
        requests:
          description: Requests is how many requests the window covers.
          type: integer
        resetsAt:
          description: |-
            ResetsAt is when the measured window rolls over, RFC3339. Empty is
            allowed; anything else that is not RFC3339 is refused.
          type: string
        synthetic:
          description: Synthetic marks a window the meter inferred rather than read.
          type: boolean
        totalTokens:
          description: TotalTokens is the window's total tokens.
          type: integer
        usedPct:
          description: UsedPct is how much of the window's allowance is consumed,
            0–100.
          type: number
        window:
          description: |-
            Window is the window class: 6h, day, week or month. Required, and a class
            this surface does not know is refused rather than rewritten.
          type: string
        windowMinutes:
          description: |-
            WindowMinutes is the window's real length in minutes, as the meter reports
            it.
          type: integer
        windowStart:
          description: |-
            WindowStart is when the measured window opened, RFC3339. Empty is allowed;
            anything else that is not RFC3339 is refused.
          type: string
      type: object
    sampleView:
      properties:
        at:
          type: string
        costCents:
          type: integer
        cpus:
          type: integer
        gpuModel:
          type: string
        gpuUtil:
          type: number
        gpus:
          type: integer
        host:
          type: string
        kind:
          type: string
        load1:
          type: number
        load5:
          type: number
        load15:
          type: number
        memFree:
          type: integer
        memUsed:
          type: integer
        memory:
          type: integer
        source:
          type: string
        unit:
          type: string
      type: object
    scoreConfigList:
      properties:
        data:
          description: Data is the caller org's rubrics, bounded by limit.
          items:
            $ref: '#/components/schemas/scoreConfigView'
          type: array
      type: object
    scoreConfigReq:
      properties:
        categories:
          description: |-
            Categories is the closed set of labels a CATEGORICAL score may carry. A
            CATEGORICAL rubric with none is refused.
          items:
            type: string
          type: array
        dataType:
          description: DataType is NUMERIC (the default), CATEGORICAL or BOOLEAN.
          type: string
        maxValue:
          description: MaxValue is the inclusive ceiling a NUMERIC score must stay
            under, finite.
          type: number
        minValue:
          description: |-
            MinValue is the inclusive floor a NUMERIC score must clear. It must be
            finite and must not exceed MaxValue.
          type: number
        name:
          description: |-
            Name is the score name this rubric governs, matching
            ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. The name is the key, so re-posting one
            replaces its rules.
          type: string
      required:
      - name
      type: object
    scoreConfigView:
      properties:
        categories:
          description: Categories is the closed set of labels a CATEGORICAL score
            may carry.
          items:
            type: string
          type: array
        createdAt:
          description: CreatedAt is when the rubric was first declared.
          type: string
        dataType:
          description: |-
            DataType is NUMERIC, CATEGORICAL or BOOLEAN, and is authoritative — a
            score recorded under this name cannot claim a different one.
          type: string
        maxValue:
          description: MaxValue is the inclusive ceiling a NUMERIC score must stay
            under, absent when unbounded.
          type: number
        minValue:
          description: MinValue is the inclusive floor a NUMERIC score must clear,
            absent when unbounded.
          type: number
        name:
          description: Name is the score name this rubric governs.
          type: string
        updatedAt:
          description: UpdatedAt is when it last changed.
          type: string
      type: object
    scoreList:
      properties:
        data:
          description: Data is the caller org's score events matching the filters,
            bounded by limit.
          items:
            $ref: '#/components/schemas/scoreView'
          type: array
      type: object
    scoreReq:
      properties:
        comment:
          description: Comment is the grader's reasoning, truncated at 2000 characters.
          type: string
        dataType:
          description: |-
            DataType is NUMERIC, CATEGORICAL or BOOLEAN. A declared rubric overrides it
            — a caller cannot claim a type the org's rubric contradicts.
          type: string
        datasetItemId:
          description: ItemID attaches the score to one graded example.
          type: string
        datasetName:
          description: Dataset attaches the score to one dataset.
          type: string
        name:
          description: |-
            Name is the score name. A rubric of the same name, if the org has declared
            one, decides this score's type and the values it may take.
          type: string
        runName:
          description: RunName attaches the score to one run.
          type: string
        stringValue:
          description: |-
            StringValue is the label of a CATEGORICAL score, which must be one the
            rubric allows.
          type: string
        traceId:
          description: TraceID attaches the score to one model call.
          type: string
        value:
          description: |-
            Value is the numeric score, which must be finite: NaN and Inf are refused.
            A BOOLEAN score takes 0 or 1.
          type: number
      required:
      - name
      type: object
    scoreView:
      properties:
        comment:
          description: Comment is the grader's reasoning, truncated at 2000 characters.
          type: string
        dataType:
          description: DataType is NUMERIC, CATEGORICAL or BOOLEAN.
          type: string
        id:
          description: ID is the score event's handle.
          type: string
        name:
          description: Name is the score name, which a rubric of the same name governs.
          type: string
        runName:
          description: RunName is the run this score was recorded under, when it came
            from one.
          type: string
        stringValue:
          description: StringValue is the label of a CATEGORICAL score.
          type: string
        timestamp:
          description: Timestamp is when the score was recorded.
          type: string
        traceId:
          description: TraceID is the model call this score grades, when it grades
            one.
          type: string
        value:
          description: Value is the numeric score; for BOOLEAN it is 0 or 1.
          type: number
      type: object
    searchIn:
      properties:
        doctypes:
          description: |-
            DocTypes restricts retrieval to a subset of the indexed knowledge doctypes
            (kb-page, kb-memory, kb-source). An empty or foreign list reads all of them.
          items:
            type: string
          type: array
        limit:
          description: Limit bounds the hits returned. Default 10, maximum 50.
          type: integer
        project:
          description: Project narrows retrieval to one project scope.
          type: string
        query:
          description: Query is the natural-language question. Required.
          type: string
      type: object
    searchIndex:
      properties:
        createdAt:
          description: |-
            CreatedAt is the index's creation time (RFC 3339); it falls back to now when
            the index list could not be read.
          type: string
        docCount:
          description: DocCount is how many documents the index currently holds.
          type: integer
        lastIndexedAt:
          description: |-
            LastIndexedAt is the index's last update time (RFC 3339), null when the
            index list could not be read.
          type: string
        name:
          description: Name is the index uid.
          type: string
      type: object
    searchIndexList:
      properties:
        indexes:
          description: |-
            Indexes is one row per Meilisearch index, sorted by name. Empty — never
            absent — when the search service cannot be reached.
          items:
            $ref: '#/components/schemas/searchIndex'
          type: array
      type: object
    searchOut:
      properties:
        degraded:
          description: |-
            Degraded is true when the index was unreachable and this answer is honestly
            empty rather than wrong — a RAG caller continues with no context instead of
            failing the turn. Absent on a normal answer.
          type: boolean
        hits:
          description: Hits are the matching passages, most relevant first.
          items:
            $ref: '#/components/schemas/hit'
          type: array
      type: object
    searchResults:
      properties:
        degraded:
          description: |-
            Degraded is true when retrieval failed and the empty result set is an
            outage rather than a real absence of matches. Absent on a healthy answer.
          type: boolean
        query:
          description: Query echoes the query that was run.
          type: string
        results:
          description: |-
            Results are the matching spans, best first. Never null — an empty search is
            an empty array.
          items:
            $ref: '#/components/schemas/Span'
          type: array
        type:
          description: Type echoes the retrieval tier that ran, after defaulting.
          type: string
      type: object
    searchStats:
      properties:
        searchesPerDay:
          description: SearchesPerDay is always empty, for the same reason as totalSearches.
          items:
            $ref: '#/components/schemas/dayCount'
          type: array
        totalDocuments:
          description: TotalDocuments is the sum of every index's document count.
          type: integer
        totalSearches:
          description: |-
            TotalSearches is always 0: Meilisearch keeps no query-history counter, so
            this surface reports the honest zero rather than an estimate.
          type: integer
        totalSessions:
          description: TotalSessions is always 0, for the same reason as totalSearches.
          type: integer
      type: object
    secretList:
      properties:
        secrets:
          description: |-
            Secrets is one row per distinct (namespace, name) a function mounts. Values
            are NEVER read or returned.
          items:
            $ref: '#/components/schemas/secretView'
          type: array
      type: object
    secretView:
      properties:
        mountedBy:
          description: MountedBy is a function that mounts it.
          type: string
        name:
          description: Name is the environment variable the function mounts.
          type: string
        namespace:
          description: Namespace is the group the mounting function belongs to.
          type: string
      type: object
    seriesLine:
      properties:
        key:
          description: agent name
          type: string
        points:
          items:
            $ref: '#/components/schemas/seriesPoint'
          type: array
      type: object
    seriesPoint:
      properties:
        t:
          description: bucket start, RFC3339 UTC
          type: string
        v:
          description: real invocation count in the bucket
          type: integer
      type: object
    sessionDetail:
      properties:
        account:
          type: string
        actor:
          type: string
        agent:
          type: string
        childSessions:
          items:
            $ref: '#/components/schemas/sessionView'
          type: array
        children:
          type: integer
        createdAt:
          type: string
        cwd:
          type: string
        endedAt:
          type: string
        events:
          type: integer
        host:
          type: string
        id:
          type: string
        lastEvent:
          $ref: '#/components/schemas/lastEventView'
        org:
          type: string
        parentSessionId:
          type: string
        project:
          type: string
        provider:
          type: string
        published:
          type: boolean
        recentEvents:
          items:
            $ref: '#/components/schemas/eventView'
          type: array
        repo:
          type: string
        rootSessionId:
          type: string
        startedAt:
          type: string
        status:
          type: string
        target:
          type: string
        taskRunId:
          type: string
        taskWorkflowId:
          type: string
        terminal:
          type: string
        title:
          type: string
        updatedAt:
          type: string
      type: object
    sessionList:
      properties:
        sessions:
          description: |-
            Sessions is the matching sessions, each with its event and child counts and
            a one-line preview of its latest event.
          items:
            $ref: '#/components/schemas/sessionView'
          type: array
      type: object
    sessionUser:
      properties:
        groups:
          description: |-
            Groups is the caller's group list, always empty here: this console
            authorizes on the platform SuperAdmin fact alone, not on argocd RBAC groups.
            Absent for an anonymous caller.
          items:
            type: string
          type: array
        iss:
          description: |-
            Iss is the token issuer as the SPA expects to see it — the literal "argocd",
            so the UI never triggers an SSO redirect of its own. Absent for an anonymous
            caller.
          type: string
        loggedIn:
          description: LoggedIn reports whether this browser holds a session this
            console accepts.
          type: boolean
        loginUrl:
          description: LoginURL is where an anonymous caller signs in. Absent once
            signed in.
          type: string
        logoutUrl:
          description: LogoutURL is where a signed-in caller ends the session. Absent
            when anonymous.
          type: string
        username:
          description: |-
            Username is the validated principal's user ID — the opaque gateway id, which
            is what argocd's UI renders as the signed-in user here — or "admin" when the
            principal carries none. Absent when anonymous.
          type: string
      type: object
    sessionView:
      properties:
        account:
          type: string
        actor:
          type: string
        agent:
          type: string
        children:
          type: integer
        createdAt:
          type: string
        cwd:
          type: string
        endedAt:
          type: string
        events:
          type: integer
        host:
          description: |-
            Execution context (mission-control): the machine/repo/cwd a card shows and
            the run-target a session is dispatched to. Omitted when a surface didn't report it.
          type: string
        id:
          type: string
        lastEvent:
          $ref: '#/components/schemas/lastEventView'
          description: |-
            LastEvent is the compact latest-activity line for the list projection (nil in
            register/patch/tree responses; set by list + detail). It lets a swipe card show
            a live one-line preview without fetching full detail.
        org:
          description: |-
            Org is the caller's OWN tenant, echoed so a client can build the public
            build URL (/builds/:org/:project) without a second call or a guess. It is
            never another tenant's — every read is org-scoped before it gets here.
          type: string
        parentSessionId:
          type: string
        project:
          description: |-
            The readable build: the product this session built and whether its story
            is public (provenance.go).
          type: string
        provider:
          type: string
        published:
          type: boolean
        repo:
          type: string
        rootSessionId:
          type: string
        startedAt:
          type: string
        status:
          type: string
        target:
          type: string
        taskRunId:
          type: string
        taskWorkflowId:
          type: string
        terminal:
          description: |-
            Terminal is where this session can be WATCHED — the URL the machine
            published for its live terminal. Omitted when it publishes none.
          type: string
        title:
          type: string
        updatedAt:
          type: string
      type: object
    setEnvReq:
      properties:
        app:
          description: App is the application's slug, from the path.
          type: string
        env:
          description: |-
            Env is the app's whole environment set, REPLACING what it had. Keys must
            match `^[A-Za-z_][A-Za-z0-9_]*$`; a variable marked `secret: true` is
            sealed into KMS and blanked in the database.
          items:
            $ref: '#/components/schemas/EnvVarJSON'
          type: array
        project:
          description: Project is the project the application lives under, from the
            path.
          type: string
      type: object
    settingsReq:
      properties:
        config:
          additionalProperties:
            type: object
          description: |-
            Config is the product's non-secret configuration, stored verbatim. Bounded at
            64 KiB once serialized. Omit it to store an empty object.
          type: object
        product:
          description: |-
            Product is the catalog slug, from the PATH. zip binds the path last, so the
            URL names the product being written whatever a body field claims.
          type: string
        secrets:
          additionalProperties:
            type: string
          description: |-
            Secrets are the secret fields, by name. Each VALUE is sealed into KMS and
            never reaches this deployment's database; a value that is empty or equal to
            the mask the read path returns means "unchanged" and is skipped, so a console
            round-trip cannot blank a stored secret. A key must match
            ^[a-z0-9][a-z0-9._-]{0,62}$, a value is bounded at 8 KiB, and an org may hold
            at most 64 secret fields per product.
          type: object
      type: object
    settingsView:
      properties:
        config:
          description: |-
            Config is the product's non-secret configuration, an opaque JSON object the
            server stores and returns verbatim. `{}` when nothing has been saved.
        createdAt:
          description: CreatedAt is when this configuration was first written, RFC
            3339 UTC.
          type: string
        product:
          description: Product is the catalog slug this configuration belongs to.
          type: string
        secretKeys:
          description: |-
            SecretKeys names the secret fields that ARE set. Their VALUES live only in KMS
            and are never returned here — the console renders a mask.
          items:
            type: string
          type: array
        updatedAt:
          description: |-
            UpdatedAt is when this configuration was last written, RFC 3339 UTC. Empty
            when nothing has been saved.
          type: string
      type: object
    skillDeleted:
      properties:
        deleted:
          description: Deleted is the skill id that is now gone.
          type: string
      type: object
    skillIn:
      properties:
        content:
          description: Content is the SKILL.md body. Required, at most 256 KiB.
          type: string
        description:
          description: Description is the one-line summary discovery shows for the
            skill.
          type: string
        name:
          description: |-
            Name is the skill's id within the org: one lowercase path segment
            (a-z0-9, _ or -). Writing an existing name REVISES that skill.
          type: string
      type: object
    skillWritten:
      properties:
        skill:
          $ref: '#/components/schemas/Skill'
          description: Skill is the skill as stored, with its derived id and creation
            time.
      type: object
    sourceFailure:
      properties:
        reason:
          description: Reason is a terse, log-safe summary — never the upstream's
            response body.
          type: string
        source:
          description: Source is the dependency that failed, named as an operator
            names it.
          type: string
      type: object
    sourceToolList:
      properties:
        source:
          description: Source is the source these tools came from.
          type: string
        tools:
          description: Tools is the caller's tools from that source. Never null.
          items:
            $ref: '#/components/schemas/Tool'
          type: array
      type: object
    statsOut:
      properties:
        admin:
          description: Admin is the upstream service's server-panel flag, always false
            here.
          type: boolean
        metrics:
          description: |-
            Metrics is the upstream transactor's metrics block. This server does not
            populate it, so it is always the empty object — the front reads the key,
            not its contents.
          properties: {}
          type: object
        statistics:
          $ref: '#/components/schemas/statsSessions'
          description: Statistics carries the live sessions.
      type: object
    statsSessions:
      properties:
        activeSessions:
          additionalProperties:
            items:
              $ref: '#/components/schemas/statsUser'
            type: array
          description: |-
            ActiveSessions maps a workspace uuid to its connected sessions. It carries
            only the token's OWN workspace, and is empty for a token that names none.
          type: object
      type: object
    statsUser:
      properties:
        userId:
          description: UserID is the account the session is authenticated as.
          type: string
      type: object
    statusBreakdown:
      properties:
        error:
          description: invocations that ran and failed
          type: integer
        success:
          description: invocations whose code ran and wrote nothing to stderr
          type: integer
        timeout:
          description: invocations that hit their configured deadline
          type: integer
      type: object
    subdomainSetIn:
      properties:
        enabled:
          description: |-
            Enabled publishes the script on <script>.<subdomain>.workers.dev when true,
            and withdraws it when false.
          type: boolean
        script:
          description: Script is the Worker script name, from the path.
          type: string
      type: object
    subscribeReq:
      properties:
        channel:
          description: |-
            Channel is the Slack channel the notifier posts to — an id (C…/G…), a
            #name, or a bare name. Required.
          type: string
        events:
          description: |-
            Events narrows delivery to these lifecycle kinds (push.landed,
            deploy.live, deploy.failed). Omit it to receive every deliverable kind; a
            kind that is never posted to Slack is refused rather than silently dropped.
          items:
            type: string
          type: array
        name:
          description: Name is the repo to subscribe, from the :name path segment.
          type: string
      type: object
    subscriptionList:
      properties:
        data:
          description: Data holds the repo's subscriptions.
          items:
            $ref: '#/components/schemas/subscriptionView'
          type: array
      type: object
    subscriptionView:
      properties:
        channel:
          description: Channel is the Slack channel id or name the notifier posts
            to.
          type: string
        createdAt:
          description: CreatedAt is RFC 3339 UTC.
          type: string
        events:
          description: Events is the kind filter; absent means every deliverable kind.
          items:
            type: string
          type: array
        id:
          description: ID is the subscription's identifier ("sub_…"), the handle to
            delete it by.
          type: string
        repo:
          description: Repo is the repo whose lifecycle events are delivered.
          type: string
      type: object
    summary:
      properties:
        calls:
          type: integer
        messages:
          type: integer
        numbers:
          type: integer
      type: object
    syncList:
      properties:
        data:
          description: Data is the org's syncs, each with its endpoints, policy and
            last-synced time.
          items:
            $ref: '#/components/schemas/syncView'
          type: array
      type: object
    syncQueued:
      properties:
        id:
          description: ID is the sync the reconcile was queued for.
          type: string
        queued:
          description: Queued is true when the reconcile was accepted; it has not
            run yet.
          type: boolean
      type: object
    syncReq:
      properties:
        actor:
          description: |-
            Actor is the identity the sync writes as, used as the loop guard so its own
            writes do not re-trigger it. Defaults to the deployment's GIT_SYNC_ACTOR.
          type: string
        direction:
          description: Direction is both (the default), pull, push or off.
          type: string
        kind:
          description: Kind is what is being synced. Only "git" today, which is also
            the default.
          type: string
        run:
          description: Run reconciles once immediately after the upsert, in the background.
          type: boolean
        source:
          $ref: '#/components/schemas/endpointReq'
          description: Source is the upstream end. Required.
        target:
          $ref: '#/components/schemas/endpointReq'
          description: |-
            Target is the downstream end. Optional for git: a native repository named
            after the source is derived when it is omitted.
        trigger:
          description: 'Trigger is what starts a reconcile: webhook (the default),
            poll or manual.'
          type: string
      type: object
    syncView:
      properties:
        actor:
          type: string
        createdAt:
          type: string
        direction:
          type: string
        id:
          type: string
        kind:
          type: string
        source:
          $ref: '#/components/schemas/endpointView'
        target:
          $ref: '#/components/schemas/endpointView'
        trigger:
          type: string
        updatedAt:
          description: bumped on every reconcile — the last-synced time
          type: string
      type: object
    tagConfig:
      properties:
        tags:
          items:
            $ref: '#/components/schemas/browserTagOut'
          type: array
      type: object
    targetDeleted:
      properties:
        deleted:
          description: Deleted is true when the target was removed.
          type: boolean
        id:
          description: ID is the target that was removed.
          type: string
      type: object
    targetList:
      properties:
        targets:
          description: Targets is every target registered to the caller's org.
          items:
            $ref: '#/components/schemas/targetView'
          type: array
      type: object
    targetReq:
      properties:
        capacity:
          type: string
        host:
          type: string
        kind:
          type: string
        label:
          type: string
        metrics:
          $ref: '#/components/schemas/Metrics'
        spec:
          $ref: '#/components/schemas/Spec'
        status:
          type: string
      type: object
    targetView:
      properties:
        capacity:
          type: string
        createdAt:
          type: string
        host:
          type: string
        id:
          type: string
        kind:
          type: string
        label:
          type: string
        metrics:
          $ref: '#/components/schemas/Metrics'
        metricsAt:
          type: string
        running:
          type: integer
        sessions:
          type: integer
        spec:
          $ref: '#/components/schemas/Spec'
        status:
          type: string
        updatedAt:
          type: string
      type: object
    testResult:
      properties:
        delivered:
          type: boolean
        durationMs:
          type: integer
        error:
          type: string
        httpStatus:
          type: integer
      type: object
    todoProject:
      properties:
        createdAt:
          type: integer
        description:
          type: string
        id:
          type: string
        key:
          type: string
        name:
          type: string
        org:
          type: string
        updatedAt:
          type: integer
      type: object
    toolCall:
      properties:
        arguments:
          additionalProperties:
            type: object
          description: |-
            Arguments is the tool's own input object, passed through verbatim to
            whichever source owns it.
          type: object
        name:
          description: Name is the tool to run, exactly as GET /v1/tools reports it.
          type: string
      type: object
    toolList:
      properties:
        tools:
          description: |-
            Tools is every tool the caller may see, deduplicated by name with source
            precedence applied.
          items:
            $ref: '#/components/schemas/Tool'
          type: array
      type: object
    toolResult:
      properties:
        name:
          description: Name is the tool that ran.
          type: string
        result:
          description: |-
            Result is the tool's own output, verbatim — its shape is the tool's, not
            this plane's.
          type: object
      type: object
    traceList:
      properties:
        data:
          description: Data is the caller org's traces matching the filters, bounded
            by limit.
          items:
            $ref: '#/components/schemas/traceView'
          type: array
      type: object
    traceView:
      properties:
        apiKeyHash:
          description: |-
            APIKeyHash is the non-reversible credential ref (never a plaintext key), so
            a trace correlates to the key that drove it without the store holding a secret.
          type: string
        datasetItemId:
          description: ItemID is the example the call answered.
          type: string
        datasetName:
          description: Dataset is the set the graded example came from.
          type: string
        endTime:
          description: EndTime is when it returned.
          type: string
        id:
          description: ID is the trace's handle, the value a score points at.
          type: string
        input:
          description: Input is what the model was given.
          type: object
        latencyMs:
          description: |-
            LatencyMs is EndTime-StartTime in milliseconds, nil when the trace carries
            no timing (so the console renders "—", never a fabricated 0).
          type: number
        model:
          description: Model is the model that answered.
          type: string
        name:
          description: Name is the trace's label, "eval:<run>" for a call a run made.
          type: string
        output:
          description: Output is what it answered.
          type: string
        projectId:
          description: ProjectID is the sub-scope within the org the call was made
            under.
          type: string
        runName:
          description: RunName is the run the call belongs to.
          type: string
        sessionId:
          description: SessionID groups the calls of one run.
          type: string
        startTime:
          description: StartTime is when the call began.
          type: string
        timestamp:
          description: Timestamp is the trace's own clock, equal to StartTime for
            a timed call.
          type: string
      type: object
    trailPage:
      properties:
        data:
          description: Data is one page of the org's events, newest first. Empty,
            never null.
          items:
            $ref: '#/components/schemas/Wire'
          type: array
        msg:
          description: Msg is the envelope's message slot, empty on success.
          type: string
        status:
          description: Status is the envelope's status slot, "ok" on success.
          type: string
        total:
          description: |-
            Total is how many events match the filter, across all pages — what a pager
            needs to size itself.
          type: integer
      type: object
    transferReq:
      properties:
        authCode:
          description: |-
            AuthCode is the transfer authorization the losing registrar issued. It is
            required.
          type: string
        domain:
          description: Domain is the name to move in. It is required.
          type: string
        years:
          description: Years is the term to buy on transfer, defaulting to 1.
          type: integer
      required:
      - domain
      - authCode
      type: object
    treeEntryJSON:
      properties:
        mode:
          description: Mode is the octal git file mode ("100644", "040000", "120000").
          type: string
        name:
          description: Name is the entry's own name, no directory part.
          type: string
        path:
          description: Path is the entry's full repo-relative path.
          type: string
        size:
          description: Size is the file's byte length; 0 for a directory.
          type: integer
        type:
          description: Type is "tree" for a directory, "blob" for a file.
          type: string
      type: object
    treeJSON:
      properties:
        entries:
          description: Entries are the immediate children, directories before files.
          items:
            $ref: '#/components/schemas/treeEntryJSON'
          type: array
      type: object
    treeNode:
      properties:
        children:
          items:
            $ref: '#/components/schemas/treeNode'
          type: array
        session:
          $ref: '#/components/schemas/sessionView'
      type: object
    triggerList:
      properties:
        triggers:
          description: Triggers is one row per function, describing how it is reached.
          items:
            $ref: '#/components/schemas/triggerView'
          type: array
      type: object
    triggerView:
      properties:
        enabled:
          description: whether it currently fires
          type: boolean
        functionName:
          description: the function it calls
          type: string
        id:
          description: the trigger's handle
          type: string
        name:
          description: a human label for it
          type: string
        target:
          description: the path it calls
          type: string
        type:
          description: what kind of trigger it is; HTTP is the only one today
          type: string
      type: object
    updateAgentIn:
      properties:
        computeRef:
          type: string
        description:
          type: string
        executionMode:
          type: string
        instructions:
          type: string
        model:
          type: string
        ref:
          description: Ref is the agent to update — its public id or org-unique name,
            from the path.
          type: string
        schedule:
          type: string
        serviceAccountId:
          type: string
        tools:
          items:
            type: string
          type: array
      type: object
    updateEndpointIn:
      properties:
        description:
          description: Description is a free-text label for the console. Optional,
            clipped to 1024 bytes.
          type: string
        events:
          description: |-
            Events are NATS subject patterns to subscribe to. An empty or omitted list
            means EVERY event. Max 64 patterns, each max 256 bytes.
          items:
            type: string
          type: array
        status:
          description: Status is "active" or "disabled". Empty defaults to active.
          type: string
        url:
          description: |-
            URL is the https:// address each matching event is POSTed to. Required,
            max 2048 bytes; http:// and every other scheme is refused.
          type: string
      type: object
    usage:
      properties:
        costCents:
          description: null — no per-invocation cost source
          type: integer
        series:
          description: one line per function that ran in the window
          items:
            $ref: '#/components/schemas/costLine'
          type: array
        status:
          $ref: '#/components/schemas/statusBreakdown'
          description: how those invocations ended
      type: object
    usageAnalyticsAccess:
      properties:
        access:
          $ref: '#/components/schemas/usageAnalyticsGrant'
          description: Access is what that plan grants.
        plan:
          description: Plan echoes the plan id that was resolved, exactly as it was
            asked for.
          type: string
      type: object
    usageAnalyticsGrant:
      properties:
        datastore:
          description: |-
            Datastore is whether the plan may read GET /v1/usage/analytics at all. The
            free floor is false, and that is what a catalog outage resolves to.
          type: boolean
        export:
          description: Export is whether the plan may export the analytics it can
            read.
          type: boolean
        retentionDays:
          description: |-
            RetentionDays is how far back the plan may read. GET /v1/usage/analytics
            clamps a custom window's start to this, so an older `start` returns the
            clamped window rather than an error.
          type: integer
      type: object
    usageAnalyticsView:
      properties:
        end:
          description: End is the window's exclusive end, RFC3339 UTC.
          type: string
        export:
          description: Export is whether the resolved plan allows exporting these
            rows.
          type: boolean
        plan:
          description: Plan echoes the plan id the entitlement was resolved from.
          type: string
        providers:
          $ref: '#/components/schemas/ProviderBreakdown'
          description: Providers is the per-provider roll-up over the window.
        range:
          description: |-
            Range is the label that was ASKED for. A plan whose retention is shorter
            than that window is served the retention instead, so read start and end for
            the window the rows actually cover and retentionDays for the reason — on a
            clamped read the label is longer than what was served.
          type: string
        retentionDays:
          description: RetentionDays is how far back the resolved plan allows reading.
          type: integer
        scope:
          $ref: '#/components/schemas/usageScope'
          description: Scope is the tenant the rows were read under — the validated
            principal's org.
        start:
          description: |-
            Start is the window's inclusive start, RFC3339 UTC, AFTER the retention
            clamp — so it may be later than the start that was asked for.
          type: string
      type: object
    usageRepo:
      properties:
        name:
          description: Name is the repo's org-unique handle.
          type: string
        project:
          description: Project is the sub-scope the repo lives in; absent for the
            default scope.
          type: string
        sizeBytes:
          description: SizeBytes is the repo's on-disk size at its last measurement.
          type: integer
      type: object
    usageScope:
      properties:
        org:
          description: |-
            Org is the IAM org slug the rows were read under — the validated
            principal's, never a client header.
          type: string
        user:
          description: |-
            User is the caller's own subject, whose linked-account rows the accounts
            block carries. Absent on a read that is org-scoped only.
          type: string
      type: object
    usageSummary:
      properties:
        accounts:
          $ref: '#/components/schemas/Accounts'
          description: |-
            Accounts is the caller's own linked provider accounts beside the org's
            Hanzo-routed usage, labelled row by row and never summed together.
        end:
          description: End is the window's exclusive end, RFC3339 UTC.
          type: string
        interval:
          description: Interval is the bucket width the spend series is gap-filled
            at.
          type: string
        llm:
          $ref: '#/components/schemas/LLM'
          description: LLM is the org's Hanzo-routed inference totals from the warehouse.
        range:
          description: Range is the window label that was served.
          type: string
        scope:
          $ref: '#/components/schemas/usageScope'
          description: Scope is the tenant and subject the roll-up was answered for.
        sources:
          $ref: '#/components/schemas/Sources'
          description: |-
            Sources says which upstreams actually answered, so a zero can be read as
            "no data yet" rather than as a measurement.
        spend:
          $ref: '#/components/schemas/Spend'
          description: Spend is the categorized cost roll-up from the billing ledger.
        start:
          description: Start is the window's inclusive start, RFC3339 UTC.
          type: string
      type: object
    usageView:
      properties:
        org:
          description: Org the rollup is for.
          type: string
        repos:
          description: Repos is every repo the org owns, across every project sub-scope.
          items:
            $ref: '#/components/schemas/usageRepo'
          type: array
        totalBytes:
          description: TotalBytes is the sum over Repos — the org's whole git footprint.
          type: integer
      type: object
    usageWindowView:
      properties:
        account:
          description: Account is the linked provider account the window belongs to.
          type: string
        cachedInputTokens:
          description: |-
            CachedInputTokens is the prompt tokens served from the provider's cache;
            omitted when unknown.
          type: integer
        confidence:
          description: |-
            Confidence says how much the counters beside it mean — a meter that
            reported only a percentage leaves them at zero, and this is how a reader
            tells that from a true zero.
          type: string
        costCents:
          description: |-
            CostCents is what the window cost on the PROVIDER's own plan, in US cents.
            It is not a Hanzo charge.
          type: integer
        costLimitCents:
          description: CostLimitCents is the plan's spend ceiling for the window,
            in US cents.
          type: integer
        currency:
          description: Currency is the provider's currency when it is not US cents.
          type: string
        inputTokens:
          description: InputTokens is prompt tokens consumed in the window; omitted
            when unknown.
          type: integer
        lane:
          description: |-
            Lane is the meter lane this instance belongs to, e.g. a provider's own
            rolling-window meter.
          type: string
        machine:
          description: Machine is the host whose meter reported the window.
          type: string
        outputTokens:
          description: |-
            OutputTokens is completion tokens produced in the window; omitted when
            unknown.
          type: integer
        plan:
          description: Plan is the subscription plan the account is on, as the provider
            names it.
          type: string
        requests:
          description: |-
            Requests is how many requests were made in the window; omitted when the
            meter did not report it.
          type: integer
        resetsAt:
          description: ResetsAt is when this window rolls over, RFC3339 UTC; omitted
            when unknown.
          type: string
        synthetic:
          description: Synthetic marks an instance the meter inferred rather than
            read.
          type: boolean
        totalTokens:
          description: TotalTokens is the window's total tokens; omitted when unknown.
          type: integer
        usedPct:
          description: UsedPct is how much of the window's allowance is consumed,
            0–100.
          type: number
        window:
          description: 'Window is the window class: 6h, day, week or month.'
          type: string
        windowMinutes:
          description: |-
            WindowMinutes is the window's real length in minutes when the meter
            reported one; omitted when it did not.
          type: integer
        windowStart:
          description: WindowStart is when this window opened, RFC3339 UTC; omitted
            when unknown.
          type: string
      type: object
    userEnablementItem:
      properties:
        canOptIn:
          description: beta && not yet opted in
          type: boolean
        effective:
          description: visible to the caller's org
          type: boolean
        id:
          type: string
        kind:
          type: string
        optedIn:
          description: caller's org on the beta list
          type: boolean
        state:
          description: off|beta|ga
          type: string
      type: object
    userOptinReq:
      properties:
        handle:
          description: |-
            Handle is the display name shown on a listed row: 1-40 characters of letters,
            digits, space, dot, underscore, apostrophe or hyphen. Left empty on a listing
            opt-in it defaults to the caller's username.
          type: string
        listed:
          description: |-
            Listed publishes the caller's row to other viewers of the board when true, and
            anonymizes it when false.
          type: boolean
      type: object
    userOptinView:
      properties:
        canSet:
          description: |-
            CanSet is false when the caller's ledger identity cannot be resolved (no user
            name on the principal). Writing the preference would fail, so hide the control.
          type: boolean
        handle:
          description: |-
            Handle is the display name on the caller's listed row. Empty when they never
            chose one; opting in without a handle sets it to their username, so a listed row
            is never blank.
          type: string
        listed:
          description: |-
            Listed is true when the caller's board row is published under Handle to other
            viewers. False — the default for anyone who never opted in — anonymizes the row;
            the metric still counts, only the name is withheld.
          type: boolean
      type: object
    verdict:
      properties:
        builds:
          type: integer
        commit:
          type: string
        fired:
          type: boolean
        org:
          type: string
        reason:
          type: string
        ref:
          type: string
        repo:
          type: string
      type: object
    verifyOut:
      properties:
        account:
          description: Account is the account label the provider reported. Present
            only when active.
          type: string
        active:
          description: Active is whether the stored credential verified live against
            the provider.
          type: boolean
        externalId:
          description: ExternalID is the provider's account id. Present only when
            active.
          type: string
        provider:
          description: Provider is the connector's registry id.
          type: string
        reason:
          description: Reason is why the check failed. Present only when active is
            false.
          type: string
        scopes:
          description: Scopes are the permissions the credential carries. Present
            only when active.
          items:
            type: string
          type: array
      type: object
    versionMessage:
      properties:
        BuildDate:
          description: |-
            BuildDate is the time THIS RESPONSE was generated, in RFC 3339 — not a build
            timestamp. There is no argocd build here to report one for.
          type: string
        Compiler:
          description: |-
            Compiler is the constant "gc" the SPA expects; it is not read from this
            process.
          type: string
        GoVersion:
          description: GoVersion is always empty.
          type: string
        Platform:
          description: |-
            Platform is the constant "linux/amd64" the SPA expects; it is not this
            process's own GOOS/GOARCH.
          type: string
        Version:
          description: Version names the projection, "hanzo-cd (projection)".
          type: string
      type: object
    versionPage:
      properties:
        data:
          description: Data is the page of versions, newest first.
          items:
            $ref: '#/components/schemas/FlowVersion'
          type: array
      type: object
    versionView:
      properties:
        createdAt:
          description: CreatedAt is when this revision was appended, RFC 3339 UTC.
          type: string
        type:
          description: |-
            Type is the kind this revision was written with, which may differ from the
            current one.
          type: string
        version:
          description: |-
            Version is this revision's number, 1 for the first. Numbers are dense and
            never reused: deleting the prompt drops the whole history with it.
          type: integer
      type: object
    waitlistModeView:
      properties:
        host:
          description: Host is the queried host, normalized (lowercased, port stripped).
          type: string
        known:
          description: |-
            Known is false when no registered service claims this host, or when the
            registry is unavailable — the guard then lets the request through, which is
            why the two cases answer alike.
          type: boolean
        service:
          description: Service is the registered service that governs this host, empty
            when none does.
          type: string
        waitlistMode:
          description: |-
            WaitlistMode is true when the service is GATED to approved users, false when
            it is open. Always false for an ungoverned host.
          type: boolean
      type: object
    webEngine:
      properties:
        name:
          description: Name is the engine, matching the `engine` stamped on each result.
          type: string
        outcome:
          description: |-
            Outcome is "answered", "blind" or "failed" — see outcome.go. "blind" means
            the page came back and no results could be read out of it.
          type: string
        results:
          description: |-
            Results is how many hits this engine contributed, before the merge
            deduplicated them against the others.
          type: integer
      type: object
    webQuestion:
      properties:
        language:
          description: |-
            Language narrows the search to a locale, BCP-47-ish ("en", "ja"). Empty
            means no narrowing.
          type: string
        max_sources:
          description: MaxSources caps how many pages are read. Empty means the mode's
            own budget.
          type: integer
        mode:
          description: |-
            Mode is how much work to do: `search` (fast, one pass), `news` (recency
            biased), `research` (a plan and several rounds) or `deep` (the widest
            survey). Empty means research.
          type: string
        q:
          description: Q is the question, in plain language. Required.
          type: string
        sources:
          description: |-
            Sources narrows where the evidence comes from: any of `web`, `news`,
            `academic`, `github`, `reddit`, `x`. Each becomes a site-scoped search, so
            `["x"]` researches X/Twitter posts rather than the open web.
          items:
            type: string
          type: array
      type: object
    webResult:
      properties:
        content:
          description: |-
            Content is the ENGINE's snippet — the few lines shown under the title, not
            the page's text. Read the page itself with POST /v1/crawl.
          type: string
        engine:
          description: |-
            Engine names the backend that found this hit, so one engine's view of a
            query can be told from another's.
          type: string
        title:
          description: Title is the page's title.
          type: string
        url:
          description: URL is the page's address, as the engine reported it.
          type: string
      type: object
    webSearchQuery:
      properties:
        language:
          description: |-
            Language narrows the engines to a locale, BCP-47-ish ("en", "ja", "de").
            Empty means no narrowing.
          type: string
        q:
          description: |-
            Q is the query. Required — an empty one is refused rather than answered
            with the whole web.
          type: string
      type: object
    webSearchResults:
      properties:
        engines:
          description: |-
            Engines is one entry per engine asked, in the order they were asked. It is
            ADDITIVE to the SearXNG contract, which the LibreChat client ignores as an
            unknown field exactly as it ignores `engine` on a result.
          items:
            $ref: '#/components/schemas/webEngine'
          type: array
        number_of_results:
          description: |-
            NumberOfResults is len(results) — what this answer carries, never an
            estimate of what the web holds.
          type: integer
        query:
          description: Query is the query that ran, echoed back.
          type: string
        results:
          description: |-
            Results are the merged hits, deduplicated by normalised URL and capped at
            30. Always an array and never null: no hits is an ANSWER, not a fault.
          items:
            $ref: '#/components/schemas/webResult'
          type: array
      type: object
    workerList:
      properties:
        workers:
          description: |-
            Workers is one row per connected BYO machine, each carrying the host's own
            report (GPUs, driver versions, capabilities) rather than a normalized view.
          items:
            $ref: '#/components/schemas/byoWorker'
          type: array
      type: object
    zapProcReq:
      properties:
        description:
          type: string
        name:
          type: string
        project:
          type: string
      type: object
  securitySchemes:
    bearer:
      description: 'The caller''s credential, sent as `Authorization: Bearer <token>`.
        It is either an access token minted by Hanzo IAM or an API key (pk-/sk-).
        X-Authorization, and HTTP Basic carrying the token as the password, are accepted
        spellings of the same header for clients that cannot set Authorization; a
        browser presents the session cookie instead. Every operation requires it unless
        it says `security: []`.'
      scheme: bearer
      type: http
info:
  description: 'The Hanzo Cloud API as a customer calls it: every operation under
    /v1/ except the operator''s admin product, relay doors and legacy spellings. The
    full document at /v1/openapi.json carries those too. Tagged by product: the first
    path segment after /v1/.'
  title: Hanzo Cloud API
  version: v1
openapi: 3.1.0
paths:
  /v1/account/appearance:
    get:
      description: |-
        Returns the signed-in caller's own appearance preference — text
        size, density and accent — read from their IAM account so it is the same on
        every device and every Hanzo surface. An unset preference is an empty object.

        A transient IAM read failure reports the empty preference rather than a 5xx, so
        a surface applies its published default and never error-toasts on load — the
        same fail-soft the key read uses.
      operationId: get_account_appearance
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appearance'
          description: ok
      summary: Returns the signed-in caller's own appearance preference — text size,
        density and accent — read from their IAM account so it is the same on every
        device and every Hanzo surface.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
    post:
      description: |-
        Stores the caller's appearance preference on their IAM account,
        preserving every other field of the row. The accent is validated as a real
        colour token before it is stored; an unset or invalid axis is dropped rather
        than stored.
      operationId: post_account_appearance
      requestBody:
        content:
          application/json:
            example:
              accent: '#8b5cf6'
              density: compact
              type: 1.15
            schema:
              $ref: '#/components/schemas/appearance'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appearance'
          description: ok
      summary: Stores the caller's appearance preference on their IAM account, preserving
        every other field of the row.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
  /v1/account/avatar:
    post:
      description: |-
        Stores one image as the signed-in user's profile photo and answers the URL it is served from, which is also written to the user's IAM record — so every surface that already renders `avatar` picks it up with no further call.

        The body is a multipart form with a `file` part. The format is decided by the BYTES, never the filename or the part's Content-Type: png, jpeg, gif and webp are accepted and everything else is refused with 415, so an SVG cannot be stored as a picture and later served as a program. Over 8 MiB is 413; empty is 400.

        The photo is addressed by the sha256 of its bytes, so setting a new one yields a new URL rather than a stale cache of the old face. The caller is taken from the validated identity ONLY — there is no way to name a different subject — so this always sets your own photo, and a caller with no organization yet is refused.
      operationId: post_account_avatar
      summary: Set your profile photo
      tags:
      - account
      x-app: account
      x-public: true
  /v1/account/avatar/{org}/{user}/{digest}:
    get:
      description: |-
        Streams a profile photo's raw BYTES. This is the address stored on the user's IAM record and rendered directly by an `<img>`, so it takes no credentials — the 64-hex content digest in the path is the capability, and it can only be produced by someone who already has the image.

        The Content-Type is derived from the stored bytes and the response carries nosniff, so only a real raster image is ever served and only under its true type. Anything else — a miss, a malformed path, an object that is not an image — is one 404, and a hit caches for a year because the address is the content.
      operationId: get_account_avatar_by_org_by_user_by_digest
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: user
        required: true
        schema:
          type: string
      - in: path
        name: digest
        required: true
        schema:
          type: string
      summary: Fetch a profile photo
      tags:
      - account
      x-app: account
      x-public: true
  /v1/account/csrf:
    get:
      description: |-
        IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token on
        every money write (mint/revoke a key, top up, onboard, and the billing/commerce
        write verbs). The token is bound to the caller's validated identity and expires,
        so one minted for one identity cannot authorize a write as another.

        It is answered no-store, so it is never cached by a shared proxy. This is the
        same-origin endpoint the embedded console reads — the Same-Origin Policy is what
        stops a cross-site page from reading the response and forging a write.
      operationId: get_account_csrf
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/csrfResp'
          description: ok
      summary: IssueCSRFToken mints the anti-CSRF token a browser echoes as X-CSRF-Token
        on every money write (mint/revoke a key, top up, onboard, and the billing/commerce
        write verbs).
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
  /v1/account/embed:
    get:
      description: |-
        Reports whether one of this brand's shared embedded apps (cms, erp,
        help) may be framed by the caller and is actually running, so a console module
        can choose between the embed and the provision panel.

        It answers two questions the browser cannot answer for itself. ENTITLEMENT is
        server-authoritative: each app is a single shared per-BRAND instance, so only a
        member of the owning brand org — or a SuperAdmin — is given the embed URL; every
        other caller gets phase "not-entitled" and no URL. REACHABILITY is a probe of
        that origin, which a cross-origin page cannot read for itself.

        The probed host is always <app>.<this deployment's own brand domain>: no part of
        it comes from the request, so this can never be steered into probing an
        arbitrary origin.
      operationId: get_account_embed
      parameters:
      - description: 'App is the embedded app to report on: cms (Content Studio),
          erp or help.'
        example: cms
        in: query
        name: app
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/embedStatusResp'
          description: ok
      summary: Reports whether one of this brand's shared embedded apps (cms, erp,
        help) may be framed by the caller and is actually running, so a console module
        can choose between the embed and the provision panel.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
  /v1/account/keys:
    delete:
      description: |-
        Revokes the caller's own API key of the requested class. The class is
        the same field mint takes — `?type=publishable`, defaulting to secret — so
        revoking the key that ships in a browser bundle does not sign its holder out of
        their own API: the other key keeps working.

        Revoking is how a key is replaced when it does not need replacing; minting the
        same class again rotates it in one step. IAM drops the credential immediately,
        but the gateway caches keys for a few minutes, so a request that beat the cache
        expiry may still be served.

        For callers written against the older shape, the class is also accepted in a JSON
        request body, read only when `?type=` is absent.
      operationId: delete_account_keys
      parameters:
      - description: |-
          Type is the key class to act on: "secret" (sk-, session-equivalent, belongs
          on a server) or "publishable" (pk-, org-identifying, safe in a browser
          bundle). Omitted means secret, which is what every existing caller means.
        example: publishable
        in: query
        name: type
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/revokedKey'
          description: ok
      summary: Revokes the caller's own API key of the requested class.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns the caller's own API keys — every type they hold, read
        AUTHORITATIVELY from IAM rather than from the session claim, which lags a key
        minted moments ago. No secret material comes back: a secret key is represented
        by its prefix, and only a publishable key (public by construction) carries its
        full value.

        A transient IAM read failure reports an empty set rather than a 5xx, so the
        page shows the honest empty state and never a fabricated key.
      operationId: get_account_keys
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/apiKeyList'
          description: ok
      summary: Returns the caller's own API keys — every type they hold, read AUTHORITATIVELY
        from IAM rather than from the session claim, which lags a key minted moments
        ago.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates — or rotates — the caller's API key of the requested type and
        returns it ONCE. A real IAM failure surfaces as 502, never a fabricated key.

        Rotating is what creating means here: a user holds one key per type, so the
        endpoint is idempotent by (caller, type) and the superseded credential stops
        working. Two live secrets for one user would make "revoke my key" a lie.
      operationId: post_account_keys
      requestBody:
        content:
          application/json:
            example:
              type: publishable
            schema:
              $ref: '#/components/schemas/keyTypeIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mintedKey'
          description: ok
      summary: Creates — or rotates — the caller's API key of the requested type and
        returns it ONCE.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
  /v1/account/orgs:
    post:
      description: |-
        Onboard creates the caller's organization. Two flows, keyed on whether the caller
        already has a home org (mirrors app/onboard/route.ts):

          - FIRST-RUN (no home org): create + MOVE the user in as admin, so their next
            JWT carries the new owner and the cloud scopes everything to it. This is the
            path a fresh OAuth sign-up takes, from the sign-up application's org.
          - ADDITIONAL (owner set): create the org but do NOT move the user — a move
            changes their IAM owner (stripping a SuperAdmin's status + orphaning their
            current org). They reach the new org via the OrgSwitcher, which re-scopes
            X-Org-Id without touching IAM membership. A personal-org request from someone
            who already has an org is meaningless → 409.
      operationId: post_account_orgs
      requestBody:
        content:
          application/json:
            example:
              name: Acme
            schema:
              $ref: '#/components/schemas/onboardReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/onboardResp'
          description: ok
      summary: Onboard creates the caller's organization.
      tags:
      - account
      x-app: account
      x-public: true
      x-tool: true
  /v1/admission/waitlist:
    get:
      description: |-
        Reports whether ONE host is currently gated by the launch waitlist.
        It resolves the host to the service that governs it and reads that service's
        waitlist switch, so a guard sitting in front of a hosted surface can decide in one
        call whether to show the waitlist or the product. It answers for the ONE host
        asked about and never enumerates the registry, which is why it needs no
        credential. It FAILS OPEN: an unregistered host, an unmounted registry and a store
        fault all answer known=false with mode=false, so a request is never gated pre-boot
        or on a registry fault.
      operationId: get_admission_waitlist
      parameters:
      - description: |-
          Host is the host to resolve, e.g. "chat.hanzo.ai". Defaults to the request's
          own Host header when omitted, which is what lets a guard running on the
          governed host ask about itself with no argument.
        in: query
        name: host
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/waitlistModeView'
          description: ok
      summary: Reports whether ONE host is currently gated by the launch waitlist.
      tags:
      - admission
      x-app: admission
      x-public: true
      x-tool: true
  /v1/agents:
    get:
      description: |-
        Returns every agent defined in the caller's org, each with the
        number of runs recorded against it.
      operationId: get_agents
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/agentList'
          description: ok
      summary: Returns every agent defined in the caller's org, each with the number
        of runs recorded against it.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    post:
      description: |-
        Defines an agent in the caller's org: a model, a system prompt
        (instructions) and a set of tool names. The name must be unique in the org and
        match ^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$. An omitted model takes the
        deployment's configured default; a named one is checked against the gateway's
        served catalog, so a model this deployment never serves is refused here rather
        than failing at run time. A long-running agent must carry a 5-field cron
        schedule (the scheduler would otherwise never fire it) and counts against a
        per-org cap on scheduled agents.
      operationId: post_agents
      requestBody:
        content:
          application/json:
            example:
              instructions: be terse
              model: enso-flash
              name: helper
            schema:
              $ref: '#/components/schemas/createAgentIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/agentView'
          description: created
      summary: 'Defines an agent in the caller''s org: a model, a system prompt (instructions)
        and a set of tool names.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/{ref}:
    delete:
      description: Removes an agent and every run recorded against it. Answers 204.
      operationId: delete_agents_by_ref
      parameters:
      - description: |-
          Ref is the agent's public id (the agent_… handle create and list return) or
          its org-unique name, from the path. Either resolves the same agent.
        example: helper
        in: path
        name: ref
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes an agent and every run recorded against it.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one agent with its system prompt and its 20 most recent runs.
        The ref is the agent's public id or its org-unique name — a created agent is
        immediately gettable by whatever create handed back.
      operationId: get_agents_by_ref
      parameters:
      - description: |-
          Ref is the agent's public id (the agent_… handle create and list return) or
          its org-unique name, from the path. Either resolves the same agent.
        example: helper
        in: path
        name: ref
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/agentDetail'
          description: ok
      summary: Returns one agent with its system prompt and its 20 most recent runs.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes an agent in place. Every field is optional; a field the
        request omits keeps its stored value. The resulting mode+schedule are
        re-validated together, so a partial update can never leave a long-running
        agent without the cron the scheduler needs to fire it, and a transition INTO
        long-running counts against the per-org cap on scheduled agents.
      operationId: patch_agents_by_ref
      parameters:
      - description: Ref is the agent to update — its public id or org-unique name,
          from the path.
        example: helper
        in: path
        name: ref
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              instructions: be terse and cite sources
              ref: helper
            schema:
              $ref: '#/components/schemas/updateAgentIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/agentView'
          description: ok
      summary: Changes an agent in place.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/{ref}/run:
    post:
      description: |-
        Composes the agent's stored instructions with the caller's `input`, executes one real chat completion through the same in-process AI client the rest of the console uses, and answers with the run that was recorded: its id, status, model, output, duration and error. Every run this returns reflects an execution that actually happened — a model failure is recorded and reported, never hidden and never fabricated. A transient upstream failure (429, 5xx, empty choices) is retried up to three times with jittered backoff, and a configured failover model is tried before the run is called an error.

        `ref` is the agent's public `agent_…` id or its org-unique name; either resolves the same agent, and it must belong to the caller's org, so an agent in another tenant is a 404 exactly like one that does not exist. A validated principal is required and the check is made twice on purpose: this route MOVES MONEY, so the debit's principal requirement is asserted where the money moves rather than inherited from the tenant lookup.

        The org's balance is authorized BEFORE any inference, so an unfunded tenant gets 402 and no free compute, and a billing plane that cannot answer gets 503 rather than a free run. The flat per-run fee is an operator knob; setting it to zero makes runs free and removes the balance gate with them. Only a SUCCESSFUL run is billed, attributed to the model actually used — a failover run bills the model it fell over to, not the one it started on. A deployment with no inference wired answers 503 before any of this.

        THE RULE A READER GETS WRONG: a failed run is a 502 whose body is the RUN, not an error envelope. The execution happened, the run was persisted to this agent's history, and its `error` field is the product — so a client that treats every non-2xx as an opaque failure throws away the only account of what went wrong. Each run also opens a root session in the live session registry, best-effort: a bookkeeping failure there never fails the run, because the run and its billing already happened.
      operationId: post_agents_by_ref_run
      parameters:
      - in: path
        name: ref
        required: true
        schema:
          type: string
      summary: Run one of your org's agents and get the recorded run back.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/{ref}/runs:
    get:
      description: |-
        Returns one agent's execution history, newest first — each run's
        input, its output or its error, and how long it took. Every row is a run that
        actually happened.
      operationId: get_agents_by_ref_runs
      parameters:
      - description: Ref is the agent's public id or its org-unique name, from the
          path.
        example: helper
        in: path
        name: ref
        required: true
        schema:
          type: string
      - description: |-
          Limit caps how many runs come back, newest first. Absent, zero or out of
          range (1..200) reads as 50.
        example: 20
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runList'
          description: ok
      summary: Returns one agent's execution history, newest first — each run's input,
        its output or its error, and how long it took.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/activity:
    get:
      description: |-
        Serves the org-wide recent-activity feed. Events are REAL: each
        recorded run is an invoked (ok) or failed (error) event; each agent's own
        create/update timestamps are created/updated events. Merged, newest first,
        capped. Nothing is invented — an org with no agents and no runs gets [].
      operationId: get_agents_activity
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activityFeed'
          description: ok
      summary: Serves the org-wide recent-activity feed.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/builds:
    get:
      description: |-
        Returns the public index of every published build, most recently
        updated first, so a gallery can link straight to the story behind each product.
        PUBLIC, no tenancy: publishing is the author's act, and only published root
        sessions appear here.
      operationId: get_agents_builds
      parameters:
      - description: Limit caps the page. Absent, zero or over 500 reads as 100.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/buildList'
          description: ok
      summary: Returns the public index of every published build, most recently updated
        first, so a gallery can link straight to the story behind each product.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/builds/{org}/{project}:
    get:
      description: |-
        Returns the readable build of one product: the agent session that
        produced it, turn by turn — the prompts, the reasoning, the commits each turn
        produced — plus the exact `git log` that re-derives every commit binding from
        git itself, so nothing here has to be taken on trust.

        PUBLIC, no tenancy: it answers only for a session its author explicitly
        published, which is what makes it safe to be anonymous. An unpublished session
        is invisible here no matter who asks; its owner reads it through the org-scoped
        /v1/agents/sessions routes, which need a validated principal.
      operationId: get_agents_builds_by_org_by_project
      parameters:
      - description: Org is the org that published the build, from the path.
        example: hanzo
        in: path
        name: org
        required: true
        schema:
          type: string
      - description: Project is the product's slug, from the path.
        example: landing
        in: path
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/buildView'
          description: ok
      summary: 'Returns the readable build of one product: the agent session that
        produced it, turn by turn — the prompts, the reasoning, the commits each turn
        produced — plus the exact `git log` that re-derives every commit binding from
        git itself, so nothing here has to be taken on trust.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/chat:
    post:
      description: |-
        Answers one turn of a conversation with four things: the model's `reply`, the `actions` the server executed on the caller's behalf, the `ops` the client must apply itself, and the `conversationId` the turn was recorded under.

        The split between actions and ops is the rule most easily got wrong. A tool call is executed HERE only when the chosen preset is server-executing AND the tool resolves in the caller's own scope; every other call is handed back as an op for the client to apply to its own graph or UI. A tool that fails still comes back as an action, carrying its error rather than failing the round.

        `preset` selects the system prompt and the tool set (`capability` is a legacy alias for it); an unknown one is refused. `conversationId` continues an existing thread, and its absence starts one. A validated principal with a non-empty org is required — the org is the sole authority for both persistence and tool scope, and is NEVER read from the body.

        A completion refused for the caller's own reason — 402 insufficient balance, 429, 403 — is relayed with its own status and body verbatim, so the real billing message reaches the client instead of an opaque gateway error. Only a genuine upstream fault becomes a 502.
      operationId: post_agents_chat
      summary: Run one tool-calling round against your org's own tools
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/chat/conversations:
    get:
      description: |-
        Returns a summary of every agent conversation in the caller's org — id, derived title, and when it was last appended to — for populating a thread list.

        Scoped to the caller's org and nothing else, and that isolation is structural rather than a filter: conversations are persisted in a store opened PER ORG, so there is no query in which another tenant's threads could appear. A validated principal with a non-empty org is required; 403 without one.
      operationId: get_agents_chat_conversations
      summary: List the agent threads in your org
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/chat/conversations/{id}:
    get:
      description: |-
        Returns every message of one conversation in order — role, content, the assistant's tool calls where it made any, and each message's creation time — which is the transcript a client replays to resume a thread.

        The lookup happens inside the caller's OWN per-org store, so an id belonging to another tenant is not refused, it is simply absent: the answer is 200 with an empty message list. Read it as "no such conversation for you" rather than as an empty thread. A validated principal with a non-empty org is required; 403 without one.
      operationId: get_agents_chat_conversations_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Read one agent thread in full
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/chat/presets:
    get:
      description: |-
        Returns the preset catalog: each entry's id, its description and whether it is server-executing — the flag that decides if a preset's tool calls run here or come back for the client to apply. The ids are what the round accepts in `preset`.

        The catalog is compiled into the build, identical for every caller, and this is the one read in the group that needs no principal.
      operationId: get_agents_chat_presets
      summary: List the agent presets available to a caller
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/coding:
    post:
      description: |-
        Runs a coding task on a repository: clones it into a sandbox, lets a model read
        and edit the code, run the tests, and push the work to a branch. Say the thing
        you want done — "fix the failing auth test in hanzoai/cloud" — and the run
        infers the repo, the branch and the plan. No prefix, no ceremony.

        It answers 202 with the run's handle the moment the run is ADMITTED — not when
        it finishes. A coding run takes minutes; holding a request open for one would
        tie a connection to a model loop and give the caller nothing it cannot get
        better from the session stream.

        The handle is a session id, and that is deliberate: the session is already the
        run's durable record and its live stream (/v1/agents/sessions/{id}/stream), so
        this door does not grow a progress endpoint, a status endpoint or a cancel
        endpoint of its own. One way to watch a run, whoever started it.

        It is also how work CONTINUES. Pass an earlier run's session as `after` and
        this one starts from where that one stopped, so "now add tests for it" builds
        on the branch already pushed instead of a fresh clone. The follow-up still gets
        its own branch and its own session — one run, one branch, always reviewable on
        its own.
      operationId: post_agents_coding
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CodingStartIn'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodingStarted'
          description: accepted
      summary: Start one autonomous coding run against a repo in the caller's org
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/metrics:
    get:
      description: |-
        Serves the invocations-over-time histogram for the org's Agents
        dashboard. Every point is a REAL count of recorded runs in that time bucket —
        one series line per agent that ran in the window. The Resource Usage rollup is
        all-null because this store meters no CPU/memory/storage/cost; the console
        renders those as "—" rather than a fabricated figure. No runs => empty series
        (an honest "not connected / no activity yet"), never a synthesized trend.
      operationId: get_agents_metrics
      parameters:
      - description: 'Range is the window to bucket: 24H, 7D or 30D. Anything else
          reads as 30D.'
        example: 7D
        in: query
        name: range
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/metricsView'
          description: ok
      summary: Serves the invocations-over-time histogram for the org's Agents dashboard.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/runs:
    get:
      description: |-
        Returns the org's agent runs across EVERY agent, newest first —
        what ran here, for whom, on which model, how long it took, and why it failed.

        It is the feed the per-agent history could not be: an operator asking "what is
        this tenant's agent plane doing" does not start out knowing an agent ref, and
        answering by listing the agents and then paging each one's history is N+1 round
        trips to reconstruct one ordering the database already has (RunsSince, ordered
        by created_at over the org index).

        The org is the CALLER's, resolved from identity by tenantStore — never a
        parameter. There is deliberately no org field on orgRunsQuery to forge: run
        history is the tenant's own record, and the only tenant this can answer for is
        the one asking.
      operationId: get_agents_runs
      parameters:
      - description: |-
          Limit caps how many runs come back, newest first. Absent, zero or out of
          range (1..200) reads as 50.
        example: 20
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          Status keeps only runs with this outcome ("ok" or "error"). Empty keeps
          both. It is the filter an operator reaches for first — "show me what broke"
          — and answering it here rather than by paging the whole history client-side
          is the difference between a usable feed and a download.
        example: error
        in: query
        name: status
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runList'
          description: ok
      summary: Returns the org's agent runs across EVERY agent, newest first — what
        ran here, for whom, on which model, how long it took, and why it failed.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/sessions:
    get:
      description: |-
        Returns the caller org's live sessions, newest first — each with
        its event count, its direct-child count and a one-line preview of its latest
        event. With no filter it returns ROOT sessions only, so a dashboard shows one
        row per flow rather than one per subagent; ?root= or ?parent= descends.
      operationId: get_agents_sessions
      parameters:
      - description: Root scopes the page to one subagent tree (its root session id).
        in: query
        name: root
        required: false
        schema:
          type: string
      - description: |-
          Parent scopes the page to the direct children of one session. Ignored when
          root is set; with neither, only ROOT sessions come back.
        in: query
        name: parent
        required: false
        schema:
          type: string
      - description: Status filters to running, paused, done or error.
        example: running
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Project filters to the sessions tagged with one product slug.
        in: query
        name: project
        required: false
        schema:
          type: string
      - description: Limit caps the page. Absent, zero or over 500 reads as 100.
        example: 20
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sessionList'
          description: ok
      summary: Returns the caller org's live sessions, newest first — each with its
        event count, its direct-child count and a one-line preview of its latest event.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    post:
      description: |-
        Opens a live agent session in the caller's org — the row every
        surface (the CLI's outer agent, hanzo.bot, the console, chat) hangs its
        activity off. A session with a parentSessionId becomes a subagent of that
        session and inherits its root, so one flow is one tree; without one it is
        itself a root. Registering with a terminal status records a session that has
        already finished.
      operationId: post_agents_sessions
      requestBody:
        content:
          application/json:
            example:
              agent: hanzo-dev
              host: gpu-01
              title: ship the landing page
            schema:
              $ref: '#/components/schemas/registerReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sessionView'
          description: created
      summary: Opens a live agent session in the caller's org — the row every surface
        (the CLI's outer agent, hanzo.bot, the console, chat) hangs its activity off.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/sessions/{id}:
    get:
      description: |-
        Returns one session with its direct child sessions and its 50 most
        recent events, oldest of those first.
      operationId: get_agents_sessions_by_id
      parameters:
      - description: ID is the session to act on, from the path.
        example: sess_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sessionDetail'
          description: ok
      summary: Returns one session with its direct child sessions and its 50 most
        recent events, oldest of those first.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    patch:
      description: |-
        Updates a session's surface-owned truth: its status, its title,
        the run-target it is dispatched to, and the product it built plus whether that
        build's story is public. A FINISHED session stays finished — reopening a
        done/error run would fabricate liveness — and publishing is refused unless the
        session names the project it built, because the public build route is keyed on
        (org, project).
      operationId: patch_agents_sessions_by_id
      parameters:
      - description: ID is the session to update, from the path.
        example: sess_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              id: sess_1
              status: done
            schema:
              $ref: '#/components/schemas/patchSessionIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sessionView'
          description: ok
      summary: 'Updates a session''s surface-owned truth: its status, its title, the
        run-target it is dispatched to, and the product it built plus whether that
        build''s story is public.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/sessions/{id}/control:
    get:
      description: |-
        Returns the steering commands (pause/resume/stop/message)
        recorded against the caller's own session that are newer than the cursor,
        oldest first, with the cursor to poll from next. It is how a locally started
        `hanzo code` session — which is not task-backed, so nothing forwards its
        commands to an execution engine — consumes what the dashboard posted. Read-only
        and bounded at 200 per poll, so a steady poll is cheap and an applied command is
        never redelivered.
      operationId: get_agents_sessions_by_id_control
      parameters:
      - description: ID is the session whose commands are being drained, from the
          path.
        example: sess_1
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: |-
          After is the last seq this poller applied; only commands newer than it come
          back. Absent or negative reads as 0, which drains from the beginning.
        example: 12
        in: query
        name: after
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/controlDrain'
          description: ok
      summary: Returns the steering commands (pause/resume/stop/message) recorded
        against the caller's own session that are newer than the cursor, oldest first,
        with the cursor to poll from next.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/sessions/{id}/events:
    post:
      description: |-
        Records a message, tool-call, spawn, log, status or control turn against the session and answers 201 with the stored event, including the monotonic `seq` the store assigned — the cursor every reader pages from. The same turn is fanned out live to every stream subscriber watching that session's tree.

        Requires a validated principal carrying an org, and the session must already exist IN THAT ORG: an id belonging to another tenant is a 404 exactly like one that does not exist, so the log can never be written across a tenant boundary. `actor` defaults to the calling principal when the body names none. `kind` must be one of the six above, and `payload` must be valid JSON of at most 64 KiB.

        The payload is scanned for credentials BEFORE it is stored, and a hit REFUSES the write with 422 rather than redacting it: {status, code: "secret_in_transcript", error, findings:[…]}, each finding naming the rule, severity, line, a masked preview and a SHA-256 fingerprint the author can match against the value they rotate. The detected value itself appears nowhere in that body, because it was never stored. That in-band findings array is the reason this operation cannot be typed.
      operationId: post_agents_sessions_by_id_events
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Append one turn to a session's ordered log.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/sessions/{id}/message:
    post:
      description: "Records `message` as a durable control event carrying the caller's
        text and answers 200 with {command, event, forwarded} — this is how a dashboard
        steers an agent mid-run. It is the one command with a required body: a `message`
        (up to 16 KiB) or a `payload`, and 400 with neither. The credential scan that
        guards an appended turn covers `payload` here; `message` is bounded but not
        scanned. \n\nRequires a validated principal carrying an org, and the session
        must exist IN THAT ORG — a foreign id is a 404, so no tenant can steer another's
        agents. A FINISHED session (done or error) refuses every command with 409:
        a run that has ended cannot be steered.\n\nTHE COMMAND IS AN INTENT, NOT A
        STATE CHANGE. Nothing here writes the session's status. A 200 means the command
        was durably recorded and delivered, never that the agent has actually paused,
        resumed or stopped; the status becomes paused, done or error only when the
        surface running the agent reports it back through a session update. That surface
        learns of the command in one of two ways: a task-backed session (one carrying
        a workflow id, with a tasks backend wired) has it forwarded to the durable-execution
        engine, and `forwarded` says so; everything else is record-only, and the running
        surface — a locally started `hanzo code` session, for one — drains it by polling
        the session's control endpoint. Today that is every session: the only controller
        wired forwards nothing, so `forwarded` is false and polling is how a command
        arrives. If a forward is attempted and fails, the answer is 502 stating that
        the command was recorded but not forwarded: the intent is never lost."
      operationId: post_agents_sessions_by_id_message
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Send text into a running session.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/sessions/{id}/pause:
    post:
      description: "Records `pause` as a durable control event on the session and
        answers 200 with {command, event, forwarded} — the stored event carries the
        `seq` that orders it against every other turn. \n\nRequires a validated principal
        carrying an org, and the session must exist IN THAT ORG — a foreign id is
        a 404, so no tenant can steer another's agents. A FINISHED session (done or
        error) refuses every command with 409: a run that has ended cannot be steered.\n\nTHE
        COMMAND IS AN INTENT, NOT A STATE CHANGE. Nothing here writes the session's
        status. A 200 means the command was durably recorded and delivered, never
        that the agent has actually paused, resumed or stopped; the status becomes
        paused, done or error only when the surface running the agent reports it back
        through a session update. That surface learns of the command in one of two
        ways: a task-backed session (one carrying a workflow id, with a tasks backend
        wired) has it forwarded to the durable-execution engine, and `forwarded` says
        so; everything else is record-only, and the running surface — a locally started
        `hanzo code` session, for one — drains it by polling the session's control
        endpoint. Today that is every session: the only controller wired forwards
        nothing, so `forwarded` is false and polling is how a command arrives. If
        a forward is attempted and fails, the answer is 502 stating that the command
        was recorded but not forwarded: the intent is never lost."
      operationId: post_agents_sessions_by_id_pause
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Ask a running session to pause.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/sessions/{id}/resume:
    post:
      description: "Records `resume` as a durable control event on the session and
        answers 200 with {command, event, forwarded}. The session is NOT required
        to be paused first: the only status this refuses is a finished one, because
        the live status is the running surface's to report rather than this endpoint's
        to enforce. \n\nRequires a validated principal carrying an org, and the session
        must exist IN THAT ORG — a foreign id is a 404, so no tenant can steer another's
        agents. A FINISHED session (done or error) refuses every command with 409:
        a run that has ended cannot be steered.\n\nTHE COMMAND IS AN INTENT, NOT A
        STATE CHANGE. Nothing here writes the session's status. A 200 means the command
        was durably recorded and delivered, never that the agent has actually paused,
        resumed or stopped; the status becomes paused, done or error only when the
        surface running the agent reports it back through a session update. That surface
        learns of the command in one of two ways: a task-backed session (one carrying
        a workflow id, with a tasks backend wired) has it forwarded to the durable-execution
        engine, and `forwarded` says so; everything else is record-only, and the running
        surface — a locally started `hanzo code` session, for one — drains it by polling
        the session's control endpoint. Today that is every session: the only controller
        wired forwards nothing, so `forwarded` is false and polling is how a command
        arrives. If a forward is attempted and fails, the answer is 502 stating that
        the command was recorded but not forwarded: the intent is never lost."
      operationId: post_agents_sessions_by_id_resume
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Ask a paused session to carry on.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/sessions/{id}/stop:
    post:
      description: "Records `stop` as a durable control event on the session and answers
        200 with {command, event, forwarded}. Stop is the one command that CANCELS
        a task-backed session's durable workflow instead of signalling it — pause,
        resume and message are cooperative signals the workflow decides how to act
        on, while this tears it down, with the request's `message` recorded as the
        cancellation reason (a default stands in when none is given). \n\nRequires
        a validated principal carrying an org, and the session must exist IN THAT
        ORG — a foreign id is a 404, so no tenant can steer another's agents. A FINISHED
        session (done or error) refuses every command with 409: a run that has ended
        cannot be steered.\n\nTHE COMMAND IS AN INTENT, NOT A STATE CHANGE. Nothing
        here writes the session's status. A 200 means the command was durably recorded
        and delivered, never that the agent has actually paused, resumed or stopped;
        the status becomes paused, done or error only when the surface running the
        agent reports it back through a session update. That surface learns of the
        command in one of two ways: a task-backed session (one carrying a workflow
        id, with a tasks backend wired) has it forwarded to the durable-execution
        engine, and `forwarded` says so; everything else is record-only, and the running
        surface — a locally started `hanzo code` session, for one — drains it by polling
        the session's control endpoint. Today that is every session: the only controller
        wired forwards nothing, so `forwarded` is false and polling is how a command
        arrives. If a forward is attempted and fails, the answer is 502 stating that
        the command was recorded but not forwarded: the intent is never lost."
      operationId: post_agents_sessions_by_id_stop
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Ask a session to stop for good.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/sessions/{id}/tree:
    get:
      description: |-
        Returns the subagent-flow graph rooted at this session: the session,
        its children, their children, each node carrying its own event count. One
        indexed read pulls the whole flow (every node of a flow shares a root id), so
        the shape is assembled in memory rather than by walking the store per node.
      operationId: get_agents_sessions_by_id_tree
      parameters:
      - description: ID is the session to act on, from the path.
        example: sess_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/treeNode'
          description: ok
      summary: 'Returns the subagent-flow graph rooted at this session: the session,
        its children, their children, each node carrying its own event count.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/sessions/stream:
    get:
      description: |-
        Holds the connection open as text/event-stream and pushes a frame each time the org's registry moves: an `event: session` frame carrying the same session shape the list and detail reads answer with (a registration, an update, or a login-manager revoke tearing a session down), and an `event: event` frame carrying one appended turn. Optional ?root=<session id> narrows the feed to a single subagent tree.

        Requires a validated principal carrying an org; 403 without one. Org-scoped fail-closed: the bus filters on tenant before it fans out, so a subscriber only ever receives its own org's updates, and ?root= narrows that further but can never widen it.

        Delivery is best-effort and the GET reads remain the source of truth. A subscriber that falls more than 256 frames behind is DROPPED — its channel is closed and the stream ends — so one stuck dashboard can never back-pressure a session write; the client reconnects and re-reads the session endpoints to resynchronise. A `: ping` comment every 25 seconds holds the connection open through proxies and is how a departed client is noticed.
      operationId: get_agents_sessions_stream
      summary: Live session and event updates for the caller's org, as Server-Sent
        Events.
      tags:
      - agents
      x-app: agents
      x-public: true
  /v1/agents/targets:
    get:
      description: |-
        Returns every machine registered to the caller's org, newest
        first, each with its live session load.
      operationId: get_agents_targets
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/targetList'
          description: ok
      summary: Returns every machine registered to the caller's org, newest first,
        each with its live session load.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers a machine as an agent target, or re-links one that is
        already registered. Re-linking is idempotent and keyed on org+host+owner, so a
        machine that reconnects refreshes its own row rather than piling up duplicates;
        it answers 200, while a first registration answers 201.
      operationId: post_agents_targets
      requestBody:
        content:
          application/json:
            example:
              host: gpu-01
              kind: gpu
              label: workshop
            schema:
              $ref: '#/components/schemas/targetReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/targetView'
          description: ok
      summary: Registers a machine as an agent target, or re-links one that is already
        registered.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/targets/{id}:
    delete:
      description: |-
        Deregisters one machine. Only its owner, or an org admin, may
        remove it; an unknown id, a cross-org id and a machine owned by someone else
        all answer the same not-found, so a probe learns nothing about what exists.
      operationId: delete_agents_targets_by_id
      parameters:
      - description: ID is the target to act on, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/targetDeleted'
          description: ok
      summary: Deregisters one machine.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    get:
      description: Returns one registered machine, with its live session load.
      operationId: get_agents_targets_by_id
      parameters:
      - description: ID is the target to act on, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/targetView'
          description: ok
      summary: Returns one registered machine, with its live session load.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
    patch:
      description: |-
        Updates one machine in place. Every field is optional; a field the
        request omits is left alone. A metrics patch IS a heartbeat — the server stamps
        its own clock, so a client can neither forge nor backdate staleness.
      operationId: patch_agents_targets_by_id
      parameters:
      - description: ID is the target to update, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              id: tgt_1
              status: draining
            schema:
              $ref: '#/components/schemas/patchTargetIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/targetView'
          description: ok
      summary: Updates one machine in place.
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/targets/{id}/claim:
    post:
      description: |-
        ClaimRoutedRun is the machine's long poll for work: it authenticates the
        daemon, stamps the liveness the dispatch gate reads (the poll IS the proof a
        runner is listening), and waits up to 25 seconds for the next run addressed to
        THIS machine. It answers the run when one arrives and 204 with no body when the
        window elapses, on which the daemon re-polls immediately.

        TWO independent proofs are required and both fail closed to the same 403: the
        caller must own this machine (or be an org admin) AND present its claim key in
        X-Target-Key. A run offered to one machine is unreachable from another's claim.
      operationId: post_agents_targets_by_id_claim
      parameters:
      - description: ID is the target to act on, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/routedRunOut'
          description: ok
      summary: 'ClaimRoutedRun is the machine''s long poll for work: it authenticates
        the daemon, stamps the liveness the dispatch gate reads (the poll IS the proof
        a runner is listening), and waits up to 25 seconds for the next run addressed
        to THIS machine.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/targets/{id}/key:
    post:
      description: |-
        Mints (or rotates) the claim key a `hanzo code --serve`
        daemon presents to claim work for this machine, and returns it ONCE: only its
        SHA-256 hash is stored. Rotating supersedes any prior daemon, so only the
        machine's owner — or an org admin — may call it; every other caller gets the
        same not-found an unknown id gets, and learns nothing about what exists.
      operationId: post_agents_targets_by_id_key
      parameters:
      - description: ID is the target to act on, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/claimKeyOut'
          description: ok
      summary: 'Mints (or rotates) the claim key a `hanzo code --serve` daemon presents
        to claim work for this machine, and returns it ONCE: only its SHA-256 hash
        is stored.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/agents/targets/{id}/runs/{runId}/report:
    post:
      description: |-
        Completes a claimed run: it delivers the terminal result to the
        run's durable owner, which is what lets that workflow finish. Scoped to (org,
        target, run) and claim-key authenticated, so a machine can only ever report a
        run it legitimately holds. Idempotent — a report for an unknown or
        already-finished run answers delivered:false rather than failing, because the
        session's terminal state was already set by the machine's own stream.
      operationId: post_agents_targets_by_id_runs_by_runid_report
      parameters:
      - description: ID is the machine reporting, from the path.
        example: tgt_1
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: RunID is the routed run being completed, from the path.
        example: run_1
        in: path
        name: runId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              branch: hanzo/fix
              changed: true
              id: tgt_1
              ok: true
              runId: run_1
            schema:
              $ref: '#/components/schemas/reportRunIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reportOut'
          description: ok
      summary: 'Completes a claimed run: it delivers the terminal result to the run''s
        durable owner, which is what lets that workflow finish.'
      tags:
      - agents
      x-app: agents
      x-public: true
      x-tool: true
  /v1/ai/account:
    get:
      operationId: get_ai_account
      summary: Account
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/activities:
    get:
      description: List the caller's activities.
      operationId: get_ai_activities
      summary: List activities
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/answer:
    get:
      operationId: get_ai_answer
      summary: Answer
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/articles:
    get:
      description: List the caller's articles.
      operationId: get_ai_articles
      summary: List articles
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one article.
      operationId: post_ai_articles
      summary: Create a article
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/articles/{owner}/{name}:
    delete:
      description: Delete one article.
      operationId: delete_ai_articles_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a article
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one article by its (owner, name) key.
      operationId: get_ai_articles_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a article
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one article. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_articles_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a article
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_articles_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a article
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/articles/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_articles_global
      summary: List articles across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/assets:
    get:
      description: List the caller's assets.
      operationId: get_ai_assets
      summary: List assets
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one asset.
      operationId: post_ai_assets
      summary: Create a asset
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/assets/{owner}/{name}:
    delete:
      description: Delete one asset.
      operationId: delete_ai_assets_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a asset
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one asset by its (owner, name) key.
      operationId: get_ai_assets_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a asset
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one asset. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_assets_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a asset
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_assets_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a asset
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/assets/{owner}/{name}/scan:
    post:
      operationId: post_ai_assets_by_owner_by_name_scan
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Scan (asset)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/assets/scan:
    post:
      operationId: post_ai_assets_scan
      summary: Scan (asset)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/chats:
    get:
      description: List the caller's chats.
      operationId: get_ai_chats
      summary: List chats
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one chat.
      operationId: post_ai_chats
      summary: Create a chat
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/chats/{owner}/{name}:
    delete:
      description: Delete one chat.
      operationId: delete_ai_chats_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a chat
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one chat by its (owner, name) key.
      operationId: get_ai_chats_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a chat
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one chat. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_chats_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a chat
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_chats_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a chat
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/chats/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_chats_global
      summary: List chats across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/connections:
    get:
      description: |-
        Lists the org's connectable AI accounts and whether each is
        currently connected. Never returns a key or a kms:// reference.
      operationId: get_ai_connections
      summary: Lists the org's connectable AI accounts and whether each is currently
        connected.
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        Connects (or reconnects) a third-party AI account for the org by
        sealing the supplied key into KMS and upserting the org's provider row. The raw
        key is sealed BEFORE the row is built and is never persisted or echoed.
      operationId: post_ai_connections
      summary: Connects (or reconnects) a third-party AI account for the org by sealing
        the supplied key into KMS and upserting the org's provider row.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/connections/{provider}:
    delete:
      description: |-
        Disconnects a third-party AI account: it deactivates the org's
        row so completion resolution falls back to the global Hanzo account (no BYO), and
        best-effort tombstones the sealed secret. Idempotent.
      operationId: delete_ai_connections_by_provider
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: 'Disconnects a third-party AI account: it deactivates the org''s row
        so completion resolution falls back to the global Hanzo account (no BYO),
        and best-effort tombstones the sealed secret.'
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        Disconnects a third-party AI account: it deactivates the org's
        row so completion resolution falls back to the global Hanzo account (no BYO), and
        best-effort tombstones the sealed secret. Idempotent.
      operationId: post_ai_connections_by_provider
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: 'Disconnects a third-party AI account: it deactivates the org''s row
        so completion resolution falls back to the global Hanzo account (no BYO),
        and best-effort tombstones the sealed secret.'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/connections/{provider}/authorize:
    get:
      description: |-
        Begins an OAuth connection for the caller's org: it binds the
        org into a signed state and sends the caller to the provider's authorize URL. By
        default it 302-redirects (a top-level browser "connect your login" click); a
        SPA/BFF that needs to drive the redirect itself passes ?format=json and gets
        {authorizeUrl} in the standard envelope. The org is the VERIFIED principal, so
        only the caller's own connection can result.
      operationId: get_ai_connections_by_provider_authorize
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: 'Begins an OAuth connection for the caller''s org: it binds the org
        into a signed state and sends the caller to the provider''s authorize URL.'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/connections/{provider}/callback:
    get:
      description: |-
        Completes OAuth: the org is recovered from the SIGNED state
        (not a header), the code is exchanged for a token, the token is SEALED into KMS
        (never the row/logs) through the same path as a BYOK key, and the org's provider
        row is upserted to "connected". The browser is then redirected back to the console
        with ?ai_connected=<provider> (or ?ai_connect_error=<provider> on failure). Because
        the org comes from the state THIS server signed, an attacker cannot land their
        token in a victim org — which is why this endpoint is state-authenticated rather
        than credential-gated.
      operationId: get_ai_connections_by_provider_callback
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: 'Completes OAuth: the org is recovered from the SIGNED state (not a
        header), the code is exchanged for a token, the token is SEALED into KMS (never
        the row/logs) through the same path as a BYOK key, and the org''s provider
        row is upserted to "connected".'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/connections/{provider}/usage:
    get:
      description: |-
        Imports the caller org's usage for a connected third-party
        account. The org is resolved from the VERIFIED principal (requireConnectionOrg), so a
        tenant reads only its own connection. The key is unsealed SERVER-SIDE and never
        returned. An unconnected account, a missing importer, or a scope-denied provider all
        return a 200 ProviderUsage with connected/available flags + a human note — the UI's
        honest-empty states — never a fabricated figure.
      operationId: get_ai_connections_by_provider_usage
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: Imports the caller org's usage for a connected third-party account.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/dashboards/agents:
    get:
      operationId: get_ai_dashboards_agents
      summary: Dashboards Agents
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/dashboards/vm:
    get:
      operationId: get_ai_dashboards_vm
      summary: Dashboards Vm
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/deployments:
    get:
      description: List the caller's deployments.
      operationId: get_ai_deployments
      summary: List deployments
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one application.
      operationId: post_ai_deployments
      summary: Create a application
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/deployments/{owner}/{name}:
    delete:
      description: Delete one application.
      operationId: delete_ai_deployments_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a application
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one application by its (owner, name) key.
      operationId: get_ai_deployments_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a application
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one application. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_deployments_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a application
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_deployments_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a application
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/deployments/{owner}/{name}/deploy:
    post:
      operationId: post_ai_deployments_by_owner_by_name_deploy
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Deploy (application)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/deployments/{owner}/{name}/undeploy:
    post:
      operationId: post_ai_deployments_by_owner_by_name_undeploy
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Undeploy (application)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/feedback:
    post:
      description: |-
        Attaches a per-request outcome reward to the routing decision
        that served request_id — the enso training loop's quality signal. Org-scoped via
        the same session-OR-Bearer principal the usage read uses (RequirePrincipal): the
        reward lands only on the caller's OWN org's event, so a request_id from another
        org (or unknown) is a 404 — cross-org writes are impossible and unknown ids are
        indistinguishable from foreign ones. Idempotent: a repeat overwrites. The body
        carries NO prompt text — only {request_id, reward|rating}.
      operationId: post_ai_feedback
      summary: Attaches a per-request outcome reward to the routing decision that
        served request_id — the enso training loop's quality signal.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files:
    get:
      description: List the caller's files.
      operationId: get_ai_files
      summary: List files
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one file.
      operationId: post_ai_files
      summary: Create a file
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/{owner}/{name}:
    delete:
      description: Delete one file.
      operationId: delete_ai_files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a file
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one file by its (owner, name) key.
      operationId: get_ai_files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a file
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one file. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a file
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a file
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/{owner}/{name}/vectors:
    post:
      operationId: post_ai_files_by_owner_by_name_vectors
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Vectors (file)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/activate:
    post:
      operationId: post_ai_files_activate
      summary: Activate (file)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/active:
    get:
      operationId: get_ai_files_active
      summary: Active (file)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_files_global
      summary: List files across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/files/upload:
    post:
      operationId: post_ai_files_upload
      summary: Upload (file)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/cancel:
    post:
      description: |-
        Deletes the TrainJob CR, meters the GPU-hours used so far, and
        marks the job cancelled. ?id= or ?name=
      operationId: post_ai_finetune_cancel
      summary: Deletes the TrainJob CR, meters the GPU-hours used so far, and marks
        the job cancelled.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/deploy:
    post:
      description: |-
        Serves a completed job's checkpoints and registers the result
        as a routable model on api.hanzo.ai. ?id= or ?name=
      operationId: post_ai_finetune_deploy
      summary: Serves a completed job's checkpoints and registers the result as a
        routable model on api.hanzo.ai.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/hf/datasets:
    get:
      description: Proxies a HuggingFace dataset search (dataset picker).
      operationId: get_ai_finetune_hf_datasets
      summary: Proxies a HuggingFace dataset search (dataset picker).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/hf/models:
    get:
      description: Proxies a HuggingFace model search (base-model picker).
      operationId: get_ai_finetune_hf_models
      summary: Proxies a HuggingFace model search (base-model picker).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/hf/repo:
    get:
      description: Returns a repo's detail (files, gated/private state). ?id=&kind=model|dataset
      operationId: get_ai_finetune_hf_repo
      summary: Returns a repo's detail (files, gated/private state).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/job:
    get:
      description: Returns one job with refreshed live status. ?id=owner/name or ?name=
      operationId: get_ai_finetune_job
      summary: Returns one job with refreshed live status.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/jobs:
    get:
      description: Returns the org's jobs, refreshing live status for active ones.
      operationId: get_ai_finetune_jobs
      summary: Returns the org's jobs, refreshing live status for active ones.
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        Validates the request, resolves efficient defaults, persists the
        job, and submits a real TrainJob CR. A submit failure (e.g. no cluster wired) is
        surfaced honestly: the job is saved with status "failed" + the reason, never faked.
      operationId: post_ai_finetune_jobs
      summary: Validates the request, resolves efficient defaults, persists the job,
        and submits a real TrainJob CR.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/finetune/presets:
    get:
      description: |-
        Returns the new-job catalog plus, when a selection is passed
        (?baseModel&method&task&preset[&datasetExamples]), the recommended config so the
        console can render "Recommended" as a one-click, ready-to-run default.
      operationId: get_ai_finetune_presets
      summary: Returns the new-job catalog plus, when a selection is passed (?baseModel&method&task&preset[&datasetExamples]),
        the recommended config so the console can render "Recommended" as a one-click,
        ready-to-run default.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/forms:
    get:
      description: List the caller's forms.
      operationId: get_ai_forms
      summary: List forms
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one form.
      operationId: post_ai_forms
      summary: Create a form
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/forms/{owner}/{name}:
    delete:
      description: Delete one form.
      operationId: delete_ai_forms_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a form
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one form by its (owner, name) key.
      operationId: get_ai_forms_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a form
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one form. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_forms_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a form
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_forms_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a form
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/forms/data:
    get:
      operationId: get_ai_forms_data
      summary: Data (form)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/forms/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_forms_global
      summary: List forms across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/graphs:
    get:
      description: List the caller's graphs.
      operationId: get_ai_graphs
      summary: List graphs
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one graph.
      operationId: post_ai_graphs
      summary: Create a graph
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/graphs/{owner}/{name}:
    delete:
      description: Delete one graph.
      operationId: delete_ai_graphs_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a graph
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one graph by its (owner, name) key.
      operationId: get_ai_graphs_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a graph
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one graph. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_graphs_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a graph
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_graphs_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a graph
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/graphs/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_graphs_global
      summary: List graphs across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/k8s-status:
    get:
      operationId: get_ai_k8s-status
      summary: K8s Status
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/mcp/tools:
    get:
      description: |-
        Tools reports what THIS PROCESS's MCP door carries: how many tools its own
        registry projects, optionally their names, and which subsystems this process
        composed. It is the answer to "is this door up and does it have anything behind
        it" — a question a status code cannot answer, since an empty door and a full
        one are both 200. What the FLEET's door carries is the fleet door's own answer:
        POST /v1/mcp, tools/list, which asks every subsystem and names the ones that
        did not reply.
      operationId: aiMCPTools
      parameters:
      - description: |-
          Names asks for this process's tool NAMES and not only how many there are.
          Off by default: a list of names is a page, and the question this op exists
          to answer ("is the door up and does it have anything behind it") is answered
          by the count.
        in: query
        name: names
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/aiMCPSurface'
          description: ok
      summary: 'Tools reports what THIS PROCESS''s MCP door carries: how many tools
        its own registry projects, optionally their names, and which subsystems this
        process composed.'
      tags:
      - ai
      x-app: ai
      x-public: true
      x-tool: true
  /v1/ai/memory/delete:
    post:
      description: Delete one of the authenticated user's memories
      operationId: post_ai_memory_delete
      summary: Delete one of the authenticated user's memories
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/facts:
    get:
      description: List the authenticated user's stored facts
      operationId: get_ai_memory_facts
      summary: List the authenticated user's stored facts
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/list:
    get:
      description: List the authenticated user's memories, newest first
      operationId: get_ai_memory_list
      summary: List the authenticated user's memories, newest first
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/recall:
    get:
      description: |-
        Recall recent/relevant memories for context injection; with q it
        ranks semantically, without q it returns the most recent
      operationId: get_ai_memory_recall
      summary: Recall recent/relevant memories for context injection; with q it
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/remember:
    post:
      description: Store a memory for the authenticated user
      operationId: post_ai_memory_remember
      summary: Store a memory for the authenticated user
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/search:
    get:
      description: Search the authenticated user's memories (semantic, text fallback)
      operationId: get_ai_memory_search
      summary: Search the authenticated user's memories (semantic, text fallback)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/memory/update:
    post:
      description: Update one of the authenticated user's memories
      operationId: post_ai_memory_update
      summary: Update one of the authenticated user's memories
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/messages:
    get:
      description: List the caller's messages.
      operationId: get_ai_messages
      summary: List messages
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one message.
      operationId: post_ai_messages
      summary: Create a message
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/messages/{owner}/{name}:
    delete:
      description: Delete one message.
      operationId: delete_ai_messages_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a message
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one message by its (owner, name) key.
      operationId: get_ai_messages_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a message
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one message. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_messages_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a message
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_messages_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a message
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/messages/{owner}/{name}/answer:
    get:
      operationId: get_ai_messages_by_owner_by_name_answer
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Answer (message)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/messages/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_messages_global
      summary: List messages across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/messages/welcome:
    delete:
      operationId: delete_ai_messages_welcome
      summary: Welcome (message)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/nodes:
    get:
      description: List the caller's nodes.
      operationId: get_ai_nodes
      summary: List nodes
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one node.
      operationId: post_ai_nodes
      summary: Create a node
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/nodes/{owner}/{name}:
    delete:
      description: Delete one node.
      operationId: delete_ai_nodes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a node
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one node by its (owner, name) key.
      operationId: get_ai_nodes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a node
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one node. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_nodes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a node
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_nodes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a node
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/nodes/{owner}/{name}/tunnel:
    get:
      operationId: get_ai_nodes_by_owner_by_name_tunnel
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Tunnel (node)
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      operationId: post_ai_nodes_by_owner_by_name_tunnel
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Tunnel (node)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/org/settings:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_org_settings
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_org_settings
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_org_settings
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_org_settings
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_org_settings
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/org/settings/list:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_org_settings_list
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_org_settings_list
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_org_settings_list
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_org_settings_list
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_org_settings_list
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/preferences:
    patch:
      operationId: patch_ai_preferences
      summary: Preferences
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      operationId: put_ai_preferences
      summary: Preferences
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/prometheus:
    get:
      operationId: get_ai_prometheus
      summary: Prometheus
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/providers:
    get:
      description: List the caller's providers.
      operationId: get_ai_providers
      summary: List providers
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one provider.
      operationId: post_ai_providers
      summary: Create a provider
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/providers/{owner}/{name}:
    delete:
      description: Delete one provider.
      operationId: delete_ai_providers_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a provider
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one provider by its (owner, name) key.
      operationId: get_ai_providers_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a provider
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one provider. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_providers_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a provider
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_providers_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a provider
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/providers/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_providers_global
      summary: List providers across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/providers/mcp-tools:
    post:
      operationId: post_ai_providers_mcp-tools
      summary: Mcp Tools (provider)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/context:
    get:
      description: |-
        Return every stored chunk of one file_id (full document context).
        Consolidates the retired chat-rag-api GET /documents/{id}/context.
      operationId: get_ai_rag_context
      summary: Return every stored chunk of one file_id (full document context).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/delete:
    post:
      description: |-
        Delete all chunks of one or more uploaded files (by file_id) from
        the owner's Search+Vector index. Consolidates the retired chat-rag-api
        DELETE /documents.
      operationId: post_ai_rag_delete
      summary: Delete all chunks of one or more uploaded files (by file_id) from the
        owner's Search+Vector index.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/embed:
    post:
      description: |-
        Parse, chunk, and embed one uploaded file under its file_id into
        the unified Search+Vector index, scoped to the authenticated owner. Provide
        inline `content` or a `url` to fetch+parse (PDF/CSV/XLSX/PPTX/…). Re-embedding
        the same file_id replaces its chunks. Consolidates the retired chat-rag-api
        POST /embed and /local/embed.
      operationId: post_ai_rag_embed
      summary: Parse, chunk, and embed one uploaded file under its file_id into the
        unified Search+Vector index, scoped to the authenticated owner.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/ingest:
    post:
      description: |-
        Unified RAG ingest: parse + chunk + embed documents and pipe them
        to BOTH Hanzo Vector (semantic) AND Hanzo Search (keyword) under the tenant
        index {owner}-{store}-docs — the same index /v1/chat retrieval reads. The
        source is pluggable: "upload" (inline files/documents), "github" (index a
        repo), "crawl" (web), or "s3" (the store's object-storage space). The owner
        is bound to the authenticated principal; the client-supplied owner is never
        trusted.
      operationId: post_ai_rag_ingest
      summary: 'Unified RAG ingest: parse + chunk + embed documents and pipe them
        to BOTH Hanzo Vector (semantic) AND Hanzo Search (keyword) under the tenant
        index {owner}-{store}-docs — the same index /v1/chat retrieval reads.'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/query:
    post:
      description: |-
        Retrieve the top-K chunks relevant to a query, scoped to a single
        uploaded file (`file_id`). Hybrid keyword+vector retrieval over the same
        index. Consolidates the retired chat-rag-api POST /query.
      operationId: post_ai_rag_query
      summary: Retrieve the top-K chunks relevant to a query, scoped to a single uploaded
        file (`file_id`).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/rag/query-multiple:
    post:
      description: |-
        Retrieve the top-K chunks relevant to a query, scoped to a SET of
        uploaded files (`file_ids`). Consolidates the retired chat-rag-api POST
        /query_multiple. Shares one retrieval path with /rag/query.
      operationId: post_ai_rag_query-multiple
      summary: Retrieve the top-K chunks relevant to a query, scoped to a SET of uploaded
        files (`file_ids`).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records:
    get:
      description: List the caller's records.
      operationId: get_ai_records
      summary: List records
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one record.
      operationId: post_ai_records
      summary: Create a record
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/{owner}/{name}:
    delete:
      description: Delete one record.
      operationId: delete_ai_records_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a record
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one record by its (owner, name) key.
      operationId: get_ai_records_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a record
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one record. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_records_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a record
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_records_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a record
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/batch:
    post:
      operationId: post_ai_records_batch
      summary: Batch (record)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/commit:
    post:
      operationId: post_ai_records_commit
      summary: Commit (record)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/commit-second:
    post:
      operationId: post_ai_records_commit-second
      summary: Commit Second (record)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/query:
    get:
      operationId: get_ai_records_query
      summary: Query (record)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/records/query-second:
    get:
      operationId: get_ai_records_query-second
      summary: Query Second (record)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/remote-connections:
    get:
      description: List the caller's remote-connections.
      operationId: get_ai_remote-connections
      summary: List remote-connections
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one connection.
      operationId: post_ai_remote-connections
      summary: Create a connection
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/remote-connections/{owner}/{name}:
    delete:
      description: Delete one connection.
      operationId: delete_ai_remote-connections_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a connection
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one connection by its (owner, name) key.
      operationId: get_ai_remote-connections_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a connection
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one connection. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_remote-connections_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a connection
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_remote-connections_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a connection
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/remote-connections/{owner}/{name}/start:
    post:
      operationId: post_ai_remote-connections_by_owner_by_name_start
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Start (connection)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/remote-connections/{owner}/{name}/stop:
    post:
      operationId: post_ai_remote-connections_by_owner_by_name_stop
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Stop (connection)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/artifact-meta:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_router_artifact-meta
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_router_artifact-meta
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_router_artifact-meta
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_router_artifact-meta
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_router_artifact-meta
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/data:
    delete:
      operationId: delete_ai_router_data
      summary: Router Data
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      operationId: get_ai_router_data
      summary: Router Data
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/defaults:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_router_defaults
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_router_defaults
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_router_defaults
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_router_defaults
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_router_defaults
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/history:
    get:
      description: |-
        Returns the router-improvement time-series. Two scopes, one route,
        mirroring /v1/ai/router/stats:

          - ?scope=platform — PUBLIC-safe aggregate over ALL orgs, no authentication. Emits
            the daily reward/cost-saved/adoption series (task mix included, model ids NOT)
            and the retrain timeline. This is what world.hanzo.ai polls.
          - default (org scope) — requires a signed-in principal, scoped to the caller's OWN
            org (a super admin may pass ?org= to target another or "" for all).

        Window: ?days=N (default 30, capped at 90). Aggregates only.
      operationId: get_ai_router_history
      summary: Returns the router-improvement time-series.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/judge-panel:
    get:
      description: |-
        Returns the LIVE Mean-Field Judge Panel state: the configured
        panel + dynamic judge posture (enabled/sample) resolved from the "*"
        GlobalDefaultOwner row, the live in-process per-judge calibration (weight/mean/n),
        and the static published benchmark. PUBLIC-safe and platform-global (model ids +
        scalars only), so it rides the same unauthenticated, balance-exempt class as
        /v1/ai/router/stats?scope=platform — the world widget polls it with no auth. The judge
        state is a single in-process population (not per-org), so there is nothing to scope;
        ?scope=platform is accepted for symmetry with router-stats.
      operationId: get_ai_router_judge-panel
      summary: 'Returns the LIVE Mean-Field Judge Panel state: the configured panel
        + dynamic judge posture (enabled/sample) resolved from the "*" GlobalDefaultOwner
        row, the live in-process per-judge calibration (weight/mean/n), and the static
        published benchmark.'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/ledger:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_router_ledger
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_router_ledger
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_router_ledger
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_router_ledger
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_router_ledger
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/policy:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_router_policy
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_router_policy
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_router_policy
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_router_policy
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_router_policy
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/rewards:
    delete:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: delete_ai_router_rewards
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: get_ai_router_rewards
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: patch_ai_router_rewards
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: post_ai_router_rewards
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: |-
        The HTTP transport binding for the RESTful router-config
        nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta} and
        /v1/ai/org/settings[/list]). It dispatches IN-PROCESS through dispatchGateway —
        the SAME canonical ZAP gateway registry (zap_registry.go) that the MsgType 200
        handler serves over the gateway transport. The native ZAP
        handler is the ONE and ONLY implementation of these routes; this is purely the
        api.hanzo.ai HTTP binding, so there is NO controller twin to drift from and the
        split-brain the router refactor removed stays removed.

        Why a bridge and not a twin controller method: every other migrated route
        (get-records, get-connections, …) carries BOTH a controller method and a
        ZAP handler — the exact dual-impl drift that silently NULLed customer router
        settings (the update-router-policy data-wipe). Routing these nouns through the
        ZAP handler over one adapter keeps a single source of truth.

        Identity is the request's own Bearer credential (Authorization header), which
        the native handlers resolve exactly as the gateway does — every caller
        (console, chat, app) already sends it. The dispatched handler returns a ZAP
        message whose status is field 0 and body is field 4 (BuildCloudResponse /
        BuildGatewayResponse layout); both are relayed verbatim. The route is mapped
        "*" (any verb) because the native handler is method-aware: /v1/ai/router/policy
        splits GET (read) vs PUT (write), /v1/ai/org/settings GET/PUT/DELETE, and returns
        405 for a verb it does not own.
      operationId: put_ai_router_rewards
      summary: The HTTP transport binding for the RESTful router-config nouns (/v1/ai/router/{policy,defaults,ledger,rewards,artifact-meta}
        and /v1/ai/org/settings[/list]).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/router/stats:
    get:
      description: |-
        Returns the router observability aggregate. Two scopes, one route:

          - ?scope=platform — PUBLIC-safe aggregate over ALL orgs, no authentication.
            Emits rates, shares, per-task/per-model counts, throughput, and the cost
            RATIO (saved_pct) + counterfactual model id, but NEVER absolute $ levels,
            org identity, raw events, or feature vectors. This is what world.hanzo.ai
            polls.
          - default (org scope) — requires a signed-in principal; scoped to the caller's
            OWN org (a super admin may pass ?org= to target another org or "" for all).
            Carries the absolute $/MTok indices for the admin savings panel.

        Window: ?since= (RFC3339) or ?hours= (default 24, capped). Aggregates only.
      operationId: get_ai_router_stats
      summary: Returns the router observability aggregate.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/routes:
    get:
      description: List the caller's routes.
      operationId: get_ai_routes
      summary: List routes
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one model-route.
      operationId: post_ai_routes
      summary: Create a model-route
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/routes/{owner}/{name}:
    delete:
      description: Delete one model-route.
      operationId: delete_ai_routes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a model-route
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one model-route by its (owner, name) key.
      operationId: get_ai_routes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a model-route
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one model-route. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_routes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a model-route
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_routes_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a model-route
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scales:
    get:
      description: List the caller's scales.
      operationId: get_ai_scales
      summary: List scales
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one scale.
      operationId: post_ai_scales
      summary: Create a scale
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scales/{owner}/{name}:
    delete:
      description: Delete one scale.
      operationId: delete_ai_scales_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a scale
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one scale by its (owner, name) key.
      operationId: get_ai_scales_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a scale
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one scale. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_scales_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a scale
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_scales_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a scale
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scales/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_scales_global
      summary: List scales across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scales/public:
    get:
      operationId: get_ai_scales_public
      summary: Public (scale)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scans:
    get:
      description: List the caller's scans.
      operationId: get_ai_scans
      summary: List scans
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one scan.
      operationId: post_ai_scans
      summary: Create a scan
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/scans/{owner}/{name}:
    delete:
      description: Delete one scan.
      operationId: delete_ai_scans_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a scan
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one scan by its (owner, name) key.
      operationId: get_ai_scans_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a scan
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one scan. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_scans_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a scan
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_scans_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a scan
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/signin:
    post:
      operationId: post_ai_signin
      summary: Signin
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/signin-sessions:
    get:
      description: List the caller's signin-sessions.
      operationId: get_ai_signin-sessions
      summary: List signin-sessions
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one session.
      operationId: post_ai_signin-sessions
      summary: Create a session
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/signin-sessions/{owner}/{name}:
    delete:
      description: Delete one session.
      operationId: delete_ai_signin-sessions_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a session
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one session by its (owner, name) key.
      operationId: get_ai_signin-sessions_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a session
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one session. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_signin-sessions_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a session
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_signin-sessions_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a session
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/signin-sessions/duplicated:
    get:
      operationId: get_ai_signin-sessions_duplicated
      summary: Duplicated (session)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/signout:
    post:
      operationId: post_ai_signout
      summary: Signout
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores:
    get:
      description: List the caller's stores.
      operationId: get_ai_stores
      summary: List stores
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one store.
      operationId: post_ai_stores
      summary: Create a store
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores/{owner}/{name}:
    delete:
      description: Delete one store.
      operationId: delete_ai_stores_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a store
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one store by its (owner, name) key.
      operationId: get_ai_stores_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a store
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one store. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_stores_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a store
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_stores_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a store
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores/{owner}/{name}/vectors:
    post:
      operationId: post_ai_stores_by_owner_by_name_vectors
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Vectors (store)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_stores_global
      summary: List stores across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores/names:
    get:
      operationId: get_ai_stores_names
      summary: Names (store)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/stores/providers:
    get:
      operationId: get_ai_stores_providers
      summary: Providers (store)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/system:
    get:
      operationId: get_ai_system
      summary: System
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tasks:
    get:
      description: List the caller's tasks.
      operationId: get_ai_tasks
      summary: List tasks
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one task.
      operationId: post_ai_tasks
      summary: Create a task
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tasks/{owner}/{name}:
    delete:
      description: Delete one task.
      operationId: delete_ai_tasks_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a task
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one task by its (owner, name) key.
      operationId: get_ai_tasks_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a task
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one task. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_tasks_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a task
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_tasks_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a task
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tasks/{owner}/{name}/analyze:
    post:
      operationId: post_ai_tasks_by_owner_by_name_analyze
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Analyze (task)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tasks/{owner}/{name}/document:
    post:
      operationId: post_ai_tasks_by_owner_by_name_document
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Document (task)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tasks/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_tasks_global
      summary: List tasks across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/templates:
    get:
      description: List the caller's templates.
      operationId: get_ai_templates
      summary: List templates
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one template.
      operationId: post_ai_templates
      summary: Create a template
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/templates/{owner}/{name}:
    delete:
      description: Delete one template.
      operationId: delete_ai_templates_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a template
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one template by its (owner, name) key.
      operationId: get_ai_templates_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a template
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one template. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_templates_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a template
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_templates_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a template
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/traffic/globe:
    get:
      description: |-
        Returns the PUBLIC live request-geo aggregate for the
        world.hanzo.ai "Hanzo mode" globe: WHERE requests to api.hanzo.ai are coming from,
        as country/region points with per-service-class counts, plus headline throughput
        rates.

        It is AUTH-exempt and BALANCE-exempt exactly like /v1/ai/router/stats?scope=platform:
          - auth: the controller name "traffic/globe" is neither a get-/update- CRUD name
            nor a super-admin/present-credential endpoint, so the authz filter passes it
            through, and this handler requires no principal.
          - balance: isBalanceExempt("/v1/ai/traffic/...") returns true.

        It exposes ONLY aggregates — counts, rates, and country/region centroids — and
        NEVER any IP, per-request row, org, or user dimension (see object/traffic.go).
        Marketing telemetry; nothing sensitive.
      operationId: get_ai_traffic_globe
      summary: 'Returns the PUBLIC live request-geo aggregate for the world.hanzo.ai
        "Hanzo mode" globe: WHERE requests to api.hanzo.ai are coming from, as country/region
        points with per-service-class counts, plus headline throughput rates.'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/training-contribution:
    get:
      operationId: get_ai_training-contribution
      summary: Training Contribution
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      operationId: patch_ai_training-contribution
      summary: Training Contribution
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      operationId: put_ai_training-contribution
      summary: Training Contribution
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tree-files:
    post:
      description: Create one tree-file.
      operationId: post_ai_tree-files
      summary: Create a tree-file
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/tree-files/{owner}/{name}:
    delete:
      description: Delete one tree-file.
      operationId: delete_ai_tree-files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a tree-file
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one tree-file. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_tree-files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a tree-file
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_tree-files_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a tree-file
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/usages:
    get:
      description: List the caller's usages.
      operationId: get_ai_usages
      summary: List usages
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/usages/by-user:
    get:
      operationId: get_ai_usages_by-user
      summary: By User (usage)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/usages/cloud:
    get:
      operationId: get_ai_usages_cloud
      summary: Cloud (usage)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/usages/range:
    get:
      operationId: get_ai_usages_range
      summary: Range (usage)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/usages/user-names:
    get:
      operationId: get_ai_usages_user-names
      summary: User Names (usage)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/vectors:
    get:
      description: List the caller's vectors.
      operationId: get_ai_vectors
      summary: List vectors
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one vector.
      operationId: post_ai_vectors
      summary: Create a vector
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/vectors/{owner}/{name}:
    delete:
      description: Delete one vector.
      operationId: delete_ai_vectors_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a vector
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one vector by its (owner, name) key.
      operationId: get_ai_vectors_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a vector
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one vector. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_vectors_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a vector
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_vectors_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a vector
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/vectors/all:
    delete:
      operationId: delete_ai_vectors_all
      summary: All (vector)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/vectors/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_vectors_global
      summary: List vectors across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/version:
    get:
      operationId: get_ai_version
      summary: Version
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/videos:
    get:
      description: List the caller's videos.
      operationId: get_ai_videos
      summary: List videos
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one video.
      operationId: post_ai_videos
      summary: Create a video
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/videos/{owner}/{name}:
    delete:
      description: Delete one video.
      operationId: delete_ai_videos_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a video
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one video by its (owner, name) key.
      operationId: get_ai_videos_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a video
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one video. PATCH and PUT reach the same handler, which has
        always taken a whole object.
      operationId: patch_ai_videos_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a video
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_videos_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a video
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/videos/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_videos_global
      summary: List videos across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/videos/upload:
    post:
      operationId: post_ai_videos_upload
      summary: Upload (video)
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/workflows:
    get:
      description: List the caller's workflows.
      operationId: get_ai_workflows
      summary: List workflows
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: Create one workflow.
      operationId: post_ai_workflows
      summary: Create a workflow
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/workflows/{owner}/{name}:
    delete:
      description: Delete one workflow.
      operationId: delete_ai_workflows_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Delete a workflow
      tags:
      - ai
      x-app: ai
      x-public: true
    get:
      description: Read one workflow by its (owner, name) key.
      operationId: get_ai_workflows_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Retrieve a workflow
      tags:
      - ai
      x-app: ai
      x-public: true
    patch:
      description: Update one workflow. PATCH and PUT reach the same handler, which
        has always taken a whole object.
      operationId: patch_ai_workflows_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Update a workflow
      tags:
      - ai
      x-app: ai
      x-public: true
    put:
      description: Identical to PATCH — the handler takes a whole object either way.
      operationId: put_ai_workflows_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Replace a workflow
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/ai/workflows/global:
    get:
      description: Cross-tenant listing. Admin-only; a tenant caller is refused.
      operationId: get_ai_workflows_global
      summary: List workflows across tenants
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/allowance:
    get:
      description: |-
        Answers what the CALLER has left of their plan's free-call allowance this period,
        and the instant the count starts again.

        This is the number a product shows beside the composer — "17 of 20 left today" —
        and the moment to offer a plan is when it reaches zero. It READS: asking does not
        spend, so a page that polls it costs the caller nothing.

        The subject is the caller's own, resolved from the verified credential, and can
        never be named in the request — so this is a mirror, not a lookup of someone else.
        An unauthenticated caller is refused: there is no allowance without someone to
        hold it.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: get_allowance
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Allowance'
          description: ok
      summary: Answers what the CALLER has left of their plan's free-call allowance
        this period, and the instant the count starts again.
      tags:
      - allowance
      x-app: allowance
      x-public: true
      x-tool: true
  /v1/analytics/health:
    get:
      description: |-
        Health reports whether the event plane can take a write and the warehouse can
        answer a read.

        It reports the analytics subsystem's own liveness in BOTH directions: plane is
        the event plane it WRITES (the bus and the JetStream stream every accepted
        event is published to, both named in the report), and datastore is the
        warehouse it READS, with each read lens's table reported as it is provisioned
        (the LLM usage ledger and the product-event table).

        EITHER ONE DOWN IS A 503, and the report says WHICH — they are probed
        independently and never collapse into a single bit. This endpoint used to
        report the read half only, and answered 200/ok while every POST /v1/event
        failed on a stream that could not bind: a total ingest outage behind a green
        probe. A readiness gate here now gates on the write path too.

        plane.ready IS A REAL PROBE and walks the ingest path itself — the same
        connection and the same stream a publish uses — so it cannot answer ready while
        a publish would 503. plane.reason carries the plane's own error text when it is
        false.

        datastore IS NOT PROBED WITH A QUERY. It is the state of the process's own
        shared client — established, and not since closed — so a warehouse accepting
        connections and failing reads still reports true. Degraded CARRIES the report
        (status, the failing half, reason) as its body rather than an error envelope,
        so a gate reads the cause off the same object it got at 200.

        A MISSING LENS TABLE IS NOT A FAILURE and never moves the status: a lens
        reported available:false answers honest-empty rather than erroring, so a fresh
        deployment whose collector has not emitted yet is legitimately 200 with the
        product-event lens unavailable. The lens block is reported whenever the
        warehouse is REACHABLE — including on a report degraded by the plane, where the
        tables genuinely were probed — and is absent only when the warehouse is not,
        having nothing to say about tables it could not reach.

        Unauthenticated on purpose — liveness has to be probe-able — and it reads NO
        tenant data: table existence and stream presence only, never a row and never an
        event.
      operationId: get_analytics_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/healthReport'
          description: ok
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/healthReport'
          description: service unavailable
      summary: Health reports whether the event plane can take a write and the warehouse
        can answer a read.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/analytics/overview:
    get:
      description: |-
        Overview returns the caller org's analytics KPIs for one time window. Three lenses
        over one warehouse: llm is the live per-org LLM usage ledger (requests, tokens,
        spend, models, providers, errors) and is always real; web (pageviews, visitors,
        sessions) and commerce (orders, revenue, AOV) read the product-event table and
        report available=false rather than fabricating zeros when it holds nothing yet.

        The org is the validated principal's — never a parameter — so a caller can only
        ever read its own tenant. 403 without a validated bearer, 400 on an unknown range,
        503 when the warehouse is unreachable.
      operationId: get_analytics_overview
      parameters:
      - description: |-
          Range is a relative window: a count and a unit — 24h, 7d, 90d, any <N>h or
          <N>d — or day, week, month, all. Default 24h. Ignored when both start and
          end are given. An unknown value, or one past the 730-day horizon, is a 400.
        example: 7d
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: Start is the inclusive lower bound of a custom window, RFC3339.
          Requires end.
        in: query
        name: start
        required: false
        schema:
          type: string
      - description: End is the exclusive upper bound of a custom window, RFC3339.
          Requires start.
        in: query
        name: end
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Overview'
          description: ok
      summary: Overview returns the caller org's analytics KPIs for one time window.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/analytics/timeseries:
    get:
      description: |-
        Timeseries returns the caller org's LLM usage over time as an evenly-spaced series.
        One point per hour or per day — the bucket the window implies, 24h giving hours and
        7d/30d giving days — carrying requests, total tokens and spend in cents. Empty
        buckets are filled with zeros so a client charts a continuous line.

        The org is the validated principal's — never a parameter. 403 without a validated
        bearer, 400 on an unknown range, 503 when the warehouse is unreachable.
      operationId: get_analytics_timeseries
      parameters:
      - description: |-
          Range is a relative window: a count and a unit — 24h, 7d, 90d, any <N>h or
          <N>d — or day, week, month, all. Default 24h. Ignored when both start and
          end are given. An unknown value, or one past the 730-day horizon, is a 400.
        example: 30d
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: Start is the inclusive lower bound of a custom window, RFC3339.
          Requires end.
        in: query
        name: start
        required: false
        schema:
          type: string
      - description: End is the exclusive upper bound of a custom window, RFC3339.
          Requires start.
        in: query
        name: end
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Timeseries'
          description: ok
      summary: Timeseries returns the caller org's LLM usage over time as an evenly-spaced
        series.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/analytics/top:
    get:
      description: |-
        Top returns the caller org's ranked lenses for one window, five of them at once.
        models ranks LLM models by spend and is always real; products ranks commerce orders
        by revenue; topPages ranks requested paths, topReferrers the external referrer
        domains ("(direct)" for a missing or same-origin one) and topSources the utm_source
        campaigns ("(none)" when absent), each by pageviews. Every lens carries each row's
        share of the in-window total, so a top-N honestly shows the long tail.

        The four event lenses report available=false rather than fabricating zeros when the
        product-event table holds nothing yet. The org is the validated principal's — never
        a parameter. 403 without a validated bearer, 400 on an unknown range, 503 when the
        warehouse is unreachable.
      operationId: get_analytics_top
      parameters:
      - description: |-
          Range is a relative window: a count and a unit — 24h, 7d, 90d, any <N>h or
          <N>d — or day, week, month, all. Default 24h. Ignored when both start and
          end are given. An unknown value, or one past the 730-day horizon, is a 400.
        example: 7d
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: Start is the inclusive lower bound of a custom window, RFC3339.
          Requires end.
        in: query
        name: start
        required: false
        schema:
          type: string
      - description: End is the exclusive upper bound of a custom window, RFC3339.
          Requires start.
        in: query
        name: end
        required: false
        schema:
          type: string
      - description: |-
          Limit bounds every ranked lens in the response. Default 10, maximum 100; a
          value at or below zero, or one that is not a number, takes the default.
        example: 25
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Top'
          description: ok
      summary: Top returns the caller org's ranked lenses for one window, five of
        them at once.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/ask:
    post:
      description: |-
        Answers a natural-language question about the CALLER'S OWN org, from real figures rather than from the model's memory.

        The question is classified to a grounded domain, that domain's read runs IN-PROCESS under the caller's own credentials, and only then is the result narrated. So the figures and their sources are the domain's, resolved before any model call and never altered by one — a wrong answer is a wrong query, never an invention.

        Domains: books (the org's ledger), projects (what is built and what of it is deployed), git (the org's repositories and what changed in them), and web (search, news, research, deep). A validated principal is required; the answer is scoped to that principal's org and nothing else.
      operationId: post_ask
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/askRequest'
      summary: Ask a grounded question about your own org
      tags:
      - ask
      x-app: ask
      x-public: true
  /v1/ask/web:
    post:
      description: |-
        Researches a question on the live web and answers it with its
        sources cited.

        This is the DEEP one. It plans the question into topics, runs several web
        searches, FETCHES AND READS the pages it finds, ranks them, and writes a
        grounded answer with inline markdown citations. Use it for anything that needs
        evidence, comparison or current fact — "what changed in X", "compare A and B",
        "is this claim true". For a plain list of links, use search_web instead; for
        one page you already have the URL of, use read_page.

        `mode` buys depth: `search` is a single fast pass, `news` biases to recency,
        `research` plans and iterates, `deep` surveys widest. `sources` narrows the
        evidence to `web`, `news`, `academic`, `github`, `reddit` or `x` — each becomes
        a site-scoped search, which is how this reaches X/Twitter posts.

        EVERY CITATION IS A PAGE THIS CALL FETCHED. That is a property of the text and
        not an instruction to the model: each source is fenced with a per-request nonce
        so a crawled page cannot print itself a source number, and every markdown link
        in the answer is checked against the gathered set before it is returned. So a
        link in `answer` always appears in `sources`, and a page that was not read
        cannot be cited.

        It is BOUNDED and it degrades rather than failing: a mode's rounds, wall clock
        and token ceiling all cap it, and a search that finds little or a page that
        will not load yields a thinner answer, never an error. A validated principal is
        required, and the answer is billed once to that principal's org.
      operationId: research_web
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webQuestion'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Report'
          description: ok
      summary: Research a question on the live web and answer it with sources cited
      tags:
      - ask
      x-app: ask
      x-public: true
      x-tool: true
  /v1/audio/foley:
    post:
      description: |-
        Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively. It resolves the SKU and, for a Zen
        model, forwards to zen's matching verb billed per call at the discovered price.
        These verbs are Zen-native; a non-Zen model is rejected.
      operationId: post_audio_foley
      summary: Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/audio/music:
    post:
      description: |-
        Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively. It resolves the SKU and, for a Zen
        model, forwards to zen's matching verb billed per call at the discovered price.
        These verbs are Zen-native; a non-Zen model is rejected.
      operationId: post_audio_music
      summary: Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/audio/speech:
    post:
      description: |-
        The OpenAI-compatible TTS endpoint (POST /v1/audio/speech). It
        authenticates the caller, resolves `model` to its TTS provider (the SAME model-route
        resolution the chat/images/video endpoints use — so a BYO node registered as a TTS
        provider works transparently), synthesizes the audio, and streams the bytes back.
        This is the ONE way to synthesize speech: OpenAI-shaped, with no store or
        message coupling, so a caller needs no chat to speak.
      operationId: post_audio_speech
      summary: The OpenAI-compatible TTS endpoint (POST /v1/audio/speech).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/audio/transcriptions:
    post:
      description: |-
        The OpenAI-compatible STT endpoint
        (POST /v1/audio/transcriptions, multipart: file + model [+ language +
        response_format]). It mirrors AudioSpeech exactly: authenticate the caller,
        resolve `model` to its STT provider through the SAME model-route resolution
        (so the in-cluster speech service — or any BYO node registered as an STT
        provider — works transparently), transcribe, and return the OpenAI body.
        This is the ONE way to transcribe: OpenAI-shaped, with no store coupling, so a
        caller needs no chat to be heard.
      operationId: post_audio_transcriptions
      summary: 'The OpenAI-compatible STT endpoint (POST /v1/audio/transcriptions,
        multipart: file + model [+ language + response_format]).'
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/audio/voice:
    post:
      description: |-
        Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively. It resolves the SKU and, for a Zen
        model, forwards to zen's matching verb billed per call at the discovered price.
        These verbs are Zen-native; a non-Zen model is rejected.
      operationId: post_audio_voice
      summary: Serves the generative audio verbs — /v1/audio/voice (TTS), /music,
        /foley — that the Zen family serves natively.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/audit:
    get:
      description: |-
        List reads the caller's OWN org audit trail, newest first, with the total the
        filter matched so a console can page it.

        Every filter is optional and applies WITHIN the caller's org — the org itself is
        the validated principal's and can never be widened by a request. Fails closed:
        an absent principal is a true "not signed in" (401), and a deployment with no
        local tamper-evident store answers an honest 501 rather than silently serving
        somebody else's trail.
      operationId: get_audit
      parameters:
      - description: |-
          Sub narrows the trail to one actor — the validated subject that made the
          request. Blank means every actor in the org.
        in: query
        name: sub
        required: false
        schema:
          type: string
      - description: Action narrows it to one action name, e.g. "machine.create".
        example: machine.create
        in: query
        name: action
        required: false
        schema:
          type: string
      - description: Resource narrows it to one resource TYPE, e.g. "apikey".
        in: query
        name: resource
        required: false
        schema:
          type: string
      - description: ResourceID narrows it to one resource instance.
        in: query
        name: resourceId
        required: false
        schema:
          type: string
      - description: 'Result narrows it to one outcome: "success", "deny" or "error".'
        example: success
        in: query
        name: result
        required: false
        schema:
          type: string
      - description: |-
          Since is the inclusive lower time bound, RFC3339. An unparseable value is
          ignored rather than refused — one malformed filter must not hide the trail.
        example: "2026-07-01T00:00:00Z"
        in: query
        name: since
        required: false
        schema:
          type: string
      - description: Until is the upper time bound, RFC3339, with the same tolerance.
        in: query
        name: until
        required: false
        schema:
          type: string
      - description: |-
          PageSize is rows per page, default 100. A value that is not a positive
          integer falls back to the default.
        example: "50"
        in: query
        name: pageSize
        required: false
        schema:
          type: string
      - description: |-
          Page is the 1-based page number, driving the offset. Anything below 2 reads
          the first page.
        in: query
        name: p
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/trailPage'
          description: ok
      summary: List reads the caller's OWN org audit trail, newest first, with the
        total the filter matched so a console can page it.
      tags:
      - audit
      x-app: audit
      x-public: true
      x-tool: true
  /v1/authz/check:
    post:
      description: |-
        Answers one policy question — may this subject take this action on this object — against the CALLER'S OWN org policy set, and answers it with a bare allow/deny.

        The org comes from the gateway-minted X-Org-Id and picks the per-org enforcer, so a decision is always rendered by that tenant's policies and never by another's. A request carrying no org is refused rather than answered from a shared or default set: collapsing tenants together is the one failure a policy engine must not have.

        Body: {sub, obj, act}, all three required. The reply echoes them beside `allow` so a cached or logged decision carries the question it answered.
      operationId: post_authz_check
      summary: Ask whether a subject may act on an object
      tags:
      - authz
      x-app: authz
      x-public: true
  /v1/authz/health:
    get:
      description: 'Reports that the authz process is up. Unauthenticated by design
        and never org-scoped: it answers while every tenant''s enforcer is still cold,
        because a probe that needed a tenant would fail for reasons that have nothing
        to do with the process being alive.'
      operationId: get_authz_health
      summary: Liveness of the policy engine
      tags:
      - authz
      x-app: authz
      x-public: true
  /v1/authz/readyz:
    get:
      description: 'Reports that the authz process is ready to serve decisions. Unauthenticated
        and not org-scoped, for the same reason health is: readiness is a property
        of this process, not of any one tenant''s policy set.'
      operationId: get_authz_readyz
      summary: Readiness of the policy engine
      tags:
      - authz
      x-app: authz
      x-public: true
  /v1/auto/connectors:
    get:
      description: |-
        Connectors returns the connector catalogue. Each entry is an external service a
        flow step can invoke, carrying its auth descriptor and the input properties of its
        actions and triggers. The catalogue is the same for every tenant, so the gate is a
        validated principal rather than a per-org view.
      operationId: get_auto_connectors
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Catalog'
          description: ok
      summary: Connectors returns the connector catalogue.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows:
    get:
      description: |-
        Returns the caller org's automations, most-recently-updated first. The
        optional `limit` query bounds the page.
      operationId: get_auto_flows
      parameters:
      - description: Limit bounds the page (default 200, maximum 1000).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/flowPage'
          description: ok
      summary: Returns the caller org's automations, most-recently-updated first.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates an automation and its initial DRAFT version in one call. The
        new flow is DISABLED — creating it does not arm its trigger; POST
        /v1/automations/flows/{id}/enable does that.
      operationId: post_auto_flows
      requestBody:
        content:
          application/json:
            example:
              displayName: Nightly Sync
              trigger:
                displayName: Start
                name: trigger
                settings:
                  pieceName: core
                  triggerName: manual
                strategy: MANUAL
                type: PIECE_TRIGGER
            schema:
              $ref: '#/components/schemas/createFlowReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/populatedFlow'
          description: created
      summary: Creates an automation and its initial DRAFT version in one call.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows/{id}:
    delete:
      description: |-
        Deletes one automation, its versions and its run history. It answers
        no content, and a flow of another org answers not-found.
      operationId: delete_auto_flows_by_id
      parameters:
      - description: ID is the flow to act on, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one automation, its versions and its run history.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one automation and its latest version. That is the flow record
        plus the step tree the builder edits; a flow of another org answers not-found.
      operationId: get_auto_flows_by_id
      parameters:
      - description: ID is the flow to act on, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/populatedFlow'
          description: ok
      summary: Returns one automation and its latest version.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
    patch:
      description: |-
        Updates one automation's metadata in place. Every field is optional; a
        field the request omits is left alone. Publishing a version pins which one runs,
        and is refused unless that version belongs to this flow.
      operationId: patch_auto_flows_by_id
      parameters:
      - description: ID is the flow to update, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              folderId: ops
              id: flow_1
            schema:
              $ref: '#/components/schemas/patchFlowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Flow'
          description: ok
      summary: Updates one automation's metadata in place.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows/{id}/disable:
    post:
      description: |-
        Disarms a flow's trigger and marks it DISABLED. Its schedule and its
        event subscriptions are dropped, so a disabled flow is never a live target; runs
        already in flight are unaffected, and it can still be started on demand.
      operationId: post_auto_flows_by_id_disable
      parameters:
      - description: ID is the flow to act on, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Flow'
          description: ok
      summary: Disarms a flow's trigger and marks it DISABLED.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows/{id}/enable:
    post:
      description: |-
        Arms a flow's trigger and marks it ENABLED. A POLLING trigger gets a
        cron schedule on the durable engine; a WEBHOOK trigger gets a subscription in the
        routing index, so an inbound event starts it; a MANUAL trigger arms nothing and
        still runs on demand.
      operationId: post_auto_flows_by_id_enable
      parameters:
      - description: ID is the flow to act on, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Flow'
          description: ok
      summary: Arms a flow's trigger and marks it ENABLED.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows/{id}/operations:
    post:
      description: |-
        Applies ONE flow operation and answers the thing it changed. The operation is named by `type`, with its arguments under `request`: `CHANGE_NAME`, `UPDATE_TRIGGER`, `ADD_ACTION`, `UPDATE_ACTION`, `MOVE_ACTION`, `DELETE_ACTION` edit the flow's LATEST version and answer with that version, and `CHANGE_STATUS` instead enables or disables the flow and answers with the FLOW. Two response shapes on one address is the rule a reader would otherwise get wrong, and it is why this route is not a typed op.

        Edits land on the latest version only — the published version a run executes is untouched until it is republished — and the whole resulting step tree is re-validated against the step-count and size caps after every operation, so a long sequence of `ADD_ACTION` calls cannot grow a flow past a bound one step at a time (422 when it would). Org-scoped and fails closed: a validated principal is required (403 without one), the flow and its version are read under the caller's OWN org so another tenant's id is a 404, and an operation whose `request` does not decode is a 400.
      operationId: post_auto_flows_by_id_operations
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Edit a flow — rename it, retarget its trigger, or add, move and delete
        steps
      tags:
      - automations
      x-app: automations
      x-public: true
  /v1/auto/flows/{id}/run:
    post:
      description: |-
        Starts one durable run of a flow now. It runs the flow's published
        version if one is pinned, else its latest, and answers the run record it created.
        The run is bounded by the org's per-minute run-start budget and its in-flight
        concurrency ceiling; over either, or with the engine not ready, no run is started
        and no run id is burned.
      operationId: post_auto_flows_by_id_run
      parameters:
      - description: ID is the flow to act on, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowRun'
          description: created
      summary: Starts one durable run of a flow now.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/flows/{id}/versions:
    get:
      description: |-
        Returns one flow's versions, newest first. The optional `limit`
        query bounds the page.
      operationId: get_auto_flows_by_id_versions
      parameters:
      - description: ID is the flow whose versions to list, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Limit bounds the page (default 200, maximum 1000).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/versionPage'
          description: ok
      summary: Returns one flow's versions, newest first.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds a new DRAFT version to a flow. The version is created invalid
        unless it carries a trigger, and it does not become the running version until it
        is published (PATCH the flow's publishedVersionId) or becomes the latest.
      operationId: post_auto_flows_by_id_versions
      parameters:
      - description: ID is the flow to add a version to, from the path.
        example: flow_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              displayName: v2
              id: flow_1
            schema:
              $ref: '#/components/schemas/createVersionIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowVersion'
          description: created
      summary: Adds a new DRAFT version to a flow.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/hooks/{source}/{event}:
    post:
      description: |-
        Delivers one event to the org's automation triggers and answers `{matched:n}` — how many enabled flows had a webhook trigger on this `(source, event)` key and were started by it. A zero match is a success, not an error: nothing was subscribed.

        The path is the trigger key and the JSON object body is the event payload, threaded into each started run as `{{trigger.*}}` with all of its keys intact — which is why this is not a typed op, since a declared input struct would silently DISCARD every payload key it had no field for. Re-delivery is a no-op: an `X-Idempotency-Key` header dedupes, and with none the body is content-hashed instead, so a hammer of identical posts collapses to ONE run rather than minting a fresh one per post. An in-platform producer may propagate `X-Causation-Depth` so a firing that a flow caused is bounded against a loop; an absent or invalid header reads as depth 0, an external origin.

        Authenticated and org-scoped, unlike a provider's public webhook URL: a validated principal is required (403 without one) and the org is that principal's, never the body's, so a producer can only fire into its own tenant's flows. Both path segments are required (400) and a payload over the size limit is a 413.
      operationId: post_auto_hooks_by_source_by_event
      parameters:
      - in: path
        name: source
        required: true
        schema:
          type: string
      - in: path
        name: event
        required: true
        schema:
          type: string
      summary: Fire an event that starts every enabled flow subscribed to it
      tags:
      - automations
      x-app: automations
      x-public: true
  /v1/auto/pieces:
    get:
      description: |-
        Pieces is the retired-name alias of the connector catalogue. It serves exactly
        what GET /v1/automations/connectors serves, under the name this surface used
        before "piece" (the ActivePieces term) became "connector", and stays valid for
        clients pinned to the old path. Prefer /connectors.
      operationId: get_auto_pieces
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Catalog'
          description: ok
      summary: Pieces is the retired-name alias of the connector catalogue.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/runs:
    get:
      description: |-
        Returns the caller org's run history, newest first. The optional
        `flowId` query narrows it to one flow and `limit` bounds the page.
      operationId: get_auto_runs
      parameters:
      - description: FlowID narrows the history to one flow. Omit it for the whole
          org's runs.
        in: query
        name: flowId
        required: false
        schema:
          type: string
      - description: Limit bounds the page (default 200, maximum 1000).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runPage'
          description: ok
      summary: Returns the caller org's run history, newest first.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/runs/{id}:
    get:
      description: |-
        Returns one run. A run that has not reached a terminal status is refreshed
        from the durable engine first — scoped to the org's own namespace — so the caller
        sees live progress rather than the last status that happened to be persisted.
      operationId: get_auto_runs_by_id
      parameters:
      - description: ID is the run to read, from the path.
        example: run_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FlowRun'
          description: ok
      summary: Returns one run.
      tags:
      - automations
      x-app: automations
      x-public: true
      x-tool: true
  /v1/auto/runs/{id}/resume:
    post:
      description: |-
        Delivers the durable `resume` signal to a run parked on a `wait_for_approval` waitpoint and answers `{resumed:true}` once the engine has taken it.

        The body is an ARBITRARY JSON value — object, array, string, number — delivered VERBATIM into the workflow as that waitpoint's output, so it is what the steps after the approval read as their input. An empty body resumes with no payload. That open shape is why this route is not a typed op: an operation's input can carry the payload or the run address, never both.

        Org-scoped and fails closed: a validated principal is required (403 without one), the run is read under the caller's OWN org so another tenant's run id is a 404, a body that is not JSON is a 400, and a payload over the size limit is a 413 — it becomes durable engine state, so it is bounded here rather than after it lands. The resume is audited as `automations.run.resume`.
      operationId: post_auto_runs_by_id_resume
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Release a run waiting at an approval step, with the approval payload
      tags:
      - automations
      x-app: automations
      x-public: true
  /v1/base/health:
    get:
      description: |-
        Reports that the base subsystem is serving.

        It is deliberately INDEPENDENT of whether this deployment actually embeds the
        Base engine: the route answers before the CLOUD_BASE_EMBED gate and before the
        /v1/base/* wildcard, so a liveness probe measures the process rather than an
        optional feature, and the wildcard can never shadow it. It reads no tenant, so a
        prober that sends no principal is answered rather than refused.
      operationId: get_base_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/baseHealth'
          description: ok
      summary: Reports that the base subsystem is serving.
      tags:
      - base
      x-app: base
      x-public: true
      x-tool: true
  /v1/billing/accounts:
    get:
      description: |-
        Returns the billing accounts visible to the caller. One organisation is exactly one billing account here, so an authenticated caller sees precisely one: their own. The list shape is the honest one — it is what a caller with access to several would receive — rather than a promise that more will ever appear for a token scoped to a single org.

        The account is derived from the validated org claim and from nothing the caller sends, so there is no account parameter and a cross-tenant read is not expressible. An unauthenticated call is 401.
      operationId: get_billing_accounts
      summary: The billing account you are signed in to
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/accounts/{id}/members:
    get:
      description: |-
        Returns the members of one billing account. The id must be the caller's OWN account — the handler compares it against the org resolved from the token and answers 403 when they differ, which is what guards this route: unlike its siblings it carries no subject key for the pin to overwrite, so it checks the path segment itself.

        The roster it can answer is currently the requesting user alone. Membership lives in IAM, not in the ledger, and this operation reports what commerce actually holds rather than inventing a roster from a source it does not read. An unauthenticated call is 401.
      operationId: get_billing_accounts_by_id_members
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Who is on a billing account
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/alerts:
    get:
      description: Returns the caps and alerts keyed to the caller's own billing subject,
        each with its threshold, enforcement flag, soft-warning percentage and current
        period spend. Any authenticated member of the org may read them — only the
        writes require an admin. The rows are keyed on the org subject the enforcement
        gate itself reads, which is why a cap created here is the one that actually
        binds. A caller with no resolvable org or subject gets an empty list, never
        another tenant's caps.
      operationId: get_billing_alerts
      summary: List your org's spend caps and rate limits
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'Creates a cap for the caller''s own org and answers the stored
        row with its current period spend. A spend cap is a FINANCIAL SAFETY control,
        so writing one requires an ORG ADMIN, a platform admin, or the internal service
        token — a plain authenticated member is refused 403, because a member who
        could delete the cap could uncap the org''s spend and a member who could set
        a one-cent enforcing cap could deny the whole org. The cap is always keyed
        to the caller''s own billing subject: a userId in the body is overwritten,
        never honored, so a cap cannot be planted on another subject. At least one
        of a positive threshold or a positive rateLimitRpm is required, softPct must
        be within 0 to 100, and an org that has reached its row limit is refused 400.'
      operationId: post_billing_alerts
      summary: Set a spend cap or rate limit on your org
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/alerts/{id}:
    delete:
      description: Deletes the addressed cap and answers 204. Requires an ORG ADMIN,
        a platform admin, or the internal service token — deleting a cap uncaps the
        org's spend, so a plain member is refused 403. Ownership is checked per row
        and a cap the caller does not own is refused as 404 rather than 403, so the
        response cannot confirm that another org's id exists.
      operationId: delete_billing_alerts_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Remove one of your org's spend caps
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'Applies only the fields the body actually carries — title, threshold,
        project, service, enforce, softPct, rateLimitRpm — and leaves the rest as
        stored, answering the merged row with its current period spend. Requires an
        ORG ADMIN, a platform admin, or the internal service token, for the same reason
        creation does: a member who could edit the cap could raise it to nothing or
        drop it to a punitive floor. Ownership is checked per row and a cap the caller
        does not own is refused as 404, never 403, so the id space cannot be probed.'
      operationId: patch_billing_alerts_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Change one of your org's spend caps
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/alerts/authorize:
    get:
      description: 'Answers allow, reason, capCents, spentCents and warnPct for a
        proposed amount against a (project, service) scope — the verdict the request-edge
        metering gate reads before admitting a call. It evaluates EVERY covering cap
        and the most restrictive enforcing one wins; soft caps and an enforcing project
        cap whose project axis is not validated never block, they only raise the warning
        utilization. It is a service-to-service read authenticated by the internal
        service token with the org pinned by the gateway, not a browser call. Two
        rules matter: the spend it scores comes from the finance ledger''s current-month
        total, and it FAILS OPEN on unknown spend — a transient read failure allows
        rather than denies, so a backend blip never bills-blocks an under-cap customer,
        while a known overage still denies.'
      operationId: get_billing_alerts_authorize
      summary: The per-request spend-cap verdict the metering gate consumes
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/balance:
    get:
      description: |-
        Answers the spendable prepaid balance of the wallet this caller bills from — the same wallet the AI prepaid gate reads before admitting a paid request, the edge meter debits, and a top-up credits.

        The wallet is an ADDRESS, not an org: `account` echoes the key resolved within the ledger — the org's shared pool for a tenant org, a personal account for a member of the shared signup org. The echo is the point. A browser could only GUESS its own payer by decoding its own token, and a guess that disagrees with the server is how money lands in an account the gate never reads.

        `balance`, `holds` and `available` are whole USD cents, ROUNDED from the ledger's exact 18-decimal value. On the co-resident ledger `holds` is 0 and `available` equals `balance`: the gate's reservations live in its own pod and are never posted, so the settled balance IS the spendable one.

        The ledger is the caller's own org, taken from the VALIDATED IAM owner claim and never from a client header. No validated principal is 401 — with one exception, the trusted in-process service token the AI gate itself presents, which reads the gateway-pinned org and nothing it could name. A balance that cannot be READ is 502, never 0: unknown is not broke.
      operationId: get_billing_balance
      summary: Prepaid credit the caller's org can still spend
      tags:
      - billing
      x-app: billing
      x-public: true
  /v1/billing/credit-balance:
    get:
      description: |-
        Returns the total credit still available to the caller's own subject — the sum of what the grants have left, which is the figure the console shows above the usage meter. It is the balance a metered act draws down, so it answers the one question a customer asks before spending: how much is there.

        Like every read in this family the subject is pinned to the caller before the handler runs, so the userId parameter the handler reads can never name another tenant. For the grants BEHIND this number — each with its original amount and its expiry — read /v1/billing/credits. A subject with no credit is zero, which is an answer and not an error.
      operationId: get_billing_credit-balance
      summary: What is left of your credit, as one number
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/credit-balance/breakdown:
    get:
      description: |-
        Returns the same balance /v1/billing/credit-balance reports, split by the tag each grant carries, so a reader can tell trial credit from bought credit and show the earliest expiry within each group. A console needs the split to say what will lapse and when; the single number cannot.

        The subject is pinned to the caller before the handler runs, exactly as in the sibling reads, so the userId parameter can never name another tenant. A subject with no grants is an empty breakdown and a zero total, which is an answer and not an error.
      operationId: get_billing_credit-balance_breakdown
      summary: What is left of your credit, grouped by where it came from
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/credits:
    get:
      description: 'Returns the caller org''s credit grants — each with its original
        amount, what remains and when it expires — so a customer can see what was
        given and what is left before metered spend draws it down. It is a READ of
        the caller''s own subject, pinned before the handler runs, so a grant belonging
        to another tenant is simply absent. Granting credit is not this route and
        never has been: minting lands on the mint-gated POST /v1/billing/credit, which
        no browser can reach. Reading an empty balance is an empty array, not an error.'
      operationId: get_billing_credits
      summary: List the credit grants on your org's balance
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/crypto/deposit:
    post:
      description: |-
        Mints a deposit address held by the MPC signer fleet — no single party holds the key — on the chain and token you name, and returns it with the intent that tracks it.

        The account credited is the PINNED caller's, never a value in the body, so a deposit cannot be aimed at someone else's balance. A caller who already has an open intent gets that same address back rather than a new one, so reloading the page cannot spray keygens across the signer fleet.

        NO BALANCE MOVES HERE. This hands out an address; the chain watcher credits the account when a real transfer confirms, which is also why an address handed out and never funded costs nothing and expires nothing.
      operationId: post_billing_crypto_deposit
      summary: Get a deposit address for a crypto top-up
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/crypto/deposit/{id}:
    get:
      description: |-
        Answers the addressed deposit intent's current state — pending until a transfer is seen, confirming while the chain buries it, succeeded once it is credited — so a payment page can poll one deposit rather than the whole balance.

        Scoped to the caller: an intent belonging to another payer is not found and answers 404, never another account's state. The credit itself is the chain watcher's to make; this read reports it and never performs it.
      operationId: get_billing_crypto_deposit_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Follow one crypto deposit to settlement
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/crypto/options:
    get:
      description: |-
        Answers the custody processor's LIVE capability list — the chains and the tokens on each that this deployment can actually take a deposit on. A payment page renders its asset picker straight from it rather than from a list of its own, so a chain the processor stops supporting disappears from the picker instead of minting an address nothing watches.

        It is a capability read, not an account read: it says what may be paid with, never anything about this caller's balance or deposits.
      operationId: get_billing_crypto_options
      summary: Which chains and tokens a crypto top-up can use
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/invoices:
    get:
      description: Returns the caller org's invoices with a count, read from that
        org's own namespaced store, narrowable by userId, status or subscriptionId.
        The org is the one the gateway validated and the caller's billing subject
        is pinned into the query before the handler runs, so a read can never widen
        past the caller. A request that carries no resolvable org gets an honest empty
        list rather than an error or another tenant's rows.
      operationId: get_billing_invoices
      summary: List your org's billing invoices
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: |-
        Raises a DRAFT invoice against a customer in the caller's own org.

        The invoice is not collectible yet: a draft exists so it can be read and
        corrected, and issueInvoice is the separate act that turns it into a demand for
        payment. The subtotal and amount due are computed from the lines, so there is
        no total to send and none to get wrong.

        The billing org is the caller's, taken from the validated principal, so an
        invoice can only ever be raised on the caller's own books.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: raiseInvoice
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RaiseInvoiceIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceOut'
          description: created
      summary: Raise a draft invoice against a customer
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/billing/invoices/{id}:
    get:
      description: |-
        Reads one invoice out of the caller's org.

        The org scopes the read by construction — the store is namespaced to it — so an
        id belonging to another tenant is not found rather than found and then filtered.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: getInvoice
      parameters:
      - description: ID is the invoice id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceOut'
          description: ok
      summary: Read one invoice
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/billing/invoices/{id}/collect:
    post:
      description: |-
        Collects an issued invoice: credit grants first, then prepaid balance, then the
        card on file — the same waterfall the dunning workflow runs.

        A DECLINE IS NOT AN ERROR. It answers with paid=false, a reason, and the
        invoice still open, because a declined collection is a normal business outcome
        that must remain retryable — and because sealing it as a failure would wedge
        dunning behind a replayed decline. Only a successful collection is sealed, so a
        retry of a paid invoice replays the receipt instead of charging again.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: collectInvoice
      parameters:
      - description: ID is the invoice id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CollectOut'
          description: ok
      summary: Collect an issued invoice from credits, balance, then card
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/billing/invoices/{id}/issue:
    post:
      description: |-
        Issues a draft invoice: moves it to OPEN, assigns its number, and makes it
        collectible.

        Only a draft can be issued. An invoice already open, paid or void is refused
        with the state machine's own reason rather than being silently re-issued, which
        would mint a second number for one debt.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: issueInvoice
      parameters:
      - description: ID is the invoice id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceOut'
          description: ok
      summary: Issue a draft invoice, making it collectible
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/billing/invoices/{id}/pdf:
    get:
      description: Renders the addressed invoice as a single-page PDF and answers
        it as an attachment named after the invoice number. The render is a pure function
        of the invoice — no timestamps, no random ids — so the same invoice always
        produces identical bytes and a re-download is stable. The invoice is resolved
        inside the caller org's own namespace, so an id belonging to another tenant
        is simply absent and reads as 404; a caller with no validated org gets 401
        rather than a document.
      operationId: get_billing_invoices_by_id_pdf
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Download one invoice as a PDF attachment
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/invoices/{id}/void:
    post:
      description: |-
        Voids a draft or issued invoice — the cancel.

        A paid invoice cannot be voided: money has moved, and the correction for that
        is a refund, not an erasure. The state machine refuses it and that refusal is
        the answer.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: voidInvoice
      parameters:
      - description: ID is the invoice id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/InvoiceOut'
          description: ok
      summary: Void a draft or issued invoice
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/billing/ledger:
    get:
      description: |-
        Answers the org's own postings inside `range=`, each as a signed
        entry: a DEPOSIT CREDITS the wallet (positive, account `credits:<org>`) and
        every other posting DEBITS it (negative, account `usage:<org>`), described by
        its notes or its tags. The sign is the posting's own meaning, read through ONE
        vocabulary shared with the ledger that wrote it — a reader with its own
        spelling for `deposit` rendered a customer's grant as a charge.

        This is the closest projection of the truth. The org's double-entry postings
        are the source of record — balanced, only ever appended, one file per org —
        and this lane is that list, wider than either half of it: the deposits are the
        grants /v1/billing/credits lists and the debits are the spend /v1/billing/usage
        rolls up. It answers 503 where this deployment runs no ledger, rather than
        reporting an empty wallet.

        A row whose timestamp will not parse is KEPT rather than dropped — a malformed
        date must show up in a money list, not vanish from it. `balanceCents` is
        omitted: these are MOVEMENTS, and the standing balance is /v1/billing/balance.

        Cents are ROUNDED from the ledger's exact 18-decimal USD. Scoped to the
        caller's own org, where the org's ledger file is the tenant boundary; 401
        without a validated principal.
      operationId: get_billing_ledger
      parameters:
      - description: |-
          Range is the window: 24h, 7d, 30d or 90d. Anything else — including
          absent — is 30d, so a typo silently widens the window to a month rather
          than failing.
        example: 30d
        in: query
        name: range
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/financeLedgerEntry'
                type: array
          description: ok
          headers:
            Cache-Control:
              description: Set by GET /v1/billing/ledger.
              schema:
                type: string
      summary: 'Answers the org''s own postings inside `range=`, each as a signed
        entry: a DEPOSIT CREDITS the wallet (positive, account `credits:<org>`) and
        every other posting DEBITS it (negative, account `usage:<org>`), described
        by its notes or its tags.'
      tags:
      - billing
      x-app: billing
      x-public: true
      x-tool: true
  /v1/billing/methods:
    get:
      description: |-
        Answers the cards saved against your own account as masked descriptors: brand, last four, expiry and the processor's reusable reference. No card number and no security code exist here to return; both live at the processor and never enter this system. It is what a checkout prefills its payment step from.

        The customer face of the list a service token reads at /v1/billing/portal/methods — same rows, different principal, no hop between them.

        The subject filter is pinned to the VALIDATED caller before the handler runs, so the answer is your own account's cards whatever customerId the request carries, and another org's rows are outside the namespace entirely. A caller who is not signed in is refused before the read.
      operationId: get_billing_methods
      summary: Your saved cards, masked — the customer read
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: |-
        Vaults the card the processor already holds — you send its one-time reference, never a card number — as a reusable card on file, and stores the billing address with it. That vaulted card is what a subscription renewal or an auto-recharge charges later, which is why saving one is the step that makes a monthly plan billable at all.

        It charges nothing. Saving a card moves no money; the first charge is whatever arrangement you then attach it to.

        The subject is pinned from the validated caller and OVERWRITES the customerId in the body while leaving the card fields untouched, so a card can only ever be attached to the caller's OWN account whatever the body claims. That pin is the whole control on this write, not decoration: this is the one handler in the family that reads its subject from the body.
      operationId: post_billing_methods
      summary: Save a card for later charges
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/methods/{id}:
    delete:
      description: |-
        Detaches the addressed card: the stored reference is removed here AND withdrawn from the processor's vault, so nothing is left that a later charge could bill.

        The customer twin of DELETE /v1/billing/portal/methods/{id}. The id is resolved INSIDE your own org namespace, so a card that is not yours is simply not found there and answers 404 — never 403, which would confirm the id exists.

        Removing the card an auto-recharge or a running lease bills leaves that arrangement with nothing to charge; that is yours to decide.
      operationId: delete_billing_methods_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Remove one of your saved cards
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/mode:
    post:
      description: 'Flips the org''s live flag, which is the single authority for
        both the payment environment and the ledger bucket its transactions land in.
        This is a money-MINT control, not a customer action: it is gated on the internal
        service token AND platform scope, so an ORG ADMIN CANNOT move their own org
        — otherwise a tenant could drop itself into sandbox and stop paying. The rule
        most callers get wrong is the default: an org that has never been flipped
        transacts in SANDBOX, which is why a production-credentialled deployment can
        still hand a buyer a sandbox card form. When the deployment pins the payment
        environment explicitly, that pin governs and this flag only marks the transactions.'
      operationId: post_billing_mode
      summary: Move an org between sandbox and live billing
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/payouts:
    get:
      description: Returns the caller org's payout records ordered by creation time
        descending, read from that org's own namespaced store. The org is the gateway-validated
        one and the caller's billing subject is pinned before the handler runs, so
        the list is the caller's own and cannot be widened. A request with no resolvable
        org gets an empty array rather than an error.
      operationId: get_billing_payouts
      summary: List your org's payouts, newest first
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/plans:
    get:
      description: Returns every subscription tier a buyer can choose, each carrying
        the platform promo currently in effect, optionally narrowed with the category
        query. Prices come from the admin-editable plan authority in the database;
        the embedded catalog is only a loud-failing fallback, so a failed seed or
        a query error serves the known plans rather than a silently blank list. It
        is a catalog read, not an entitlement read — it says what may be bought, never
        what this caller has.
      operationId: get_billing_plans
      summary: The public plan catalog, annotated with the active platform promotion
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/portal/methods:
    get:
      description: |-
        Answers the org's saved payment methods as masked descriptors: brand, last four, expiry and the processor's reusable reference. No card number and no security code exist here to return; both live at the processor and never enter this system.

        This is the SERVICE-TOKEN face of the same list a customer reads at /v1/billing/methods. Both are served here, in this process, and answer the same rows; they are two addresses because they admit two different principals, not because either forwards to the other.

        The customer filter is pinned to the VALIDATED caller before the handler runs, so a browser sees only its own subject's cards whatever customerId it sends; only a caller holding the internal service token may name the subject, and the org it may name it within is fixed by the gateway. Cross-tenant is closed by the org namespace for both, so an id or a subject from another org resolves to nothing. A caller who is neither is refused before the read.
      operationId: get_billing_portal_methods
      summary: Cards saved against the caller's org, masked — the portal read
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: |-
        The service-token twin of POST /v1/billing/methods: it vaults the processor's one-time reference as a reusable card on file for the named subject, with its billing address, and moves no money doing it.

        It exists so an internal caller can complete the family it can already read and detach. The subject it may name is pinned to the org the gateway fixed, so the service token acts WITHIN one tenant and never across tenants; a caller holding no service token is refused before the write.
      operationId: post_billing_portal_methods
      summary: Save a card on a subject's behalf — the portal attach
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/portal/methods/{id}:
    delete:
      description: |-
        Detaches the addressed card: the stored reference is removed here AND withdrawn from the processor's vault, so nothing is left that a later charge could bill.

        The service-token twin of the customer's DELETE /v1/billing/methods/{id}, at its own address for the same reason the portal list is — a different principal, on the same rows, in this same process.

        The id is resolved INSIDE the caller's org namespace, so another tenant's card is not found there and answers 404 — never 403, which would confirm the id exists. That bound holds for the service token too: it may act for any subject within the org the gateway pinned, and for no subject outside it.

        Removing the card an auto-recharge or a running lease bills leaves that arrangement with nothing to charge; that is the customer's call to make.
      operationId: delete_billing_portal_methods_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Remove a saved card — the portal detach
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/recharge/run-all:
    post:
      description: 'Walks every organization and, for those that enabled auto-recharge
        and whose available balance (balance minus holds) has fallen under their configured
        threshold, charges their default payment method off-session and credits the
        balance, answering a per-org result row for each one it touched. This is the
        platform cron''s door, not a customer''s: it is gated on the internal service
        token AND platform scope, so an org admin cannot run the fleet-wide sweep.
        An org above its threshold is skipped silently; an org with no default payment
        method is reported as an uncharged row with the reason rather than failing
        the whole run.'
      operationId: post_billing_recharge_run-all
      summary: 'Platform sweep: top up every org whose balance has fallen below its
        own threshold'
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/settings:
    get:
      description: 'Answers the Square application id, location id, environment and
        live flag the browser''s card iframe boots against — public values only, never
        a secret. Resolution lives in one place shared with the public tenant projection,
        so the card form can never initialize against a different Square application
        than the one commerce will actually charge. It deliberately does NOT hydrate
        credentials from KMS: the dialog blocks on this call, so it answers from the
        org and the deployment environment without a round trip, and an org with no
        per-org credentials gets the deployment''s own public app id.'
      operationId: get_billing_settings
      summary: The public payment-provider config your card form needs to initialize
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/subscribe/card:
    post:
      description: 'Vaults the tokenized card as a reusable card-on-file, charges
        the first period, and creates the subscription — answering the subscription
        and invoice ids with the amount charged. The price is SERVER-AUTHORITATIVE:
        it is the plan''s catalog price times billable seats and a client-supplied
        amount is never consulted, so a scripted request cannot underpay; a per-seat
        plan below its minimum seats is refused, and a free plan is refused outright
        because this address is the paid path. The card PAN never reaches this service
        — the browser tokenizes it and only the single-use nonce arrives here. The
        subject is the caller''s own org, with an in-org user honored only inside
        that bound, and an idempotency key (or, absent one, the nonce itself) makes
        a retry replay the first result instead of charging twice.'
      operationId: post_billing_subscribe_card
      summary: Subscribe to a paid plan with a card, charged for the first period
        immediately
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/subscriptions:
    get:
      description: Returns the caller org's subscriptions with a count, narrowable
        by userId or status, read from that org's own namespaced store. The org is
        the gateway-validated one and the caller's billing subject is pinned before
        the handler runs. A request with no resolvable org gets an empty list and
        a zero count rather than an error.
      operationId: get_billing_subscriptions
      summary: List your org's subscriptions
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/subscriptions/{id}/cancel:
    post:
      description: Cancels the addressed subscription and answers its updated state,
        emitting the cancellation event the rest of the platform keys on. The default
        is to cancel AT PERIOD END — a body that fails to parse falls back to it —
        so the customer keeps what they paid for unless atPeriodEnd is explicitly
        false. The subscription is resolved inside the caller's own org namespace,
        so another tenant's id is a 404, and the write carries the browser anti-CSRF
        gate because it is reachable with an ambient cookie.
      operationId: post_billing_subscriptions_by_id_cancel
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Cancel a subscription, at period end by default
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/subscriptions/{id}/reactivate:
    post:
      description: Clears the scheduled cancellation on the addressed subscription
        and answers its updated state. It is the inverse of cancel and applies to
        a subscription that is still within its period; one the engine will not reactivate
        is refused 400 with the reason. The subscription is resolved inside the caller's
        own org namespace, so another tenant's id reads as 404, and the write carries
        the browser anti-CSRF gate.
      operationId: post_billing_subscriptions_by_id_reactivate
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Undo a pending cancellation and keep the subscription running
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/tier:
    get:
      description: 'Answers one subject''s resolved tier — name, display name, agent
        ceiling and allowed models — with the balance that admits their next metered
        call: prepaidAvailable, creditsRemaining, dailyRemaining and the effectiveAvailable
        those fold into. The ai router reads it per request to pick that caller''s
        rate-limit tier. It sits on the org-resolving chain because a tier is org
        state, and the subject keys are pinned to the validated caller before the
        handler runs, so a browser read is always the caller''s own; user is required,
        which only a service-to-service caller can omit and be refused 400 for. The
        tier is an upstream tier claim, or an explicit tier override, when either
        is present — that is the service-to-service contract — and is otherwise DERIVED
        from the org''s active and trialing subscriptions, the highest one winning,
        its paid-ness read from the plan catalog by slug rather than from the subscription''s
        own stored copy. The rule to get right is effectiveAvailable and not prepaidAvailable:
        granted credits spend too, credits first, so an account funded only by a grant
        reads zero prepaid while holding real spendable credit — and with the daily
        term zero on every tier there is no free allowance behind it, so a zero-balance
        account is gated. A subscription-store error answers 500 rather than downgrading
        to free, so a transient failure never reports a paid subscriber as unsubscribed.'
      operationId: get_billing_tier
      summary: The subject's plan tier and the balance a metered call is admitted
        on
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/topup:
    post:
      description: 'Charges a card the caller already has on file, named by paymentMethodId,
        and credits the caller''s own balance — the SAVED-card twin of topup/token,
        sharing the one charge-and-credit core the auto-recharge cron runs on. The
        credit lands on the caller''s OWN billing subject: the request body''s subject
        field is pinned to the caller before the handler sees it, so a top-up can
        never be redirected to another subject or outside the caller''s org. It is
        screened for risk before any money moves, exactly as the token path is, because
        both credit the SPENDABLE wallet. The rule most callers get wrong is that
        paymentMethodId is NOT covered by that subject pin — it is a card id, not
        a subject key — so it is checked separately, and a card belonging to any other
        subject answers 404 rather than 403: a permission error would confirm the
        id exists, which is an ownership oracle over other people''s cards.'
      operationId: post_billing_topup
      summary: Add credit to your balance by charging one of your saved cards
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/topup/token:
    post:
      description: 'Charges the single-use card token for the given amount and credits
        the caller''s own balance, answering the transaction id and the new balance
        — the one-time top-up path, with no payment method saved. The amount is bounded
        SERVER-SIDE (roughly a one dollar floor and a five thousand dollar ceiling
        by deployment policy) and the check runs before any money moves, because the
        browser cap is not a control against a scripted request. The credit lands
        on the caller''s OWN billing subject — the same key the usage gate debits
        — and can never be redirected outside the caller''s org. Retries are safe:
        an idempotency key, or absent one the amount within a short window, replays
        the first result, and if that guard store is unreachable the call is refused
        with 503 rather than risking a second real charge.'
      operationId: post_billing_topup_token
      summary: Add credit to your balance by charging a tokenized card once
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/transactions:
    get:
      description: |-
        Returns the caller's own ledger movements — every credit and debit against the subject the usage gate charges — newest first, with a count and the subject they belong to, so a customer can reconcile a bill against the acts that produced it. Paging is limit and offset, and the currency can be narrowed.

        The subject is NOT the caller's to choose. The handler filters on a user parameter, and that parameter is overwritten with the caller's own billing subject before the handler runs — so naming another subject returns your own rows rather than theirs, and the read can never disagree with the wallet it describes. An unauthenticated call is 401 rather than 403, because a browser re-authenticates on the first and only reports the second. No movements is an empty list, not an error.
      operationId: get_billing_transactions
      summary: List the movements on your own balance, newest first
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/usage:
    get:
      description: |-
        Answers one row per BILLED call against the caller's org — transaction id, amount, timestamp and the metered unit. This is the raw charged ledger, not a rollup.

        Each row is stamped with a canonical `metadata.product` derived from what the meter persisted: `agent` becomes agents, `provisioning` becomes the provisioned kind, a token-metered row becomes inference, anything else keeps its metering surface. The ledger has no product field of its own, so this read is where that dimension is made real — from the SAME charged rows, never a second meter. A row that already carries its own product WINS, so the derivation stops the day the meter records one.

        `product=<id>` filters to one product server-side. `groupBy=product` reduces to `{product,requests,amountCents}` rollups instead of rows.

        `amount` is whole USD cents, ROUNDED; `decimal` beside it is the SAME debit exact, as an 18-decimal USD string. Sum `decimal`. A page of sub-cent token calls totals correctly there and totals ZERO in `amount` — that difference is real money.

        Scoped to the caller's own org's books, where the org's ledger file IS the tenant boundary; no client-supplied subject is ever forwarded. 401 without a validated principal. The co-resident read returns the 2000 most recent debits, newest first; `start` and `end` narrow the window only on the split-deploy upstream.
      operationId: get_billing_usage
      summary: Every billed call the caller's org made, attributed to a product
      tags:
      - billing
      x-app: billing
      x-public: true
  /v1/billing/usage/accounts:
    get:
      description: |-
        Answers per-account totals for the linked provider accounts the
        gateway ROUTED this caller's traffic through — requests, prompt and completion
        tokens, recorded cost — plus their honest sum.

        This is the one read in the billing namespace scoped to the PERSON, not the
        org. Rows are keyed on (validated org, validated user), so a caller sees the
        accounts THEY linked and never a colleague's, even inside one org — everything
        else under /v1/billing is org-wide. Neither key is ever read from the request
        body or the query.

        It is a ROUTING counter, not the money ledger. `costCents` is 0 for an account
        billed by its own subscription, where the plan pays the provider directly, so
        these totals do not reconcile against what the org was charged.
        /v1/billing/usage is the charged ledger.

        401 without a validated principal. Where the linked-account plane is not
        resident the answer is an honest 501 — never an empty breakdown, which would
        read as no usage.
      operationId: get_billing_usage_accounts
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/accounts'
          description: ok
          headers:
            Cache-Control:
              description: Set by GET /v1/billing/usage/accounts.
              schema:
                type: string
      summary: Answers per-account totals for the linked provider accounts the gateway
        ROUTED this caller's traffic through — requests, prompt and completion tokens,
        recorded cost — plus their honest sum.
      tags:
      - billing
      x-app: billing
      x-public: true
      x-tool: true
  /v1/billing/usage/rollup:
    get:
      description: 'Answers the one read the account page renders: the subject''s
        resolved plan, the plan''s included monthly allotment beside what was actually
        granted this period, consumption capped at that grant, the overage past it,
        and the spendable wallet (balance minus holds) — all for the current UTC month,
        every figure derived from the same transactions the gateway''s balance gate
        reads, so this read can never disagree with the gate that admits the next
        call. It rides the same pinned chain as the sibling reads: the user parameter
        is overwritten with the validated caller''s own billing subject before the
        handler runs, so it can never name another tenant; user is required, which
        only a service-to-service caller can omit and be refused 400 for, and plan
        is optional — omitted, it is resolved from the subject''s subscription. The
        rule to get right is that the two sides are DIFFERENT MONEY: the included
        figures are usage the subscription grants and the wallet is prepaid credit
        the customer bought, so a reader who sums them invents a balance nobody holds
        — and before the period''s first allotment grant runs, monthlyCents shows
        the plan''s entitlement while grantedCents is zero, which is the figure consumption
        actually draws down.'
      operationId: get_billing_usage_rollup
      summary: What plan you are on and how much of it is left, beside the wallet
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/webhooks/{provider}:
    post:
      description: 'Accepts a payment provider''s event, verifies it, records it for
        audit, and applies subscription lifecycle changes to the matching local row.
        There is no bearer here and there cannot be: the provider''s SIGNATURE over
        the body IS the authentication, so a request with no recognized signature
        header is 400 and one whose signature does not verify is 401. The provider
        path segment is only a hint for dashboard configuration — verification picks
        the processor regardless of what the URL says. Redelivery is safe: an event
        id already recorded is acknowledged as a duplicate without re-applying any
        side effect, which matters because providers retry for days until they see
        a 2xx.'
      operationId: post_billing_webhooks_by_provider
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: Payment-provider webhook intake for settlement and subscription lifecycle
        events
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/billing/wire:
    get:
      description: |-
        Answers the receiving bank details for the brand this deployment serves — the account the funds actually land in, hydrated per brand rather than hard-coded — together with the payment reference to put on the transfer.

        THE REFERENCE IS THE POINT. It carries your own billing key, and it is how an arriving wire is attributed to your account; a transfer sent without it arrives as an unidentified receipt. That is why this read is gated at all: an unpinned caller would be handed an unattributable reference.

        Reading it credits nothing and reserves nothing. A wire is settled by an operator when the bank shows the funds, so the balance moves on receipt, not on this call.
      operationId: get_billing_wire
      summary: Where to wire funds, and the reference that credits them to you
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/blueprint:
    get:
      description: |-
        Returns every deployable blueprint with its service count and estimated
        monthly compute cost.

        It is the lightweight index the console renders as a template gallery before
        drilling into one stack's bill of images — GET /v1/blueprint/sbom?template=<id>
        is the detail view. The cost is the same figure the deploy path meters the
        deploying org on and the 20% author royalty is taken from, priced from the
        active rate card (GET /v1/blueprint/health echoes that card).
      operationId: get_blueprint
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blueprintIndex'
          description: ok
      summary: Returns every deployable blueprint with its service count and estimated
        monthly compute cost.
      tags:
      - blueprint
      x-app: blueprint
      x-public: true
      x-tool: true
  /v1/blueprint/health:
    get:
      description: |-
        Reports blueprint liveness and echoes the compute rate card in force.

        The rate card is the one the estimator actually applies after the operator env
        overlay, so an operator can confirm a tuned knob took effect rather than
        inferring it from a price. Not JWT-gated — a liveness probe must be reachable —
        and it always answers 200 while the subsystem is mounted.
      operationId: get_blueprint_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/blueprintHealth'
          description: ok
      summary: Reports blueprint liveness and echoes the compute rate card in force.
      tags:
      - blueprint
      x-app: blueprint
      x-public: true
      x-tool: true
  /v1/blueprint/sbom:
    get:
      description: |-
        Answers a blueprint's SBOM — the container images its compose stack runs, each with the CPU/memory footprint that was applied to it — together with the compute cost that footprint prices out to on the active rate card.

        ONE address, TWO shapes at 200: `?template=<id>` returns that blueprint's Estimate alone (404 on an id no embedded blueprint carries), and no `template` returns `{data:[Estimate]}` for every blueprint — the batch the console's template gallery reads in one round-trip.

        The blueprints are reference content embedded in the binary and validated at mount, so this read is the same for every caller and is scoped to no tenant. The per-hour figure it returns is the one the deploy path meters the deploying org on and the 20% author royalty is taken from; GET /v1/blueprint/health echoes the rate card it was priced from.
      operationId: get_blueprint_sbom
      summary: A blueprint's bill of images and what running it costs
      tags:
      - blueprint
      x-app: blueprint
      x-public: true
  /v1/catalog:
    get:
      description: |-
        Browse searches AND browses the cross-org catalog: every project, app and site
        the fleet has built, whichever org built it.

        It reads TWO corpora and returns them as one page — the published,
        world-readable catalog that every caller sees, plus the caller's OWN org's
        private entries when the request carries a validated principal. Each row says
        which it came from in `scope`, so a client can warn before sharing a link. An
        anonymous caller simply gets the published one; no filter can ever widen a
        caller into another tenant's corpus, because the query that would return it is
        never run for them.

        A request with no q is a browse rather than a search, and both answer the same
        shape: the page, the total before paging, and the facet counts over the whole
        matching set.
      operationId: get_catalog
      parameters:
      - description: |-
          Q is the free-text query the lexical index scores relevance on. Empty is a
          browse rather than a search — the same request either way.
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: 'Org narrows to one builder org: hanzo | lux | zoo. Case-insensitive.'
        in: query
        name: org
        required: false
        schema:
          type: string
      - description: Kind narrows to repo | site. Case-insensitive.
        in: query
        name: kind
        required: false
        schema:
          type: string
      - description: |-
          Origin narrows to what a row IS to you: template | community | third-party |
          product. This is the axis the two hanzo.app lanes are cut on.
        example: template
        in: query
        name: origin
        required: false
        schema:
          type: string
      - description: Archetype narrows to one project archetype. Case-insensitive.
        in: query
        name: archetype
        required: false
        schema:
          type: string
      - description: Language narrows to one implementation language. Case-insensitive.
        example: typescript
        in: query
        name: language
        required: false
        schema:
          type: string
      - description: |-
          Template narrows a lane to ONE lineage: the id of the parent everything
          returned was forked from.
        in: query
        name: template
        required: false
        schema:
          type: string
      - description: |-
          Forkable is tri-state: "true" selects the forkable rows, "false" selects the
          rest, and anything else — including absent — applies no filter at all.
        example: "true"
        in: query
        name: forkable
        required: false
        schema:
          type: string
      - description: |-
          Limit caps the page at 200, default 50. A value that is not a non-negative
          integer falls back to the default.
        example: "20"
        in: query
        name: limit
        required: false
        schema:
          type: string
      - description: Offset is where the page starts, default 0, with the same tolerance.
        in: query
        name: offset
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/catalogPage'
          description: ok
      summary: 'Browse searches AND browses the cross-org catalog: every project,
        app and site the fleet has built, whichever org built it.'
      tags:
      - catalog
      x-app: catalog
      x-public: true
      x-tool: true
  /v1/channels:
    get:
      description: |-
        Reports every chat channel this org can send through, and whether it can
        send through it right now.

        A channel appears here whether or not it is connected — an empty list would
        leave a caller unable to tell "this org has no Slack" from "Slack is down",
        which are different problems with different fixes. Each entry carries the
        connection behind it, so the answer to "why can I not post?" is in the same
        response as the channel that cannot post.
      operationId: get_channels
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/chatChannels'
          description: ok
      summary: Reports every chat channel this org can send through, and whether it
        can send through it right now.
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
  /v1/channels/{channel}/send:
    post:
      description: |-
        Delivers text, attachments and actions to one room on a connected chat transport — discord, slack, teams or telegram — and answers that transport's own receipt, the `messageId` it assigned and the Unix second it landed. An unknown channel is a 404.

        The body is the envelope's NARROW outbound projection: `room`, `text`, `attachments`, `actions`, `replyTo` and `idempotency`, and nothing else. Identity is not a field — the channel is the path segment and the sender is the caller's validated org — so a body carrying `sender`, `account` or `channel` is refused with 400 rather than having it silently dropped. `room.id` is required, and so is something to say: text, or at least one attachment.

        Requires a validated principal; 403 without one. The room must already belong to the caller's org — each transport verifies the binding itself, so a room this org has not bound is 403 and a room whose route the bot has never learned is 409, meaning someone has to message the bot there first. A transport that fails answers 502 carrying status and shape only, never a token.

        Sending is at-most-once only if you ask for it: pass an `idempotency` string and a replay answers 200 with the PRIOR receipt instead of sending twice, while a send that fails releases the key so the caller can re-attempt. Bodies over 1 MiB are refused. All four transports currently render text only, so attachments and actions are flattened deterministically to one line each after the text rather than dropped.
      operationId: post_channels_by_channel_send
      parameters:
      - in: path
        name: channel
        required: true
        schema:
          type: string
      summary: Send a message from your org's bot to one chat room
      tags:
      - channels
      x-app: channels
      x-public: true
  /v1/channels/allowlist:
    get:
      description: |-
        Returns the caller org's access policy for one channel: whether
        DMs are pairing-gated, allowlisted or open, whether group rooms are open,
        allowlisted or disabled, the config-managed DM and group allow entries, the
        senders approved through PAIRING (read-only here), and the org's named access
        groups. An unknown channel is a 404.
      operationId: get_channels_allowlist
      parameters:
      - description: |-
          Channel is the transport to read: discord, slack, teams or telegram.
          Required; an unknown value is a 404.
        example: slack
        in: query
        name: channel
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/allowlistView'
          description: ok
      summary: 'Returns the caller org''s access policy for one channel: whether DMs
        are pairing-gated, allowlisted or open, whether group rooms are open, allowlisted
        or disabled, the config-managed DM and group allow entries, the senders approved
        through PAIRING (read-only here), and the org''s named access groups.'
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
    put:
      description: |-
        Edits the caller org's access policy for one channel and answers
        the policy as GET would, so both verbs return ONE shape. It requires ORG ADMIN.
        Every field but `channel` is optional and applied only when provided: an empty
        policy string leaves that policy alone, an absent or null list leaves that list
        alone, and an EMPTY list clears it. It writes only CONFIG-sourced allow entries
        — senders approved through pairing belong to the approval flow, so a policy
        edit can never revoke one. An unknown channel is a 404.
      operationId: put_channels_allowlist
      requestBody:
        content:
          application/json:
            example:
              channel: slack
              dm:
              - U024BE7LH
              dmPolicy: allowlist
            schema:
              $ref: '#/components/schemas/allowlistPutIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/allowlistView'
          description: ok
      summary: Edits the caller org's access policy for one channel and answers the
        policy as GET would, so both verbs return ONE shape.
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
  /v1/channels/inbox:
    get:
      description: |-
        Returns the messages people have sent to the caller org's connected chat
        bots, oldest first, in the portable envelope shape every transport normalises
        into. It is a CURSOR feed, not a search: pass the returned cursor back as
        `since` to get only what has arrived since. Only this org's messages are
        stored under this org, so the feed can never carry another tenant's chat.
      operationId: get_channels_inbox
      parameters:
      - description: |-
          Since is the exclusive cursor: only messages with a higher row id come
          back. Empty starts at the beginning. Must parse as an integer.
        example: "1042"
        in: query
        name: since
        required: false
        schema:
          type: string
      - description: |-
          Limit caps how many messages come back. Empty or 0 uses the store's
          default page size. Must parse as an integer.
        example: "100"
        in: query
        name: limit
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/inboxPage'
          description: ok
      summary: Returns the messages people have sent to the caller org's connected
        chat bots, oldest first, in the portable envelope shape every transport normalises
        into.
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
  /v1/channels/pairing:
    get:
      description: |-
        Returns the pairing requests waiting for the caller org to approve
        — one per person who messaged a connected bot on a channel whose DM policy is
        "pairing" and who is not allowed yet. Each row carries the CODE an org admin
        passes to POST /v1/channels/pairing/approve. Expired requests are not
        returned. Codes are capability strings: they are shown here, and never logged.
      operationId: get_channels_pairing
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pairingQueue'
          description: ok
      summary: Returns the pairing requests waiting for the caller org to approve
        — one per person who messaged a connected bot on a channel whose DM policy
        is "pairing" and who is not allowed yet.
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
  /v1/channels/pairing/approve:
    post:
      description: |-
        Turns one pending pairing code into a standing allow entry, so
        that person can DM the org's bot on that channel from now on. It requires ORG
        ADMIN, not merely membership. The first approval an org makes on a channel also
        bootstraps that sender as the channel's owner, which the answer reports. An
        unknown or expired code is a 404, and a code always belongs to exactly one
        org, so it can never approve someone into another tenant.
      operationId: post_channels_pairing_approve
      requestBody:
        content:
          application/json:
            example:
              channel: telegram
              code: PAIR-7Q2M
            schema:
              $ref: '#/components/schemas/approvePairingIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pairingApproved'
          description: ok
      summary: Turns one pending pairing code into a standing allow entry, so that
        person can DM the org's bot on that channel from now on.
      tags:
      - channels
      x-app: channels
      x-public: true
      x-tool: true
  /v1/chat:
    post:
      description: Implements the OpenAI-compatible chat completions API
      operationId: post_chat
      summary: Implements the OpenAI-compatible chat completions API
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/chat/completions:
    post:
      description: Implements the OpenAI-compatible chat completions API
      operationId: post_chat_completions
      summary: Implements the OpenAI-compatible chat completions API
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/chat/public:
    post:
      description: Serves one completion to a caller with no account.
      operationId: post_chat_public
      summary: Serves one completion to a caller with no account.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/cloudflare/d1/databases:
    get:
      description: |-
        Lists the D1 databases on the org's Cloudflare account. Any org
        member may read.
      operationId: get_cloudflare_d1_databases
      parameters:
      - description: Page is the 1-based page of databases to return.
        in: query
        name: page
        required: false
        schema:
          type: string
      - description: PerPage is how many databases one page holds.
        in: query
        name: per_page
        required: false
        schema:
          type: string
      - description: Name filters to the database with this name.
        in: query
        name: name
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the D1 databases on the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a D1 database on the org's Cloudflare account.
        Requires org admin.
      operationId: post_cloudflare_d1_databases
      requestBody:
        content:
          application/json:
            example:
              name: orders
            schema:
              $ref: '#/components/schemas/databaseCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Creates a D1 database on the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/d1/databases/{database}:
    delete:
      description: |-
        Deletes a D1 database and everything stored in it. Requires
        org admin.
      operationId: delete_cloudflare_d1_databases_by_database
      parameters:
      - description: Database is the Cloudflare D1 database id or name.
        example: orders
        in: path
        name: database
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Deletes a D1 database and everything stored in it.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/d1/databases/{database}/query:
    post:
      description: |-
        Executes a statement on one D1 database on the org's OWN Cloudflare account and relays D1's result set. `sql` is required and `params` carries the bound values in placeholder order — use them rather than interpolating values into the statement.

        The body is checked for a non-empty `sql` and then forwarded VERBATIM, so every field D1 accepts reaches D1 even though only two are named here; the declared schema is open for that reason. That verbatim forward is why this is not a typed op — decoding and re-encoding the body would drop `params`, where the query's bound values live. Requires ORG ADMIN (403 otherwise); a malformed body or missing `sql` is 400; 503 if the org has never connected a Cloudflare token.
      operationId: post_cloudflare_d1_databases_by_database_query
      parameters:
      - in: path
        name: database
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/D1Query'
      responses:
        2XX:
          content:
            application/json:
              schema: {}
          description: Success
      summary: Run a SQL statement against a D1 database
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
  /v1/cloudflare/kv/namespaces:
    get:
      description: |-
        KVNamespaceList lists the Workers KV namespaces on the org's Cloudflare
        account. Any org member may read.
      operationId: get_cloudflare_kv_namespaces
      parameters:
      - description: Page is the 1-based page of namespaces to return.
        in: query
        name: page
        required: false
        schema:
          type: string
      - description: PerPage is how many namespaces one page holds.
        in: query
        name: per_page
        required: false
        schema:
          type: string
      - description: Order names the field to sort by, and Direction sorts asc or
          desc.
        in: query
        name: order
        required: false
        schema:
          type: string
      - in: query
        name: direction
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: KVNamespaceList lists the Workers KV namespaces on the org's Cloudflare
        account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    post:
      description: |-
        KVNamespaceCreate creates a Workers KV namespace on the org's Cloudflare
        account. Requires org admin. Cloudflare mints the namespace id the value routes
        address.
      operationId: post_cloudflare_kv_namespaces
      requestBody:
        content:
          application/json:
            example:
              title: sessions
            schema:
              $ref: '#/components/schemas/namespaceCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: KVNamespaceCreate creates a Workers KV namespace on the org's Cloudflare
        account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/kv/namespaces/{namespace}:
    delete:
      description: |-
        KVNamespaceDelete deletes a Workers KV namespace and every key in it. Requires
        org admin.
      operationId: delete_cloudflare_kv_namespaces_by_namespace
      parameters:
      - description: Namespace is the Cloudflare KV namespace id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: namespace
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: KVNamespaceDelete deletes a Workers KV namespace and every key in it.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/kv/namespaces/{namespace}/values/{key}:
    delete:
      description: KVValueDelete removes one key from a Workers KV namespace. Requires
        org admin.
      operationId: delete_cloudflare_kv_namespaces_by_namespace_values_by_key
      parameters:
      - description: Namespace is the Cloudflare KV namespace id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: namespace
        required: true
        schema:
          type: string
      - description: |-
          Key is the key within that namespace. KV keys are broad (up to 512 bytes),
          so this one is escaped rather than charset-restricted.
        example: session/abc
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: KVValueDelete removes one key from a Workers KV namespace.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    get:
      description: Answers one KV key's value from the org's OWN Cloudflare account
        as RAW BYTES under the content type it was written with — not wrapped in a
        JSON envelope, which is why this is not a typed op. Any org member may read.
        A key that does not exist is Cloudflare's own 404; an invalid namespace, or
        a key that is empty, over 512 bytes, not valid UTF-8, or carries a control
        character, is 400; 503 if the org has never connected a Cloudflare token.
      operationId: get_cloudflare_kv_namespaces_by_namespace_values_by_key
      parameters:
      - in: path
        name: namespace
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Read a Workers KV value as its stored bytes
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
    put:
      description: Stores one KV key on the org's OWN Cloudflare account. The REQUEST
        BODY IS THE VALUE, forwarded verbatim under the caller's own Content-Type
        (`text/plain` when none is sent), so a value is never re-encoded on the way
        in — which is why this is not a typed op. `expiration` and `expiration_ttl`
        may ride the query string and are passed through to Cloudflare. Requires ORG
        ADMIN (403 otherwise); the same namespace and key validation as the read answers
        400; 503 if the org has never connected a Cloudflare token.
      operationId: put_cloudflare_kv_namespaces_by_namespace_values_by_key
      parameters:
      - in: path
        name: namespace
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Write a Workers KV value from the request body
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
  /v1/cloudflare/pages/projects:
    get:
      description: Lists the org's Cloudflare Pages projects. Any org member may read.
      operationId: get_cloudflare_pages_projects
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the org's Cloudflare Pages projects.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a Cloudflare Pages project on the org's account. Requires
        org admin. Only the modeled fields reach Cloudflare, so an unmodeled key in the
        request is dropped rather than forwarded.
      operationId: post_cloudflare_pages_projects
      requestBody:
        content:
          application/json:
            example:
              name: marketing-site
              production_branch: main
            schema:
              $ref: '#/components/schemas/PagesProjectCreate'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Creates a Cloudflare Pages project on the org's account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/pages/projects/{project}:
    delete:
      description: |-
        Deletes a Cloudflare Pages project, and with it every deployment it
        has ever made. Requires org admin.
      operationId: delete_cloudflare_pages_projects_by_project
      parameters:
      - description: Project is the Pages project name.
        example: marketing-site
        in: path
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Deletes a Cloudflare Pages project, and with it every deployment it
        has ever made.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    get:
      description: |-
        Reads one Cloudflare Pages project — its build config, deployment
        configs and latest deployment. Any org member may read.
      operationId: get_cloudflare_pages_projects_by_project
      parameters:
      - description: Project is the Pages project name.
        example: marketing-site
        in: path
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Reads one Cloudflare Pages project — its build config, deployment configs
        and latest deployment.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/pages/projects/{project}/deployments:
    post:
      description: |-
        Starts a build and deployment of one Cloudflare Pages project on the org's OWN Cloudflare account, and relays Cloudflare's deployment record back. `branch` picks what to build; OMITTING it builds the project's production branch.

        A body it cannot parse is IGNORED rather than refused — the deployment falls back to the production branch — which is the one rule to get right here and the reason this is not a typed op: a typed request would answer 400 where this deploys. Requires ORG ADMIN (403 otherwise), and 503 if the org has never connected a Cloudflare token.
      operationId: post_cloudflare_pages_projects_by_project_deployments
      parameters:
      - in: path
        name: project
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PagesDeploy'
      responses:
        2XX:
          content:
            application/json:
              schema: {}
          description: Success
      summary: Trigger a new Pages deployment for a project
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
  /v1/cloudflare/pages/projects/{project}/domains:
    post:
      description: |-
        Attaches a custom domain to a Cloudflare Pages project. Requires
        org admin. Cloudflare owns validation and certificate issuance from here on.
      operationId: post_cloudflare_pages_projects_by_project_domains
      parameters:
      - description: Project is the Pages project name, from the path.
        example: marketing-site
        in: path
        name: project
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              name: www.acme.com
              project: marketing-site
            schema:
              $ref: '#/components/schemas/domainAddIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Attaches a custom domain to a Cloudflare Pages project.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/pages/projects/{project}/domains/{domain}:
    delete:
      description: |-
        Detaches a custom domain from a Cloudflare Pages project.
        Requires org admin.
      operationId: delete_cloudflare_pages_projects_by_project_domains_by_domain
      parameters:
      - description: Project is the Pages project name.
        example: marketing-site
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: Domain is the attached custom domain to detach.
        example: www.acme.com
        in: path
        name: domain
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Detaches a custom domain from a Cloudflare Pages project.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/r2/buckets:
    get:
      description: |-
        Lists the R2 buckets on the org's Cloudflare account. Any org
        member may read.
      operationId: get_cloudflare_r2_buckets
      parameters:
      - description: PerPage is how many buckets one page holds.
        in: query
        name: per_page
        required: false
        schema:
          type: string
      - description: Cursor continues from the position a previous page returned.
        in: query
        name: cursor
        required: false
        schema:
          type: string
      - description: NameContains filters to buckets whose name contains this substring.
        in: query
        name: name_contains
        required: false
        schema:
          type: string
      - description: Order names the field to sort by, and Direction sorts asc or
          desc.
        in: query
        name: order
        required: false
        schema:
          type: string
      - in: query
        name: direction
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the R2 buckets on the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates an R2 bucket on the org's Cloudflare account. Requires
        org admin.
      operationId: post_cloudflare_r2_buckets
      requestBody:
        content:
          application/json:
            example:
              name: assets
            schema:
              $ref: '#/components/schemas/bucketCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Creates an R2 bucket on the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/r2/buckets/{bucket}:
    delete:
      description: |-
        Deletes an R2 bucket. Requires org admin. Cloudflare refuses a
        bucket that still holds objects, and that refusal is relayed.
      operationId: delete_cloudflare_r2_buckets_by_bucket
      parameters:
      - description: Bucket is the R2 bucket name.
        example: assets
        in: path
        name: bucket
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Deletes an R2 bucket.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/workers/scripts:
    get:
      description: |-
        Lists the Worker scripts on the org's Cloudflare account. Any
        org member may read.
      operationId: get_cloudflare_workers_scripts
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the Worker scripts on the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/workers/scripts/{script}:
    delete:
      description: |-
        Removes a Worker script from the org's Cloudflare account.
        Requires org admin. Routes bound to the script stop serving it.
      operationId: delete_cloudflare_workers_scripts_by_script
      parameters:
      - description: Script is the Worker script name.
        example: edge-router
        in: path
        name: script
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Removes a Worker script from the org's Cloudflare account.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    put:
      description: |-
        Publishes a module Worker to the org's OWN Cloudflare account under the name in the path, replacing whatever was there, and relays Cloudflare's result. `script` carries the module SOURCE; the optional compatibility date, compatibility flags and bindings are packed into the multipart upload Cloudflare expects.

        The path names the script and the body field named `script` is its source — two different things that share a name, which is exactly why this cannot be a typed op: a binder that gives the URL the last word would overwrite the source with the script's name. Requires ORG ADMIN (403 otherwise); an unparseable body or empty source is 400; 503 if the org has never connected a Cloudflare token.
      operationId: put_cloudflare_workers_scripts_by_script
      parameters:
      - in: path
        name: script
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WorkerScriptPut'
      responses:
        2XX:
          content:
            application/json:
              schema: {}
          description: Success
      summary: Upload or replace a module Worker script
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
  /v1/cloudflare/workers/scripts/{script}/subdomain:
    post:
      description: |-
        Publishes or withdraws one Worker script on the
        account's workers.dev subdomain. Requires org admin.
      operationId: post_cloudflare_workers_scripts_by_script_subdomain
      parameters:
      - description: Script is the Worker script name, from the path.
        example: edge-router
        in: path
        name: script
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              enabled: true
              script: edge-router
            schema:
              $ref: '#/components/schemas/subdomainSetIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Publishes or withdraws one Worker script on the account's workers.dev
        subdomain.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/workers/subdomain:
    get:
      description: |-
        Reads the org account's workers.dev subdomain — the name
        under which every subdomain-enabled script is served. Any org member may read.
      operationId: get_cloudflare_workers_subdomain
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Reads the org account's workers.dev subdomain — the name under which
        every subdomain-enabled script is served.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/workers/zones/{zone}/routes:
    get:
      description: |-
        Lists the Worker routes bound within one zone — the URL
        patterns that dispatch to a script. Any org member may read. Routes are
        zone-scoped, so no account is resolved.
      operationId: get_cloudflare_workers_zones_by_zone_routes
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the Worker routes bound within one zone — the URL patterns that
        dispatch to a script.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
    post:
      description: |-
        Binds a URL pattern in a zone to a Worker script. Requires
        org admin — a route is what puts a script in front of live traffic.
      operationId: post_cloudflare_workers_zones_by_zone_routes
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id, from the path.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              pattern: acme.com/api/*
              script: edge-router
              zone: 0123456789abcdef0123456789abcdef
            schema:
              $ref: '#/components/schemas/routeCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Binds a URL pattern in a zone to a Worker script.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/workers/zones/{zone}/routes/{route}:
    delete:
      description: |-
        Unbinds a Worker route, so its pattern stops dispatching to a
        script. Requires org admin.
      operationId: delete_cloudflare_workers_zones_by_zone_routes_by_route
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      - description: Route is the 32-hex Cloudflare route id.
        example: fedcba9876543210fedcba9876543210
        in: path
        name: route
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Unbinds a Worker route, so its pattern stops dispatching to a script.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/zones:
    get:
      description: |-
        Lists the Cloudflare zones the org's connected API token can see,
        paged and filtered by the query parameters Cloudflare itself accepts. Zones are
        token-scoped by Cloudflare, so no account is resolved. Any org member may read.

        Zone and DNS-record MANAGEMENT is not here: it stays on the Hanzo DNS plane
        (/v1/dns). This only surfaces the Cloudflare zone objects the asset plane needs
        — a zone id is what addresses a Worker route or an analytics read.
      operationId: get_cloudflare_zones
      parameters:
      - description: Page is the 1-based page of zones to return.
        in: query
        name: page
        required: false
        schema:
          type: string
      - description: PerPage is how many zones one page holds.
        in: query
        name: per_page
        required: false
        schema:
          type: string
      - description: Name filters to the zone with this domain name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Status filters by zone status (active, pending, initializing,
          …).
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Order names the field to sort by, and Direction sorts asc or
          desc.
        in: query
        name: order
        required: false
        schema:
          type: string
      - in: query
        name: direction
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Lists the Cloudflare zones the org's connected API token can see, paged
        and filtered by the query parameters Cloudflare itself accepts.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/zones/{zone}:
    get:
      description: |-
        Reads one Cloudflare zone the org's token can see. Any org member may
        read. A zone id the token cannot see is Cloudflare's own not-found, relayed.
      operationId: get_cloudflare_zones_by_zone
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Reads one Cloudflare zone the org's token can see.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/zones/{zone}/analytics:
    get:
      description: |-
        Reads a zone's Cloudflare traffic dashboard — requests, bandwidth,
        threats and pageviews over the since/until window. Any org member may read.

        A zone whose Cloudflare plan does not serve this endpoint yields Cloudflare's
        OWN error, never a fabricated success.
      operationId: get_cloudflare_zones_by_zone_analytics
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      - description: |-
          Since and Until bound the window, in the form Cloudflare accepts — an RFC 3339
          time or a negative number of minutes from now ("-1440" is the last day).
        example: "-1440"
        in: query
        name: since
        required: false
        schema:
          type: string
      - example: "0"
        in: query
        name: until
        required: false
        schema:
          type: string
      - description: Continuous asks Cloudflare for only fully-aggregated buckets.
        in: query
        name: continuous
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Reads a zone's Cloudflare traffic dashboard — requests, bandwidth,
        threats and pageviews over the since/until window.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/cloudflare/zones/{zone}/purge:
    post:
      description: |-
        Drops a zone's Cloudflare edge cache — either the whole zone
        (purge_everything) or exactly the listed file URLs. Requires org admin.

        Purging is the one zone-scoped WRITE this plane owns. It is not DNS — no record
        changes — so it does not belong on /v1/dns, and it is not a connection, so it does
        not belong on the integrations plane. It is a cache operation on a zone, which is
        what this asset plane is for. It takes the admin gate because dropping a zone's
        cache sends every subsequent request to the origin: on a site fronting a small
        origin that is a self-inflicted load spike, so it is a change, not a look.

        Exactly one selector is required. Cloudflare treats a body with neither as a
        no-op and answers 200, which reads as "purged" to a caller that never purged
        anything — the failure we refuse to pass through.
      operationId: post_cloudflare_zones_by_zone_purge
      parameters:
      - description: Zone is the 32-hex Cloudflare zone id, from the path.
        example: 0123456789abcdef0123456789abcdef
        in: path
        name: zone
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              purge_everything: true
              zone: 0123456789abcdef0123456789abcdef
            schema:
              $ref: '#/components/schemas/purgeIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Drops a zone's Cloudflare edge cache — either the whole zone (purge_everything)
        or exactly the listed file URLs.
      tags:
      - cloudflare
      x-app: cloudflare
      x-public: true
      x-tool: true
  /v1/code/ask:
    get:
      description: |-
        Answers a question about the caller org's code with a CITED answer:
        retrieval packs grounding context, then the synthesizer writes the answer over
        exactly those spans, which come back alongside it. It never answers without
        grounding — with no matched code the answer is empty and says so, and with no
        synthesizer available the citations still come back with "degraded": true so
        the caller can reason over the spans itself.
      operationId: get_code_ask
      parameters:
      - description: Q is the question to answer. Required, max 4000 bytes.
        example: where is the per-org SQLite file opened
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: |-
          Repo narrows retrieval to one repository. Empty searches every repo the org
          has indexed.
        example: cloud
        in: query
        name: repo
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AskAnswer'
          description: ok
      summary: 'Answers a question about the caller org''s code with a CITED answer:
        retrieval packs grounding context, then the synthesizer writes the answer
        over exactly those spans, which come back alongside it.'
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
    post:
      description: |-
        Is askGet with the question in the request BODY, for a question too
        long or too awkward to put in a URL. `query` and `repo` in the body take
        precedence over `?q=` and `?repo=`; either source works alone.
      operationId: post_code_ask
      requestBody:
        content:
          application/json:
            example:
              query: where is the per-org SQLite file opened
              repo: cloud
            schema:
              $ref: '#/components/schemas/askPostIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AskAnswer'
          description: ok
      summary: Is askGet with the question in the request BODY, for a question too
        long or too awkward to put in a URL.
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/code/context:
    post:
      description: |-
        Packs the most relevant code for a query into a token budget — THE
        primitive for a coding agent that has to decide what to put in a prompt. It
        retrieves seed spans, expands each with the definitions it calls and its key
        callers, then greedily fills the budget, so the answer is a coherent slice of
        the codebase rather than a list of disconnected matches. The top match is
        always included, truncated if it alone overflows, so a matched query never
        comes back empty. A retrieval outage answers 200 with an empty bundle rather
        than a 5xx.
      operationId: post_code_context
      requestBody:
        content:
          application/json:
            example:
              budgetTokens: 4000
              query: how does the store open a per-org database
              repo: cloud
            schema:
              $ref: '#/components/schemas/contextIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ContextBundle'
          description: ok
      summary: Packs the most relevant code for a query into a token budget — THE
        primitive for a coding agent that has to decide what to put in a prompt.
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/code/file:
    get:
      description: |-
        Returns the INDEXED content of one file — read_file over the chunks the
        search tiers hold, for pulling up code an agent just found. It is NOT
        byte-verbatim: the git object plane is the source of record for exact bytes,
        history and blame. A file absent from the index is a 404, so an agent can tell
        "not indexed" from "empty file".
      operationId: get_code_file
      parameters:
      - description: Path is the file's repo-relative path. Required.
        example: apps/code/store.go
        in: query
        name: path
        required: false
        schema:
          type: string
      - description: Repo is the repository the file belongs to. REQUIRED.
        example: cloud
        in: query
        name: repo
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/fileContent'
          description: ok
      summary: Returns the INDEXED content of one file — read_file over the chunks
        the search tiers hold, for pulling up code an agent just found.
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/code/index:
    post:
      description: |-
        (re)indexes a repository for the caller's org, incrementally: files whose
        content hash is unchanged are skipped, so re-sending a whole tree is cheap.
        Each file is parsed for symbols, split at AST boundaries and — when the
        semantic tier is available — embedded, which is what makes it searchable across
        all three retrieval tiers. Pass `prune` to also DELETE indexed files absent
        from the request, which turns the call into a full sync; without it the call is
        an upsert. The index is written to the caller org's own physically separate
        database.
      operationId: post_code_index
      requestBody:
        content:
          application/json:
            example:
              files:
              - content: |
                  package main
                path: main.go
              prune: true
              repo: cloud
            schema:
              $ref: '#/components/schemas/indexIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/indexResult'
          description: ok
      summary: '(re)indexes a repository for the caller''s org, incrementally: files
        whose content hash is unchanged are skipped, so re-sending a whole tree is
        cheap.'
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/code/search:
    get:
      description: |-
        Finds code in the caller org's index across three orthogonal retrieval
        tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over
        code-tokenized text), symbolic (real definition and reference edges), and
        semantic (embedding cosine over AST-boundary chunks). Pick one tier with
        `type`, or leave it to run all three as hybrid, which is what a coding agent
        usually wants. It is FAIL-HONEST: a retrieval outage answers 200 with an empty
        result set and "degraded": true rather than a 5xx, so an agent degrades instead
        of stalling. A malformed regex is a 400.
      operationId: get_code_search
      parameters:
      - description: |-
          Q is the search query. Required, max 4000 bytes. For type=regex it is a
          regular expression; for type=symbol it is a symbol name.
        example: func openStore
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: |-
          Type selects the retrieval tier: "text" (FTS5 trigram), "regex",
          "symbol" (definitions), "semantic" (embeddings) or "hybrid". Anything
          else — including empty — reads as hybrid.
        example: hybrid
        in: query
        name: type
        required: false
        schema:
          type: string
      - description: Repo narrows to one repository. Empty searches every repo the
          org has indexed.
        example: cloud
        in: query
        name: repo
        required: false
        schema:
          type: string
      - description: |-
          Limit caps how many spans come back: default 20, maximum 100. A value that
          is not a positive integer reads as the default.
        example: 20
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/searchResults'
          description: ok
      summary: 'Finds code in the caller org''s index across three orthogonal retrieval
        tiers fused by reciprocal-rank fusion: lexical (FTS5 trigram over code-tokenized
        text), symbolic (real definition and reference edges), and semantic (embedding
        cosine over AST-boundary chunks).'
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/code/tree:
    get:
      description: |-
        Returns one repository's file structure with a per-file symbol count —
        get_repo_structure over the org's own index, with no git checkout involved. A
        repository that has not been indexed answers an empty tree rather than an
        error, so an agent can tell "nothing here" without handling a failure.
      operationId: get_code_tree
      parameters:
      - description: Repo is the repository to walk. REQUIRED — a tree is repo-scoped.
        example: cloud
        in: query
        name: repo
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/repoTree'
          description: ok
      summary: Returns one repository's file structure with a per-file symbol count
        — get_repo_structure over the org's own index, with no git checkout involved.
      tags:
      - code
      x-app: code
      x-public: true
      x-tool: true
  /v1/commerce/admin/catalog:
    get:
      description: 'Returns the brand-scoped catalog carrying the administrative economics
        the public projection withholds — upstream cost and margin percentage — for
        the margin surface the platform console administrates. The brand comes from
        the query and defaults to hanzo. PLATFORM admin only, enforced by the handler
        on top of the route''s IAM gate: an ORG-level admin is refused 403 precisely
        so upstream cost and margin never reach a tenant.'
      operationId: get_commerce_admin_catalog
      summary: The catalog projection with cost and margin included
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/cart:
    post:
      description: |-
        Opens an empty cart for a shopper to fill, and answers it with its new id.

        This is the first step of a sale: hold the id, add items to it with
        setCartItem, then hand it to checkout. Every field of the request is optional —
        an empty body opens a perfectly good anonymous cart — and the fields exist only
        to pre-fill what is already known about the shopper.

        The STORE defaults to the org's own default storefront, so a merchant selling
        through one storefront never has to name it. The CURRENCY defaults to usd; note
        that checkout overrides it with the store's own currency when the sale is
        authorized, so a currency set here is a hint rather than a commitment.

        The cart is created in the CALLER'S OWN org namespace, taken from the validated
        principal and never from the body, so a cart can never be opened on another
        tenant's books.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: openCart
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CartOpen'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
          description: created
      summary: Open a cart for a shopper to fill
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/cart/{id}:
    get:
      description: |-
        Reads one cart: its lines, its status and what it comes to.

        This is what a storefront calls to render the basket, and what a support agent
        calls to see what a shopper is looking at. The totals are the cart's STORED
        tally — shipping and tax stay zero until checkout resolves a shipping option
        and a tax region, so a cart total before checkout is the merchandise total and
        is meant to be.

        The org scopes the read by construction: the store is namespaced to it, so a
        cart id belonging to another tenant is simply not found rather than found and
        then filtered, and answers 404 rather than 403 so the id space cannot be probed.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: getCart
      parameters:
      - description: ID is the cart's id, as the open call answered it.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
          description: ok
      summary: Read one cart with its lines and totals
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/cart/{id}/discard:
    post:
      description: |-
        Discards a cart the shopper abandoned, and answers it in its final state.

        A discarded cart is CLOSED, not deleted: the row stays, so abandoned-basket
        reporting and any follow-up that keys on it still have something to read. It
        stops being a cart anything will check out, which is the point — it is how a
        storefront says "this basket is over" without destroying the evidence that it
        existed.

        Discarding is idempotent: a cart already discarded answers its stored state
        rather than failing, so a retry is safe.

        The cart is resolved inside the caller's own org namespace, so another tenant's
        id answers 404.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: discardCart
      parameters:
      - description: ID is the cart's id, as the open call answered it.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
          description: ok
      summary: Discard a cart the shopper abandoned
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/cart/{id}/item:
    post:
      description: |-
        Sets how many of one item a cart holds, and answers the whole updated cart.

        This is the ONE way a cart's contents change. The quantity is the RESULT, not a
        delta: sending 3 leaves 3 however many were there before, so a retry is safe and
        a double-submit cannot double an order. ZERO REMOVES the line — there is
        deliberately no separate delete, because removal is the same act at the boundary
        value and a second spelling would be a second set of edge cases.

        Name the item with EITHER product OR variant, never both. Prefer variant for
        anything sold in sizes, colours or tiers: the price and the stock belong to the
        variant, so a product-level line on a varianted product prices the wrong thing.
        Either may be given as an id or as the human key — a product's URL slug, a
        variant's SKU — which is what lets a storefront add to cart straight from a
        product page URL without a lookup first.

        The item's price and name are CACHED onto the line as it is added, so the cart
        keeps the price the shopper was shown even if the catalog moves underneath it.

        An item that resolves to nothing in the catalog is refused 400 and the cart is
        left exactly as it was; nothing is partially applied.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: setCartItem
      parameters:
      - description: ID is the cart to amend, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CartItemSet'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cart'
          description: ok
      summary: Set one item's quantity in a cart; zero removes it
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/catalog:
    get:
      description: Returns the brand's published catalog — the shared source docs,
        the console sidebar and the pricing pages all read — with the brand taken
        from the query and defaulting to hanzo. It is public and cacheable, and it
        is the projection that deliberately omits cost and margin; those live only
        on the platform-admin projection.
      operationId: get_commerce_catalog
      summary: The public product catalog projection for a brand
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/catalog/entries:
    get:
      description: 'Returns every catalog row as stored — the admin view, which unlike
        the public projection includes entries that are not published. It is cross-tenant
        platform data, so the gate is a PLATFORM admin: an org-level admin is refused
        403 no matter how privileged they are inside their own org, enforced by the
        handler itself and not only by the route''s token middleware.'
      operationId: get_commerce_catalog_entries
      summary: The raw catalog entries, including the unpublished ones
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: Creates a catalog row from the body and answers it at 201. The
        slug is required and is the globally-unique catalog key, so a second entry
        claiming a slug already in use is refused 409 rather than shadowing the first.
        PLATFORM admin only — this is cross-tenant pricing and packaging data, and
        an org-level admin is refused 403.
      operationId: post_commerce_catalog_entries
      summary: Add a catalog entry
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/catalog/models:
    post:
      description: Takes a batch of model rows and upserts each one's upstream COST
        and machine-observable facts, answering what was created and changed. It deliberately
        touches nothing a human owns — not the retail price, not the markup, not the
        entitlement tier — so a sync can never overwrite an administrator's pricing
        decision. The gate is a PLATFORM principal rather than a platform ADMIN, because
        the caller is normally a scheduled job holding the internal service token,
        which carries platform scope but no admin claim.
      operationId: post_commerce_catalog_models
      summary: 'Land a syncer''s view of the model catalog: upstream costs and machine
        facts'
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/catalog/models/refresh:
    post:
      description: 'Pulls the upstream model list and lands it through the same upsert
        the push door uses, so the rule that a sync owns cost and an administrator
        owns price holds no matter which door a row came through. It takes no body
        — the upstream is READ rather than told. If that upstream cannot be read the
        call answers 502 and writes NOTHING: a sync that cannot see its source must
        never conclude the source is empty, because that conclusion would withdraw
        every model on sale. The gate is a PLATFORM principal so the scheduled job''s
        service token qualifies.'
      operationId: post_commerce_catalog_models_refresh
      summary: Refresh the model catalog by reading the upstream provider
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/catalog/seed:
    post:
      description: Upserts the shipped catalog seed and answers how many entries it
        created. It is idempotent and non-destructive — an entry an administrator
        has since edited is left alone — so it is safe to run against a live catalog
        to fill in what is missing. PLATFORM admin only; an org-level admin is refused
        403.
      operationId: post_commerce_catalog_seed
      summary: Seed the embedded catalog, without disturbing edits already made
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/collection/:
    get:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Answers a pagination envelope — the page and display echoed back, the rows
        under models, a total count and a facets array — read from the caller org''s
        own namespaced store, so one tenant can never list another''s. Sorting defaults
        to the slug and is overridable with sort. display is the page size and page
        applies only alongside it; either one that is not a positive integer is refused
        with 500 rather than silently ignored, and the limit query overrides the reported
        COUNT only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        Any valid access token reaches it. The org must also be entitled to the commerce
        admin: the paywall answers 402 subscription_required unless the org holds
        an active or trialing pro subscription, a live trial credit or a redeemed
        invite, and 503 when that entitlement cannot be read rather than admitting
        on an unknown. The internal service token and a platform superadmin pass straight
        through. The token must also carry Admin or the Collection list scope.'
      operationId: get_commerce_collection
      summary: List your org's collections, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Decodes the body into a new row in the caller org''s own namespaced store
        — isolated to that tenant from its first write — and answers the stored row
        at 201 with a Location header naming its id. The id is assigned by the store,
        not taken from the body. A body that fails to decode is 400 and a store that
        refuses the write is 500. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The token must also carry Admin or WriteCollection.'
      operationId: post_commerce_collection
      summary: Create a collection
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/collection/{collectionid}:
    delete:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Removes the addressed row and answers 204 with no body. Before the live row
        goes it is written once more under a deleted tombstone kind, so a deletion
        leaves a recoverable copy rather than destroying the record outright — and
        a tombstone that cannot be written fails the call with 500 before anything
        is removed. The id is resolved inside the caller org''s own namespace, so
        an absent or foreign id is 404. Any valid access token reaches it. The org
        must also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The token must also carry Admin or WriteCollection.'
      operationId: delete_commerce_collection_by_collectionid
      parameters:
      - in: path
        name: collectionid
        required: true
        schema:
          type: string
      summary: Delete a collection, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Reads the addressed row from the caller org''s own namespaced store. An id
        that is not there is 404 — and another tenant''s id is not there by construction,
        so it reads exactly like a typo instead of confirming the row exists somewhere
        else. Any valid access token reaches it. The org must also be entitled to
        the commerce admin: the paywall answers 402 subscription_required unless the
        org holds an active or trialing pro subscription, a live trial credit or a
        redeemed invite, and 503 when that entitlement cannot be read rather than
        admitting on an unknown. The internal service token and a platform superadmin
        pass straight through. The token must also carry Admin or ReadCollection.'
      operationId: get_commerce_collection_by_collectionid
      parameters:
      - in: path
        name: collectionid
        required: true
        schema:
          type: string
      summary: Fetch one collection
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Loads the stored row and decodes the body OVER it, so only the fields the
        body names change and everything else keeps its stored value — the difference
        from the full replace, which clears what it is not told. Answers the merged
        row. An id absent from the caller org''s namespace is 404 and a body that
        fails to decode is 400. Any valid access token reaches it. The org must also
        be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The token must also carry Admin, or ReadCollection
        and WriteCollection together.'
      operationId: patch_commerce_collection_by_collectionid
      parameters:
      - in: path
        name: collectionid
        required: true
        schema:
          type: string
      summary: Change part of a collection
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        Re-dispatches the request into the handler the intended verb would have reached,
        taking that verb from a _method form value or query parameter and then from
        the X-HTTP-Method-Override header. PUT replaces the row, PATCH changes part
        of it, DELETE removes it, and anything else is 405. The trap is the DEFAULT:
        naming no override at all leaves the method POST, which this tunnel maps to
        the PARTIAL UPDATE — it is never a create, and creating is the collection
        root''s job. Behaviour and authorization are the underlying operation''s,
        since the real handler runs. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through.'
      operationId: post_commerce_collection_by_collectionid
      parameters:
      - in: path
        name: collectionid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a collection — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A collection is a merchandising group a storefront renders — a
        slug and name, copy and media, flat lists of the product and variant ids it
        holds, published, preorder and out-of-stock flags, and an availability window.
        Membership lives on the collection as those id lists rather than as a join,
        so putting a product into a collection is a write here and not on the product.
        This is a true REPLACEMENT, not a merge: the stored row''s key is preserved,
        but the body is decoded onto a FRESH entity, so every field the body omits
        is written back as its ZERO value. Patch is the verb for changing part of
        a row. The id is resolved inside the caller org''s own namespace and an absent
        one is 404 before anything is written; a body that fails to decode is 400.
        Answers the stored result. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The token must also carry Admin, or ReadCollection
        and WriteCollection together.'
      operationId: put_commerce_collection_by_collectionid
      parameters:
      - in: path
        name: collectionid
        required: true
        schema:
          type: string
      summary: Replace a collection outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/currencies:
    get:
      description: Returns every reference currency as one global list, so a store
        settings form or a product price picker binds real rows instead of a hardcoded
        array. It is a default-namespace read shared by every tenant rather than per-org
        data, and it is public and cacheable.
      operationId: get_commerce_currencies
      summary: The reference currency list the price and settings pickers render
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/disclosure/:
    get:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Answers a pagination envelope — the page and display
        echoed back, the rows under models, a total count and a facets array — read
        from the caller org''s own namespaced store, so one tenant can never list
        another''s. Sorting defaults to the last-updated time and is overridable with
        sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. Any valid access
        token reaches it. The per-kind permission table has no entry for disclosure,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: get_commerce_disclosure
      summary: List your org's disclosures, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Decodes the body into a new row in the caller org''s
        own namespaced store — isolated to that tenant from its first write — and
        answers the stored row at 201 with a Location header naming its id. The id
        is assigned by the store, not taken from the body. A body that fails to decode
        is 400 and a store that refuses the write is 500. Any valid access token reaches
        it. The per-kind permission table has no entry for disclosure, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: post_commerce_disclosure
      summary: Create a disclosure
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/disclosure/{disclosureid}:
    delete:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Removes the addressed row and answers 204 with no
        body. Before the live row goes it is written once more under a deleted tombstone
        kind, so a deletion leaves a recoverable copy rather than destroying the record
        outright — and a tombstone that cannot be written fails the call with 500
        before anything is removed. The id is resolved inside the caller org''s own
        namespace, so an absent or foreign id is 404. Any valid access token reaches
        it. The per-kind permission table has no entry for disclosure, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: delete_commerce_disclosure_by_disclosureid
      parameters:
      - in: path
        name: disclosureid
        required: true
        schema:
          type: string
      summary: Delete a disclosure, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Reads the addressed row from the caller org''s own
        namespaced store. An id that is not there is 404 — and another tenant''s id
        is not there by construction, so it reads exactly like a typo instead of confirming
        the row exists somewhere else. Any valid access token reaches it. The per-kind
        permission table has no entry for disclosure, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.'
      operationId: get_commerce_disclosure_by_disclosureid
      parameters:
      - in: path
        name: disclosureid
        required: true
        schema:
          type: string
      summary: Fetch one disclosure
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Loads the stored row and decodes the body OVER it,
        so only the fields the body names change and everything else keeps its stored
        value — the difference from the full replace, which clears what it is not
        told. Answers the merged row. An id absent from the caller org''s namespace
        is 404 and a body that fails to decode is 400. Any valid access token reaches
        it. The per-kind permission table has no entry for disclosure, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: patch_commerce_disclosure_by_disclosureid
      parameters:
      - in: path
        name: disclosureid
        required: true
        schema:
          type: string
      summary: Change part of a disclosure
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. Re-dispatches the request into the handler the intended
        verb would have reached, taking that verb from a _method form value or query
        parameter and then from the X-HTTP-Method-Override header. PUT replaces the
        row, PATCH changes part of it, DELETE removes it, and anything else is 405.
        The trap is the DEFAULT: naming no override at all leaves the method POST,
        which this tunnel maps to the PARTIAL UPDATE — it is never a create, and creating
        is the collection root''s job. Behaviour and authorization are the underlying
        operation''s, since the real handler runs. Any valid access token reaches
        it.'
      operationId: post_commerce_disclosure_by_disclosureid
      parameters:
      - in: path
        name: disclosureid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a disclosure — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A disclosure is a published-document record — a publication body,
        a content hash, a type and a named receiver. The hash LOOKS like a field you
        set and is in fact derived, but only on update: a freshly created disclosure
        keeps whatever hash the caller sent until the first replace or patch recomputes
        it, so a new row''s hash attests to nothing. This kind lives in commerce''s
        demo tree — a live writable resource in your tenant''s real store that nothing
        else in commerce reads. This is a true REPLACEMENT, not a merge: the stored
        row''s key is preserved, but the body is decoded onto a FRESH entity, so every
        field the body omits is written back as its ZERO value. Patch is the verb
        for changing part of a row. The id is resolved inside the caller org''s own
        namespace and an absent one is 404 before anything is written; a body that
        fails to decode is 400. Answers the stored result. Any valid access token
        reaches it. The per-kind permission table has no entry for disclosure, so
        the scaffold skips that second check with a warning and the gate above is
        the whole authorization story.'
      operationId: put_commerce_disclosure_by_disclosureid
      parameters:
      - in: path
        name: disclosureid
        required: true
        schema:
          type: string
      summary: Replace a disclosure outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/discount/:
    get:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Answers
        a pagination envelope — the page and display echoed back, the rows under models,
        a total count and a facets array — read from the caller org''s own namespaced
        store, so one tenant can never list another''s. Sorting defaults to the last-updated
        time and is overridable with sort. display is the page size and page applies
        only alongside it; either one that is not a positive integer is refused with
        500 rather than silently ignored, and the limit query overrides the reported
        COUNT only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        Any valid access token reaches it. The org must also be entitled to the commerce
        admin: the paywall answers 402 subscription_required unless the org holds
        an active or trialing pro subscription, a live trial credit or a redeemed
        invite, and 503 when that entitlement cannot be read rather than admitting
        on an unknown. The internal service token and a platform superadmin pass straight
        through. The per-kind permission table has no entry for discount, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_discount
      summary: List your org's discounts, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Decodes
        the body into a new row in the caller org''s own namespaced store — isolated
        to that tenant from its first write — and answers the stored row at 201 with
        a Location header naming its id. The id is assigned by the store, not taken
        from the body. A body that fails to decode is 400 and a store that refuses
        the write is 500. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for discount, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: post_commerce_discount
      summary: Create a discount
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/discount/{discountid}:
    delete:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Removes
        the addressed row and answers 204 with no body. Before the live row goes it
        is written once more under a deleted tombstone kind, so a deletion leaves
        a recoverable copy rather than destroying the record outright — and a tombstone
        that cannot be written fails the call with 500 before anything is removed.
        The id is resolved inside the caller org''s own namespace, so an absent or
        foreign id is 404. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for discount, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: delete_commerce_discount_by_discountid
      parameters:
      - in: path
        name: discountid
        required: true
        schema:
          type: string
      summary: Delete a discount, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Reads
        the addressed row from the caller org''s own namespaced store. An id that
        is not there is 404 — and another tenant''s id is not there by construction,
        so it reads exactly like a typo instead of confirming the row exists somewhere
        else. Any valid access token reaches it. The org must also be entitled to
        the commerce admin: the paywall answers 402 subscription_required unless the
        org holds an active or trialing pro subscription, a live trial credit or a
        redeemed invite, and 503 when that entitlement cannot be read rather than
        admitting on an unknown. The internal service token and a platform superadmin
        pass straight through. The per-kind permission table has no entry for discount,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: get_commerce_discount_by_discountid
      parameters:
      - in: path
        name: discountid
        required: true
        schema:
          type: string
      summary: Fetch one discount
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Loads
        the stored row and decodes the body OVER it, so only the fields the body names
        change and everything else keeps its stored value — the difference from the
        full replace, which clears what it is not told. Answers the merged row. An
        id absent from the caller org''s namespace is 404 and a body that fails to
        decode is 400. Any valid access token reaches it. The org must also be entitled
        to the commerce admin: the paywall answers 402 subscription_required unless
        the org holds an active or trialing pro subscription, a live trial credit
        or a redeemed invite, and 503 when that entitlement cannot be read rather
        than admitting on an unknown. The internal service token and a platform superadmin
        pass straight through. The per-kind permission table has no entry for discount,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: patch_commerce_discount_by_discountid
      parameters:
      - in: path
        name: discountid
        required: true
        schema:
          type: string
      summary: Change part of a discount
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. Re-dispatches
        the request into the handler the intended verb would have reached, taking
        that verb from a _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        Any valid access token reaches it. The org must also be entitled to the commerce
        admin: the paywall answers 402 subscription_required unless the org holds
        an active or trialing pro subscription, a live trial credit or a redeemed
        invite, and 503 when that entitlement cannot be read rather than admitting
        on an unknown. The internal service token and a platform superadmin pass straight
        through.'
      operationId: post_commerce_discount_by_discountid
      parameters:
      - in: path
        name: discountid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a discount — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A discount is a price rule: a type (flat, percent, free-shipping,
        free-item or bulk), a window, a scope naming the store, collection, product
        or variant it applies to, a target, and rules pairing a trigger — a price
        or quantity threshold — with an action, an amount off or a percentage. It
        is ENABLED BY DEFAULT, so a bare create makes a live discount rather than
        a draft. The rule engine caches per replica for about thirty seconds, so a
        discount switched off here can keep applying briefly on other replicas. This
        is a true REPLACEMENT, not a merge: the stored row''s key is preserved, but
        the body is decoded onto a FRESH entity, so every field the body omits is
        written back as its ZERO value. Patch is the verb for changing part of a row.
        The id is resolved inside the caller org''s own namespace and an absent one
        is 404 before anything is written; a body that fails to decode is 400. Answers
        the stored result. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for discount, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: put_commerce_discount_by_discountid
      parameters:
      - in: path
        name: discountid
        required: true
        schema:
          type: string
      summary: Replace a discount outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/health:
    get:
      description: |-
        Answers ok whenever the commerce subsystem is mounted. It is registered
        before the module embed boots, so it keeps answering even when the embed
        failed and every business route serves the fail-closed 503 — which is the
        point: it reports that the process is reachable, never that the money plane
        is healthy. Unauthenticated: a probe that needs a credential is a probe that
        reports the credential.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: get_commerce_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/liveness'
          description: ok
      summary: Answers ok whenever the commerce subsystem is mounted.
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/movie/:
    get:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Answers a pagination envelope
        — the page and display echoed back, the rows under models, a total count and
        a facets array — read from the caller org''s own namespaced store, so one
        tenant can never list another''s. Sorting defaults to the slug and is overridable
        with sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. Any valid access
        token reaches it. The per-kind permission table has no entry for movie, so
        the scaffold skips that second check with a warning and the gate above is
        the whole authorization story.'
      operationId: get_commerce_movie
      summary: List your org's movies, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Decodes the body into a new
        row in the caller org''s own namespaced store — isolated to that tenant from
        its first write — and answers the stored row at 201 with a Location header
        naming its id. The id is assigned by the store, not taken from the body. A
        body that fails to decode is 400 and a store that refuses the write is 500.
        Any valid access token reaches it. The per-kind permission table has no entry
        for movie, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: post_commerce_movie
      summary: Create a movie
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/movie/{movieid}:
    delete:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Removes the addressed row and
        answers 204 with no body. Before the live row goes it is written once more
        under a deleted tombstone kind, so a deletion leaves a recoverable copy rather
        than destroying the record outright — and a tombstone that cannot be written
        fails the call with 500 before anything is removed. The id is resolved inside
        the caller org''s own namespace, so an absent or foreign id is 404. Any valid
        access token reaches it. The per-kind permission table has no entry for movie,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: delete_commerce_movie_by_movieid
      parameters:
      - in: path
        name: movieid
        required: true
        schema:
          type: string
      summary: Delete a movie, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Reads the addressed row from
        the caller org''s own namespaced store. An id that is not there is 404 — and
        another tenant''s id is not there by construction, so it reads exactly like
        a typo instead of confirming the row exists somewhere else. Any valid access
        token reaches it. The per-kind permission table has no entry for movie, so
        the scaffold skips that second check with a warning and the gate above is
        the whole authorization story.'
      operationId: get_commerce_movie_by_movieid
      parameters:
      - in: path
        name: movieid
        required: true
        schema:
          type: string
      summary: Fetch one movie
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Loads the stored row and decodes
        the body OVER it, so only the fields the body names change and everything
        else keeps its stored value — the difference from the full replace, which
        clears what it is not told. Answers the merged row. An id absent from the
        caller org''s namespace is 404 and a body that fails to decode is 400. Any
        valid access token reaches it. The per-kind permission table has no entry
        for movie, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: patch_commerce_movie_by_movieid
      parameters:
      - in: path
        name: movieid
        required: true
        schema:
          type: string
      summary: Change part of a movie
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. Re-dispatches the request into
        the handler the intended verb would have reached, taking that verb from a
        _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        Any valid access token reaches it.'
      operationId: post_commerce_movie_by_movieid
      parameters:
      - in: path
        name: movieid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a movie — for clients that cannot send PUT,
        PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A movie is a film catalog record — a slug plus EIDR and IMDB ids,
        all three required, with title and synopsis copy, artwork, screenshots, trailers,
        cast and crew, and available and hidden flags. It carries NO price: the money
        for a film lives on the product that sells it. This is a true REPLACEMENT,
        not a merge: the stored row''s key is preserved, but the body is decoded onto
        a FRESH entity, so every field the body omits is written back as its ZERO
        value. Patch is the verb for changing part of a row. The id is resolved inside
        the caller org''s own namespace and an absent one is 404 before anything is
        written; a body that fails to decode is 400. Answers the stored result. Any
        valid access token reaches it. The per-kind permission table has no entry
        for movie, so the scaffold skips that second check with a warning and the
        gate above is the whole authorization story.'
      operationId: put_commerce_movie_by_movieid
      parameters:
      - in: path
        name: movieid
        required: true
        schema:
          type: string
      summary: Replace a movie outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/note/:
    get:
      description: 'A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller''s
        own field and is distinct from the row''s creation stamp; the note search
        filters on it, so a note written without one is a zero-time note the ops log
        will never surface. Answers a pagination envelope — the page and display echoed
        back, the rows under models, a total count and a facets array — read from
        the caller org''s own namespaced store, so one tenant can never list another''s.
        Sorting defaults to the last-updated time and is overridable with sort. display
        is the page size and page applies only alongside it; either one that is not
        a positive integer is refused with 500 rather than silently ignored, and the
        limit query overrides the reported COUNT only, never the rows returned. No
        search backend is wired, so the datastore is the one and only list path and
        facets is always empty. A request resolving no org namespace is served an
        EMPTY page rather than an unscoped scan: the namespace IS the tenant filter,
        so without one there is nothing safe to return. Any valid access token reaches
        it. The per-kind permission table has no entry for note, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_note
      summary: List your org's notes, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller's own
        field and is distinct from the row's creation stamp; the note search filters
        on it, so a note written without one is a zero-time note the ops log will
        never surface. Decodes the body into a new row in the caller org's own namespaced
        store — isolated to that tenant from its first write — and answers the stored
        row at 201 with a Location header naming its id. The id is assigned by the
        store, not taken from the body. A body that fails to decode is 400 and a store
        that refuses the write is 500. Any valid access token reaches it. The per-kind
        permission table has no entry for note, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.
      operationId: post_commerce_note
      summary: Create a note
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/note/{noteid}:
    delete:
      description: A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller's own
        field and is distinct from the row's creation stamp; the note search filters
        on it, so a note written without one is a zero-time note the ops log will
        never surface. Removes the addressed row and answers 204 with no body. Before
        the live row goes it is written once more under a deleted tombstone kind,
        so a deletion leaves a recoverable copy rather than destroying the record
        outright — and a tombstone that cannot be written fails the call with 500
        before anything is removed. The id is resolved inside the caller org's own
        namespace, so an absent or foreign id is 404. Any valid access token reaches
        it. The per-kind permission table has no entry for note, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.
      operationId: delete_commerce_note_by_noteid
      parameters:
      - in: path
        name: noteid
        required: true
        schema:
          type: string
      summary: Delete a note, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller's own
        field and is distinct from the row's creation stamp; the note search filters
        on it, so a note written without one is a zero-time note the ops log will
        never surface. Reads the addressed row from the caller org's own namespaced
        store. An id that is not there is 404 — and another tenant's id is not there
        by construction, so it reads exactly like a typo instead of confirming the
        row exists somewhere else. Any valid access token reaches it. The per-kind
        permission table has no entry for note, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.
      operationId: get_commerce_note_by_noteid
      parameters:
      - in: path
        name: noteid
        required: true
        schema:
          type: string
      summary: Fetch one note
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller's own
        field and is distinct from the row's creation stamp; the note search filters
        on it, so a note written without one is a zero-time note the ops log will
        never surface. Loads the stored row and decodes the body OVER it, so only
        the fields the body names change and everything else keeps its stored value
        — the difference from the full replace, which clears what it is not told.
        Answers the merged row. An id absent from the caller org's namespace is 404
        and a body that fails to decode is 400. Any valid access token reaches it.
        The per-kind permission table has no entry for note, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.
      operationId: patch_commerce_note_by_noteid
      parameters:
      - in: path
        name: noteid
        required: true
        schema:
          type: string
      summary: Change part of a note
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller''s
        own field and is distinct from the row''s creation stamp; the note search
        filters on it, so a note written without one is a zero-time note the ops log
        will never surface. Re-dispatches the request into the handler the intended
        verb would have reached, taking that verb from a _method form value or query
        parameter and then from the X-HTTP-Method-Override header. PUT replaces the
        row, PATCH changes part of it, DELETE removes it, and anything else is 405.
        The trap is the DEFAULT: naming no override at all leaves the method POST,
        which this tunnel maps to the PARTIAL UPDATE — it is never a create, and creating
        is the collection root''s job. Behaviour and authorization are the underlying
        operation''s, since the real handler runs. Any valid access token reaches
        it.'
      operationId: post_commerce_note_by_noteid
      parameters:
      - in: path
        name: noteid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a note — for clients that cannot send PUT,
        PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A note is a timestamped free-text log line — a caller-supplied
        time, a source, a message and an enabled flag. That time is the caller''s
        own field and is distinct from the row''s creation stamp; the note search
        filters on it, so a note written without one is a zero-time note the ops log
        will never surface. This is a true REPLACEMENT, not a merge: the stored row''s
        key is preserved, but the body is decoded onto a FRESH entity, so every field
        the body omits is written back as its ZERO value. Patch is the verb for changing
        part of a row. The id is resolved inside the caller org''s own namespace and
        an absent one is 404 before anything is written; a body that fails to decode
        is 400. Answers the stored result. Any valid access token reaches it. The
        per-kind permission table has no entry for note, so the scaffold skips that
        second check with a warning and the gate above is the whole authorization
        story.'
      operationId: put_commerce_note_by_noteid
      parameters:
      - in: path
        name: noteid
        required: true
        schema:
          type: string
      summary: Replace a note outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/org:
    get:
      description: Answers the branding, identity issuer and client id, identity-verification
        config, enabled payment providers, return-URL allowlist and public payment
        application config for the org the request HOST resolves to. It is genuinely
        public and unauthenticated — a checkout page calls it before anyone has signed
        in — and it carries the same public payment config the authenticated config
        read does, so the card iframe can never initialize against a different application
        than the one that will be charged. Only ENABLED providers are listed and no
        credential path is ever projected. An unresolvable host answers a constant
        404 that does not echo the host, so the endpoint cannot be used to enumerate
        orgs; a successful answer is cacheable for a minute.
      operationId: get_commerce_org
      summary: The public org configuration a checkout page boots from
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/payments:
    post:
      description: |-
        Takes a payment: charges a single-use card token and credits the caller's org
        balance, exactly once.

        This is the operation behind "collect money from a customer". It runs the SAME
        core the console's card top-up runs (commerce billing.TakePayment), so the
        server-side amount bounds, the idempotency guard and the ledger credit are
        shared rather than reimplemented — a second charge path would eventually
        double-charge somebody.

        The ORG is the caller's, taken from the validated principal and never from the
        input, so a payment can only ever credit the account of whoever made the call.

        A payment is RISK-SCREENED before the card is charged, so this can be refused
        without any money moving: 403 means the screen did not authorise it, and 503 means
        the screen could not reach a decision — that one is worth retrying, and no charge
        was attempted either way.

        Send an idempotencyKey. An agent retries by construction, and the key is what
        turns a retry into a replay of the first receipt instead of a second charge.

        The answer states whether it settled in SANDBOX or live mode (`test`), and
        carries the processor's own reference (`processorRef`) so the charge can be
        reconciled against the processor rather than taken on trust.

        A named builder, not a closure, so zipdoc can lift this prose into the registry.

        It BUILDS the handler rather than being it, because the screen has to sit inside
        the value every projection of this op dispatches to — see exposePayments. `charge`
        is the money move, `take` is the screened door onto it, and the only registrable
        one is the second.
      operationId: takePayment
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PaymentIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentOut'
          description: created
      summary: Take a card payment and credit the org's balance
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/payments/{id}:
    get:
      description: |-
        Reads one settled payment out of the caller's org ledger.

        The org scopes the read by construction — the ledger is namespaced to it — so
        an id belonging to another tenant is simply not found rather than found and
        then filtered. A ledger row that is not a payment is likewise not found, so
        this cannot be used to walk the org's usage debits.

        A named handler, not a closure, so zipdoc can lift this prose into the registry.
      operationId: getPayment
      parameters:
      - description: ID is the ledger transaction id a payment returned.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PaymentRecord'
          description: ok
      summary: Read one settled payment by its id
      tags:
      - commerce
      x-app: commerce
      x-public: true
      x-tool: true
  /v1/commerce/plans/entries:
    get:
      description: 'Returns every plan row as stored — the administrative view behind
        the public plan catalog. The plan authority is cross-tenant pricing data,
        so the gate is a PLATFORM admin enforced by the handler itself: an org-level
        admin is refused 403 no matter what they may do inside their own org.'
      operationId: get_commerce_plans_entries
      summary: The raw plan authority rows
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: Creates a plan from the body and answers it at 201. The slug is
        required and globally unique — a duplicate is 409 — and the row is marked
        authoritative on creation, so the corrective seed will leave it alone. Price,
        annual price and the contact-sales flag are stored exactly as sent, never
        coerced, so the difference between a free plan and a quote-only plan survives.
        PLATFORM admin only.
      operationId: post_commerce_plans_entries
      summary: Add a subscription plan
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/plans/entries/{slug}:
    delete:
      description: Deletes the addressed plan and answers 204. It removes the plan
        from the catalog buyers choose from; it does not touch subscriptions already
        sold against it, which keep their stored plan id. PLATFORM admin only — an
        org-level admin is refused 403 — and an unknown slug is 404.
      operationId: delete_commerce_plans_entries_by_slug
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      summary: Remove a plan from the authority
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'Loads the addressed plan, applies the body over it and answers
        the stored result, so a partial edit never silently zeroes a price or the
        contact-sales flag. The slug is IMMUTABLE: a body naming a different slug
        is rejected outright before anything is written, because a rename would orphan
        every subscription that stored the old id — deprecate and create instead.
        An admin edit marks the row authoritative so the seed stops correcting it.
        PLATFORM admin only; an unknown slug is 404.'
      operationId: put_commerce_plans_entries_by_slug
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      summary: Edit a plan, leaving the fields you omit alone
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/plans/seed:
    post:
      description: Upserts the shipped plan rows and answers how many were created
        and how many corrected. It is idempotent and non-destructive — a row an administrator
        authored or edited is left as it stands — so it is safe against a live authority
        and fills only what is missing or has drifted. PLATFORM admin only, and a
        deployment with no seed source wired answers 500 rather than quietly seeding
        nothing.
      operationId: post_commerce_plans_seed
      summary: Seed the embedded plan catalog, without overwriting administrative
        edits
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/product/:
    get:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Answers a pagination envelope — the
        page and display echoed back, the rows under models, a total count and a facets
        array — read from the caller org''s own namespaced store, so one tenant can
        never list another''s. Sorting defaults to the slug and is overridable with
        sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. Any valid access
        token reaches it. The org must also be entitled to the commerce admin: the
        paywall answers 402 subscription_required unless the org holds an active or
        trialing pro subscription, a live trial credit or a redeemed invite, and 503
        when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.
        The token must also carry Admin or the Product list scope.'
      operationId: get_commerce_product
      summary: List your org's products, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Decodes the body into a new row in
        the caller org''s own namespaced store — isolated to that tenant from its
        first write — and answers the stored row at 201 with a Location header naming
        its id. The id is assigned by the store, not taken from the body. A body that
        fails to decode is 400 and a store that refuses the write is 500. Any valid
        access token reaches it. The org must also be entitled to the commerce admin:
        the paywall answers 402 subscription_required unless the org holds an active
        or trialing pro subscription, a live trial credit or a redeemed invite, and
        503 when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.
        The token must also carry Admin or WriteProduct.'
      operationId: post_commerce_product
      summary: Create a product
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/product/{productid}:
    delete:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Removes the addressed row and answers
        204 with no body. Before the live row goes it is written once more under a
        deleted tombstone kind, so a deletion leaves a recoverable copy rather than
        destroying the record outright — and a tombstone that cannot be written fails
        the call with 500 before anything is removed. The id is resolved inside the
        caller org''s own namespace, so an absent or foreign id is 404. Any valid
        access token reaches it. The org must also be entitled to the commerce admin:
        the paywall answers 402 subscription_required unless the org holds an active
        or trialing pro subscription, a live trial credit or a redeemed invite, and
        503 when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.
        The token must also carry Admin or WriteProduct.'
      operationId: delete_commerce_product_by_productid
      parameters:
      - in: path
        name: productid
        required: true
        schema:
          type: string
      summary: Delete a product, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Reads the addressed row from the caller
        org''s own namespaced store. An id that is not there is 404 — and another
        tenant''s id is not there by construction, so it reads exactly like a typo
        instead of confirming the row exists somewhere else. Any valid access token
        reaches it. The org must also be entitled to the commerce admin: the paywall
        answers 402 subscription_required unless the org holds an active or trialing
        pro subscription, a live trial credit or a redeemed invite, and 503 when that
        entitlement cannot be read rather than admitting on an unknown. The internal
        service token and a platform superadmin pass straight through. The token must
        also carry Admin or ReadProduct.'
      operationId: get_commerce_product_by_productid
      parameters:
      - in: path
        name: productid
        required: true
        schema:
          type: string
      summary: Fetch one product
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Loads the stored row and decodes the
        body OVER it, so only the fields the body names change and everything else
        keeps its stored value — the difference from the full replace, which clears
        what it is not told. Answers the merged row. An id absent from the caller
        org''s namespace is 404 and a body that fails to decode is 400. Any valid
        access token reaches it. The org must also be entitled to the commerce admin:
        the paywall answers 402 subscription_required unless the org holds an active
        or trialing pro subscription, a live trial credit or a redeemed invite, and
        503 when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.
        The token must also carry Admin, or ReadProduct and WriteProduct together.'
      operationId: patch_commerce_product_by_productid
      parameters:
      - in: path
        name: productid
        required: true
        schema:
          type: string
      summary: Change part of a product
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. Re-dispatches the request into the
        handler the intended verb would have reached, taking that verb from a _method
        form value or query parameter and then from the X-HTTP-Method-Override header.
        PUT replaces the row, PATCH changes part of it, DELETE removes it, and anything
        else is 405. The trap is the DEFAULT: naming no override at all leaves the
        method POST, which this tunnel maps to the PARTIAL UPDATE — it is never a
        create, and creating is the collection root''s job. Behaviour and authorization
        are the underlying operation''s, since the real handler runs. Any valid access
        token reaches it. The org must also be entitled to the commerce admin: the
        paywall answers 402 subscription_required unless the org holds an active or
        trialing pro subscription, a live trial credit or a redeemed invite, and 503
        when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.'
      operationId: post_commerce_product_by_productid
      parameters:
      - in: path
        name: productid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a product — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A product is a sellable catalog item: slug, SKU and UPC, name
        and copy, media, availability and preorder flags, a reservation block, and
        its money — currency, price, MSRP, list price and inventory cost in minor
        units, inventory count, taxability, and the subscription interval when it
        is subscribeable. Its variants and options are carried as a denormalized JSON
        snapshot inside the product, separate from the standalone variant rows, and
        nothing keeps the two in step for you. This is a true REPLACEMENT, not a merge:
        the stored row''s key is preserved, but the body is decoded onto a FRESH entity,
        so every field the body omits is written back as its ZERO value. Patch is
        the verb for changing part of a row. The id is resolved inside the caller
        org''s own namespace and an absent one is 404 before anything is written;
        a body that fails to decode is 400. Answers the stored result. Any valid access
        token reaches it. The org must also be entitled to the commerce admin: the
        paywall answers 402 subscription_required unless the org holds an active or
        trialing pro subscription, a live trial credit or a redeemed invite, and 503
        when that entitlement cannot be read rather than admitting on an unknown.
        The internal service token and a platform superadmin pass straight through.
        The token must also carry Admin, or ReadProduct and WriteProduct together.'
      operationId: put_commerce_product_by_productid
      parameters:
      - in: path
        name: productid
        required: true
        schema:
          type: string
      summary: Replace a product outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/return/:
    get:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Answers a pagination envelope
        — the page and display echoed back, the rows under models, a total count and
        a facets array — read from the caller org''s own namespaced store, so one
        tenant can never list another''s. Sorting defaults to the last-updated time
        and is overridable with sort. display is the page size and page applies only
        alongside it; either one that is not a positive integer is refused with 500
        rather than silently ignored, and the limit query overrides the reported COUNT
        only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        Any valid access token reaches it. The token must also carry Admin or the
        Return list scope.'
      operationId: get_commerce_return
      summary: List your org's returns, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Decodes the body into a new row
        in the caller org''s own namespaced store — isolated to that tenant from its
        first write — and answers the stored row at 201 with a Location header naming
        its id. The id is assigned by the store, not taken from the body. A body that
        fails to decode is 400 and a store that refuses the write is 500. Any valid
        access token reaches it. The token must also carry Admin or WriteReturn.'
      operationId: post_commerce_return
      summary: Create a return
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/return/{returnid}:
    delete:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Removes the addressed row and
        answers 204 with no body. Before the live row goes it is written once more
        under a deleted tombstone kind, so a deletion leaves a recoverable copy rather
        than destroying the record outright — and a tombstone that cannot be written
        fails the call with 500 before anything is removed. The id is resolved inside
        the caller org''s own namespace, so an absent or foreign id is 404. Any valid
        access token reaches it. The token must also carry Admin or WriteReturn.'
      operationId: delete_commerce_return_by_returnid
      parameters:
      - in: path
        name: returnid
        required: true
        schema:
          type: string
      summary: Delete a return, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Reads the addressed row from the
        caller org''s own namespaced store. An id that is not there is 404 — and another
        tenant''s id is not there by construction, so it reads exactly like a typo
        instead of confirming the row exists somewhere else. Any valid access token
        reaches it. The token must also carry Admin or ReadReturn.'
      operationId: get_commerce_return_by_returnid
      parameters:
      - in: path
        name: returnid
        required: true
        schema:
          type: string
      summary: Fetch one return
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Loads the stored row and decodes
        the body OVER it, so only the fields the body names change and everything
        else keeps its stored value — the difference from the full replace, which
        clears what it is not told. Answers the merged row. An id absent from the
        caller org''s namespace is 404 and a body that fails to decode is 400. Any
        valid access token reaches it. The token must also carry Admin, or ReadReturn
        and WriteReturn together.'
      operationId: patch_commerce_return_by_returnid
      parameters:
      - in: path
        name: returnid
        required: true
        schema:
          type: string
      summary: Change part of a return
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. Re-dispatches the request into
        the handler the intended verb would have reached, taking that verb from a
        _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        Any valid access token reaches it.'
      operationId: post_commerce_return_by_returnid
      parameters:
      - in: path
        name: returnid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a return — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A return is an RMA — the store, user and order it belongs to,
        the line items coming back, a fulfillment block carrying its own type, status
        and pricing, a summary, and eight lifecycle timestamps from submitted through
        delivered and processed. Its status is a FREE STRING with no enumeration behind
        it, and there is no refund amount on the return itself: the money sits inside
        the line items and the fulfillment pricing. This is a true REPLACEMENT, not
        a merge: the stored row''s key is preserved, but the body is decoded onto
        a FRESH entity, so every field the body omits is written back as its ZERO
        value. Patch is the verb for changing part of a row. The id is resolved inside
        the caller org''s own namespace and an absent one is 404 before anything is
        written; a body that fails to decode is 400. Answers the stored result. Any
        valid access token reaches it. The token must also carry Admin, or ReadReturn
        and WriteReturn together.'
      operationId: put_commerce_return_by_returnid
      parameters:
      - in: path
        name: returnid
        required: true
        schema:
          type: string
      summary: Replace a return outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/saleschannel/:
    get:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Answers
        a pagination envelope — the page and display echoed back, the rows under models,
        a total count and a facets array — read from the caller org''s own namespaced
        store, so one tenant can never list another''s. Sorting defaults to the last-updated
        time and is overridable with sort. display is the page size and page applies
        only alongside it; either one that is not a positive integer is refused with
        500 rather than silently ignored, and the limit query overrides the reported
        COUNT only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        Any valid access token reaches it. The org must also be entitled to the commerce
        admin: the paywall answers 402 subscription_required unless the org holds
        an active or trialing pro subscription, a live trial credit or a redeemed
        invite, and 503 when that entitlement cannot be read rather than admitting
        on an unknown. The internal service token and a platform superadmin pass straight
        through. The per-kind permission table has no entry for saleschannel, so the
        scaffold skips that second check with a warning and the gate above is the
        whole authorization story.'
      operationId: get_commerce_saleschannel
      summary: List your org's sales channels, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Decodes
        the body into a new row in the caller org''s own namespaced store — isolated
        to that tenant from its first write — and answers the stored row at 201 with
        a Location header naming its id. The id is assigned by the store, not taken
        from the body. A body that fails to decode is 400 and a store that refuses
        the write is 500. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for saleschannel, so the scaffold skips that second check with a warning and
        the gate above is the whole authorization story.'
      operationId: post_commerce_saleschannel
      summary: Create a sales channel
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/saleschannel/{saleschannelid}:
    delete:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Removes
        the addressed row and answers 204 with no body. Before the live row goes it
        is written once more under a deleted tombstone kind, so a deletion leaves
        a recoverable copy rather than destroying the record outright — and a tombstone
        that cannot be written fails the call with 500 before anything is removed.
        The id is resolved inside the caller org''s own namespace, so an absent or
        foreign id is 404. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for saleschannel, so the scaffold skips that second check with a warning and
        the gate above is the whole authorization story.'
      operationId: delete_commerce_saleschannel_by_saleschannelid
      parameters:
      - in: path
        name: saleschannelid
        required: true
        schema:
          type: string
      summary: Delete a sales channel, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Reads
        the addressed row from the caller org''s own namespaced store. An id that
        is not there is 404 — and another tenant''s id is not there by construction,
        so it reads exactly like a typo instead of confirming the row exists somewhere
        else. Any valid access token reaches it. The org must also be entitled to
        the commerce admin: the paywall answers 402 subscription_required unless the
        org holds an active or trialing pro subscription, a live trial credit or a
        redeemed invite, and 503 when that entitlement cannot be read rather than
        admitting on an unknown. The internal service token and a platform superadmin
        pass straight through. The per-kind permission table has no entry for saleschannel,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: get_commerce_saleschannel_by_saleschannelid
      parameters:
      - in: path
        name: saleschannelid
        required: true
        schema:
          type: string
      summary: Fetch one sales channel
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Loads
        the stored row and decodes the body OVER it, so only the fields the body names
        change and everything else keeps its stored value — the difference from the
        full replace, which clears what it is not told. Answers the merged row. An
        id absent from the caller org''s namespace is 404 and a body that fails to
        decode is 400. Any valid access token reaches it. The org must also be entitled
        to the commerce admin: the paywall answers 402 subscription_required unless
        the org holds an active or trialing pro subscription, a live trial credit
        or a redeemed invite, and 503 when that entitlement cannot be read rather
        than admitting on an unknown. The internal service token and a platform superadmin
        pass straight through. The per-kind permission table has no entry for saleschannel,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: patch_commerce_saleschannel_by_saleschannelid
      parameters:
      - in: path
        name: saleschannelid
        required: true
        schema:
          type: string
      summary: Change part of a sales channel
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. Re-dispatches
        the request into the handler the intended verb would have reached, taking
        that verb from a _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        Any valid access token reaches it. The org must also be entitled to the commerce
        admin: the paywall answers 402 subscription_required unless the org holds
        an active or trialing pro subscription, a live trial credit or a redeemed
        invite, and 503 when that entitlement cannot be read rather than admitting
        on an unknown. The internal service token and a platform superadmin pass straight
        through.'
      operationId: post_commerce_saleschannel_by_saleschannelid
      parameters:
      - in: path
        name: saleschannelid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a sales channel — for clients that cannot
        send PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A sales channel is a named selling surface — a name, a description,
        a disabled flag and metadata. The flag is NEGATIVE, so a channel created from
        an empty body is enabled. Nothing on this row links products, prices or stock
        to the channel; here it is a label other surfaces scope themselves by. This
        is a true REPLACEMENT, not a merge: the stored row''s key is preserved, but
        the body is decoded onto a FRESH entity, so every field the body omits is
        written back as its ZERO value. Patch is the verb for changing part of a row.
        The id is resolved inside the caller org''s own namespace and an absent one
        is 404 before anything is written; a body that fails to decode is 400. Answers
        the stored result. Any valid access token reaches it. The org must also be
        entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for saleschannel, so the scaffold skips that second check with a warning and
        the gate above is the whole authorization story.'
      operationId: put_commerce_saleschannel_by_saleschannelid
      parameters:
      - in: path
        name: saleschannelid
        required: true
        schema:
          type: string
      summary: Replace a sales channel outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/stocklocation/:
    get:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Answers a pagination envelope — the page and display echoed back, the
        rows under models, a total count and a facets array — read from the caller
        org''s own namespaced store, so one tenant can never list another''s. Sorting
        defaults to the last-updated time and is overridable with sort. display is
        the page size and page applies only alongside it; either one that is not a
        positive integer is refused with 500 rather than silently ignored, and the
        limit query overrides the reported COUNT only, never the rows returned. No
        search backend is wired, so the datastore is the one and only list path and
        facets is always empty. A request resolving no org namespace is served an
        EMPTY page rather than an unscoped scan: the namespace IS the tenant filter,
        so without one there is nothing safe to return. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through. The per-kind permission table
        has no entry for stocklocation, so the scaffold skips that second check with
        a warning and the gate above is the whole authorization story.'
      operationId: get_commerce_stocklocation
      summary: List your org's stock locations, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Decodes the body into a new row in the caller org''s own namespaced store
        — isolated to that tenant from its first write — and answers the stored row
        at 201 with a Location header naming its id. The id is assigned by the store,
        not taken from the body. A body that fails to decode is 400 and a store that
        refuses the write is 500. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for stocklocation, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: post_commerce_stocklocation
      summary: Create a stock location
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/stocklocation/{stocklocationid}:
    delete:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Removes the addressed row and answers 204 with no body. Before the live
        row goes it is written once more under a deleted tombstone kind, so a deletion
        leaves a recoverable copy rather than destroying the record outright — and
        a tombstone that cannot be written fails the call with 500 before anything
        is removed. The id is resolved inside the caller org''s own namespace, so
        an absent or foreign id is 404. Any valid access token reaches it. The org
        must also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for stocklocation, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: delete_commerce_stocklocation_by_stocklocationid
      parameters:
      - in: path
        name: stocklocationid
        required: true
        schema:
          type: string
      summary: Delete a stock location, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Reads the addressed row from the caller org''s own namespaced store.
        An id that is not there is 404 — and another tenant''s id is not there by
        construction, so it reads exactly like a typo instead of confirming the row
        exists somewhere else. Any valid access token reaches it. The org must also
        be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for stocklocation, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: get_commerce_stocklocation_by_stocklocationid
      parameters:
      - in: path
        name: stocklocationid
        required: true
        schema:
          type: string
      summary: Fetch one stock location
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Loads the stored row and decodes the body OVER it, so only the fields
        the body names change and everything else keeps its stored value — the difference
        from the full replace, which clears what it is not told. Answers the merged
        row. An id absent from the caller org''s namespace is 404 and a body that
        fails to decode is 400. Any valid access token reaches it. The org must also
        be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for stocklocation, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: patch_commerce_stocklocation_by_stocklocationid
      parameters:
      - in: path
        name: stocklocationid
        required: true
        schema:
          type: string
      summary: Change part of a stock location
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. Re-dispatches the request into the handler the intended verb would have
        reached, taking that verb from a _method form value or query parameter and
        then from the X-HTTP-Method-Override header. PUT replaces the row, PATCH changes
        part of it, DELETE removes it, and anything else is 405. The trap is the DEFAULT:
        naming no override at all leaves the method POST, which this tunnel maps to
        the PARTIAL UPDATE — it is never a create, and creating is the collection
        root''s job. Behaviour and authorization are the underlying operation''s,
        since the real handler runs. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through.'
      operationId: post_commerce_stocklocation_by_stocklocationid
      parameters:
      - in: path
        name: stocklocationid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a stock location — for clients that cannot
        send PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A stock location is a physical address inventory can be held at
        — a name, street lines, city, province, country, postal code and a phone.
        None of it is validated, there are no coordinates, and the row carries no
        enabled flag and no inventory link, so deleting it is the only way to retire
        one. This is a true REPLACEMENT, not a merge: the stored row''s key is preserved,
        but the body is decoded onto a FRESH entity, so every field the body omits
        is written back as its ZERO value. Patch is the verb for changing part of
        a row. The id is resolved inside the caller org''s own namespace and an absent
        one is 404 before anything is written; a body that fails to decode is 400.
        Answers the stored result. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The per-kind permission table has no entry
        for stocklocation, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: put_commerce_stocklocation_by_stocklocationid
      parameters:
      - in: path
        name: stocklocationid
        required: true
        schema:
          type: string
      summary: Replace a stock location outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/:
    get:
      description: Answers a pagination envelope — page, display, the rows, and a
        total count — read from the caller org's OWN namespaced database, so one tenant
        can never list another's stores. Sorting defaults to the store slug and is
        overridable with sort; display is the page size and page applies only alongside
        it, and either one that is not a positive integer is refused rather than silently
        ignored. The limit query overrides the reported COUNT only and never the rows
        returned. A request that resolves no org namespace is served an empty page,
        never an unscoped scan. Readable with an admin token, a store-scoped token,
        or the anonymous published storefront key.
      operationId: get_commerce_store
      summary: List your org's storefronts as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'Creates a store from the body inside the caller org''s own namespaced
        database, so the row is physically isolated to that tenant from its first
        write, and answers it at 201 with a Location header naming its id. Requires
        an admin or store-write token: the anonymous published storefront key may
        READ stores but never create one. A body that fails to decode is 400.'
      operationId: post_commerce_store
      summary: Create a storefront
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}:
    delete:
      description: Removes the addressed store and answers 204 with no body. Before
        the live row goes, the entity is written once more under a tombstone kind,
        so the deletion leaves a recoverable copy rather than destroying the record
        outright; the store's listing overrides live inside that row and go with it.
        The id is resolved inside the caller org's own namespace, so an unknown or
        foreign id is 404. Requires an admin or store-write token.
      operationId: delete_commerce_store_by_storeid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Delete a storefront, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: Reads the addressed store from the caller org's own namespaced
        database, so an id belonging to another tenant is simply absent there and
        answers 404 rather than leaking its existence. The body is the stored entity
        including its embedded listing override map. Readable with an admin or store-read
        token and also with the anonymous published storefront key, which is what
        lets a logged-out storefront resolve the store it is rendering.
      operationId: get_commerce_store_by_storeid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Fetch one storefront
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: Loads the stored store and decodes the body over it, so only the
        fields the body names change and everything else keeps its stored value —
        the difference from the full replace, which clears what it is not told. Answers
        the merged entity. The id is resolved inside the caller org's own namespace,
        so an unknown or foreign id is 404. Requires an admin token, or one holding
        both store read and store write.
      operationId: patch_commerce_store_by_storeid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Change part of a storefront
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'Re-dispatches the request into the handler the intended verb would
        have reached, taking that verb from a _method form value or query parameter
        and then from the X-HTTP-Method-Override header, the header winning when both
        are present. Only PUT, PATCH and DELETE are accepted; anything else resolves
        to 405. The trap is the default: naming NO override at all is treated as a
        partial update, never as a create. Authorization is whatever the underlying
        operation requires, since the real handler runs.'
      operationId: post_commerce_store_by_storeid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for clients that cannot send PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'This is a true REPLACEMENT, not a merge: the stored key is preserved
        but the body is decoded onto a fresh entity, so every field the body omits
        is written back as its zero value. Use the partial update when you mean to
        change part of a store. The id is resolved inside the caller org''s own namespace,
        so an unknown or foreign id is a 404 before anything is written. Requires
        an admin token, or one holding both store read and store write.'
      operationId: put_commerce_store_by_storeid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Replace a storefront outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/authorize:
    post:
      description: Tallies a new order for the addressed store from the user, payment
        and order body, reserves its items, runs the processor authorization and answers
        the saved order with a Location header pointing at it. The gate is a token
        carrying admin or published scope, so a published storefront key is enough;
        no token is 401 and a token with neither bit is 403. The store is loaded BEFORE
        any payment work and its currency OVERRIDES whatever the body asked for, so
        a store that will not load ends the call with 500 and nothing is charged.
        On any authorization failure the reservations are released and the order and
        payment are persisted as cancelled, so a failed attempt still leaves a durable
        record. Capture is a separate call.
      operationId: post_commerce_store_by_storeid_authorize
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Authorize a new order against a storefront, holding the funds without
        settling them
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/authorize/{orderid}:
    post:
      description: Continues the order named in the path rather than minting a new
        one, holding funds for it. The order is loaded from the caller org's own store,
        so an id belonging to another tenant is a 404. The rule most callers get wrong
        is that the body's order object is MERGED onto the loaded order before the
        tally — this is not a read-only reference, and a field sent here overwrites
        what is stored. The gate, the store resolution and the currency override behave
        exactly as on the bodiless-id sibling, and settling is still the capture call's
        job.
      operationId: post_commerce_store_by_storeid_authorize_by_orderid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: orderid
        required: true
        schema:
          type: string
      summary: Authorize an order that already exists, holding the funds without settling
        them
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/bundle/{key}:
    get:
      description: 'Returns the stored bundle with the store''s listing for it laid
        over the top — every non-empty listing field wins, and the currency is forced
        to the store''s own — so the caller reads what this storefront actually sells
        rather than the catalog-wide record. The overlay is keyed by the item''s ID:
        a listing filed only under a slug or SKU does not reach it, unlike the listing
        reads, which do fall back to those. An unknown store or key is 404. Readable
        with an admin token or the anonymous published storefront key.'
      operationId: get_commerce_store_by_storeid_bundle_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Fetch a bundle as this storefront sells it
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/capture/{orderid}:
    post:
      description: 'Settles the order named in the path — the second half of the two-step
        flow — and answers the updated order with a Location header. Dispatch follows
        the order''s STORED payment type, and a successful capture is the moment the
        rest of the system learns about the sale: order and payment rows are updated,
        coupon redemptions, referral, cart and stats are written, the confirmation
        email goes out, and the paid and completed events are emitted. A capture failure
        releases the order''s inventory reservations and answers 400, so a failed
        settlement never leaves items held.'
      operationId: post_commerce_store_by_storeid_capture_by_orderid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: orderid
        required: true
        schema:
          type: string
      summary: Capture a previously authorized order and settle the payment
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/charge:
    post:
      description: 'Runs authorization and capture back to back against a freshly
        created order — the one-step flow for callers with no reason to hold funds.
        It takes the authorize body and inherits every authorize rule: the store''s
        currency wins over the body, the items are reserved before the processor is
        called, and the amount bounds the processor enforces still apply. There is
        no order id on this address, so it can never continue an existing order. Either
        half failing answers 400, and the capture side effects — confirmation email,
        redemptions, stats, the paid and completed events — run only when both halves
        succeed.'
      operationId: post_commerce_store_by_storeid_charge
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Authorize and capture a new order in one call
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/authorize:
    post:
      description: 'Authorizes a new order for the addressed store and holds the funds,
        answering the saved order with a Location header. It binds the identical handler
        as the shorter authorize address, so the two are ONE operation at two spellings
        and not two behaviours; the checkout prefix is the newer one. Every rule carries
        over: admin or published scope on the token, the store loaded first with its
        currency overriding the body, items reserved before the processor call, and
        reservations released with the order persisted cancelled on failure. Nothing
        is settled here.'
      operationId: post_commerce_store_by_storeid_checkout_authorize
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Authorize a new order against a storefront, holding the funds — the
        checkout spelling
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/authorize/{orderid}:
    post:
      description: Continues the order named in the path rather than minting one,
        and shares its handler byte for byte with the unprefixed authorize-by-id address.
        The order is loaded from the caller org's own store, so another tenant's id
        is a 404, and the body's order object is merged onto the loaded row before
        the tally — a field sent here overwrites what is stored. Store resolution,
        the token gate and the currency override behave as on every other authorize
        address; settle with the capture address and the same order id.
      operationId: post_commerce_store_by_storeid_checkout_authorize_by_orderid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: orderid
        required: true
        schema:
          type: string
      summary: Authorize an existing order, holding the funds — the checkout spelling
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/capture/{orderid}:
    post:
      description: Settles the authorized order named in the path and answers the
        updated order with a Location header, running the same handler as the unprefixed
        capture address. Dispatch follows the order's stored payment type. Success
        is what triggers the downstream work — order and payment updates, redemptions,
        referral, cart and stats, the confirmation email, and the paid and completed
        events — while a failure releases the order's inventory reservations and answers
        400.
      operationId: post_commerce_store_by_storeid_checkout_capture_by_orderid
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: orderid
        required: true
        schema:
          type: string
      summary: Capture a previously authorized order and settle it — the checkout
        spelling
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/charge:
    post:
      description: Performs authorization and capture back to back against a newly
        created order for the addressed store, on the same handler as the unprefixed
        charge address. It takes the authorize body and inherits every authorize rule,
        including the store's currency winning over the body and the items being reserved
        before the processor is called. There is no order id on this address, so it
        can never continue an existing order. Either half failing answers 400, and
        the capture side effects run only when both succeed.
      operationId: post_commerce_store_by_storeid_checkout_charge
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Authorize and capture a new order in one call — the checkout spelling
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/paypal/cancel/{payKey}:
    post:
      description: Meant to void the payments carrying the given pay key, stamp them
        cancelled and cancel the order, but the shared checkout handler resolves its
        order from an ORDER ID path parameter this route does not carry. The result
        is an untyped order and a cancel dispatch that refuses with 400 before the
        pay key lookup ever runs. Token gate, namespacing and store resolution happen
        first, so a missing token is still 401 and an unloadable store still 500.
        It is the same handler as the unprefixed cancel address, with the same outcome.
      operationId: post_commerce_store_by_storeid_checkout_paypal_cancel_by_paykey
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: payKey
        required: true
        schema:
          type: string
      summary: PayPal cancel by pay key — refuses, exactly as the unprefixed address
        does
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/paypal/confirm/{payKey}:
    post:
      description: 'Meant to mark the payments carrying the given pay key as paid
        and set the order to paid, it cannot reach that work from this address: the
        shared checkout handler takes its order from an ORDER ID path parameter this
        route does not carry, so the order is always fresh and untyped and the confirm
        dispatch refuses with 400 before the pay key is queried. The token gate, the
        namespace middleware and the store lookup all run ahead of that, so authentication
        and store failures surface first. Behaviour is identical to the unprefixed
        confirm address; the checkout prefix changes nothing here.'
      operationId: post_commerce_store_by_storeid_checkout_paypal_confirm_by_paykey
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: payKey
        required: true
        schema:
          type: string
      summary: PayPal confirm by pay key — refuses, exactly as the unprefixed address
        does
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/checkout/paypal/pay:
    post:
      description: Begins a PayPal authorization by running the ordinary store authorize
        flow, since the route binds that exact handler — body, store resolution, tally,
        reservations and failure behaviour are the authorize address's, unchanged.
        The processor is chosen from the body's payment type, so this path reaches
        PayPal only when that type says so. A successful PayPal authorization stamps
        a pay key onto the payment, which is the key the confirm and cancel addresses
        filter on. Build against the plain authorize address instead.
      operationId: post_commerce_store_by_storeid_checkout_paypal_pay
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Start a PayPal authorization for a new order — the checkout spelling
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/listing:
    get:
      description: 'Returns every override this store applies to catalog items — name,
        price, list price, media, availability and the hidden flag — keyed by product
        or variant id, in one read. A listing is an OVERRIDE, not a product: the catalog
        item exists independently and this map only says how this storefront presents
        it. Read from the caller org''s own namespaced database, so a store id belonging
        to another tenant is 404. Readable with an admin token or the anonymous published
        storefront key.'
      operationId: get_commerce_store_by_storeid_listing
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: The storefront's whole listing override map
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/listing/{key}:
    delete:
      description: 'Drops the key from the store''s listing map and re-saves the store,
        answering 204 with no body. It UN-OVERRIDES rather than deletes: the product,
        variant or bundle itself is untouched and simply reverts to its catalog values
        on this storefront. A key that is not present is 404, and so is a store id
        outside the caller org''s namespace. Admin-gated.'
      operationId: delete_commerce_store_by_storeid_listing_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Remove a listing override
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: Looks the key up in the store's listing map first and, failing
        that, matches it against each listing's slug and then its SKU — so a storefront
        holding only a product's URL slug can still resolve the override. That fallback
        is unique to the listing reads; the item overlay routes match by id alone.
        A key matching none of the three is 404, as is a store id outside the caller
        org's namespace. Readable with an admin token or the anonymous published storefront
        key.
      operationId: get_commerce_store_by_storeid_listing_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Fetch one listing override, by item id or by its slug or SKU
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'Requires the key to already be present — an absent one is 404
        — and answers the store''s listing map at 200. Read the behaviour before relying
        on it: the decoded body is applied to a COPY taken out of the map and is never
        assigned back, so the stored listing is unchanged and the map returned is
        exactly the map that was already there. An actual edit to an existing listing
        has to go through the upsert, which does write its result back into the store.
        A body that fails to decode is still 400. Admin-gated and namespaced to the
        caller''s org.'
      operationId: patch_commerce_store_by_storeid_listing_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Confirm a listing override exists and re-save the store
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'Creates the override and answers the store''s ENTIRE listing map
        at 201 with a Location header — not just the entry that was added. A key already
        present is refused 400: creation never silently overwrites, so changing an
        existing listing has to be an explicit replace. The stored listing has its
        currency stamped from the store''s own, which the replace path does not do.
        The key is matched exactly here, with none of the slug or SKU fallback the
        read allows. Admin-gated and resolved inside the caller org''s namespace.'
      operationId: post_commerce_store_by_storeid_listing_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Add a listing override under a new key
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: Decodes the body over the existing listing when the key is present,
        so fields it omits keep their stored values, and builds the listing from the
        body alone when the key is new. Answers 200 when it replaced something and
        201 with a Location header when it created it; either way the body is the
        store's entire listing map, not the single entry. Unlike creation, this path
        does NOT restamp the listing's currency from the store. Admin-gated, with
        the store resolved inside the caller org's namespace.
      operationId: put_commerce_store_by_storeid_listing_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Upsert a listing override
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/paypal/cancel/{payKey}:
    post:
      description: 'Intended to void the payments carrying the given pay key, stamp
        them cancelled and cancel the order, it never reaches that work: the shared
        checkout handler reads its order from an ORDER ID path parameter this route
        does not carry, leaving an untyped order that the cancel dispatch refuses
        with 400 before the pay key lookup runs. Authentication, namespacing and store
        resolution happen ahead of the refusal, so a missing token is 401 and an unloadable
        store 500. Cancelling a real PayPal authorization needs an address that carries
        the order id.'
      operationId: post_commerce_store_by_storeid_paypal_cancel_by_paykey
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: payKey
        required: true
        schema:
          type: string
      summary: PayPal cancel by pay key — refuses, because a pay key alone does not
        identify the order
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/paypal/confirm/{payKey}:
    post:
      description: 'Intended to mark every payment carrying the given pay key as paid
        and flip the order to paid, it cannot do that from this address and does not
        pretend to: the shared checkout handler resolves its order from an ORDER ID
        path parameter that this route does not carry, so it always works against
        a fresh untyped order and the confirm dispatch refuses it with 400 before
        the pay key is ever queried. The token gate, the namespace and the store lookup
        all run ahead of that, so a missing token is still 401 and an unloadable store
        still 500. Drive a PayPal return through an address that carries the order
        id.'
      operationId: post_commerce_store_by_storeid_paypal_confirm_by_paykey
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: payKey
        required: true
        schema:
          type: string
      summary: PayPal confirm by pay key — refuses, because a pay key alone does not
        identify the order
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/paypal/pay:
    post:
      description: Runs the ordinary store authorize flow — the route binds that very
        handler, so the body, the store resolution, the tally, the reservations and
        the failure behaviour are the authorize address's, unchanged. It reaches PayPal
        only when the body's payment type says so; nothing about this path forces
        the processor, so a card-typed payment posted here authorizes on the card
        processor instead. A successful PayPal authorization stamps a pay key onto
        the payment, which is the key the confirm and cancel addresses filter on.
        It is the older entry point; the plain authorize address is the one to build
        against.
      operationId: post_commerce_store_by_storeid_paypal_pay
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Start a PayPal authorization for a new order
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/product/{key}:
    get:
      description: Returns the stored product with the store's listing for it laid
        over the top — non-empty listing fields replace the catalog values and the
        currency is forced to the store's own — which is what lets two storefronts
        sell the same catalog product at their own price, name and media. The overlay
        is keyed by the product's ID, so a listing filed only under a slug or SKU
        does not apply here. An unknown store or key is 404. Readable with an admin
        token or the anonymous published storefront key.
      operationId: get_commerce_store_by_storeid_product_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Fetch a product as this storefront sells it
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/trial:
    post:
      description: 'Creates a trialing subscription for the addressed store on the
        entry plan and grants that plan''s trial credit, answering 201 when this call
        actually started one and 200 with a reason otherwise — not_new when the store
        already has billing history, trial_not_configured when no entry plan is wired.
        The window is always the SEVEN-DAY no-card trial, because this address never
        presents a card; the longer card-present window is reached only by adding
        a card afterwards. Entitlement is per store while the billing subject is the
        org, so every store an org owns takes its own trial. Admin-gated and namespaced
        to the caller''s org: no resolvable store is 404 with store_required, and
        a backing-store failure is 503.'
      operationId: post_commerce_store_by_storeid_trial
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      summary: Start this store's no-card trial on the entry plan
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/{storeid}/variant/{key}:
    get:
      description: Returns the stored variant with the store's listing for it overlaid
        — non-empty listing fields replace the catalog values and the currency is
        forced to the store's own — which is what makes per-storefront pricing of
        a shared variant possible. The overlay is keyed by the variant's ID, never
        by its slug or SKU. An unknown store or key is 404. Readable with an admin
        token or the anonymous published storefront key.
      operationId: get_commerce_store_by_storeid_variant_by_key
      parameters:
      - in: path
        name: storeid
        required: true
        schema:
          type: string
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Fetch a variant as this storefront sells it
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/access:
    get:
      description: 'Answers allowed, the store id, and a status of trial, active,
        payment_required, store_required or unavailable — the entitlement check a
        merchant surface gates on. The rule that surprises people is that entitlement
        is PER STORE, not per org: the store needs its own current subscription on
        the entry plan, either trialing with a trial end still ahead or active with
        a period end still ahead, so an org-wide balance or a sibling store''s plan
        unlocks nothing here. The store comes from the X-Store-Id header and otherwise
        falls back to the org''s first store; neither resolving is store_required
        with allowed false, and a backing-store failure is 503 with status unavailable
        — a retry signal, not a denial.'
      operationId: get_commerce_store_access
      summary: Whether a store is entitled to trade, and why
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/current:
    get:
      description: Returns the caller org's store resolved FROM THE AUTHENTICATED
        ORG rather than from a path id — which is how an admin dashboard or a storefront
        edge learns the store id it should then read and write against. An X-Store-Id
        header selects a specific store, resolved only inside the caller's own namespace,
        so a foreign id cannot cross the tenant boundary and answers 404 instead.
        With no header the org's first store is returned, and an org that has none
        yet has its canonical default provisioned lazily and idempotently, carrying
        no payment credentials. Only when there is no org in context, or provisioning
        fails, does it fall back to a placeholder store literally named default, which
        a storefront edge should treat as unconfigured.
      operationId: get_commerce_store_current
      summary: Resolve your org's active storefront without naming an id
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/store/token:
    post:
      description: 'Answers a freshly minted token carrying ONLY the published-read
        permission — enough for a logged-out shopper''s storefront to read your published
        catalog and nothing more, with no write and no admin scope. It is org-bound,
        signed with the org''s own secret and subject to the org id, so unlike a shared
        service token it can never act on another tenant. Minting ROTATES rather than
        accumulates: the previous storefront token is dropped first and is invalid
        immediately, so re-minting is how you revoke. Admin is enforced by the handler
        as well as the route, because the route''s token gate does not apply on the
        identity path and a plain member must not be able to mint their org''s key.'
      operationId: post_commerce_store_token
      summary: Mint your org's least-privilege storefront read key
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/submission/:
    get:
      description: 'A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form''s own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Answers a pagination envelope — the page and display echoed back, the rows
        under models, a total count and a facets array — read from the caller org''s
        own namespaced store, so one tenant can never list another''s. Sorting defaults
        to the last-updated time and is overridable with sort. display is the page
        size and page applies only alongside it; either one that is not a positive
        integer is refused with 500 rather than silently ignored, and the limit query
        overrides the reported COUNT only, never the rows returned. No search backend
        is wired, so the datastore is the one and only list path and facets is always
        empty. A request resolving no org namespace is served an EMPTY page rather
        than an unscoped scan: the namespace IS the tenant filter, so without one
        there is nothing safe to return. Any valid access token reaches it. The per-kind
        permission table has no entry for submission, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.'
      operationId: get_commerce_submission
      summary: List your org's submissions, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form's own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Decodes the body into a new row in the caller org's own namespaced store —
        isolated to that tenant from its first write — and answers the stored row
        at 201 with a Location header naming its id. The id is assigned by the store,
        not taken from the body. A body that fails to decode is 400 and a store that
        refuses the write is 500. Any valid access token reaches it. The per-kind
        permission table has no entry for submission, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.
      operationId: post_commerce_submission
      summary: Create a submission
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/submission/{submissionid}:
    delete:
      description: A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form's own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Removes the addressed row and answers 204 with no body. Before the live row
        goes it is written once more under a deleted tombstone kind, so a deletion
        leaves a recoverable copy rather than destroying the record outright — and
        a tombstone that cannot be written fails the call with 500 before anything
        is removed. The id is resolved inside the caller org's own namespace, so an
        absent or foreign id is 404. Any valid access token reaches it. The per-kind
        permission table has no entry for submission, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.
      operationId: delete_commerce_submission_by_submissionid
      parameters:
      - in: path
        name: submissionid
        required: true
        schema:
          type: string
      summary: Delete a submission, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form's own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Reads the addressed row from the caller org's own namespaced store. An id
        that is not there is 404 — and another tenant's id is not there by construction,
        so it reads exactly like a typo instead of confirming the row exists somewhere
        else. Any valid access token reaches it. The per-kind permission table has
        no entry for submission, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.
      operationId: get_commerce_submission_by_submissionid
      parameters:
      - in: path
        name: submissionid
        required: true
        schema:
          type: string
      summary: Fetch one submission
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form's own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Loads the stored row and decodes the body OVER it, so only the fields the
        body names change and everything else keeps its stored value — the difference
        from the full replace, which clears what it is not told. Answers the merged
        row. An id absent from the caller org's namespace is 404 and a body that fails
        to decode is 400. Any valid access token reaches it. The per-kind permission
        table has no entry for submission, so the scaffold skips that second check
        with a warning and the gate above is the whole authorization story.
      operationId: patch_commerce_submission_by_submissionid
      parameters:
      - in: path
        name: submissionid
        required: true
        schema:
          type: string
      summary: Change part of a submission
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form''s own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        Re-dispatches the request into the handler the intended verb would have reached,
        taking that verb from a _method form value or query parameter and then from
        the X-HTTP-Method-Override header. PUT replaces the row, PATCH changes part
        of it, DELETE removes it, and anything else is 405. The trap is the DEFAULT:
        naming no override at all leaves the method POST, which this tunnel maps to
        the PARTIAL UPDATE — it is never a create, and creating is the collection
        root''s job. Behaviour and authorization are the underlying operation''s,
        since the real handler runs. Any valid access token reaches it.'
      operationId: post_commerce_submission_by_submissionid
      parameters:
      - in: path
        name: submissionid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a submission — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A submission is one filled-in form from a site visitor — an email,
        an optional user id, the client details the server observed (user agent, referer,
        geography) and the form''s own fields as free metadata. It carries no form
        id, so the link back to the form that produced it is not stored on the row.
        This is a true REPLACEMENT, not a merge: the stored row''s key is preserved,
        but the body is decoded onto a FRESH entity, so every field the body omits
        is written back as its ZERO value. Patch is the verb for changing part of
        a row. The id is resolved inside the caller org''s own namespace and an absent
        one is 404 before anything is written; a body that fails to decode is 400.
        Answers the stored result. Any valid access token reaches it. The per-kind
        permission table has no entry for submission, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.'
      operationId: put_commerce_submission_by_submissionid
      parameters:
      - in: path
        name: submissionid
        required: true
        schema:
          type: string
      summary: Replace a submission outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/subscriber/:
    get:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Answers a pagination envelope — the page
        and display echoed back, the rows under models, a total count and a facets
        array — read from the caller org''s own namespaced store, so one tenant can
        never list another''s. Sorting defaults to the last-updated time and is overridable
        with sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. Any valid access
        token reaches it. The token must also carry Admin or the Subscriber list scope.'
      operationId: get_commerce_subscriber
      summary: List your org's subscribers, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Decodes the body into a new row in the caller
        org''s own namespaced store — isolated to that tenant from its first write
        — and answers the stored row at 201 with a Location header naming its id.
        The id is assigned by the store, not taken from the body. A body that fails
        to decode is 400 and a store that refuses the write is 500. Any valid access
        token reaches it. The token must also carry Admin or WriteSubscriber.'
      operationId: post_commerce_subscriber
      summary: Create a subscriber
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/subscriber/{subscriberid}:
    delete:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Removes the addressed row and answers 204
        with no body. Before the live row goes it is written once more under a deleted
        tombstone kind, so a deletion leaves a recoverable copy rather than destroying
        the record outright — and a tombstone that cannot be written fails the call
        with 500 before anything is removed. The id is resolved inside the caller
        org''s own namespace, so an absent or foreign id is 404. Any valid access
        token reaches it. The token must also carry Admin or WriteSubscriber.'
      operationId: delete_commerce_subscriber_by_subscriberid
      parameters:
      - in: path
        name: subscriberid
        required: true
        schema:
          type: string
      summary: Delete a subscriber, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Reads the addressed row from the caller org''s
        own namespaced store. An id that is not there is 404 — and another tenant''s
        id is not there by construction, so it reads exactly like a typo instead of
        confirming the row exists somewhere else. Any valid access token reaches it.
        The token must also carry Admin or ReadSubscriber.'
      operationId: get_commerce_subscriber_by_subscriberid
      parameters:
      - in: path
        name: subscriberid
        required: true
        schema:
          type: string
      summary: Fetch one subscriber
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Loads the stored row and decodes the body
        OVER it, so only the fields the body names change and everything else keeps
        its stored value — the difference from the full replace, which clears what
        it is not told. Answers the merged row. An id absent from the caller org''s
        namespace is 404 and a body that fails to decode is 400. Any valid access
        token reaches it. The token must also carry Admin, or ReadSubscriber and WriteSubscriber
        together.'
      operationId: patch_commerce_subscriber_by_subscriberid
      parameters:
      - in: path
        name: subscriberid
        required: true
        schema:
          type: string
      summary: Change part of a subscriber
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. Re-dispatches the request into the handler
        the intended verb would have reached, taking that verb from a _method form
        value or query parameter and then from the X-HTTP-Method-Override header.
        PUT replaces the row, PATCH changes part of it, DELETE removes it, and anything
        else is 405. The trap is the DEFAULT: naming no override at all leaves the
        method POST, which this tunnel maps to the PARTIAL UPDATE — it is never a
        create, and creating is the collection root''s job. Behaviour and authorization
        are the underlying operation''s, since the real handler runs. Any valid access
        token reaches it.'
      operationId: post_commerce_subscriber_by_subscriberid
      parameters:
      - in: path
        name: subscriberid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a subscriber — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A subscriber is a mailing-list member — name, email, the form
        id that captured them, unsubscribed state and date, client details, tags and
        metadata. Writing one FIRES A WEBHOOK: subscriber.created on create and subscriber.updated
        on replace or patch, emitted BEFORE the write is known to have succeeded and
        carrying the row as sent, so the payload holds the raw email rather than the
        normalized one that gets stored. This is a true REPLACEMENT, not a merge:
        the stored row''s key is preserved, but the body is decoded onto a FRESH entity,
        so every field the body omits is written back as its ZERO value. Patch is
        the verb for changing part of a row. The id is resolved inside the caller
        org''s own namespace and an absent one is 404 before anything is written;
        a body that fails to decode is 400. Answers the stored result. Any valid access
        token reaches it. The token must also carry Admin, or ReadSubscriber and WriteSubscriber
        together.'
      operationId: put_commerce_subscriber_by_subscriberid
      parameters:
      - in: path
        name: subscriberid
        required: true
        schema:
          type: string
      summary: Replace a subscriber outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/tokentransaction/:
    get:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Answers a pagination envelope — the page and display echoed back, the rows
        under models, a total count and a facets array — read from the caller org''s
        own namespaced store, so one tenant can never list another''s. Sorting defaults
        to the last-updated time and is overridable with sort. display is the page
        size and page applies only alongside it; either one that is not a positive
        integer is refused with 500 rather than silently ignored, and the limit query
        overrides the reported COUNT only, never the rows returned. No search backend
        is wired, so the datastore is the one and only list path and facets is always
        empty. A request resolving no org namespace is served an EMPTY page rather
        than an unscoped scan: the namespace IS the tenant filter, so without one
        there is nothing safe to return. Any valid access token reaches it. The per-kind
        permission table has no entry for tokentransaction, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_tokentransaction
      summary: List your org's token transactions, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Decodes the body into a new row in the caller org''s own namespaced store
        — isolated to that tenant from its first write — and answers the stored row
        at 201 with a Location header naming its id. The id is assigned by the store,
        not taken from the body. A body that fails to decode is 400 and a store that
        refuses the write is 500. Any valid access token reaches it. The per-kind
        permission table has no entry for tokentransaction, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: post_commerce_tokentransaction
      summary: Create a token transaction
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/tokentransaction/{tokentransactionid}:
    delete:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Removes the addressed row and answers 204 with no body. Before the live row
        goes it is written once more under a deleted tombstone kind, so a deletion
        leaves a recoverable copy rather than destroying the record outright — and
        a tombstone that cannot be written fails the call with 500 before anything
        is removed. The id is resolved inside the caller org''s own namespace, so
        an absent or foreign id is 404. Any valid access token reaches it. The per-kind
        permission table has no entry for tokentransaction, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: delete_commerce_tokentransaction_by_tokentransactionid
      parameters:
      - in: path
        name: tokentransactionid
        required: true
        schema:
          type: string
      summary: Delete a token transaction, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Reads the addressed row from the caller org''s own namespaced store. An id
        that is not there is 404 — and another tenant''s id is not there by construction,
        so it reads exactly like a typo instead of confirming the row exists somewhere
        else. Any valid access token reaches it. The per-kind permission table has
        no entry for tokentransaction, so the scaffold skips that second check with
        a warning and the gate above is the whole authorization story.'
      operationId: get_commerce_tokentransaction_by_tokentransactionid
      parameters:
      - in: path
        name: tokentransactionid
        required: true
        schema:
          type: string
      summary: Fetch one token transaction
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Loads the stored row and decodes the body OVER it, so only the fields the
        body names change and everything else keeps its stored value — the difference
        from the full replace, which clears what it is not told. Answers the merged
        row. An id absent from the caller org''s namespace is 404 and a body that
        fails to decode is 400. Any valid access token reaches it. The per-kind permission
        table has no entry for tokentransaction, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.'
      operationId: patch_commerce_tokentransaction_by_tokentransactionid
      parameters:
      - in: path
        name: tokentransactionid
        required: true
        schema:
          type: string
      summary: Change part of a token transaction
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        Re-dispatches the request into the handler the intended verb would have reached,
        taking that verb from a _method form value or query parameter and then from
        the X-HTTP-Method-Override header. PUT replaces the row, PATCH changes part
        of it, DELETE removes it, and anything else is 405. The trap is the DEFAULT:
        naming no override at all leaves the method POST, which this tunnel maps to
        the PARTIAL UPDATE — it is never a create, and creating is the collection
        root''s job. Behaviour and authorization are the underlying operation''s,
        since the real handler runs. Any valid access token reaches it.'
      operationId: post_commerce_tokentransaction_by_tokentransactionid
      parameters:
      - in: path
        name: tokentransactionid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a token transaction — for clients that cannot
        send PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A token transaction records a transfer between two identified
        parties — amount and fees, a timestamp, sending and receiving addresses, names,
        user ids, states and countries, a flag per side, a protocol name and a transaction
        hash. Nothing here touches a chain: the hash is an unvalidated string and
        the flags are plain writable booleans with no screening behind them. Amounts
        are floating-point rather than the exact minor units every real money field
        in commerce uses, and there is no currency field at all — this kind lives
        in commerce''s demo tree, so it is a live writable resource in your tenant''s
        store that nothing else in commerce reads, and it must never carry real money.
        This is a true REPLACEMENT, not a merge: the stored row''s key is preserved,
        but the body is decoded onto a FRESH entity, so every field the body omits
        is written back as its ZERO value. Patch is the verb for changing part of
        a row. The id is resolved inside the caller org''s own namespace and an absent
        one is 404 before anything is written; a body that fails to decode is 400.
        Answers the stored result. Any valid access token reaches it. The per-kind
        permission table has no entry for tokentransaction, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: put_commerce_tokentransaction_by_tokentransactionid
      parameters:
      - in: path
        name: tokentransactionid
        required: true
        schema:
          type: string
      summary: Replace a token transaction outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/transfer/:
    get:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Answers a
        pagination envelope — the page and display echoed back, the rows under models,
        a total count and a facets array — read from the caller org''s own namespaced
        store, so one tenant can never list another''s. Sorting defaults to the last-updated
        time and is overridable with sort. display is the page size and page applies
        only alongside it; either one that is not a positive integer is refused with
        500 rather than silently ignored, and the limit query overrides the reported
        COUNT only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for transfer, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_transfer
      summary: List your org's transfers, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Decodes the
        body into a new row in the caller org''s own namespaced store — isolated to
        that tenant from its first write — and answers the stored row at 201 with
        a Location header naming its id. The id is assigned by the store, not taken
        from the body. A body that fails to decode is 400 and a store that refuses
        the write is 500. The token must carry the ADMIN permission; an ordinary access
        token is refused. The per-kind permission table has no entry for transfer,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: post_commerce_transfer
      summary: Create a transfer
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/transfer/{transferid}:
    delete:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Removes the
        addressed row and answers 204 with no body. Before the live row goes it is
        written once more under a deleted tombstone kind, so a deletion leaves a recoverable
        copy rather than destroying the record outright — and a tombstone that cannot
        be written fails the call with 500 before anything is removed. The id is resolved
        inside the caller org''s own namespace, so an absent or foreign id is 404.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for transfer, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: delete_commerce_transfer_by_transferid
      parameters:
      - in: path
        name: transferid
        required: true
        schema:
          type: string
      summary: Delete a transfer, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Reads the
        addressed row from the caller org''s own namespaced store. An id that is not
        there is 404 — and another tenant''s id is not there by construction, so it
        reads exactly like a typo instead of confirming the row exists somewhere else.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for transfer, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_transfer_by_transferid
      parameters:
      - in: path
        name: transferid
        required: true
        schema:
          type: string
      summary: Fetch one transfer
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Loads the
        stored row and decodes the body OVER it, so only the fields the body names
        change and everything else keeps its stored value — the difference from the
        full replace, which clears what it is not told. Answers the merged row. An
        id absent from the caller org''s namespace is 404 and a body that fails to
        decode is 400. The token must carry the ADMIN permission; an ordinary access
        token is refused. The per-kind permission table has no entry for transfer,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: patch_commerce_transfer_by_transferid
      parameters:
      - in: path
        name: transferid
        required: true
        schema:
          type: string
      summary: Change part of a transfer
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. Re-dispatches
        the request into the handler the intended verb would have reached, taking
        that verb from a _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        The token must carry the ADMIN permission; an ordinary access token is refused.'
      operationId: post_commerce_transfer_by_transferid
      parameters:
      - in: path
        name: transferid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a transfer — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A transfer records that a payable WAS PAID — the annotation a
        human writes after paying out of band. Commerce executes no payout: creating
        one moves no money, and it marks the referenced payable settled. It carries
        the payable and payee ids, the amount it settles and the amount actually sent
        (which may be a different asset), a type of eth, wire or other, the transaction
        hash or wire reference, when it was paid and who recorded it; amounts are
        exact decimal strings with an asset, not cents. It is admin-gated because
        writing one settles money we owe, and nothing enforces uniqueness on the reference
        — so posting the same transfer twice settles the payable twice. This is a
        true REPLACEMENT, not a merge: the stored row''s key is preserved, but the
        body is decoded onto a FRESH entity, so every field the body omits is written
        back as its ZERO value. Patch is the verb for changing part of a row. The
        id is resolved inside the caller org''s own namespace and an absent one is
        404 before anything is written; a body that fails to decode is 400. Answers
        the stored result. The token must carry the ADMIN permission; an ordinary
        access token is refused. The per-kind permission table has no entry for transfer,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: put_commerce_transfer_by_transferid
      parameters:
      - in: path
        name: transferid
        required: true
        schema:
          type: string
      summary: Replace a transfer outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/variant/:
    get:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Answers a pagination envelope — the page and display
        echoed back, the rows under models, a total count and a facets array — read
        from the caller org''s own namespaced store, so one tenant can never list
        another''s. Sorting defaults to the SKU and is overridable with sort. display
        is the page size and page applies only alongside it; either one that is not
        a positive integer is refused with 500 rather than silently ignored, and the
        limit query overrides the reported COUNT only, never the rows returned. No
        search backend is wired, so the datastore is the one and only list path and
        facets is always empty. A request resolving no org namespace is served an
        EMPTY page rather than an unscoped scan: the namespace IS the tenant filter,
        so without one there is nothing safe to return. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through. The token must also carry Admin
        or the Variant list scope.'
      operationId: get_commerce_variant
      summary: List your org's variants, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Decodes the body into a new row in the caller org''s
        own namespaced store — isolated to that tenant from its first write — and
        answers the stored row at 201 with a Location header naming its id. The id
        is assigned by the store, not taken from the body. A body that fails to decode
        is 400 and a store that refuses the write is 500. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through. The token must also carry Admin
        or WriteVariant.'
      operationId: post_commerce_variant
      summary: Create a variant
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/variant/{variantid}:
    delete:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Removes the addressed row and answers 204 with no body.
        Before the live row goes it is written once more under a deleted tombstone
        kind, so a deletion leaves a recoverable copy rather than destroying the record
        outright — and a tombstone that cannot be written fails the call with 500
        before anything is removed. The id is resolved inside the caller org''s own
        namespace, so an absent or foreign id is 404. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through. The token must also carry Admin
        or WriteVariant.'
      operationId: delete_commerce_variant_by_variantid
      parameters:
      - in: path
        name: variantid
        required: true
        schema:
          type: string
      summary: Delete a variant, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Reads the addressed row from the caller org''s own namespaced
        store. An id that is not there is 404 — and another tenant''s id is not there
        by construction, so it reads exactly like a typo instead of confirming the
        row exists somewhere else. Any valid access token reaches it. The org must
        also be entitled to the commerce admin: the paywall answers 402 subscription_required
        unless the org holds an active or trialing pro subscription, a live trial
        credit or a redeemed invite, and 503 when that entitlement cannot be read
        rather than admitting on an unknown. The internal service token and a platform
        superadmin pass straight through. The token must also carry Admin or ReadVariant.'
      operationId: get_commerce_variant_by_variantid
      parameters:
      - in: path
        name: variantid
        required: true
        schema:
          type: string
      summary: Fetch one variant
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Loads the stored row and decodes the body OVER it, so
        only the fields the body names change and everything else keeps its stored
        value — the difference from the full replace, which clears what it is not
        told. Answers the merged row. An id absent from the caller org''s namespace
        is 404 and a body that fails to decode is 400. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through. The token must also carry Admin,
        or ReadVariant and WriteVariant together.'
      operationId: patch_commerce_variant_by_variantid
      parameters:
      - in: path
        name: variantid
        required: true
        schema:
          type: string
      summary: Change part of a variant
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. Re-dispatches the request into the handler the intended
        verb would have reached, taking that verb from a _method form value or query
        parameter and then from the X-HTTP-Method-Override header. PUT replaces the
        row, PATCH changes part of it, DELETE removes it, and anything else is 405.
        The trap is the DEFAULT: naming no override at all leaves the method POST,
        which this tunnel maps to the PARTIAL UPDATE — it is never a create, and creating
        is the collection root''s job. Behaviour and authorization are the underlying
        operation''s, since the real handler runs. Any valid access token reaches
        it. The org must also be entitled to the commerce admin: the paywall answers
        402 subscription_required unless the org holds an active or trialing pro subscription,
        a live trial credit or a redeemed invite, and 503 when that entitlement cannot
        be read rather than admitting on an unknown. The internal service token and
        a platform superadmin pass straight through.'
      operationId: post_commerce_variant_by_variantid
      parameters:
      - in: path
        name: variantid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a variant — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A variant is one purchasable SKU of a product — its product id,
        SKU and UPC, name, media, availability, the option name and value pairs that
        distinguish it, a sold counter, and its own money and stock: currency, price,
        MSRP, inventory cost, inventory count and taxability. Inventory and sold are
        plain writable numbers with no decrement logic behind them here. The same
        variant also exists as a JSON copy inside its product, and writing one does
        not update the other. This is a true REPLACEMENT, not a merge: the stored
        row''s key is preserved, but the body is decoded onto a FRESH entity, so every
        field the body omits is written back as its ZERO value. Patch is the verb
        for changing part of a row. The id is resolved inside the caller org''s own
        namespace and an absent one is 404 before anything is written; a body that
        fails to decode is 400. Answers the stored result. Any valid access token
        reaches it. The org must also be entitled to the commerce admin: the paywall
        answers 402 subscription_required unless the org holds an active or trialing
        pro subscription, a live trial credit or a redeemed invite, and 503 when that
        entitlement cannot be read rather than admitting on an unknown. The internal
        service token and a platform superadmin pass straight through. The token must
        also carry Admin, or ReadVariant and WriteVariant together.'
      operationId: put_commerce_variant_by_variantid
      parameters:
      - in: path
        name: variantid
        required: true
        schema:
          type: string
      summary: Replace a variant outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/wallet/:
    get:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Answers a pagination envelope
        — the page and display echoed back, the rows under models, a total count and
        a facets array — read from the caller org''s own namespaced store, so one
        tenant can never list another''s. Sorting defaults to the last-updated time
        and is overridable with sort. display is the page size and page applies only
        alongside it; either one that is not a positive integer is refused with 500
        rather than silently ignored, and the limit query overrides the reported COUNT
        only, never the rows returned. No search backend is wired, so the datastore
        is the one and only list path and facets is always empty. A request resolving
        no org namespace is served an EMPTY page rather than an unscoped scan: the
        namespace IS the tenant filter, so without one there is nothing safe to return.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for wallet, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_wallet
      summary: List your org's wallets, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Decodes the body into a
        new row in the caller org''s own namespaced store — isolated to that tenant
        from its first write — and answers the stored row at 201 with a Location header
        naming its id. The id is assigned by the store, not taken from the body. A
        body that fails to decode is 400 and a store that refuses the write is 500.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for wallet, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: post_commerce_wallet
      summary: Create a wallet
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/wallet/{walletid}:
    delete:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Removes the addressed row
        and answers 204 with no body. Before the live row goes it is written once
        more under a deleted tombstone kind, so a deletion leaves a recoverable copy
        rather than destroying the record outright — and a tombstone that cannot be
        written fails the call with 500 before anything is removed. The id is resolved
        inside the caller org''s own namespace, so an absent or foreign id is 404.
        The token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for wallet, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: delete_commerce_wallet_by_walletid
      parameters:
      - in: path
        name: walletid
        required: true
        schema:
          type: string
      summary: Delete a wallet, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Reads the addressed row
        from the caller org''s own namespaced store. An id that is not there is 404
        — and another tenant''s id is not there by construction, so it reads exactly
        like a typo instead of confirming the row exists somewhere else. The token
        must carry the ADMIN permission; an ordinary access token is refused. The
        per-kind permission table has no entry for wallet, so the scaffold skips that
        second check with a warning and the gate above is the whole authorization
        story.'
      operationId: get_commerce_wallet_by_walletid
      parameters:
      - in: path
        name: walletid
        required: true
        schema:
          type: string
      summary: Fetch one wallet
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Loads the stored row and
        decodes the body OVER it, so only the fields the body names change and everything
        else keeps its stored value — the difference from the full replace, which
        clears what it is not told. Answers the merged row. An id absent from the
        caller org''s namespace is 404 and a body that fails to decode is 400. The
        token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for wallet, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: patch_commerce_wallet_by_walletid
      parameters:
      - in: path
        name: walletid
        required: true
        schema:
          type: string
      summary: Change part of a wallet
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. Re-dispatches the request
        into the handler the intended verb would have reached, taking that verb from
        a _method form value or query parameter and then from the X-HTTP-Method-Override
        header. PUT replaces the row, PATCH changes part of it, DELETE removes it,
        and anything else is 405. The trap is the DEFAULT: naming no override at all
        leaves the method POST, which this tunnel maps to the PARTIAL UPDATE — it
        is never a create, and creating is the collection root''s job. Behaviour and
        authorization are the underlying operation''s, since the real handler runs.
        The token must carry the ADMIN permission; an ordinary access token is refused.'
      operationId: post_commerce_wallet_by_walletid
      parameters:
      - in: path
        name: walletid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a wallet — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A wallet is a container of custodial blockchain accounts, and
        its only field is that account list — each account carrying a name, an address,
        a chain type, and the ENCRYPTED private key with its salt. Creating a wallet
        through this table generates NO KEYS: key generation lives on the account
        routes, so a wallet made here is an empty shell and an account posted into
        one is stored exactly as sent, with no key generation and no validation behind
        it. Know what a read renders: the plaintext private key is never marshalled
        and never stored, but the encrypted blob and its salt ARE returned, so whoever
        can read a wallet can attack it offline down to the strength of the owner''s
        passphrase. That is why this kind is admin-gated. This is a true REPLACEMENT,
        not a merge: the stored row''s key is preserved, but the body is decoded onto
        a FRESH entity, so every field the body omits is written back as its ZERO
        value. Patch is the verb for changing part of a row. The id is resolved inside
        the caller org''s own namespace and an absent one is 404 before anything is
        written; a body that fails to decode is 400. Answers the stored result. The
        token must carry the ADMIN permission; an ordinary access token is refused.
        The per-kind permission table has no entry for wallet, so the scaffold skips
        that second check with a warning and the gate above is the whole authorization
        story.'
      operationId: put_commerce_wallet_by_walletid
      parameters:
      - in: path
        name: walletid
        required: true
        schema:
          type: string
      summary: Replace a wallet outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/watchlist/:
    get:
      description: 'A watchlist is a viewer''s saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Answers a pagination envelope — the page and display
        echoed back, the rows under models, a total count and a facets array — read
        from the caller org''s own namespaced store, so one tenant can never list
        another''s. Sorting defaults to the last-updated time and is overridable with
        sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. Any valid access
        token reaches it. The per-kind permission table has no entry for watchlist,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: get_commerce_watchlist
      summary: List your org's watchlists, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: A watchlist is a viewer's saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Decodes the body into a new row in the caller org's
        own namespaced store — isolated to that tenant from its first write — and
        answers the stored row at 201 with a Location header naming its id. The id
        is assigned by the store, not taken from the body. A body that fails to decode
        is 400 and a store that refuses the write is 500. Any valid access token reaches
        it. The per-kind permission table has no entry for watchlist, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.
      operationId: post_commerce_watchlist
      summary: Create a watchlist
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/watchlist/{watchlistid}:
    delete:
      description: A watchlist is a viewer's saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Removes the addressed row and answers 204 with
        no body. Before the live row goes it is written once more under a deleted
        tombstone kind, so a deletion leaves a recoverable copy rather than destroying
        the record outright — and a tombstone that cannot be written fails the call
        with 500 before anything is removed. The id is resolved inside the caller
        org's own namespace, so an absent or foreign id is 404. Any valid access token
        reaches it. The per-kind permission table has no entry for watchlist, so the
        scaffold skips that second check with a warning and the gate above is the
        whole authorization story.
      operationId: delete_commerce_watchlist_by_watchlistid
      parameters:
      - in: path
        name: watchlistid
        required: true
        schema:
          type: string
      summary: Delete a watchlist, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: A watchlist is a viewer's saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Reads the addressed row from the caller org's own
        namespaced store. An id that is not there is 404 — and another tenant's id
        is not there by construction, so it reads exactly like a typo instead of confirming
        the row exists somewhere else. Any valid access token reaches it. The per-kind
        permission table has no entry for watchlist, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.
      operationId: get_commerce_watchlist_by_watchlistid
      parameters:
      - in: path
        name: watchlistid
        required: true
        schema:
          type: string
      summary: Fetch one watchlist
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: A watchlist is a viewer's saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Loads the stored row and decodes the body OVER
        it, so only the fields the body names change and everything else keeps its
        stored value — the difference from the full replace, which clears what it
        is not told. Answers the merged row. An id absent from the caller org's namespace
        is 404 and a body that fails to decode is 400. Any valid access token reaches
        it. The per-kind permission table has no entry for watchlist, so the scaffold
        skips that second check with a warning and the gate above is the whole authorization
        story.
      operationId: patch_commerce_watchlist_by_watchlistid
      parameters:
      - in: path
        name: watchlistid
        required: true
        schema:
          type: string
      summary: Change part of a watchlist
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A watchlist is a viewer''s saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. Re-dispatches the request into the handler the
        intended verb would have reached, taking that verb from a _method form value
        or query parameter and then from the X-HTTP-Method-Override header. PUT replaces
        the row, PATCH changes part of it, DELETE removes it, and anything else is
        405. The trap is the DEFAULT: naming no override at all leaves the method
        POST, which this tunnel maps to the PARTIAL UPDATE — it is never a create,
        and creating is the collection root''s job. Behaviour and authorization are
        the underlying operation''s, since the real handler runs. Any valid access
        token reaches it.'
      operationId: post_commerce_watchlist_by_watchlistid
      parameters:
      - in: path
        name: watchlistid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a watchlist — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A watchlist is a viewer''s saved list of movies — a user id, an
        email, and the movies themselves. It stores WHOLE MOVIE SNAPSHOTS rather than
        movie ids, so a list goes stale the moment a film record changes and grows
        without bound as it fills. This is a true REPLACEMENT, not a merge: the stored
        row''s key is preserved, but the body is decoded onto a FRESH entity, so every
        field the body omits is written back as its ZERO value. Patch is the verb
        for changing part of a row. The id is resolved inside the caller org''s own
        namespace and an absent one is 404 before anything is written; a body that
        fails to decode is 400. Answers the stored result. Any valid access token
        reaches it. The per-kind permission table has no entry for watchlist, so the
        scaffold skips that second check with a warning and the gate above is the
        whole authorization story.'
      operationId: put_commerce_watchlist_by_watchlistid
      parameters:
      - in: path
        name: watchlistid
        required: true
        schema:
          type: string
      summary: Replace a watchlist outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/webhook/:
    get:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Answers a pagination envelope — the page and display
        echoed back, the rows under models, a total count and a facets array — read
        from the caller org''s own namespaced store, so one tenant can never list
        another''s. Sorting defaults to the last-updated time and is overridable with
        sort. display is the page size and page applies only alongside it; either
        one that is not a positive integer is refused with 500 rather than silently
        ignored, and the limit query overrides the reported COUNT only, never the
        rows returned. No search backend is wired, so the datastore is the one and
        only list path and facets is always empty. A request resolving no org namespace
        is served an EMPTY page rather than an unscoped scan: the namespace IS the
        tenant filter, so without one there is nothing safe to return. The token must
        carry the ADMIN permission; an ordinary access token is refused. The per-kind
        permission table has no entry for webhook, so the scaffold skips that second
        check with a warning and the gate above is the whole authorization story.'
      operationId: get_commerce_webhook
      summary: List your org's webhooks, as a page
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Decodes the body into a new row in the caller org''s
        own namespaced store — isolated to that tenant from its first write — and
        answers the stored row at 201 with a Location header naming its id. The id
        is assigned by the store, not taken from the body. A body that fails to decode
        is 400 and a store that refuses the write is 500. The token must carry the
        ADMIN permission; an ordinary access token is refused. The per-kind permission
        table has no entry for webhook, so the scaffold skips that second check with
        a warning and the gate above is the whole authorization story.'
      operationId: post_commerce_webhook
      summary: Create a webhook
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/commerce/webhook/{webhookid}:
    delete:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Removes the addressed row and answers 204 with no body.
        Before the live row goes it is written once more under a deleted tombstone
        kind, so a deletion leaves a recoverable copy rather than destroying the record
        outright — and a tombstone that cannot be written fails the call with 500
        before anything is removed. The id is resolved inside the caller org''s own
        namespace, so an absent or foreign id is 404. The token must carry the ADMIN
        permission; an ordinary access token is refused. The per-kind permission table
        has no entry for webhook, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: delete_commerce_webhook_by_webhookid
      parameters:
      - in: path
        name: webhookid
        required: true
        schema:
          type: string
      summary: Delete a webhook, keeping a recoverable copy
      tags:
      - commerce
      x-app: commerce
      x-public: true
    get:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Reads the addressed row from the caller org''s own namespaced
        store. An id that is not there is 404 — and another tenant''s id is not there
        by construction, so it reads exactly like a typo instead of confirming the
        row exists somewhere else. The token must carry the ADMIN permission; an ordinary
        access token is refused. The per-kind permission table has no entry for webhook,
        so the scaffold skips that second check with a warning and the gate above
        is the whole authorization story.'
      operationId: get_commerce_webhook_by_webhookid
      parameters:
      - in: path
        name: webhookid
        required: true
        schema:
          type: string
      summary: Fetch one webhook
      tags:
      - commerce
      x-app: commerce
      x-public: true
    patch:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Loads the stored row and decodes the body OVER it, so
        only the fields the body names change and everything else keeps its stored
        value — the difference from the full replace, which clears what it is not
        told. Answers the merged row. An id absent from the caller org''s namespace
        is 404 and a body that fails to decode is 400. The token must carry the ADMIN
        permission; an ordinary access token is refused. The per-kind permission table
        has no entry for webhook, so the scaffold skips that second check with a warning
        and the gate above is the whole authorization story.'
      operationId: patch_commerce_webhook_by_webhookid
      parameters:
      - in: path
        name: webhookid
        required: true
        schema:
          type: string
      summary: Change part of a webhook
      tags:
      - commerce
      x-app: commerce
      x-public: true
    post:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. Re-dispatches the request into the handler the intended
        verb would have reached, taking that verb from a _method form value or query
        parameter and then from the X-HTTP-Method-Override header. PUT replaces the
        row, PATCH changes part of it, DELETE removes it, and anything else is 405.
        The trap is the DEFAULT: naming no override at all leaves the method POST,
        which this tunnel maps to the PARTIAL UPDATE — it is never a create, and creating
        is the collection root''s job. Behaviour and authorization are the underlying
        operation''s, since the real handler runs. The token must carry the ADMIN
        permission; an ordinary access token is refused.'
      operationId: post_commerce_webhook_by_webhookid
      parameters:
      - in: path
        name: webhookid
        required: true
        schema:
          type: string
      summary: Method-override tunnel for a webhook — for clients that cannot send
        PUT, PATCH or DELETE
      tags:
      - commerce
      x-app: commerce
      x-public: true
    put:
      description: 'A webhook is a merchant-registered endpoint that receives commerce
        event callbacks — a name, a URL, live and all flags, a per-event map, an enabled
        flag, and the shared access token each delivery posts IN THE BODY. Two things
        to know before registering one: that token is a plainly readable field, so
        anyone who may read webhooks reads every endpoint''s secret, and delivery
        consults only the all flag and the event map — it does NOT consult enabled
        or live, so setting enabled false does not stop delivery and deleting the
        row is the only thing that does. Delivery is a single POST with a twenty-second
        timeout and no retry. This is a true REPLACEMENT, not a merge: the stored
        row''s key is preserved, but the body is decoded onto a FRESH entity, so every
        field the body omits is written back as its ZERO value. Patch is the verb
        for changing part of a row. The id is resolved inside the caller org''s own
        namespace and an absent one is 404 before anything is written; a body that
        fails to decode is 400. Answers the stored result. The token must carry the
        ADMIN permission; an ordinary access token is refused. The per-kind permission
        table has no entry for webhook, so the scaffold skips that second check with
        a warning and the gate above is the whole authorization story.'
      operationId: put_commerce_webhook_by_webhookid
      parameters:
      - in: path
        name: webhookid
        required: true
        schema:
          type: string
      summary: Replace a webhook outright
      tags:
      - commerce
      x-app: commerce
      x-public: true
  /v1/completions:
    post:
      description: Implements the OpenAI-compatible chat completions API
      operationId: post_completions
      summary: Implements the OpenAI-compatible chat completions API
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/crawl:
    post:
      description: |-
        Reads one URL and answers with the page as markdown.

        It fetches a single URL from inside the cluster and answers with the address it
        actually landed on, the document's title, its content rendered to MARKDOWN, and
        whatever the page said about itself. One URL per call: batching would make the
        answer a partial-failure envelope every caller then has to unpack.

        A PAGE THAT COULD NOT BE FETCHED IS A NORMAL ANSWER, not a fault. An
        unreachable host, a refused address and a content type that is not a document
        all answer 200 with `success:false` and the reason in `error`, because the
        caller sent a well-formed ask and gets a well-formed answer. Non-2xx is reserved
        for a caller problem — 400 with the same body when there is no url, 401 for a
        bad key, 503 when the surface is unconfigured — so error handling can trust the
        status. Check `success` before reading `data`.

        Admission is either a validated principal or the shared service key, presented
        as X-API-Key or a Bearer; neither is refused, and an unset key fails closed
        rather than opening the fetcher to the private network. Pages are archived under
        the scope of the VERIFIED principal and NEVER a scope named in the body, so a
        URL already read under that scope is answered from the archive without touching
        the network; a service caller has no org and its pages land in the shared
        corpus.

        The URL is caller-supplied and dialled from INSIDE the cluster, which makes this
        a request-forgery primitive by construction. Only http and https are accepted,
        and every address actually dialled must be public unicast — loopback,
        link-local, private and multicast are refused. The check lives in the DIALER
        rather than on the hostname, because resolving a name to validate it and then
        letting the transport resolve it again is a gap DNS rebinding walks straight
        through; redirects re-enter the same dialer.
      operationId: read_page
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/crawlRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/crawlResult'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/crawlResult'
          description: bad request
      summary: Fetch one URL and read it back as markdown
      tags:
      - crawl
      x-app: crawl
      x-public: true
      x-tool: true
  /v1/deploy/applications:
    get:
      description: |-
        Returns the fleet as an argocd ApplicationList: one
        projected Application per operator App CR, carrying the image tag the CR
        DECLARES, the tag actually RUNNING in the cluster's Deployment, the reconciled
        health, and the sync verdict those two produce (declared == running ⇒ Synced,
        both known and different ⇒ OutOfSync, either unknown ⇒ Unknown).

        It is TENANT-SCOPED: a platform SuperAdmin reads every platform namespace, a
        validated org member reads only its own org's tenant namespace and only the App
        CRs labelled with its org, and anyone else is refused. A cross-tenant CR is
        never projected into an answer.
      operationId: get_deploy_applications
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoAppList'
          description: ok
      summary: 'Returns the fleet as an argocd ApplicationList: one projected Application
        per operator App CR, carrying the image tag the CR DECLARES, the tag actually
        RUNNING in the cluster''s Deployment, the reconciled health, and the sync
        verdict those two produce (declared == running ⇒ Synced, both known and different
        ⇒ OutOfSync, either unknown ⇒ Unknown).'
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/applications/{name}:
    get:
      description: |-
        Returns ONE projected argocd Application by name, with
        status.resources filled in from its reconciled resource tree — which is what
        makes it the detail view rather than a row of the list.

        It is TENANT-SCOPED, and a name that belongs to another org is reported NOT
        FOUND rather than refused: a 403 would confirm the application exists, so the
        route would become a cross-tenant existence oracle. A name that is not a
        DNS-1123 label is a 400 before any cluster read.
      operationId: get_deploy_applications_by_name
      parameters:
      - description: |-
          Name is the application to read, from the path. It must be a DNS-1123 label
          (lowercase alphanumerics and hyphens, starting and ending alphanumeric) —
          every operator App CR's metadata.name satisfies that, and anything else is a
          400 rather than a lookup.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoApp'
          description: ok
      summary: Returns ONE projected argocd Application by name, with status.resources
        filled in from its reconciled resource tree — which is what makes it the detail
        view rather than a row of the list.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/applications/{name}/resource-tree:
    get:
      description: |-
        Returns one application's argocd ApplicationTree: the
        objects the operator reconciled from its App CR, reached by ownerRef — the
        Deployment and, under it, the ReplicaSet and Pods, plus the Service, Ingress,
        HorizontalPodAutoscaler, PodDisruptionBudget and ConfigMaps it owns — each node
        carrying its parent edges and its health.

        Secrets are DELIBERATELY not walked, so no materialized environment can ever
        appear in the tree. Tenant-scoped exactly like the application read: another
        org's name is not found, a malformed name is a 400.
      operationId: get_deploy_applications_by_name_resource-tree
      parameters:
      - description: |-
          Name is the application to read, from the path. It must be a DNS-1123 label
          (lowercase alphanumerics and hyphens, starting and ending alphanumeric) —
          every operator App CR's metadata.name satisfies that, and anything else is a
          400 rather than a lookup.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoTree'
          description: ok
      summary: 'Returns one application''s argocd ApplicationTree: the objects the
        operator reconciled from its App CR, reached by ownerRef — the Deployment
        and, under it, the ReplicaSet and Pods, plus the Service, Ingress, HorizontalPodAutoscaler,
        PodDisruptionBudget and ConfigMaps it owns — each node carrying its parent
        edges and its health.'
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/applications/{name}/revisions/{revision}/metadata:
    get:
      description: |-
        Returns the argocd RevisionMetadata for one revision
        of one application — what the detail view shows beside a revision.

        An App CR is IMAGE-pinned rather than commit-pinned: the deploy names an image
        tag, and the git source this projection reports is the display-only manifest
        repo, not the application's own source. Nothing in this process can read a
        commit's author or message for an arbitrary revision. So rather than 404 (which
        the SPA turns into an error toast) or invent a git author, it answers the
        HONEST minimum: date is when the App CR was created, message is the revision
        asked for — with the empty revision and "HEAD" resolving to the image tag the
        CR declares — and author is empty. An over-long revision is truncated before it
        is echoed back.

        Tenant-scoped exactly like the application read.
      operationId: get_deploy_applications_by_name_revisions_by_revision_metadata
      parameters:
      - description: Name is the application to read, from the path. It must be a
          DNS-1123 label.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: |-
          Revision is the revision to describe, from the path. The empty revision and
          "HEAD" both mean "whatever this application currently declares".
        in: path
        name: revision
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoRevisionMetadata'
          description: ok
      summary: Returns the argocd RevisionMetadata for one revision of one application
        — what the detail view shows beside a revision.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/applications/{name}/rollback:
    post:
      description: |-
        Performs exactly what the sync action performs: it stamps the sync-requested timestamp onto the application's App CR and answers the application re-projected. It does NOT select, pin or revert to a prior image tag, and that is the one thing to know before wiring anything to it — the name is the console's, the behaviour is the sync. Pinning a previous release rides the release seam, which this address does not call yet.

        SuperAdmin-only and fail-closed, reading no request body, with an unknown application name a 404 and no cluster client a 503 — the same gate and the same failures as the sync it shares a handler with.
      operationId: post_deploy_applications_by_name_rollback
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: The console's rollback control — today it requests a reconcile, nothing
        more
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/applications/{name}/sync:
    post:
      description: |-
        Requests an immediate reconcile of one application by stamping a sync-requested timestamp onto its App CR, which the operator's watch observes, and answers the application re-projected. It ASKS, it does not apply: the operator performs the reconcile on its own clock, so a 200 means the request landed, not that the rollout finished — the returned row's running version still lags until it does. The CR is the desired source today, so this is a nudge; when git becomes the source the same address becomes apply-from-git.

        SuperAdmin-only and fail-closed — a non-SuperAdmin is refused before any cluster object is read or patched, and the write surface stays admin-only while the tenant surface is read-only reflection. It reads no request body. An unknown application name is a 404; no cluster client configured is a 503.
      operationId: post_deploy_applications_by_name_sync
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Ask the operator to reconcile one application now
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/applications/{name}/syncwindows:
    get:
      description: |-
        Returns one application's argocd
        ApplicationSyncWindowState — the answer to "is anything blocking a sync of this
        application right now?".

        This platform runs NO sync windows, so the answer is always the permissive
        empty one: canSync true, with no active and no assigned windows. The
        application is still resolved first, so a name that is not the caller's is not
        found rather than handed the static body — the endpoint discloses nothing about
        another tenant's fleet.
      operationId: get_deploy_applications_by_name_syncwindows
      parameters:
      - description: |-
          Name is the application to read, from the path. It must be a DNS-1123 label
          (lowercase alphanumerics and hyphens, starting and ending alphanumeric) —
          every operator App CR's metadata.name satisfies that, and anything else is a
          400 rather than a lookup.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoSyncWindows'
          description: ok
      summary: Returns one application's argocd ApplicationSyncWindowState — the answer
        to "is anything blocking a sync of this application right now?".
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/callback:
    get:
      description: |-
        Completes the redirect from IAM: it validates `state` against the single-use flow cookie in constant time, redeems the authorization code with the PKCE verifier, and then VERIFIES the resulting token exactly as this deployment's identity boundary will on every later request — so a token that would be refused next request fails here with the real reason instead of producing a sign-in loop. On success it sets the session cookie, bounded by the token's own expiry, and redirects to the validated return path.

        It fails closed, and closes on the ADMIN ORG: a principal whose verified owner claim is not the reserved admin org is told plainly that it lacks the role (403) and no cookie is minted for it. That check is not the authorization decision — every gated route re-derives SuperAdmin from the verified JWT — it exists so nobody is handed a session that silently 403s everything. No flow in progress, or a mismatched `state`, is a 400; a refused or unexchangeable code is a 401.
      operationId: get_deploy_callback
      summary: Finish the sign-in round trip and mint the console session
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/clusters:
    get:
      description: |-
        Returns the argocd ClusterList of the destinations the
        caller's applications reconcile into: one entry per distinct destination
        server, carrying the count of applications reconciling into it. The in-cluster
        destination is always present, so an empty fleet still answers one cluster, and
        no cluster credential can appear — the projected type physically has no config
        field.

        It is TENANT-SCOPED and reads the SAME App CRs the applications list reads: a
        platform SuperAdmin counts the whole fleet, a validated org member counts only
        its own org's applications, anyone else is refused.
      operationId: get_deploy_clusters
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoClusterList'
          description: ok
      summary: 'Returns the argocd ClusterList of the destinations the caller''s applications
        reconcile into: one entry per distinct destination server, carrying the count
        of applications reconciling into it.'
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/gitops:
    get:
      description: |-
        Lists every Hanzo CD Application in the cluster: the git source
        each one polls, the commit it last APPLIED, how its last sync operation ended,
        and its recent deploy history — newest deploy first, ordered by namespace then
        name.

        This is the layer ABOVE the application board, and the two disagree in exactly
        the case an operator most needs to see: main carries a new image pin, CD has
        not applied that commit yet, so every App CR still declares the old tag and the
        application board is legitimately "Synced" while the deploy has not landed.
        Only the applied revision here can show that.

        installed is false — with a reason and an empty list — when the CD CRD is not
        served in this cluster. That is a FACT about the cluster rather than a failure
        of the request, so the caller can say "no CD plane here" instead of rendering
        an error it cannot act on; a genuine transport or RBAC failure still errors.

        Read-only, and platform SuperAdmin only: the CD plane is fleet infrastructure
        with no tenant dimension. This view observes CD and never drives it — the sync
        policy is automated with self-heal, and the actionable verb an operator has is
        the per-application reconcile at POST /v1/deploy/applications/{name}/sync.
      operationId: get_deploy_gitops
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/GitOpsPlane'
          description: ok
      summary: 'Lists every Hanzo CD Application in the cluster: the git source each
        one polls, the commit it last APPLIED, how its last sync operation ended,
        and its recent deploy history — newest deploy first, ordered by namespace
        then name.'
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/health:
    get:
      description: |-
        Reports the plane's real reachability: 200 only when the Kubernetes API server answers AND the App CRD is served, 503 with the same body shape otherwise, so a caller reads the same `k8s` and `crd` booleans either way rather than parsing an error envelope. It is a genuine dependency probe, not a process liveness ping — a running plane with no cluster behind it reports degraded.

        This is the ONE unauthenticated route that reports state, because liveness must be probe-able without a JWT. It therefore discloses booleans only: the underlying failure — the API server address, an RBAC refusal — is logged server-side and never put on the wire.
      operationId: get_deploy_health
      summary: Whether this control plane can actually reach the cluster it deploys
        to
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/login:
    get:
      description: |-
        Redirects the browser to IAM's authorize endpoint, having minted a nonce and a PKCE verifier into a short-lived, single-use flow cookie. The nonce comes back as `state` and is what proves the code belongs to the round trip THIS browser started; the verifier never appears in the address bar.

        Necessarily public — this is how a browser gets a principal for this host in the first place — and it grants nothing by itself. An optional `returnTo` names where to land afterwards and is run through the open-redirect guard, so only a same-host path survives. A deployment with no sign-in configured answers 503 rather than redirecting nowhere.
      operationId: get_deploy_login
      summary: Start the sign-in round trip for this console
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/logout:
    post:
      description: |-
        Clears this console's session cookie and answers the signed-out state with the sign-in URL to start again. IAM's own session is untouched — this ends the console session only, so signing back in may not prompt for credentials.

        It is a POST because it changes state. As a GET it was reachable by a cross-site top-level navigation, which a SameSite=Lax cookie still rides, so any page could sign a SuperAdmin out; a POST is not carried cross-site by that cookie.
      operationId: post_deploy_logout
      summary: End the console session on this host
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/projects:
    get:
      description: |-
        Returns the argocd AppProjectList this console groups and
        filters applications by. Projects are owned by Hanzo IAM rather than by argocd,
        so they are REFLECTED read-only from the IAM project store and nothing is
        persisted here: a validated org member gets its own organization's projects and
        a platform SuperAdmin gets every organization's.

        A SuperAdmin whose IAM store is not reachable falls back to the real
        argoproj.io AppProject CRs when that CRD is served, and otherwise to one
        permissive synthesized project per distinct project name the App CRs declare.
        A project named "default" is always present, because that is what an App CR
        carrying no project label projects to.
      operationId: get_deploy_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/argoProjectList'
          description: ok
      summary: Returns the argocd AppProjectList this console groups and filters applications
        by.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/reconcile:
    post:
      description: |-
        Runs one full GitOps sync through the embedded engine — render the configured repo, ref and path, then three-way server-side apply with scoped prune — and answers the revision it applied, the source it came from, the declared/synced/pruned/failed counts and a per-resource result. This is the WRITE half of the plane: it mutates live cluster objects and, with prune enabled, deletes objects the source no longer declares.

        SuperAdmin-only and fail-closed — a non-SuperAdmin is refused before any cluster object is read or touched. The git source is read AS THE CALLER, so the source plane scopes the answer itself rather than trusting this one to have scoped it. It reads no request body; the source is configuration, not a parameter. A deployment with the engine switched off, or with no usable cluster config, answers 503; a failure to start, render or sync is a 502.
      operationId: post_deploy_reconcile
      summary: Render the configured git source and apply it to the cluster, once
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/session/userinfo:
    get:
      description: |-
        Answers "is this browser signed in, and if not where does it
        sign in?" — the dashboard SPA's bootstrap question, and the only route on this
        plane that answers for an anonymous caller.

        The anonymous answer carries loggedIn:false and a URL and NOTHING else: no
        username, no org, no groups, no issuer, no hint about who the caller might be or
        what exists in the cluster. Answering it costs nothing (the caller already knows
        whether it holds a cookie) and withholding it costs the whole sign-in journey.

        The predicate is the platform SuperAdmin fact — the SAME one every other route
        here gates on, minted from a validated principal whose org is the reserved admin
        org — so a validated-but-not-SuperAdmin caller is reported as NOT signed in,
        which is the truth as this console defines it: they cannot use it.
      operationId: get_deploy_session_userinfo
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sessionUser'
          description: ok
      summary: Answers "is this browser signed in, and if not where does it sign in?"
        — the dashboard SPA's bootstrap question, and the only route on this plane
        that answers for an anonymous caller.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/settings:
    get:
      description: |-
        Returns the argocd AuthSettings object the dashboard SPA
        awaits before its first render.

        Every value is a CONSTANT of this projection rather than configuration read
        from anywhere: the SPA's own login form is reported disabled and its OIDC
        config null because Hanzo IAM owns identity at the edge and this console's
        sign-in is GET /v1/deploy/login, and every argocd feature the projection does
        not implement — status badges, Dex connectors, config-management plugins,
        kustomize versions, the exec terminal, apps-in-any-namespace, the hydrator,
        sync-with-replace — is reported off. Platform SuperAdmin only.
      operationId: get_deploy_settings
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/consoleSettings'
          description: ok
      summary: Returns the argocd AuthSettings object the dashboard SPA awaits before
        its first render.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/deploy/stream/applications:
    get:
      description: |-
        Holds the connection open as text/event-stream and pushes one watch event per application change. It opens with an `ADDED` frame for every application currently present — the same projection the applications list serves, so a client renders a complete fleet from the stream alone — and then forwards `ADDED`, `MODIFIED` and `DELETED` as they happen, with a keep-alive every 25 seconds that is also how a vanished client is noticed and its watch torn down.

        Read-only and TENANT-SCOPED, fail-closed: a platform SuperAdmin streams the whole fleet, a validated org member streams only its own org's applications, anyone else gets 403 and no stream. No cluster client configured is 503. If the deployment is not granted the watch verb the stream degrades to keep-alives only — the initial state still renders, it simply stops updating — rather than failing the connection.
      operationId: get_deploy_stream_applications
      summary: Live application fleet updates as Server-Sent Events
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/stream/applications/{name}/resource-tree:
    get:
      description: |-
        Holds the connection open as text/event-stream and pushes the application's whole resource tree — its live child objects and each one's derived health — once immediately and again on every keep-alive tick, so a client always has a current picture without polling. The refresh IS the keep-alive: it is a cheap rebuild rather than a watch, so there is no multi-resource watch to leak.

        TENANT-SCOPED and fail-closed BEFORE the stream opens, which is the rule that matters: the caller's scope and the application's namespace are resolved first, so an unvalidated caller gets a plain 403 and an application belonging to another tenant gets a plain 404 — never an opened stream that emits nothing. A SuperAdmin reaches the whole fleet, an org member only its own org's applications. No cluster client configured is 503.
      operationId: get_deploy_stream_applications_by_name_resource-tree
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Live resource tree for one application, as Server-Sent Events
      tags:
      - deploy
      x-app: deploy
      x-public: true
  /v1/deploy/version:
    get:
      description: |-
        Returns the argocd VersionMessage the dashboard SPA reads at
        bootstrap. There is no argocd binary behind this plane — it is a projection
        over operator App CRs — so the fields say so rather than describing a build:
        Version names the projection, BuildDate is the moment this response was
        generated, and Compiler/Platform/GoVersion are the constants the SPA tolerates
        rather than facts about this process. Platform SuperAdmin only.
      operationId: get_deploy_version
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/versionMessage'
          description: ok
      summary: Returns the argocd VersionMessage the dashboard SPA reads at bootstrap.
      tags:
      - deploy
      x-app: deploy
      x-public: true
      x-tool: true
  /v1/destinations:
    get:
      description: |-
        Reports every destination this deployment can forward to, each with the
        caller org's connection state: whether it is connected, whether it is enabled,
        whether a credential resolves right now, and the config fields the console
        renders for it.
      operationId: get_destinations
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/destinationList'
          description: ok
      summary: 'Reports every destination this deployment can forward to, each with
        the caller org''s connection state: whether it is connected, whether it is
        enabled, whether a credential resolves right now, and the config fields the
        console renders for it.'
      tags:
      - destinations
      x-app: destinations
      x-public: true
      x-tool: true
  /v1/destinations/{platform}:
    delete:
      description: |-
        Forgets a destination for the caller's org: every credential held in
        KMS, then the stored config. Idempotent, and it requires org admin.
      operationId: delete_destinations_by_platform
      parameters:
      - description: |-
          Platform is the destination to act on, from the path: ga4 | meta | tiktok |
          linkedin | x | reddit | posthog | umami.
        example: ga4
        in: path
        name: platform
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/destinationDisconnected'
          description: ok
      summary: 'Forgets a destination for the caller''s org: every credential held
        in KMS, then the stored config.'
      tags:
      - destinations
      x-app: destinations
      x-public: true
      x-tool: true
    get:
      description: |-
        Reports one destination's card for the caller's org — its config fields,
        its connection state, and whether a credential resolves right now. A platform
        this deployment does not carry is not found.
      operationId: get_destinations_by_platform
      parameters:
      - description: |-
          Platform is the destination to act on, from the path: ga4 | meta | tiktok |
          linkedin | x | reddit | posthog | umami.
        example: ga4
        in: path
        name: platform
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DestinationStatus'
          description: ok
      summary: Reports one destination's card for the caller's org — its config fields,
        its connection state, and whether a credential resolves right now.
      tags:
      - destinations
      x-app: destinations
      x-public: true
      x-tool: true
    post:
      description: 'Stores the addressed platform''s non-secret ids (its measurement,
        pixel or dataset ids) and seals its API credential into KMS under a path scoped
        to the caller''s own org, then answers the same status card the read routes
        do — with live telling you whether the credential actually resolves right
        now. The body''s property NAMES are the platform''s own: each field the platform
        declares, plus each secret under its camelCase name, so the accepted keys
        differ per platform and a missing REQUIRED field is refused. Connecting is
        an ORG ADMIN action — a validated member without the admin bit gets 403 —
        and it fails closed with 503 when the KMS master key is unavailable rather
        than persisting a destination whose secret was never sealed. The secret itself
        never appears in the response, in the store, or in a log line; only its NAME
        is ever published. Set enabled to false to keep the connection but stop the
        analytics fan-out to it.'
      operationId: post_destinations_by_platform
      parameters:
      - in: path
        name: platform
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: {}
              type: object
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DestinationStatus'
          description: Success
      summary: Connect one conversion destination for your org, or update the one
        you have
      tags:
      - destinations
      x-app: destinations
      x-public: true
  /v1/destinations/{platform}/test:
    post:
      description: |-
        Sends ONE synthetic pageview through the connected destination end to end
        and reports what the platform said. A send the platform refuses is reported as
        data — {"ok": false, "error": …} at 200 — so the console shows the platform's
        own words rather than an error about Hanzo. It requires org admin.
      operationId: post_destinations_by_platform_test
      parameters:
      - description: |-
          Platform is the destination to act on, from the path: ga4 | meta | tiktok |
          linkedin | x | reddit | posthog | umami.
        example: ga4
        in: path
        name: platform
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/destinationTest'
          description: ok
      summary: Sends ONE synthetic pageview through the connected destination end
        to end and reports what the platform said.
      tags:
      - destinations
      x-app: destinations
      x-public: true
      x-tool: true
  /v1/domain/availability:
    get:
      description: |-
        Checks exact names rather than searching for them, and answers the
        same quote shape search does — purchasable, premium, first-term and renewal price
        in cents.

        It requires a validated principal; 403 without one. Nothing is charged and
        nothing is held. A deployment with no registrar credentials answers 503.
      operationId: get_domain_availability
      parameters:
      - description: |-
          Domain is one name, or several comma-separated, to check in one call. Names
          are lowercased. It is required.
        in: query
        name: domain
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/quoteList'
          description: ok
      summary: Checks exact names rather than searching for them, and answers the
        same quote shape search does — purchasable, premium, first-term and renewal
        price in cents.
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/domains:
    get:
      description: |-
        Is the domains your org has bought here, newest registration first, each
        carrying the name, when it was registered, when it expires, what the org paid,
        the registrar order id and the nameservers it points at.

        Scoped to the validated principal's org — 403 without one, and there is no
        parameter that reaches another org's holdings.

        This is the deployment's OWN ownership record, not a query to the registrar: it
        lists what was bought THROUGH this surface, so a domain the org holds elsewhere
        is not here. The default store is in-process, so a deployment that has not
        swapped in a durable store answers from what this process registered.
      operationId: get_domain_domains
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/holdings'
          description: ok
      summary: Is the domains your org has bought here, newest registration first,
        each carrying the name, when it was registered, when it expires, what the
        org paid, the registrar order id and the nameservers it points at.
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/health:
    get:
      description: |-
        Reports registrar reachability honestly: ok only when the wholesale
        credentials are present AND name.com accepted them on a live call made while you
        waited.

        Missing credentials or an unreachable registrar is 503 carrying configured,
        reachable and the reason, so an operator reads the blocker instead of guessing at
        it. It takes no principal, like every subsystem health probe.
      operationId: get_domain_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reachability'
          description: ok
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reachability'
          description: service unavailable
      summary: 'Reports registrar reachability honestly: ok only when the wholesale
        credentials are present AND name.com accepted them on a live call made while
        you waited.'
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/register:
    post:
      description: |-
        Buys a domain for your org and answers the ownership record together
        with the quote it was bought at.

        The order of operations is the product guarantee: quote, refuse anything
        unpurchasable or unpriced, AUTHORIZE the org's prepaid balance, provision the
        authoritative zone in Hanzo DNS, register at the registrar already pointing at
        Hanzo's nameservers, and only then CAPTURE the charge and record ownership. A
        registrar failure therefore leaves the balance untouched — the org is never
        billed for a domain it did not get.

        It requires a validated principal; that principal's org owns the domain and is
        the ledger the charge lands on. Re-buying a name the org already holds is 409,
        not a second purchase.

        Refusals are distinct on purpose: 402 when the prepaid balance cannot cover the
        quoted price, 409 when the name is not available, 503 when the deployment has no
        registrar credentials, and the registrar's own message with its own 4xx — or 502
        for its 5xx — when it rejects the purchase. Zone provisioning is best-effort: if
        the zone service is down the domain is still registered against Hanzo's
        nameservers and the zone reconciles afterwards, rather than the purchase failing.
      operationId: post_domain_register
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/order'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResult'
          description: ok
      summary: Buys a domain for your org and answers the ownership record together
        with the quote it was bought at.
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/renew:
    post:
      description: |-
        Extends a domain your org already owns and answers the updated record with
        its new expiry alongside what was paid.

        Ownership is the gate: a name the caller's org does not hold is 404, so a renewal
        can never reach another tenant's domain.

        The price is re-quoted at the CURRENT renewal rate rather than the one paid at
        purchase. If the registrar returns no renewal price the org's original price is
        charged instead, so a renewal is never accidentally free. The balance is
        authorized before the registrar is called and captured after it confirms — 402
        when the prepaid balance cannot cover it, 503 when the deployment has no
        registrar credentials. Requires a validated principal.
      operationId: post_domain_renew
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/renewReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RenewResult'
          description: ok
      summary: Extends a domain your org already owns and answers the updated record
        with its new expiry alongside what was paid.
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/search:
    get:
      description: |-
        Finds names built from the keyword q, plus the registrar's alternate-TLD
        suggestions, and answers a quote for each: the name, whether it is purchasable,
        whether it is premium, the first-term and renewal price in cents, and the TLD.

        Prices are RETAIL — this deployment's markup is already applied and the wholesale
        cost is never on the wire.

        It requires a validated principal; 403 without one. Nothing is charged and
        nothing is held — a quote is not a reservation, and the price is re-quoted at
        purchase, so a name quoted here can be gone or dearer by the time you buy it. A
        deployment with no registrar credentials answers 503.
      operationId: get_domain_search
      parameters:
      - description: Q is the keyword to build names from. It is required.
        in: query
        name: q
        required: true
        schema:
          type: string
      - description: TLD narrows the search to a comma-separated set of top-level
          domains.
        in: query
        name: tld
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/quoteList'
          description: ok
      summary: 'Finds names built from the keyword q, plus the registrar''s alternate-TLD
        suggestions, and answers a quote for each: the name, whether it is purchasable,
        whether it is premium, the first-term and renewal price in cents, and the
        TLD.'
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/domain/transfer:
    post:
      description: |-
        Moves a domain you own at another registrar onto your org here, using
        its authCode, and answers the same record-plus-quote a purchase does.

        It is priced and charged exactly like a registration: authorize the org's prepaid
        balance, ask the registrar for the transfer, capture only after the registrar
        accepts. A name the registrar will not price is 409, an insufficient balance is
        402, and a deployment with no registrar credentials is 503.

        It requires a validated principal; the ownership record is written under that org
        as soon as the registrar ACCEPTS the request, which is not the same instant the
        transfer completes at the losing registrar. Unlike a registration this does not
        provision a zone, so the record carries this deployment's configured nameservers.
      operationId: post_domain_transfer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/transferReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/RegisterResult'
          description: ok
      summary: Moves a domain you own at another registrar onto your org here, using
        its authCode, and answers the same record-plus-quote a purchase does.
      tags:
      - domain
      x-app: domain
      x-public: true
      x-tool: true
  /v1/embeddings:
    post:
      description: |-
        Implements POST /v1/embeddings (OpenAI-compatible).

        Body: {"model": "...", "input": "..."|["...", ...], "encoding_format"?, "dimensions"?}
        It authenticates the caller, resolves the model to its upstream provider via
        the shared routing table, rewrites the user-facing model name to the upstream
        id, and proxies the request to the provider's /embeddings endpoint verbatim.
      operationId: post_embeddings
      summary: Implements POST /v1/embeddings (OpenAI-compatible).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/engine/model:
    get:
      description: |-
        Model reads one model's load state — loaded, unloading, or not_found, as
        the engine itself reports it.
      operationId: engineModel
      parameters:
      - description: Model is the model id to inspect, exactly as the model list reports
          it.
        example: Qwen/Qwen3-4B
        in: query
        name: model
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Read one model's load state on the serving runtime
      tags:
      - engine
      x-app: engine
      x-public: true
      x-tool: true
  /v1/engine/models:
    get:
      description: |-
        Models lists the models the engine serves, each with its load state — the
        server's own model table (its standard list envelope, load status
        included), relayed verbatim.
      operationId: engineModels
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: List the models the serving runtime holds, with each one's load state
      tags:
      - engine
      x-app: engine
      x-public: true
      x-tool: true
  /v1/engine/status:
    get:
      description: |-
        Status reports whether the engine deployment is reachable and which build
        revision it runs — an honest lens for "is the serving runtime up", never a
        fabricated ok.
      operationId: engineStatus
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/engineStatus'
          description: ok
      summary: Whether the serving runtime is reachable, and which build it runs
      tags:
      - engine
      x-app: engine
      x-public: true
      x-tool: true
  /v1/engine/system:
    get:
      description: |-
        System reads the engine host's inventory: OS, CPU, memory, every accelerator
        device with its VRAM and compute capability, and the build's capabilities
        (CUDA/Metal/flash-attention) — the real hardware under the serving runtime,
        relayed verbatim.
      operationId: engineSystem
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: 'The serving host''s own inventory: devices, memory and build capabilities'
      tags:
      - engine
      x-app: engine
      x-public: true
      x-tool: true
  /v1/entitlements:
    get:
      description: |-
        Projection reports which console apps the CALLER's org may open, and the plan slug
        that decides it. It is the READ side of the unified paywall: the org's plan tier
        resolved from commerce, which is a different authority from the enablement store
        behind GET /v1/entitlements/orgs/{org} (that one is the org's own on/off intent).

        It fails SAFE-TO-LOCKED, never 500: an unvalidated principal is a 403, but a
        commerce outage reports every app locked at 200 rather than breaking the shell.
        The ENFORCEMENT path still fails open, so functionality survives the same outage
        even while the UI conservatively shows locked.
      operationId: get_entitlements
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectionView'
          description: ok
      summary: Projection reports which console apps the CALLER's org may open, and
        the plan slug that decides it.
      tags:
      - entitlements
      x-app: entitlements
      x-public: true
      x-tool: true
  /v1/entitlements/orgs/{org}:
    get:
      description: |-
        Get lists the products an org has ENABLED — its own intent, which the console's
        paid-product sidebar reads to decide what to show. It is distinct from what the
        org's plan ENTITLES it to (that is GET /v1/entitlements, resolved from commerce).

        A caller may only read its OWN org's row; a platform super admin may read any.
      operationId: get_entitlements_orgs_by_org
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/entitlementsView'
          description: ok
      summary: Get lists the products an org has ENABLED — its own intent, which the
        console's paid-product sidebar reads to decide what to show.
      tags:
      - entitlements
      x-app: entitlements
      x-public: true
      x-tool: true
    post:
      description: |-
        Post turns products on or off for an org and returns the enabled set afterwards.

        A product may only be ENABLED if the org's plan already ENTITLES it, so enabling
        never spends new money — a product the plan does not grant answers 402 and the
        console routes that to an upgrade prompt. DISABLING is never gated. A platform
        super admin bypasses the plan check (operator comp/grant) and may target any org;
        everyone else may only change their own. Commerce unreachable is a 503, never an
        implicit yes.
      operationId: post_entitlements_orgs_by_org
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              add:
              - chat
              remove:
              - engine
            schema:
              $ref: '#/components/schemas/mutateReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/entitlementsView'
          description: ok
      summary: Post turns products on or off for an org and returns the enabled set
        afterwards.
      tags:
      - entitlements
      x-app: entitlements
      x-public: true
      x-tool: true
  /v1/errors:
    get:
      description: |-
        Errors returns the caller org's most recently captured errors, newest first. The
        error-tracking read view over event.error — the plane table the write core's error
        facts land in (errors are DELIBERATELY not on event.event) — each with its captured
        exception surfaced from the attributes map as a first-class field.

        The org is the validated principal's — never a parameter — and this read requires a
        real bearer, NEVER the write-only publishable key: pk- can attribute a write and can
        read nothing. 403 without a validated bearer, 503 when the warehouse is unreachable.
      operationId: get_errors
      parameters:
      - description: |-
          Limit is how many rows to return, newest first. Default 50, maximum 200; a
          value at or below zero, or one that is not a number, takes the default.
        example: 100
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/errorList'
          description: ok
      summary: Errors returns the caller org's most recently captured errors, newest
        first.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/evals/datasets:
    get:
      description: |-
        Is the datasets your org has, each with its name, description,
        metadata and timestamps.

        It is the only way to enumerate what an org holds. Requires a validated
        principal; 403 without one. Every row is filtered on the validated org, so
        there is no parameter that reaches another tenant's datasets. The item count is
        NOT populated here — read one dataset to get it.
      operationId: get_evals_datasets
      parameters:
      - description: |-
          Limit caps the rows returned. It defaults to 100 and is capped at 500; a
          non-positive or unparseable value falls back to the default rather than
          failing, because a typo about paging is not a reason to refuse a read.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/datasetList'
          description: ok
      summary: Is the datasets your org has, each with its name, description, metadata
        and timestamps.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Writes a dataset — the named set of graded examples a run scores
        a model against — under the caller's org and answers 201 with it.

        The NAME is the key, not an id: posting a name the org already has updates that
        dataset's description and metadata and keeps its original creation time, so this
        is create-or-edit and never a duplicate. Its items are untouched.

        Requires a validated principal; 403 without one. The org comes from the
        validated owner claim, never from a client X-Org-Id, so a dataset can only ever
        be written under the caller's own tenant. A description over 64 KiB is 400.
      operationId: post_evals_datasets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/datasetReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/datasetView'
          description: created
      summary: Writes a dataset — the named set of graded examples a run scores a
        model against — under the caller's org and answers 201 with it.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/datasets/{name}:
    delete:
      description: |-
        Removes the named dataset of the caller's org AND all of its
        examples, in one transaction.

        This is not a detach: the examples are gone with the set, so a dataset cannot
        be resurrected by re-creating the name. A name this org does not have is 404 —
        never a silent success — and a name belonging to another tenant is the same
        404, because the delete is predicated on the validated org. Requires a
        validated principal; 403 without one. Runs and scores already recorded against
        the dataset are telemetry events and are NOT deleted with it.
      operationId: delete_evals_datasets_by_name
      parameters:
      - description: Name is the dataset the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/none'
          description: ok
      summary: Removes the named dataset of the caller's org AND all of its examples,
        in one transaction.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one dataset of the caller's org by name, together with its
        live item count — the one read that answers how big the set actually is.

        A name this org does not have is 404, which is also what another tenant's
        dataset looks like from here. Requires a validated principal; 403 without one.
      operationId: get_evals_datasets_by_name
      parameters:
      - description: Name is the dataset the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/datasetView'
          description: ok
      summary: Returns one dataset of the caller's org by name, together with its
        live item count — the one read that answers how big the set actually is.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/datasets/{name}/items:
    get:
      description: |-
        Is the examples in one of your datasets — the set is named in the
        path, because this collection only exists inside one.

        Archived examples are included, so the caller sees the whole set rather than
        only what a run would use. Requires a validated principal; 403 without one, and
        the read is filtered on the validated org, so naming another tenant's dataset
        returns nothing rather than its contents.
      operationId: get_evals_datasets_by_name_items
      parameters:
      - description: |-
          Dataset is the set to read, from the path — this collection only exists
          inside one.
        in: path
        name: name
        required: true
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/itemList'
          description: ok
      summary: Is the examples in one of your datasets — the set is named in the path,
        because this collection only exists inside one.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Writes one graded example — its input, its expected output,
        free-form metadata and a status — into the dataset named in the path, and
        answers 201 with it.

        That dataset MUST already exist for this org: an unknown one is 404, never a
        silent create. Requires a validated principal; 403 without one.
      operationId: post_evals_datasets_by_name_items
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/itemReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/itemView'
          description: created
      summary: Writes one graded example — its input, its expected output, free-form
        metadata and a status — into the dataset named in the path, and answers 201
        with it.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/evaluators:
    get:
      description: |-
        Is the judges your org has defined, each with its judge model,
        criteria and the score name it writes under.

        Requires a validated principal; 403 without one, and the listing is filtered on
        the validated org.
      operationId: get_evals_evaluators
      parameters:
      - description: |-
          Limit caps the rows returned. It defaults to 100 and is capped at 500; a
          non-positive or unparseable value falls back to the default rather than
          failing, because a typo about paging is not a reason to refuse a read.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/evaluatorList'
          description: ok
      summary: Is the judges your org has defined, each with its judge model, criteria
        and the score name it writes under.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Saves a reusable judge for the caller's org — the judge model
        and the written criteria it grades against — and answers 201 with it.

        Like a dataset, the NAME is the key: re-posting a name edits that judge rather
        than adding a second one. Requires a validated principal; 403 without one.
      operationId: post_evals_evaluators
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/evaluatorReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/evaluatorView'
          description: created
      summary: Saves a reusable judge for the caller's org — the judge model and the
        written criteria it grades against — and answers 201 with it.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/metrics:
    get:
      description: |-
        Is your org's AI overview board over a window: totals
        (generations, prompt and completion tokens, cost in cents, errors, success
        rate, distinct models and users), a gap-filled time series, a per-model
        breakdown with the long tail folded into "other", and latency percentiles read
        from the GenAI spans.

        The window the answer was actually computed over is echoed back, so a client
        never has to infer it. A platform admin sees the board across ALL orgs;
        everyone else sees their own.

        The board is HONEST-EMPTY where it cannot be computed: with no datastore wired,
        or under a named project scope the usage ledger does not yet carry, it answers a
        valid board with zero totals and a flat series rather than a fabricated number
        or a 500. Requires a validated principal; 403 without one.
      operationId: get_evals_metrics
      parameters:
      - description: |-
          Range is 24h (the default), 7d or 30d. Anything else normalises to 24h
          rather than failing, so the board always has a valid window.
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: |-
          Interval overrides the bucket the series is grouped into: "hour" or "day".
          Any other value leaves the range's own default in place.
        in: query
        name: interval
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Board'
          description: ok
      summary: 'Is your org''s AI overview board over a window: totals (generations,
        prompt and completion tokens, cost in cents, errors, success rate, distinct
        models and users), a gap-filled time series, a per-model breakdown with the
        long tail folded into "other", and latency percentiles read from the GenAI
        spans.'
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/rubrics:
    get:
      description: |-
        Is the score shapes your org has declared — each name's data
        type, its numeric bounds and its allowed categories.

        Requires a validated principal; 403 without one, and the listing is filtered on
        the validated org.
      operationId: get_evals_rubrics
      parameters:
      - description: |-
          Limit caps the rows returned. It defaults to 100 and is capped at 500; a
          non-positive or unparseable value falls back to the default rather than
          failing, because a typo about paging is not a reason to refuse a read.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scoreConfigList'
          description: ok
      summary: Is the score shapes your org has declared — each name's data type,
        its numeric bounds and its allowed categories.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Defines the shape of one score name for the caller's org and
        answers 201 with it.

        This is the integrity contract, not documentation: once a rubric exists for a
        name, every score recorded under that name is checked against it and the
        rubric's data type is AUTHORITATIVE — a caller cannot claim a different one.
        Out-of-range values, unlisted labels and non-finite numbers are refused at
        write time.

        A CATEGORICAL rubric with no categories is 400, as is a non-finite bound or a
        minValue above maxValue. Requires a validated principal; 403 without one.
      operationId: post_evals_rubrics
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/scoreConfigReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scoreConfigView'
          description: created
      summary: Defines the shape of one score name for the caller's org and answers
        201 with it.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/runs:
    get:
      description: |-
        Is your past runs and how they scored — the dataset and model, the
        judge model, how many examples were attempted and how many scored, the average
        score, and when it happened.

        Requires a validated principal; 403 without one, and rows are filtered on the
        validated org. These records come from the metastore rather than the datastore,
        so they are readable on a deployment with no telemetry wired — but a run's
        traces and scores are not.
      operationId: get_evals_runs
      parameters:
      - description: Dataset narrows to the runs against one dataset.
        in: query
        name: datasetName
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runs'
          description: ok
      summary: Is your past runs and how they scored — the dataset and model, the
        judge model, how many examples were attempted and how many scored, the average
        score, and when it happened.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Runs a real evaluation and answers the summary when it is finished —
        this is synchronous work, not a job id.

        For each ACTIVE example in the dataset it calls the model under test, records a
        trace, calls the LLM-as-judge, and records the judge's score with its
        reasoning. The answer carries the per-item results (item id, trace id, score,
        output or error) alongside items, scored and avgScore.

        The dataset must belong to the caller's org (404 otherwise) and must have at
        least one ACTIVE example (422 otherwise).

        It runs as YOU: the caller's own Authorization bearer drives the model gateway,
        so a request without one is 401 rather than a run made anonymously or under a
        service identity. Only a non-reversible hash of that credential is recorded on
        the traces.

        Bounded and honest about it: an org may have at most 4 runs in flight and the
        fifth is 429 rather than queued, and the whole run is capped at 10 minutes —
        examples past the deadline come back with an error instead of a score, and
        scored counts only real successes. A run where NOTHING scored answers 502, not
        a 200 that looks like an evaluation. A run must be able to persist what it
        produces, so a deployment with no datastore wired is 503 up front. Requires a
        validated principal; 403 without one.
      operationId: post_evals_runs
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/runRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runSummary'
          description: ok
        "502":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runSummary'
          description: bad gateway
      summary: Runs a real evaluation and answers the summary when it is finished
        — this is synchronous work, not a job id.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/scores:
    get:
      description: |-
        Is the score events your org has recorded, narrowed by any of name,
        runName and traceId.

        The org is bound as an authoritative predicate on the query, never taken from a
        header, so a filter can narrow the caller's own scores but can never widen past
        them. Requires a validated principal; 403 without one. Scores live in the
        datastore, so a deployment with none wired answers 503 rather than an empty
        page that would read as "no scores".
      operationId: get_evals_scores
      parameters:
      - description: Name narrows to one score name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: RunName narrows to the scores of one run.
        in: query
        name: runName
        required: false
        schema:
          type: string
      - description: TraceID narrows to the scores on one model call.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scoreList'
          description: ok
      summary: Is the score events your org has recorded, narrowed by any of name,
        runName and traceId.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
    post:
      description: |-
        Files one score event for the caller's org and answers 201 with it.

        This is how human review and out-of-band graders land beside the automatic
        ones: name the score, give it a value (or a stringValue for a categorical
        label), and attach it to a trace, a run, a dataset example, or any combination.

        Scores are validated fail-closed. A value must be FINITE — NaN and Inf are 400
        — and if the org has declared a rubric for this name, that rubric decides the
        type and the value must satisfy it: inside the numeric bounds, or one of the
        allowed categories. A caller cannot override the declared type by sending a
        different dataType.

        A score is TELEMETRY, not metadata, so it needs the datastore: a deployment
        with none wired answers 503 rather than accepting a score it cannot persist.
        Requires a validated principal; 403 without one, and the org is stamped from
        the validated claim rather than read off the body.
      operationId: post_evals_scores
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/scoreReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/scoreView'
          description: created
      summary: Files one score event for the caller's org and answers 201 with it.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/evals/traces:
    get:
      description: |-
        Is the traces behind your evaluations — one per model call an
        evaluation made, carrying its input, output, model and timing — narrowed by any
        of sessionId, runName and datasetName.

        Scoped by org AND by project: the project is the caller's server-minted scope,
        not a parameter, so it cannot be widened by asking. Requires a validated
        principal; 403 without one. Traces live in the datastore, so a deployment with
        none wired answers 503 rather than an empty page.
      operationId: get_evals_traces
      parameters:
      - description: SessionID narrows to one session, which for an evaluation is
          one run.
        in: query
        name: sessionId
        required: false
        schema:
          type: string
      - description: RunName narrows to the calls one run made.
        in: query
        name: runName
        required: false
        schema:
          type: string
      - description: Dataset narrows to the calls made against one dataset.
        in: query
        name: datasetName
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/traceList'
          description: ok
      summary: Is the traces behind your evaluations — one per model call an evaluation
        made, carrying its input, output, model and timing — narrowed by any of sessionId,
        runName and datasetName.
      tags:
      - evals
      x-app: evals
      x-public: true
      x-tool: true
  /v1/event:
    post:
      description: |-
        Stores pageviews, browser errors, identifies and custom commerce events as rows in the caller's own tenant, and answers a receipt {accepted, dropped} that always totals what was sent — a beacon is never silently discarded.

        THE STATUS SAYS WHETHER ANYTHING LANDED, so a green check can never mean an empty warehouse. 200 means at least one event was stored (or that nothing was sent), and a nonzero `dropped` beside a nonzero `accepted` is a PARTIAL batch, never a failed one — a batch is not refused whole for its worst element. If NOTHING was stored the request is an error, and it names the one thing that fixes it: 401 `ingest_key_required` when every event was refused for want of a credential (the same events land with a key), and 400 `unroutable_events` when the caller HAD capability and the body still named nothing storable.

        ONE door for every wire a Hanzo surface emits, dispatched by the SHAPE of the body and never by a second path: a bare event object, a bare array of them, the {batch:[…]} / {events:[…]} envelope, the team console's snake_case array, and the PostHog wire (spelled `distinct_id`/`api_key`, which the canonical wire never uses). BATCH IS A BODY, NOT A PATH — there is no /v1/event/batch, because an array already is one.

        WHAT THE CALLER PRESENTS DECIDES WHAT IT MAY WRITE, and the door itself grants nothing. A validated bearer or an org API key writes the full event at full fidelity. A PUBLISHABLE key (pk-, on Authorization: Bearer, x-hanzo-ingest-key, or ?ingest_key= for navigator.sendBeacon, which cannot set headers) does the same, and is the credential a browser bundle ships: it is deliberately NOT a secret, it resolves WHICH tenant a beacon belongs to and nothing more. A pk- never authenticates and can READ NOTHING — not this org's errors, not a lens, not any other route on this API — so a leaked one lets a stranger write into your stream, and never lets one read out of it. Reading these rows back always takes a real bearer. A Hanzo Team workspace token resolves its org at REDUCED capability: the signed account names the person, so a `distinctId` in the body cannot pin events on a colleague.

        NO CREDENTIAL IS REFUSED: a write the server cannot attribute to a project is 401 `ingest_key_required`, and a credential that IS presented but resolves to no project is 403 `ingest_key_unknown`. Nothing is filed under a shared tenant — events nobody can read are worse than events nobody sent, because the caller is told it succeeded. A browser bundle therefore always ships a pk-, which is what /v1/event.js takes.

        A REDUCED principal — a Hanzo Team workspace token — writes through the PROJECTION into its own org: narrowed to what the SERVER can name (pageviews and errors, plus the closed autocapture vocabulary $click, $input, $change, $submit, $view), where every one of those names is resolved through a server-owned table and stored as that table's value, so the name on the wire is never the name in the row. Stripped, too, to the fields the projection names, so revenue, personId, groupId and every property but the element annotation cannot reach a row — and an exception is carried only on an error, never on an interaction, so a click cannot ship a stack trace into a row's attributes. It does NOT name the person: the signed account is the identity, so a `distinctId` in the body cannot pin events on a colleague. Everything refused is counted in `dropped`.

        The projected lane alone is bounded: 413 over 64 KiB, 400 over 50 events, 429 on the per-client-IP and per-peer caps, and a DNT:1 or Sec-GPC:1 request stores nothing and says so in the receipt. Two stored values carry their own bounds on top, because a request cap does not bound one value: an element annotation over 2 KiB (or a trail over 32 steps) and an exception class over 256 bytes are dropped from the row, which still lands. Authenticated bodies are offered to the observability plane first, which claims LLM-observability ingestion batches and declines everything else.
      operationId: post_event
      requestBody:
        content:
          application/json:
            schema:
              oneOf:
              - $ref: '#/components/schemas/Event'
              - items:
                  $ref: '#/components/schemas/Event'
                type: array
              - $ref: '#/components/schemas/CaptureBatch'
              - $ref: '#/components/schemas/insightsBody'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureResult'
          description: Success
      summary: Capture product events into your org's warehouse
      tags:
      - analytics
      x-app: analytics
      x-public: true
  /v1/event.js:
    get:
      description: |-
        Serves the browser tag that autocaptures pageviews (initial and SPA) and uncaught errors onto the canonical wire at POST /v1/event.

        Install is one line, and it is the same line for a Hanzo property and for a customer's own page:

            <script defer src="https://api.hanzo.ai/v1/event.js" data-key="pk-…"></script>

        `data-key` is the publishable key the project mints; `data-product` optionally names the emitting surface. The key may also ride the src as `?key=` for a host that strips data attributes.

        WITHOUT A KEY THE TAG SENDS NOTHING. A keyless beacon is accepted 200 into $public, a reserved tenant the owning org cannot read — so silence is the honest failure, and the tag picks it rather than reporting success into a tenant nobody reads.
      operationId: get_event.js
      responses:
        2XX:
          content:
            application/javascript:
              schema:
                format: binary
                type: string
          description: Success
      summary: The Hanzo event tag — the one-line install for a surface with no bundler
      tags:
      - analytics
      x-app: analytics
      x-public: true
  /v1/event/{project}/envelope:
    post:
      description: |-
        Accepts the CURRENT Sentry wire — the framed envelope a modern SDK posts, carrying its items in one request — so an application already instrumented with Sentry reports into Hanzo's error tracking by pointing its DSN here and changing nothing else.

        CLOUD ROUTES IT AND READS NONE OF IT. The body is relayed byte-for-byte to the observability plane, which parses the wire, verifies the credential and answers; this door declares no response shape because it does not know one. A deployment with no observability plane mounted answers 503.

        THE CREDENTIAL IS A SENTRY DSN KEY, NOT A HANZO PRINCIPAL. This is one of the few writes on the platform that carries no bearer and no org header by design — a Sentry SDK has neither — and it is exempt from the principal gate for that reason. The observability plane verifies the DSN key itself, fail-closed: a request without a valid one is refused there, never admitted here. Presenting a Hanzo bearer instead does nothing.

        `project` IS THE DSN'S PROJECT ID — the identifier in the DSN the SDK was configured with, and what the tenant is derived from. It is NOT a Hanzo IAM project and NOT a todo project key. Only these two ingest paths map through: no observability READ API is reachable by any other suffix under this prefix.
      operationId: post_event_by_project_envelope
      parameters:
      - in: path
        name: project
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Sentry SDK envelope ingest — errors and traces from an unmodified Sentry
        client
      tags:
      - analytics
      x-app: analytics
      x-public: true
  /v1/event/{project}/store:
    post:
      description: |-
        Accepts the LEGACY Sentry wire: one event per request, what an SDK predating envelopes sends. Same door, same credential, same destination as the envelope endpoint — kept open so an old client reports without being upgraded first. New instrumentation has no reason to choose it.

        CLOUD ROUTES IT AND READS NONE OF IT. The body is relayed byte-for-byte to the observability plane, which parses the wire, verifies the credential and answers; this door declares no response shape because it does not know one. A deployment with no observability plane mounted answers 503.

        THE CREDENTIAL IS A SENTRY DSN KEY, NOT A HANZO PRINCIPAL. This is one of the few writes on the platform that carries no bearer and no org header by design — a Sentry SDK has neither — and it is exempt from the principal gate for that reason. The observability plane verifies the DSN key itself, fail-closed: a request without a valid one is refused there, never admitted here. Presenting a Hanzo bearer instead does nothing.

        `project` IS THE DSN'S PROJECT ID — the identifier in the DSN the SDK was configured with, and what the tenant is derived from. It is NOT a Hanzo IAM project and NOT a todo project key. Only these two ingest paths map through: no observability READ API is reachable by any other suffix under this prefix.
      operationId: post_event_by_project_store
      parameters:
      - in: path
        name: project
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Sentry SDK store ingest — the legacy single-event wire
      tags:
      - analytics
      x-app: analytics
      x-public: true
  /v1/exec:
    post:
      description: |-
        Executes a program in a throwaway sandbox and answers with what it printed
        and what it left behind.

        `lang` names one of the thirteen the sandbox image carries — py, js, ts, bash, r,
        php, go, rs, c, cpp, java, d, f90 — and `code` is the whole program, not a
        fragment: a compiled language is compiled and then run, an interpreted one is
        interpreted, and `args` becomes the program's own argv either way. Nothing is
        installed for you; the image is the environment.

        A PROGRAM THAT FAILS IS A SUCCESSFUL CALL. A non-zero exit answers 200 with the
        diagnostics on `stderr`, because "the code threw" and "the interpreter is down"
        are different facts a caller renders differently. Only the second is an error
        status.

        Runs are stateful through `session_id`. Omit it and the run gets a fresh sandbox
        whose id comes back on the answer; pass that id again and the next run sees the
        same filesystem, so a program can write a file one call and read it the next.
        `files` names bytes already uploaded to a session (POST /v1/exec/upload), copied in
        before the program starts. `files` on the ANSWER is what the program created or
        changed, by comparison against a marker taken at start — so it is the run's real
        output, not a listing of the directory — and each is fetched from
        GET /v1/exec/download/{session}/{name}.

        The tenant is the caller's, never the body's, at every door. A typed op is also
        an MCP tool and an op-plane op; MCP's tools/call invokes it directly, with no
        route and therefore no middleware, so nothing there could have checked a
        credential. tenantOf refuses a context carrying neither a validated principal nor
        exec's own admission marker, so those doors fail closed without a second gate to
        keep in step.
      operationId: post_exec
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CodeRun'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CodeResult'
          description: ok
      summary: Run a code snippet in a sandboxed interpreter
      tags:
      - exec
      x-app: exec
      x-public: true
      x-tool: true
  /v1/exec/files/{sid}:
    get:
      description: |-
        Lists what a session's sandbox holds — the uploads a run can read and the artifacts it produced — each then fetched from /v1/exec/download.

        It answers a BARE JSON ARRAY of {name, lastModified}, where `name` is the same {session_id}/{fileId} identifier download takes, because that is what the client matches on. An object wrapper would be a wire change, which is why this is not a typed operation.
      operationId: get_exec_files_by_sid
      parameters:
      - in: path
        name: sid
        required: true
        schema:
          type: string
      summary: List the files in an execution session
      tags:
      - exec
      x-app: exec
      x-public: true
  /v1/exec/programmatic:
    post:
      description: 'Answers 501. This address belongs to a DIFFERENT protocol from
        /v1/exec: the server suspends a program on each tool call, returns the pending
        calls with a continuation token, and resumes when the client posts results
        back. Serving it means implementing suspension and resumption, so it refuses
        in the open rather than answering with a shape the caller''s parser cannot
        read.'
      operationId: post_exec_programmatic
      summary: Programmatic tool calling (not served here)
      tags:
      - exec
      x-app: exec
      x-public: true
  /v1/exec/upload:
    post:
      description: |-
        Takes a multipart upload and writes the file into the session's sandbox, so a later run can read it. Answers the session id and the identifier the file is addressed by; `session_id` in the form joins an existing session instead of opening one.

        The body is multipart/form-data, which is why this is not a typed operation: every non-empty typed body is decoded as JSON.
      operationId: post_exec_upload
      summary: Upload a file into an execution session
      tags:
      - exec
      x-app: exec
      x-public: true
  /v1/flags:
    post:
      description: |-
        Evaluate runs the caller's flag definitions for one identity and returns the
        flag verdict: which flags are on (or which variant), their payloads,
        and whether any definition failed to compute. Evaluation is in-process over the
        caller's own (org, project) definitions — no network hop, no shared KV — so a
        tenant can only ever evaluate its own flags.
      operationId: post_flags
      requestBody:
        content:
          application/json:
            example:
              distinct_id: u1
              groups:
                "0":
                  key: acme
              person_properties:
                plan: pro
            schema:
              $ref: '#/components/schemas/evaluateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                errorsWhileComputingFlags: false
                featureFlagPayloads: {}
                featureFlags:
                  new-editor: true
              schema: {}
          description: ok
      summary: 'Evaluate runs the caller''s flag definitions for one identity and
        returns the flag verdict: which flags are on (or which variant), their payloads,
        and whether any definition failed to compute.'
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flags/activity:
    get:
      description: |-
        Returns the caller's flag change log newest-first: every
        create, update and delete, with the actor and the time.
      operationId: get_flags_activity
      parameters:
      - description: Limit caps the rows returned. 1–500; anything else takes the
          default 100.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activityOut'
          description: ok
      summary: 'Returns the caller''s flag change log newest-first: every create,
        update and delete, with the actor and the time.'
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flags/decide:
    post:
      description: |-
        Evaluate runs the caller's flag definitions for one identity and returns the
        flag verdict: which flags are on (or which variant), their payloads,
        and whether any definition failed to compute. Evaluation is in-process over the
        caller's own (org, project) definitions — no network hop, no shared KV — so a
        tenant can only ever evaluate its own flags.
      operationId: post_flags_decide
      requestBody:
        content:
          application/json:
            example:
              distinct_id: u1
              groups:
                "0":
                  key: acme
              person_properties:
                plan: pro
            schema:
              $ref: '#/components/schemas/evaluateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                errorsWhileComputingFlags: false
                featureFlagPayloads: {}
                featureFlags:
                  new-editor: true
              schema: {}
          description: ok
      summary: 'Evaluate runs the caller''s flag definitions for one identity and
        returns the flag verdict: which flags are on (or which variant), their payloads,
        and whether any definition failed to compute.'
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flags/defs:
    get:
      description: |-
        Returns every flag definition in the caller's (org,
        project) store, by key, with its version and who last changed it.
      operationId: get_flags_defs
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/defsOut'
          description: ok
      summary: Returns every flag definition in the caller's (org, project) store,
        by key, with its version and who last changed it.
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flags/defs/{key}:
    delete:
      description: |-
        Removes one flag definition by key and records the
        deletion in the change log. A key the caller's store does not hold is a 404.
      operationId: delete_flags_defs_by_key
      parameters:
      - description: Key is the flag key to act on, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deletedOut'
          description: ok
      summary: Removes one flag definition by key and records the deletion in the
        change log.
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one flag definition by key, or 404 when the caller's
        store has none under that key.
      operationId: get_flags_defs_by_key
      parameters:
      - description: Key is the flag key to act on, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DefRow'
          description: ok
      summary: Returns one flag definition by key, or 404 when the caller's store
        has none under that key.
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
    put:
      description: |-
        Creates or replaces the flag definition at the path's key and
        returns the stored row. The BODY IS THE DEFINITION DOCUMENT — the flag-definition
        JSON object the evaluator consumes — and it is stored verbatim except that its
        "key" is forced to the key in the URL, so a document can never be filed under a
        name other than the one it was addressed by. Every write bumps the version and
        appends to the change log under the caller's identity.
      operationId: put_flags_defs_by_key
      parameters:
      - description: Key is the flag key to write, from the path.
        example: new-editor
        in: path
        name: key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              active: true
              filters:
                groups:
                - rollout_percentage: 25
              key: new-editor
            schema: {}
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DefRow'
          description: ok
      summary: Creates or replaces the flag definition at the path's key and returns
        the stored row.
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flags/health:
    get:
      description: |-
        Health reports that the flag engine is serving. It is not gated: liveness must
        be probe-able without a token.
      operationId: get_flags_health
      responses:
        "200":
          content:
            application/json:
              example:
                engine: hanzo-flags
                ok: true
              schema:
                $ref: '#/components/schemas/healthOut'
          description: ok
      summary: Health reports that the flag engine is serving.
      tags:
      - flags
      x-app: flags
      x-public: true
      x-tool: true
  /v1/flow/runs:
    get:
      description: |-
        Runs reads one workflow's recorded runs: every component build with its
        result, keyed by component. Ownership is verified first — run records never
        cross the org boundary.
      operationId: get_flow_runs
      parameters:
      - description: |-
          Workflow is the UUID of the workflow whose run records to read. It rides
          the query string.
        in: query
        name: workflow
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: 'Runs reads one workflow''s recorded runs: every component build with
        its result, keyed by component.'
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
    post:
      description: |-
        Run executes one of the caller's workflows synchronously: the graph runs in
        the flow service and the response carries the run's session and outputs. A
        graph whose components fail reports the product's own error. Runs are
        bounded by the product's five-minute sync ceiling.
      operationId: post_flow_runs
      requestBody:
        content:
          application/json:
            example:
              input: summarize today's tickets
              workflow: 8f14e45f-…
            schema:
              $ref: '#/components/schemas/flowRun'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: 'Run executes one of the caller''s workflows synchronously: the graph
        runs in the flow service and the response carries the run''s session and outputs.'
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
  /v1/flow/status:
    get:
      description: |-
        Status reports whether the flow service is reachable and which version it
        runs. It is the product's own /health and /v1/version composed — an honest
        lens for "is the workflow plane up", never a fabricated ok.
      operationId: get_flow_status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/flowStatus'
          description: ok
      summary: Status reports whether the flow service is reachable and which version
        it runs.
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
  /v1/flow/workflows:
    get:
      description: |-
        Workflows lists the caller's workflows, paged. The list is scoped
        server-side to the org's project — the page can only ever hold the caller's
        own workflows.
      operationId: get_flow_workflows
      parameters:
      - description: Page is the 1-based page of workflows to return.
        in: query
        name: page
        required: false
        schema:
          type: string
      - description: Size is how many workflows one page holds (the product caps it
          at 100).
        in: query
        name: size
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Workflows lists the caller's workflows, paged.
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a workflow in the caller's org. The org's project id
        is pinned server-side from the validated principal — there is no field by
        which a caller could place a workflow in another org.
      operationId: post_flow_workflows
      requestBody:
        content:
          application/json:
            example:
              description: route tickets
              name: support-triage
            schema:
              $ref: '#/components/schemas/flowCreate'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Creates a workflow in the caller's org.
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
  /v1/flow/workflows/{workflow}:
    delete:
      description: |-
        Deletes one of the caller's workflows and its runs. Ownership
        is verified first; a foreign id answers 404 and deletes nothing.
      operationId: delete_flow_workflows_by_workflow
      parameters:
      - description: Workflow is the workflow's UUID, taken from the path.
        in: path
        name: workflow
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Deletes one of the caller's workflows and its runs.
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
    get:
      description: |-
        Workflow reads one of the caller's workflows — the full record, graph
        included. A workflow outside the caller's org answers 404, indistinguishable
        from one that does not exist.
      operationId: get_flow_workflows_by_workflow
      parameters:
      - description: Workflow is the workflow's UUID, taken from the path.
        in: path
        name: workflow
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Workflow reads one of the caller's workflows — the full record, graph
        included.
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
    patch:
      description: |-
        Patches one of the caller's workflows: name, description,
        graph, or the locked flag — only the stated fields move. Ownership is
        verified before the patch reaches the product.
      operationId: patch_flow_workflows_by_workflow
      parameters:
      - description: Workflow is the workflow's UUID, taken from the path.
        in: path
        name: workflow
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              description: route tickets to the right queue
            schema:
              $ref: '#/components/schemas/flowUpdate'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: 'Patches one of the caller''s workflows: name, description, graph,
        or the locked flag — only the stated fields move.'
      tags:
      - flow
      x-app: flow
      x-public: true
      x-tool: true
  /v1/functions:
    get:
      description: |-
        Is every serverless function the caller's org has published, each with its
        real 7-day rollup.

        A row carries the function's runtime, resource limits, deployment target and its
        invoke endpoint, plus envCount — how many secrets it mounts. The rollup fields
        are ABSENT rather than zero when the function has not run in the window, so a
        console renders "—" instead of a fabricated 0.

        Requires a validated principal; the listing is scoped to its org.
      operationId: get_functions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/fnList'
          description: ok
      summary: Is every serverless function the caller's org has published, each with
        its real 7-day rollup.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
    post:
      description: |-
        Publishes a serverless function under the caller's org and answers 201
        with it.

        The name is the key and is claimed once; the names that would shadow a
        collection route are reserved. runtime and environment are the same field —
        either spelling is accepted — and default to node.

        Bounds are clamped rather than refused where a clamp is honest: a timeout above
        the 900-second ceiling becomes the ceiling instead of silently reverting to the
        30-second default, and an omitted memory limit becomes 256Mi. target=fleet runs
        on the org's own GPU fleet and supports runtime=python only.

        Requires a validated principal; the function is owned by that principal's org.
      operationId: post_functions
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/definition'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/functionView'
          description: created
      summary: Publishes a serverless function under the caller's org and answers
        201 with it.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/{name}:
    delete:
      description: |-
        Removes one of the caller org's functions and answers 204.

        A name this org does not hold is 404 — never a silent success — and a name
        belonging to another tenant is the same 404, because the delete is predicated on
        the validated org.
      operationId: delete_functions_by_name
      parameters:
      - description: Name is the function the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/none'
          description: no content
      summary: Removes one of the caller org's functions and answers 204.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
    get:
      description: |-
        Is one function with everything a detail page needs in one round-trip: its
        definition, its 7-day rollup, its trigger, its twenty most recent invocations
        and the NAMES of the secrets it mounts.

        Secret values are never read or returned. A name the caller's org does not hold
        is 404, which is also what another tenant's function looks like from here.
      operationId: get_functions_by_name
      parameters:
      - description: Name is the function the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/functionDetail'
          description: ok
      summary: 'Is one function with everything a detail page needs in one round-trip:
        its definition, its 7-day rollup, its trigger, its twenty most recent invocations
        and the NAMES of the secrets it mounts.'
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/{name}/invocations:
    get:
      description: |-
        Is one function's past runs, newest first — each with its status,
        HTTP code, method, time and duration.

        These are real recorded rows, not a projection: an invocation appears here only
        once it actually ran. Requires a validated principal; the read is scoped to its
        org.
      operationId: get_functions_by_name_invocations
      parameters:
      - description: Name is the function the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Limit caps the page, defaulting to 100.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invocationList'
          description: ok
      summary: Is one function's past runs, newest first — each with its status, HTTP
        code, method, time and duration.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/{name}/invoke:
    post:
      description: |-
        Runs a function and records a REAL invocation.

        The answer is the invocation record whatever happened to it: 200 when the org's
        code ran clean, 502 when it ran and failed, 503 when this deployment has no
        sandbox to run code in. The record IS the evidence, so it rides the failure
        rather than being replaced by an error envelope.

        Billing is two-part and both parts are prepaid-then-metered on the one shared
        meter: a flat per-invocation request fee, gated BEFORE any sandbox compute runs
        so an unfunded org gets 402 and nothing executes, and a usage-native
        GB-seconds compute debit taken after the run. Either is independently free when
        its fee is zero, so an operator can bill by request alone, by compute alone, or
        by both — and a zero request fee removes the balance gate with it.

        A TRANSPORT failure is not charged: the sandbox being unreachable ran no
        billable compute. Code that ran and exited non-zero IS charged — that is a
        successful invocation of a failing program, not a billing failure.

        When the sandbox is not configured on this deployment, a non-fleet function
        fails closed before anything is recorded — no execution and no fabricated
        output. Scoped to the caller's org; requires a validated principal.
      operationId: post_functions_by_name_invoke
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/invokeReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invocationView'
          description: ok
        "502":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invocationView'
          description: bad gateway
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invocationView'
          description: service unavailable
      summary: Runs a function and records a REAL invocation.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/{name}/logs:
    get:
      description: |-
        Is the output of a function's most recent run — its error text when that
        run failed, else what it printed.

        It is the LAST run only, and it is empty when the function has never run. There
        is no log retention behind this beyond the recorded invocation itself.
      operationId: get_functions_by_name_logs
      parameters:
      - description: Name is the function the URL names.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/logLines'
          description: ok
      summary: Is the output of a function's most recent run — its error text when
        that run failed, else what it printed.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/deployments:
    get:
      description: |-
        Is what is live right now — each function's current record IS its
        live deployment, so this is the deployment inventory.

        There is no deployment history behind it: a function has one record, and
        publishing replaces it. The 7-day rollup is deliberately absent here, because
        this read is about what is deployed rather than about how it has performed.
      operationId: get_functions_deployments
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/fnList'
          description: ok
      summary: Is what is live right now — each function's current record IS its live
        deployment, so this is the deployment inventory.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/metrics:
    get:
      description: |-
        Is the org's serverless dashboard over a window: a per-function
        invocation costLine and how those invocations ended.

        Every point is a REAL count of rows that fell in that bucket — nothing is
        interpolated or invented, so an empty window draws a flat line rather than a
        fabricated one.

        costCents is null and stays null: there is no per-invocation cost source to read,
        and reporting a number computed some other way would be a guess presented as a
        measurement. Requires a validated principal; the read is scoped to its org.
      operationId: get_functions_metrics
      parameters:
      - description: |-
          Range is 1H, 6H, 24H (the default), 7D or 30D. Anything else falls back to
          24H rather than failing.
        in: query
        name: range
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/usage'
          description: ok
      summary: 'Is the org''s serverless dashboard over a window: a per-function invocation
        costLine and how those invocations ended.'
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/secrets:
    get:
      description: |-
        Is the NAMES of the secrets the caller org's functions mount.

        Values are NEVER read or returned — this surface knows which names a function
        asks for and nothing about what is behind them, which is what makes it safe to
        list at all. One row per distinct (namespace, name).
      operationId: get_functions_secrets
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/secretList'
          description: ok
      summary: Is the NAMES of the secrets the caller org's functions mount.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/functions/triggers:
    get:
      description: |-
        Is what calls the caller org's functions — one row per function.

        Every function has exactly one trigger today, its HTTP invoke endpoint, so this
        is the function list read as "how is each of these reached".
      operationId: get_functions_triggers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/triggerList'
          description: ok
      summary: Is what calls the caller org's functions — one row per function.
      tags:
      - functions
      x-app: functions
      x-public: true
      x-tool: true
  /v1/gateway/config:
    get:
      description: |-
        Read returns the EFFECTIVE edge policy the caller is subject to: the platform CORS
        allowlist and pre-auth per-IP flood cap in force, plus the caller's own authenticated
        rate ceiling, edge-cache TTLs and accepted-method allowlist. A SuperAdmin may inspect
        a specific tenant's effective policy with ?org=<slug>.
      operationId: get_gateway_config
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Policy'
          description: ok
      summary: 'Read returns the EFFECTIVE edge policy the caller is subject to: the
        platform CORS allowlist and pre-auth per-IP flood cap in force, plus the caller''s
        own authenticated rate ceiling, edge-cache TTLs and accepted-method allowlist.'
      tags:
      - gateway
      x-app: gateway
      x-public: true
      x-tool: true
    put:
      description: |-
        Write updates one policy scope and returns the policy in force after the write.
        A body carrying any PLATFORM field (cors_origins, per_ip_rpm, window_sec) is a
        platform write and requires SuperAdmin; otherwise it is a per-org write (org_rpm,
        cache_ttl_sec, cache_paths, methods) scoped to the caller's own org — or, for a
        SuperAdmin, the tenant named by ?org=<slug>. A body that sets nothing is a 400.
        The abuse gate's mode is an OPERATOR field: setting it requires SuperAdmin,
        whichever organization it lands on. updated_at and updated_by are
        server-stamped; a client-supplied value is ignored.
      operationId: put_gateway_config
      requestBody:
        content:
          application/json:
            example:
              cache_ttl_sec: 30
              methods:
              - GET
              - POST
              org_rpm: 120
            schema:
              $ref: '#/components/schemas/Policy'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Policy'
          description: ok
      summary: Write updates one policy scope and returns the policy in force after
        the write.
      tags:
      - gateway
      x-app: gateway
      x-public: true
      x-tool: true
  /v1/gateway/traffic:
    get:
      description: |-
        Traffic reports who is calling this organization's API right now: the request
        count for the last minute split by AGENCY LANE — agent, human, bot, unknown —
        and the busiest callers behind it, each with its request count, its
        authentication-failure count, how many distinct paths it touched, and any
        verdict currently held against it.

        The lane split is the answer to the question a generic bot filter cannot
        answer: which of this traffic is the customer's own automation and which is
        somebody working through a list. It is computed from credentials we issued, not
        from the client's self-description, so a scraper cannot move itself into the
        agent lane by editing a header.

        A validated caller appears as a FINGERPRINT — a one-way, per-process digest. It
        is stable enough to recognise the same caller across a minute and cannot be
        turned back into a key, so this report is safe to read, screenshot and paste.

        It also reports what the sensor's own ceilings are doing (strain, tracked,
        ceiling, refused) and how many screens the scorer did not answer (unscored), so
        a control that has stopped measuring or a judge that has stopped answering is a
        number here rather than a quiet day.

        Scoped to the caller's own validated organization. A SuperAdmin may inspect a
        specific tenant with ?org=<slug>, or the lane that has no tenant — every caller
        the identity boundary could not validate — with an empty ?org=.
      operationId: gatewayTraffic
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TrafficView'
          description: ok
      summary: Report who is calling this org's API right now
      tags:
      - gateway
      x-app: gateway
      x-public: true
      x-tool: true
  /v1/git:
    get:
      description: The repository list for the signed-in caller's org — each repo
        with its description, default branch, size and last update. SIGNED OUT it
        renders the public explore page instead of refusing, because most Hanzo repos
        are open source and the open face is the default one; signed in, the caller's
        own org shows its private repositories alongside its public ones. This is
        a server-rendered browser page, not JSON — the console repo-browser reads
        the same repository through the JSON ops under /v1/git/repos. Repository names,
        paths and file contents all render through auto-escaping templates rather
        than being concatenated into HTML.
      operationId: get_git
      summary: Browse your org's repositories
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{project}/{repo}/git-receive-pack:
    post:
      description: |-
        The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.

        Once the pack is on disk the repository's storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.

        A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: post_git_by_org_by_project_by_repo_git-receive-pack
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: project
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Accept a push, and turn it into a build
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{project}/{repo}/git-upload-pack:
    post:
      description: |-
        The pack-transfer phase of a clone or fetch: the request and the response are git's binary pack protocol, streamed straight through git itself — request body to git's stdin, git's stdout to the response — so a multi-gigabyte clone never lands in this process's memory.

        A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: post_git_by_org_by_project_by_repo_git-upload-pack
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: project
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Serve a clone or fetch
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{project}/{repo}/info/refs:
    get:
      description: |-
        The ref-advertisement phase of git's smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.

        ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, with the PROJECT as a middle path segment: project scope otherwise rides a header a git client cannot send, so this path is the only usable remote for a project-scoped repository. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: get_git_by_org_by_project_by_repo_info_refs
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: project
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      summary: Advertise a repository's refs to a git client
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{repo}:
    get:
      description: 'A repository at a glance: its branches, the tree at the tip, its
        most recent commits, its README rendered, and the HTTPS and SSH clone URLs.
        `?ref=` selects a branch, tag or commit; the default branch is used when it
        is omitted. A repository with no commits yet renders its clone instructions
        rather than an error, which is what a caller who has just created one needs
        to see. A public repository is readable by anyone; a private one only by its
        own org. A repository that does not exist and one belonging to another org
        answer the SAME 404, so the page is never an existence oracle. This is a server-rendered
        browser page, not JSON — the console repo-browser reads the same repository
        through the JSON ops under /v1/git/repos. Repository names, paths and file
        contents all render through auto-escaping templates rather than being concatenated
        into HTML.'
      operationId: get_git_by_org_by_repo
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      summary: Open a repository's home page
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{repo}/commits:
    get:
      description: The hundred most recent commits on one ref, each with its author,
        message and date. `?ref=` selects the branch, tag or commit, defaulting to
        the repository's default branch; an unknown one is 404. A public repository
        is readable by anyone; a private one only by its own org. A repository that
        does not exist and one belonging to another org answer the SAME 404, so the
        page is never an existence oracle. This is a server-rendered browser page,
        not JSON — the console repo-browser reads the same repository through the
        JSON ops under /v1/git/repos. Repository names, paths and file contents all
        render through auto-escaping templates rather than being concatenated into
        HTML.
      operationId: get_git_by_org_by_repo_commits
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      summary: Read a repository's commit log
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{repo}/git-receive-pack:
    post:
      description: |-
        The pack-transfer phase of a push, and the point at which a push becomes an EVENT. NEVER ANONYMOUS: a push always requires an authenticated org, and the org in the path must equal it.

        Once the pack is on disk the repository's storage usage is metered and a build is fired for every branch whose tip actually moved, computed from the before/after branch diff rather than from what the client claimed. That runs on a cancel-immune context, so a client that hangs up the moment its push lands still gets its build, and it runs even when git itself exited non-zero — the refs on disk are the ground truth. Repacking housekeeping is detached and never blocks the response.

        A Content-Type other than `application/x-git-receive-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: post_git_by_org_by_repo_git-receive-pack
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Accept a push, and turn it into a build
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{repo}/git-upload-pack:
    post:
      description: |-
        The pack-transfer phase of a clone or fetch: the request and the response are git's binary pack protocol, streamed straight through git itself — request body to git's stdin, git's stdout to the response — so a multi-gigabyte clone never lands in this process's memory.

        A PUBLIC repository is fetched anonymously; a private one requires its own org, and a wrong or absent org is 404 rather than a hint that the repository exists. A Content-Type other than `application/x-git-upload-pack-request` is 400. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: post_git_by_org_by_repo_git-upload-pack
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      summary: Serve a clone or fetch
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/{org}/{repo}/info/refs:
    get:
      description: |-
        The ref-advertisement phase of git's smart-HTTP protocol — the first request a clone, a fetch and a push all make. `?service=` selects which: `git-upload-pack` advertises for a fetch, `git-receive-pack` for a push, and any other value is 400.

        ANONYMOUS ONLY FOR FETCH, AND ONLY ON A PUBLIC REPOSITORY. The push advertisement always requires an authenticated org, and where a path org is present it must equal the authenticated one. A private repository reached without its org is 404, indistinguishable from one that does not exist. Addressed under the API prefix, so `git clone https://<host>/v1/git/<org>/<repo>.git` works on any host the binary serves. This is git's own wire protocol, not an API call to make by hand: point a git client at the clone URL and it makes this request itself.
      operationId: get_git_by_org_by_repo_info_refs
      parameters:
      - in: path
        name: org
        required: true
        schema:
          type: string
      - in: path
        name: repo
        required: true
        schema:
          type: string
      summary: Advertise a repository's refs to a git client
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/explore:
    get:
      description: 'The open, unauthenticated face of the git host: every PUBLIC repository
        in the fleet, org-qualified, so a project can be found and cloned with no
        account at all — signing in is for private repos and for writes. Repositories
        live in per-org stores with no global index, so this unions each org''s public
        rows and is bounded to a fixed number of stores per request, keeping discovery
        quick however many orgs exist. A fleet with no orgs yet is an empty page,
        not an error. This is a server-rendered browser page, not JSON — the console
        repo-browser reads the same repository through the JSON ops under /v1/git/repos.
        Repository names, paths and file contents all render through auto-escaping
        templates rather than being concatenated into HTML.'
      operationId: get_git_explore
      summary: Discover public repositories across every org
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/keys:
    get:
      description: |-
        Returns the SSH public keys registered to the caller's org — the keys
        that authenticate `git clone git@<host>:<org>/<repo>.git`. Keys are org-scoped
        on read even though the fingerprint index is global, so one org never sees
        another's.
      operationId: get_git_keys
      responses:
        "200":
          content:
            application/json:
              example:
                data:
                - createdAt: "2026-07-01T10:00:00Z"
                  fingerprint: SHA256:9pQ…
                  id: gitkey_4a1b
                  publicKey: ssh-ed25519 AAAAC3Nz…
                  title: laptop
              schema:
                $ref: '#/components/schemas/keyList'
          description: ok
      summary: Returns the SSH public keys registered to the caller's org — the keys
        that authenticate `git clone git@<host>:<org>/<repo>.git`.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers an SSH public key so it can authenticate `git clone
        git@<host>:<org>/<repo>.git` for the caller's org. The key line is parsed and
        canonicalized before storage, its SHA256 fingerprint becomes the auth lookup
        handle, and the full public key round-trips (it is public). Answers 201.
        Fingerprints are globally unique, so a key already registered — to this org or
        any other — is a 409: one key belongs to exactly one org.
      operationId: post_git_keys
      requestBody:
        content:
          application/json:
            example:
              publicKey: ssh-ed25519 AAAAC3Nz… z@hanzo.ai
              title: laptop
            schema:
              $ref: '#/components/schemas/registerKeyReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/keyView'
          description: created
      summary: Registers an SSH public key so it can authenticate `git clone git@<host>:<org>/<repo>.git`
        for the caller's org.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/keys/{id}:
    delete:
      description: |-
        Removes a registered SSH key, scoped to the caller's org: an org can
        only delete its own, and a key id it does not own is not found. Answers 204
        with no body. Once removed the key no longer authenticates any SSH git access.
      operationId: delete_git_keys_by_id
      parameters:
      - description: ID is the key's identifier ("gitkey_…"), from the :id path segment.
        example: gitkey_4a1b
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: 'Removes a registered SSH key, scoped to the caller''s org: an org
        can only delete its own, and a key id it does not own is not found.'
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos:
    get:
      description: |-
        Returns the repos in the caller's scope, most recently updated
        first. The scope is the request principal's — the gateway-minted org and its
        optional project — never anything off the wire, so a caller only ever sees its
        own. Rows carry no branches or HEAD; read one repo for those.
      operationId: get_git_repos
      responses:
        "200":
          content:
            application/json:
              example:
                data:
                - cloneUrl: https://api.hanzo.ai/v1/git/acme/widgets.git
                  createdAt: "2026-07-01T10:00:00Z"
                  defaultBranch: main
                  id: repo_9f3c
                  name: widgets
                  org: acme
                  public: false
                  sizeBytes: 4096
                  sshUrl: git@git.hanzo.ai:acme/widgets.git
              schema:
                $ref: '#/components/schemas/repoList'
          description: ok
      summary: Returns the repos in the caller's scope, most recently updated first.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    post:
      description: |-
        Provisions an empty bare repository in the caller's scope and
        returns it with its clone URLs. Answers 201. The name must be unique within
        the scope — a repeat is a 409, never a silent overwrite of an existing repo.
        The org comes from the validated principal, so a repo is always born owned by
        the caller's own tenant.
      operationId: post_git_repos
      requestBody:
        content:
          application/json:
            example:
              description: the widget service
              name: widgets
            schema:
              $ref: '#/components/schemas/createReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/repoView'
          description: created
      summary: Provisions an empty bare repository in the caller's scope and returns
        it with its clone URLs.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}:
    delete:
      description: |-
        Removes a repo's metadata and purges its storage. Answers 204 with
        no body. The metadata row is the source of truth for existence, so a storage
        purge that fails is logged and the delete still succeeds — and a second call
        is a 404, not a second delete.
      operationId: delete_git_repos_by_name
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a repo's metadata and purges its storage.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one repo with its live ref state: every branch name and the
        resolved HEAD commit. Both are read from the object store on each call, so an
        empty repo reports no branches and an empty head rather than failing. A repo
        outside the caller's scope is not found.
      operationId: get_git_repos_by_name
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/repoView'
          description: ok
      summary: 'Returns one repo with its live ref state: every branch name and the
        resolved HEAD commit.'
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    patch:
      description: |-
        Flips a repo's public bit, the one mutable repo setting today.
        Public grants ANONYMOUS fetch only; push and the whole control plane stay
        org-authed. Returns the updated repo.
      operationId: patch_git_repos_by_name
      parameters:
      - description: Name is the repo to update, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              name: widgets
              public: true
            schema:
              $ref: '#/components/schemas/patchIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/repoView'
          description: ok
      summary: Flips a repo's public bit, the one mutable repo setting today.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/blob:
    get:
      description: |-
        Returns one file's bytes at one revision. Text comes back verbatim,
        binary comes back base64, and a file past the 1 MiB view cap comes back marked
        truncated with NO content — the client is expected to clone instead.
      operationId: get_git_repos_by_name_blob
      parameters:
      - description: Name is the repo to read, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Ref is a branch, tag or commit; empty means the repo's HEAD.
        example: main
        in: query
        name: ref
        required: false
        schema:
          type: string
      - description: Path is repo-relative; empty is the tree root. Traversal is stripped.
        example: go.mod
        in: query
        name: path
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                binary: false
                content: |
                  module widgets
                encoding: utf8
                path: go.mod
                size: 42
                truncated: false
              schema:
                $ref: '#/components/schemas/blobJSON'
          description: ok
      summary: Returns one file's bytes at one revision.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/commits:
    get:
      description: |-
        Walks a ref's history newest first, or one path's history when a
        path is given. There is no cursor: the page is the newest `limit` commits.
      operationId: get_git_repos_by_name_commits
      parameters:
      - description: Name is the repo to read, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Ref is the branch, tag or commit to walk back from; empty means
          HEAD.
        example: main
        in: query
        name: ref
        required: false
        schema:
          type: string
      - description: Path narrows the history to commits touching it; empty walks
          the whole ref.
        in: query
        name: path
        required: false
        schema:
          type: string
      - description: Limit caps the page. Anything not positive means 50; the cap
          is 100.
        example: 2
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              example:
                commits:
                - authorEmail: ada@hanzo.ai
                  authorName: Ada
                  date: "2026-07-01T10:00:00Z"
                  message: add the widget service
                  sha: a1b2c3d4e5f6
                  shortSha: a1b2c3d
              schema:
                $ref: '#/components/schemas/commitsJSON'
          description: ok
      summary: Walks a ref's history newest first, or one path's history when a path
        is given.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/files:
    get:
      description: |-
        Returns every file a glob selects at one revision, WITH its bytes
        and the revision they came from. It is the read a delivery generator makes:
        one call answers "what is the inventory at this commit, and what does it say",
        where listing and then fetching would be a request per file.

        Returning the resolved revision matters as much as the bytes. A generator that
        lists at `main` and then reads at `main` can straddle a push and assemble half
        its inventory from one commit and half from the next; resolving once makes the
        whole read consistent by construction.

        A file past the read cap comes back Truncated with no content rather than
        being dropped. A caller building a desired set has to know the difference
        between "this file is empty" and "this file was not read" — silently omitting
        it is how a pruning reconcile deletes what the missing file declared.
      operationId: get_git_repos_by_name_files
      parameters:
      - description: Name is the repo to read, from the :name path segment.
        example: universe
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Ref is a branch, tag or commit; empty means the repo's HEAD.
        example: main
        in: query
        name: ref
        required: false
        schema:
          type: string
      - description: |-
          Glob selects files, matched segment by segment so `*` never crosses a `/`.
          `**` matches zero or more whole segments.
        example: charts/app/values/*/*.yaml
        in: query
        name: glob
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/filesJSON'
          description: ok
      summary: Returns every file a glob selects at one revision, WITH its bytes and
        the revision they came from.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/gc:
    post:
      description: |-
        Repacks a repo into one bitmapped pack and rewrites its commit-graph, so
        the next clone reuses the bitmap instead of walking the whole object graph.
        Idempotent, and safe to interrupt — git swaps both artifacts atomically. It
        runs under one pack slot with the same memory bounds as a clone, so it can
        block behind heavy pack traffic rather than compete with it. Storage usage is
        re-measured afterwards, since a repack reclaims space.
      operationId: post_git_repos_by_name_gc
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                maintained: true
                repo: widgets
                sizeBytes: 3072
              schema:
                $ref: '#/components/schemas/gcOut'
          description: ok
      summary: Repacks a repo into one bitmapped pack and rewrites its commit-graph,
        so the next clone reuses the bitmap instead of walking the whole object graph.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/mirror:
    post:
      description: |-
        Imports an external git repository into the caller's repo, provisioning
        it on first use. Fetch is FORCED and covers every ref, so a first call clones
        the source and a repeat call re-syncs it — the endpoint is idempotent by mirror
        semantics. Mirrored bytes are metered exactly like a push, and a push.landed
        event is emitted for the default branch so the code index picks the repo up.
      operationId: post_git_repos_by_name_mirror
      parameters:
      - description: |-
          Name is the local repo to mirror into, from the :name path segment. It is
          CREATED on first use.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              name: widgets
              source: https://github.com/acme/widgets.git
            schema:
              $ref: '#/components/schemas/mirrorReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/repoView'
          description: ok
      summary: Imports an external git repository into the caller's repo, provisioning
        it on first use.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/mirrors:
    get:
      description: |-
        Returns a repo's outbound mirror targets — the downstream remotes
        the mirror reactor pushes to whenever a push lands here.
      operationId: get_git_repos_by_name_mirrors
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                data:
                - createdAt: "2026-07-01T10:00:00Z"
                  host: github.com
                  id: mir_2d90
                  repo: widgets
                  url: https://github.com/acme/widgets.git
              schema:
                $ref: '#/components/schemas/mirrorList'
          description: ok
      summary: Returns a repo's outbound mirror targets — the downstream remotes the
        mirror reactor pushes to whenever a push lands here.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers a downstream remote the repo's advanced refs are pushed to
        whenever a push lands here. Answers 201. The URL must be https to a host on the
        mirror allowlist (github.com / gitlab.com): the same set the mirror credential
        may be sent to, so a target can never capture the shared token or point the push
        at an internal service. Any embedded userinfo is stripped — credentials ride
        env-only at push time and never enter the stored URL. One mirror per host per
        repo; a second is a 409.
      operationId: post_git_repos_by_name_mirrors
      parameters:
      - description: |-
          Name is the repo whose advanced refs are pushed downstream, from the :name
          path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              name: widgets
              url: https://github.com/acme/widgets.git
            schema:
              $ref: '#/components/schemas/mirrorTargetReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mirrorTargetView'
          description: created
      summary: Registers a downstream remote the repo's advanced refs are pushed to
        whenever a push lands here.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/mirrors/{id}:
    delete:
      description: |-
        Removes one outbound mirror target; later pushes stop being
        forwarded to it. Answers 204 with no body. Nothing is done to the downstream
        remote itself — only this repo's intent to push there is dropped.
      operationId: delete_git_repos_by_name_mirrors_by_id
      parameters:
      - description: Name is the repo, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: ID is the row to remove, from the :id path segment.
        example: mir_2d90
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one outbound mirror target; later pushes stop being forwarded
        to it.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/pulls:
    get:
      description: |-
        Returns a repo's pull requests, newest number first — what is
        waiting to be reviewed, and what has already landed. Narrow it with
        ?state=open or ?state=merged; omit state for every proposal.
      operationId: get_git_repos_by_name_pulls
      parameters:
      - description: Name is the repo, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: State narrows the list to "open" or "merged". Omit it for every
          proposal.
        example: open
        in: query
        name: state
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                data:
                - base: main
                  createdAt: "2026-08-07T10:00:00Z"
                  head: agent/cache-catalog
                  number: 4
                  repo: widgets
                  state: open
                  title: cache the catalog read
                  updatedAt: "2026-08-07T10:00:00Z"
              schema:
                $ref: '#/components/schemas/pullList'
          description: ok
      summary: Returns a repo's pull requests, newest number first — what is waiting
        to be reviewed, and what has already landed.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    post:
      description: |-
        Proposes a branch for merging and returns it with its number. Answers
        201. Both branches must already exist — a proposal naming a branch nobody
        pushed is a typo, not a plan — and base defaults to the repo's default branch.

        Proposing the same head into the same base twice is a 409 while the first
        proposal is still open, so a retried agent run leaves ONE thing to review
        rather than a pile of identical ones. A repo outside the caller's scope is a
        404, exactly as reading it is.
      operationId: post_git_repos_by_name_pulls
      parameters:
      - description: Name is the repo the proposal belongs to, from the :name path
          segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              base: main
              head: agent/cache-catalog
              name: widgets
              title: cache the catalog read
            schema:
              $ref: '#/components/schemas/openReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pullView'
          description: created
      summary: Proposes a branch for merging and returns it with its number.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/pulls/{number}:
    get:
      description: |-
        Returns one pull request by its per-repo number. A number belonging to
        another tenant's repo is not found, exactly as the repo itself is not.
      operationId: get_git_repos_by_name_pulls_by_number
      parameters:
      - description: Name is the repo, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Number is the proposal's per-repo number, from the :number path
          segment.
        example: 4
        in: path
        name: number
        required: true
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pullView'
          description: ok
      summary: Returns one pull request by its per-repo number.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/pulls/{number}/merge:
    post:
      description: |-
        Merges an open pull request by FAST-FORWARDING base to head, and
        answers the proposal in its merged state with the revision base now points at.

        It merges only when base is already an ancestor of head — the case where head
        contains every commit base has, so moving the branch loses nothing and invents
        nothing. When base has moved on independently, this REFUSES with 409 and says
        so: a real three-way merge is not implemented here, and reporting one would
        claim a result these bytes do not produce. Rebase head onto base and merge
        again.

        The move is judged by the same ref policy a `git push` of it would face, and
        fires the same build and notify reactions, so merging is not a way around
        either. Merging an already-merged proposal is a 409.
      operationId: post_git_repos_by_name_pulls_by_number_merge
      parameters:
      - description: Name is the repo, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Number is the proposal's per-repo number, from the :number path
          segment.
        example: 4
        in: path
        name: number
        required: true
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pullView'
          description: ok
      summary: Merges an open pull request by FAST-FORWARDING base to head, and answers
        the proposal in its merged state with the revision base now points at.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/push:
    post:
      description: |-
        Lands a set of files as one commit without a git client — the
        hanzo.app builder's push. The repo is CREATED on first push, the files are
        merged onto the branch tip (unlisted files survive), and the same
        push-to-deploy hook a real receive-pack fires is fired, so downstream this is
        indistinguishable from a `git push`.
      operationId: post_git_repos_by_name_push
      parameters:
      - description: |-
          Name is the repo to push into, from the :name path segment. It is CREATED
          on first push if it does not exist.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              branch: main
              files:
              - content: <h1>hi</h1>
                path: index.html
              message: generated build
              name: widgets
            schema:
              $ref: '#/components/schemas/pushReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                branch: main
                cloneUrl: https://api.hanzo.ai/v1/git/acme/widgets.git
                commit: a1b2c3d4e5f6
                sshUrl: git@git.hanzo.ai:acme/widgets.git
              schema:
                $ref: '#/components/schemas/pushResp'
          description: ok
      summary: Lands a set of files as one commit without a git client — the hanzo.app
        builder's push.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/readme:
    get:
      description: |-
        Returns the README at the tree root as plain text — unrendered, so
        the caller decides how to present it. A repo with no README is not found.
      operationId: get_git_repos_by_name_readme
      parameters:
      - description: Name is the repo to read, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Ref is a branch, tag or commit; empty means the repo's HEAD.
        example: main
        in: query
        name: ref
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                content: |
                  # widgets
                encoding: utf8
                path: README.md
              schema:
                $ref: '#/components/schemas/readmeJSON'
          description: ok
      summary: Returns the README at the tree root as plain text — unrendered, so
        the caller decides how to present it.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/refs:
    get:
      description: |-
        Lists a repo's branches, tags and default branch — what a branch
        picker needs in one call. Unlike the other read ops it tolerates a repo with no
        commits: the ref sets come back empty and the default branch is still named.
      operationId: get_git_repos_by_name_refs
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                branches:
                - name: main
                  sha: a1b2c3d4
                default: main
                tags: []
              schema:
                $ref: '#/components/schemas/refsJSON'
          description: ok
      summary: Lists a repo's branches, tags and default branch — what a branch picker
        needs in one call.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/subscriptions:
    get:
      description: |-
        Returns a repo's Slack subscriptions — which channels the
        lifecycle notifier posts this repo's push and deploy events to.
      operationId: get_git_repos_by_name_subscriptions
      parameters:
      - description: |-
          Name is the repo's org-unique handle, from the :name path segment. A
          trailing ".git" is stripped.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                data:
                - channel: '#builds'
                  createdAt: "2026-07-01T10:00:00Z"
                  events:
                  - push.landed
                  id: sub_7c2e
                  repo: widgets
              schema:
                $ref: '#/components/schemas/subscriptionList'
          description: ok
      summary: Returns a repo's Slack subscriptions — which channels the lifecycle
        notifier posts this repo's push and deploy events to.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
    post:
      description: |-
        Binds a Slack channel to a repo, so the lifecycle notifier posts
        that repo's push and deploy events there. Answers 201. The same channel twice
        on one repo is a 409; a repo outside the caller's scope is a 404, exactly as
        reading it is.
      operationId: post_git_repos_by_name_subscriptions
      parameters:
      - description: Name is the repo to subscribe, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              channel: '#builds'
              events:
              - push.landed
              name: widgets
            schema:
              $ref: '#/components/schemas/subscribeReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/subscriptionView'
          description: created
      summary: Binds a Slack channel to a repo, so the lifecycle notifier posts that
        repo's push and deploy events there.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/subscriptions/{id}:
    delete:
      description: |-
        Removes one Slack subscription from a repo; the notifier stops
        posting that repo's events to that channel. Answers 204 with no body. An id
        that is not this repo's subscription is not found.
      operationId: delete_git_repos_by_name_subscriptions_by_id
      parameters:
      - description: Name is the repo, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: ID is the row to remove, from the :id path segment.
        example: sub_7c2e
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one Slack subscription from a repo; the notifier stops posting
        that repo's events to that channel.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/repos/{name}/tree:
    get:
      description: |-
        Lists the immediate children of one directory at one revision,
        directories before files. It does not recurse — walk down a level at a time.
      operationId: get_git_repos_by_name_tree
      parameters:
      - description: Name is the repo to read, from the :name path segment.
        example: widgets
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Ref is a branch, tag or commit; empty means the repo's HEAD.
        example: main
        in: query
        name: ref
        required: false
        schema:
          type: string
      - description: Path is repo-relative; empty is the tree root. Traversal is stripped.
        example: cmd
        in: query
        name: path
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                entries:
                - mode: "040000"
                  name: server
                  path: cmd/server
                  size: 0
                  type: tree
              schema:
                $ref: '#/components/schemas/treeJSON'
          description: ok
      summary: Lists the immediate children of one directory at one revision, directories
        before files.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/usage:
    get:
      description: |-
        Returns per-repo and total storage bytes for the caller's org — the
        queryable, per-tenant number commerce and o11y meter on. It spans EVERY
        project sub-scope, unlike the repo list, so a billing consumer sees the whole
        tenant footprint in one call. Sizes are last-measured values (create, push,
        mirror and gc each re-measure), not a live walk of the disk.
      operationId: get_git_usage
      responses:
        "200":
          content:
            application/json:
              example:
                org: acme
                repos:
                - name: widgets
                  sizeBytes: 4096
                - name: site
                  project: web
                  sizeBytes: 8192
                totalBytes: 12288
              schema:
                $ref: '#/components/schemas/usageView'
          description: ok
      summary: Returns per-repo and total storage bytes for the caller's org — the
        queryable, per-tenant number commerce and o11y meter on.
      tags:
      - git
      x-app: git
      x-public: true
      x-tool: true
  /v1/git/webhook:
    post:
      description: |-
        GONE (410). This was the canonical forge's push-to-deploy door, and it never dispatched a build in its life.

        It handed each verified push to cloud.OnGitPush, a single-registrant seam whose only registrant lives in apps/platform. cloud runs each app as its own OS process, so in the git process that builder is nil forever — and this handler answered 204 either way. Delivered, signature valid, green on the forge's hook page, and nothing built.

        Push-to-deploy now belongs to POST https://platform.hanzo.ai/v1/git-webhook, which owns the build system-of-record and dispatches BuildKit Jobs. git.hanzo.ai delivers there through ONE forge-wide system webhook covering every repository; a repo opts in by committing hanzo.yml, not by owning a hook of its own.

        The route is kept, and answers 410 naming that address, precisely so a misdirected delivery says what is wrong. Deleting it would 404, and a 404 here reads as 'the API is switched off' — the wrong conclusion this estate has already drawn twice.
      operationId: post_git_webhook
      summary: Retired — forge pushes build via platform.hanzo.ai
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/zap/createRepo:
    post:
      description: |-
        Creates a repository in the caller's org and project scope and answers with its record. `name` is required and `description` is optional; `project` narrows the scope within the org. A name already taken in that scope is a 409 envelope and an invalid name a 400.

        A ZAP PROCEDURE, not a REST resource. It answers the bridge's {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller's scope. Without a validated org the answer is a 403 envelope.
      operationId: post_git_zap_createrepo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zapProcReq'
      summary: Create a repository over the ZAP transport
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/zap/deleteRepo:
    post:
      description: |-
        Deletes the repository named by `name` and answers with the deleted name. A repository outside the caller's org and project scope is a 404 envelope, so a delete can never reach another tenant's repository.

        A ZAP PROCEDURE, not a REST resource. It answers the bridge's {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller's scope. Without a validated org the answer is a 403 envelope.
      operationId: post_git_zap_deleterepo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zapProcReq'
      summary: Delete a repository over the ZAP transport
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/zap/getRepo:
    post:
      description: |-
        Answers a single repository's record, named by `name`. A repository outside the caller's org and project scope is a 404 envelope, the same answer one that does not exist gets.

        A ZAP PROCEDURE, not a REST resource. It answers the bridge's {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller's scope. Without a validated org the answer is a 403 envelope.
      operationId: post_git_zap_getrepo
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/zapProcReq'
      summary: Read one repository over the ZAP transport
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/zap/listRepos:
    post:
      description: |-
        Answers every repository in the caller's org and project scope. It reads NO body — the scope is entirely the caller's identity — so a request with an empty object is correct.

        A ZAP PROCEDURE, not a REST resource. It answers the bridge's {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller's scope. Without a validated org the answer is a 403 envelope.
      operationId: post_git_zap_listrepos
      summary: List your repositories over the ZAP transport
      tags:
      - git
      x-app: git
      x-public: true
  /v1/git/zap/usage:
    post:
      description: |-
        Answers every repository in the caller's org with its size in bytes, plus the org's total — what git storage is actually being used, and by which repository. It reads NO body, and it is scoped to the caller's own org, so it is that org's footprint and never the fleet's.

        A ZAP PROCEDURE, not a REST resource. It answers the bridge's {status, msg, data} envelope rather than the raw view the /v1 route returns — which is a wire shape a typed op cannot produce, and the reason this stays a raw handler — and it calls the SAME core function the REST route calls, so the two transports cannot diverge in behaviour. Org and project scope come from the request identity and NEVER from the body: the body cannot widen the caller's scope. Without a validated org the answer is a 403 envelope.
      operationId: post_git_zap_usage
      summary: Report your org's git storage footprint over the ZAP transport
      tags:
      - git
      x-app: git
      x-public: true
  /v1/help/articles:
    get:
      description: |-
        Returns the public knowledge base: the help center's Published,
        publicly-visible articles as cards. The org is server-fixed and the
        status/is_public filter is server-set, so neither the tenant nor the visibility
        can be widened by the caller. A deployment with no help center answers 404.
      operationId: get_help_articles
      parameters:
      - description: |-
          Category narrows the list to one knowledge-base section, matched against
          the article's category by exact name. Empty lists every section.
        in: query
        name: category
        required: false
        schema:
          type: string
      - description: |-
          Limit caps how many articles are returned. Anything that is not a positive
          integer uses 50, and values above 200 are clamped to 200.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/helpArticleList'
          description: ok
      summary: 'Returns the public knowledge base: the help center''s Published, publicly-visible
        articles as cards.'
      tags:
      - help
      x-app: help
      x-public: true
      x-tool: true
  /v1/help/articles/{slug}:
    get:
      description: |-
        Returns one public article by slug, with its body. A missing, Draft,
        or internal (non-public) article is 404 — fail-closed, so this route is no
        existence oracle for anything beyond "published and public".
      operationId: get_help_articles_by_slug
      parameters:
      - description: |-
          Slug is the article's public identifier, from the path. It IS the document
          name in the help center's store.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/helpArticle'
          description: ok
      summary: Returns one public article by slug, with its body.
      tags:
      - help
      x-app: help
      x-public: true
      x-tool: true
  /v1/help/categories:
    get:
      description: |-
        Returns the knowledge-base sections for the public center's
        navigation — but ONLY the sections that front at least one Published, public
        article, so an internal (agent-only) category name or description never leaks. A
        section with no public article is invisible; a center with no public articles has
        no sections, which is an empty list rather than an error.
      operationId: get_help_categories
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/helpCategoryList'
          description: ok
      summary: Returns the knowledge-base sections for the public center's navigation
        — but ONLY the sections that front at least one Published, public article,
        so an internal (agent-only) category name or description never leaks.
      tags:
      - help
      x-app: help
      x-public: true
      x-tool: true
  /v1/help/tickets:
    post:
      description: |-
        Files a customer support ticket into the public help center. It
        creates the ticket (status Open, source portal) with the customer's message on
        the description, then records that same message as the opening entry of the
        ticket's conversation thread; the description carries it regardless, so failing
        to write that entry loses nothing. Answers 201 with an opaque reference.

        A deployment with no help center answers 404, one whose center has not installed
        the Help model answers 503, and a body over 64 KiB answers 413 — in that order,
        which is the order the route has always decided them in.
      operationId: post_help_tickets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/helpTicketIntake'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/helpTicketFiled'
          description: created
      summary: Files a customer support ticket into the public help center.
      tags:
      - help
      x-app: help
      x-public: true
      x-tool: true
  /v1/iam/.well-known/jwks:
    get:
      description: |-
        Publishes the public keys that verify the tokens issued here — the
        one URL you point a service at so it can check a token itself, offline, without
        calling back and without holding any secret of ours.

        Keys appear here before they start signing and stay after they stop, so a
        rotation never leaves a live token unverifiable. Nothing private is ever
        published.
      operationId: get_iam_.well-known_jwks
      summary: Publishes the public keys that verify the tokens issued here — the
        one URL you point a service at so it can check a token itself, offline, without
        calling back and without holding any secret of ours.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/.well-known/oauth-authorization-server:
    get:
      description: |-
        Returns the OpenID Connect discovery document — the one URL you
        point a standards-compliant client at so it can find every other endpoint on
        its own, instead of you configuring them by hand.

        It advertises only what is actually implemented, so a client that reads it
        cannot ask for a flow that will fail: the authorization-code flow, PKCE with
        S256, the supported grants, and the signing algorithms whose public keys the
        JWKS really publishes.

        The issuer is derived from the host you asked on and is the same value the
        tokens carry, so a client that pins the issuer never sees it change.
      operationId: get_iam_.well-known_oauth-authorization-server
      summary: Returns the OpenID Connect discovery document — the one URL you point
        a standards-compliant client at so it can find every other endpoint on its
        own, instead of you configuring them by hand.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/.well-known/openid-configuration:
    get:
      description: |-
        Returns the OpenID Connect discovery document — the one URL you
        point a standards-compliant client at so it can find every other endpoint on
        its own, instead of you configuring them by hand.

        It advertises only what is actually implemented, so a client that reads it
        cannot ask for a flow that will fail: the authorization-code flow, PKCE with
        S256, the supported grants, and the signing algorithms whose public keys the
        JWKS really publishes.

        The issuer is derived from the host you asked on and is the same value the
        tokens carry, so a client that pins the issuer never sees it change.
      operationId: get_iam_.well-known_openid-configuration
      summary: Returns the OpenID Connect discovery document — the one URL you point
        a standards-compliant client at so it can find every other endpoint on its
        own, instead of you configuring them by hand.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/account:
    get:
      description: |-
        Returns the signed-in person's own account and the organization
        they belong to — what a console reads to draw the account menu.

        Passwords, API secrets and MFA material are stripped. It answers for a session
        cookie or a bearer token alike.
      operationId: get_iam_account
      summary: Returns the signed-in person's own account and the organization they
        belong to — what a console reads to draw the account menu.
      tags:
      - iam
      x-app: iam
      x-public: true
    put:
      description: |-
        Saves the calling person's own profile — the name they are
        shown by, their picture, a line about themselves and a link.

        Only their own: the request names nobody, so it cannot reach another account.
        Send only what you are changing; a field you leave out keeps the value it had,
        and a field you send empty is cleared.

        A picture is an https link or an inline image up to 96 KiB, the same value an
        organization's mark is (schema.AvatarRef) — one rule for how a subject appears,
        whether the subject is a person or an organization.
      operationId: put_iam_account
      parameters:
      - in: header
        name: Cookie
        required: false
        schema:
          type: string
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.accountBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: Saves the calling person's own profile — the name they are shown by,
        their picture, a line about themselves and a link.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/add-membership:
    post:
      description: |-
        Lets a person or an application act in an organization. It is the grant
        behind "add someone to the team", and it is safe to repeat — granting a
        membership that already exists changes nothing. Granting membership IS the org's authority to give, so it takes the
        same gate a write to that org's own registry row takes: a SuperAdmin, an admin
        of the org itself, or an org-admin-capable confidential client. One rule, one
        place (internal/authz).
      operationId: post_iam_add-membership
      summary: Lets a person or an application act in an organization.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/admin/applications/upsert:
    post:
      description: |-
        Creates an application or updates it in place, so a
        deployment can declare the applications it needs and run the same declaration
        on every environment and on every redeploy.

        It says which of the two it did. Leave the client secret out and the existing
        one is kept — so re-running your deployment does not rotate a credential your
        running services are holding.
      operationId: upsertApplication
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.registration'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: bad request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: internal server error
      summary: Creates an application or updates it in place, so a deployment can
        declare the applications it needs and run the same declaration on every environment
        and on every redeploy.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/admin/provision:
    post:
      description: |-
        Sets up an account on someone's behalf — the same
        onboarding a person gets themselves, driven by one of your own services
        instead of by them.

        It authenticates as your service rather than as a person, which is why the
        person to provision is named in the request. The setup it performs is
        identical to self-service onboarding; there is one provisioning path, not
        two that can drift.
      operationId: post_iam_admin_provision
      summary: Sets up an account on someone's behalf — the same onboarding a person
        gets themselves, driven by one of your own services instead of by them.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/admin/users/upsert:
    post:
      description: |-
        Creates a person or updates them in place, so a deployment can
        declare the accounts it needs and re-run that declaration safely.

        Passwords are hashed before they are stored. Leave the password out and their
        current one is kept, so a redeploy never locks somebody out.
      operationId: upsertUser
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.person'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: bad request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: unauthorized
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.reply'
          description: internal server error
      summary: Creates a person or updates them in place, so a deployment can declare
        the accounts it needs and re-run that declaration safely.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/applications:
    get:
      description: |-
        Returns the applications in one organization, newest first —
        each product or site your people sign in to, with the sign-in methods and
        redirect URIs it allows.
      operationId: get_iam_applications
      parameters:
      - in: query
        name: owner
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.ApplicationListResult'
          description: ok
      summary: Returns the applications in one organization, newest first — each product
        or site your people sign in to, with the sign-in methods and redirect URIs
        it allows.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers an application in your organization — one product or site
        your people sign in to, with its own client credentials, sign-in methods and
        allowed redirect URIs. A name already used in the organization is refused
        rather than overwritten.

        Exported so the legacy add-application alias reuses this exact path — one
        create, two spellings.
      operationId: post_iam_applications
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Application'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Application'
          description: ok
      summary: Registers an application in your organization — one product or site
        your people sign in to, with its own client credentials, sign-in methods and
        allowed redirect URIs.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/applications/delete:
    post:
      description: |-
        Removes an application. Anyone mid-sign-in through it is
        turned away and its client credentials stop working, so retire the integration
        before deleting it.
      operationId: post_iam_applications_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.ApplicationRef'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.DeleteResult'
          description: ok
      summary: Removes an application.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/applications/get:
    get:
      description: |-
        Returns one application: its sign-in methods, its allowed
        redirect URIs and the client credentials your integration authenticates with.
      operationId: get_iam_applications_get
      parameters:
      - in: query
        name: owner
        required: true
        schema:
          type: string
      - in: query
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Application'
          description: ok
      summary: 'Returns one application: its sign-in methods, its allowed redirect
        URIs and the client credentials your integration authenticates with.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/applications/update:
    post:
      description: |-
        Changes an application's display, its sign-in methods and the redirect
        URIs it may return to — the call that makes login work from a new host. Which
        organization it belongs to and what it is named are fixed when it is created
        and are not editable here.

        Exported so the legacy update-application alias reuses this exact path — one
        update, two spellings.
      operationId: post_iam_applications_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Application'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Application'
          description: ok
      summary: Changes an application's display, its sign-in methods and the redirect
        URIs it may return to — the call that makes login work from a new host.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/assume:
    post:
      description: |-
        Steps a platform operator into an organization: it returns their
        own access token re-scoped to that tenant, so they see what the tenant sees.

        The token still names the operator — stepping in is not becoming somebody
        else — and records the organization it was scoped to, so everything done with
        it is attributed to the person who did it. Only a platform operator may, and
        the attempt is recorded whether or not it succeeds.
      operationId: post_iam_assume
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      - in: header
        name: X-Forwarded-For
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.assumeBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: unauthorized
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: forbidden
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: not found
      summary: 'Steps a platform operator into an organization: it returns their own
        access token re-scoped to that tenant, so they see what the tenant sees.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/audit-logs:
    get:
      description: |-
        Returns your organization's audit trail, newest first — who did
        what, when, and from where. It is the record you reach for during a security
        review or an incident.

        You see your own organization's audit trail and no one else's; which organization that
        is comes from your credentials, not from the request.
      operationId: get_iam_audit-logs
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.ListOutput'
          description: ok
      summary: Returns your organization's audit trail, newest first — who did what,
        when, and from where.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Records an audit entry, so activity from your own systems lands in the
        same trail as everything the Hanzo Cloud records for you.
      operationId: post_iam_audit-logs
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.auditlogs.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.AuditLog'
          description: ok
      summary: Records an audit entry, so activity from your own systems lands in
        the same trail as everything the Hanzo Cloud records for you.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/audit-logs/delete:
    post:
      description: |-
        Removes an audit entry. Retention policy is normally what should expire
        a trail; deleting by hand leaves a gap a reviewer will notice.
      operationId: post_iam_audit-logs_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.DeleteOutput'
          description: ok
      summary: Removes an audit entry.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/audit-logs/get:
    post:
      description: |-
        Returns one audit entry in full: the action, the person or key behind it,
        and the request it came in on.
      operationId: post_iam_audit-logs_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.AuditLog'
          description: ok
      summary: 'Returns one audit entry in full: the action, the person or key behind
        it, and the request it came in on.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/audit-logs/update:
    post:
      description: |-
        Corrects an audit entry. The trail is append-only in normal operation
        and nothing in the Hanzo Cloud rewrites it — this exists for an administrator
        to correct an entry their own systems recorded wrongly.
      operationId: post_iam_audit-logs_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.auditlogs.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.AuditLog'
          description: ok
      summary: Corrects an audit entry.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/auth/application:
    get:
      description: |-
        Returns everything a login screen needs to draw itself for one
        application: its branding, and each sign-in method it offers with the provider
        details that method needs.

        The client secret is masked. Read before anyone has signed in, so it carries
        only what is safe for a browser to see.
      operationId: get_iam_auth_application
      parameters:
      - description: |-
          ClientId is the application's OAuth client id — the one field that selects
          which login screen this is.
        in: query
        name: clientId
        required: false
        schema:
          type: string
      - description: |-
          ResponseType is the OAuth response type the screen will ask for. Only "code"
          is served; anything else is refused here rather than at the authorize leg,
          where the person has already typed a password.
        in: query
        name: responseType
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: 'Returns everything a login screen needs to draw itself for one application:
        its branding, and each sign-in method it offers with the provider details
        that method needs.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/auth/methods:
    get:
      description: |-
        Returns the sign-in methods one application actually has switched
        on, so a login screen can render the right buttons for it without you
        hard-coding a list that drifts the moment you add a provider.

        Public by design: it is read before anyone has signed in, and it exposes only
        which methods exist, never their credentials.
      operationId: get_iam_auth_methods
      parameters:
      - description: ClientId is the application's OAuth client id.
        in: query
        name: clientId
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: Returns the sign-in methods one application actually has switched on,
        so a login screen can render the right buttons for it without you hard-coding
        a list that drifts the moment you add a provider.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/certs:
    get:
      description: |-
        Returns your organization's signing certificates, newest first — the keys
        the tokens your applications verify are signed with. Private key material is
        masked.

        You see your own organization's certificates and no one else's; which
        organization that is comes from your credentials, not from the request, so a
        query parameter can never widen the listing.
      operationId: get_iam_certs
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.certs.ListOutput'
          description: ok
      summary: Returns your organization's signing certificates, newest first — the
        keys the tokens your applications verify are signed with.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds a signing certificate your applications can verify tokens against
        — the call you make to bring your own key, or to stage the next one before a
        rotation. A name already used in your organization is refused.
      operationId: post_iam_certs
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Cert'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Cert'
          description: ok
      summary: Adds a signing certificate your applications can verify tokens against
        — the call you make to bring your own key, or to stage the next one before
        a rotation.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/certs/delete:
    post:
      description: |-
        Removes a signing certificate. Tokens signed with it can no longer be
        verified, so retire it only once nothing is still presenting them.
      operationId: post_iam_certs_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.certs.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.certs.DeleteOutput'
          description: ok
      summary: Removes a signing certificate.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/certs/get:
    post:
      description: |-
        Returns one signing certificate — its algorithm, its validity window and
        its public half. The private key is masked.
      operationId: post_iam_certs_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.certs.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Cert'
          description: ok
      summary: Returns one signing certificate — its algorithm, its validity window
        and its public half.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/certs/update:
    post:
      description: |-
        Changes a signing certificate's settings. What it is called does not
        change, and neither does when it was added.
      operationId: post_iam_certs_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Cert'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Cert'
          description: ok
      summary: Changes a signing certificate's settings.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/consent:
    get:
      description: |-
        Returns the calling person's own privacy and communication
        choices. Somebody who has never set them gets the defaults rather than
        nothing, so a consent screen always has something to show — insights on, and
        training UNANSWERED, which is the state that means the screen still has to ask.
      operationId: get_iam_consent
      summary: Returns the calling person's own privacy and communication choices.
      tags:
      - iam
      x-app: iam
      x-public: true
    put:
      description: |-
        Records the calling person's privacy and communication
        choices. Only their own — there is no way to set consent for somebody else.

        Send only the answers you are changing. A question you leave out keeps the
        answer it already had, so a screen that saves one switch never revokes the
        other, and two screens saving at once do not undo each other.

        An answer this version does not recognize is refused here rather than stored,
        so nothing is ever persisted for a later reader to have to interpret.
      operationId: put_iam_consent
      summary: Records the calling person's privacy and communication choices.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/delete-membership:
    post:
      description: |-
        Takes away a person's or an application's right to act in an
        organization. Their account survives; what ends is their access to that
        organization. Revoking a membership that is already gone reports that nothing
        was removed rather than failing, so a retry is safe. It is the mirror of ensure and takes the SAME gate:
        revoking membership is the org's authority to give or take, so a SuperAdmin, an
        admin of the org itself, or an org-admin-capable confidential client. Idempotent
        through the store — deleting an absent membership reports removed=false, never an
        error — so a retried revoke is safe.
      operationId: post_iam_delete-membership
      summary: Takes away a person's or an application's right to act in an organization.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/delete-mfa:
    post:
      description: |-
        Turns a factor off, so sign-in stops asking for it. Naming no factor turns
        off ALL of them — the reset path. People may do this for themselves; doing it for
        somebody else takes an administrator, which is what makes it the way back in when a
        phone is lost.

        The recovery codes go with the last factor: they are the way past a challenge, so
        keeping them alive for an account with nothing to challenge would leave a standing
        credential behind.
      operationId: post_iam_delete-mfa
      summary: Turns a factor off, so sign-in stops asking for it.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-account:
    get:
      description: |-
        Returns the signed-in person's own account and the organization
        they belong to — what a console reads to draw the account menu.

        Passwords, API secrets and MFA material are stripped. It answers for a session
        cookie or a bearer token alike.
      operationId: get_iam_get-account
      summary: Returns the signed-in person's own account and the organization they
        belong to — what a console reads to draw the account menu.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-app-login:
    get:
      description: |-
        Returns everything a login screen needs to draw itself for one
        application: its branding, and each sign-in method it offers with the provider
        details that method needs.

        The client secret is masked. Read before anyone has signed in, so it carries
        only what is safe for a browser to see.
      operationId: get_iam_get-app-login
      parameters:
      - description: |-
          ClientId is the application's OAuth client id — the one field that selects
          which login screen this is.
        in: query
        name: clientId
        required: false
        schema:
          type: string
      - description: |-
          ResponseType is the OAuth response type the screen will ask for. Only "code"
          is served; anything else is refused here rather than at the authorize leg,
          where the person has already typed a password.
        in: query
        name: responseType
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: 'Returns everything a login screen needs to draw itself for one application:
        its branding, and each sign-in method it offers with the provider details
        that method needs.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/get-application:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-application
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-applications:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-applications
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-cert:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-cert
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-certs:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-certs
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-global-users:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-global-users
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-invitations:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-invitations
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-memberships:
    get:
      description: |-
        Answers either question about who belongs where: which organizations one
        person can act in, or who can act in one organization.

        Both are org-scoped: a non-SuperAdmin may ask about ITS OWN org's roster, or
        about a user whose home org is its own, and nothing else. The bound comes from
        the verified credential via authz.Scope, so a request parameter can never
        widen it — a membership row names who may act and spend in an org, so a
        cross-tenant read is a customer roster leak.
      operationId: get_iam_get-memberships
      parameters:
      - description: User is "<homeOrg>/<username>" — which organizations that identity
          may act in.
        in: query
        name: user
        required: false
        schema:
          type: string
      - description: Org is an organization — who may act in it.
        in: query
        name: org
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: 'Answers either question about who belongs where: which organizations
        one person can act in, or who can act in one organization.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/get-organization:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-organization
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-organization-projects:
    get:
      description: |-
        Returns one organization's projects — what a scope switcher
        lists so somebody can move between them.

        You see your own organization and no other, whatever the request asks for.
      operationId: get_iam_get-organization-projects
      summary: Returns one organization's projects — what a scope switcher lists so
        somebody can move between them.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-organization-workspaces:
    get:
      description: |-
        Returns one organization's workspaces — what a scope
        switcher lists so somebody can move between them.

        You see your own organization and no other, whatever the request asks for.
      operationId: get_iam_get-organization-workspaces
      summary: Returns one organization's workspaces — what a scope switcher lists
        so somebody can move between them.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-organizations:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-organizations
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-permission:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-permission
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-permissions:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-permissions
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-provider:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-provider
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-providers:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-providers
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-records:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-records
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-role:
    get:
      description: |-
        Reads one record — the older spelling of the single reads on the
        REST surface, over the same data and the same permissions.

        Secrets are stripped. Naming a record in another organization does not reach
        it, however the request spells it.
      operationId: get_iam_get-role
      summary: Reads one record — the older spelling of the single reads on the REST
        surface, over the same data and the same permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-roles:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-roles
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-user:
    get:
      description: |-
        Reads one person, two ways.

        Name them and it is an ordinary read, with secrets stripped. Or hand it a
        SECRET API key and it answers with the person that key belongs to — how a
        service of yours turns a credential on an incoming request into an identity.

        A publishable key resolves to nobody here, deliberately: it is safe to ship in
        a browser precisely because it names an organization and never a person.

        get-user is handler-authorized (authz.handlerAuthorizedExact) because the key
        variant carries no owner/name for the Guard to authorize; so the owner/name
        variant reinstates the SAME read authorization the Guard applies, through the ONE
        policy function (authz.Can) — identical behavior, a cross-tenant or non-self read
        still refused 403 — then reuses the generic getHandler verbatim for resolution and
        redaction. No authz and no CRUD is reimplemented.
      operationId: get_iam_get-user
      summary: Reads one person, two ways.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/get-users:
    get:
      description: |-
        Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.

        Secrets are stripped from every row. Send both a page number and a page size to
        page, and the total comes back alongside; send neither and you get the whole
        set. You see your own organization and no other, whatever the request asks for.

        Scoping note (intentional, fail-closed): iam's ownership model is mixed —
        users/roles/permissions are owned by their tenant org, while organizations/
        applications/providers/certs are platform-owned (Owner "admin"). A SuperAdmin
        (Scope → the requested owner, empty = all) therefore lists every entity, which
        is the console-admin path. A non-super is pinned by Scope to its own org, so it
        lists its tenant-owned entities correctly and is refused the platform-owned
        lists at the Guard (owner "" or "admin" both deny) — a safe 403, never another
        tenant's rows. Non-super, membership-scoped views of the platform-owned
        entities (e.g. an org console's own app list keyed on Application.Organization)
        are a separate, additive surface, not a silent behavior of this generic lister.
      operationId: get_iam_get-users
      summary: Lists one kind of record in your organization — the older spelling
        of the collection reads on the REST surface, over the same data and the same
        permissions.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/invitations:
    get:
      description: |-
        Returns your organization's invitations, newest first — who has
        been asked to join, on what terms, and how many seats each invitation still
        has left.

        You see your own organization's invitations and no one else's; which organization that
        is comes from your credentials, not from the request.
      operationId: get_iam_invitations
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.invitations.ListOutput'
          description: ok
      summary: Returns your organization's invitations, newest first — who has been
        asked to join, on what terms, and how many seats each invitation still has
        left.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Issues an invitation to join your organization — the code or link a new
        member redeems, with the role they arrive holding and the date it stops
        working. A name already used in the organization is refused.
      operationId: post_iam_invitations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.invitations.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Invitation'
          description: ok
      summary: Issues an invitation to join your organization — the code or link a
        new member redeems, with the role they arrive holding and the date it stops
        working.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/invitations/delete:
    post:
      description: |-
        Withdraws an invitation. It stops being redeemable at once; anyone who
        already joined through it keeps their account.
      operationId: post_iam_invitations_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.invitations.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.invitations.DeleteOutput'
          description: ok
      summary: Withdraws an invitation.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/invitations/get:
    post:
      description: |-
        Returns one invitation: who it is for, what it grants on acceptance, and
        when it expires.
      operationId: post_iam_invitations_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.invitations.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Invitation'
          description: ok
      summary: 'Returns one invitation: who it is for, what it grants on acceptance,
        and when it expires.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/invitations/update:
    post:
      description: |-
        Changes an invitation's terms — the role it grants, how many may redeem
        it, or when it expires. What it is called does not change.
      operationId: post_iam_invitations_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.invitations.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Invitation'
          description: ok
      summary: Changes an invitation's terms — the role it grants, how many may redeem
        it, or when it expires.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/issue-user-token:
    post:
      description: |-
        Mints an access token for the `?id=<owner>/<name>` target
        user (optional `?aud=` resource, RFC 8707), issued by the authenticated +
        allow-listed confidential client. The token's subject + owner are the TARGET
        USER's, so a resource server scopes on the validated owner claim to the user's
        tenant — indistinguishable from a token the user obtained directly. Response is
        the camelCase `{accessToken, expiresIn}` body identity.ts consumes. Equivalent to
        the RFC 8693 token-exchange grant, minus the subject_token proof (the console has
        the user's id, not a token) — the reason this compat shim exists.
      operationId: post_iam_issue-user-token
      summary: Mints an access token for the `?id=<owner>/<name>` target user (optional
        `?aud=` resource, RFC 8707), issued by the authenticated + allow-listed confidential
        client.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/keys:
    get:
      description: |-
        Returns your organization's API keys, newest first — what each is called,
        what it may reach, and its publishable half. Secret halves are never listed.
      operationId: get_iam_keys
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.ListResponse'
          description: ok
      summary: Returns your organization's API keys, newest first — what each is called,
        what it may reach, and its publishable half.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Issues an API key. A standard key comes back as a publishable half you
        may ship in client code and a secret half you must not — the secret is shown
        once, at creation, and cannot be retrieved afterwards. A publish-scoped key is
        issued with the publishable half only, so there is no secret to leak.

        A name already used in your organization is refused rather than reissued, so
        creating twice never silently invalidates a key that is in production.
      operationId: post_iam_keys
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Key'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Key'
          description: ok
      summary: Issues an API key.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/keys/delete:
    post:
      description: |-
        Revokes an API key. Anything still presenting it stops being authorized at
        once, so roll the replacement out before you revoke.
      operationId: post_iam_keys_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.keys.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.DeleteResponse'
          description: ok
      summary: Revokes an API key.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/keys/get:
    get:
      description: |-
        Returns one API key: what it is called, what it may reach, and when it was
        issued.
      operationId: get_iam_keys_get
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      - in: query
        name: name
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Key'
          description: ok
      summary: 'Returns one API key: what it is called, what it may reach, and when
        it was issued.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/keys/mint:
    post:
      description: |-
        (re)generates the target user's key of the requested TYPE and
        returns it once, over the shared authorizeMinter + mintTarget seam. `?type=secret`
        (the default) yields the confidential sk-; `?type=publishable` yields the pk- that
        is safe to ship in client JS and resolves to an org, never a principal.

        It writes the schema.Key row that the resolvers actually read. schema.User.AccessKey
        is not a credential and nothing resolves it, so a key stamped there would
        authenticate nobody.
      operationId: post_iam_keys_mint
      summary: (re)generates the target user's key of the requested TYPE and returns
        it once, over the shared authorizeMinter + mintTarget seam.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/keys/revoke:
    post:
      description: |-
        Clears the target user's key of the requested TYPE (immediate
        revoke). Scoped by the same `?type` field mint takes, so revoking the browser key
        leaves the server key working. A secret key's stored value is the sk- in its
        schema.Key row.
      operationId: post_iam_keys_revoke
      summary: Clears the target user's key of the requested TYPE (immediate revoke).
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/keys/update:
    post:
      description: |-
        Changes what a key is called or what it may reach. The credential
        itself is not reissued — the key in your deployment keeps working.
      operationId: post_iam_keys_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Key'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Key'
          description: ok
      summary: Changes what a key is called or what it may reach.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/link:
    post:
      description: |-
        Starts connecting another sign-in identity to the account you are already
        signed in as. It answers with the provider's URL for the browser to follow; when
        the provider returns, that identity is attached and you come back to returnUri.

        Your account is fixed here, from the credential you are already holding, and is
        carried server-side for the rest of the round-trip — so nothing that happens at
        the provider can point the link at somebody else.
      operationId: post_iam_link
      summary: Starts connecting another sign-in identity to the account you are already
        signed in as.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/linked-accounts:
    get:
      description: |-
        Returns the sign-in identities linked to the calling
        person's account — every provider they can currently sign in with. It is what
        a security page lists next to the option to disconnect one.
      operationId: get_iam_linked-accounts
      summary: Returns the sign-in identities linked to the calling person's account
        — every provider they can currently sign in with.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/login:
    post:
      description: |-
        Signs a person in with the credential they typed, and — when the
        request is part of an OAuth flow — hands back the one-time code that finishes
        it. A second factor, if the account has one, is asked for and required here.

        The password is compared against a stored one-way hash and is never logged,
        echoed or stored as typed.
      operationId: post_iam_login
      summary: Signs a person in with the credential they typed, and — when the request
        is part of an OAuth flow — hands back the one-time code that finishes it.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/memberships:
    get:
      description: |-
        Answers either question about who belongs where: which organizations one
        person can act in, or who can act in one organization.

        Both are org-scoped: a non-SuperAdmin may ask about ITS OWN org's roster, or
        about a user whose home org is its own, and nothing else. The bound comes from
        the verified credential via authz.Scope, so a request parameter can never
        widen it — a membership row names who may act and spend in an org, so a
        cross-tenant read is a customer roster leak.
      operationId: get_iam_memberships
      parameters:
      - description: User is "<homeOrg>/<username>" — which organizations that identity
          may act in.
        in: query
        name: user
        required: false
        schema:
          type: string
      - description: Org is an organization — who may act in it.
        in: query
        name: org
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: 'Answers either question about who belongs where: which organizations
        one person can act in, or who can act in one organization.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Lets a person or an application act in an organization. It is the grant
        behind "add someone to the team", and it is safe to repeat — granting a
        membership that already exists changes nothing. Granting membership IS the org's authority to give, so it takes the
        same gate a write to that org's own registry row takes: a SuperAdmin, an admin
        of the org itself, or an org-admin-capable confidential client. One rule, one
        place (internal/authz).
      operationId: post_iam_memberships
      summary: Lets a person or an application act in an organization.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/mfa/disable:
    post:
      description: |-
        Turns a factor off, so sign-in stops asking for it. Naming no factor turns
        off ALL of them — the reset path. People may do this for themselves; doing it for
        somebody else takes an administrator, which is what makes it the way back in when a
        phone is lost.

        The recovery codes go with the last factor: they are the way past a challenge, so
        keeping them alive for an account with nothing to challenge would leave a standing
        credential behind.
      operationId: post_iam_mfa_disable
      summary: Turns a factor off, so sign-in stops asking for it.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/mfa/preferred:
    post:
      description: |-
        Picks which second factor an account is asked for first when it has
        more than one. Only a factor the account actually holds: storing an unheld one told
        the login gate "MFA is on" — factor.Enabled reads that column — while leaving it
        nothing to ask for, so the sign-in required the password alone.
      operationId: post_iam_mfa_preferred
      summary: Picks which second factor an account is asked for first when it has
        more than one.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/mfa/setup/enable:
    post:
      description: |-
        Finishes the enrolment: from here the account's sign-ins ask for this factor.
        It requires the proof initiate handed out — a passcode from the authenticator, or the
        code that was sent — and verifies it BEFORE writing anything.

        It used to write on the strength of a `secret` field alone. A client that skipped
        the verify step, scanned the QR into the wrong app, or was simply buggy switched on
        a factor no code would ever satisfy, and the account was then locked out with no
        self-service way back: the gate holds the sign-in before minting, so the person
        cannot obtain the bearer that disable requires.

        The recovery codes are minted here and returned ONCE, on the first factor the
        account adds. Answering with them is the way back in when no factor can be
        produced, so they are the same value the row's digests were made from — by
        construction, not by a client echoing them back.
      operationId: post_iam_mfa_setup_enable
      summary: 'Finishes the enrolment: from here the account''s sign-ins ask for
        this factor.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/mfa/setup/initiate:
    post:
      description: "Starts enrolling a factor and hands over whatever the person needs
        to prove\nthey hold it:\n\n\tapp   a fresh secret and the otpauth:// URL to
        render as a QR code\n\tsms   a code texted to the number on the account\n\temail
        a code mailed to the address on the account\n\nNothing is switched on yet,
        so abandoning this step leaves the account exactly as\nit was. Response: {status:\"ok\",
        data:{mfaType, secret, url}} — secret and url only\nfor the authenticator."
      operationId: post_iam_mfa_setup_initiate
      summary: 'Starts enrolling a factor and hands over whatever the person needs
        to prove they hold it: app a fresh secret and the otpauth:// URL to render
        as a QR code sms a code texted to the number on the account email a code mailed
        to the address on the account Nothing is switched on yet, so abandoning this
        step leaves the account exactly as it was.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/mint-user-keys:
    post:
      description: |-
        (re)generates the target user's key of the requested TYPE and
        returns it once, over the shared authorizeMinter + mintTarget seam. `?type=secret`
        (the default) yields the confidential sk-; `?type=publishable` yields the pk- that
        is safe to ship in client JS and resolves to an org, never a principal.

        It writes the schema.Key row that the resolvers actually read. schema.User.AccessKey
        is not a credential and nothing resolves it, so a key stamped there would
        authenticate nobody.
      operationId: post_iam_mint-user-keys
      summary: (re)generates the target user's key of the requested TYPE and returns
        it once, over the shared authorizeMinter + mintTarget seam.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/authorize:
    get:
      description: |-
        Starts a sign-in — the address you send a browser to, and the
        beginning of every OAuth and OpenID Connect flow.

        If the person is ALREADY signed in here, it does not ask them again: it
        returns them to the application with a one-time code and they never see this
        page. Otherwise it shows the right way to sign in for the application they are
        signing in to, or hands off to another identity provider if that is what they
        pick.

        A client can say what it wants with `prompt`: `none` means answer without any
        screen at all — with the code if a session exists, with an error if not, but
        never with a page; `login` means ask for the password again even if a session
        exists; `select_account` means let the person choose which identity to use.

        It returns only to an address the application has registered. That check
        happens before anything else, so a request naming an unregistered address is
        refused where the person can see it rather than being bounced onwards.
      operationId: get_iam_oauth_authorize
      summary: Starts a sign-in — the address you send a browser to, and the beginning
        of every OAuth and OpenID Connect flow.
      tags:
      - iam
      x-app: iam
      x-public: true
    post:
      description: |-
        Starts a sign-in — the address you send a browser to, and the
        beginning of every OAuth and OpenID Connect flow.

        If the person is ALREADY signed in here, it does not ask them again: it
        returns them to the application with a one-time code and they never see this
        page. Otherwise it shows the right way to sign in for the application they are
        signing in to, or hands off to another identity provider if that is what they
        pick.

        A client can say what it wants with `prompt`: `none` means answer without any
        screen at all — with the code if a session exists, with an error if not, but
        never with a page; `login` means ask for the password again even if a session
        exists; `select_account` means let the person choose which identity to use.

        It returns only to an address the application has registered. That check
        happens before anything else, so a request naming an unregistered address is
        refused where the person can see it rather than being bounced onwards.
      operationId: post_iam_oauth_authorize
      summary: Starts a sign-in — the address you send a browser to, and the beginning
        of every OAuth and OpenID Connect flow.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/callback:
    get:
      description: |-
        Completes the round-trip: it resolves and burns the
        single-use transaction (checking expiry + browser binding), exchanges and
        verifies the IdP response, links or provisions the local user, and mints the
        iam authorization code the relying party expects — then redirects to the
        original redirect_uri with code + state.
      operationId: get_iam_oauth_callback
      summary: 'Completes the round-trip: it resolves and burns the single-use transaction
        (checking expiry + browser binding), exchanges and verifies the IdP response,
        links or provisions the local user, and mints the iam authorization code the
        relying party expects — then redirects to the original redirect_uri with code
        + state.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/device:
    post:
      description: |-
        Starts a sign-in on a device with no browser and no keyboard —
        a TV, a CLI, a headless box. It returns a short code to show the person and
        the address to send them to on a phone or laptop.

        Nothing is granted until a human approves it there; until then the code is
        just a pending request.
      operationId: post_iam_oauth_device
      summary: Starts a sign-in on a device with no browser and no keyboard — a TV,
        a CLI, a headless box.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/device/info:
    post:
      description: |-
        Answers "what am I approving?" for a pending device code.

        The approval page exists to tell a human WHICH application they are authorizing;
        a page that names the wrong one defeats the control it implements. It used to
        render the portal's own app name — a constant, `hanzo-console` for every code —
        so a device code minted by `hanzo-cli` was approved under a screen naming a
        different application entirely. The client is a property of the CODE, so it is
        read from the code's row here and nowhere else.

        Requires a signed-in session, and answers with the same ONE opaque refusal
        approveDevice uses. That is deliberate: the user_code is only 40 bits and is the
        one secret in this flow, so an unauthenticated lookup — or one that
        distinguished unknown from expired from already-approved — would be an oracle
        for hunting live codes. Gated and opaque, it reveals strictly less than the
        approval the same caller could already attempt.
      operationId: post_iam_oauth_device_info
      summary: Answers "what am I approving?" for a pending device code.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/federation/mfa:
    post:
      description: |-
        Completes a sign-in that came in through another identity
        provider and still owes a second factor. The person supplies the factor here
        and the login finishes.

        The account is fixed when the challenge is issued, not by the request, so no
        one can redirect a half-finished login onto somebody else's account. A wrong
        factor uses the challenge up: retrying means starting the sign-in again, the
        same as a mistyped password.
      operationId: post_iam_oauth_federation_mfa
      summary: Completes a sign-in that came in through another identity provider
        and still owes a second factor.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/introspect:
    post:
      description: |-
        Answers whether an access token is still good, and what it
        is good for — the check a resource server of yours makes before honouring a
        token it did not mint.

        A token counts as active only if it verifies AND has not been revoked, so a
        revoked token reads as dead here immediately rather than until it expires. A
        token that is unknown, expired or revoked answers simply that it is not
        active, and nothing more — the endpoint is not a way to learn about tokens you
        were not given.
      operationId: post_iam_oauth_introspect
      summary: Answers whether an access token is still good, and what it is good
        for — the check a resource server of yours makes before honouring a token
        it did not mint.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/logout:
    get:
      description: |-
        Ends a sign-in and sends the browser somewhere sensible. Accepts
        GET or POST, so it works as a plain link.

        It ACTUALLY signs you out, which is worth stating because the endpoint spent a
        release not doing it: the whole body computed a redirect and answered
        {"status":"ok"} unconditionally — no session ended, no token revoked. A logout
        that reports success while leaving the session live is worse than no logout at
        all, because the person on the shared machine believes it worked. Three things
        happen here now, in this order:

         1. The browser session dies — sid revoked server-side AND the cookie expired
            (sessions.Clear). Server-side revocation is the load-bearing half: a copy
            of the cookie taken before logout must not still resolve.
         2. The relying party's tokens are revoked when an id_token_hint names it, so
            the refresh token cannot mint a fresh access token after the human left.
            Revocation state is authoritative — a JWT's `exp` still reads valid for
            days, so expiry is necessary but never sufficient.
         3. Only then is a redirect considered, and only to a REGISTERED uri.

        The open-redirect guard is unchanged: a redirect happens only when a VERIFIED
        id_token_hint identifies the application and that application has registered
        the target. Anything else refuses to redirect — nobody can turn your logout
        link into a redirect to a site of their choosing.
      operationId: get_iam_oauth_logout
      summary: Ends a sign-in and sends the browser somewhere sensible.
      tags:
      - iam
      x-app: iam
      x-public: true
    post:
      description: |-
        Ends a sign-in and sends the browser somewhere sensible. Accepts
        GET or POST, so it works as a plain link.

        It ACTUALLY signs you out, which is worth stating because the endpoint spent a
        release not doing it: the whole body computed a redirect and answered
        {"status":"ok"} unconditionally — no session ended, no token revoked. A logout
        that reports success while leaving the session live is worse than no logout at
        all, because the person on the shared machine believes it worked. Three things
        happen here now, in this order:

         1. The browser session dies — sid revoked server-side AND the cookie expired
            (sessions.Clear). Server-side revocation is the load-bearing half: a copy
            of the cookie taken before logout must not still resolve.
         2. The relying party's tokens are revoked when an id_token_hint names it, so
            the refresh token cannot mint a fresh access token after the human left.
            Revocation state is authoritative — a JWT's `exp` still reads valid for
            days, so expiry is necessary but never sufficient.
         3. Only then is a redirect considered, and only to a REGISTERED uri.

        The open-redirect guard is unchanged: a redirect happens only when a VERIFIED
        id_token_hint identifies the application and that application has registered
        the target. Anything else refuses to redirect — nobody can turn your logout
        link into a redirect to a site of their choosing.
      operationId: post_iam_oauth_logout
      summary: Ends a sign-in and sends the browser somewhere sensible.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/revoke:
    post:
      description: |-
        Retires a token before it expires — what you call when someone
        signs out or a credential may have leaked.

        Revoking an access token kills that token. Revoking a REFRESH token kills the
        whole chain it belongs to, so no further access tokens can be minted from it
        and every token already minted from it dies with it.

        A token that is not yours, or that never existed, answers success and does
        nothing — so the endpoint cannot be used to discover which tokens are real.

        PUBLIC clients revoke too, and must: sign-out is the only control a long-lived
        refresh token has. hanzo-cli is a public PKCE client holding a 30-day rotating
        refresh token, so a confidential-only revocation endpoint made `hanzo auth
        logout` a LOCAL DELETE — the credential it dropped stayed spendable at
        hanzo.id for the rest of the month, with nothing able to kill it. Measured
        2026-08-01: revoke answered 401 invalid_client and the refresh token went on
        minting access tokens.

        Widening authentication does not widen authority. The caller must still POSSESS
        the token — and possession already permits USE, of which revocation is the
        strict opposite — and the row must belong to the client that presents it, so a
        public client_id buys the ability to destroy exactly what its holder could
        otherwise spend. RFC 6749 §3.2.1 is the same reading: a client with no
        credentials identifies itself with client_id.
      operationId: post_iam_oauth_revoke
      summary: Retires a token before it expires — what you call when someone signs
        out or a credential may have leaked.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/token:
    post:
      description: |-
        Exchanges what your application is holding for the tokens it
        needs — the one-time code from a finished sign-in, a refresh token, or your
        own client credentials when the caller is a program rather than a person.

        A refresh returns a NEW refresh token and retires the one you sent. If a
        retired one is ever presented again the whole chain is revoked, on the
        assumption that a token which came back from the dead was copied — so a stolen
        refresh token buys an attacker one use and costs them the session.

        Responses are never cached, by any hop.
      operationId: post_iam_oauth_token
      summary: Exchanges what your application is holding for the tokens it needs
        — the one-time code from a finished sign-in, a refresh token, or your own
        client credentials when the caller is a program rather than a person.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/oauth/userinfo:
    get:
      description: |-
        Returns the profile claims for whoever the access token
        belongs to — the standard OpenID Connect way to find out who is calling you
        without your application storing anything itself.

        The token must still be live: revoke it and this stops answering.
      operationId: get_iam_oauth_userinfo
      summary: Returns the profile claims for whoever the access token belongs to
        — the standard OpenID Connect way to find out who is calling you without your
        application storing anything itself.
      tags:
      - iam
      x-app: iam
      x-public: true
    post:
      description: |-
        Returns the profile claims for whoever the access token
        belongs to — the standard OpenID Connect way to find out who is calling you
        without your application storing anything itself.

        The token must still be live: revoke it and this stops answering.
      operationId: post_iam_oauth_userinfo
      summary: Returns the profile claims for whoever the access token belongs to
        — the standard OpenID Connect way to find out who is calling you without your
        application storing anything itself.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/onboard:
    post:
      description: |-
        Finishes setting up the account of whoever is calling — it
        creates their organization if they have none and puts them in it, so a person
        who has just signed up lands somewhere they can work.

        It always acts on the caller and never on somebody named in the request, so
        there is no way to onboard another person's account through it.
      operationId: post_iam_onboard
      summary: Finishes setting up the account of whoever is calling — it creates
        their organization if they have none and puts them in it, so a person who
        has just signed up lands somewhere they can work.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/organizations:
    get:
      description: |-
        Returns the organizations you can see, newest first. Narrow it to one
        parent account, and set a limit and offset to page through the rest.
      operationId: listOrganizations
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.ListOrganizationsOutput'
          description: ok
      summary: Returns the organizations you can see, newest first.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Makes a new organization — the account your users, applications, roles,
        projects and workspaces are all named inside. It is the first write in a new
        tenant, and a name already in use is refused rather than taken over.
      operationId: createOrganization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.CreateOrganizationInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Organization'
          description: ok
      summary: Makes a new organization — the account your users, applications, roles,
        projects and workspaces are all named inside.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/organizations/avatar:
    post:
      description: |-
        Changes how an organization appears across Hanzo: the square mark
        beside its name, as an uploaded image or as a single emoji. Sending an image
        clears the emoji and sending an emoji clears the image — an organization has
        one mark, not a preference order — and sending neither clears both, which is
        how it goes back to being drawn as its initial.

        An image is an https link or the bytes inline as a data URL, up to 96 KiB.
        Anyone who administers the organization may set this; it is not reserved to
        the platform.

        It writes the two fields onto the stored row and touches nothing else, which
        update cannot do: update replaces the whole record, and a record read back
        first arrives masked, so a read-modify-write through it would persist the mask
        over the organization's own credential settings.
      operationId: setOrganizationAvatar
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.SetAvatarInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Organization'
          description: ok
      summary: 'Changes how an organization appears across Hanzo: the square mark
        beside its name, as an uploaded image or as a single emoji.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/organizations/delete:
    post:
      description: |-
        Removes an organization and everything named inside it. There is no
        undo, and every session issued under it stops working.

        The built-in admin organization cannot be deleted — losing it would leave the
        account with no way back in.
      operationId: deleteOrganization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.DeleteOrganizationInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.DeleteOrganizationOutput'
          description: ok
      summary: Removes an organization and everything named inside it.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/organizations/get:
    get:
      description: |-
        Returns one organization: its display, its defaults and the sign-in rules
        everyone in it inherits.
      operationId: getOrganization
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      - in: query
        name: name
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Organization'
          description: ok
      summary: 'Returns one organization: its display, its defaults and the sign-in
        rules everyone in it inherits.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/organizations/search:
    get:
      description: |-
        Returns the organizations you can act in, the ones you belong to first
        and the rest after, newest first, narrowed by an optional query against the
        name or the display name.

        Platform operators see every organization; everyone else sees their own. Pass
        the cursor from the previous page to continue; an empty cursor in the answer
        means there is nothing more.
      operationId: searchOrganizations
      parameters:
      - in: header
        name: X-Forwarded-For
        required: false
        schema:
          type: string
      - in: query
        name: q
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      - in: query
        name: cursor
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.SearchOrganizationsOutput'
          description: ok
      summary: Returns the organizations you can act in, the ones you belong to first
        and the rest after, newest first, narrowed by an optional query against the
        name or the display name.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/organizations/update:
    post:
      description: |-
        Changes an organization's display, its defaults and the sign-in rules
        everyone in it inherits. Which organization it is does not change, and neither
        does when it was created.
      operationId: updateOrganization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.UpdateOrganizationInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Organization'
          description: ok
      summary: Changes an organization's display, its defaults and the sign-in rules
        everyone in it inherits.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/password:
    put:
      description: |-
        Replaces the calling person's password. Only their own —
        there is no shape of this request that writes somebody else's.

        Prove who you are with the password you are replacing, or — when you cannot
        sign in at all — with a code sent to the address the account already holds.
        Exactly one of the two: a request carrying both proves nothing more than
        either, and answering it would mean deciding which one mattered.

        A reset also clears the account lockout, in the SAME transaction as the
        digest. Replacing a credential retires the run of guesses against the old one,
        and without this a person who reset a forgotten password was still refused for
        up to fifteen more minutes — with the brand-new password they had just chosen.
      operationId: put_iam_password
      parameters:
      - in: header
        name: Cookie
        required: false
        schema:
          type: string
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.passwordBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: Replaces the calling person's password.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/permissions:
    get:
      description: |-
        Returns the permissions in one organization, newest first — each one a
        grant saying which people or roles may do what, and to which resources.
      operationId: get_iam_permissions
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.permission.ListResponse'
          description: ok
      summary: Returns the permissions in one organization, newest first — each one
        a grant saying which people or roles may do what, and to which resources.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Grants a permission — the call that gives a person or a role the ability to
        do something. Adding refuses to overwrite a grant that already exists, so
        widening an existing one is an update, never an accident.
      operationId: post_iam_permissions
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Permission'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Permission'
          description: ok
      summary: Grants a permission — the call that gives a person or a role the ability
        to do something.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/permissions/delete:
    post:
      description: |-
        Revokes a permission. Everyone who held access only through it loses
        that access immediately; grants they hold by another route are untouched.
      operationId: post_iam_permissions_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.permission.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.permission.DeleteResponse'
          description: ok
      summary: Revokes a permission.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/permissions/get:
    get:
      description: |-
        Returns one permission: who it grants to, what it allows, and the
        resources it covers.
      operationId: get_iam_permissions_get
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      - in: query
        name: name
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Permission'
          description: ok
      summary: 'Returns one permission: who it grants to, what it allows, and the
        resources it covers.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/permissions/update:
    post:
      description: |-
        Changes who a permission grants to, what it allows, or the resources it
        covers. Access changes as soon as the write lands. What the permission is
        called does not change, and neither does when it was created.
      operationId: post_iam_permissions_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Permission'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Permission'
          description: ok
      summary: Changes who a permission grants to, what it allows, or the resources
        it covers.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/preferences:
    post:
      description: |-
        Saves the calling person's own settings and returns
        the full set afterwards. Send only the settings you are changing — the rest
        are kept, so two screens can save at once without one undoing the other.
      operationId: post_iam_preferences
      summary: Saves the calling person's own settings and returns the full set afterwards.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/projects:
    get:
      description: |-
        Returns your organization's projects, newest first — the scope
        people pick between when their work is separated by product or client rather
        than by team.

        You see your own organization's projects and no one else's; which organization that
        is comes from your credentials, not from the request.
      operationId: get_iam_projects
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.projects.ListOutput'
          description: ok
      summary: Returns your organization's projects, newest first — the scope people
        pick between when their work is separated by product or client rather than
        by team.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Makes a project inside your organization — the scope people pick
        between when their work is separated by product or client rather than by team.
        A name already used in the organization is refused.
      operationId: post_iam_projects
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Project'
          description: ok
      summary: Makes a project inside your organization — the scope people pick between
        when their work is separated by product or client rather than by team.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/projects/delete:
    post:
      description: |-
        Removes a project. The people and roles in your organization are
        unchanged; what goes is the scope itself, so move anything addressed by it
        first.
      operationId: post_iam_projects_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.projects.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.projects.DeleteOutput'
          description: ok
      summary: Removes a project.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/projects/get:
    post:
      description: 'Returns one project: what it is called and how it is set up.'
      operationId: post_iam_projects_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.projects.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Project'
          description: ok
      summary: 'Returns one project: what it is called and how it is set up.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/projects/update:
    post:
      description: |-
        Changes a project's settings. What it is called does not change, and
        neither does when it was created.
      operationId: post_iam_projects_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Project'
          description: ok
      summary: Changes a project's settings.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/providers:
    get:
      description: |-
        Returns your organization's providers, newest first — the
        identity providers your people sign in with, and the senders and connectors
        your applications go through.

        You see your own organization's providers and no one else's; which organization
        that is comes from your credentials, not from the request.
      operationId: listProviders
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.listProvidersOut'
          description: ok
      summary: Returns your organization's providers, newest first — the identity
        providers your people sign in with, and the senders and connectors your applications
        go through.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds an identity provider your people can sign in with, or a
        service your applications send through — a social or enterprise login, an email
        or SMS sender, a storage or payment connector.

        A provider is configured once and then switched on per application, so several
        applications can share one set of credentials.
      operationId: addProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Provider'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.providerResult'
          description: ok
      summary: Adds an identity provider your people can sign in with, or a service
        your applications send through — a social or enterprise login, an email or
        SMS sender, a storage or payment connector.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/providers/delete:
    post:
      description: |-
        Removes a provider. Sign-in through it stops for every
        application that used it, so give those applications another method first.

        A provider that is already gone answers "nothing changed" rather than an
        error, so the call is safe to repeat.
      operationId: deleteProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.providerKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.mutationResult'
          description: ok
      summary: Removes a provider.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/providers/get:
    post:
      description: |-
        Returns one provider: what it connects to and how it is
        configured. Its credentials come back masked.
      operationId: getProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.providerKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.providerResult'
          description: ok
      summary: 'Returns one provider: what it connects to and how it is configured.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/providers/update:
    post:
      description: |-
        Changes a provider's settings or rotates the credentials it
        holds. The change takes effect on the next sign-in through it — sessions
        already issued are unaffected.

        A provider that is not there answers "nothing changed" rather than an error, so
        the call is safe to repeat.
      operationId: updateProvider
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Provider'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.mutationResult'
          description: ok
      summary: Changes a provider's settings or rotates the credentials it holds.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/registry/jwks:
    get:
      description: |-
        Publishes the public key your registry uses to verify the tokens issued
        above — the one URL to configure so the registry trusts logins without holding
        any secret of its own.

        If no signing key is available it refuses rather than publishing an empty set,
        because a registry that trusts nothing looks identical to one that trusts
        everything until somebody tries to push.
      operationId: get_iam_registry_jwks
      summary: Publishes the public key your registry uses to verify the tokens issued
        above — the one URL to configure so the registry trusts logins without holding
        any secret of its own.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/registry/token:
    get:
      description: |-
        Signs a container client in to your registry. `docker login`, and every
        build tool that pushes or pulls images, lands here: it exchanges the
        credential for a short-lived token scoped to exactly the repositories that
        credential may touch.

        Both of the shapes container tooling uses are accepted, so the same login works
        whichever client your pipeline runs.
      operationId: get_iam_registry_token
      summary: Signs a container client in to your registry.
      tags:
      - iam
      x-app: iam
      x-public: true
    post:
      description: |-
        Signs a container client in to your registry. `docker login`, and every
        build tool that pushes or pulls images, lands here: it exchanges the
        credential for a short-lived token scoped to exactly the repositories that
        credential may touch.

        Both of the shapes container tooling uses are accepted, so the same login works
        whichever client your pipeline runs.
      operationId: post_iam_registry_token
      summary: Signs a container client in to your registry.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/release:
    post:
      description: |-
        Steps a platform operator back out: it returns their own access
        token with no organization assumed, which is the credential they had before
        they stepped in. Recorded like the step in.
      operationId: post_iam_release
      parameters:
      - in: header
        name: Authorization
        required: false
        schema:
          type: string
      - in: header
        name: X-Forwarded-For
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.assumeBody'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: unauthorized
      summary: 'Steps a platform operator back out: it returns their own access token
        with no organization assumed, which is the credential they had before they
        stepped in.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/resolve-key:
    get:
      description: |-
        Answers which organization a PUBLISHABLE key belongs to —
        what a service of yours calls to attribute a request that arrived carrying a
        key shipped in a browser.

        It names an organization and never a person: no path through it can load or
        return a user, so a key you put in client code cannot become a way to learn
        who anyone is. A key that is expired, secret rather than publishable, or
        simply unknown all answer with the same sentence, and with a `code` saying
        which of those it was. Only a confidential service that already proved it may
        resolve keys at all ever reads that code — there is no anonymous caller here
        to probe for which keys exist — and telling it apart is what lets the holder
        be told to re-mint an expired key instead of hunting a configuration error.
      operationId: get_iam_resolve-key
      summary: Answers which organization a PUBLISHABLE key belongs to — what a service
        of yours calls to attribute a request that arrived carrying a key shipped
        in a browser.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/revoke-user-keys:
    post:
      description: |-
        Clears the target user's key of the requested TYPE (immediate
        revoke). Scoped by the same `?type` field mint takes, so revoking the browser key
        leaves the server key working. A secret key's stored value is the sk- in its
        schema.Key row.
      operationId: post_iam_revoke-user-keys
      summary: Clears the target user's key of the requested TYPE (immediate revoke).
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/roles:
    get:
      description: |-
        Returns your organization's roles, newest first — each a named group of
        people that permissions are granted to.

        You see your own organization's roles and no one else's; which organization
        that is comes from your credentials, not from the request.
      operationId: get_iam_roles
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.roles.ListOutput'
          description: ok
      summary: Returns your organization's roles, newest first — each a named group
        of people that permissions are granted to.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Makes a role — a named group of people that permissions are granted to.
        Granting to a role rather than to each person is what keeps access correct as
        your team changes: add someone to the role and they inherit everything it can
        do. A name already used in your organization is refused.
      operationId: post_iam_roles
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.roles.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Role'
          description: ok
      summary: Makes a role — a named group of people that permissions are granted
        to.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/roles/delete:
    post:
      description: |-
        Removes a role. Everyone in it loses the access it carried; their
        accounts, and any other role they hold, are untouched.
      operationId: post_iam_roles_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.roles.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.roles.DeleteOutput'
          description: ok
      summary: Removes a role.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/roles/get:
    post:
      description: 'Returns one role: who is in it, and the roles it includes.'
      operationId: post_iam_roles_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.roles.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Role'
          description: ok
      summary: 'Returns one role: who is in it, and the roles it includes.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/roles/update:
    post:
      description: |-
        Changes who is in a role, or which roles it includes. Access changes for
        everyone in it as soon as the write lands. What the role is called does not
        change, and neither does when it was created.
      operationId: post_iam_roles_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.roles.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Role'
          description: ok
      summary: Changes who is in a role, or which roles it includes.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/ResourceTypes:
    get:
      description: |-
        Returns the kinds of record this directory provisions and
        the address of each, so your identity provider discovers them rather than
        having them configured by hand.
      operationId: get_iam_scim_v2_resourcetypes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.listResponse'
          description: ok
      summary: Returns the kinds of record this directory provisions and the address
        of each, so your identity provider discovers them rather than having them
        configured by hand.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/ResourceTypes/{name}:
    get:
      description: Returns one provisionable record kind in full.
      operationId: get_iam_scim_v2_resourcetypes_by_name
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
        "404":
          content:
            application/json:
              schema: {}
          description: not found
      summary: Returns one provisionable record kind in full.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/Schemas:
    get:
      description: |-
        Returns the attribute definitions this directory understands, so
        your identity provider knows which fields it may send and what they mean
        before it sends any.
      operationId: get_iam_scim_v2_schemas
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.listResponse'
          description: ok
      summary: Returns the attribute definitions this directory understands, so your
        identity provider knows which fields it may send and what they mean before
        it sends any.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/Schemas/{id}:
    get:
      description: Returns one attribute definition in full.
      operationId: get_iam_scim_v2_schemas_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
        "404":
          content:
            application/json:
              schema: {}
          description: not found
      summary: Returns one attribute definition in full.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/ServiceProviderConfig:
    get:
      description: |-
        Tells your identity provider which parts of SCIM this
        directory supports, so it configures itself instead of you filling in a form.

        Filtering and partial updates are supported. Bulk operations, sorting and
        entity tags are not — an IdP that reads this will not attempt them.
      operationId: get_iam_scim_v2_serviceproviderconfig
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.config'
          description: ok
      summary: Tells your identity provider which parts of SCIM this directory supports,
        so it configures itself instead of you filling in a form.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/scim/v2/Users:
    get:
      description: |-
        Returns the people in your organization to your identity provider,
        in the standard SCIM shape, so an IdP can reconcile its directory against
        ours. Searchable by username or email address, and paged.

        Reading the whole list takes an administrator; an ordinary person is refused.
      operationId: get_iam_scim_v2_users
      summary: Returns the people in your organization to your identity provider,
        in the standard SCIM shape, so an IdP can reconcile its directory against
        ours.
      tags:
      - iam
      x-app: iam
      x-public: true
    post:
      description: |-
        Provisions a person from your identity provider — how a new hire
        gets an account here automatically when they are added over there.

        Takes an administrator. Making someone an administrator takes more than that,
        so an IdP integration cannot escalate anyone by setting a flag.
      operationId: post_iam_scim_v2_users
      summary: Provisions a person from your identity provider — how a new hire gets
        an account here automatically when they are added over there.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/scim/v2/Users/{owner}/{name}:
    delete:
      description: |-
        Deprovisions a person — how removing someone in your identity
        provider removes their access here. Their sessions stop working immediately.
        Takes an administrator.
      operationId: delete_iam_scim_v2_users_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Deprovisions a person — how removing someone in your identity provider
        removes their access here.
      tags:
      - iam
      x-app: iam
      x-public: true
    get:
      description: |-
        Returns one person in the standard SCIM shape. An administrator may
        read anyone in the organization; everyone else may read only themselves.
      operationId: get_iam_scim_v2_users_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Returns one person in the standard SCIM shape.
      tags:
      - iam
      x-app: iam
      x-public: true
    patch:
      description: |-
        Applies a partial change from your identity provider — one attribute
        moved, not the whole record resent.

        The change is applied onto the person as they currently are, so everything you
        did not mention keeps its value, including the parts SCIM does not describe.
      operationId: patch_iam_scim_v2_users_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Applies a partial change from your identity provider — one attribute
        moved, not the whole record resent.
      tags:
      - iam
      x-app: iam
      x-public: true
    put:
      description: |-
        Overwrites a person's SCIM attributes with what your identity
        provider sends — how a change made there lands here.

        Only the attributes SCIM describes are replaced. Anything the standard does not
        cover — their multi-factor enrolment above all — survives untouched, so a
        routine sync from your IdP can never quietly strip someone's second factor or
        bring a deleted account back.
      operationId: put_iam_scim_v2_users_by_owner_by_name
      parameters:
      - in: path
        name: owner
        required: true
        schema:
          type: string
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Overwrites a person's SCIM attributes with what your identity provider
        sends — how a change made there lands here.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/send-verification-code:
    post:
      description: |-
        Validates the request and asks otp to get a code to the
        person. The request fields are read via fiber's FormValue — the escape hatch zip
        exposes for form bodies (multipart or urlencoded) — since the typed JSON Bind does
        not apply here. v1 also accepts countryCode/method/checkUser/captchaType; iam
        ignores them (the captcha/forget/MFA flows those drive are not ported), and
        CAPTCHA verification is likewise not enforced — iam models no captcha provider —
        so the code is issued once the destination and application validate.
      operationId: post_iam_send-verification-code
      summary: Validates the request and asks otp to get a code to the person.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/service-accounts:
    get:
      description: |-
        Returns your organization's service accounts — what each is called and
        when it was created. Never their secrets: a key's secret half exists in a
        response exactly once, when it is minted. Paginated in memory over the already org-scoped
        slice — the set per org is small, so a dedicated count query is overkill
        (v1 service_account.go:296-307).
      operationId: get_iam_service-accounts
      parameters:
      - description: Organization is the organization whose service accounts to list.
          Required.
        in: query
        name: organization
        required: false
        schema:
          type: string
      - description: |-
          P is the 1-indexed page to return. Paging takes both p and pageSize —
          leave either out, or send something that is not a number, and the whole
          list comes back.
        in: query
        name: p
        required: false
        schema:
          type: integer
      - description: Size is how many accounts a page holds.
        in: query
        name: pageSize
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: ok
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Answer'
          description: bad request
      summary: Returns your organization's service accounts — what each is called
        and when it was created.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Makes a service account — an identity for a program rather than a
        person, for a script, a bot or a deployment that has to authenticate on its
        own.

        It comes back with its first key, and the secret half is shown ONCE, here.
        There is no way to read it again; if you lose it, rotate.
      operationId: post_iam_service-accounts
      summary: Makes a service account — an identity for a program rather than a person,
        for a script, a bot or a deployment that has to authenticate on its own.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/service-accounts/{name}:
    delete:
      description: Serves DELETE /v1/iam/service-accounts/:name.
      operationId: delete_iam_service-accounts_by_name
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: Serves DELETE /v1/iam/service-accounts/:name.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/service-accounts/{name}/keys:
    post:
      description: |-
        Serves POST /v1/iam/service-accounts/:name/keys: mint a fresh key,
        invalidating the prior one, and return the new raw secret exactly once.
      operationId: post_iam_service-accounts_by_name_keys
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      summary: 'Serves POST /v1/iam/service-accounts/:name/keys: mint a fresh key,
        invalidating the prior one, and return the new raw secret exactly once.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/sessions/create:
    post:
      description: |-
        Records a sign-in. Signing in again from another browser adds to the
        session rather than replacing it, so one person can be signed in from a laptop
        and a phone at once.

        Ask for an exclusive sign-in and the opposite holds: the new sign-in is the only
        one left and every other browser is signed out. That is the setting to use when
        one person may hold only one live session at a time.
      operationId: createSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.CreateSessionIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Session'
          description: ok
      summary: Records a sign-in.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/sessions/delete:
    post:
      description: |-
        Signs a person out of one application — the session ends and every
        browser carrying it stops being authenticated.

        A session that is already gone reports that nothing was deleted rather than an
        error, so the call is safe to repeat.
      operationId: deleteSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.SessionRef'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.DeleteSessionOut'
          description: ok
      summary: Signs a person out of one application — the session ends and every
        browser carrying it stops being authenticated.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/sessions/get:
    post:
      description: |-
        Returns one person's session in one application — when it began and which
        browsers or devices are still carrying it.
      operationId: getSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.SessionRef'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Session'
          description: ok
      summary: Returns one person's session in one application — when it began and
        which browsers or devices are still carrying it.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/sessions/list:
    post:
      description: |-
        Returns who is currently signed in to your organization, newest first, and
        can be narrowed to one person or one application. It is what you read before
        signing someone out.
      operationId: listSessions
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.ListSessionsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.ListSessionsOut'
          description: ok
      summary: Returns who is currently signed in to your organization, newest first,
        and can be narrowed to one person or one application.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/sessions/update:
    post:
      description: |-
        Replaces the set of browsers a session covers — signing out the ones you
        leave off while the session itself stays live. A session that does not exist is
        reported as missing rather than created.
      operationId: updateSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.UpdateSessionIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Session'
          description: ok
      summary: Replaces the set of browsers a session covers — signing out the ones
        you leave off while the session itself stays live.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/set-preferred-mfa:
    post:
      description: |-
        Picks which second factor an account is asked for first when it has
        more than one. Only a factor the account actually holds: storing an unheld one told
        the login gate "MFA is on" — factor.Enabled reads that column — while leaving it
        nothing to ask for, so the sign-in required the password alone.
      operationId: post_iam_set-preferred-mfa
      summary: Picks which second factor an account is asked for first when it has
        more than one.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/signin:
    post:
      description: |-
        Completes a sign-in: it exchanges the one-time code your
        application was handed at the end of the login flow for a live session, and
        returns the signed-in account.

        The code works once. This is the call that turns a finished login into
        something your application can act on.
      operationId: post_iam_signin
      summary: 'Completes a sign-in: it exchanges the one-time code your application
        was handed at the end of the login flow for a live session, and returns the
        signed-in account.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/signup:
    post:
      description: |-
        Creates an account from the sign-up form and applies the
        application's own sign-up rules — whether self-service registration is open at
        all, and which fields it requires.

        The password is hashed before it is stored and is never returned.
      operationId: post_iam_signup
      summary: Creates an account from the sign-up form and applies the application's
        own sign-up rules — whether self-service registration is open at all, and
        which fields it requires.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/tokens:
    get:
      description: |-
        Returns the access tokens issued in your organization, newest
        first, and can be narrowed to one organization. Use it to see what is currently
        authorized before revoking anything.
      operationId: listTokens
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      - in: query
        name: organization
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.listTokensOut'
          description: ok
      summary: Returns the access tokens issued in your organization, newest first,
        and can be narrowed to one organization.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Records an access token — the credential an application or integration
        presents on a caller's behalf.
      operationId: addToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Token'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.tokenResult'
          description: ok
      summary: Records an access token — the credential an application or integration
        presents on a caller's behalf.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/tokens/delete:
    post:
      description: |-
        Revokes an access token. Whatever was using it stops being
        authorized at once.

        A token that is already gone answers "nothing changed" rather than an error, so
        the call is safe to repeat.
      operationId: deleteToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.tokenKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.tokenMutation'
          description: ok
      summary: Revokes an access token.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/tokens/get:
    post:
      description: |-
        Returns one access token: who and what it was issued to, and when it
        expires.
      operationId: getToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.tokenKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.tokenResult'
          description: ok
      summary: 'Returns one access token: who and what it was issued to, and when
        it expires.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/tokens/issue:
    post:
      description: |-
        Mints an access token for the `?id=<owner>/<name>` target
        user (optional `?aud=` resource, RFC 8707), issued by the authenticated +
        allow-listed confidential client. The token's subject + owner are the TARGET
        USER's, so a resource server scopes on the validated owner claim to the user's
        tenant — indistinguishable from a token the user obtained directly. Response is
        the camelCase `{accessToken, expiresIn}` body identity.ts consumes. Equivalent to
        the RFC 8693 token-exchange grant, minus the subject_token proof (the console has
        the user's id, not a token) — the reason this compat shim exists.
      operationId: post_iam_tokens_issue
      summary: Mints an access token for the `?id=<owner>/<name>` target user (optional
        `?aud=` resource, RFC 8707), issued by the authenticated + allow-listed confidential
        client.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/tokens/update:
    post:
      description: |-
        Changes an access token's scope or expiry.

        A token that is not there answers "nothing changed" rather than an error, so
        the call is safe to repeat.
      operationId: updateToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.Token'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.tokenMutation'
          description: ok
      summary: Changes an access token's scope or expiry.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/unlink:
    post:
      description: |-
        Disconnects one sign-in identity from an account, so that provider can
        no longer be used to sign in as that person. Their account and every other way
        they sign in are untouched. Two principals may do it, and
        only two: the account holder itself, and a SuperAdmin (a member of the reserved
        admin org, the one predicate). An ORG ADMIN deliberately may NOT — unlinking is
        not tenant administration, it is unpicking someone's own sign-in method, so the
        generic org-admin rule is the wrong answer here.

        A holder unlinking itself must also be permitted by the application — the
        provider link's CanUnlink flag — so an organization that mandates federated
        sign-in cannot have its users strand themselves. A SuperAdmin is not bound by
        that flag; it is the platform's own recovery path. Fail-closed throughout.
      operationId: post_iam_unlink
      summary: Disconnects one sign-in identity from an account, so that provider
        can no longer be used to sign in as that person.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/update-preferences:
    post:
      description: |-
        Saves the calling person's own settings and returns
        the full set afterwards. Send only the settings you are changing — the rest
        are kept, so two screens can save at once without one undoing the other.
      operationId: post_iam_update-preferences
      summary: Saves the calling person's own settings and returns the full set afterwards.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/users:
    get:
      description: |-
        Returns a page of the people in your organization, with the total so you
        can page through the rest. Passwords, API secrets and MFA material are stripped
        from every entry.
      operationId: get_iam_users
      parameters:
      - in: query
        name: owner
        required: true
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      - in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.users.ListOutput'
          description: ok
      summary: Returns a page of the people in your organization, with the total so
        you can page through the rest.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds a person to your organization. Send a password and it becomes the
        one they sign in with; it is hashed before it is stored and never comes back
        in any response.

        The username is checked against the same rule every account in the Hanzo Cloud
        is held to, whichever way it was created — this call, password signup, a social
        sign-in, or SCIM — so a name accepted here works everywhere.

        A name already taken in your organization is refused rather than overwritten.
      operationId: post_iam_users
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.CreateInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.User'
          description: ok
      summary: Adds a person to your organization.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/users/delete:
    post:
      description: |-
        Removes a person from your organization. Their sessions stop working
        immediately and the account is gone rather than suspended — to keep the record
        and only stop sign-in, update the user instead.
      operationId: post_iam_users_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.users.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.users.DeleteOutput'
          description: ok
      summary: Removes a person from your organization.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/users/get:
    get:
      description: |-
        Returns one person in your organization, addressed by their username or by
        their email address. Passwords, API secrets and MFA material are stripped from
        the response.

        An address that names two accounts names none: the read refuses rather than
        picking one, and says so instead of reporting "no such user". Handing back an
        arbitrary one of two rows is how somebody gets added to a team under a
        colleague's identity.
      operationId: get_iam_users_get
      parameters:
      - in: query
        name: owner
        required: true
        schema:
          type: string
      - in: query
        name: name
        required: false
        schema:
          type: string
      - in: query
        name: email
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.User'
          description: ok
      summary: Returns one person in your organization, addressed by their username
        or by their email address.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/users/update:
    post:
      description: |-
        Changes a person's profile, their roles, or the credentials they sign
        in with. Send a password to reset it; leave it out and their current one keeps
        working.

        Who they are does not change: their organization, username and the identifier
        their existing sessions are keyed on all survive the write, so an update never
        signs anyone out.
      operationId: post_iam_users_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.UpdateInput'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.User'
          description: ok
      summary: Changes a person's profile, their roles, or the credentials they sign
        in with.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/verification-codes:
    post:
      description: |-
        Validates the request and asks otp to get a code to the
        person. The request fields are read via fiber's FormValue — the escape hatch zip
        exposes for form bodies (multipart or urlencoded) — since the typed JSON Bind does
        not apply here. v1 also accepts countryCode/method/checkUser/captchaType; iam
        ignores them (the captcha/forget/MFA flows those drive are not ported), and
        CAPTCHA verification is likewise not enforced — iam models no captcha provider —
        so the code is issued once the destination and application validate.
      operationId: post_iam_verification-codes
      summary: Validates the request and asks otp to get a code to the person.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/web3/nonce:
    get:
      description: |-
        Starts a wallet sign-in: it returns a one-time challenge for the wallet
        to sign. The challenge is good once and is tied to the site that asked for it,
        so a signature collected elsewhere cannot be replayed here.
      operationId: get_iam_web3_nonce
      summary: 'Starts a wallet sign-in: it returns a one-time challenge for the wallet
        to sign.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/web3/verify:
    post:
      description: |-
        Completes a wallet sign-in: it verifies the signed challenge and, if it
        holds, signs the wallet's owner in.

        This IS the login — it answers exactly as a password sign-in does, so the rest
        of your flow does not branch on how somebody arrived.
      operationId: post_iam_web3_verify
      summary: 'Completes a wallet sign-in: it verifies the signed challenge and,
        if it holds, signs the wallet''s owner in.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/webauthn-credentials:
    get:
      description: |-
        Returns the passkeys and security keys registered to
        one person, newest first — which device each lives on and when it was
        registered.

        Yours by default. Name somebody else and you get them only if you already
        administer their account, which is the same authority that governs reading
        their user record — so this list can never show more people than the surface
        beside it already does.

        There is no organization-wide list. It used to scope to the ORG, which handed
        an org admin every member's credential rows in one answer, and a SuperAdmin
        every tenant's; a plain member meanwhile could not read even their own,
        because an unnamed target fails the Guard's tenant rule. One scope fixes both
        halves: the answer is a person's, and the caller is the person unless they say
        otherwise and may.
      operationId: listWebauthnCredentials
      parameters:
      - in: query
        name: user
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.listWebauthnCredentialsOut'
          description: ok
      summary: Returns the passkeys and security keys registered to one person, newest
        first — which device each lives on and when it was registered.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers a passkey or security key for a person, so they
        can sign in with their device instead of a password.
      operationId: addWebauthnCredential
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.WebauthnCredential'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.webauthnCredentialResult'
          description: ok
      summary: Registers a passkey or security key for a person, so they can sign
        in with their device instead of a password.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/webauthn-credentials/delete:
    post:
      description: |-
        Removes a passkey or security key — what you call when
        a device is lost. Make sure the person has another way to sign in first.

        A credential that is already gone answers "nothing changed" rather than an
        error, so the call is safe to repeat.
      operationId: deleteWebauthnCredential
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.webauthnCredentialKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.webauthnCredentialMutationResult'
          description: ok
      summary: Removes a passkey or security key — what you call when a device is
        lost.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/webauthn-credentials/get:
    post:
      description: |-
        Returns one passkey or security key: whose it is, what
        device it lives on, and when it was registered.
      operationId: getWebauthnCredential
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.webauthnCredentialKey'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.webauthnCredentialResult'
          description: ok
      summary: 'Returns one passkey or security key: whose it is, what device it lives
        on, and when it was registered.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/webauthn-credentials/update:
    post:
      description: |-
        Renames a registered passkey or security key, so a
        person can tell their devices apart.

        A credential that is not there answers "nothing changed" rather than an error,
        so the call is safe to repeat.
      operationId: updateWebauthnCredential
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.WebauthnCredential'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.webauthnCredentialMutationResult'
          description: ok
      summary: Renames a registered passkey or security key, so a person can tell
        their devices apart.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/webauthn/signin/begin:
    get:
      description: |-
        Starts a passkey sign-in: it returns the challenge the person's
        authenticator signs.

        The account is named in the query, and the challenge is bound to it, so what may
        answer is decided here — by the server, from the row — and the finish checks the
        answer against that decision rather than recomputing it.
      operationId: get_iam_webauthn_signin_begin
      summary: 'Starts a passkey sign-in: it returns the challenge the person''s authenticator
        signs.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/webauthn/signin/finish:
    post:
      description: |-
        Verifies the signed challenge and signs the person in.

        It answers exactly as a password sign-in does — the same envelope, through the
        same grant — so nothing downstream branches on how somebody arrived.
      operationId: post_iam_webauthn_signin_finish
      summary: Verifies the signed challenge and signs the person in.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/webauthn/signup/begin:
    get:
      description: |-
        Starts enrolling a passkey for the signed-in person: it returns the
        options their browser hands to the authenticator.

        Passkeys already on the account are EXCLUDED, so a second enrollment on a device
        that already holds one is refused by the authenticator itself rather than
        silently producing a duplicate the person cannot tell apart.
      operationId: get_iam_webauthn_signup_begin
      summary: 'Starts enrolling a passkey for the signed-in person: it returns the
        options their browser hands to the authenticator.'
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/webauthn/signup/finish:
    post:
      description: |-
        Verifies the newly created passkey and stores it, so the person
        can sign in with their device from then on.
      operationId: post_iam_webauthn_signup_finish
      summary: Verifies the newly created passkey and stores it, so the person can
        sign in with their device from then on.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/whoami:
    get:
      description: |-
        Tells you who the current caller is — the lightweight check a
        page makes on load to decide whether to render signed-in or signed-out.

        It answers for a session cookie or a bearer token alike, and says plainly when
        nobody is signed in rather than failing.
      operationId: get_iam_whoami
      summary: Tells you who the current caller is — the lightweight check a page
        makes on load to decide whether to render signed-in or signed-out.
      tags:
      - iam
      x-app: iam
      x-public: true
  /v1/iam/workspaces:
    get:
      description: |-
        Returns your organization's workspaces, newest first — the scope a
        team works in, alongside projects rather than instead of them.

        You see your own organization's workspaces and no one else's; which organization that
        is comes from your credentials, not from the request.
      operationId: get_iam_workspaces
      parameters:
      - in: query
        name: owner
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.workspaces.ListOutput'
          description: ok
      summary: Returns your organization's workspaces, newest first — the scope a
        team works in, alongside projects rather than instead of them.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
    post:
      description: |-
        Makes a workspace inside your organization — the scope a team works in,
        alongside projects rather than instead of them. A name already used in the
        organization is refused.
      operationId: post_iam_workspaces
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.workspaces.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Workspace'
          description: ok
      summary: Makes a workspace inside your organization — the scope a team works
        in, alongside projects rather than instead of them.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/workspaces/delete:
    post:
      description: |-
        Removes a workspace. The people and roles in your organization are
        unchanged; what goes is the scope itself.
      operationId: post_iam_workspaces_delete
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.workspaces.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.workspaces.DeleteOutput'
          description: ok
      summary: Removes a workspace.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/workspaces/get:
    post:
      description: 'Returns one workspace: what it is called and how it is set up.'
      operationId: post_iam_workspaces_get
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.workspaces.Ref'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Workspace'
          description: ok
      summary: 'Returns one workspace: what it is called and how it is set up.'
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/iam/workspaces/update:
    post:
      description: |-
        Changes a workspace's settings. What it is called does not change, and
        neither does when it was created.
      operationId: post_iam_workspaces_update
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/iam.workspaces.Input'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/iam.Workspace'
          description: ok
      summary: Changes a workspace's settings.
      tags:
      - iam
      x-app: iam
      x-public: true
      x-tool: true
  /v1/images/generations:
    post:
      description: "Implements POST /v1/images/generations (OpenAI-compatible).\n\nBody:
        {\"model\": \"...\", \"prompt\": \"...\", \"n\"?: int, \"size\"?: \"1024x1024\",\n\n\t\"response_format\"?:
        \"url\"|\"b64_json\"}\n\nIt authenticates the caller, resolves the model to
        its upstream provider via\nthe shared routing table (zen3-image* → do-ai fal
        diffusion), reserves the\nper-image budget, generates the image(s) through
        the do-ai async image\nclient, records usage for billing, and returns the
        OpenAI images response."
      operationId: post_images_generations
      summary: Implements POST /v1/images/generations (OpenAI-compatible).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/index/health:
    get:
      description: Answers Meilisearch's `{"status":"available"}` when the index store
        is readable. It FAILS CLOSED — an unreadable store answers 503 and `unavailable`
        — so a replica whose volume has gone bad stops taking traffic instead of answering
        every search with nothing found. It touches no tenant data and needs no credential.
      operationId: get_index_health
      summary: Report whether the search plane can serve
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes:
    get:
      description: Answers every index in the caller's org with its primary key and
        timestamps. It is the only way to enumerate what an org holds — without it
        an index whose uid a caller has forgotten is unreachable. The tenant is the
        org minted from the VALIDATED bearer's owner claim, never a client-supplied
        header, and every query filters on it, so two orgs may both hold an index
        named "messages" and neither can see the other's documents. Without a validated
        principal the answer is 403 carrying Meilisearch's `invalid_api_key` body.
        Errors use Meilisearch's {message, code, type, link} shape rather than cloud's,
        because that `code` is a wire contract a Meilisearch client branches on.
      operationId: get_index_indexes
      summary: List the indexes your org holds
      tags:
      - index
      x-app: index
      x-public: true
    post:
      description: |-
        Creates an index named by `uid` in the caller's org. `primaryKey` names the document field that identifies a document and defaults to `id`. Creating an index that already exists is not an error — it settles on the existing one, primary key included — so a client that creates before every write is safe to run repeatedly. A missing or over-long uid is 400 `invalid_index_uid`. A new index starts with `user` filterable, which is what lets a multi-user app narrow searches to one end user without configuring anything. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: post_index_indexes
      summary: Create an index
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}:
    delete:
      description: |-
        Drops one index in the caller's org together with all of its documents. This is the only way to retire an index; without it a mistaken uid would be permanent. It is idempotent — dropping an index that is not there still succeeds. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: delete_index_indexes_by_uid
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Delete an index and everything in it
      tags:
      - index
      x-app: index
      x-public: true
    get:
      description: Answers a single index's uid, primary key and timestamps. An index
        the caller's org does not hold is 404 `index_not_found` — which is the same
        answer another org's index gives, since the org is a bound predicate on the
        read. The tenant is the org minted from the VALIDATED bearer's owner claim,
        never a client-supplied header, and every query filters on it, so two orgs
        may both hold an index named "messages" and neither can see the other's documents.
        Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key`
        body. Errors use Meilisearch's {message, code, type, link} shape rather than
        cloud's, because that `code` is a wire contract a Meilisearch client branches
        on.
      operationId: get_index_indexes_by_uid
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Read one index's definition
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}/documents:
    get:
      description: Answers the documents in one index with a total count. `limit`
        defaults to 20 and is capped at 1000, `offset` pages, and the response echoes
        both back so a pager knows what it actually got. An index the caller's org
        does not hold is 404 `index_not_found`. The tenant is the org minted from
        the VALIDATED bearer's owner claim, never a client-supplied header, and every
        query filters on it, so two orgs may both hold an index named "messages" and
        neither can see the other's documents. Without a validated principal the answer
        is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's
        {message, code, type, link} shape rather than cloud's, because that `code`
        is a wire contract a Meilisearch client branches on.
      operationId: get_index_indexes_by_uid_documents
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Page through the documents in an index
      tags:
      - index
      x-app: index
      x-public: true
    post:
      description: |-
        Upserts documents into one index, keyed by the index's primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400'd. The index is created on demand, so a first write needs no create call.

        This and the PUT on the same path are the SAME operation: both are a whole document upsert, which is what a Meilisearch client's addDocuments and updateDocuments both reduce to here. A body that is neither an array nor an object is 400. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: post_index_indexes_by_uid_documents
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Add or replace documents in an index
      tags:
      - index
      x-app: index
      x-public: true
    put:
      description: |-
        Upserts documents into one index, keyed by the index's primary key: a document whose key is already present is REPLACED, one that is not is added, and it becomes searchable immediately. Send an array, or a single object — a hand-rolled caller sending one document is accepted rather than 400'd. The index is created on demand, so a first write needs no create call.

        This and the POST on the same path are the SAME operation, served by one handler. Both exist because the Meilisearch dialect has both verbs; there is no partial-update semantics on this one — a document is replaced whole either way. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: put_index_indexes_by_uid_documents
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Add or update documents in an index
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}/documents/{id}:
    delete:
      description: |-
        Removes one document from an index. It is IDEMPOTENT: deleting a key that is not there succeeds rather than 404, so a retry after a lost response is safe. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: delete_index_indexes_by_uid_documents_by_id
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Delete one document by its primary key
      tags:
      - index
      x-app: index
      x-public: true
    get:
      description: Answers the stored document whose primary key matches, exactly
        as it was written. A missing document is 404 `document_not_found` and a missing
        index is 404 `index_not_found` — two different codes, because a client that
        branches on them treats the cases differently. The tenant is the org minted
        from the VALIDATED bearer's owner claim, never a client-supplied header, and
        every query filters on it, so two orgs may both hold an index named "messages"
        and neither can see the other's documents. Without a validated principal the
        answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's
        {message, code, type, link} shape rather than cloud's, because that `code`
        is a wire contract a Meilisearch client branches on.
      operationId: get_index_indexes_by_uid_documents_by_id
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Read one document by its primary key
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}/documents/delete-batch:
    post:
      description: |-
        Removes every document named by an array of primary keys. Keys may be sent as strings or numbers — a number keeps its exact decimal form, so an integer key round-trips as `42` and never as scientific notation. Keys that are absent from the index are skipped rather than failing the batch, so this is idempotent. A body that is not an array is 400. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: post_index_indexes_by_uid_documents_delete-batch
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Delete many documents by primary key in one call
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}/search:
    post:
      description: |-
        Answers the documents in one index matching `q`, ranked by how many of the query's terms they match, with prefix matching so a partial word still finds its document. `limit` defaults to 20 and is capped at 1000, `offset` pages; a negative value falls back to the default rather than erroring.

        `filter` takes a Meilisearch filter expression, or an array of them, and the `user = "…"` and `user IN […]` forms are honoured — that is how an app with many end users narrows results to one of them WITHIN the org. `estimatedTotalHits` is exact for the page returned, not an estimate, because every hit is materialised. An index the caller's org does not hold is 404 `index_not_found`. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.
      operationId: post_index_indexes_by_uid_search
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Search an index, forgiving typos
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/indexes/{uid}/settings:
    get:
      description: Answers the attributes an index allows filtering on. This dialect
        implements the filterable-attributes setting and no other, so that is the
        whole of what comes back. An index the caller's org does not hold is 404 `index_not_found`.
        The tenant is the org minted from the VALIDATED bearer's owner claim, never
        a client-supplied header, and every query filters on it, so two orgs may both
        hold an index named "messages" and neither can see the other's documents.
        Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key`
        body. Errors use Meilisearch's {message, code, type, link} shape rather than
        cloud's, because that `code` is a wire contract a Meilisearch client branches
        on.
      operationId: get_index_indexes_by_uid_settings
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Read an index's filterable attributes
      tags:
      - index
      x-app: index
      x-public: true
    patch:
      description: |-
        Replaces an index's filterable attributes with the list in `filterableAttributes`; omitting the field leaves them as they are. The index is CREATED ON DEMAND rather than 404'd, because a client that configures an index it has just asked for should not have to create it first — this is the one read-shaped path on the surface that writes. The tenant is the org minted from the VALIDATED bearer's owner claim, never a client-supplied header, and every query filters on it, so two orgs may both hold an index named "messages" and neither can see the other's documents. Without a validated principal the answer is 403 carrying Meilisearch's `invalid_api_key` body. Errors use Meilisearch's {message, code, type, link} shape rather than cloud's, because that `code` is a wire contract a Meilisearch client branches on.

        The 202 and its `enqueued` task are DIALECT COMPATIBILITY, not a promise of later work: the write is already applied when this answers, and the task it names is already complete. A client that polls waitForTask resolves immediately rather than waiting, and a client that does not poll has still had its write committed.
      operationId: patch_index_indexes_by_uid_settings
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Set which attributes an index can be filtered on
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/stats:
    get:
      description: 'Answers a document count per index for the caller''s org, plus
        their sum. `isIndexing` is always false, which is the honest answer here rather
        than a stub: writes are applied before their response, so there is never a
        backlog in progress to report. The tenant is the org minted from the VALIDATED
        bearer''s owner claim, never a client-supplied header, and every query filters
        on it, so two orgs may both hold an index named "messages" and neither can
        see the other''s documents. Without a validated principal the answer is 403
        carrying Meilisearch''s `invalid_api_key` body. Errors use Meilisearch''s
        {message, code, type, link} shape rather than cloud''s, because that `code`
        is a wire contract a Meilisearch client branches on.'
      operationId: get_index_stats
      summary: Count the documents in each of your indexes
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/tasks/{uid}:
    get:
      description: 'Answers `succeeded` for the task id given. It ALWAYS answers succeeded,
        and that is honest rather than a stub: writes on this surface are applied
        before their response returns, so by the time any task id exists to ask about,
        its work is done. It exists so a Meilisearch client''s waitForTask resolves
        at once instead of polling forever for a queue that was never there. It requires
        a validated principal but reads no tenant data.'
      operationId: get_index_tasks_by_uid
      parameters:
      - in: path
        name: uid
        required: true
        schema:
          type: string
      summary: Check a write task, which has already finished
      tags:
      - index
      x-app: index
      x-public: true
  /v1/index/version:
    get:
      description: Answers the version shape a Meilisearch client expects. It names
        THIS implementation rather than a Meilisearch release — the commit field reads
        `hanzo-cloud` — so a client that logs it records which server actually answered
        instead of implying a Meilisearch build. Needs no credential.
      operationId: get_index_version
      summary: Identify the search implementation answering
      tags:
      - index
      x-app: index
      x-public: true
  /v1/ingress/middlewares:
    get:
      description: |-
        Returns every edge transform the caller's org has configured,
        ordered by id. A route names the ones it wants, in order.
      operationId: get_ingress_middlewares
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ingressMiddlewares'
          description: ok
      summary: Returns every edge transform the caller's org has configured, ordered
        by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates or replaces one edge transform and hot-applies it. POST
        mints an id when the body omits one; PUT takes the id from the URL, which wins
        over any id in the body. type must be one of redirectScheme, stripPrefix,
        addPrefix or headers, and stripPrefix/addPrefix each require their config key.
      operationId: post_ingress_middlewares
      requestBody:
        content:
          application/json:
            example:
              config:
                prefixes: /api
              id: strip-api
              type: stripPrefix
            schema:
              $ref: '#/components/schemas/Middleware'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Middleware'
          description: ok
      summary: Creates or replaces one edge transform and hot-applies it.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/middlewares/{id}:
    delete:
      description: |-
        Removes one of the caller org's edge transforms and hot-applies
        the change. Routes still naming it stop being served (they compile as skipped)
        until they name a transform that exists. Answers 204; an id this org does not
        hold is 404.
      operationId: delete_ingress_middlewares_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: strip-api
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one of the caller org's edge transforms and hot-applies the
        change.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    get:
      description: Returns one of the caller org's edge transforms by id.
      operationId: get_ingress_middlewares_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: strip-api
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Middleware'
          description: ok
      summary: Returns one of the caller org's edge transforms by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    put:
      description: |-
        Creates or replaces one edge transform and hot-applies it. POST
        mints an id when the body omits one; PUT takes the id from the URL, which wins
        over any id in the body. type must be one of redirectScheme, stripPrefix,
        addPrefix or headers, and stripPrefix/addPrefix each require their config key.
      operationId: put_ingress_middlewares_by_id
      parameters:
      - description: |-
          ID identifies the transform within the org: [A-Za-z0-9-_.], at most 128
          chars. A create that omits it gets a generated one. Routes reference it by
          this id.
        example: strip-api
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              config:
                prefixes: /api
              id: strip-api
              type: stripPrefix
            schema:
              $ref: '#/components/schemas/Middleware'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Middleware'
          description: ok
      summary: Creates or replaces one edge transform and hot-applies it.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/routes:
    get:
      description: |-
        Returns every routing rule the caller's org has configured, ordered
        by id. A route maps an exact Host (and optional path prefix) to a service.
      operationId: get_ingress_routes
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ingressRoutes'
          description: ok
      summary: Returns every routing rule the caller's org has configured, ordered
        by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates or replaces one routing rule and hot-applies the new table —
        there is no config file and no restart. POST mints an id when the body omits
        one; PUT takes the id from the URL, which wins over any id in the body. A
        route's host is a GLOBALLY unique DNS claim: a host another org's route already
        holds is refused 409, so no tenant can hijack another's hostname.
      operationId: post_ingress_routes
      requestBody:
        content:
          application/json:
            example:
              host: app.example.com
              id: web
              service: app-pool
              tls: true
            schema:
              $ref: '#/components/schemas/Route'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
          description: ok
      summary: Creates or replaces one routing rule and hot-applies the new table
        — there is no config file and no restart.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/routes/{id}:
    delete:
      description: |-
        Removes one of the caller org's routing rules and hot-applies the
        shrunken table, freeing its host for another claim. Answers 204; an id this org
        does not hold is 404.
      operationId: delete_ingress_routes_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: web
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one of the caller org's routing rules and hot-applies the shrunken
        table, freeing its host for another claim.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    get:
      description: Returns one of the caller org's routing rules by id.
      operationId: get_ingress_routes_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: a1b2c3d4e5f60718
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
          description: ok
      summary: Returns one of the caller org's routing rules by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    put:
      description: |-
        Creates or replaces one routing rule and hot-applies the new table —
        there is no config file and no restart. POST mints an id when the body omits
        one; PUT takes the id from the URL, which wins over any id in the body. A
        route's host is a GLOBALLY unique DNS claim: a host another org's route already
        holds is refused 409, so no tenant can hijack another's hostname.
      operationId: put_ingress_routes_by_id
      parameters:
      - description: |-
          ID identifies the route within the org: [A-Za-z0-9-_.], at most 128 chars.
          A create that omits it gets a generated one.
        example: web
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              host: app.example.com
              id: web
              service: app-pool
              tls: true
            schema:
              $ref: '#/components/schemas/Route'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Route'
          description: ok
      summary: Creates or replaces one routing rule and hot-applies the new table
        — there is no config file and no restart.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/services:
    get:
      description: |-
        Returns every backend pool the caller's org has configured,
        ordered by id. A service is the weighted round-robin target a route dispatches
        to.
      operationId: get_ingress_services
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ingressServices'
          description: ok
      summary: Returns every backend pool the caller's org has configured, ordered
        by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates or replaces one backend pool and hot-applies it. POST mints
        an id when the body omits one; PUT takes the id from the URL, which wins over
        any id in the body. A pool needs at least one backend and every backend URL
        must be http(s)://host[:port].
      operationId: post_ingress_services
      requestBody:
        content:
          application/json:
            example:
              backends:
              - url: http://10.0.0.7:8000
                weight: 1
              id: app-pool
            schema:
              $ref: '#/components/schemas/Upstream'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Upstream'
          description: ok
      summary: Creates or replaces one backend pool and hot-applies it.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/services/{id}:
    delete:
      description: |-
        Removes one of the caller org's backend pools and hot-applies the
        change. Routes still pointing at it stop being served (they compile as skipped)
        until they name a pool that exists. Answers 204; an id this org does not hold
        is 404.
      operationId: delete_ingress_services_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: app-pool
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one of the caller org's backend pools and hot-applies the change.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    get:
      description: Returns one of the caller org's backend pools by id.
      operationId: get_ingress_services_by_id
      parameters:
      - description: ID is the object to act on, from the path.
        example: app-pool
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Upstream'
          description: ok
      summary: Returns one of the caller org's backend pools by id.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    put:
      description: |-
        Creates or replaces one backend pool and hot-applies it. POST mints
        an id when the body omits one; PUT takes the id from the URL, which wins over
        any id in the body. A pool needs at least one backend and every backend URL
        must be http(s)://host[:port].
      operationId: put_ingress_services_by_id
      parameters:
      - description: |-
          ID identifies the pool within the org: [A-Za-z0-9-_.], at most 128 chars.
          A create that omits it gets a generated one. Routes reference it by this id.
        example: app-pool
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              backends:
              - url: http://10.0.0.7:8000
                weight: 1
              id: app-pool
            schema:
              $ref: '#/components/schemas/Upstream'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Upstream'
          description: ok
      summary: Creates or replaces one backend pool and hot-applies it.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/status:
    get:
      description: |-
        Status reports the ingress edge's live posture: the role this instance runs in
        (app or edge), whether its listeners are bound and on which addresses, the ACME
        posture (staging flag and certificate cache directory), how many hosts the
        compiled route table currently serves, and how many the ACME HostPolicy will
        issue a certificate for.
      operationId: get_ingress_status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ingressStatus'
          description: ok
      summary: 'Status reports the ingress edge''s live posture: the role this instance
        runs in (app or edge), whether its listeners are bound and on which addresses,
        the ACME posture (staging flag and certificate cache directory), how many
        hosts the compiled route table currently serves, and how many the ACME HostPolicy
        will issue a certificate for.'
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/ingress/tls:
    get:
      description: |-
        GetTLS returns the caller org's ACME intent together with the edge-wide TLS
        facts it lands in: which role this instance runs in, whether its listeners are
        bound, every host the ACME HostPolicy will issue a certificate for (the union
        across ALL orgs of TLS-marked routes and configured extraHosts, because one
        process holds one certificate cache), and the ACME directory and account email
        the process was started with.
      operationId: get_ingress_tls
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ingressTLS'
          description: ok
      summary: 'GetTLS returns the caller org''s ACME intent together with the edge-wide
        TLS facts it lands in: which role this instance runs in, whether its listeners
        are bound, every host the ACME HostPolicy will issue a certificate for (the
        union across ALL orgs of TLS-marked routes and configured extraHosts, because
        one process holds one certificate cache), and the ACME directory and account
        email the process was started with.'
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
    put:
      description: |-
        PutTLS replaces the caller org's ACME intent and hot-applies what can be
        hot-applied. extraHosts are normalized and validated, then feed the ACME
        HostPolicy on the reload this op performs, alongside the per-route tls flags.
        acmeEmail and staging bind an ACME account for the lifetime of an edge process,
        so they only take effect when the edge (re)starts — the returned note says so.
      operationId: put_ingress_tls
      requestBody:
        content:
          application/json:
            example:
              extraHosts:
              - www.example.com
            schema:
              $ref: '#/components/schemas/TLSConfig'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TLSConfig'
          description: ok
      summary: PutTLS replaces the caller org's ACME intent and hot-applies what can
        be hot-applied.
      tags:
      - ingress
      x-app: ingress
      x-public: true
      x-tool: true
  /v1/insights/events:
    get:
      description: |-
        Returns the caller org's most recent product events, newest first.
        The console's raw-event view over event.event — the same table the capture doors
        fill — one row per stored event, with the row's attributes returned as the
        properties object.

        The org is the validated principal's — never a parameter — and a read requires a
        real bearer, never the write-only publishable key. 403 without a validated bearer,
        503 when the warehouse is unreachable.
      operationId: get_insights_events
      parameters:
      - description: |-
          Limit is how many rows to return, newest first. Default 50, maximum 200; a
          value at or below zero, or one that is not a number, takes the default.
        example: 100
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/eventList'
          description: ok
      summary: Returns the caller org's most recent product events, newest first.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/insights/health:
    get:
      description: |-
        Reports that the unified insights surface is serving. It reads no
        tenant data and consults no dependency, so it answers 200 unconditionally and needs
        no principal — liveness must be probe-able. The warehouse-connectivity probe is a
        different question and lives at GET /v1/analytics/health.
      operationId: get_insights_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/insightsStatus'
          description: ok
      summary: Reports that the unified insights surface is serving.
      tags:
      - analytics
      x-app: analytics
      x-public: true
      x-tool: true
  /v1/integrations:
    get:
      description: |-
        Returns every registered integration provider together with THIS org's
        connection status for it — the catalog the console's Integrations page renders.
        Org-authed: a caller with no validated principal is 403, because the status is
        per-org and there is no org-less answer. User-plane providers (the /v1/integrations/connectors
        surface) are omitted; the two planes are disjoint.
      operationId: get_integrations
      responses:
        "200":
          content:
            application/json:
              example:
                providers:
                - available: true
                  category: Communication
                  connected: true
                  connection:
                    account: Acme
                    connectedAt: "2026-07-01T10:00:00Z"
                    externalId: T0231
                    scopes:
                    - chat:write
                  description: Connect your workspace.
                  id: slack
                  name: Slack
              schema:
                $ref: '#/components/schemas/listOut'
          description: ok
      summary: Returns every registered integration provider together with THIS org's
        connection status for it — the catalog the console's Integrations page renders.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/{provider}:
    get:
      description: |-
        Returns ONE provider with this org's connection status — the same view list
        carries, for a single id. An unknown id is 404, and so is a user-plane provider:
        the org surface never resolves one.
      operationId: get_integrations_by_provider
      parameters:
      - description: |-
          Provider is the registry id of the connector — "slack", "github",
          "cloudflare". Unknown ids are 404, as are the user-plane (/v1/integrations/connectors)
          providers, which this surface never resolves.
        example: slack
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                available: true
                category: Communication
                connected: false
                description: Connect your workspace.
                id: slack
                name: Slack
              schema:
                $ref: '#/components/schemas/providerView'
          description: ok
      summary: Returns ONE provider with this org's connection status — the same view
        list carries, for a single id.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/{provider}/callback:
    get:
      description: |-
        The single address every connector's OAuth flow returns to. It exchanges the authorization the provider granted, records the connection, and ALWAYS redirects the browser back to the console — on success and on every labeled failure alike, so a user never lands on a raw JSON dead end.

        It is public and carries no principal, so the org is taken ONLY from the signed state minted when the flow began; no header is trusted here. That state is single-use and is burned BEFORE the exchange, so one authorization is one attempt and a replayed return fails instead of exchanging twice.

        Tokens are sealed into the org's KMS namespace BEFORE the connection row is written, so a failure of the secret store leaves no half-connected integration advertising a credential that was never stored. Token values never appear in the redirect, in a log line or in an error.

        One generalization is worth knowing: a GitHub App installation returns an installation identifier instead of an OAuth code, and it is accepted in the code's place so the App model needs no second address.
      operationId: get_integrations_by_provider_callback
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: OAuth return for any connector
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/{provider}/connect:
    post:
      description: |-
        Acquires the org's credential for one provider. It has TWO paths and the
        REQUEST picks which: a "token" key in the body seals that credential directly
        (verify-before-store), and its absence begins the 3-legged OAuth flow — minting a
        single-use nonce plus an HMAC-signed state that binds this org to this provider,
        and answering with the provider's authorize URL for the caller to redirect to.

        Fail-closed order, unchanged: no principal → 403; unknown provider → 404; an
        AdminOnly connector without the caller's own-org admin bit → 403; not configured
        → 503; KMS not ready → 503 (the flow WILL need to seal a token, so refuse now
        rather than dead-end at the callback).
      operationId: post_integrations_by_provider_connect
      parameters:
      - description: Provider is the connector's registry id, from the :provider path
          segment.
        example: cloudflare
        in: path
        name: provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              accountId: a1b2c3
              provider: cloudflare
              token: cf-scoped-api-token
            schema:
              $ref: '#/components/schemas/connectIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                account: Acme
                connected: true
                externalId: a1b2c3
                provider: cloudflare
                scopes: []
              schema:
                $ref: '#/components/schemas/connectOut'
          description: ok
      summary: Acquires the org's credential for one provider.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/{provider}/disconnect:
    post:
      description: |-
        Revokes (best-effort) and forgets an org's connection: it deletes
        every custodied KMS secret and the connection row. Idempotent — disconnecting a
        provider that was never connected still returns {disconnected:true}. Symmetric
        with connect: an AdminOnly connector needs the caller's own-org admin bit.
      operationId: post_integrations_by_provider_disconnect
      parameters:
      - description: |-
          Provider is the registry id of the connector — "slack", "github",
          "cloudflare". Unknown ids are 404, as are the user-plane (/v1/integrations/connectors)
          providers, which this surface never resolves.
        example: slack
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                disconnected: true
              schema:
                $ref: '#/components/schemas/disconnectOut'
          description: ok
      summary: 'Revokes (best-effort) and forgets an org''s connection: it deletes
        every custodied KMS secret and the connection row.'
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/{provider}/verify:
    post:
      description: |-
        Re-checks a CONNECTED apikey connector's stored credential against the
        provider, live (`hanzo connector verify`). Org-scoped (any member may check
        status); the credential is read from KMS, verified, and NEVER returned or logged.
        A verification failure is reported as {active:false}, not an error — the console/
        CLI renders it. Only apikey providers support verify (OAuth tokens are checked at
        use, not re-verified here).
      operationId: post_integrations_by_provider_verify
      parameters:
      - description: |-
          Provider is the registry id of the connector — "slack", "github",
          "cloudflare". Unknown ids are 404, as are the user-plane (/v1/integrations/connectors)
          providers, which this surface never resolves.
        example: cloudflare
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                account: Acme
                active: true
                externalId: a1b2c3
                provider: cloudflare
                scopes:
                - zone:read
              schema:
                $ref: '#/components/schemas/verifyOut'
          description: ok
      summary: Re-checks a CONNECTED apikey connector's stored credential against
        the provider, live (`hanzo connector verify`).
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors:
    get:
      description: |-
        Lists the caller's OWN connectors across every provider — the set
        `hanzo connector ls` prints. Rows are keyed (org,user), so this can never
        surface another user's connector, and no secret is in the view.
      operationId: get_integrations_connectors
      responses:
        "200":
          content:
            application/json:
              example:
                connectors:
                - account: me@acme.com
                  connectedAt: "2026-07-01T10:00:00Z"
                  expiresAt: "2026-07-01T11:00:00Z"
                  externalId: u-42
                  id: openai:default
                  label: default
                  provider: openai
                  scopes:
                  - api
              schema:
                $ref: '#/components/schemas/connectorsOut'
          description: ok
      summary: Lists the caller's OWN connectors across every provider — the set `hanzo
        connector ls` prints.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{id}:
    delete:
      description: |-
        Forgets a connector: every custodied secret, then the row.
        Idempotent — dropping a never-connected id still answers {disconnected:true}
        (disconnect() parity). No provider Revoke: none of the user-plane providers
        exposes a revoke endpoint.
      operationId: delete_integrations_connectors_by_id
      parameters:
      - description: |-
          ID is the connector id, provider + ":" + label ("openai:default") — the
          auth-profile-id shape. Another user's id is simply no row, so 404.
        example: openai:work
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                disconnected: true
              schema:
                $ref: '#/components/schemas/disconnectOut'
          description: ok
      summary: 'Forgets a connector: every custodied secret, then the row.'
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{id}/refresh:
    post:
      description: |-
        Forces a token rotation for a connected connector, ahead of the
        automatic rotation a token read would do inside the expiry window. Only
        providers that declare a Refresh support it.
      operationId: post_integrations_connectors_by_id_refresh
      parameters:
      - description: |-
          ID is the connector id, provider + ":" + label ("openai:default") — the
          auth-profile-id shape. Another user's id is simply no row, so 404.
        example: openai:work
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                connector:
                  account: me@acme.com
                  connectedAt: "2026-07-01T10:00:00Z"
                  expiresAt: "2026-07-01T12:00:00Z"
                  externalId: u-42
                  id: openai:work
                  label: work
                  provider: openai
                  scopes:
                  - api
                refreshed: true
              schema:
                $ref: '#/components/schemas/refreshOut'
          description: ok
      summary: Forces a token rotation for a connected connector, ahead of the automatic
        rotation a token read would do inside the expiry window.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{id}/token:
    get:
      description: |-
        Hands the custodied access token to its owner — the ONE place
        custody exits. The (org,user)-keyed row IS the same-user gate: another user's
        id is simply "no row" → 404. fresh() auto-rotates within the refreshSkew
        window; static providers degenerate to a plain kmsGet of Secrets[0]. Refresh
        tokens are NEVER returned — custody keeps the sink. The token is never logged.
      operationId: get_integrations_connectors_by_id_token
      parameters:
      - description: |-
          ID is the connector id, provider + ":" + label ("openai:default") — the
          auth-profile-id shape. Another user's id is simply no row, so 404.
        example: openai:work
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                expiresAt: "2026-07-01T11:00:00Z"
                label: work
                provider: openai
                token: <the access token>
              schema:
                $ref: '#/components/schemas/connectorTokenOut'
          description: ok
      summary: Hands the custodied access token to its owner — the ONE place custody
        exits.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{provider}/credential:
    post:
      description: |-
        Is the direct intake path: a customer-held token/setup-token
        (Verify) or an externally obtained OAuth bundle from the CLI's local PKCE
        (Adopt). ALWAYS verify-before-store: a bad credential is refused and NOTHING
        is persisted (connectByCredential's fail-closed order).
      operationId: post_integrations_connectors_by_provider_credential
      parameters:
      - description: Provider is the user-scoped provider's registry id, from the
          path.
        example: openai
        in: path
        name: provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              label: work
              provider: openai
              token: sk-user-owned-key
            schema:
              $ref: '#/components/schemas/credentialIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                connected: true
                connector:
                  account: me@acme.com
                  connectedAt: "2026-07-01T10:00:00Z"
                  expiresAt: ""
                  externalId: u-42
                  id: openai:work
                  label: work
                  provider: openai
                  scopes:
                  - api
              schema:
                $ref: '#/components/schemas/credentialOut'
          description: ok
      summary: 'Is the direct intake path: a customer-held token/setup-token (Verify)
        or an externally obtained OAuth bundle from the CLI''s local PKCE (Adopt).'
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{provider}/device:
    post:
      description: |-
        Begins a device sign-in and returns the code to show the user plus
        how to poll for completion. KMS readiness is checked NOW rather than dead-ending
        the user at poll-done (connect() parity), and the per-provider connector cap is
        checked before the provider is called. The provider's device code is persisted
        only in the encrypted grants table and is NEVER returned.
      operationId: post_integrations_connectors_by_provider_device
      parameters:
      - description: Provider is the user-scoped provider's registry id, from the
          path.
        example: openai
        in: path
        name: provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              label: work
              provider: openai
            schema:
              $ref: '#/components/schemas/deviceStartIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                expiresAt: "2026-07-01T10:15:00Z"
                flow: g_7f2c
                interval: 5
                userCode: WDJB-MJHT
                verifyUrl: https://example.com/device
              schema:
                $ref: '#/components/schemas/deviceStartOut'
          description: ok
      summary: Begins a device sign-in and returns the code to show the user plus
        how to poll for completion.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/{provider}/device/{flow}/poll:
    post:
      description: |-
        Advances a device sign-in. Terminal outcomes are DATA, not errors
        (verifyConn {active:false} discipline) — the status set is closed:
        pending|connected|denied|expired. pollSlow collapses to "pending" on the
        wire; the raised cadence rides interval.
      operationId: post_integrations_connectors_by_provider_device_by_flow_poll
      parameters:
      - description: Provider is the user-scoped provider's registry id, from the
          path.
        example: openai
        in: path
        name: provider
        required: true
        schema:
          type: string
      - description: |-
          Flow is the id deviceStartOut returned. Expired or another user's flow is
          indistinguishable from an unknown one: 404.
        example: g_7f2c
        in: path
        name: flow
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                connector:
                  account: me@acme.com
                  connectedAt: "2026-07-01T10:05:00Z"
                  expiresAt: "2026-07-01T11:00:00Z"
                  externalId: u-42
                  id: openai:work
                  label: work
                  provider: openai
                  scopes:
                  - api
                status: connected
              schema:
                $ref: '#/components/schemas/devicePollOut'
          description: ok
      summary: Advances a device sign-in.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/connectors/providers:
    get:
      description: |-
        Lists the user-scoped provider cards — the catalog of what a
        user can connect, and how. Methods derive from capabilities (Device/Adopt/Verify
        — Mount asserts at least one), never from a parallel kind enum.
      operationId: get_integrations_connectors_providers
      responses:
        "200":
          content:
            application/json:
              example:
                providers:
                - category: AI
                  description: Use your own OpenAI account.
                  id: openai
                  methods:
                  - device
                  - token
                  name: OpenAI
                  scopes:
                  - api
              schema:
                $ref: '#/components/schemas/connectorProvidersOut'
          description: ok
      summary: Lists the user-scoped provider cards — the catalog of what a user can
        connect, and how.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/discord/interactions:
    post:
      description: |-
        The Interactions Endpoint URL for the Discord app. It answers Discord's PING with a PONG, and handles the `/hanzo` slash command by acknowledging with a deferred ephemeral reply and editing that reply with the answer once the agent has run. Any other interaction is acknowledged and ignored.

        Requests are verified by ED25519 SIGNATURE over the timestamp and body against the app's public key — not by HMAC, unlike the Slack webhooks. Interactions work over plain HTTP, so no gateway connection and no message-content intent is involved.

        Discord does not retry, so this is the one channel where being at capacity is shown to the user as an ephemeral ask-to-run-it-again rather than answered as a retriable failure — nothing is recorded either way, so the next attempt is clean.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.
      operationId: post_integrations_discord_interactions
      summary: Discord interactions endpoint
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/discord/link:
    get:
      description: |-
        The entry point behind the connect prompt Hanzo shows in a Discord server. It starts a link session and redirects to Discord's OAuth `identify` consent — the narrowest scope that establishes which Discord user is asking, and nothing more.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_discord_link
      summary: Begin linking a Hanzo account from Discord
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/discord/link/callback:
    get:
      description: |-
        The final leg: it binds the verified Discord user to the Hanzo account that just signed in, and answers a short confirmation page telling them to return to Discord. The Hanzo credential is sealed into the connected org's KMS namespace rather than stored beside the link.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_discord_link_callback
      summary: Complete the Discord account link
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/discord/link/discord:
    get:
      description: |-
        Where Discord returns the user after the identify consent. It resolves the verified Discord user, confirms the server is connected to an org, and hands the browser to the Hanzo sign-in that completes the link.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_discord_link_discord
      summary: Discord sign-in return leg
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/github/claim:
    post:
      description: |-
        Binds installations the App ALREADY holds to the org the caller is
        acting in — the reconciliation for a grant that happened outside our connect
        flow.

        An installation IS the grant: GitHub recorded the consent when the App was
        installed, and our connection row is bookkeeping that never got written because
        nobody came through our callback. This writes that row from the App's own view,
        so 23 accounts granted straight from GitHub stop reading as nothing.

        The org is taken from the VALIDATED PRINCIPAL and never from the body, because
        it is the one part GitHub cannot tell us. An installation carries an account
        login, a type and a repository selection — nothing that names a Hanzo org. So
        the binding cannot be DERIVED, only asserted, and the only unforgeable assertion
        available is the org the caller is already acting in. Inferring one from the
        account name would be a guess the store cannot catch: its key is
        (org,provider,owner), so a wrong org is a valid row, and a valid row is a
        mirror pointed at the wrong tenant.

        SUPER ADMIN only, for that same reason. A tenant's proof that an account is
        theirs is GitHub's own consent screen — the connect flow — and without it any
        org could claim any account the App holds. Platform sudo is already the scope
        that reads the whole install list, so it is the scope that may bind from it;
        giving a tenant this verb would hand it every other tenant's repositories.

        Idempotent: the row is keyed (org,provider,owner) and connected_at survives an
        upsert, so claiming twice rebinds the same account to the same org and reports
        it under `already`. Re-claiming also REFRESHES the installation id, so an
        account reinstalled on GitHub — new id, same login — self-heals instead of
        minting tokens against a dead installation.

        Claiming an account another org holds ADDS this org's row and leaves theirs
        standing, so no org loses an integration it is using.
      operationId: post_integrations_github_claim
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/githubClaimIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                already:
                - zooai
                claimed:
                - hanzoai
                - luxfi
              schema:
                $ref: '#/components/schemas/githubClaimOut'
          description: ok
      summary: Binds installations the App ALREADY holds to the org the caller is
        acting in — the reconciliation for a grant that happened outside our connect
        flow.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/fork:
    post:
      description: |-
        Forks a granted repository.

        GitHub's fork is ASYNCHRONOUS: it answers 202 with the target repo and
        populates it in the background, and it answers the same 202 when the fork
        already exists. So this reports what GitHub said rather than waiting — a call
        that blocked until the clone finished would time out on a large repository and
        tell the caller nothing it does not already know.
      operationId: post_integrations_github_fork
      requestBody:
        content:
          application/json:
            example:
              repo: widgets
            schema:
              $ref: '#/components/schemas/githubForkReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                clone_url: https://github.com/acme/widgets.git
                default_branch: main
                existing: false
                full_name: acme/widgets
                html_url: https://github.com/acme/widgets
              schema:
                $ref: '#/components/schemas/githubForkOut'
          description: ok
      summary: Forks a granted repository.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/installations:
    get:
      description: |-
        Lists the GitHub accounts the caller may see the App
        installed on, each confirmed against the App's own list, plus where to add
        another.

        The confirmation is the point. A connection row holds an installation id, and
        an id whose installation was since removed on GitHub is a row that mints
        nothing — every list and import against it fails with a token error, which
        reads as "our git integration is broken" rather than "that install is gone".
        Checking the App's view turns that into a fact the caller can act on.

        ORG-SCOPED for a tenant, deliberately. The App is installed across every
        customer, so the raw list is the customer list; a tenant sees only accounts its
        own org has bound. It discovers a NEW account by installing it (InstallURL),
        which is GitHub's own consent screen — not by reading ours.

        A SUPER ADMIN sees the App's whole install list, because that list is the
        platform's own inventory rather than any one tenant's data, and platform sudo
        is the single cross-tenant scope this house has. Without it an App installed
        out-of-band — granted straight from GitHub, so no connect flow ever ran and no
        connection row exists — is invisible to everyone: the console card reads "not
        connected" and an operator asked "which GitHub orgs do you see" can only
        answer for accounts already bound, which is precisely the accounts that were
        never the question.
      operationId: get_integrations_github_installations
      responses:
        "200":
          content:
            application/json:
              example:
                installUrl: https://github.com/apps/hanzo/installations/new
                installations:
                - connected: true
                  grant: all
                  htmlUrl: https://github.com/hanzoai
                  login: hanzoai
                  type: Organization
              schema:
                $ref: '#/components/schemas/githubInstallationsOut'
          description: ok
      summary: Lists the GitHub accounts the caller may see the App installed on,
        each confirmed against the App's own list, plus where to add another.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/issues/backfill:
    post:
      description: |-
        Seeds the native todo with the EXISTING issues across the
        org's granted repos (default state=open); the webhook keeps them live thereafter.
        Org-scoped by the validated principal — a caller only ever backfills its OWN org.
        Synchronous + bounded (a total time budget and an issue cap) so it returns the
        counts directly; idempotent by ExtRef, so a re-run continues where a truncated
        pass left off and never duplicates.
      operationId: post_integrations_github_issues_backfill
      requestBody:
        content:
          application/json:
            example:
              state: all
            schema:
              $ref: '#/components/schemas/githubBackfillIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                created: 410
                failed: 0
                issues: 430
                repos: 12
                updated: 20
              schema:
                $ref: '#/components/schemas/githubBackfillResult'
          description: ok
      summary: Seeds the native todo with the EXISTING issues across the org's granted
        repos (default state=open); the webhook keeps them live thereafter.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/repos:
    get:
      description: |-
        Lists the org's granted GitHub repositories, each annotated with its
        native import + sync status from the git object plane. Org-authed: the org comes
        from the validated principal, and the granted set is bounded to THAT org's
        installation token — an org can never enumerate another org's repos. The console
        polls it to watch an import flip a repo to imported.
      operationId: get_integrations_github_repos
      responses:
        "200":
          content:
            application/json:
              example:
                repos:
                - defaultBranch: main
                  fullName: acme/widgets
                  htmlUrl: https://github.com/acme/widgets
                  imported: true
                  lastSyncedAt: "2026-07-01T10:00:00Z"
                  name: widgets
                  private: true
                  syncStatus: synced
              schema:
                $ref: '#/components/schemas/githubReposOut'
          description: ok
      summary: Lists the org's granted GitHub repositories, each annotated with its
        native import + sync status from the git object plane.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/repos/{repo}/pages:
    delete:
      description: |-
        Deletes the repo's Pages site. 404 when there is none, so a
        caller can tell "turned it off" from "there was nothing on".
      operationId: delete_integrations_github_repos_by_repo_pages
      parameters:
      - description: |-
          Repo is the repository's short name within the org's installation, with no
          owner prefix (the owner is server-derived from the grant). A trailing ".git"
          is stripped.
        example: widgets
        in: path
        name: repo
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                disabled: true
                repo: widgets
              schema:
                $ref: '#/components/schemas/githubPagesDisabledOut'
          description: ok
      summary: Deletes the repo's Pages site.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns the repo's Pages status, live URL, custom domain and build
        source. The repo is resolved against the org installation's GRANTED set, so a
        caller can never address a repo the App was not granted; 404 when the repo has no
        Pages site.
      operationId: get_integrations_github_repos_by_repo_pages
      parameters:
      - description: |-
          Repo is the repository's short name within the org's installation, with no
          owner prefix (the owner is server-derived from the grant). A trailing ".git"
          is stripped.
        example: widgets
        in: path
        name: repo
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                buildType: legacy
                cname: docs.acme.com
                custom404: false
                httpsEnforced: true
                repo: widgets
                source:
                  branch: main
                  path: /docs
                status: built
                url: https://acme.github.io/widgets/
              schema:
                $ref: '#/components/schemas/githubPagesView'
          description: ok
      summary: Returns the repo's Pages status, live URL, custom domain and build
        source.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates the repo's Pages site and answers 201 Created with it.
        With buildType "workflow" the site builds via GitHub Actions; otherwise it builds
        from a branch source, defaulting to the repo's own default branch when none is
        given. Only "/" and "/docs" are legal source paths (GitHub's rule).
      operationId: post_integrations_github_repos_by_repo_pages
      parameters:
      - description: Repo is the repository, from the :repo path segment.
        example: widgets
        in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              branch: main
              path: /docs
              repo: widgets
            schema:
              $ref: '#/components/schemas/githubPagesEnableReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                buildType: legacy
                custom404: false
                httpsEnforced: true
                repo: widgets
                source:
                  branch: main
                  path: /docs
                status: building
                url: https://acme.github.io/widgets/
              schema:
                $ref: '#/components/schemas/githubPagesView'
          description: ok
      summary: Creates the repo's Pages site and answers 201 Created with it.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
    put:
      description: |-
        Sets or clears the custom domain (cname) and updates HTTPS
        enforcement, build type, or source. ONLY the provided fields are sent to GitHub,
        so an update never resets a setting the caller did not mention.
      operationId: put_integrations_github_repos_by_repo_pages
      parameters:
      - description: Repo is the repository, from the :repo path segment.
        example: widgets
        in: path
        name: repo
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              cname: docs.acme.com
              httpsEnforced: true
              repo: widgets
            schema:
              $ref: '#/components/schemas/githubPagesUpdateReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                repo: widgets
                updated: true
              schema:
                $ref: '#/components/schemas/githubPagesUpdatedOut'
          description: ok
      summary: Sets or clears the custom domain (cname) and updates HTTPS enforcement,
        build type, or source.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/repos/{repo}/pages/builds:
    post:
      description: |-
        Requests a Pages rebuild and returns the queued build's status.
        The build is queued AT GITHUB, not completed here, so the answer is 202 Accepted
        and its status is the one GitHub reported at queue time. 404 when the repository
        has no Pages site, or when the org's installation was not granted it.
      operationId: post_integrations_github_repos_by_repo_pages_builds
      parameters:
      - description: |-
          Repo is the repository's short name within the org's installation, with no
          owner prefix (the owner is server-derived from the grant). A trailing ".git"
          is stripped.
        example: widgets
        in: path
        name: repo
        required: true
        schema:
          type: string
      responses:
        "202":
          content:
            application/json:
              example:
                repo: widgets
                status: queued
                url: https://api.github.com/repos/acme/widgets/pages/builds/1
              schema:
                $ref: '#/components/schemas/githubPagesBuildOut'
          description: accepted
      summary: Requests a Pages rebuild and returns the queued build's status.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/repos/import:
    post:
      description: |-
        Imports the selected (or all) granted repos into git.hanzo.ai. The
        selection is intersected with the installation's GRANTED set, so a client can
        never import a repo the App was not granted (org isolation + a grant check). The
        import runs in a bounded background worker (don't block the request), so the
        answer is 202 Accepted; poll GET /v1/integrations/github/repos for the per-repo
        status to flip to imported.
      operationId: post_integrations_github_repos_import
      requestBody:
        content:
          application/json:
            example:
              repos:
              - widgets
            schema:
              $ref: '#/components/schemas/githubImportIn'
        required: true
      responses:
        "202":
          content:
            application/json:
              example:
                queued: 1
                repos:
                - widgets
              schema:
                $ref: '#/components/schemas/githubImportOut'
          description: accepted
      summary: Imports the selected (or all) granted repos into git.hanzo.ai.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/search:
    post:
      description: |-
        Finds repositories on GitHub.

        This reads the PUBLIC index and returns nothing an installation unlocks: it is
        how you find a repository to fork, not a way to see inside one. The org's own
        token is used only so the query is rate-limited against the installation
        rather than anonymously — the results are the same ones anyone would get.
      operationId: post_integrations_github_search
      requestBody:
        content:
          application/json:
            example:
              limit: 5
              q: language:go raft
            schema:
              $ref: '#/components/schemas/githubSearchReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                count: 1
                repos:
                - full_name: hashicorp/raft
                  language: Go
                  stars: 8000
              schema:
                $ref: '#/components/schemas/githubSearchOut'
          description: ok
      summary: Finds repositories on GitHub.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/github/webhook:
    post:
      description: |-
        The address the GitHub App delivers events to. A push is handed to the repository sync engine, and an issue or issue-comment event is mirrored into the native todo — idempotently, so the same issue re-syncs to one row however many times it is edited, closed or reopened.

        It answers a benign 200 for everything it does not act on — the ping, other event types, an unknown installation — deliberately, so GitHub does not enter a retry storm over events that were never going to do anything. Only a bad signature and a genuine sync failure are non-200, and an oversized payload is refused outright.

        Two sync rules are worth stating because neither is guessable. EVERY ref syncs, tags as well as branches, because releases are cut by tag and filtering them would stop publishing with nothing reporting a failure. And a delete is NEVER propagated: the native side is canonical, so an inbound delete never removes a native ref.

        The payload is verified by HMAC against the webhook secret before it is parsed.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.
      operationId: post_integrations_github_webhook
      summary: GitHub App webhook
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/gitlab/projects:
    get:
      description: |-
        Lists the projects the org's GitLab connection can reach —
        membership projects, most recently active first.
      operationId: get_integrations_gitlab_projects
      responses:
        "200":
          content:
            application/json:
              example:
                account: acme
                projects:
                - cloneUrl: https://gitlab.com/acme/widgets.git
                  defaultBranch: main
                  fullName: acme/widgets
                  htmlUrl: https://gitlab.com/acme/widgets
                  name: widgets
                  private: true
                  pushedAt: "2026-07-01T10:00:00Z"
              schema:
                $ref: '#/components/schemas/gitlabProjectsOut'
          description: ok
      summary: Lists the projects the org's GitLab connection can reach — membership
        projects, most recently active first.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/openrouter/webhook:
    post:
      description: |-
        OpenRouter's spend is invisible to every Hanzo money lens because those lenses read hanzo.cloud_usage and OpenRouter meters keys of its own. Point a Broadcast destination (Settings ▸ Observability ▸ Webhook) at this door and each generation span becomes ONE row in that same ledger with provider `openrouter`, so one query answers what we spend everywhere. Enable the Cost and Identity field categories: cost is the money and identity carries `openrouter.api_key_name`, which is what says WHICH key spent it — it lands in `account` as openrouter/<key name>.

        AUTHENTICATION IS A HANZO KEY. Broadcast signs nothing; its only authentication is the destination's Headers map, so send a key as `Authorization: Bearer pk-…` and it is admitted exactly as /v1/event admits a beacon's: a project key resolves through the project that minted it, an IAM-issued key through IAM. That key names the org every row is filed under; it can write and cannot read. No key, or a key that names no org, is 401 and nothing is stored.

        The body is OTLP/JSON — `{resourceSpans:[{scopeSpans:[{spans:[…]}]}]}` — exactly as OpenTelemetry defines it; the model, tokens and cost are read from each span's `gen_ai.*` attributes and the key name from `openrouter.api_key_name`. The answer is `{stored, dropped}`: how many generations became rows, and how many spans named no model. Those are OpenRouter's trace and span parents — they carry no cost to meter. An empty payload stores nothing and answers 200, which is what makes Test Connection pass. A warehouse that cannot take the rows answers 503 so the delivery shows red and can be replayed: a row is keyed by its span id, so a redelivery collapses rather than double-counting.
      operationId: post_integrations_openrouter_webhook
      requestBody:
        content:
          application/json:
            schema:
              additionalProperties: {}
              type: object
      responses:
        2XX:
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: Success
      summary: Receive OpenRouter Broadcast traces as usage rows
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/commands:
    post:
      description: |-
        The address Slack posts a slash command to, form-encoded. It acknowledges inside Slack's three-second budget and posts the answer afterwards to the command's own response URL, which is why the immediate reply is empty.

        The body is verified against the same app signing secret as the events webhook, and a repeat of the same command invocation is absorbed rather than answered twice.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.

        The answer is acknowledged immediately and the work happens afterwards, because every one of these platforms times out a slow webhook. Duplicate deliveries are absorbed durably, so a platform retry of an event that already ran never runs it a second time or bills for it twice. When the agent pool is full nothing at all is recorded and the delivery is refused as retriable, so the message is re-delivered later rather than being lost or half-processed.
      operationId: post_integrations_slack_commands
      summary: Slack slash command webhook
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/events:
    post:
      description: |-
        The address a Slack app posts workspace events to. It answers Slack's url_verification handshake with the challenge, and routes an @mention or a direct message to an agent turn that replies in the same thread. The turn holds the product's own tools, so a request to change code starts a sandbox run because the model chose to — there is no prefix and no second path.

        The raw body and its timestamp are verified against the app's signing secret before anything is read from them. Hanzo's own bot messages are dropped, so a reply cannot trigger another reply.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.

        The answer is acknowledged immediately and the work happens afterwards, because every one of these platforms times out a slow webhook. Duplicate deliveries are absorbed durably, so a platform retry of an event that already ran never runs it a second time or bills for it twice. When the agent pool is full nothing at all is recorded and the delivery is refused as retriable, so the message is re-delivered later rather than being lost or half-processed.
      operationId: post_integrations_slack_events
      summary: Slack Events API webhook
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/install:
    get:
      description: |-
        The address behind Slack's "Add to Slack" and Marketplace Install buttons. It answers a 302 to Slack's own consent screen and does nothing else — it is a redirector by design.

        It exists because Slack refuses a slack.com URL in that field and requires one of ours that redirects there, which makes the field an ATTRIBUTION hook: routing the click through our own address is what lets an install be counted, and always answering the redirect is what keeps the counter from becoming a detour that never reaches consent. The destination is the same consent URL every time, built from the same scopes the console's Connect button asks for, so a workspace is asked to grant one thing however the install began.

        It is PUBLIC and carries no principal, because whoever clicks Install in Slack's directory has no Hanzo session yet. It binds no org either, and that is deliberate rather than missing: the org is resolved at the shared provider callback, from the signed state a console connect minted or from the workspace's existing connection. Minting an org for an anonymous click is the one thing that would break tenant isolation, so an install begun here finishes under exactly the rules every other install obeys.

        Where the app is not configured it answers 503, rather than a consent URL carrying an empty client_id that Slack would render as its own dead-end error page.
      operationId: get_integrations_slack_install
      summary: Install the Hanzo app into a Slack workspace
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/link:
    get:
      description: |-
        The entry point behind the connect prompt Hanzo posts in Slack. It starts a link session in the browser and redirects to Slack's own sign-in, which is what proves which Slack user is asking.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_slack_link
      summary: Begin linking a Hanzo account from Slack
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/link/callback:
    get:
      description: |-
        The final leg: the user has proved both who they are in Slack and who they are in Hanzo, and this binds the two. It answers a short confirmation page telling them to return to Slack.

        The Hanzo credential obtained here is sealed into the connected workspace's own KMS namespace; it is never written to a database column and never logged. A deployment whose secret store is unavailable refuses the link rather than completing it without custody of the credential.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_slack_link_callback
      summary: Complete the Slack account link
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/slack/link/slack:
    get:
      description: |-
        Where Slack returns the user after they sign in. It establishes the verified Slack workspace and user, confirms that workspace is connected to an org, and hands the browser on to the Hanzo sign-in that completes the link.

        The verified pair is carried onward in a host-bound cookie rather than in the URL, so the identity being linked cannot be edited in transit.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_slack_link_slack
      summary: Slack sign-in return leg
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/teams/events:
    post:
      description: |-
        The messaging endpoint for the Teams bot. A message activity is routed to an agent turn and answered proactively through the Bot Connection; anything that is not a message with text is acknowledged and ignored.

        Authentication is the Bot Framework's RS256 JWT, verified against its published keys and bound BOTH to this deployment's app id and to the activity's own service URL. The service-URL binding is the part that matters: without it a token valid for one activity could point the outbound reply somewhere else.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.

        The answer is acknowledged immediately and the work happens afterwards, because every one of these platforms times out a slow webhook. Duplicate deliveries are absorbed durably, so a platform retry of an event that already ran never runs it a second time or bills for it twice. When the agent pool is full nothing at all is recorded and the delivery is refused as retriable, so the message is re-delivered later rather than being lost or half-processed.
      operationId: post_integrations_teams_events
      summary: Microsoft Teams Bot Framework webhook
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/teams/link:
    get:
      description: |-
        The entry point behind the connect prompt Hanzo shows in Teams. It starts a link session and redirects to Microsoft sign-in addressed to the CHAT'S OWN tenant, not the common endpoint, so only a member of that tenant can complete it.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_teams_link
      summary: Begin linking a Hanzo account from Teams
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/teams/link/aad:
    get:
      description: |-
        Where Microsoft returns the user after sign-in. It resolves the verified directory identity and then re-checks the tenant: the signed-in user's tenant must equal the tenant of the chat the link started from, so a valid Microsoft sign-in from a different organization is refused here rather than accepted.

        This is the leg Teams has and the other platforms do not, which is why the Teams flow has an extra address.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_teams_link_aad
      summary: Microsoft sign-in return leg
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/teams/link/callback:
    get:
      description: |-
        The final leg: it binds the verified directory identity to the Hanzo account that just signed in, and answers a short confirmation page telling them to return to Teams. The Hanzo credential is sealed into the connected org's KMS namespace.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_teams_link_callback
      summary: Complete the Teams account link
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/telegram/connect:
    post:
      description: |-
        Mints a short, single-use deep-link code bound to the caller's
        org and returns the t.me link the console navigates to. Org-authed: a caller with
        no validated principal is 403 (same gate as the framework connect). The code is
        stored as an oauth_nonce (org,telegram); the webhook's /start handler claims it to
        bind chat→org. It is short (128-bit hex) so it fits Telegram's 64-char `start`
        payload limit.
      operationId: post_integrations_telegram_connect
      responses:
        "200":
          content:
            application/json:
              example:
                authorizeUrl: https://t.me/hanzo_bot?start=9f3c1d2e4b5a6c7d8e9f0a1b2c3d4e5f
              schema:
                $ref: '#/components/schemas/authorizeOut'
          description: ok
      summary: Mints a short, single-use deep-link code bound to the caller's org
        and returns the t.me link the console navigates to.
      tags:
      - integrations
      x-app: integrations
      x-public: true
      x-tool: true
  /v1/integrations/telegram/link:
    get:
      description: |-
        The entry point behind the connect prompt Hanzo sends in Telegram. Unlike the other platforms it answers an HTML PAGE rather than a redirect: Telegram has no OAuth flow, so the page hosts Telegram's Login Widget, and the browser is sent onward only after the user signs in through it.

        The widget only appears on the domain registered for the bot, so a deployment whose bot domain is unset renders a page with nothing on it.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_telegram_link
      summary: Begin linking a Hanzo account from Telegram
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/telegram/link/auth:
    get:
      description: |-
        Where Telegram's Login Widget sends the user with its signed authentication data. That data is verified against the bot token — this is the identity source, and it is the widget's signature rather than a code exchange — and the chat is confirmed to be bound to an org before the browser is handed to the Hanzo sign-in.

        Widget data is only accepted while it is fresh, so a captured sign-in blob cannot be replayed later even though its signature stays valid.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_telegram_link_auth
      summary: Telegram Login Widget return leg
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/telegram/link/callback:
    get:
      description: |-
        The final leg: it binds the verified Telegram user to the Hanzo account that just signed in, and answers a short confirmation page telling them to return to Telegram. The Hanzo credential is sealed into the connected org's KMS namespace.

        This is one leg of a three-leg flow, and the legs are not interchangeable: a browser is expected to arrive here only from the leg before it. The link URL's state proves the prompt was server-minted and carries the CHAT it started from — it is provenance only, and it never decides which account gets linked. The account identity always comes from the platform's own verified sign-in and a host-bound cookie, so forwarding a link to someone else cannot bind their account, and a session lifted into another browser is refused rather than completed. Each link is single-use, and a deployment without linking configured answers 503.
      operationId: get_integrations_telegram_link_callback
      summary: Complete the Telegram account link
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/integrations/telegram/webhook:
    post:
      description: |-
        The update webhook for the Telegram bot. It does two jobs: `/start <code>` or `/connect <code>` binds the chat it was sent from to an org, idempotently; anything else is treated as a possible agent trigger.

        What counts as a trigger differs by chat type, and it is easy to get wrong: in a private chat every message is a trigger, while in a group the message must mention the bot or use the `/hanzo` command. Non-triggers and non-message updates are acknowledged and dropped.

        Authentication is the secret token Telegram echoes on every update, compared in constant time. A message in a chat that has never been bound is dropped, which is why the bind command exists.

        The caller here is the PLATFORM, not a Hanzo tenant, so there is no bearer and no principal. The signature check IS the authentication, and it fails closed. The tenant is never read from the payload either: it is resolved from the verified platform identifier through the connection map, so an event from a workspace nobody connected does nothing. Refusals are written with their own status rather than being flattened to a 500, so a rejected signature reads as 401 and a malformed body as 400.

        The answer is acknowledged immediately and the work happens afterwards, because every one of these platforms times out a slow webhook. Duplicate deliveries are absorbed durably, so a platform retry of an event that already ran never runs it a second time or bills for it twice. When the agent pool is full nothing at all is recorded and the delivery is refused as retriable, so the message is re-delivered later rather than being lost or half-processed.
      operationId: post_integrations_telegram_webhook
      summary: Telegram Bot API webhook
      tags:
      - integrations
      x-app: integrations
      x-public: true
  /v1/kms/auth/login:
    post:
      description: |-
        Takes a tenant's machine credential — a client id and client secret — and returns an owner-scoped IAM access token with its lifetime, which is the bearer the caller then carries on the org-scoped secret operations.

        It is deliberately public and unauthenticated, because it IS the credential exchange and runs before any principal exists. That makes it the one route in this subsystem rate-limited PER SOURCE IP, keyed on the real TCP peer rather than on any caller-supplied header.

        The submitted secret is never logged and never echoed, and failures collapse to one clean status with no upstream detail: 401 when the credential does not authenticate, 502 when the identity provider is unreachable, 503 when no issuer is configured. That is on purpose — a richer error would be a validity oracle for guessed credentials.
      operationId: post_kms_auth_login
      summary: Exchange a machine credential for an IAM bearer token
      tags:
      - kms
      x-app: kms
      x-public: true
  /v1/kms/config:
    get:
      description: |-
        Returns what the console needs before anyone has signed in: the brand, the OIDC issuer it authenticates against, the API base for this subsystem and the path of the login exchange.

        Public on purpose, and it holds nothing sensitive — it is deliberately kept under this subsystem's own namespace rather than under an admin prefix, so a gateway that admin-gates the admin routes cannot break the console's legitimate pre-login fetch.
      operationId: get_kms_config
      summary: Runtime configuration for the KMS console
      tags:
      - kms
      x-app: kms
      x-public: true
  /v1/kms/health:
    get:
      description: |-
        A real readiness probe, not a liveness stub: 200 only when the store is open AND a master key is configured, with `signing` reporting whether signing keys are set up too. Anything less answers 503 with `ready:false` and the reason — no in-process store, or no master key — which are exactly the two states in which the secret operations refuse.

        Not token-gated, because the platform must be able to probe it without a credential. It reports the broker's configuration state only; no secret, no key material and no tenant name appears in it.
      operationId: get_kms_health
      summary: Whether this broker can actually serve secrets
      tags:
      - kms
      x-app: kms
      x-public: true
  /v1/kms/secrets:
    get:
      description: |-
        Returns the METADATA of the caller's own secrets: each one's name, path, environment and sealing scheme. No value and no ciphertext is included — this operation exists to enumerate what is held, and reading a value is a separate, per-secret call.

        Scoped to the caller's own org and nothing else, structurally: there is no org in the path, the store root is derived from the validated org claim, and a caller therefore has no way to name another tenant's namespace. `path` narrows to a subpath and `env` selects the environment; both are also accepted under the operator's spellings, `secretPath` and `environment`.

        Admission is fail-closed and in order: a validated member, an org that is a DNS-1123 label, and a store holding a master key — 403, 400 and 503 respectively, all decided before any record is touched.
      operationId: get_kms_secrets
      summary: List the secrets your org holds, without their values
      tags:
      - kms
      x-app: kms
      x-public: true
    post:
      description: |-
        Upserts one secret under the caller's own org. The value is sealed before it is written — a fresh per-secret data key, itself wrapped by the master key — so plaintext never reaches disk. The receipt confirms the name and environment that were written and does not echo the value.

        `env` is REQUIRED on a write and has no default, which is the rule most easily got wrong here: reads and deletes still fall back to the default environment for older callers, but a write must not, because the environment is part of the storage key. A silently defaulted write lands in a bucket the readers that resolve project, environment and path never look in, and the stale value keeps being served — so the write fails loudly instead.

        `name` is required, `path` is an optional subpath beneath the org root, and the org is taken from the validated claim rather than the body.

        Requires ADMIN authority over the org — a member reads, an admin writes. A machine credential holds no membership and so is never an org admin: it can read the secrets it was issued for and cannot replace one. Fail-closed admission, in order: admin of the org, well-formed org, master key present — 403, 400 and 503, all decided before any record is touched.
      operationId: post_kms_secrets
      summary: Store or replace one secret in your org
      tags:
      - kms
      x-app: kms
      x-public: true
  /v1/knowledge/connectors:
    get:
      description: |-
        Returns every supported knowledge connector with THIS org's
        connection state and the REAL number of documents each has ingested into the
        org's store. A provider that is configured for the deployment but not yet
        connected appears as disconnected, so the console can offer a Connect button.
        No secret is ever returned.
      operationId: get_knowledge_connectors
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kbConnectorsOut'
          description: ok
      summary: Returns every supported knowledge connector with THIS org's connection
        state and the REAL number of documents each has ingested into the org's store.
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/connectors/{provider}:
    delete:
      description: |-
        Revokes a connection: it tombstones the stored credential
        so a later sync cannot reuse it, purges this provider's points from the org's
        vector namespace, and marks the connector disconnected. The documents already
        ingested stay in the org's store — they are the org's own data — but stop being
        retrievable by search; a caller deletes them through the document surface.
      operationId: delete_knowledge_connectors_by_provider
      parameters:
      - description: 'Provider is the connector to act on: github, slack, google or
          notion.'
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connectionOut'
          description: ok
      summary: 'Revokes a connection: it tombstones the stored credential so a later
        sync cannot reuse it, purges this provider''s points from the org''s vector
        namespace, and marks the connector disconnected.'
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/connectors/{provider}/callback:
    get:
      description: |-
        CompleteConnectorOAuth finishes an OAuth connection: it exchanges the
        provider's code for a token, seals that token in KMS, and records the
        connection. THE ORG COMES FROM THE SIGNED STATE, not from a header and not from
        the provider, so an attacker cannot bind their own account to someone else's
        org — a tampered, expired or foreign-provider state is refused outright. The
        token itself is never returned, never written into the document, and never
        logged; the document holds only its KMS path.
      operationId: get_knowledge_connectors_by_provider_callback
      parameters:
      - description: Provider is the connector completing its flow, from the path.
        in: path
        name: provider
        required: true
        schema:
          type: string
      - description: Code is the provider's authorization code, exchanged for a token.
        in: query
        name: code
        required: false
        schema:
          type: string
      - description: State is the org-bound value this server signed at connect time.
        in: query
        name: state
        required: false
        schema:
          type: string
      - description: Error is the provider's denial reason when the user refused consent.
        in: query
        name: error
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/connectionOut'
          description: ok
      summary: 'CompleteConnectorOAuth finishes an OAuth connection: it exchanges
        the provider''s code for a token, seals that token in KMS, and records the
        connection.'
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/connectors/{provider}/connect:
    get:
      description: |-
        StartConnectorOAuth returns the provider authorize URL the console opens to
        connect this org's account. There is no server-side redirect — the console
        stays in control of the navigation. The URL carries a state this server SIGNED
        over the caller's validated org, so the connection the callback completes can
        only ever land in that org.
      operationId: get_knowledge_connectors_by_provider_connect
      parameters:
      - description: 'Provider is the connector to act on: github, slack, google or
          notion.'
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kbAuthorizeOut'
          description: ok
      summary: StartConnectorOAuth returns the provider authorize URL the console
        opens to connect this org's account.
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/connectors/{provider}/sync:
    post:
      description: |-
        Pulls the provider's documents for the caller's org and files
        them as knowledge sources, which the store's own hook then indexes — so a
        synced document is retrievable exactly like a hand-written page. The org is the
        validated tenant and the credential is read from KMS, so an org can only ever
        sync its own connection. A provider failure is reported honestly (502) and
        recorded on the connector rather than silently swallowed.
      operationId: post_knowledge_connectors_by_provider_sync
      parameters:
      - description: 'Provider is the connector to act on: github, slack, google or
          notion.'
        in: path
        name: provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kbSyncOut'
          description: ok
      summary: Pulls the provider's documents for the caller's org and files them
        as knowledge sources, which the store's own hook then indexes — so a synced
        document is retrievable exactly like a hand-written page.
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/connectors/catalog:
    get:
      description: |-
        Returns the ONE catalog of everything a caller can
        connect: every first-party connector and every long-tail one, in a single list
        sorted by provider. `configured` reports whether this deployment holds OAuth
        credentials for a source, so the console can show Connect rather than a dead
        button, and `kind` is a badge only — the connect and sync lifecycle is
        identical for both. The catalog itself is org-independent; a validated
        principal is still required. It is metadata only: no secret is ever returned.
      operationId: get_knowledge_connectors_catalog
      responses:
        "200":
          content:
            application/json:
              example:
                connectors:
                - configured: true
                  description: Repositories, READMEs, and issues.
                  displayName: GitHub
                  kind: native
                  provider: github
              schema:
                $ref: '#/components/schemas/catalogOut'
          description: ok
      summary: 'Returns the ONE catalog of everything a caller can connect: every
        first-party connector and every long-tail one, in a single list sorted by
        provider.'
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/graph:
    get:
      description: |-
        Returns the caller org's knowledge as a node/edge graph
        shaped for a force-directed renderer: pages, memories and synced sources as
        nodes; the page parent tree, the wikilinks between pages, and each source's
        connector provenance as edges. Wikilink targets are resolved HERE by title or
        slug, so a rename never needs an edge rewrite and a link that matches no page
        renders as its own "unresolved" node instead of vanishing. ?project= narrows
        it. A store outage degrades to an honest empty graph, never a 5xx.
      operationId: get_knowledge_graph
      parameters:
      - description: Project narrows the graph to one project scope. Empty reads the
          whole org.
        in: query
        name: project
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/graphOut'
          description: ok
      summary: 'Returns the caller org''s knowledge as a node/edge graph shaped for
        a force-directed renderer: pages, memories and synced sources as nodes; the
        page parent tree, the wikilinks between pages, and each source''s connector
        provenance as edges.'
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/knowledge/import:
    post:
      description: |-
        Ingests an uploaded export as a tree of kb-page documents with its link structure intact. `?format=` picks the normalizer — obsidian, notion, roam or evernote — and the export arrives as a multipart `file` part, or as the raw request body when there is no multipart part: an Obsidian or Notion vault zip, a Roam JSON (raw or inside the zip Roam downloads), or an Evernote .enex.

        The pages are filed through the SAME ingest path a connector sync uses, so the kb-page hook indexes each one for retrieval AND extracts its `[[wikilinks]]` into kb-link edges — the imported vault is searchable and its graph is navigable without a second pass. Parents are filed before their children, and each page takes a slug unique within the org (suffixed -2, -3, … on collision), so a re-import adds pages rather than overwriting the ones already there.

        Scoped to the caller's validated org; `?project=` narrows every imported page to one project. No validated principal is 403, and an org that has not installed the kb module is refused with the install call to make first. The bounds are 64 MB per upload, 5000 pages and 8 MB per archive entry: pages past the five-thousandth are dropped and a larger entry is truncated at its bound, and a page the store rejects is skipped — so the answer's `imported` count is what was actually filed, not what was sent.
      operationId: post_knowledge_import
      summary: Import an Obsidian, Notion, Roam or Evernote export into the org's
        knowledge base
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
  /v1/knowledge/search:
    post:
      description: |-
        Runs a semantic search over the caller org's own knowledge —
        its wiki pages, its agent memories and everything its connectors have synced —
        and returns the matching passages. This is the RAG entry point: an agent asks
        "what does this org know about X" and the org's OWN vector namespace answers.
        The org comes from the validated principal, and both the collection and the
        payload filter are pinned to it, so cross-tenant retrieval is impossible. An
        unreachable index returns an honest empty result set with degraded=true, never
        a 5xx.
      operationId: post_knowledge_search
      requestBody:
        content:
          application/json:
            example:
              limit: 5
              query: how do we rotate the signing key
            schema:
              $ref: '#/components/schemas/searchIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/searchOut'
          description: ok
      summary: Runs a semantic search over the caller org's own knowledge — its wiki
        pages, its agent memories and everything its connectors have synced — and
        returns the matching passages.
      tags:
      - knowledge
      x-app: knowledge
      x-public: true
      x-tool: true
  /v1/leaderboard:
    get:
      description: |-
        Leaderboard ranks AI usage over a window, either the users of the caller's own org
        or organizations against each other, and always reports the caller's own standing
        even when it falls outside the returned page. Identities are private by default: a
        caller sees themselves, plus the peers or orgs that opted into public listing, and
        only an admin sees their own org's members named. Cross-org spend is restricted to
        platform admins. When the warehouse is not connected the board answers empty with
        available=false rather than a fabricated rank.
      operationId: get_leaderboard
      parameters:
      - description: |-
          Scope picks the board: "personal" (default) ranks the caller among their own
          org's users, "org" is that same org board named for an admin, "global" ranks
          organizations against each other.
        example: personal
        in: query
        name: scope
        required: false
        schema:
          type: string
      - description: 'Metric is the value ranked: tokens (default), requests, or cost.'
        example: tokens
        in: query
        name: metric
        required: false
        schema:
          type: string
      - description: 'Period is the window ranked: day, week, month (default) or all.'
        example: week
        in: query
        name: period
        required: false
        schema:
          type: string
      - description: |-
          Limit caps the rows returned, clamped to 100. Defaults to 10, which is also
          what a non-positive or unparseable value takes.
        example: 10
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LeaderboardView'
          description: ok
      summary: Leaderboard ranks AI usage over a window, either the users of the caller's
        own org or organizations against each other, and always reports the caller's
        own standing even when it falls outside the returned page.
      tags:
      - leaderboard
      x-app: leaderboard
      x-public: true
      x-tool: true
  /v1/leaderboard/activity:
    get:
      description: |-
        Activity returns the per-day usage series for ONE authorized subject — the points a
        contribution heatmap and a timeline are drawn from, gap-filled so every day in the
        range is present. Authorization is resolved server-side from the validated
        principal, so a caller can never widen the subject past what they are entitled to:
        a non-admin reads only themselves and their own org. subject=project answers empty
        with a note, because the usage ledger records no project column yet. When the
        warehouse is not connected the series answers empty with available=false rather
        than fabricated days.
      operationId: get_leaderboard_activity
      parameters:
      - description: 'Subject is what the series is about: "user" (default), "org"
          or "project".'
        example: user
        in: query
        name: subject
        required: false
        schema:
          type: string
      - description: |-
          ID names the subject within what the caller is entitled to see. Omitted (or
          "me") it is the caller themselves, or their own org. Another user requires org
          admin and must belong to the caller's org; another org requires a SuperAdmin.
        in: query
        name: id
        required: false
        schema:
          type: string
      - description: From is the first day of the range, "2006-01-02". Defaults to
          90 days back.
        example: "2026-01-01"
        in: query
        name: from
        required: false
        schema:
          type: string
      - description: |-
          To is the last day of the range, "2006-01-02". Defaults to today; the span is
          clamped to 366 days.
        example: "2026-03-31"
        in: query
        name: to
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ActivityView'
          description: ok
      summary: Activity returns the per-day usage series for ONE authorized subject
        — the points a contribution heatmap and a timeline are drawn from, gap-filled
        so every day in the range is present.
      tags:
      - leaderboard
      x-app: leaderboard
      x-public: true
      x-tool: true
  /v1/leaderboard/optin:
    get:
      description: |-
        Returns the caller's own public-listing preference and their org's,
        each with whether the caller may change it. Public listing is opt-in and private
        by default, so a fresh caller reads listed=false for both.
      operationId: get_leaderboard_optin
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/optinView'
          description: ok
      summary: Returns the caller's own public-listing preference and their org's,
        each with whether the caller may change it.
      tags:
      - leaderboard
      x-app: leaderboard
      x-public: true
      x-tool: true
    put:
      description: |-
        Sets the CALLER's own public-listing preference on the leaderboard.
        Self only: the row written is keyed by the caller's validated ledger identity, so
        this can never edit another member's visibility whatever the request says. A
        caller opting in with no handle is given their username, so a listed row never
        renders as "Anonymous" to its own owner.
      operationId: put_leaderboard_optin
      requestBody:
        content:
          application/json:
            example:
              handle: ada
              listed: true
            schema:
              $ref: '#/components/schemas/userOptinReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/userOptinView'
          description: ok
      summary: Sets the CALLER's own public-listing preference on the leaderboard.
      tags:
      - leaderboard
      x-app: leaderboard
      x-public: true
      x-tool: true
  /v1/leaderboard/optin/org:
    put:
      description: |-
        Sets the ORG's listing on the cross-org global board. Only an admin of
        the caller's own org — an org admin or a platform SuperAdmin — may change it, and
        the org written is the caller's validated tenant, never a value from the request.
        Listing consents to publishing the org's usage VOLUME; cross-org spend stays
        restricted to platform admins regardless.
      operationId: put_leaderboard_optin_org
      requestBody:
        content:
          application/json:
            example:
              display: Acme
              listed: true
            schema:
              $ref: '#/components/schemas/orgOptinReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/orgOptinView'
          description: ok
      summary: Sets the ORG's listing on the cross-org global board.
      tags:
      - leaderboard
      x-app: leaderboard
      x-public: true
      x-tool: true
  /v1/logs/health:
    get:
      description: |-
        Reports the native log store's live state for the calling tenant: the subsystem version and `records`, the count actually held right now rather than a constant. Not a dependency probe — the store is in-process, so this answers 200 whenever the process is up.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`.
      operationId: get_logs_health
      summary: How many log records this deployment holds for your org
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/logs/query:
    get:
      description: |-
        Answers `{count, records}`, newest first. `match` is the same `k=v,k2=v2` superset label matcher the metrics query uses; `contains` is a case-insensitive substring test against the record body; `start` and `end` are nanosecond bounds.

        A bound that is absent, empty or unparseable becomes 0, which means UNBOUNDED — a malformed `start` widens the search rather than failing it. `limit` caps the page and defaults to 100 when absent or non-positive, so an unfiltered read is never the whole ring.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`, so a search can only reach the org the edge asserted.
      operationId: get_logs_query
      summary: Search your org's logs by label, time and substring
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/logs/write:
    post:
      description: |-
        Takes `{records:[{t, level, body, labels}]}`, appends each one, and answers `{written}`. Bodies are stored verbatim; `labels` are the indexed dimensions a query filters on, so what you do not label you can only find by substring.

        `t` is NANOSECONDS since the Unix epoch. A record sent without one is stored at 0 and then falls outside any query carrying a lower bound — the usual reason a successful write does not read back. Retention is a bounded ring, 1048576 records per org, oldest evicted first. No record is validated or rejected, so `written` is the number of records SENT; only a body that does not decode at all is 400.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`; each org's records live in its own WAL-durable store.
      operationId: post_logs_write
      summary: Append structured log records for your org
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/lsp/complete:
    post:
      description: |-
        Offers the candidates a language server has at a position, typed and
        resolved through the repository's dependencies rather than guessed from text.
      operationId: post_lsp_complete
      requestBody:
        content:
          application/json:
            example:
              character: 18
              line: 120
              path: apps/lsp/lsp.go
              repo: cloud
            schema:
              $ref: '#/components/schemas/Query'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Answer'
          description: ok
      summary: Offers the candidates a language server has at a position, typed and
        resolved through the repository's dependencies rather than guessed from text.
      tags:
      - lsp
      x-app: lsp
      x-public: true
      x-tool: true
  /v1/lsp/diagnostics:
    post:
      description: |-
        Reports every problem the language server finds in one file —
        compile errors, type errors and lints, each with its span and its severity (1
        error, 2 warning, 3 information, 4 hint). The position is ignored.
      operationId: post_lsp_diagnostics
      requestBody:
        content:
          application/json:
            example:
              path: apps/lsp/lsp.go
              repo: cloud
            schema:
              $ref: '#/components/schemas/Query'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Answer'
          description: ok
      summary: Reports every problem the language server finds in one file — compile
        errors, type errors and lints, each with its span and its severity (1 error,
        2 warning, 3 information, 4 hint).
      tags:
      - lsp
      x-app: lsp
      x-public: true
      x-tool: true
  /v1/lsp/hover:
    post:
      description: |-
        Renders the type and documentation of the symbol at a position, as the
        language server itself renders it.

        Positions are the LSP's: line and character are 0-BASED and character counts
        UTF-16 code units, so an editor's 1-based line must have 1 subtracted before it
        is sent. The repository is named by slug and is always one in the caller's own
        org; rev pins a branch, tag or commit sha, and empty means the default branch.
      operationId: post_lsp_hover
      requestBody:
        content:
          application/json:
            example:
              character: 18
              line: 120
              path: apps/lsp/lsp.go
              repo: cloud
            schema:
              $ref: '#/components/schemas/Query'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Answer'
          description: ok
      summary: Renders the type and documentation of the symbol at a position, as
        the language server itself renders it.
      tags:
      - lsp
      x-app: lsp
      x-public: true
      x-tool: true
  /v1/lsp/locate:
    post:
      description: |-
        Finds where a symbol lives: its definition, its references, its type or
        its implementations, chosen by relation (definition, reference, type,
        implementation — empty means definition).

        It resolves THROUGH dependencies. An answer whose external flag is set left the
        repository, and its path is then the module coordinate it landed in — which is
        the question a static index cannot answer and this service exists for.
      operationId: post_lsp_locate
      requestBody:
        content:
          application/json:
            example:
              character: 18
              line: 120
              path: apps/lsp/lsp.go
              relation: definition
              repo: cloud
            schema:
              $ref: '#/components/schemas/Query'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Answer'
          description: ok
      summary: 'Finds where a symbol lives: its definition, its references, its type
        or its implementations, chosen by relation (definition, reference, type, implementation
        — empty means definition).'
      tags:
      - lsp
      x-app: lsp
      x-public: true
      x-tool: true
  /v1/lsp/symbols:
    post:
      description: |-
        Outlines one file: every declaration in it, with its kind and its span.
        The position is ignored — the answer is the whole file.
      operationId: post_lsp_symbols
      requestBody:
        content:
          application/json:
            example:
              path: apps/lsp/lsp.go
              repo: cloud
            schema:
              $ref: '#/components/schemas/Query'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Answer'
          description: ok
      summary: 'Outlines one file: every declaration in it, with its kind and its
        span.'
      tags:
      - lsp
      x-app: lsp
      x-public: true
      x-tool: true
  /v1/mcp:
    post:
      description: 'Model Context Protocol over JSON-RPC 2.0 — one POST per message,
        stateless, protocol revision 2026-07-28. tools/list answers without a credential
        with one tool per subsystem (its operations in the "op" enum) plus "describe",
        which returns one operation''s input schema. tools/call names a subsystem
        tool and carries {"op": <operation>, "input": <its arguments>}; it takes the
        same bearer the REST API does, and a call that carries none is answered 401
        with a WWW-Authenticate header naming the resource metadata at /.well-known/oauth-protected-resource,
        which names the authorization server to sign in at. The tool surface is the
        public contract: the operator''s admin product is not offered, and a name
        that would disclose a secret or mutate an identity is withheld — the list
        says how many, under _meta.'
      operationId: post_mcp
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MCPRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPResponse'
          description: Success
      summary: 'The agent door: every subsystem''s operations as MCP tools'
      tags:
      - openapi
      x-app: openapi
      x-public: true
  /v1/mesh/services:
    get:
      description: |-
        Returns the Zero Trust edge services the caller's org owns.

        One row per real ZT edge service tagged with the org's "org-<org>" role
        attribute: mtls is "required" when the service mandates end-to-end encryption and
        "enabled" otherwise (the fabric always mutually authenticates every link), and
        status is "active" because a listed service is a configured, dialable entry. A
        service tagged for another org, or tagged for none, is invisible here.

        Unlike the network and router reads this does NOT degrade: an unconfigured
        deployment answers 503 and an unreachable controller surfaces the upstream's
        status, so a mesh page never renders "no services" for a fabric it simply could
        not read.
      operationId: get_mesh_services
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/meshServiceList'
          description: ok
      summary: Returns the Zero Trust edge services the caller's org owns.
      tags:
      - zt
      x-app: zt
      x-public: true
      x-tool: true
  /v1/messages:
    post:
      description: Implements the Anthropic Messages API.
      operationId: post_messages
      summary: Implements the Anthropic Messages API.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/messages/count_tokens:
    post:
      description: |-
        Implements POST /v1/messages/count_tokens. Claude Code
        calls it before a request; it returns {"input_tokens": N} for the given
        model + messages + tools.
      operationId: post_messages_count_tokens
      summary: Implements POST /v1/messages/count_tokens.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/metrics/batch:
    post:
      description: |-
        Writes every sample in a luxfi/metric `MetricBatch` into the calling org's store and answers `{written}`: the number of SAMPLES stored, not families and not metrics. This is the exact wire shape the ZAP `MsgMetricBatch` transport carries, so the HTTP door and the optional ZAP push receiver share one code path and one meaning — the transport is an optimisation, never a different contract.

        A counter or gauge lands as one sample. A histogram or summary contributes DERIVED `<name>_sum` and `<name>_count` series, so one metric can write more than one sample and `written` can exceed the number of metrics you sent. The batch's own `TimestampNs` stamps every sample it carries.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`; each org gets its own store, WAL-durable under the deployment's data dir. A body that does not decode is 400.
      operationId: post_metrics_batch
      summary: Ingest a MetricBatch — the same payload the ZAP transport carries
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/metrics/health:
    get:
      description: |-
        Reports the native metrics store's live state for the calling tenant: the subsystem version, the resolved `org`, and `series` — the number of distinct series actually held right now, read out of the store rather than a constant. It is not a dependency probe and has nothing downstream to fail on: the store is in-process, so this answers 200 whenever the process is up.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`. This surface trusts the edge rather than re-deriving the org from a validated claim of its own, so it belongs behind the gateway and nowhere else.
      operationId: get_metrics_health
      summary: How many metric series this deployment holds for your org
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/metrics/query:
    get:
      description: |-
        Answers `{count, series}`, where `count` is the number of matching SERIES and each series carries the samples that fall inside the window. `name` selects one series name, and an absent or empty `name` returns every series the org holds. `match` is a `k=v,k2=v2` label matcher applied as a SUPERSET test: a series matches when it carries all the named labels with those values, extra labels and all.

        `start` and `end` are nanoseconds since the Unix epoch, and here is the rule worth knowing: a bound that is absent, empty or unparseable becomes 0, which this store reads as UNBOUNDED. A malformed `start` therefore silently widens the query instead of failing it. There is no limit parameter — the window and the matcher are the whole of what bounds the answer.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`, so a query can only ever read the org the edge asserted.
      operationId: get_metrics_query
      summary: Read your org's series back over a time range
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/metrics/write:
    post:
      description: |-
        Takes `{series:[{name, labels, samples:[{t, v}]}]}`, appends every sample, creating each series on first write, and answers `{written}` — again counting SAMPLES, so three series of ten samples is 30.

        A series is identified by its name PLUS its whole label set, so adding one label makes a different series rather than annotating an existing one. Timestamps `t` are NANOSECONDS since the Unix epoch; a sample sent without one is stored at 0 and is then excluded by any query that sets a lower bound, which is the usual reason a write that reported success does not read back. Retention is per series and bounded — past 65536 samples the oldest are evicted.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`. A body that does not decode is 400; nothing else is validated or rejected.
      operationId: post_metrics_write
      summary: Append samples to your org's named, labelled series
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/models:
    get:
      description: |-
        Returns the list of available models from the routing table.

        PUBLIC BY DESIGN, AND IT DOES NOT AUTHENTICATE — that is the whole contract, so it
        is stated here rather than left to be inferred. The catalogue is the same for
        everyone (listAvailableModels takes no principal), docs.hanzo.ai fetches it from
        the browser, and every policy layer around it already says so out loud: the authz
        filter lists "models" as public, filter_balance refuses to gate it (a 402 here was
        a console-wide outage), the rate limiter excludes it, and cloud's spend.Reachable
        carries /v1/models/ as "the model catalog the shell reads for discovery".

        SO THE Authorization HEADER IS NOT AN ADMISSION CHECK HERE. It is read for ONE
        thing — annotating gated SKUs with the caller's own access standing — and
        annotation degrades to nothing when there is no verified principal.

        It used to hold a "require authentication" gate that authenticated nobody: it
        rejected an ABSENT credential and a MALFORMED one, then accepted any string that
        merely looked like a key. `Bearer sk-` followed by 36 zeroes returned 200 in
        production; so did a JWT three days expired. It was a shape check wearing an auth
        check's clothes, and its cost was diagnostic: /v1/models is the natural "is my auth
        working?" probe, and answering 200 to a dead credential sent people debugging the
        wrong system. A public endpoint must not appear to validate. Either check the
        credential or ignore it — this one ignores it, deliberately and visibly.

        Removing that gate discloses nothing new: the catalogue was already reachable by
        anyone willing to type three characters, so there is no confidentiality delta, only
        an honesty one.
      operationId: get_models
      summary: Returns the list of available models from the routing table.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/models/{model}/access:
    get:
      description: |-
        Returns the caller's own standing for a gated model:
        "granted", "requested", or empty when they have never asked.
      operationId: get_models_by_model_access
      parameters:
      - in: path
        name: model
        required: true
        schema:
          type: string
      summary: 'Returns the caller''s own standing for a gated model: "granted", "requested",
        or empty when they have never asked.'
      tags:
      - ai
      x-app: ai
      x-public: true
    post:
      description: |-
        Records the caller's waitlist request for a gated model and
        answers their new standing. Authed, idempotent, and self-scoped: the row is keyed
        to the caller's own org and identity, never to a body-supplied owner.
      operationId: post_models_by_model_access
      parameters:
      - in: path
        name: model
        required: true
        schema:
          type: string
      summary: Records the caller's waitlist request for a gated model and answers
        their new standing.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/models/providers:
    get:
      description: |-
        Public, secret-free list of the providers serving the models that
        GET /v1/models lists — the same source, projected. Safe unauthenticated: no
        keys, URLs, or config are returned, and it reports a SET of names, never
        which provider serves which model.
      operationId: get_models_providers
      summary: Public, secret-free list of the providers serving the models that GET
        /v1/models lists — the same source, projected.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/mq/health:
    get:
      description: Reports whether the message plane behind this surface answers.
      operationId: get_mq_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Health'
          description: ok
      summary: Reports whether the message plane behind this surface answers.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/info:
    get:
      description: Returns the broker's identity and the org's stream count.
      operationId: get_mq_info
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/infoOut'
          description: ok
      summary: Returns the broker's identity and the org's stream count.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream:
    get:
      description: Returns the org's streams, name-ordered, with their live state.
      operationId: get_mq_stream
      parameters:
      - description: Limit caps the streams returned (1–1000, default 100).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Offset skips that many streams, name-ordered.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Streams'
          description: ok
      summary: Returns the org's streams, name-ordered, with their live state.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
    post:
      description: Creates a durable stream in the org's namespace and returns it.
      operationId: post_mq_stream
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Config'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stream'
          description: created
      summary: Creates a durable stream in the org's namespace and returns it.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{name}:
    delete:
      description: Removes a stream with all its messages and consumers. Irreversible.
      operationId: delete_mq_stream_by_name
      parameters:
      - description: Name is the stream name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a stream with all its messages and consumers.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
    get:
      description: Returns one stream's configuration and live state.
      operationId: get_mq_stream_by_name
      parameters:
      - description: Name is the stream name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stream'
          description: ok
      summary: Returns one stream's configuration and live state.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
    put:
      description: |-
        Reconfigures an existing stream; the path names the stream, and the
        immutable fields (storage, retention) must restate what they are.
      operationId: put_mq_stream_by_name
      parameters:
      - description: Name is the stream name, unique within the org (alphanumeric,
          hyphens, underscores).
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Config'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stream'
          description: ok
      summary: Reconfigures an existing stream; the path names the stream, and the
        immutable fields (storage, retention) must restate what they are.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{name}/message:
    get:
      description: |-
        Reads stored messages without a consumer: by sequence, by newest on a
        subject, or walking a subject forward from a sequence.
      operationId: get_mq_stream_by_name_message
      parameters:
      - description: Name is the stream name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: 'Seq reads the message at this sequence (with next_by_subject:
          the walk''s start).'
        in: query
        name: seq
        required: false
        schema:
          type: integer
      - description: LastBySubject reads the newest message on this org-relative subject.
        in: query
        name: last_by_subject
        required: false
        schema:
          type: string
      - description: NextBySubject walks forward from seq collecting messages on this
          org-relative subject (wildcards supported).
        in: query
        name: next_by_subject
        required: false
        schema:
          type: string
      - description: Limit caps a next_by_subject walk (1–1000, default 100).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/readOut'
          description: ok
      summary: 'Reads stored messages without a consumer: by sequence, by newest on
        a subject, or walking a subject forward from a sequence.'
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{name}/message/{seq}:
    delete:
      description: Erases one message by sequence; the sequence gap remains.
      operationId: delete_mq_stream_by_name_message_by_seq
      parameters:
      - description: Name is the stream name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      - description: Seq is the message's stream sequence, from the path.
        in: path
        name: seq
        required: true
        schema:
          type: integer
      responses:
        "204":
          description: no content
      summary: Erases one message by sequence; the sequence gap remains.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{name}/purge:
    post:
      description: Removes messages from a stream, leaving its consumers in place.
      operationId: post_mq_stream_by_name_purge
      parameters:
      - description: Name is the stream name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Purge'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/purgeOut'
          description: ok
      summary: Removes messages from a stream, leaving its consumers in place.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{stream}/consumer:
    get:
      description: Returns a stream's consumers, name-ordered, with delivery state.
      operationId: get_mq_stream_by_stream_consumer
      parameters:
      - description: Stream is the stream name, from the path.
        in: path
        name: stream
        required: true
        schema:
          type: string
      - description: Limit caps the consumers returned (1–1000, default 100).
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Offset skips that many consumers, name-ordered.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pickOut'
          description: ok
      summary: Returns a stream's consumers, name-ordered, with delivery state.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
    post:
      description: Creates a durable pull consumer on a stream and returns it.
      operationId: post_mq_stream_by_stream_consumer
      parameters:
      - description: Stream is the stream name, from the path.
        in: path
        name: stream
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/makeIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consumer'
          description: created
      summary: Creates a durable pull consumer on a stream and returns it.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{stream}/consumer/{name}:
    delete:
      description: |-
        Removes a consumer and its delivery state; unacknowledged messages
        stay in the stream.
      operationId: delete_mq_stream_by_stream_consumer_by_name
      parameters:
      - description: Stream is the stream name, from the path.
        in: path
        name: stream
        required: true
        schema:
          type: string
      - description: Name is the consumer name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a consumer and its delivery state; unacknowledged messages
        stay in the stream.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
    get:
      description: Returns one consumer's configuration and delivery state.
      operationId: get_mq_stream_by_stream_consumer_by_name
      parameters:
      - description: Stream is the stream name, from the path.
        in: path
        name: stream
        required: true
        schema:
          type: string
      - description: Name is the consumer name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Consumer'
          description: ok
      summary: Returns one consumer's configuration and delivery state.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/mq/stream/{stream}/consumer/{name}/next:
    post:
      description: |-
        Pulls the consumer's next batch. Delivered messages are acknowledged on
        delivery — the broker will not redeliver what this call returns; an empty
        wait answers 408.
      operationId: post_mq_stream_by_stream_consumer_by_name_next
      parameters:
      - description: Stream is the stream name, from the path.
        in: path
        name: stream
        required: true
        schema:
          type: string
      - description: Name is the consumer name, from the path.
        in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/nextIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/readOut'
          description: ok
      summary: Pulls the consumer's next batch.
      tags:
      - mq
      x-app: mq
      x-public: true
      x-tool: true
  /v1/networks:
    get:
      description: |-
        Returns the caller's org overlay network on the Zero Trust fabric.

        The org has at most ONE overlay, projected from the edge-routers tagged with its
        "org-<org>" role attribute: nodes is the real router count and status is
        "connected" once at least one router has dialed home, "provisioning" while none
        has. An org with no routers gets an empty list, never a fabricated network.

        The read degrades rather than erroring: a deployment with no ZT credential, and a
        controller that cannot be reached, both answer 200 with an empty list so the
        console's Networks page renders a clean empty state instead of an error.
      operationId: get_networks
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/networkList'
          description: ok
      summary: Returns the caller's org overlay network on the Zero Trust fabric.
      tags:
      - zt
      x-app: zt
      x-public: true
      x-tool: true
  /v1/networks/{id}:
    get:
      description: |-
        Returns one overlay network by id, scoped to the caller's org.

        The org has exactly one overlay network and its id is derived from the org, so
        any other id — another tenant's, or one that does not exist — is 404 rather than
        a peek across the tenant boundary. An org whose network exists but has no
        edge-routers is 404 too, for the same reason the list is empty: there is no
        overlay until something is on it.
      operationId: get_networks_by_id
      parameters:
      - description: |-
          ID is the network id from the path. The URL is the addressing authority, so
          it binds from there whatever else the request carries.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/networkView'
          description: ok
      summary: Returns one overlay network by id, scoped to the caller's org.
      tags:
      - zt
      x-app: zt
      x-public: true
      x-tool: true
  /v1/networks/routers:
    get:
      description: |-
        Returns the Zero Trust routers the caller's org owns.

        One row per real ZT edge-router tagged with the org's "org-<org>" role attribute,
        carrying the controller's own health signal: "online" when connected, "disabled"
        when administratively disabled, "offline" otherwise. region is filled only from a
        "region-<slug>" role attribute and omitted when the router carries none, so the
        column renders "—" rather than a guess.

        The read degrades rather than erroring: a deployment with no ZT credential, and a
        controller that cannot be reached, both answer 200 with an empty list.
      operationId: get_networks_routers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/routerList'
          description: ok
      summary: Returns the Zero Trust routers the caller's org owns.
      tags:
      - zt
      x-app: zt
      x-public: true
      x-tool: true
  /v1/notify/health:
    get:
      description: |-
        Reports that the notify send surface is mounted.

        It is a pure liveness probe: it answers 200 whenever this subsystem is mounted
        and checks nothing downstream, so an "ok" here says the routes are reachable, not
        that any provider credential is configured. The body is notifyd's verbatim, so
        probes and clients that keyed on the standalone service keep working unchanged.
      operationId: get_notify_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/notifyHealth'
          description: ok
      summary: Reports that the notify send surface is mounted.
      tags:
      - notify
      x-app: notify
      x-public: true
      x-tool: true
  /v1/notify/send:
    post:
      description: |-
        Delivers one transactional message by email or SMS through the caller
        org's own provider credential.

        The channel comes from the body — sms or email — and the provider credential is
        read from KMS at orgs/<org>/notify/<service>/<key>, never from the environment.
        The org is the validated principal's, never a client-supplied value, so a caller
        can only ever send as their own tenant; an unauthenticated caller gets 401.
        Naming no provider picks the one whose credentials are actually configured
        (Twilio, then Plivo for SMS; Twilio Email, then SMTP for email) and fails closed
        when none is. Delivery is synchronous and per recipient: one recipient answers
        the bare {message_id,status} outcome, several answer the {items:[…]} envelope. A
        terminal provider failure is a 200 whose status is failed with the reason in
        error, never a transport error. sync=true is REQUIRED — an async dispatch
        answers 503, because the queue plane that would run it is owned elsewhere. The
        message body wins verbatim when present; otherwise template_id (or the event
        name) selects a built-in template rendered against template_vars.
      operationId: post_notify_send
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/notifySend'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Delivers one transactional message by email or SMS through the caller
        org's own provider credential.
      tags:
      - notify
      x-app: notify
      x-public: true
      x-tool: true
  /v1/notify/send/email:
    post:
      description: |-
        Delivers one transactional email through the caller org's own
        provider credential.

        It is the channel-pinned form of the generic send: identical in every respect
        except that the channel is fixed to email, OVERRIDING whatever the body names —
        so a body that says sms still goes out as mail. The provider is the org's own
        email credential from KMS (Twilio Email, then SMTP), resolved for the validated
        principal's org; an unauthenticated caller gets 401. Subject is carried on the
        email channel only.
      operationId: post_notify_send_email
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/notifySend'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Delivers one transactional email through the caller org's own provider
        credential.
      tags:
      - notify
      x-app: notify
      x-public: true
      x-tool: true
  /v1/notify/send/sms:
    post:
      description: |-
        Delivers one transactional SMS through the caller org's own provider
        credential.

        It is the channel-pinned form of the generic send: identical in every respect
        except that the channel is fixed to sms, OVERRIDING whatever the body names —
        so a body that says email still goes out as a text message. The provider is the
        org's own SMS credential from KMS (Twilio, then Plivo), resolved for the
        validated principal's org; an unauthenticated caller gets 401.
      operationId: post_notify_send_sms
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/notifySend'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema: {}
          description: ok
      summary: Delivers one transactional SMS through the caller org's own provider
        credential.
      tags:
      - notify
      x-app: notify
      x-public: true
      x-tool: true
  /v1/o11y/alerts:
    get:
      description: Returns the org's current alerts. Viewer gate.
      operationId: GetAlerts
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAlertsOut'
          description: ok
      summary: Returns the org's current alerts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/alerts/{receiver}:
    post:
      description: |-
        Records one Alertmanager webhook delivery and pages the on-call. Each alert prints an `ALERT-RECEIVED` line and joins the replay ring, then the batch is carried out of the process by the egress chain: the org's KMS-custodied Slack bot token first (the ONE product Slack egress, not a second webhook credential), falling back to a plain POST to `CLOUD_ALERTS_WEBHOOK_URL` — which needs no Slack connection and so works in exactly the state that silences the first. Resolved notifications page too: "it recovered" is the half of an incident people are actually waiting for.

        THE STATUS CODE REPORTS DELIVERY, NOT ARRIVAL. 200 `ok` means an egress accepted the batch. If none did — including when none is configured at all — it answers **503** naming the failure, so Alertmanager retries and counts it in `alertmanager_notifications_failed_total`. An alert nobody could be told about must never answer the same way as one that was delivered.

        A body that will not parse is still recorded (with empty fields) rather than rejected: the delivery happened, which is the fact being recorded, and a 400 would make Alertmanager retry a malformed payload forever.

        The receiver segment is Alertmanager's own receiver name, a parameter rather than a hand-listed route because the receiver set is config, not code.
      operationId: post_o11y_alerts_by_receiver
      parameters:
      - in: path
        name: receiver
        required: true
        schema:
          type: string
      summary: Take an Alertmanager notification and page a human
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/alerts/last:
    get:
      description: |-
        Answers the most recent Alertmanager deliveries THIS process received, as plain text — one greppable `ALERT-RECEIVED` line per alert, followed by the `ALERT-DELIVERED` / `ALERT-UNDELIVERED` outcome of carrying it out of the process, newest last, so piping to `tail` reads in arrival order. `(none)` when nothing has landed.

        Arrival and delivery are separate lines because they are separate facts that fail independently. Alertmanager can tell you it dispatched a notification, never that anything received it; this process taking the call says nothing about whether a human was reached. Reading only the first as if it were the second is how a pager stays silent for months behind a log where everything looks fine.

        The ring is PROCESS-LOCAL and bounded to the last 200 lines. Both are the point: a record that outlived the process that took the call would be a claim about something nobody observed, and an unbounded log is a memory leak with a nice name. A restart empties it.
      operationId: get_o11y_alerts_last
      summary: Replay the alert records this process took
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/api/{project_id}/envelope/:
    post:
      description: |-
        Accepts an application/x-sentry-envelope frame from a Sentry SDK — the batched wire format carrying events, sessions and attachments — and ingests it against the project named in the path.

        THE /api/ SEGMENT IS NOT OURS TO NAME. An SDK appends its own fixed /api/<project>/envelope/ suffix to whatever DSN it is given, so this address is the SDK's, received verbatim. We receive this shape; we do not publish it. The clean spelling of the same wire is /v1/sentinel/{project}/envelope/.

        AUTHENTICATED BY THE DSN PUBLIC KEY, never a Hanzo session, and therefore exempt from the principal gate: the ingest verifier checks the key in constant time, fails closed, and derives the org from it. A keyless submission is a 401 from that verifier — not a 403 from the gate, and not a 404 — which is how you tell the hops apart. The exemption is matched by method plus prefix plus suffix, never a bare prefix, so no read is reachable through it.
      operationId: post_o11y_api_by_project_id_envelope
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          type: string
      summary: Receive a Sentry envelope on the SDK's own DSN path
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/api/{project_id}/store/:
    post:
      description: |-
        The legacy single-event form of the envelope ingest: one JSON event rather than a framed batch, kept because SDKs in the field still send it.

        Same address ownership and same authentication as the envelope route — the /api/ segment is the SDK's, the DSN public key is the credential, the principal gate does not apply, and a keyless submission is a 401 from the ingest verifier.
      operationId: post_o11y_api_by_project_id_store
      parameters:
      - in: path
        name: project_id
        required: true
        schema:
          type: string
      summary: Receive a single Sentry event on the SDK's own DSN path
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/authz/check:
    post:
      description: |-
        Evaluates a batch of transactions — relation plus object — for
        the authenticated caller and answers each with its authorization verdict, in
        the order they were asked. It is the read a UI uses to decide which controls
        to show.
      operationId: AuthzCheck
      requestBody:
        content:
          application/json:
            schema:
              items:
                $ref: '#/components/schemas/o11y.O11yTransaction'
              type: array
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCheckOut'
          description: ok
      summary: Evaluates a batch of transactions — relation plus object — for the
        authenticated caller and answers each with its authorization verdict, in the
        order they were asked.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/auto_complete/attribute_values:
    post:
      description: |-
        Reads the attribute-value request from the body rather
        than off the query string — the spelling the newer builder uses to send its
        filters alongside the request.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_auto_complete_attribute_values
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.FilterAttributeValueRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAttributeValuesOut'
          description: ok
      summary: Reads the attribute-value request from the body rather than off the
        query string — the spelling the newer builder uses to send its filters alongside
        the request.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/autocomplete/aggregate_attributes:
    get:
      description: |-
        Lists the attributes usable as an aggregate target for
        the given telemetry and operator — what a filter builder offers after the
        aggregation is chosen.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_autocomplete_aggregate_attributes
      parameters:
      - description: |-
          DataSource is the telemetry the attributes come from — traces, logs,
          metrics or meter. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the attribute will be used under, e.g.
          count, avg, sum. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: SearchText narrows the attributes to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: Limit caps how many attributes come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAggregateAttributesOut'
          description: ok
      summary: Lists the attributes usable as an aggregate target for the given telemetry
        and operator — what a filter builder offers after the aggregation is chosen.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/autocomplete/attribute_keys:
    get:
      description: |-
        Lists the attribute keys available for filtering the given
        telemetry, each with its data type and whether it is a materialized column.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_autocomplete_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — traces, logs, metrics or
          meter. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under. The
          runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAttributeKeysOut'
          description: ok
      summary: Lists the attribute keys available for filtering the given telemetry,
        each with its data type and whether it is a materialized column.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/autocomplete/attribute_values:
    get:
      description: |-
        Lists the values one attribute key has taken — string,
        number and bool values in their own lists — for completing a filter.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_autocomplete_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — traces, logs or
          metrics. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under. The
          runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64, float64
          or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAttributeValuesOut'
          description: ok
      summary: Lists the values one attribute key has taken — string, number and bool
        values in their own lists — for completing a filter.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/availability:
    get:
      description: |-
        Reports how much of the Hanzo fleet is up — the current
        per-service inventory plus an up-versus-reporting trend across the window.
        Both come from the fleet prober's own measurements: every service is asked its
        health URL every 30 seconds, so a service is listed as down because it did not
        answer, never because something failed to collect it. PLATFORM SUDO ONLY —
        this is the whole fleet's inventory, not tenant data, so every customer is
        403. An unreachable telemetry store answers 503 rather than an empty trend,
        because a board of zeroes and a fleet that is down look identical.
      operationId: get_o11y_availability
      parameters:
      - description: Range is the trend window in seconds. Default 3600, capped at
          604800 (7d).
        example: 3600
        in: query
        name: range
        required: false
        schema:
          type: integer
      - description: |-
          StepSec is the bucket width in seconds, clamped to [30, 3600]. Absent
          picks ~60 buckets across the range.
        in: query
        name: stepSec
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.availabilityResponse'
          description: ok
      summary: Reports how much of the Hanzo fleet is up — the current per-service
        inventory plus an up-versus-reporting trend across the window.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/channels:
    get:
      description: Lists the org's notification channels. Viewer gate.
      operationId: ListChannels
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yChannelsOut'
          description: ok
      summary: Lists the org's notification channels.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a notification channel, answering with the stored
        channel. Admin gate.
      operationId: CreateChannel
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableChannel'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yChannelOut'
          description: created
      summary: Creates a notification channel, answering with the stored channel.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/channels/{id}:
    delete:
      description: Removes a notification channel, by id. Admin gate.
      operationId: DeleteChannelByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a notification channel, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one notification channel, by id. Viewer gate.
      operationId: GetChannelByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yChannelOut'
          description: ok
      summary: Returns one notification channel, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Replaces a notification channel's receiver, by id. Admin gate.
      operationId: UpdateChannelByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yChannelUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces a notification channel's receiver, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/channels/test:
    post:
      description: Sends a test notification to the posted receiver. Editor gate.
      operationId: TestChannel
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.alertmanagertypes.Receiver'
        required: true
      responses:
        "204":
          description: no content
      summary: Sends a test notification to the posted receiver.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud-integrations/{cloud_provider}/agent-check-in:
    post:
      description: |-
        Is the deployed agent's check-in on its original
        hyphenated path, kept for backward compatibility with agents already
        running. Viewer gate — the agent's role is viewer.
      operationId: AgentCheckInDeprecated
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yAgentCheckInIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAgentCheckInOut'
          description: ok
      summary: Is the deployed agent's check-in on its original hyphenated path, kept
        for backward compatibility with agents already running.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/accounts:
    get:
      description: |-
        Lists the cloud-integration accounts connected for the given
        provider. Admin gate.
      operationId: ListAccounts
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAccountsOut'
          description: ok
      summary: Lists the cloud-integration accounts connected for the given provider.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Connects a new cloud-integration account for the given
        provider from its posted config and credentials, answering with the account
        and the artifact the agent deploys to complete the connection. Admin gate.
      operationId: CreateAccount
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yCreateAccountIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCreateAccountOut'
          description: created
      summary: Connects a new cloud-integration account for the given provider from
        its posted config and credentials, answering with the account and the artifact
        the agent deploys to complete the connection.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/accounts/{id}:
    delete:
      description: |-
        Tears down a connected account for the given provider, by
        id. Admin gate.
      operationId: DisconnectAccount
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Tears down a connected account for the given provider, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one connected account for the given provider, by id. Admin
        gate.
      operationId: GetAccount
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAccountOut'
          description: ok
      summary: Returns one connected account for the given provider, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Changes a connected account's configuration for the given
        provider, by id. Admin gate.
      operationId: UpdateAccount
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdateAccountIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes a connected account's configuration for the given provider,
        by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/accounts/{id}/services:
    get:
      description: |-
        Lists the services metadata for one connected
        account of the given provider, by account id. Admin gate.
      operationId: ListAccountServicesMetadata
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServicesMetadataOut'
          description: ok
      summary: Lists the services metadata for one connected account of the given
        provider, by account id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/accounts/{id}/services/{service_id}:
    get:
      description: |-
        Returns one service and its configuration for a connected
        account of the given provider, by account id and service id. Admin gate.
      operationId: GetAccountService
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: service_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceOut'
          description: ok
      summary: Returns one service and its configuration for a connected account of
        the given provider, by account id and service id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Changes a service's configuration for one connected account of
        the given provider, by account id and service id. Admin gate.
      operationId: UpdateService
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: service_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdateServiceIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes a service's configuration for one connected account of the
        given provider, by account id and service id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/accounts/check_in:
    post:
      description: |-
        Is the deployed agent's check-in — the path consistent with the
        account surface, reporting the agent's account and telemetry state so the
        connection can be tracked. Viewer gate — the agent's role is viewer.
      operationId: AgentCheckIn
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yAgentCheckInIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAgentCheckInOut'
          description: ok
      summary: Is the deployed agent's check-in — the path consistent with the account
        surface, reporting the agent's account and telemetry state so the connection
        can be tracked.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/credentials:
    get:
      description: |-
        Returns the credentials the connecting agent needs
        to establish the cloud integration, for the given cloud provider. Admin gate.
      operationId: GetConnectionCredentials
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCredentialsOut'
          description: ok
      summary: Returns the credentials the connecting agent needs to establish the
        cloud integration, for the given cloud provider.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/services:
    get:
      description: |-
        Lists the services the given provider can collect from,
        optionally scoped to one cloud integration. Admin gate.
      operationId: ListServicesMetadata
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - description: CloudIntegrationID, when set, scopes the listing to one cloud
          integration.
        in: query
        name: cloud_integration_id
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServicesMetadataOut'
          description: ok
      summary: Lists the services the given provider can collect from, optionally
        scoped to one cloud integration.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/cloud_integrations/{cloud_provider}/services/{service_id}:
    get:
      description: |-
        Returns one service the given provider can collect from, by
        service id, optionally scoped to one cloud integration. Admin gate.
      operationId: GetService
      parameters:
      - in: path
        name: cloud_provider
        required: true
        schema:
          type: string
      - in: path
        name: service_id
        required: true
        schema:
          type: string
      - description: CloudIntegrationID, when set, scopes the service to one cloud
          integration.
        in: query
        name: cloud_integration_id
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceOut'
          description: ok
      summary: Returns one service the given provider can collect from, by service
        id, optionally scoped to one cloud integration.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/clusters/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes clusters
        report, for building cluster filters.
      operationId: get_o11y_clusters_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes clusters report, for building
        cluster filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/clusters/attribute_values:
    get:
      description: |-
        Lists the values one cluster attribute key has taken,
        for building cluster filters.
      operationId: get_o11y_clusters_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one cluster attribute key has taken, for building
        cluster filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/clusters/list:
    post:
      description: |-
        Lists Kubernetes clusters over a time range, each with its CPU
        and memory usage against allocatable capacity and its attributes;
        filterable, groupable and paginated.
      operationId: post_o11y_clusters_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.ClusterListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yClusterListOut'
          description: ok
      summary: Lists Kubernetes clusters over a time range, each with its CPU and
        memory usage against allocatable capacity and its attributes; filterable,
        groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/complete/google:
    get:
      description: |-
        The callback Google redirects a user back to after they approve the sign-in. It exchanges the authorization code, establishes the session and answers 303 to the console.

        The answer is a Location header and no body, which is why it is not a typed operation — declaring a JSON response for a redirect would publish a shape that does not exist and hide the header that is the entire point.

        UNAUTHENTICATED by necessity: it is how a caller GETS a principal, so requiring one would be circular. It is not an open door — the code it carries is single-use and verified against the provider.
      operationId: get_o11y_complete_google
      summary: Complete a Google sign-in
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/complete/oidc:
    get:
      description: |-
        The callback any configured OIDC provider redirects back to. Same shape and same reasoning as the Google callback: the code is exchanged, the session is established, and the answer is a 303 to the console rather than a body.

        UNAUTHENTICATED by necessity — this is the act of obtaining a principal, and the provider's own code is what authenticates it.
      operationId: get_o11y_complete_oidc
      summary: Complete a generic OIDC sign-in
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/complete/saml:
    post:
      description: |-
        The assertion consumer service: the identity provider POSTs its signed assertion here, and a valid one establishes the session and answers 303 to the console.

        A redirect, not a value, so it is not a typed operation. UNAUTHENTICATED by necessity and authenticated in fact by the assertion's signature, which is checked against the configured provider before any session exists.
      operationId: post_o11y_complete_saml
      summary: Complete a SAML sign-in
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/countErrors:
    post:
      description: |-
        Counts the grouped exceptions in the query window for the caller's
        org.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_counterrors
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yErrorsCountIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                type: integer
          description: ok
      summary: Counts the grouped exceptions in the query window for the caller's
        org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/daemonsets/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes daemonsets
        report, for building daemonset filters.
      operationId: get_o11y_daemonsets_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes daemonsets report, for building
        daemonset filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/daemonsets/attribute_values:
    get:
      description: |-
        Lists the values one daemonset attribute key has
        taken, for building daemonset filters.
      operationId: get_o11y_daemonsets_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one daemonset attribute key has taken, for building
        daemonset filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/daemonsets/list:
    post:
      description: |-
        Lists Kubernetes daemonsets over a time range, each with the
        CPU and memory its pods used against request and limit, desired and
        available node counts, restarts and attributes; filterable, groupable and
        paginated.
      operationId: post_o11y_daemonsets_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.DaemonSetListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDaemonSetListOut'
          description: ok
      summary: Lists Kubernetes daemonsets over a time range, each with the CPU and
        memory its pods used against request and limit, desired and available node
        counts, restarts and attributes; filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboard_views:
    get:
      description: |-
        Returns every saved view in the calling user's org. Saved
        views are shared org-wide.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListDashboardViews
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardViewListOut'
          description: ok
      summary: Returns every saved view in the calling user's org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Persists the calling user's dashboard-listing state (query,
        sort, order) as a named, reusable view shared across the org.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: CreateDashboardView
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardViewPostable'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardViewOut'
          description: created
      summary: Persists the calling user's dashboard-listing state (query, sort, order)
        as a named, reusable view shared across the org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboard_views/{id}:
    delete:
      description: |-
        Removes a saved view. Saved views are shared org-wide.
        Deleting a non-existent view refuses with the runtime's not-found.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: DeleteDashboardView
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a saved view.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces a saved view's name and data. Saved views are shared
        org-wide.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UpdateDashboardView
      parameters:
      - description: ID is the saved view id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardViewUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardViewOut'
          description: ok
      summary: Replaces a saved view's name and data.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboards:
    get:
      description: |-
        Returns a page of v2-shape dashboards for the org. This is the
        pure, user-independent list — it carries no pin state; use
        dashboardListForUserV2 for the personalized, pin-aware list. Supports a filter
        DSL (query), sort (updated_at/created_at/name), order (asc/desc), and
        offset-based pagination (limit/offset).

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListDashboardsV2
      parameters:
      - description: |-
          Query is the filter DSL over dashboard columns and tags, e.g.
          `name:cpu source:user`. Empty lists everything.
        in: query
        name: query
        required: false
        schema:
          type: string
      - description: |-
          Sort is the sort field: updated_at, created_at or name. Empty sorts by
          updated_at.
        in: query
        name: sort
        required: false
        schema:
          type: string
      - description: 'Order is the sort direction: asc or desc. Empty orders desc.'
        in: query
        name: order
        required: false
        schema:
          type: string
      - description: |-
          Limit caps how many dashboards come back. Zero means the default of 20;
          the runtime caps it at 200.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Offset is how many dashboards to skip for pagination.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardListOut'
          description: ok
      summary: Returns a page of v2-shape dashboards for the org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a dashboard in the v2 format that follows the Perses
        spec and answers with the stored dashboard.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: CreateDashboardV2
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardPostable'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardOut'
          description: created
      summary: Creates a dashboard in the v2 format that follows the Perses spec and
        answers with the stored dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboards/{id}:
    delete:
      description: |-
        Deletes a v2-shape dashboard along with its tag relations.
        Locked dashboards are rejected.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: DeleteDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a v2-shape dashboard along with its tag relations.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns a v2-shape dashboard.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardOut'
          description: ok
      summary: Returns a v2-shape dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Applies an RFC 6902 JSON Patch to a v2-shape dashboard. The
        patch is applied against the postable view (metadata, spec, tags), so individual
        panels, queries, variables, layouts or tags can be updated without re-sending the
        rest. Apply is lenient — remove on a missing path is a no-op and add creates any
        missing parent objects — and the result is still validated. Locked dashboards are
        rejected. The request body is the bare JSON Patch operations array.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: PatchDashboardV2
      parameters:
      - description: ID is the dashboard id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardPatchIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardOut'
          description: ok
      summary: Applies an RFC 6902 JSON Patch to a v2-shape dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Updates a v2-shape dashboard's metadata, spec and tag set.
        The name is immutable and locked dashboards are rejected.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UpdateDashboardV2
      parameters:
      - description: ID is the dashboard id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardOut'
          description: ok
      summary: Updates a v2-shape dashboard's metadata, spec and tag set.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboards/{id}/clone:
    post:
      description: |-
        Clones an existing v2-shape dashboard. User and integration
        dashboards can be cloned; system dashboards are rejected. The clone keeps the
        source's display name, panels and tags, but gets a freshly generated unique
        internal name and is always created as an unlocked user dashboard owned by the
        caller.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: CloneDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardOut'
          description: created
      summary: Clones an existing v2-shape dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboards/{id}/lock:
    delete:
      description: |-
        Unlocks a v2-shape dashboard. Only the dashboard's creator or
        an org admin may lock or unlock.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UnlockDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Unlocks a v2-shape dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Locks a v2-shape dashboard. Only the dashboard's creator or an
        org admin may lock or unlock.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: LockDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Locks a v2-shape dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dashboards/{id}/public:
    delete:
      description: |-
        Deletes the public-sharing config and disables public
        sharing of a dashboard.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: DeletePublicDashboard
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes the public-sharing config and disables public sharing of a
        dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns the public-sharing config for a dashboard.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: GetPublicDashboard
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPublicDashboardOut'
          description: ok
      summary: Returns the public-sharing config for a dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates the public-sharing config for a dashboard and
        enables public sharing, answering with the new share's id.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: CreatePublicDashboard
      parameters:
      - description: ID is the dashboard id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yPublicDashboardWriteIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIdentifiableOut'
          description: created
      summary: Creates the public-sharing config for a dashboard and enables public
        sharing, answering with the new share's id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Updates the public-sharing config for a dashboard.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: UpdatePublicDashboard
      parameters:
      - description: ID is the dashboard id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yPublicDashboardWriteIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Updates the public-sharing config for a dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/dependency_graph:
    post:
      description: |-
        Returns the service dependency graph over the requested
        window: every parent→child edge observed, with call and error rates and
        latency percentiles per edge.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_dependency_graph
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDependencyGraphIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/o11y.O11yDependency'
                type: array
          description: ok
      summary: 'Returns the service dependency graph over the requested window: every
        parent→child edge observed, with call and error rates and latency percentiles
        per edge.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/deployments/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes
        deployments report, for building deployment filters.
      operationId: get_o11y_deployments_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes deployments report, for
        building deployment filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/deployments/attribute_values:
    get:
      description: |-
        Lists the values one deployment attribute key has
        taken, for building deployment filters.
      operationId: get_o11y_deployments_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one deployment attribute key has taken, for building
        deployment filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/deployments/list:
    post:
      description: |-
        Lists Kubernetes deployments over a time range, each with the
        CPU and memory its pods used against request and limit, desired and
        available replica counts, restarts and attributes; filterable, groupable and
        paginated.
      operationId: post_o11y_deployments_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.DeploymentListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDeploymentListOut'
          description: ok
      summary: Lists Kubernetes deployments over a time range, each with the CPU and
        memory its pods used against request and limit, desired and available replica
        counts, restarts and attributes; filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/disks:
    get:
      description: |-
        Lists the storage disks the datastore reports, with their names and
        types.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_disks
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/o11y.O11yDisk'
                type: array
          description: ok
      summary: Lists the storage disks the datastore reports, with their names and
        types.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/domains:
    get:
      description: |-
        Lists the org's auth domains — the email domains whose SSO
        configuration this org owns. Admin gate.
      operationId: ListAuthDomains
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAuthDomainsOut'
          description: ok
      summary: Lists the org's auth domains — the email domains whose SSO configuration
        this org owns.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Claims an email domain for the org and configures how its
        users sign in; the answer is the new domain's id. Admin gate.
      operationId: CreateAuthDomain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yPostableAuthDomain'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCreatedOut'
          description: created
      summary: Claims an email domain for the org and configures how its users sign
        in; the answer is the new domain's id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/domains/{id}:
    delete:
      description: |-
        Releases an email domain and discards its SSO
        configuration, by id. Admin gate.
      operationId: DeleteAuthDomain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Releases an email domain and discards its SSO configuration, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one auth domain with its SSO configuration, by id.
        Admin gate.
      operationId: GetAuthDomain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAuthDomainOut'
          description: ok
      summary: Returns one auth domain with its SSO configuration, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces one auth domain's SSO configuration, by id. Admin
        gate.
      operationId: UpdateAuthDomain
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdatableAuthDomain'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces one auth domain's SSO configuration, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/downtime_schedules:
    get:
      description: |-
        Lists all planned maintenance windows, optionally
        narrowed to the active ones or the recurring ones. Viewer gate.
      operationId: ListDowntimeSchedules
      parameters:
      - description: |-
          Active, when "true" or "false", keeps only the active or inactive windows.
          Absent lists all.
        in: query
        name: active
        required: false
        schema:
          type: string
      - description: |-
          Recurring, when "true" or "false", keeps only the recurring or one-off
          windows. Absent lists all.
        in: query
        name: recurring
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDowntimeSchedulesOut'
          description: ok
      summary: Lists all planned maintenance windows, optionally narrowed to the active
        ones or the recurring ones.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a planned maintenance window, answering with
        the stored schedule. Editor gate.
      operationId: CreateDowntimeSchedule
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostablePlannedMaintenance'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDowntimeScheduleOut'
          description: created
      summary: Creates a planned maintenance window, answering with the stored schedule.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/downtime_schedules/{id}:
    delete:
      description: Removes a planned maintenance window, by id. Editor gate.
      operationId: DeleteDowntimeScheduleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a planned maintenance window, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one planned maintenance window, by id. Viewer gate.
      operationId: GetDowntimeScheduleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDowntimeScheduleOut'
          description: ok
      summary: Returns one planned maintenance window, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Replaces a planned maintenance window, by id. Editor gate.
      operationId: UpdateDowntimeScheduleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDowntimeUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces a planned maintenance window, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/errorFromErrorID:
    get:
      description: |-
        Returns one exception instance and the span it happened on,
        by its error id within a group at a timestamp.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_errorfromerrorid
      parameters:
      - description: |-
          Timestamp is the instance's time as a nanosecond epoch spelled as a
          string. Required.
        in: query
        name: timestamp
        required: true
        schema:
          type: string
      - description: GroupID is the exception group the instance belongs to. Required.
        in: query
        name: groupID
        required: true
        schema:
          type: string
      - description: |-
          ErrorID is the exception instance id. Required by errorFromErrorID and
          nextPrevErrorIDs; unused by errorFromGroupID.
        in: query
        name: errorID
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorWithSpan'
          description: ok
      summary: Returns one exception instance and the span it happened on, by its
        error id within a group at a timestamp.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/errorFromGroupID:
    get:
      description: |-
        Returns the representative exception instance of a group at a
        timestamp, and the span it happened on.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_errorfromgroupid
      parameters:
      - description: |-
          Timestamp is the instance's time as a nanosecond epoch spelled as a
          string. Required.
        in: query
        name: timestamp
        required: true
        schema:
          type: string
      - description: GroupID is the exception group the instance belongs to. Required.
        in: query
        name: groupID
        required: true
        schema:
          type: string
      - description: |-
          ErrorID is the exception instance id. Required by errorFromErrorID and
          nextPrevErrorIDs; unused by errorFromGroupID.
        in: query
        name: errorID
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorWithSpan'
          description: ok
      summary: Returns the representative exception instance of a group at a timestamp,
        and the span it happened on.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/errortracking/issues:
    get:
      description: |-
        Lists the caller's org's grouped error issues (by
        fingerprint) with status, level, counts and first/last-seen.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_errortracking_issues
      parameters:
      - description: 'Status narrows to one lifecycle state: unresolved, resolved
          or ignored.'
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Level narrows to one severity, e.g. error, warning, info.
        in: query
        name: level
        required: false
        schema:
          type: string
      - description: Environment narrows to one deployment environment.
        in: query
        name: environment
        required: false
        schema:
          type: string
      - description: ServiceName narrows to one reporting service.
        in: query
        name: serviceName
        required: false
        schema:
          type: string
      - description: Query narrows to issues whose text contains it.
        in: query
        name: query
        required: false
        schema:
          type: string
      - description: Sort orders the page, e.g. lastSeen, firstSeen, count.
        in: query
        name: sort
        required: false
        schema:
          type: string
      - description: Offset is how many issues to skip. Zero starts at the first.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many issues come back. Zero means the default.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorIssuesOut'
          description: ok
      summary: Lists the caller's org's grouped error issues (by fingerprint) with
        status, level, counts and first/last-seen.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/errortracking/issues/{id}:
    get:
      description: |-
        Returns one grouped issue with its latest occurrence sample.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_errortracking_issues_by_id
      parameters:
      - description: ID is the issue id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorGettableIssueOut'
          description: ok
      summary: Returns one grouped issue with its latest occurrence sample.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Changes an issue's lifecycle — resolve, ignore, reopen or
        assign — and returns the updated issue. Fields left unset are left unchanged.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_o11y_errortracking_issues_by_id
      parameters:
      - description: ID is the issue id.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yErrorUpdateIssueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorIssueOut'
          description: ok
      summary: Changes an issue's lifecycle — resolve, ignore, reopen or assign —
        and returns the updated issue.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/event:
    post:
      description: |-
        Records one product-analytics event for the signed-in user — a track
        event with a name and free-form attributes.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_event
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yEventIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMessage'
          description: ok
      summary: Records one product-analytics event for the signed-in user — a track
        event with a name and free-form attributes.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/explorer/views:
    get:
      description: |-
        Lists the caller's org's saved explorer views, optionally
        narrowed to one source page, name or category.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_explorer_views
      parameters:
      - description: SourcePage narrows the views to one source page, e.g. traces,
          logs.
        in: query
        name: sourcePage
        required: false
        schema:
          type: string
      - description: Name narrows the views to one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Category narrows the views to one category.
        in: query
        name: category
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySavedViewListOut'
          description: ok
      summary: Lists the caller's org's saved explorer views, optionally narrowed
        to one source page, name or category.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Saves a new explorer view for the caller's org and returns its
        id.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_o11y_explorer_views
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.SavedView'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySavedViewCreateOut'
          description: ok
      summary: Saves a new explorer view for the caller's org and returns its id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/explorer/views/{viewId}:
    delete:
      description: |-
        Deletes one saved explorer view by id.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: delete_o11y_explorer_views_by_viewid
      parameters:
      - description: ViewID is the view's id.
        in: path
        name: viewId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySavedViewDeleteOut'
          description: ok
      summary: Deletes one saved explorer view by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one saved explorer view by id.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_explorer_views_by_viewid
      parameters:
      - description: ViewID is the view's id.
        in: path
        name: viewId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySavedViewOut'
          description: ok
      summary: Returns one saved explorer view by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces one saved explorer view by id with the given view and
        echoes it back.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: put_o11y_explorer_views_by_viewid
      parameters:
      - description: ViewID is the id of the view to replace, taken from the URL.
        in: path
        name: viewId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySavedViewUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySavedViewOut'
          description: ok
      summary: Replaces one saved explorer view by id with the given view and echoes
        it back.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/export_raw_data:
    post:
      description: |-
        Runs a query and returns its rows as a downloadable CSV or JSONL attachment, chunked, with a trailer that says whether the export completed — so a truncated download is detectable rather than silently short.

        The answer is a file, not a value, which is why it is not a typed operation: the body is neither JSON nor bounded. Use the query operations when you want rows in a response.

        A validated, org-scoped principal is required and the export carries that principal's own tenant only.
      operationId: post_o11y_export_raw_data
      summary: Export raw telemetry rows as a file
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/factor_password/forgot:
    post:
      description: |-
        Starts the forgotten-password flow: the named user is mailed
        a reset link. Unauthenticated by design, and deliberately quiet about
        whether the address exists.
      operationId: ForgotPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yForgotPasswordIn'
        required: true
      responses:
        "204":
          description: no content
      summary: 'Starts the forgotten-password flow: the named user is mailed a reset
        link.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/features:
    get:
      description: |-
        Returns the supported feature flags and their resolved values for
        the caller's org.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_features
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFeaturesOut'
          description: ok
      summary: Returns the supported feature flags and their resolved values for the
        caller's org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/fields/keys:
    get:
      description: |-
        Returns the telemetry field keys matching the selector — the
        signal's fields grouped by name, and whether the catalog is complete.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_fields_keys
      parameters:
      - description: Signal is the telemetry to read the fields of — traces, logs
          or metrics.
        in: query
        name: signal
        required: false
        schema:
          type: string
      - description: Source narrows the fields to one source within the signal.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          StartUnixMilli is the window start as a unix millisecond epoch. Zero reads
          as unset.
        in: query
        name: startUnixMilli
        required: false
        schema:
          type: integer
      - description: |-
          EndUnixMilli is the window end as a unix millisecond epoch. Zero reads as
          unset.
        in: query
        name: endUnixMilli
        required: false
        schema:
          type: integer
      - description: |-
          FieldContext narrows the keys to one context — resource, scope, attribute,
          span, log or metric.
        in: query
        name: fieldContext
        required: false
        schema:
          type: string
      - description: FieldDataType narrows the keys to one data type.
        in: query
        name: fieldDataType
        required: false
        schema:
          type: string
      - description: MetricName narrows the keys to those on one metric.
        in: query
        name: metricName
        required: false
        schema:
          type: string
      - description: MetricNamespace narrows the keys to one metric namespace.
        in: query
        name: metricNamespace
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldKeysOut'
          description: ok
      summary: Returns the telemetry field keys matching the selector — the signal's
        fields grouped by name, and whether the catalog is complete.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/fields/values:
    get:
      description: |-
        Returns the values one telemetry field has taken — string, bool,
        number and related values — and whether the value list is complete.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_fields_values
      parameters:
      - description: Signal is the telemetry to read the field of — traces, logs or
          metrics.
        in: query
        name: signal
        required: false
        schema:
          type: string
      - description: Source narrows the field to one source within the signal.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          StartUnixMilli is the window start as a unix millisecond epoch. Zero reads
          as unset.
        in: query
        name: startUnixMilli
        required: false
        schema:
          type: integer
      - description: |-
          EndUnixMilli is the window end as a unix millisecond epoch. Zero reads as
          unset.
        in: query
        name: endUnixMilli
        required: false
        schema:
          type: integer
      - description: FieldContext narrows the field to one context.
        in: query
        name: fieldContext
        required: false
        schema:
          type: string
      - description: FieldDataType narrows the field to one data type.
        in: query
        name: fieldDataType
        required: false
        schema:
          type: string
      - description: MetricName narrows the field to one metric.
        in: query
        name: metricName
        required: false
        schema:
          type: string
      - description: MetricNamespace narrows the field to one metric namespace.
        in: query
        name: metricNamespace
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: Name is the field whose values to read.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: |-
          ExistingQuery is the query the field appears in, so related values can be
          suggested for it.
        in: query
        name: existingQuery
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldValuesOut'
          description: ok
      summary: Returns the values one telemetry field has taken — string, bool, number
        and related values — and whether the value list is complete.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/filter_suggestions:
    get:
      description: |-
        Suggests attribute keys and example filter queries for the
        query builder, seeded by what the org's own telemetry carries. Only the logs
        data source is supported today.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_filter_suggestions
      parameters:
      - description: |-
          DataSource is the signal suggestions are drawn from; only logs is
          supported today. Required.
        in: query
        name: dataSource
        required: true
        schema:
          type: string
      - description: SearchText narrows attribute suggestions to keys containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          ExistingFilter is the current filter set, JSON base64url-encoded, so
          example queries build on it rather than repeat it.
        in: query
        name: existingFilter
        required: false
        schema:
          type: string
      - description: AttributesLimit caps how many attribute keys come back.
        in: query
        name: attributesLimit
        required: false
        schema:
          type: integer
      - description: ExamplesLimit caps how many example queries come back.
        in: query
        name: examplesLimit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFilterSuggestionsOut'
          description: ok
      summary: Suggests attribute keys and example filter queries for the query builder,
        seeded by what the org's own telemetry carries.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/gateway/ingestion_keys:
    get:
      description: |-
        Lists the workspace's ingestion keys, paginated. Editor
        gate.
      operationId: GetIngestionKeys
      parameters:
      - description: Page is the 1-based page number.
        in: query
        name: page
        required: false
        schema:
          type: integer
      - description: PerPage is the page size.
        in: query
        name: per_page
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIngestionKeysOut'
          description: ok
      summary: Lists the workspace's ingestion keys, paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Mints an ingestion key for the workspace, answering with
        the created key. Editor gate.
      operationId: CreateIngestionKey
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableIngestionKey'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCreatedIngestionKeyOut'
          description: created
      summary: Mints an ingestion key for the workspace, answering with the created
        key.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/gateway/ingestion_keys/{keyId}:
    delete:
      description: Removes an ingestion key, by id. Editor gate.
      operationId: DeleteIngestionKey
      parameters:
      - in: path
        name: keyId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes an ingestion key, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: Changes an ingestion key, by id. Editor gate.
      operationId: UpdateIngestionKey
      parameters:
      - in: path
        name: keyId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdateIngestionKeyIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes an ingestion key, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/gateway/ingestion_keys/{keyId}/limits:
    post:
      description: |-
        Sets a signal limit on an ingestion key, by key id,
        answering with the created limit. Editor gate.
      operationId: CreateIngestionKeyLimit
      parameters:
      - in: path
        name: keyId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yCreateLimitIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCreatedLimitOut'
          description: created
      summary: Sets a signal limit on an ingestion key, by key id, answering with
        the created limit.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/gateway/ingestion_keys/limits/{limitId}:
    delete:
      description: |-
        Removes an ingestion key limit, by limit id. Editor
        gate.
      operationId: DeleteIngestionKeyLimit
      parameters:
      - in: path
        name: limitId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes an ingestion key limit, by limit id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes an ingestion key limit, by limit id. Editor
        gate.
      operationId: UpdateIngestionKeyLimit
      parameters:
      - in: path
        name: limitId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdateLimitIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes an ingestion key limit, by limit id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/gateway/ingestion_keys/search:
    get:
      description: |-
        Lists the workspace's ingestion keys whose name matches
        the search, paginated. Editor gate.
      operationId: SearchIngestionKeys
      parameters:
      - description: Name is the substring to match ingestion-key names against.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Page is the 1-based page number.
        in: query
        name: page
        required: false
        schema:
          type: integer
      - description: PerPage is the page size.
        in: query
        name: per_page
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIngestionKeysOut'
          description: ok
      summary: Lists the workspace's ingestion keys whose name matches the search,
        paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/getResetPasswordToken/{id}:
    get:
      description: |-
        Returns a user's password-reset token, creating one
        if none is live. Deprecated in favor of the reset_password_tokens pair,
        which separates reading from minting. Admin gate.
      operationId: GetResetPasswordTokenDeprecated
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yResetTokenOut'
          description: ok
      summary: Returns a user's password-reset token, creating one if none is live.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/global/config:
    get:
      description: |-
        Returns the deployment's global configuration: its public
        endpoints and which identity providers are enabled. Open by design — the
        sign-in page reads it before anyone is signed in.

        Open by design; the runtime's own gate is OpenAccess.
      operationId: get_o11y_global_config
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yGlobalConfigOut'
          description: ok
      summary: 'Returns the deployment''s global configuration: its public endpoints
        and which identity providers are enabled.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/health:
    get:
      description: |-
        Reports service health. With live set, the datastore connection is
        checked too and an unhealthy store refuses with 503.

        Open by design; the runtime's own gate is OpenAccess.
      operationId: get_o11y_health
      parameters:
      - description: |-
          Live also checks the datastore connection; an unreachable store refuses
          with 503.
        in: query
        name: live
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yHealthOut'
          description: ok
      summary: Reports service health.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/healthz:
    get:
      description: |-
        Reports whether every service in the runtime's registry is healthy, and names them grouped by state — so a failure says WHICH component is down, not merely that something is. An unhealthy registry answers 503, not a 200 with a false flag inside, so a plain status check cannot read a sick runtime as well.

        UNAUTHENTICATED by design, like the other two probes: it carries no tenant data and is reached by k8s and by external checks that hold no principal.
      operationId: get_o11y_healthz
      summary: Health of the observability runtime's services
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/hosts/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys hosts report, for building
        host filters — each with its data type and whether it is a materialized
        column.
      operationId: get_o11y_hosts_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys hosts report, for building host filters
        — each with its data type and whether it is a materialized column.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/hosts/attribute_values:
    get:
      description: |-
        Lists the values one host attribute key has taken, for
        building host filters — string, number and bool values in their own lists.
      operationId: get_o11y_hosts_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one host attribute key has taken, for building host
        filters — string, number and bool values in their own lists.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/hosts/list:
    post:
      description: |-
        Lists monitored hosts over a time range, each with its CPU,
        memory, I/O wait and 15-minute load, whether it is actively reporting, its
        OS and its attributes; filterable, groupable and paginated. The answer also
        says whether any host metrics were received at all and which clusters and
        nodes sent them, so an empty page is distinguishable from a fleet that never
        reported.
      operationId: post_o11y_hosts_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.HostListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yHostListOut'
          description: ok
      summary: Lists monitored hosts over a time range, each with its CPU, memory,
        I/O wait and 15-minute load, whether it is actively reporting, its OS and
        its attributes; filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/checks:
    get:
      description: |-
        Reports whether the metrics and attributes an infra-monitoring
        section needs are being received — for each collector receiver or processor
        involved, what is present and what is missing, with a user-facing message
        and a docs link per missing piece. Ready is true only when nothing is
        missing.
      operationId: get_o11y_infra_monitoring_checks
      parameters:
      - description: |-
          Type is the section to check — hosts, processes, pods, nodes,
          deployments, daemonsets, statefulsets, jobs, namespaces, clusters or
          volumes. Required.
        in: query
        name: type
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraChecksOut'
          description: ok
      summary: Reports whether the metrics and attributes an infra-monitoring section
        needs are being received — for each collector receiver or processor involved,
        what is present and what is missing, with a user-facing message and a docs
        link per missing piece.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/clusters:
    post:
      description: |-
        Lists Kubernetes clusters with CPU and memory usage against
        allocatable capacity summed over their nodes, plus per-group node readiness
        and pod phase counts. Rows answer as 'list' under the default
        k8s.cluster.name grouping or 'grouped_list' under a custom groupBy; a metric
        with no data in the window answers -1. Filterable by expression, orderable
        by usage or allocatable, paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_clusters
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableClusters'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraClustersOut'
          description: ok
      summary: Lists Kubernetes clusters with CPU and memory usage against allocatable
        capacity summed over their nodes, plus per-group node readiness and pod phase
        counts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/daemonsets:
    post:
      description: |-
        Lists Kubernetes daemonsets with the CPU and memory their
        pods used against request and limit, the latest desired and current
        scheduled NODE counts (node counts, not pod counts), and per-group pod phase
        counts. Rows answer as 'list' under the default k8s.daemonset.name grouping
        or 'grouped_list' under a custom groupBy; a metric with no data in the
        window answers -1. Filterable by expression, orderable by the pod metrics or
        the node counts, paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_daemonsets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableDaemonSets'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraDaemonSetsOut'
          description: ok
      summary: Lists Kubernetes daemonsets with the CPU and memory their pods used
        against request and limit, the latest desired and current scheduled NODE counts
        (node counts, not pod counts), and per-group pod phase counts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/deployments:
    post:
      description: |-
        Lists Kubernetes deployments with the CPU and memory their
        pods used against request and limit, the latest desired and available
        replica counts, and per-group pod phase counts. Rows answer as 'list' under
        the default k8s.deployment.name grouping or 'grouped_list' under a custom
        groupBy; a metric with no data in the window answers -1. Filterable by
        expression, orderable by the pod metrics or the replica counts, paginated by
        offset and limit.
      operationId: post_o11y_infra_monitoring_deployments
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableDeployments'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraDeploymentsOut'
          description: ok
      summary: Lists Kubernetes deployments with the CPU and memory their pods used
        against request and limit, the latest desired and available replica counts,
        and per-group pod phase counts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/hosts:
    post:
      description: |-
        Lists hosts with key infrastructure metrics — CPU, memory, I/O
        wait and disk usage percentages and 15-minute load — plus an
        active/inactive status from whether the host reported in the last ten
        minutes. Rows answer as 'list' under the default host.name grouping or
        'grouped_list' under a custom groupBy; a metric with no data in the window
        answers -1. Filterable by expression and by status, orderable by any of the
        five metrics, paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_hosts
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableHosts'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraHostsOut'
          description: ok
      summary: Lists hosts with key infrastructure metrics — CPU, memory, I/O wait
        and disk usage percentages and 15-minute load — plus an active/inactive status
        from whether the host reported in the last ten minutes.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/jobs:
    post:
      description: |-
        Lists Kubernetes jobs with the CPU and memory their pods used
        against request and limit, the latest desired-successful, active, failed and
        successful pod counters, and per-group pod phase counts — the phase counts
        are current state while the counters are cumulative over the job's life.
        Rows answer as 'list' under the default k8s.job.name grouping or
        'grouped_list' under a custom groupBy; a metric with no data in the window
        answers -1. Filterable by expression, orderable by the pod metrics or the
        job counters, paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_jobs
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableJobs'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraJobsOut'
          description: ok
      summary: Lists Kubernetes jobs with the CPU and memory their pods used against
        request and limit, the latest desired-successful, active, failed and successful
        pod counters, and per-group pod phase counts — the phase counts are current
        state while the counters are cumulative over the job's life.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/namespaces:
    post:
      description: |-
        Lists Kubernetes namespaces with the CPU and memory their
        pods used and per-group pod phase counts. Rows answer as 'list' under the
        default k8s.namespace.name grouping or 'grouped_list' under a custom
        groupBy, aggregating pods either way; a metric with no data in the window
        answers -1. Filterable by expression, orderable by cpu or memory, paginated
        by offset and limit.
      operationId: post_o11y_infra_monitoring_namespaces
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableNamespaces'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraNamespacesOut'
          description: ok
      summary: Lists Kubernetes namespaces with the CPU and memory their pods used
        and per-group pod phase counts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/nodes:
    post:
      description: |-
        Lists Kubernetes nodes with CPU and memory usage against
        allocatable capacity, per-group readiness counts and per-group phase counts
        for the pods scheduled on them. Rows answer as 'list' under the default
        k8s.node.name grouping (each row one node with its readiness condition) or
        'grouped_list' under a custom groupBy; a metric with no data in the window
        answers -1. Filterable by expression, orderable by usage or allocatable,
        paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_nodes
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableNodes'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraNodesOut'
          description: ok
      summary: Lists Kubernetes nodes with CPU and memory usage against allocatable
        capacity, per-group readiness counts and per-group phase counts for the pods
        scheduled on them.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/pods:
    post:
      description: |-
        Lists Kubernetes pods with CPU and memory usage against request
        and limit, the pod's phase and its age, plus its namespace, node, owning
        workload and cluster attributes. Rows answer as 'list' under the default
        k8s.pod.uid grouping (each row one pod) or 'grouped_list' under a custom
        groupBy (each row aggregating its pods with per-phase counts); a metric with
        no data in the window answers -1. Filterable by expression, orderable by the
        six pod metrics, paginated by offset and limit.
      operationId: post_o11y_infra_monitoring_pods
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostablePods'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraPodsOut'
          description: ok
      summary: Lists Kubernetes pods with CPU and memory usage against request and
        limit, the pod's phase and its age, plus its namespace, node, owning workload
        and cluster attributes.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/pvcs:
    post:
      description: |-
        Lists Kubernetes persistent volume claims with available,
        capacity and used bytes and inode counts, plus the claim's pod, namespace,
        node, statefulset and cluster attributes. Rows answer as 'list' under the
        default k8s.persistentvolumeclaim.name grouping or 'grouped_list' under a
        custom groupBy; a metric with no data in the window answers -1. Filterable
        by expression, orderable by the six volume metrics, paginated by offset and
        limit.
      operationId: post_o11y_infra_monitoring_pvcs
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableVolumes'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraVolumesOut'
          description: ok
      summary: Lists Kubernetes persistent volume claims with available, capacity
        and used bytes and inode counts, plus the claim's pod, namespace, node, statefulset
        and cluster attributes.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_monitoring/statefulsets:
    post:
      description: |-
        Lists Kubernetes statefulsets with the CPU and memory
        their pods used against request and limit, the latest desired and current
        replica counts, and per-group pod phase counts. Rows answer as 'list' under
        the default k8s.statefulset.name grouping or 'grouped_list' under a custom
        groupBy; a metric with no data in the window answers -1. Filterable by
        expression, orderable by the pod metrics or the replica counts, paginated by
        offset and limit.
      operationId: post_o11y_infra_monitoring_statefulsets
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableStatefulSets'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraStatefulSetsOut'
          description: ok
      summary: Lists Kubernetes statefulsets with the CPU and memory their pods used
        against request and limit, the latest desired and current replica counts,
        and per-group pod phase counts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/infra_onboarding/k8s/status:
    get:
      description: |-
        Reports how far Kubernetes infra onboarding has progressed:
        which metric families have arrived and, per pod, which required metadata
        labels are present.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_infra_onboarding_k8s_status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOnboardingOut'
          description: ok
      summary: 'Reports how far Kubernetes infra onboarding has progressed: which
        metric families have arrived and, per pod, which required metadata labels
        are present.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/integrations:
    get:
      description: |-
        Lists the available integrations and whether each is
        installed in the caller's org, optionally narrowed to installed or
        not-installed. Viewer gate.
      operationId: ListIntegrations
      parameters:
      - description: |-
          IsInstalled, when "true" or "false", keeps only integrations in that
          installed state; empty lists them all.
        in: query
        name: is_installed
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIntegrationsListOut'
          description: ok
      summary: Lists the available integrations and whether each is installed in the
        caller's org, optionally narrowed to installed or not-installed.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/integrations/{integrationId}:
    get:
      description: |-
        Returns one integration's full detail — its overview,
        configuration steps, collected data and assets — together with its
        installation record when the org has installed it. Viewer gate.
      operationId: GetIntegration
      parameters:
      - in: path
        name: integrationId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIntegrationOut'
          description: ok
      summary: Returns one integration's full detail — its overview, configuration
        steps, collected data and assets — together with its installation record when
        the org has installed it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/integrations/{integrationId}/connection_status:
    get:
      description: |-
        Reports whether the integration's logs and
        metrics have been received over the lookback window, so the console can show
        a live connection state. An integration that is not installed answers with an
        empty status rather than an error. Viewer gate.
      operationId: GetIntegrationConnectionStatus
      parameters:
      - in: path
        name: integrationId
        required: true
        schema:
          type: string
      - description: |-
          LookbackSeconds is how far back to look for received telemetry, in
          seconds.
        in: query
        name: lookback_seconds
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yConnectionStatusOut'
          description: ok
      summary: Reports whether the integration's logs and metrics have been received
        over the lookback window, so the console can show a live connection state.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/integrations/install:
    post:
      description: |-
        Installs an integration into the caller's org from its id
        and configuration, answering with the installed catalog item. Viewer gate.
      operationId: InstallIntegration
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.InstallIntegrationRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInstallOut'
          description: ok
      summary: Installs an integration into the caller's org from its id and configuration,
        answering with the installed catalog item.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/integrations/uninstall:
    post:
      description: |-
        Removes an integration from the caller's org by id.
        Viewer gate.
      operationId: UninstallIntegration
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.UninstallIntegrationRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yIntegrationAck'
          description: ok
      summary: Removes an integration from the caller's org by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/invite:
    post:
      description: |-
        Invites one person to the caller's org by email, with the role
        they will hold when they accept. Deprecated in favor of creating users
        directly; kept because callers still hold it. Admin gate, enforced by the
        runtime this op relays to.
      operationId: CreateInvite
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yInviteIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInviteOut'
          description: created
      summary: Invites one person to the caller's org by email, with the role they
        will hold when they accept.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/invite/bulk:
    post:
      description: |-
        Invites several people to the caller's org in one call,
        refusing the whole batch when any email repeats. Deprecated alongside
        createInvite. Admin gate.
      operationId: CreateBulkInvite
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yBulkInviteIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAck'
          description: created
      summary: Invites several people to the caller's org in one call, refusing the
        whole batch when any email repeats.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/jobs/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes jobs report, for
        building job filters.
      operationId: get_o11y_jobs_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes jobs report, for building
        job filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/jobs/attribute_values:
    get:
      description: |-
        Lists the values one job attribute key has taken, for
        building job filters.
      operationId: get_o11y_jobs_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one job attribute key has taken, for building job
        filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/jobs/list:
    post:
      description: |-
        Lists Kubernetes jobs over a time range, each with the CPU and
        memory its pods used against request and limit, desired-successful, active,
        failed and successful pod counts, restarts and attributes; filterable,
        groupable and paginated.
      operationId: post_o11y_jobs_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.JobListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yJobListOut'
          description: ok
      summary: Lists Kubernetes jobs over a time range, each with the CPU and memory
        its pods used against request and limit, desired-successful, active, failed
        and successful pod counts, restarts and attributes; filterable, groupable
        and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/licenses:
    get:
      description: |-
        Lists the org's licenses. This build has no enterprise edition, so
        the list is intentionally empty.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_licenses
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLicensesOut'
          description: ok
      summary: Lists the org's licenses.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/licenses/active:
    get:
      description: |-
        Activates the enterprise license. This build has no
        enterprise edition, so the licensing provider refuses it as unsupported.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_licenses_active
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLicenseActiveOut'
          description: ok
      summary: Activates the enterprise license.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/listErrors:
    post:
      description: |-
        Lists the grouped exceptions in the query window — each an
        exception type with its message, count, service and first/last-seen — for the
        caller's org.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_listerrors
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yErrorsListIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/o11y.O11yListError'
                type: array
          description: ok
      summary: Lists the grouped exceptions in the query window — each an exception
        type with its message, count, service and first/last-seen — for the caller's
        org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/livez:
    get:
      description: |-
        Answers 200 unconditionally while the process is running, and asserts NOTHING about the telemetry stores behind it. That is what makes it a liveness probe: a container that answers this is worth leaving alive, and restarting on a store outage would only remove the thing reporting the outage.

        UNAUTHENTICATED by design, and one of exactly three /v1/o11y paths that are. It carries no tenant data, and gating it would break the k8s probes and the external health checks without protecting anything. Use the health probe, not this one, to ask whether the runtime can actually serve.
      operationId: get_o11y_livez
      summary: Liveness of the observability process
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/llm/annotation:
    get:
      description: |-
        Lists human annotations on traces and observations,
        optionally scoped to one review queue.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMAnnotations
      parameters:
      - description: TraceID narrows to annotations on one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: Queue narrows to one review queue.
        in: query
        name: queue
        required: false
        schema:
          type: string
      - description: Status narrows to one review status, e.g. PENDING.
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMAnnotationsOut'
          description: ok
      summary: Lists human annotations on traces and observations, optionally scoped
        to one review queue.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds a human annotation to a trace or observation,
        optionally in a review queue.

        Callers need the editor role; the runtime's own gate enforces it, and it
        validates the payload and stamps the annotation's author and org.
      operationId: CreateLLMAnnotation
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yLLMIngestAnnotation'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMAnnotationOut'
          description: created
      summary: Adds a human annotation to a trace or observation, optionally in a
        review queue.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/observations:
    get:
      description: |-
        Lists gen_ai spans as LLM observations — each an LLM call with
        its model, token counts, cost and latency projected from gen_ai.* attributes,
        newest first, over the query window.

        Callers need the viewer role; the runtime's own gate enforces it, and scopes
        the read to the caller's validated tenant.
      operationId: ListLLMObservations
      parameters:
      - description: |-
          Start is the start of the window as a unix-millisecond epoch. Zero means
          24h before the end.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a unix-millisecond epoch. Zero
          means now.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: TraceID narrows the view to one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: SessionID narrows the view to one conversation.
        in: query
        name: sessionId
        required: false
        schema:
          type: string
      - description: UserID narrows the view to one end user.
        in: query
        name: userId
        required: false
        schema:
          type: string
      - description: Name narrows the view to observations of one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Model narrows the view to one model.
        in: query
        name: model
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMObservationsOut'
          description: ok
      summary: Lists gen_ai spans as LLM observations — each an LLM call with its
        model, token counts, cost and latency projected from gen_ai.* attributes,
        newest first, over the query window.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/score/{id}:
    delete:
      description: |-
        Hard-deletes a score by id.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: DeleteLLMScore
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Hard-deletes a score by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns a single score by id.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetLLMScore
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMScoreOut'
          description: ok
      summary: Returns a single score by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/scores:
    get:
      description: |-
        Lists eval scores and human-feedback signals attached to traces
        and observations, newest first.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMScores
      parameters:
      - description: TraceID narrows to scores on one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: ObservationID narrows to scores on one observation.
        in: query
        name: observationId
        required: false
        schema:
          type: string
      - description: Name narrows to scores of one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Source narrows to scores from one source, e.g. API, EVAL.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMScoresOut'
          description: ok
      summary: Lists eval scores and human-feedback signals attached to traces and
        observations, newest first.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Attaches an eval score or human-feedback signal to a trace or a
        single observation.

        Callers need the editor role; the runtime's own gate enforces it, and it
        validates the payload and stamps the score's author and org.
      operationId: CreateLLMScore
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yLLMIngestScore'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMScoreOut'
          description: created
      summary: Attaches an eval score or human-feedback signal to a trace or a single
        observation.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/sessions:
    get:
      description: |-
        Lists conversations — gen_ai spans grouped by session.id, with
        their trace and observation counts, tokens and cost.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMSessions
      parameters:
      - description: |-
          Start is the start of the window as a unix-millisecond epoch. Zero means
          24h before the end.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a unix-millisecond epoch. Zero
          means now.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: TraceID narrows the view to one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: SessionID narrows the view to one conversation.
        in: query
        name: sessionId
        required: false
        schema:
          type: string
      - description: UserID narrows the view to one end user.
        in: query
        name: userId
        required: false
        schema:
          type: string
      - description: Name narrows the view to observations of one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Model narrows the view to one model.
        in: query
        name: model
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMSessionsOut'
          description: ok
      summary: Lists conversations — gen_ai spans grouped by session.id, with their
        trace and observation counts, tokens and cost.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/traces:
    get:
      description: |-
        Lists LLM traces — gen_ai spans grouped by trace_id, with cost,
        tokens and latency rolled up across each trace.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMTraces
      parameters:
      - description: |-
          Start is the start of the window as a unix-millisecond epoch. Zero means
          24h before the end.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a unix-millisecond epoch. Zero
          means now.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: TraceID narrows the view to one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: SessionID narrows the view to one conversation.
        in: query
        name: sessionId
        required: false
        schema:
          type: string
      - description: UserID narrows the view to one end user.
        in: query
        name: userId
        required: false
        schema:
          type: string
      - description: Name narrows the view to observations of one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Model narrows the view to one model.
        in: query
        name: model
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMTracesOut'
          description: ok
      summary: Lists LLM traces — gen_ai spans grouped by trace_id, with cost, tokens
        and latency rolled up across each trace.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm/users:
    get:
      description: |-
        Lists end users — gen_ai spans grouped by user.id, with their
        session, trace and observation counts, tokens and cost.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMUsers
      parameters:
      - description: |-
          Start is the start of the window as a unix-millisecond epoch. Zero means
          24h before the end.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a unix-millisecond epoch. Zero
          means now.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: TraceID narrows the view to one trace.
        in: query
        name: traceId
        required: false
        schema:
          type: string
      - description: SessionID narrows the view to one conversation.
        in: query
        name: sessionId
        required: false
        schema:
          type: string
      - description: UserID narrows the view to one end user.
        in: query
        name: userId
        required: false
        schema:
          type: string
      - description: Name narrows the view to observations of one name.
        in: query
        name: name
        required: false
        schema:
          type: string
      - description: Model narrows the view to one model.
        in: query
        name: model
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMUsersOut'
          description: ok
      summary: Lists end users — gen_ai spans grouped by user.id, with their session,
        trace and observation counts, tokens and cost.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm_pricing_rules:
    get:
      description: |-
        Returns the LLM pricing rules for the caller's org, with
        pagination and an optional search and override filter.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListLLMPricingRules
      parameters:
      - description: Search matches rules by model or provider.
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: |-
          IsOverride, when "true" or "false", narrows to user-pinned rules or to
          synced ones; empty returns both. It is a string because a query param is a
          string on the wire, and the runtime reads absent as "no filter".
        in: query
        name: isOverride
        required: false
        schema:
          type: string
      - description: Offset is how many rows to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rows come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMPricingRulesOut'
          description: ok
      summary: Returns the LLM pricing rules for the caller's org, with pagination
        and an optional search and override filter.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Writes the pricing-rule batch — the single write
        endpoint used by both the user and the Zeus sync job. Per-rule match is by id,
        then sourceId, then insert; an override row is fully preserved when the
        request omits isOverride, only its synced_at stamped.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: CreateOrUpdateLLMPricingRules
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yLLMUpdatablePricingRules'
        required: true
      responses:
        "204":
          description: no content
      summary: Writes the pricing-rule batch — the single write endpoint used by both
        the user and the Zeus sync job.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/llm_pricing_rules/{id}:
    delete:
      description: |-
        Hard-deletes a pricing rule by id. If the rule was
        auto-synced, the next sync cycle recreates it.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: DeleteLLMPricingRule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Hard-deletes a pricing rule by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns a single LLM pricing rule by id.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetLLMPricingRule
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLLMPricingRuleOut'
          description: ok
      summary: Returns a single LLM pricing rule by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/login:
    get:
      description: |-
        Where a failed sign-in callback lands. The module builds that redirect with a path and no host, so it can only be same-origin, and its assumption is that the console lives beside the API. Here the console is a separate host, so this answers as the API it belongs to rather than redirecting onward — a hostname baked into a shared surface is how one brand's identity ends up in front of another brand's customer.

        401 with the provider's own reason, carried from the query the module already populated. Unauthenticated by necessity: it exists precisely for the case where no principal was established.
      operationId: get_o11y_login
      summary: Report why an observability sign-in did not complete
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/logs:
    get:
      description: |-
        Returns the most recent log records in the query window, newest
        first — each record an open object carrying its nanosecond timestamp and
        whatever fields the record was ingested with.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_logs
      parameters:
      - description: Limit caps how many records come back. Zero means the default
          of 100.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          TimestampStart is the start of the window as a nanosecond epoch. Zero
          means fifteen minutes before the end.
        in: query
        name: timestampStart
        required: false
        schema:
          type: integer
      - description: |-
          TimestampEnd is the end of the window as a nanosecond epoch. Zero means
          now.
        in: query
        name: timestampEnd
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogRecordsOut'
          description: ok
      summary: Returns the most recent log records in the query window, newest first
        — each record an open object carrying its nanosecond timestamp and whatever
        fields the record was ingested with.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/aggregate:
    get:
      description: |-
        Returns the logs aggregate buckets for the query window. The
        runtime currently answers the empty set; the shape is the contract.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_logs_aggregate
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogAggregateOut'
          description: ok
      summary: Returns the logs aggregate buckets for the query window.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/fields:
    get:
      description: |-
        Returns the log field catalog: the fields already selected as
        indexed columns, and the interesting ones seen in the data that could be.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_logs_fields
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldCatalogOut'
          description: ok
      summary: 'Returns the log field catalog: the fields already selected as indexed
        columns, and the interesting ones seen in the data that could be.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Changes how one log field is stored — selects or deselects it
        as a materialized column and tunes its index — and echoes the setting back.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_o11y_logs_fields
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFieldSetting'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldSetting'
          description: ok
      summary: Changes how one log field is stored — selects or deselects it as a
        materialized column and tunes its index — and echoes the setting back.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/livetail:
    get:
      description: |-
        Streams matching log records continuously instead of answering once, so a console tail shows lines as they land rather than at the end of a window.

        It is a STREAM, which is why it is not a typed operation: there is no single complete value to name, and a generated client that waited for one would hang on the first tail. Read the bounded window with the log read instead when you want an answer rather than a feed.

        A validated, org-scoped principal is required and the feed carries that principal's own tenant only.
      operationId: get_o11y_logs_livetail
      summary: Follow log records as they arrive
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/logs/pipelines:
    post:
      description: |-
        Saves the given log parsing pipelines as the new config
        version for the caller's org and starts deploying it. The set REPLACES the
        current one: a pipeline left out of the request is dropped from the new
        version, and an empty set drops them all.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_o11y_logs_pipelines
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yLogPipelineCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogPipelinesOut'
          description: ok
      summary: Saves the given log parsing pipelines as the new config version for
        the caller's org and starts deploying it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/pipelines/{version}:
    get:
      description: |-
        Returns the caller's org's log parsing pipelines at one config
        version — "latest" for the newest — along with that version's deployment
        record and the recent version history.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_logs_pipelines_by_version
      parameters:
      - description: Version is the config version to read — a positive number, or
          "latest".
        in: path
        name: version
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogPipelinesOut'
          description: ok
      summary: Returns the caller's org's log parsing pipelines at one config version
        — "latest" for the newest — along with that version's deployment record and
        the recent version history.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/pipelines/preview:
    post:
      description: |-
        Runs the given log parsing pipelines over the given
        sample records without saving anything, and returns the transformed records
        plus whatever the collector logged while simulating them.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_logs_pipelines_preview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yLogPipelinePreviewIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogPipelinePreviewOut'
          description: ok
      summary: Runs the given log parsing pipelines over the given sample records
        without saving anything, and returns the transformed records plus whatever
        the collector logged while simulating them.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/logs/promote_paths:
    get:
      description: |-
        Lists the log body paths already promoted or indexed, with the
        indexes each carries.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_logs_promote_paths
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogPromotedOut'
          description: ok
      summary: Lists the log body paths already promoted or indexed, with the indexes
        each carries.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Promotes and indexes log body paths: each named path is lifted
        out of the JSON body into its own column, with the indexes the caller asked
        for. Paths must start with "body.".

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_o11y_logs_promote_paths
      requestBody:
        content:
          application/json:
            schema:
              items:
                $ref: '#/components/schemas/o11y.O11yLogPromotePath'
              type: array
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogPromoteOut'
          description: created
      summary: 'Promotes and indexes log body paths: each named path is lifted out
        of the JSON body into its own column, with the indexes the caller asked for.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/consumer-lag/consumer-details:
    post:
      description: |-
        Returns the consumer side of a consumer-lag view: the
        consumer groups reading the topic/partition named in variables, with their
        throughput and latency over the window.
      operationId: post_o11y_messaging-queues_kafka_consumer-lag_consumer-details
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: 'Returns the consumer side of a consumer-lag view: the consumer groups
        reading the topic/partition named in variables, with their throughput and
        latency over the window.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/consumer-lag/network-latency:
    post:
      description: |-
        Returns consumer network latency correlated per client:
        a throughput pass over the window finds the consumer clients, then their
        fetch latency joins in as a latency column per client/instance/service.
      operationId: post_o11y_messaging-queues_kafka_consumer-lag_network-latency
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: 'Returns consumer network latency correlated per client: a throughput
        pass over the window finds the consumer clients, then their fetch latency
        joins in as a latency column per client/instance/service.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/consumer-lag/producer-details:
    post:
      description: |-
        Returns the producer side of a consumer-lag view: the
        producers writing to the topic/partition named in variables, with their
        throughput and latency over the window.
      operationId: post_o11y_messaging-queues_kafka_consumer-lag_producer-details
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: 'Returns the producer side of a consumer-lag view: the producers writing
        to the topic/partition named in variables, with their throughput and latency
        over the window.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/onboarding/consumers:
    post:
      description: |-
        Checks whether the spans the Kafka consumer views need
        are arriving, row for row like producersOnboarding.
      operationId: post_o11y_messaging-queues_kafka_onboarding_consumers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueueChecksOut'
          description: ok
      summary: Checks whether the spans the Kafka consumer views need are arriving,
        row for row like producersOnboarding.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/onboarding/kafka:
    post:
      description: |-
        Checks whether Kafka's own metrics — consumer lag and
        partition telemetry — are arriving, so the lag views can be lit up.
      operationId: post_o11y_messaging-queues_kafka_onboarding_kafka
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueueChecksOut'
          description: ok
      summary: Checks whether Kafka's own metrics — consumer lag and partition telemetry
        — are arriving, so the lag views can be lit up.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/onboarding/producers:
    post:
      description: |-
        Checks whether the spans the Kafka producer views need
        are arriving — one row per required span attribute, with a pass/fail status
        and, on failure, what is missing from the instrumentation.
      operationId: post_o11y_messaging-queues_kafka_onboarding_producers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueueChecksOut'
          description: ok
      summary: Checks whether the spans the Kafka producer views need are arriving
        — one row per required span attribute, with a pass/fail status and, on failure,
        what is missing from the instrumentation.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/partition-latency/consumer:
    post:
      description: |-
        Returns the consumer-group latency detail for the
        topic and partition named in the request's variables.
      operationId: post_o11y_messaging-queues_kafka_partition-latency_consumer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Returns the consumer-group latency detail for the topic and partition
        named in the request's variables.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/partition-latency/overview:
    post:
      description: |-
        Returns the per-partition latency overview for the window —
        each topic/partition with its throughput and latency profile.
      operationId: post_o11y_messaging-queues_kafka_partition-latency_overview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Returns the per-partition latency overview for the window — each topic/partition
        with its throughput and latency profile.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/span/evaluation:
    post:
      description: |-
        Correlates producer and consumer spans over the evaluation
        window (eval_time bounds the scan) and returns the pairings with their
        end-to-end delay — the check that messages produced are being consumed.
      operationId: post_o11y_messaging-queues_kafka_span_evaluation
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Correlates producer and consumer spans over the evaluation window (eval_time
        bounds the scan) and returns the pairings with their end-to-end delay — the
        check that messages produced are being consumed.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/topic-throughput/consumer:
    post:
      description: |-
        Returns the consumer topic-throughput overview for the
        window — what each consumer group read, per topic.
      operationId: post_o11y_messaging-queues_kafka_topic-throughput_consumer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Returns the consumer topic-throughput overview for the window — what
        each consumer group read, per topic.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/topic-throughput/consumer-details:
    post:
      description: |-
        Breaks one consumer topic's throughput down using
        the topic and service named in variables.
      operationId: post_o11y_messaging-queues_kafka_topic-throughput_consumer-details
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Breaks one consumer topic's throughput down using the topic and service
        named in variables.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/topic-throughput/producer:
    post:
      description: |-
        Returns the producer topic-throughput overview for the
        window — what each producer service wrote, per topic.
      operationId: post_o11y_messaging-queues_kafka_topic-throughput_producer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Returns the producer topic-throughput overview for the window — what
        each producer service wrote, per topic.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/kafka/topic-throughput/producer-details:
    post:
      description: |-
        Breaks one producer topic's throughput down using
        the topic and service named in variables.
      operationId: post_o11y_messaging-queues_kafka_topic-throughput_producer-details
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: Breaks one producer topic's throughput down using the topic and service
        named in variables.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/messaging-queues/queue-overview:
    post:
      description: |-
        Lists the messaging destinations observed in the window — one
        row per queue/destination/service combination with its throughput and
        latency columns. Filters narrow by queue system, destination, service or any
        span attribute.
      operationId: post_o11y_messaging-queues_queue-overview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueueListIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueueRowsOut'
          description: ok
      summary: Lists the messaging destinations observed in the window — one row per
        queue/destination/service combination with its throughput and latency columns.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric/metric_metadata:
    get:
      description: |-
        Serves the OLDER /metric/metric_metadata route. It is
        NOT the same op as metrics.go's metricMetadata (/metrics/metadata): different
        path, different input (this one also scopes by service). Two slices named one
        Go function for two routes; the route is the identity, so the name follows it.
        Renamed rather than merged — collapsing them would silently drop the service
        scope this one accepts.
        It returns one metric's metadata — its type, unit, description,
        temporality, monotonicity and histogram buckets — optionally scoped to the
        metric as one service reports it.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_metric_metric_metadata
      parameters:
      - description: MetricName is the metric to read.
        in: query
        name: metricName
        required: false
        schema:
          type: string
      - description: ServiceName scopes the metadata to the metric as one service
          reports it.
        in: query
        name: serviceName
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricMetadataOut'
          description: ok
      summary: Serves the OLDER /metric/metric_metadata route.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric_reduction_rules:
    get:
      description: |-
        Lists the org's metric volume-control (label reduction)
        rules, pageable and sortable by name, volume or recency.
      operationId: ListMetricReductionRules
      parameters:
      - description: |-
          OrderBy sorts the page: metric, ingested_volume, reduced_volume or
          last_updated. Unset means ingested_volume.
        in: query
        name: orderBy
        required: false
        schema:
          type: string
      - description: Order is asc or desc. Unset means desc.
        in: query
        name: order
        required: false
        schema:
          type: string
      - description: Search narrows the page to rules whose metric name contains it.
        in: query
        name: search
        required: false
        schema:
          type: string
      - description: MetricName narrows the page to one metric's rule.
        in: query
        name: metricName
        required: false
        schema:
          type: string
      - description: Offset is how many rules to skip, for paging.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many rules come back, at most 1000. Unset means
          10.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionRuleListOut'
          description: ok
      summary: Lists the org's metric volume-control (label reduction) rules, pageable
        and sortable by name, volume or recency.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a volume-control rule for a metric and returns
        it with its id; a metric that already has a rule is refused.
      operationId: CreateMetricReductionRule
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yReductionRuleCreateIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionRuleOut'
          description: created
      summary: Creates a volume-control rule for a metric and returns it with its
        id; a metric that already has a rule is refused.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric_reduction_rules/{id}:
    delete:
      description: Deletes a volume-control rule by its id.
      operationId: DeleteMetricReductionRuleByID
      parameters:
      - description: ID is the rule's id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a volume-control rule by its id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one volume-control rule by its id.
      operationId: GetMetricReductionRuleByID
      parameters:
      - description: ID is the rule's id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionRuleOut'
          description: ok
      summary: Returns one volume-control rule by its id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Updates the match type and labels of a volume-control rule
        by its id; the metric name is immutable.
      operationId: UpdateMetricReductionRuleByID
      parameters:
      - description: ID is the rule's id.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yReductionRuleSaveIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionRuleOut'
          description: ok
      summary: Updates the match type and labels of a volume-control rule by its id;
        the metric name is immutable.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric_reduction_rules/preview:
    post:
      description: |-
        Estimates the series reduction and the dashboards and
        alerts a candidate volume-control rule would touch, without persisting it.
      operationId: PreviewMetricReductionRule
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yReductionRulePreviewIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionRulePreviewOut'
          description: ok
      summary: Estimates the series reduction and the dashboards and alerts a candidate
        volume-control rule would touch, without persisting it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric_reduction_rules/stats:
    get:
      description: |-
        Returns total ingested vs retained series and samples and
        the estimated monthly savings across all volume-control rules.
      operationId: GetMetricReductionRuleStats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionStatsOut'
          description: ok
      summary: Returns total ingested vs retained series and samples and the estimated
        monthly savings across all volume-control rules.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metric_reduction_rules/timeseries:
    get:
      description: |-
        Returns ingested vs retained series over time across
        all volume-control rules, in hourly buckets, in the query-range time-series
        response shape.
      operationId: GetMetricReductionRuleTimeseries
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yReductionSeriesOut'
          description: ok
      summary: Returns ingested vs retained series over time across all volume-control
        rules, in hourly buckets, in the query-range time-series response shape.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics:
    get:
      description: |-
        Lists the distinct metric names seen in a time range, each with
        its description, type, unit, temporality and monotonicity.
      operationId: ListMetrics
      parameters:
      - description: Start is the start of the window as a Unix timestamp in milliseconds.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a Unix timestamp in milliseconds.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: Limit caps how many metrics come back; unset means 100, at most
          5000.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: SearchText narrows the page to metric names containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: Source narrows the page by ingestion source.
        in: query
        name: source
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricListOut'
          description: ok
      summary: Lists the distinct metric names seen in a time range, each with its
        description, type, unit, temporality and monotonicity.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/alerts:
    get:
      description: Lists the alert rules that reference a metric.
      operationId: GetMetricAlerts
      parameters:
      - description: |-
          MetricName is the metric's name; it may contain slashes, e.g.
          run.googleapis.com/request_latencies. Required.
        in: query
        name: metricName
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricAlertsOut'
          description: ok
      summary: Lists the alert rules that reference a metric.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/attributes:
    get:
      description: |-
        Returns one metric's attribute keys, each with its unique
        values and their count.
      operationId: GetMetricAttributes
      parameters:
      - description: MetricName is the metric's name; it may contain slashes. Required.
        in: query
        name: metricName
        required: true
        schema:
          type: string
      - description: Start is the start of the window as a Unix timestamp in milliseconds.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the end of the window as a Unix timestamp in milliseconds.
        in: query
        name: end
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricAttributesOut'
          description: ok
      summary: Returns one metric's attribute keys, each with its unique values and
        their count.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/dashboards:
    get:
      description: Lists the dashboard panels that reference a metric.
      operationId: GetMetricDashboardsV2
      parameters:
      - description: |-
          MetricName is the metric's name; it may contain slashes, e.g.
          run.googleapis.com/request_latencies. Required.
        in: query
        name: metricName
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricDashboardsOut'
          description: ok
      summary: Lists the dashboard panels that reference a metric.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/highlights:
    get:
      description: |-
        Returns one metric's headline numbers: data points, total
        and active time series, and when it was last received.
      operationId: GetMetricHighlights
      parameters:
      - description: |-
          MetricName is the metric's name; it may contain slashes, e.g.
          run.googleapis.com/request_latencies. Required.
        in: query
        name: metricName
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricHighlightsOut'
          description: ok
      summary: 'Returns one metric''s headline numbers: data points, total and active
        time series, and when it was last received.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/inspect:
    post:
      description: |-
        Returns one metric's raw time series over a window of at most
        thirty minutes — each series with its labels and timestamp/value pairs.
      operationId: InspectMetrics
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yMetricInspectIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricInspectOut'
          description: ok
      summary: Returns one metric's raw time series over a window of at most thirty
        minutes — each series with its labels and timestamp/value pairs.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/metadata:
    get:
      description: |-
        Returns one metric's metadata: description, type, unit,
        temporality and monotonicity.
      operationId: GetMetricMetadata
      parameters:
      - description: |-
          MetricName is the metric's name; it may contain slashes, e.g.
          run.googleapis.com/request_latencies. Required.
        in: query
        name: metricName
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricMetadataOut'
          description: ok
      summary: 'Returns one metric''s metadata: description, type, unit, temporality
        and monotonicity.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Updates one metric's metadata — description, type, unit,
        temporality, monotonicity — and answers with the bare success envelope.
      operationId: UpdateMetricMetadata
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yMetricMetadataSaveIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricAckOut'
          description: ok
      summary: Updates one metric's metadata — description, type, unit, temporality,
        monotonicity — and answers with the bare success envelope.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/onboarding:
    get:
      description: |-
        Reports whether any non-O11y metrics have been ingested —
        the lightweight check onboarding polls.
      operationId: GetMetricsOnboardingStatus
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricOnboardingOut'
          description: ok
      summary: Reports whether any non-O11y metrics have been ingested — the lightweight
        check onboarding polls.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/stats:
    post:
      description: |-
        Lists metrics with their sample and time-series counts for a
        time range — the volume view of the metrics explorer, pageable and sortable.
      operationId: GetMetricsStats
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yMetricStatsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricStatsOut'
          description: ok
      summary: Lists metrics with their sample and time-series counts for a time range
        — the volume view of the metrics explorer, pageable and sortable.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/metrics/treemap:
    post:
      description: |-
        Returns the proportional distribution of metrics by sample
        count or time-series count, as the entries of a treemap.
      operationId: GetMetricsTreemap
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yMetricTreemapIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricTreemapOut'
          description: ok
      summary: Returns the proportional distribution of metrics by sample count or
        time-series count, as the entries of a treemap.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/namespaces/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes namespaces
        report, for building namespace filters.
      operationId: get_o11y_namespaces_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes namespaces report, for building
        namespace filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/namespaces/attribute_values:
    get:
      description: |-
        Lists the values one namespace attribute key has
        taken, for building namespace filters.
      operationId: get_o11y_namespaces_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one namespace attribute key has taken, for building
        namespace filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/namespaces/list:
    post:
      description: |-
        Lists Kubernetes namespaces over a time range, each with the
        CPU and memory its pods used, their phase counts and its attributes;
        filterable, groupable and paginated.
      operationId: post_o11y_namespaces_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.NamespaceListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yNamespaceListOut'
          description: ok
      summary: Lists Kubernetes namespaces over a time range, each with the CPU and
        memory its pods used, their phase counts and its attributes; filterable, groupable
        and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/nextPrevErrorIDs:
    get:
      description: |-
        Returns the ids of the exception instances immediately after
        and before a given one within its group — the paging cursor the error detail
        view walks.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_nextpreverrorids
      parameters:
      - description: |-
          Timestamp is the instance's time as a nanosecond epoch spelled as a
          string. Required.
        in: query
        name: timestamp
        required: true
        schema:
          type: string
      - description: GroupID is the exception group the instance belongs to. Required.
        in: query
        name: groupID
        required: true
        schema:
          type: string
      - description: |-
          ErrorID is the exception instance id. Required by errorFromErrorID and
          nextPrevErrorIDs; unused by errorFromGroupID.
        in: query
        name: errorID
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yNextPrevErrorIDs'
          description: ok
      summary: Returns the ids of the exception instances immediately after and before
        a given one within its group — the paging cursor the error detail view walks.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/nodes/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes nodes report,
        for building node filters.
      operationId: get_o11y_nodes_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes nodes report, for building
        node filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/nodes/attribute_values:
    get:
      description: |-
        Lists the values one node attribute key has taken, for
        building node filters.
      operationId: get_o11y_nodes_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one node attribute key has taken, for building node
        filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/nodes/list:
    post:
      description: |-
        Lists Kubernetes nodes over a time range, each with its CPU and
        memory usage against allocatable capacity, readiness condition counts and
        attributes; filterable, groupable and paginated.
      operationId: post_o11y_nodes_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.NodeListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yNodeListOut'
          description: ok
      summary: Lists Kubernetes nodes over a time range, each with its CPU and memory
        usage against allocatable capacity, readiness condition counts and attributes;
        filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/org/preferences:
    get:
      description: |-
        Lists every org-scoped preference, each with its current
        and default value. Admin gate.
      operationId: ListOrgPreferences
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPreferencesOut'
          description: ok
      summary: Lists every org-scoped preference, each with its current and default
        value.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/org/preferences/{name}:
    get:
      description: Returns one org-scoped preference, by name. Admin gate.
      operationId: GetOrgPreference
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPreferenceOut'
          description: ok
      summary: Returns one org-scoped preference, by name.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Sets one org-scoped preference, by name. Admin gate.
      operationId: UpdateOrgPreference
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdatablePreference'
        required: true
      responses:
        "204":
          description: no content
      summary: Sets one org-scoped preference, by name.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/orgs/me:
    get:
      description: Returns the caller's own organization. Admin gate.
      operationId: GetMyOrganization
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOrganizationOut'
          description: ok
      summary: Returns the caller's own organization.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Rewrites the caller's own organization record — display name,
        name, alias — always addressed as "me", never by id. Admin gate.
      operationId: UpdateMyOrganization
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yOrganization'
        required: true
      responses:
        "204":
          description: no content
      summary: Rewrites the caller's own organization record — display name, name,
        alias — always addressed as "me", never by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/orgs/me/filters:
    get:
      description: |-
        Returns the org's quick filters for every signal — the
        attribute shortlists its explorers offer as one-click filters. Viewer gate.
      operationId: GetQuickFilters
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQuickFiltersOut'
          description: ok
      summary: Returns the org's quick filters for every signal — the attribute shortlists
        its explorers offer as one-click filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces the org's quick filters for one signal with the
        attribute list given. Admin gate.
      operationId: UpdateQuickFilters
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdatableQuickFilters'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces the org's quick filters for one signal with the attribute
        list given.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/orgs/me/filters/{signal}:
    get:
      description: |-
        Returns the org's quick filters for one signal — traces,
        logs, metrics, exceptions or api_monitoring. Viewer gate.
      operationId: GetSignalFilters
      parameters:
      - in: path
        name: signal
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySignalFiltersOut'
          description: ok
      summary: Returns the org's quick filters for one signal — traces, logs, metrics,
        exceptions or api_monitoring.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pods/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes pods report, for
        building pod filters.
      operationId: get_o11y_pods_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes pods report, for building
        pod filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pods/attribute_values:
    get:
      description: |-
        Lists the values one pod attribute key has taken, for
        building pod filters.
      operationId: get_o11y_pods_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one pod attribute key has taken, for building pod
        filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pods/list:
    post:
      description: |-
        Lists Kubernetes pods over a time range, each with its CPU and
        memory usage against request and limit, restart count, phase counts and
        attributes; filterable, groupable and paginated.
      operationId: post_o11y_pods_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PodListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPodListOut'
          description: ok
      summary: Lists Kubernetes pods over a time range, each with its CPU and memory
        usage against request and limit, restart count, phase counts and attributes;
        filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/processes/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys processes report, for
        building process filters.
      operationId: get_o11y_processes_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys processes report, for building process
        filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/processes/attribute_values:
    get:
      description: |-
        Lists the values one process attribute key has taken,
        for building process filters.
      operationId: get_o11y_processes_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one process attribute key has taken, for building
        process filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/processes/list:
    post:
      description: |-
        Lists monitored processes over a time range, each with its name,
        PID, command line and CPU and memory usage; filterable, groupable and
        paginated.
      operationId: post_o11y_processes_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.ProcessListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yProcessListOut'
          description: ok
      summary: Lists monitored processes over a time range, each with its name, PID,
        command line and CPU and memory usage; filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/product/metrics:
    get:
      description: |-
        Returns one product's RED series — request rate, errors, p50
        and p95 latency — for the caller's org, plus that org's LLM usage rollup over
        the same window. The series come from org-tagged request spans, so a tenant
        only ever aggregates its own traffic; a validated platform SuperAdmin sees the
        whole product's RED, while usage stays the caller's own org either way. A
        well-formed product with no backing workload answers empty series; a malformed
        slug is a 400.
      operationId: get_o11y_product_metrics
      parameters:
      - description: Product is the console product slug to read, e.g. "kms". Required.
        example: kms
        in: query
        name: product
        required: false
        schema:
          type: string
      - description: Range is the window in seconds. Default 3600, capped at 604800
          (7d).
        example: 3600
        in: query
        name: range
        required: false
        schema:
          type: integer
      - description: |-
          StepSec is the bucket width in seconds, clamped to [30, 3600]. Absent
          picks ~60 buckets across the range.
        in: query
        name: stepSec
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.metricsResponse'
          description: ok
      summary: Returns one product's RED series — request rate, errors, p50 and p95
        latency — for the caller's org, plus that org's LLM usage rollup over the
        same window.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/public/dashboards/{id}:
    get:
      description: |-
        Returns the sanitized dashboard data for public access —
        the read a shared dashboard's public page makes.

        Anonymous, scoped to the public dashboard's read scope; the runtime's own gate
        enforces it.
      operationId: GetPublicDashboardData
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPublicDashboardDataOut'
          description: ok
      summary: Returns the sanitized dashboard data for public access — the read a
        shared dashboard's public page makes.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/public/dashboards/{id}/widgets/{idx}/query_range:
    get:
      description: |-
        Returns the query-range result for one widget
        of a public dashboard. When the share fixes its own time range the caller's
        startTime/endTime are ignored; otherwise they bound the window as millisecond
        epochs.

        Anonymous, scoped to the public dashboard's read scope; the runtime's own gate
        enforces it.
      operationId: GetPublicDashboardWidgetQueryRange
      parameters:
      - description: ID is the public dashboard id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Idx is the widget's index from the path.
        in: path
        name: idx
        required: true
        schema:
          type: string
      - description: |-
          StartTime is the window start as a millisecond epoch. Used only when the
          share enables a caller-chosen time range.
        in: query
        name: startTime
        required: false
        schema:
          type: string
      - description: |-
          EndTime is the window end as a millisecond epoch. Used only when the share
          enables a caller-chosen time range.
        in: query
        name: endTime
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yWidgetQueryRangeOut'
          description: ok
      summary: Returns the query-range result for one widget of a public dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pvcs/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys persistent volume claims
        report, for building volume filters.
      operationId: get_o11y_pvcs_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys persistent volume claims report, for
        building volume filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pvcs/attribute_values:
    get:
      description: |-
        Lists the values one persistent-volume-claim attribute
        key has taken, for building volume filters.
      operationId: get_o11y_pvcs_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one persistent-volume-claim attribute key has taken,
        for building volume filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/pvcs/list:
    post:
      description: |-
        Lists Kubernetes persistent volume claims over a time range, each
        with its available, capacity and used bytes, inode counts and attributes;
        filterable, groupable and paginated.
      operationId: post_o11y_pvcs_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.VolumeListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPvcListOut'
          description: ok
      summary: Lists Kubernetes persistent volume claims over a time range, each with
        its available, capacity and used bytes, inode counts and attributes; filterable,
        groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/query:
    get:
      description: |-
        Evaluates one instant PromQL query against the org's metrics and
        returns the result at a single point in time.

        The result is polymorphic by PromQL's own contract — a matrix, vector,
        scalar or string, discriminated by resultType — so it is carried verbatim
        rather than forced into one of its shapes.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_query
      parameters:
      - description: Query is the PromQL expression to evaluate. Required.
        in: query
        name: query
        required: true
        schema:
          type: string
      - description: |-
          Time is the evaluation timestamp — epoch seconds or RFC3339. Empty
          evaluates at now.
        in: query
        name: time
        required: false
        schema:
          type: string
      - description: Stats set to any non-empty value includes query statistics in
          the answer.
        in: query
        name: stats
        required: false
        schema:
          type: string
      - description: Timeout caps evaluation time, as a duration in seconds.
        in: query
        name: timeout
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPromQueryOut'
          description: ok
      summary: Evaluates one instant PromQL query against the org's metrics and returns
        the result at a single point in time.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/query_filter/analyze:
    post:
      description: |-
        Analyzes a query and extracts the metric names it reads
        and the columns it groups by.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_query_filter_analyze
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yAnalyzeIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAnalyzeOut'
          description: ok
      summary: Analyzes a query and extracts the metric names it reads and the columns
        it groups by.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/query_progress:
    get:
      description: |-
        Reports how far a submitted query has got — rows scanned, bytes read, elapsed — and HOLDS the connection until the next update rather than answering immediately.

        The long poll is the whole point, and the reason this cannot be a typed operation: an answer that arrived only when the query finished would report progress on nothing. The websocket form of the same read is /ws/query_progress.

        A validated, org-scoped principal is required; a query id belonging to another tenant is simply not found.
      operationId: get_o11y_query_progress
      summary: Watch one running query's progress
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/query_range:
    get:
      description: |-
        Runs a Prometheus-style range query over metrics — the
        legacy read that predates the v5 querier — and returns the matrix, vector or
        scalar the query resolved to.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_query_range
      parameters:
      - description: |-
          Start is the window start — a unix timestamp (seconds, with optional
          fraction) or an RFC 3339 time. Required.
        in: query
        name: start
        required: true
        schema:
          type: string
      - description: |-
          End is the window end, in the same form as Start, and not before it.
          Required.
        in: query
        name: end
        required: true
        schema:
          type: string
      - description: |-
          Step is the query resolution, e.g. 60s, 1m, 1h — a positive duration.
          Required.
        in: query
        name: step
        required: true
        schema:
          type: string
      - description: Query is the PromQL expression to evaluate. Required.
        in: query
        name: query
        required: true
        schema:
          type: string
      - description: Stats, when "all", asks for query statistics alongside the result.
        in: query
        name: stats
        required: false
        schema:
          type: string
      - description: |-
          Timeout caps how long the query may run, e.g. 30s, 1m — a positive
          duration. Absent means the server default.
        in: query
        name: timeout
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yMetricsQueryRangeOut'
          description: ok
      summary: Runs a Prometheus-style range query over metrics — the legacy read
        that predates the v5 querier — and returns the matrix, vector or scalar the
        query resolved to.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Executes a composite query over a time range: builder
        queries over traces, logs and metrics, formulas, trace operators, PromQL and
        Datastore SQL, answering time series, scalars or raw records as the request
        type asks.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_query_range
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.QueryRangeRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeOut'
          description: ok
      summary: 'Executes a composite query over a time range: builder queries over
        traces, logs and metrics, formulas, trace operators, PromQL and Datastore
        SQL, answering time series, scalars or raw records as the request type asks.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/query_range/format:
    post:
      description: |-
        Parses a builder query and echoes it back normalized to the
        v3 shape — the endpoint the UI uses to canonicalize a query without running
        it.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_query_range_format
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.QueryRangeParamsV3'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangeFormatOut'
          description: ok
      summary: Parses a builder query and echoes it back normalized to the v3 shape
        — the endpoint the UI uses to canonicalize a query without running it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/query_range/preview:
    post:
      description: |-
        Validates a composite query and renders the Datastore
        statements it would run WITHOUT executing it — a dry run for agentic and
        tooling use. verbose=false trades the rendered SQL and EXPLAIN for a
        lightweight per-query verdict.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_query_range_preview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yQueryRangePreviewIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yQueryRangePreviewOut'
          description: ok
      summary: Validates a composite query and renders the Datastore statements it
        would run WITHOUT executing it — a dry run for agentic and tooling use.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/readyz:
    get:
      description: |-
        Reports whether the runtime's registered services are healthy enough to take traffic, and answers 503 when they are not — which is what takes a booting or degraded replica out of the load balancer instead of letting it serve errors.

        UNAUTHENTICATED by design, like the other two probes. It reads the same service registry the health probe reads, so the two agree by construction; readiness is the question a router asks and health is the question an operator asks.
      operationId: get_o11y_readyz
      summary: Readiness of the observability runtime to serve
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/register:
    post:
      description: |-
        Creates the FIRST organization and its admin user. It is open by
        design — there is nobody to be signed in as yet — and refuses once setup has
        completed, after which new users arrive by invitation only.

        Open by design; the runtime's own gate is OpenAccess.
      operationId: post_o11y_register
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRegisterIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRegisterOut'
          description: ok
      summary: Creates the FIRST organization and its admin user.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/reset_password_tokens/verify:
    post:
      description: |-
        Checks that a reset-password token exists and has not
        expired, without consuming it. Unauthenticated: the token is the proof.
      operationId: VerifyResetPasswordToken
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yResetTokenRef'
        required: true
      responses:
        "204":
          description: no content
      summary: Checks that a reset-password token exists and has not expired, without
        consuming it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/resetPassword:
    post:
      description: |-
        Sets a new password for whoever the reset token was minted
        for, consuming the token. Unauthenticated: the token is the proof.
      operationId: ResetPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yResetPasswordIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Sets a new password for whoever the reset token was minted for, consuming
        the token.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/reviews:
    get:
      description: |-
        Returns a page of the caller org's human-review queues,
        newest first, narrowed to the caller's project. Another org's queues are never
        visible.
      operationId: get_o11y_reviews
      parameters:
      - description: Page is the 1-based page to read. Default 1.
        example: 1
        in: query
        name: page
        required: false
        schema:
          type: integer
      - description: Limit is how many rows to return. Default 20, capped at 100.
        example: 20
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annQueueList'
          description: ok
      summary: Returns a page of the caller org's human-review queues, newest first,
        narrowed to the caller's project.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a human-review queue in the caller's org and
        project. A name already used by another queue in the same project is a 409.
      operationId: post_o11y_reviews
      requestBody:
        content:
          application/json:
            example:
              name: hallucination review
              scoreConfigIds:
              - quality
            schema:
              $ref: '#/components/schemas/o11y.createQueueReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annQueueView'
          description: created
      summary: Creates a human-review queue in the caller's org and project.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/reviews/{id}:
    delete:
      description: |-
        Removes one review queue and every item in it. A queue
        id belonging to another org answers the same 404 an unknown id does, so a
        probe learns nothing about what exists.
      operationId: delete_o11y_reviews_by_id
      parameters:
      - description: ID is the annotation queue to act on, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annQueueDeleted'
          description: ok
      summary: Removes one review queue and every item in it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one review queue with its pending and completed
        counts and its first page of items. A queue id belonging to another org is a
        404, never a cross-tenant read.
      operationId: get_o11y_reviews_by_id
      parameters:
      - description: ID is the annotation queue to act on, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annQueueDetailView'
          description: ok
      summary: Returns one review queue with its pending and completed counts and
        its first page of items.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes a review queue's name, description or
        score-config set. A field the request omits is left alone. A name another
        queue in the same project already uses is a 409; a queue id belonging to
        another org is a 404.
      operationId: patch_o11y_reviews_by_id
      parameters:
      - description: ID is the annotation queue to update, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              id: annq_1
              name: hallucination review v2
            schema:
              $ref: '#/components/schemas/o11y.updateQueueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annQueueView'
          description: ok
      summary: Changes a review queue's name, description or score-config set.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/reviews/{id}/items:
    get:
      description: |-
        Returns a page of one review queue's items, newest
        first, optionally filtered to PENDING or COMPLETED. A queue id belonging to
        another org is a 404, never a cross-tenant list.
      operationId: get_o11y_reviews_by_id_items
      parameters:
      - description: ID is the annotation queue whose items to list, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Status filters to PENDING or COMPLETED items. Absent returns
          both.
        example: PENDING
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Page is the 1-based page to read. Default 1.
        in: query
        name: page
        required: false
        schema:
          type: integer
      - description: Limit is how many rows to return. Default 20, capped at 100.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annItemList'
          description: ok
      summary: Returns a page of one review queue's items, newest first, optionally
        filtered to PENDING or COMPLETED.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Enqueues traces, observations or sessions on a review
        queue. Each item names exactly one object, either by traceId / observationId /
        sessionId or by objectType plus objectId; every item enters PENDING. A queue
        id belonging to another org is a 404.
      operationId: post_o11y_reviews_by_id_items
      parameters:
      - description: ID is the annotation queue to add to, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              id: annq_1
              items:
              - traceId: tr_1
            schema:
              $ref: '#/components/schemas/o11y.addItemsIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annItemsCreated'
          description: created
      summary: Enqueues traces, observations or sessions on a review queue.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/reviews/{id}/items/{itemId}:
    patch:
      description: |-
        Moves one queue item between PENDING and COMPLETED
        and sets its assignee. Completing an item stamps its completedAt. An item that
        exists under a different queue answers the same 404 an unknown item does, and
        so does a queue belonging to another org.
      operationId: patch_o11y_reviews_by_id_items_by_itemid
      parameters:
      - description: ID is the annotation queue the item belongs to, from the path.
        example: annq_1
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: ItemID is the item to update, from the path.
        example: annqi_1
        in: path
        name: itemId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              id: annq_1
              itemId: annqi_1
              status: COMPLETED
            schema:
              $ref: '#/components/schemas/o11y.updateItemIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.annItemView'
          description: ok
      summary: Moves one queue item between PENDING and COMPLETED and sets its assignee.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/roles:
    get:
      description: |-
        Lists every role in the caller's org — the managed ones the
        platform seeds and the custom ones its admins created.
      operationId: ListRoles
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRolesOut'
          description: ok
      summary: Lists every role in the caller's org — the managed ones the platform
        seeds and the custom ones its admins created.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a custom role in the caller's org from a name, an optional
        description and the transaction groups it grants, answering the new role's id.

        Names are lowercase letters and hyphens only, and may not start with the
        reserved managed-role prefix; the runtime refuses anything else.
      operationId: CreateRole
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRoleCreateIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoleCreateOut'
          description: created
      summary: Creates a custom role in the caller's org from a name, an optional
        description and the transaction groups it grants, answering the new role's
        id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/roles/{id}:
    delete:
      description: |-
        Deletes a custom role. A role that still has user or
        service-account assignees, or an auth-domain mapping, is refused; managed
        roles cannot be deleted.
      operationId: DeleteRole
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a custom role.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one role with the transaction groups it grants.
      operationId: GetRole
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoleOut'
          description: ok
      summary: Returns one role with the transaction groups it grants.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces a custom role's description and transaction groups.
        Both fields are mandatory — send an empty string or an empty array to clear
        one — and managed roles cannot be edited.
      operationId: UpdateRole
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRoleUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces a custom role's description and transaction groups.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/roles/{id}/users:
    get:
      description: |-
        Returns every org member holding a role, by role id. Admin
        gate.
      operationId: GetUsersByRoleID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yUsersOut'
          description: ok
      summary: Returns every org member holding a role, by role id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/route_policies:
    get:
      description: Lists the org's route policies. Viewer gate.
      operationId: GetAllRoutePolicies
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoutePoliciesOut'
          description: ok
      summary: Lists the org's route policies.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: Creates a route policy, answering with the stored policy. Admin
        gate.
      operationId: CreateRoutePolicy
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableRoutePolicy'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoutePolicyOut'
          description: created
      summary: Creates a route policy, answering with the stored policy.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/route_policies/{id}:
    delete:
      description: Removes a route policy, by id. Admin gate.
      operationId: DeleteRoutePolicyByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a route policy, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one route policy, by id. Viewer gate.
      operationId: GetRoutePolicyByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoutePolicyOut'
          description: ok
      summary: Returns one route policy, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces a route policy, by id, answering with the stored
        policy. Admin gate.
      operationId: UpdateRoutePolicy
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRoutePolicyUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRoutePolicyOut'
          description: ok
      summary: Replaces a route policy, by id, answering with the stored policy.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules:
    get:
      description: Lists all alert rules with their current evaluation state. Viewer
        gate.
      operationId: ListRules
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRulesOut'
          description: ok
      summary: Lists all alert rules with their current evaluation state.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: Creates a new alert rule and answers with the stored rule. Editor
        gate.
      operationId: CreateRule
      requestBody:
        content:
          application/json:
            schema: {}
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleOut'
          description: created
      summary: Creates a new alert rule and answers with the stored rule.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}:
    delete:
      description: Removes an alert rule, by id. Editor gate.
      operationId: DeleteRuleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes an alert rule, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one alert rule with its evaluation state, by id. Viewer
        gate.
      operationId: GetRuleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleOut'
          description: ok
      summary: Returns one alert rule with its evaluation state, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Applies a partial update to an alert rule, by id, answering
        with the stored rule — the common toggle for enabling or muting a rule.
        Editor gate.
      operationId: PatchRuleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema: {}
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleOut'
          description: ok
      summary: Applies a partial update to an alert rule, by id, answering with the
        stored rule — the common toggle for enabling or muting a rule.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Replaces an alert rule's definition, by id. Editor gate.
      operationId: UpdateRuleByID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema: {}
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces an alert rule's definition, by id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/filter_keys:
    get:
      description: |-
        Returns the distinct label keys present in a rule's
        history entries over the selected range, for building history filters. Viewer gate.
      operationId: GetRuleHistoryFilterKeys
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - description: StartUnixMilli is the window start, unix milliseconds.
        in: query
        name: startUnixMilli
        required: false
        schema:
          type: integer
      - description: EndUnixMilli is the window end, unix milliseconds.
        in: query
        name: endUnixMilli
        required: false
        schema:
          type: integer
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50, capped at
          200.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryFilterKeysOut'
          description: ok
      summary: Returns the distinct label keys present in a rule's history entries
        over the selected range, for building history filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/filter_values:
    get:
      description: |-
        Returns the distinct values a given label key has
        taken across a rule's history entries. Viewer gate.
      operationId: GetRuleHistoryFilterValues
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: query
        name: startUnixMilli
        required: false
        schema:
          type: integer
      - in: query
        name: endUnixMilli
        required: false
        schema:
          type: integer
      - in: query
        name: searchText
        required: false
        schema:
          type: string
      - in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Name is the label key whose values to list. Required.
        in: query
        name: name
        required: true
        schema:
          type: string
      - description: ExistingQuery is a filter expression scoping which values appear.
        in: query
        name: existingQuery
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryFilterValuesOut'
          description: ok
      summary: Returns the distinct values a given label key has taken across a rule's
        history entries.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/overall_status:
    get:
      description: |-
        Returns the overall firing/inactive intervals for
        a rule over the selected range. Viewer gate.
      operationId: GetRuleHistoryOverallStatus
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - description: Start is the window start, unix milliseconds. Required by the
          runtime.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the window end, unix milliseconds. Required by the runtime.
        in: query
        name: end
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryOverallStatusOut'
          description: ok
      summary: Returns the overall firing/inactive intervals for a rule over the selected
        range.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Returns the overall firing/inactive windows for a
        rule, for the posted query range. Viewer gate.
      operationId: GetOverallStateTransitions
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRuleHistoryQueryIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOverallStateTransitionsOut'
          description: ok
      summary: Returns the overall firing/inactive windows for a rule, for the posted
        query range.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/stats:
    get:
      description: |-
        Returns trigger and resolution statistics for a rule over
        the selected time range, current window against the prior one. Viewer gate.
      operationId: GetRuleHistoryStats
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - description: Start is the window start, unix milliseconds. Required by the
          runtime.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the window end, unix milliseconds. Required by the runtime.
        in: query
        name: end
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryStatsOut'
          description: ok
      summary: Returns trigger and resolution statistics for a rule over the selected
        time range, current window against the prior one.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Returns trigger and resolution statistics for a rule, current
        window against the prior one, for the posted query range. Viewer gate.
      operationId: GetRuleStats
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRuleHistoryQueryIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleStatsOut'
          description: ok
      summary: Returns trigger and resolution statistics for a rule, current window
        against the prior one, for the posted query range.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/timeline:
    get:
      description: |-
        Returns paginated timeline entries for a rule's state
        transitions, filterable by state and a label expression, cursor-paginated. Viewer gate.
      operationId: GetRuleHistoryTimeline
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - description: Start is the window start, unix milliseconds. Required by the
          runtime.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the window end, unix milliseconds. Required by the runtime.
        in: query
        name: end
        required: false
        schema:
          type: integer
      - description: State keeps only entries in one alert state, e.g. firing or normal.
        in: query
        name: state
        required: false
        schema:
          type: string
      - description: FilterExpression narrows entries to those whose labels match
          it.
        in: query
        name: filterExpression
        required: false
        schema:
          type: string
      - description: Limit caps how many entries come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Order sorts by time, asc or desc.
        in: query
        name: order
        required: false
        schema:
          type: string
      - description: Cursor resumes a previous page; opaque, returned as nextCursor.
        in: query
        name: cursor
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryTimelineOut'
          description: ok
      summary: Returns paginated timeline entries for a rule's state transitions,
        filterable by state and a label expression, cursor-paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Returns a rule's state-transition timeline for the posted
        query range, each entry carrying its related-logs or related-traces link. Viewer gate.
      operationId: GetRuleStateHistory
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRuleHistoryQueryIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleStateTimelineOut'
          description: ok
      summary: Returns a rule's state-transition timeline for the posted query range,
        each entry carrying its related-logs or related-traces link.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/{id}/history/top_contributors:
    get:
      description: |-
        Returns the label combinations that contributed
        most to a rule firing over the selected range. Viewer gate.
      operationId: GetRuleHistoryTopContributors
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - description: Start is the window start, unix milliseconds. Required by the
          runtime.
        in: query
        name: start
        required: false
        schema:
          type: integer
      - description: End is the window end, unix milliseconds. Required by the runtime.
        in: query
        name: end
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleHistoryContributorsOut'
          description: ok
      summary: Returns the label combinations that contributed most to a rule firing
        over the selected range.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Returns the label combinations that
        contributed most to a rule firing, for the posted query range. Viewer gate.
      operationId: GetRuleStateHistoryTopContributors
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRuleHistoryQueryIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRuleStateContributorsOut'
          description: ok
      summary: Returns the label combinations that contributed most to a rule firing,
        for the posted query range.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/rules/test:
    post:
      description: |-
        Fires a test notification for a rule definition without saving it,
        answering with how many series would alert. Editor gate.
      operationId: TestRule
      requestBody:
        content:
          application/json:
            schema: {}
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTestRuleOut'
          description: ok
      summary: Fires a test notification for a rule definition without saving it,
        answering with how many series would alert.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service/entry_point_operations:
    post:
      description: |-
        Returns one service's entry-point operations with the
        same latency and error profile topOperations reports.
      operationId: post_o11y_service_entry_point_operations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yOperationsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOperationsOut'
          description: ok
      summary: Returns one service's entry-point operations with the same latency
        and error profile topOperations reports.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service/top_level_operations:
    post:
      description: |-
        Maps each service to its entry-point span names — for the
        one service named in the request, or for every service when none is.
      operationId: post_o11y_service_top_level_operations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yTopLevelOpsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  items:
                    type: string
                  type: array
                type: object
          description: ok
      summary: Maps each service to its entry-point span names — for the one service
        named in the request, or for every service when none is.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service/top_operations:
    post:
      description: |-
        Returns one service's heaviest operations in the window, each
        with p50/p95/p99 latency, how often it ran and how often it errored.
      operationId: post_o11y_service_top_operations
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yOperationsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOperationsOut'
          description: ok
      summary: Returns one service's heaviest operations in the window, each with
        p50/p95/p99 latency, how often it ran and how often it errored.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts:
    get:
      description: Lists the caller's org's service accounts.
      operationId: ListServiceAccounts
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceAccountsOut'
          description: ok
      summary: Lists the caller's org's service accounts.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a service account in the caller's org,
        answering its id. The name — a lowercase letter followed by lowercase
        letters, digits or hyphens — becomes the account's email local part.
      operationId: CreateServiceAccount
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yServiceAccountCreateIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceAccountCreateOut'
          description: created
      summary: Creates a service account in the caller's org, answering its id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/{id}:
    delete:
      description: |-
        Deletes a service account and revokes every key it
        holds.
      operationId: DeleteServiceAccount
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a service account and revokes every key it holds.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: Returns one service account with the roles it holds.
      operationId: GetServiceAccount
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceAccountOut'
          description: ok
      summary: Returns one service account with the roles it holds.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Renames a service account.
      operationId: UpdateServiceAccount
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yServiceAccountUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Renames a service account.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/{id}/keys:
    get:
      description: |-
        Lists a service account's API keys — metadata only,
        never the secrets.
      operationId: ListServiceAccountKeys
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAPIKeysOut'
          description: ok
      summary: Lists a service account's API keys — metadata only, never the secrets.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Mints an API key for a service account and answers
        the key's id and its secret — the one time the secret is ever shown.

        ExpiresAt is a unix timestamp in seconds; zero means the key never expires,
        and a timestamp in the past is refused.
      operationId: CreateServiceAccountKey
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yAPIKeyCreateIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAPIKeyCreateOut'
          description: created
      summary: Mints an API key for a service account and answers the key's id and
        its secret — the one time the secret is ever shown.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/{id}/keys/{fid}:
    delete:
      description: |-
        Revokes an API key. Revocation is immediate and
        permanent.
      operationId: RevokeServiceAccountKey
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: fid
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Revokes an API key.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Renames an API key or moves its expiry.
      operationId: UpdateServiceAccountKey
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: fid
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yAPIKeyUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Renames an API key or moves its expiry.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/{id}/roles:
    get:
      description: Lists the roles a service account holds.
      operationId: GetServiceAccountRoles
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceAccountRolesOut'
          description: ok
      summary: Lists the roles a service account holds.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Assigns a role, named by its id, to a service
        account.
      operationId: CreateServiceAccountRole
      parameters:
      - description: RoleID is the id of the role to assign. Required.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yServiceAccountRoleGrantIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Assigns a role, named by its id, to a service account.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/{id}/roles/{rid}:
    delete:
      description: Removes a role from a service account.
      operationId: DeleteServiceAccountRole
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: rid
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a role from a service account.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/service_accounts/me:
    get:
      description: |-
        Returns the calling service account itself, with the
        roles it holds — the self-inspection read for a key-authenticated caller.
      operationId: GetMyServiceAccount
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServiceAccountOut'
          description: ok
      summary: Returns the calling service account itself, with the roles it holds
        — the self-inspection read for a key-authenticated caller.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Renames the calling service account.
      operationId: UpdateMyServiceAccount
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yMyServiceAccountUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Renames the calling service account.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/services:
    post:
      description: |-
        Lists the instrumented services seen in the window, each with the
        request profile of its entry-point spans: p99 and average latency, call and
        error rates, and the entry-point operations the numbers were computed over.
      operationId: post_o11y_services
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yServicesIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yServicesOut'
          description: ok
      summary: 'Lists the instrumented services seen in the window, each with the
        request profile of its entry-point spans: p99 and average latency, call and
        error rates, and the entry-point operations the numbers were computed over.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/services/list:
    get:
      description: |-
        Lists the name of every service the trace store holds, with no
        window applied — the complete catalog, for pickers and autocomplete.
      operationId: get_o11y_services_list
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  type: string
                type: array
          description: ok
      summary: Lists the name of every service the trace store holds, with no window
        applied — the complete catalog, for pickers and autocomplete.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/sessions:
    delete:
      description: |-
        Signs the calling session out, invalidating its tokens. The
        access token on the call names the session to end.
      operationId: DeleteSession
      responses:
        "204":
          description: no content
      summary: Signs the calling session out, invalidating its tokens.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Answers the caller org's LLM-observability sessions — traces grouped by session id on the gen_ai span plane — paged by limit and offset, in the runtime's own envelope, passed through unchanged.

        An org-less caller is refused HERE, at the cloud boundary, before the request reaches the runtime, and the org the runtime then scopes on is that SAME validated tenant. The two cannot disagree: the tenant is minted from the principal's own claim at ingress and a client copy never survives it.

        There is deliberately no session-detail route to pair with this. The runtime serves the list only; detail is composed client-side from this list plus the traces filtered by session, so a caller looking for one is looking for something that was never served rather than something that broke.
      operationId: get_o11y_sessions
      summary: List the caller org's LLM sessions
      tags:
      - o11y
      x-app: o11y
      x-public: true
  /v1/o11y/sessions/context:
    get:
      description: |-
        Tells a sign-in page what an email address can do: which
        orgs the address belongs to and, per org, which password and SSO routes are
        open to it. Unauthenticated: it runs before any session exists.
      operationId: GetSessionContext
      parameters:
      - description: Email is the address about to sign in. Required.
        in: query
        name: email
        required: false
        schema:
          type: string
      - description: Ref is the page the sign-in started from, carried into SSO redirects.
        in: query
        name: ref
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySessionContextOut'
          description: ok
      summary: 'Tells a sign-in page what an email address can do: which orgs the
        address belongs to and, per org, which password and SSO routes are open to
        it.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/sessions/email_password:
    post:
      description: |-
        Signs a user in with email and password and
        answers with the session's token pair. Unauthenticated: this call is how
        authentication begins.
      operationId: CreateSessionByEmailPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yEmailPasswordSessionIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTokenOut'
          description: ok
      summary: Signs a user in with email and password and answers with the session's
        token pair.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/sessions/rotate:
    post:
      description: |-
        Exchanges a refresh token for a fresh token pair, retiring the
        old pair. The access token being rotated identifies the session.
      operationId: RotateSession
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRotateSessionIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTokenOut'
          description: ok
      summary: Exchanges a refresh token for a fresh token pair, retiring the old
        pair.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/settings/apdex:
    get:
      description: |-
        Returns apdex settings for the named services.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_settings_apdex
      parameters:
      - description: Services are the service names, comma separated.
        in: query
        name: services
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yApdexOut'
          description: ok
      summary: Returns apdex settings for the named services.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Sets one service's apdex threshold and the status codes excluded
        from its score.

        Admin only, as the mux tree has always gated it (AdminAccess); the runtime's
        own gate enforces it.
      operationId: post_o11y_settings_apdex
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yApdexSetIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yApdexSetOut'
          description: ok
      summary: Sets one service's apdex threshold and the status codes excluded from
        its score.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/settings/ttl:
    get:
      description: |-
        Returns the org's current retention policy: default TTL, custom
        per-label rules, and cold-storage settings where configured.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_settings_ttl
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRetentionOut'
          description: ok
      summary: 'Returns the org''s current retention policy: default TTL, custom per-label
        rules, and cold-storage settings where configured.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Sets the org's retention policy for one signal: the default TTL
        in days, ordered per-label retention rules, and optional cold-storage
        settings.

        Admin only, as the mux tree has always gated it (AdminAccess); the runtime's
        own gate enforces it.
      operationId: post_o11y_settings_ttl
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yRetentionSetIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRetentionSetOut'
          description: ok
      summary: 'Sets the org''s retention policy for one signal: the default TTL in
        days, ordered per-label retention rules, and optional cold-storage settings.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/span_mapper_groups:
    get:
      description: |-
        Lists the caller's org's mapping groups, optionally only the
        enabled ones.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListSpanMapperGroups
      parameters:
      - in: query
        name: enabled
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySpanMapperGroupsOut'
          description: ok
      summary: Lists the caller's org's mapping groups, optionally only the enabled
        ones.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a mapping group: the name it is known by, the
        span and resource attributes whose presence selects a span into it, and
        whether it is on.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: CreateSpanMapperGroup
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableSpanMapperGroup'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySpanMapperGroupOut'
          description: created
      summary: 'Creates a mapping group: the name it is known by, the span and resource
        attributes whose presence selects a span into it, and whether it is on.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/span_mapper_groups/{groupId}:
    delete:
      description: |-
        Deletes a mapping group and every mapper under it.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: DeleteSpanMapperGroup
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a mapping group and every mapper under it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes a group's name, condition or enabled state.
        Every field is optional and only the ones sent are applied.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: UpdateSpanMapperGroup
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySpanMapperGroupUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes a group's name, condition or enabled state.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/span_mapper_groups/{groupId}/span_mappers:
    get:
      description: |-
        Lists the mappers belonging to one group, in the order they are
        applied.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListSpanMappers
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySpanMappersOut'
          description: ok
      summary: Lists the mappers belonging to one group, in the order they are applied.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds a mapper to a group: which field context it reads, the
        move or copy it performs, and whether it is on.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: CreateSpanMapper
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySpanMapperCreateIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySpanMapperOut'
          description: created
      summary: 'Adds a mapper to a group: which field context it reads, the move or
        copy it performs, and whether it is on.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/span_mapper_groups/{groupId}/span_mappers/{mapperId}:
    delete:
      description: |-
        Deletes one mapper from a group.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: DeleteSpanMapper
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      - in: path
        name: mapperId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one mapper from a group.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes a mapper's field context, config or enabled state.
        Every field is optional and only the ones sent are applied.

        Callers need the admin role; the runtime's own gate enforces it.
      operationId: UpdateSpanMapper
      parameters:
      - in: path
        name: groupId
        required: true
        schema:
          type: string
      - in: path
        name: mapperId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySpanMapperUpdateIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Changes a mapper's field context, config or enabled state.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/span_percentile:
    post:
      description: |-
        Places one span's duration among its peers: the p50/p90/p99
        durations of like spans, and the percentile the given duration lands at.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_span_percentile
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySpanPercentileIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySpanPercentileOut'
          description: ok
      summary: 'Places one span''s duration among its peers: the p50/p90/p99 durations
        of like spans, and the percentile the given duration lands at.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/statefulsets/attribute_keys:
    get:
      description: |-
        Lists the metric attribute keys Kubernetes
        statefulsets report, for building statefulset filters.
      operationId: get_o11y_statefulsets_attribute_keys
      parameters:
      - description: |-
          DataSource is the telemetry the keys come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the keys will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the keys must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: SearchText narrows the keys to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: |-
          TagType narrows the keys to one kind — tag or resource. Empty means all;
          an invalid value reads as empty.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many keys come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeKeysOut'
          description: ok
      summary: Lists the metric attribute keys Kubernetes statefulsets report, for
        building statefulset filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/statefulsets/attribute_values:
    get:
      description: |-
        Lists the values one statefulset attribute key
        has taken, for building statefulset filters.
      operationId: get_o11y_statefulsets_attribute_values
      parameters:
      - description: |-
          DataSource is the telemetry the values come from — metrics for the infra
          faces. The runtime requires it.
        in: query
        name: dataSource
        required: false
        schema:
          type: string
      - description: |-
          AggregateOperator is the aggregation the values will be used under, e.g.
          noop, count, avg. The runtime requires it for non-metrics sources.
        in: query
        name: aggregateOperator
        required: false
        schema:
          type: string
      - description: AggregateAttribute is the metric the values must appear on.
        in: query
        name: aggregateAttribute
        required: false
        schema:
          type: string
      - description: AttributeKey is the key whose values to list.
        in: query
        name: attributeKey
        required: false
        schema:
          type: string
      - description: |-
          FilterAttributeKeyDataType is the key's data type — string, int64,
          float64 or bool. Empty means unspecified.
        in: query
        name: filterAttributeKeyDataType
        required: false
        schema:
          type: string
      - description: SearchText narrows the values to those containing it.
        in: query
        name: searchText
        required: false
        schema:
          type: string
      - description: TagType narrows the search to one kind of key — tag or resource.
        in: query
        name: tagType
        required: false
        schema:
          type: string
      - description: Limit caps how many values come back. Absent means 50.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yInfraAttributeValuesOut'
          description: ok
      summary: Lists the values one statefulset attribute key has taken, for building
        statefulset filters.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/statefulsets/list:
    post:
      description: |-
        Lists Kubernetes statefulsets over a time range, each with
        the CPU and memory its pods used against request and limit, desired and
        available replica counts, restarts and attributes; filterable, groupable and
        paginated.
      operationId: post_o11y_statefulsets_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.StatefulSetListRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yStatefulSetListOut'
          description: ok
      summary: Lists Kubernetes statefulsets over a time range, each with the CPU
        and memory its pods used against request and limit, desired and available
        replica counts, restarts and attributes; filterable, groupable and paginated.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/stats:
    get:
      description: |-
        Returns the collected usage statistics for the caller's org, as the
        stats reporter aggregates them — a map whose keys are the reporter's own
        counter names.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_stats
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yOrgStatsOut'
          description: ok
      summary: Returns the collected usage statistics for the caller's org, as the
        stats reporter aggregates them — a map whose keys are the reporter's own counter
        names.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/status:
    get:
      description: |-
        Reports whether a product's service is live: an in-cluster
        health probe with its measured latency, fused with the per-replica up
        inventory. Infra health is not tenant-partitioned — a service is up or down
        for everyone — so any validated caller is served, but an unvalidated one is
        refused. A product with no backing workload answers down/unknown-service
        without probing anything; a malformed slug is a 400.
      operationId: get_o11y_status
      parameters:
      - description: Product is the console product slug to probe, e.g. "kms". Required.
        example: kms
        in: query
        name: product
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.statusResult'
          description: ok
      summary: 'Reports whether a product''s service is live: an in-cluster health
        probe with its measured latency, fused with the per-replica up inventory.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/substitute_vars:
    post:
      description: |-
        Substitutes a query's variables and returns the
        resolved request, without running it — what a dashboard does before it
        queries.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_substitute_vars
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.QueryRangeRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySubstituteVarsOut'
          description: ok
      summary: Substitutes a query's variables and returns the resolved request, without
        running it — what a dashboard does before it queries.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/summary:
    get:
      description: |-
        Reports whether the platform is up. It returns the public status
        document: the incidents currently open against Hanzo's own services, derived
        from the fleet health probes, plus the address of the human status page. No
        authentication is required and no tenant data is involved — the answer is the
        same for every caller.

        A service that fails its health probe becomes one incident naming that service.
        When the availability source itself cannot be read the endpoint answers 503
        rather than an empty incident list, because "we cannot tell" and "everything is
        fine" are different answers and only one of them is true.
      operationId: get_o11y_summary
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.StatusSummary'
          description: ok
      summary: Reports whether the platform is up.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/testChannel:
    post:
      description: |-
        Sends a test notification to the posted receiver. The
        legacy path; prefer /channels/test. Editor gate.
      operationId: TestChannelDeprecated
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.alertmanagertypes.Receiver'
        required: true
      responses:
        "204":
          description: no content
      summary: Sends a test notification to the posted receiver.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/testRule:
    post:
      description: |-
        Fires a test notification for the posted rule definition
        and answers with how many series alerted and a status message. The legacy
        path; prefer /rules/test. Editor gate.
      operationId: TestRuleNotification
      requestBody:
        content:
          application/json:
            schema: {}
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTestNotificationOut'
          description: ok
      summary: Fires a test notification for the posted rule definition and answers
        with how many series alerted and a status message.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/third-party-apis/overview/domain:
    post:
      description: |-
        Returns one external domain's endpoint-level breakdown — each
        endpoint with its rate, error and latency columns over the window.
      operationId: post_o11y_third-party-apis_overview_domain
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDomainsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDomainsOut'
          description: ok
      summary: Returns one external domain's endpoint-level breakdown — each endpoint
        with its rate, error and latency columns over the window.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/third-party-apis/overview/list:
    post:
      description: |-
        Lists the external domains the instrumented services call, with
        request rate, error percentage and latency per domain. Rows whose domain is
        a bare IP address are dropped unless show_ip asks for them.
      operationId: post_o11y_third-party-apis_overview_list
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDomainsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDomainsOut'
          description: ok
      summary: Lists the external domains the instrumented services call, with request
        rate, error percentage and latency per domain.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}:
    delete:
      description: |-
        Deletes a funnel. The answer carries no data — the runtime
        acknowledges with the success envelope alone, which is what this Out says.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: DeleteTraceFunnel
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelDeleteOut'
          description: ok
      summary: Deletes a funnel.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one funnel with its steps.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetTraceFunnel
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelOut'
          description: ok
      summary: Returns one funnel with its steps.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Renames a funnel or rewrites its description, answering the
        funnel as it now stands.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UpdateTraceFunnel
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelOut'
          description: ok
      summary: Renames a funnel or rewrites its description, answering the funnel
        as it now stands.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/error-traces:
    post:
      description: |-
        Returns the errored traces through a step transition of a
        saved funnel — the entry point for "why is this step failing".
      operationId: GetTraceFunnelErrorTraces
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelStepWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the errored traces through a step transition of a saved funnel
        — the entry point for "why is this step failing".
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/overview:
    post:
      description: |-
        Returns a saved funnel's conversion overview over a window:
        how many entered, how many converted, the rate and the latency.
      operationId: GetTraceFunnelOverview
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelStepWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: 'Returns a saved funnel''s conversion overview over a window: how many
        entered, how many converted, the rate and the latency.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/slow-traces:
    post:
      description: |-
        Returns the slowest traces through a step transition of a
        saved funnel — the entry point for "why is this step slow".
      operationId: GetTraceFunnelSlowTraces
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelStepWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the slowest traces through a step transition of a saved funnel
        — the entry point for "why is this step slow".
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/steps:
    post:
      description: |-
        Returns a saved funnel's per-step metrics over a window — the
        counts and latencies at each step, in step order.
      operationId: GetTraceFunnelStepMetrics
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns a saved funnel's per-step metrics over a window — the counts
        and latencies at each step, in step order.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/steps/overview:
    post:
      description: |-
        Returns the conversion between two named steps of a saved
        funnel — the step-to-step drill-down behind the overview.
      operationId: GetTraceFunnelStepOverview
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelStepWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the conversion between two named steps of a saved funnel —
        the step-to-step drill-down behind the overview.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/{funnel_id}/analytics/validate:
    post:
      description: |-
        Lists the traces that match a saved funnel over a window — the
        read that answers "is this funnel finding anything at all".
      operationId: ValidateTraceFunnelTraces
      parameters:
      - in: path
        name: funnel_id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelWindowIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Lists the traces that match a saved funnel over a window — the read
        that answers "is this funnel finding anything at all".
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/error-traces:
    post:
      description: |-
        Returns the errored traces through a step transition of
        a funnel described inline.
      operationId: GetDraftFunnelErrorTraces
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the errored traces through a step transition of a funnel described
        inline.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/overview:
    post:
      description: |-
        Returns the conversion overview of a funnel described
        inline.
      operationId: GetDraftFunnelOverview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the conversion overview of a funnel described inline.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/slow-traces:
    post:
      description: |-
        Returns the slowest traces through a step transition of
        a funnel described inline.
      operationId: GetDraftFunnelSlowTraces
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the slowest traces through a step transition of a funnel described
        inline.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/steps:
    post:
      description: Returns the per-step metrics of a funnel described inline.
      operationId: GetDraftFunnelStepMetrics
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the per-step metrics of a funnel described inline.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/steps/overview:
    post:
      description: |-
        Returns the conversion between two steps of a funnel
        described inline.
      operationId: GetDraftFunnelStepOverview
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Returns the conversion between two steps of a funnel described inline.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/analytics/validate:
    post:
      description: |-
        Lists the traces that match a funnel described inline —
        the builder's "try this" before anything is saved.
      operationId: ValidateDraftFunnelTraces
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDraftFunnelIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelRowsOut'
          description: ok
      summary: Lists the traces that match a funnel described inline — the builder's
        "try this" before anything is saved.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/list:
    get:
      description: |-
        Lists the caller's org's funnels, each with its steps and who last
        touched it.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListTraceFunnels
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelsOut'
          description: ok
      summary: Lists the caller's org's funnels, each with its steps and who last
        touched it.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/new:
    post:
      description: |-
        Creates an empty funnel with a name, answering the funnel it
        created. Steps are added afterwards with the steps update.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: CreateTraceFunnel
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelCreateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelOut'
          description: ok
      summary: Creates an empty funnel with a name, answering the funnel it created.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/trace-funnels/steps/update:
    put:
      description: |-
        Replaces a funnel's steps — the funnel is named in the body
        rather than the path — and answers the funnel as it now stands. A name or
        description sent alongside is applied too; an empty one leaves it as it was.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UpdateTraceFunnelSteps
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFunnelStepsUpdateIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFunnelOut'
          description: ok
      summary: Replaces a funnel's steps — the funnel is named in the body rather
        than the path — and answers the funnel as it now stands.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces:
    get:
      description: |-
        Lists the caller org's recent traces — one row per trace with
        its span count and wall-clock duration, most recently active first. This is
        the trace SEARCH: it is where a trace id comes from, and the spans behind any
        row are then read from GET /v1/o11y/traces/{traceId}. Every row belongs to the
        caller's own org — the tenant is the validated principal, never an input, and
        there is no administrator widening, because a trace list is a tenant's records
        rather than a rollup over them. An unreachable telemetry store answers 503
        rather than an empty page, because "no traces" and "cannot see the traces" are
        different facts and only one of them is about the caller's system.
      operationId: get_o11y_traces
      parameters:
      - description: |-
          Range is the window in seconds, counted back from now over each trace's
          last activity. Default 3600, capped at 604800 (7d).
        example: 3600
        in: query
        name: range
        required: false
        schema:
          type: integer
      - description: Limit is how many traces to return. Default 50, capped at 500.
        example: 50
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          MinDurationMs keeps only traces that lasted at least this many
          milliseconds. Zero or absent keeps every trace in the window.
        in: query
        name: minDurationMs
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.tracesOut'
          description: ok
      summary: Lists the caller org's recent traces — one row per trace with its span
        count and wall-clock duration, most recently active first.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces/{traceId}:
    get:
      description: |-
        Returns one trace's spans as a column/row table, optionally
        centred on a span and walked a fixed number of levels up and down from it —
        the read the trace explorer opens a trace with.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: SearchTraces
      parameters:
      - in: path
        name: traceId
        required: true
        schema:
          type: string
      - in: query
        name: spanId
        required: false
        schema:
          type: string
      - in: query
        name: levelUp
        required: false
        schema:
          type: integer
      - in: query
        name: levelDown
        required: false
        schema:
          type: integer
      - in: query
        name: spanRenderLimit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/o11y.O11yTraceSpanWindow'
                type: array
          description: ok
      summary: Returns one trace's spans as a column/row table, optionally centred
        on a span and walked a fixed number of levels up and down from it — the read
        the trace explorer opens a trace with.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces/{traceId}/aggregations:
    post:
      description: |-
        Computes span aggregations over one trace — span count,
        duration or share of execution time — grouped by the resource field each
        aggregation names.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetTraceAggregations
      parameters:
      - in: path
        name: traceId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yTraceAggregationsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTraceAggregationsOut'
          description: ok
      summary: Computes span aggregations over one trace — span count, duration or
        share of execution time — grouped by the resource field each aggregation names.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces/{traceId}/flamegraph:
    post:
      description: |-
        Returns a trace's flamegraph: spans bucketed by depth level,
        each level ordered as it is drawn, around the selected span.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetFlamegraph
      parameters:
      - in: path
        name: traceId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yTraceFlamegraphIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTraceFlamegraphOut'
          description: ok
      summary: 'Returns a trace''s flamegraph: spans bucketed by depth level, each
        level ordered as it is drawn, around the selected span.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces/{traceId}/waterfall:
    post:
      description: |-
        Returns a trace's waterfall: every span when the trace is
        small enough, a capped window around the selected span when it is not, with
        the uncollapsed subtrees the caller asked to keep open.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetWaterfallV4
      parameters:
      - in: path
        name: traceId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yTraceWaterfallIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTraceWaterfallOut'
          description: ok
      summary: 'Returns a trace''s waterfall: every span when the trace is small enough,
        a capped window around the selected span when it is not, with the uncollapsed
        subtrees the caller asked to keep open.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/traces/fields:
    get:
      description: |-
        Returns the trace field catalog: the span fields already selected
        as indexed columns, and the interesting ones seen in the data that could be.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: GetTraceFields
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldCatalogOut'
          description: ok
      summary: 'Returns the trace field catalog: the span fields already selected
        as indexed columns, and the interesting ones seen in the data that could be.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Changes how one span field is stored — selects or deselects
        it as a materialized column and tunes its index — and echoes the setting back.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: UpdateTraceField
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yFieldSetting'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yFieldSetting'
          description: ok
      summary: Changes how one span field is stored — selects or deselects it as a
        materialized column and tunes its index — and echoes the setting back.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/usage:
    get:
      description: |-
        Returns ingestion usage counts bucketed over the requested window,
        optionally narrowed to one service.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_o11y_usage
      parameters:
      - description: Start is the window start, as epoch nanoseconds. Required.
        in: query
        name: start
        required: true
        schema:
          type: string
      - description: End is the window end, as epoch nanoseconds. Required.
        in: query
        name: end
        required: true
        schema:
          type: string
      - description: Step is the bucket width in seconds. The runtime requires it.
        in: query
        name: step
        required: false
        schema:
          type: integer
      - description: Service narrows usage to one service. Empty covers all.
        in: query
        name: service
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/o11y.O11yUsageItem'
                type: array
          description: ok
      summary: Returns ingestion usage counts bucketed over the requested window,
        optionally narrowed to one service.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/user:
    get:
      description: |-
        Lists the org's members with their single legacy role.
        Deprecated in favor of listUsers, which answers without the role. Admin gate.
      operationId: ListUsersDeprecated
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDeprecatedUsersOut'
          description: ok
      summary: Lists the org's members with their single legacy role.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/user/{id}:
    delete:
      description: |-
        Removes one org member, by user id. The same operation
        as deleteUser on the legacy singular path. Admin gate.
      operationId: DeleteUserDeprecated
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one org member, by user id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one org member with their single legacy role, by
        user id. Admins may read anyone; a non-admin only themselves (the runtime's
        self-access gate).
      operationId: GetUserDeprecated
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDeprecatedUserOut'
          description: ok
      summary: Returns one org member with their single legacy role, by user id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Renames one org member and may move their legacy role,
        answering with the updated record. Admins may update anyone; a non-admin
        only themselves (the runtime's self-access gate).
      operationId: UpdateUserDeprecated
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDeprecatedUserUpdate'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDeprecatedUserOut'
          description: ok
      summary: Renames one org member and may move their legacy role, answering with
        the updated record.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/user/me:
    get:
      description: |-
        Returns the calling user with their single legacy role.
        Deprecated in favor of getMyUser. Open to any authenticated caller.
      operationId: GetMyUserDeprecated
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDeprecatedUserOut'
          description: ok
      summary: Returns the calling user with their single legacy role.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/user/preferences:
    get:
      description: |-
        Lists every preference of the calling user, each with
        its current and default value. Viewer gate.
      operationId: ListUserPreferences
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPreferencesOut'
          description: ok
      summary: Lists every preference of the calling user, each with its current and
        default value.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/user/preferences/{name}:
    get:
      description: |-
        Returns one preference of the calling user, by name.
        Viewer gate.
      operationId: GetUserPreference
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yPreferenceOut'
          description: ok
      summary: Returns one preference of the calling user, by name.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Sets one preference of the calling user, by name.
        Viewer gate.
      operationId: UpdateUserPreference
      parameters:
      - in: path
        name: name
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdatablePreference'
        required: true
      responses:
        "204":
          description: no content
      summary: Sets one preference of the calling user, by name.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users:
    get:
      description: Lists the caller's org members. Admin gate.
      operationId: ListUsers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yUsersOut'
          description: ok
      summary: Lists the caller's org members.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a member of the caller's org in the pending-invite state
        and mails them their invitation; the answer is the new user's id. Admin gate.
      operationId: CreateUser
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yPostableUser'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yCreatedOut'
          description: created
      summary: Creates a member of the caller's org in the pending-invite state and
        mails them their invitation; the answer is the new user's id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/{id}:
    delete:
      description: Removes one org member, by user id. Admin gate.
      operationId: DeleteUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one org member, by user id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one org member together with every role they hold, by user
        id. Admin gate.
      operationId: GetUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yUserWithRolesOut'
          description: ok
      summary: Returns one org member together with every role they hold, by user
        id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Renames one org member, by user id — someone else, never the
        caller, who renames themselves through updateMyUser. Admin gate.
      operationId: UpdateUser
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUserUpdate'
        required: true
      responses:
        "204":
          description: no content
      summary: Renames one org member, by user id — someone else, never the caller,
        who renames themselves through updateMyUser.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/{id}/reset_password_tokens:
    get:
      description: |-
        Returns the reset-password token a user already has; absent
        one, the answer is a not-found rather than a fresh token. Admin gate.
      operationId: GetResetPasswordToken
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yResetTokenOut'
          description: ok
      summary: Returns the reset-password token a user already has; absent one, the
        answer is a not-found rather than a fresh token.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Creates or regenerates a user's reset-password token: a
        live token is returned as it is, an expired one is replaced. Admin gate.
      operationId: CreateResetPasswordToken
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yResetTokenOut'
          description: created
      summary: 'Creates or regenerates a user''s reset-password token: a live token
        is returned as it is, an expired one is replaced.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/{id}/roles:
    get:
      description: |-
        Returns every role one org member holds, by user id. Admin
        gate.
      operationId: GetRolesByUserID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yRolesOut'
          description: ok
      summary: Returns every role one org member holds, by user id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Assigns a role, by role name, to one org member — someone else,
        never the caller. Admin gate.
      operationId: SetRoleByUserID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySetRoleIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yAck'
          description: ok
      summary: Assigns a role, by role name, to one org member — someone else, never
        the caller.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/{id}/roles/{roleId}:
    delete:
      description: |-
        Takes a role away from one org member, by user id and role
        id — someone else, never the caller. Admin gate.
      operationId: RemoveUserRoleByUserIDAndRoleID
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: roleId
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Takes a role away from one org member, by user id and role id — someone
        else, never the caller.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/me:
    get:
      description: |-
        Returns the calling user together with every role they hold. Open
        to any authenticated caller.
      operationId: GetMyUser
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yUserWithRolesOut'
          description: ok
      summary: Returns the calling user together with every role they hold.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: Renames the calling user. Open to any authenticated caller.
      operationId: UpdateMyUserV2
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yUpdatableUser'
        required: true
      responses:
        "204":
          description: no content
      summary: Renames the calling user.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/me/dashboards:
    get:
      description: |-
        Is dashboardListV2 personalized for the calling user:
        each dashboard carries the caller's pinned state, and pinned dashboards float to
        the top of the requested ordering. Supports the same filter DSL, sort, order and
        pagination.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: ListDashboardsForUserV2
      parameters:
      - description: |-
          Query is the filter DSL over dashboard columns and tags, e.g.
          `name:cpu source:user`. Empty lists everything.
        in: query
        name: query
        required: false
        schema:
          type: string
      - description: |-
          Sort is the sort field: updated_at, created_at or name. Empty sorts by
          updated_at.
        in: query
        name: sort
        required: false
        schema:
          type: string
      - description: 'Order is the sort direction: asc or desc. Empty orders desc.'
        in: query
        name: order
        required: false
        schema:
          type: string
      - description: |-
          Limit caps how many dashboards come back. Zero means the default of 20;
          the runtime caps it at 200.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Offset is how many dashboards to skip for pagination.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardListForUserOut'
          description: ok
      summary: 'Is dashboardListV2 personalized for the calling user: each dashboard
        carries the caller''s pinned state, and pinned dashboards float to the top
        of the requested ordering.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/me/dashboards/{id}/pins:
    delete:
      description: |-
        Removes the caller's pin for a dashboard. Idempotent —
        unpinning a dashboard that was not pinned still succeeds.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: UnpinDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes the caller's pin for a dashboard.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Pins a dashboard for the calling user. A user can pin at most ten
        dashboards; pinning at the limit refuses with the runtime's conflict. Re-pinning
        an already-pinned dashboard is a no-op success. Pinning mutates only the caller's
        pin list, not the dashboard, so a viewer may pin what a viewer may read.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: PinDashboardV2
      parameters:
      - description: ID is the resource id from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Pins a dashboard for the calling user.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/users/me/factor_password:
    put:
      description: |-
        Replaces the calling user's password, refusing when the old
        one does not match. Open to any authenticated caller.
      operationId: UpdateMyPassword
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yChangePasswordIn'
        required: true
      responses:
        "204":
          description: no content
      summary: Replaces the calling user's password, refusing when the old one does
        not match.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/variables/query:
    post:
      description: |-
        Evaluates a dashboard variable query and returns the values the
        variable may take.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: post_o11y_variables_query
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDashboardVarsIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDashboardVarsOut'
          description: ok
      summary: Evaluates a dashboard variable query and returns the values the variable
        may take.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/version:
    get:
      description: |-
        Reports the running build: its version, whether an enterprise
        edition is present ("N" in this build), and whether first-user setup has
        completed.

        Open by design; the runtime's own gate is OpenAccess.
      operationId: get_o11y_version
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yVersionOut'
          description: ok
      summary: 'Reports the running build: its version, whether an enterprise edition
        is present ("N" in this build), and whether first-user setup has completed.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/zeus/hosts:
    get:
      description: Returns the deployment's host info from Zeus. Viewer gate.
      operationId: GetHosts
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yGettableHostOut'
          description: ok
      summary: Returns the deployment's host info from Zeus.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Records the deployment's host in Zeus, overwriting any prior one.
        Admin gate.
      operationId: PutHost
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableHost'
        required: true
      responses:
        "204":
          description: no content
      summary: Records the deployment's host in Zeus, overwriting any prior one.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/o11y/zeus/profiles:
    put:
      description: |-
        Records the deployment's profile in Zeus — how the team uses
        observability today and what they plan — overwriting any prior one. Admin
        gate.
      operationId: PutProfile
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.PostableProfile'
        required: true
      responses:
        "204":
          description: no content
      summary: Records the deployment's profile in Zeus — how the team uses observability
        today and what they plan — overwriting any prior one.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/openapi.json:
    get:
      description: |-
        Serves the OpenAPI document for the routes this process actually answers — generated from the live router at request time, not from a checked-in file that can disagree with it.

        On an app it is that app's own surface; on the fleet's front door it is the woven document for every mounted app. Unauthenticated by design: a client has to be able to read the contract before it holds a credential, and the document grants nothing.

        Rendered once and served as bytes thereafter, so the route table's immutability is what makes a repeat request a memcpy rather than a re-encode of a megabyte document.
      operationId: get_openapi.json
      security: []
      summary: The API description this SDK was generated from
      tags:
      - openapi
      x-app: openapi
      x-public: true
  /v1/openapi/commands:
    get:
      description: |-
        The command projection of the OpenAPI document at /v1/openapi.json — each operation reduced to what running it by name needs: its service and command token, its method and path, the prose lifted from the handler, its path parameters as positional arguments and its remaining inputs as typed flags.

        It is a separate address for one measured reason: the fleet document is megabytes and a command palette cannot load it, while this projection of the same operations is several times smaller because it carries no schemas, responses or components.

        Unauthenticated by design, exactly as the document it derives from: a client has to be able to read the contract before it holds a credential, and a list of operation names grants nothing. The list is TOTAL and is never filtered by caller — what you may run is decided per request by the authorizer, on the decoded input, so a filtered list would be a second claim about permission that is free to be wrong.

        Rendered once and served as bytes thereafter, under a strong ETag.
      operationId: get_openapi_commands
      security: []
      summary: Every operation this API answers, as a command
      tags:
      - openapi
      x-app: openapi
      x-public: true
  /v1/plan:
    get:
      description: |-
        Returns the Hanzo cloud plan catalog: every cloud tier with its
        price, included capacity, limits and feature list, scoped to the caller's
        catalog. A reseller org sees its own overrides in place of the canonical
        records it has replaced, and the canonical record for every tier it has not.
      operationId: get_plan
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planList'
          description: ok
      summary: 'Returns the Hanzo cloud plan catalog: every cloud tier with its price,
        included capacity, limits and feature list, scoped to the caller''s catalog.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/blockchain:
    get:
      description: |-
        Returns the blockchain RPC plan catalog: the tiers metered
        in monthly compute units, with their prices, limits and overage terms. It is
        the canonical catalog for every caller — these plans carry no reseller
        overrides.
      operationId: get_plan_blockchain
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planList'
          description: ok
      summary: 'Returns the blockchain RPC plan catalog: the tiers metered in monthly
        compute units, with their prices, limits and overage terms.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/cloud:
    get:
      description: |-
        Returns the cloud plan catalog. It is the same section
        ListCloudPlans answers and a separate operation because it is a separate
        address, and an address is what every projection keys on.
      operationId: get_plan_cloud
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planList'
          description: ok
      summary: Returns the cloud plan catalog.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/dns:
    get:
      description: |-
        ListDNSPlans returns the DNS plan catalog: the tiers priced on zones, records
        per zone and queries per day. It is the canonical catalog for every caller —
        these plans carry no reseller overrides.
      operationId: get_plan_dns
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planList'
          description: ok
      summary: 'ListDNSPlans returns the DNS plan catalog: the tiers priced on zones,
        records per zone and queries per day.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/entitlements/{id}:
    get:
      description: |-
        Returns what one plan GRANTS and not what it costs: the
        canonical namespaced entitlement block and the flat license-feature list
        derived from it. It is the entitlement half of ResolvePlan, over the same
        catalog and the same 404 for an id no catalog holds — the read a licensing or
        quota gate makes.
      operationId: get_plan_entitlements_by_id
      parameters:
      - description: |-
          ID is the plan's catalog id or slug — "pro", "team", "world-enterprise",
          "rpc-growth". Both are matched, so a slug resolves the plan it names.
        example: team
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planEntitlements'
          description: ok
      summary: 'Returns what one plan GRANTS and not what it costs: the canonical
        namespaced entitlement block and the flat license-feature list derived from
        it.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/gpu:
    get:
      description: |-
        ListGPUTiers returns the rentable GPU configurations, each with its accelerator
        count and model, VRAM, vCPUs, host memory and hourly price.
      operationId: get_plan_gpu
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planTierList'
          description: ok
      summary: ListGPUTiers returns the rentable GPU configurations, each with its
        accelerator count and model, VRAM, vCPUs, host memory and hourly price.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/health:
    get:
      description: |-
        Health reports that the plans subsystem is mounted and serving. It answers from
        the process itself and consults neither the catalog bundle nor the goja host,
        so it stays "ok" while either is degraded.
      operationId: get_plan_health
      responses:
        "200":
          content:
            application/json:
              example:
                service: plans
                status: ok
              schema:
                $ref: '#/components/schemas/planHealth'
          description: ok
      summary: Health reports that the plans subsystem is mounted and serving.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/policy:
    get:
      description: |-
        Returns the published pricing policy: whether pricing is
        transparent, the revenue-sharing terms (idle compute resale and the open-source
        share) and the principles the catalog is priced by.
      operationId: get_plan_policy
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: ok
      summary: 'Returns the published pricing policy: whether pricing is transparent,
        the revenue-sharing terms (idle compute resale and the open-source share)
        and the principles the catalog is priced by.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/regions:
    get:
      description: |-
        Returns the regions cloud capacity is offered in, each with its
        display name and physical location.
      operationId: get_plan_regions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planRegionList'
          description: ok
      summary: Returns the regions cloud capacity is offered in, each with its display
        name and physical location.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/resolve/{id}:
    get:
      description: |-
        Resolves one plan to everything a consumer of the catalog needs at
        once: its canonical entitlement block, the flat license-feature list a signed
        license carries, its billing reference, and the catalog it came from. The id
        may be the plan's id or its slug, and it is resolved against the caller's
        catalog, so a reseller's override wins over the canonical record. An id no
        catalog holds answers 404.
      operationId: get_plan_resolve_by_id
      parameters:
      - description: |-
          ID is the plan's catalog id or slug — "pro", "team", "world-enterprise",
          "rpc-growth". Both are matched, so a slug resolves the plan it names.
        example: pro
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planResolution'
          description: ok
      summary: 'Resolves one plan to everything a consumer of the catalog needs at
        once: its canonical entitlement block, the flat license-feature list a signed
        license carries, its billing reference, and the catalog it came from.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/schema:
    get:
      description: |-
        Returns the two JSON Schema documents this surface speaks:
        entitlements.schema.json, which declares every entitlement key with its type,
        unit and enum, and plan.schema.json, which a catalog plan record conforms to.
      operationId: get_plan_schema
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planSchemas'
          description: ok
      summary: 'Returns the two JSON Schema documents this surface speaks: entitlements.schema.json,
        which declares every entitlement key with its type, unit and enum, and plan.schema.json,
        which a catalog plan record conforms to.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/storage:
    get:
      description: |-
        Returns the block-storage price block: the price per GB per
        month and the volume size bounds a cloud plan may attach.
      operationId: get_plan_storage
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties: {}
                type: object
          description: ok
      summary: 'Returns the block-storage price block: the price per GB per month
        and the volume size bounds a cloud plan may attach.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/subscriptions:
    get:
      description: |-
        Returns the subscription ladder — the personal and team
        tiers a customer buys to use the cloud, each with its monthly and annual price,
        seat rules, limits and billing reference. Scoped to the caller's catalog.
      operationId: get_plan_subscriptions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planList'
          description: ok
      summary: Returns the subscription ladder — the personal and team tiers a customer
        buys to use the cloud, each with its monthly and annual price, seat rules,
        limits and billing reference.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/tools:
    get:
      description: |-
        Returns the per-use price of every metered tool — web search,
        code interpreter, image generation, speech — each with the unit it is billed
        in.
      operationId: get_plan_tools
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planToolList'
          description: ok
      summary: Returns the per-use price of every metered tool — web search, code
        interpreter, image generation, speech — each with the unit it is billed in.
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/plan/vocab:
    get:
      description: |-
        Returns the entitlement key vocabulary: every key with
        its namespace, JSON type, nullability, unit, enum and title, the list of
        namespaces, and the engine features a license can grant. It is derived from
        entitlements.schema.json on every call, so it cannot fall behind the schema.
      operationId: get_plan_vocab
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/planVocab'
          description: ok
      summary: 'Returns the entitlement key vocabulary: every key with its namespace,
        JSON type, nullability, unit, enum and title, the list of namespaces, and
        the engine features a license can grant.'
      tags:
      - plan
      x-app: plan
      x-public: true
      x-tool: true
  /v1/platform/apps:
    get:
      description: |-
        Returns the declarations in the caller's own org directory, each joined with the Hanzo CD Application reconciling it — sync verdict, health, the universe commit last applied. `cd` is null for a declaration the delivery plane has no Application for, which is the normal state of one that exists only on a branch.

        If the delivery plane cannot be read, the declarations are still returned and `cdUnavailable` says why. An unreadable plane never renders as "nothing has been reconciled".
      operationId: get_platform_apps
      summary: What this organization has declared, and what CD did with it
      tags:
      - platform
      x-app: platform
      x-public: true
    post:
      description: |-
        Builds a git repository into an image and writes the declaration that names it — a values file in `hanzoai/universe` under `charts/app/values/<namespace>/<name>.yaml`, which the `fleet` ApplicationSet renders as one Application. That file IS the deployment: nothing else has to be applied.

        `mode` decides whether anything can go live. The default, `branch`, pushes to `deploy/<namespace>/<name>/<tag>` and returns a review URL; the generator reads main, so a branch declaration deploys NOTHING and merging the review is the deliberate act. `commit` writes main, and proves the image is pullable first — a declaration naming an image the registry cannot serve is an ImagePullBackOff with no rollback path.

        Omit `tag` to build; give it to declare an image an earlier call already built, which is how a green build is released without rebuilding it.

        An org is its name: the values DIRECTORY, the destination NAMESPACE and the AppProject FENCE are all `<org>`, and the image is `<registry>/<org>/<app>`. None of them is a request field — the directory decides what CD admits the sync under and the repository decides what the cluster pulls, so a caller who could name either could reach outside its own org.

        `org` is an ACT-AS, not a placement field: it defaults to the caller's own, and naming another requires SuperAdmin. So does naming a RESERVED org — the platform's own namespace family (the brands and their environments, the control and delivery planes, `admin`) — even when it is the caller's own, because an IAM org named `kube-system` does not own Kubernetes. Both refuse rather than downgrade, so an escape attempt is never indistinguishable from a normal request.

        A host outside the caller's org subtree is refused: claim and verify a custom domain first.
      operationId: post_platform_apps
      summary: Deploy an app through cd.hanzo.ai
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/apps/{app}:
    get:
      description: 'The values file for one app as git declares it: image repository
        and tag, hosts, replicas, and whether CD is automated on it. 404 when this
        organization declares no such app.'
      operationId: get_platform_apps_by_app
      parameters:
      - in: path
        name: app
        required: true
        schema:
          type: string
      summary: One declaration
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/apps/{app}/cd:
    get:
      description: The Hanzo CD Application for one declaration, on its own — the
        poll a deploy view makes while it waits, without re-reading the whole inventory.
        404 while the declaration exists only on a branch, because the generator reads
        main.
      operationId: get_platform_apps_by_app_cd
      parameters:
      - in: path
        name: app
        required: true
        schema:
          type: string
      summary: One app's reconciliation
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/builds:
    get:
      description: |-
        Returns real build records for your org.

        It lists the org's BuildKit build records — the git build step behind a deploy —
        each with the repo it built, the short commit, its status, when it started and
        how long it took. These are real records or an honest empty list; a build appears
        here because one ran, never because a page needed a row. Builds are created only
        by /deploy and the push-to-deploy hook. Requires a validated principal; 403
        without one.
      operationId: get_platform_builds
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/buildBoard'
          description: ok
      summary: Returns real build records for your org.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/cd:
    get:
      description: |-
        Every Hanzo CD Application this caller may observe, with its sync verdict, health, the universe revision last applied, and whether automation and self-heal are on. A SuperAdmin sees the fleet; an org admin sees only Applications whose destination namespace IS its own organization, and never a reserved one.

        A cluster with no CD installed answers an empty plane. A plane that cannot be READ answers 503 and says why — the two are opposite facts and never share a shape.
      operationId: get_platform_cd
      summary: The delivery plane
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/ci:
    get:
      description: Answers 501. The forge's Actions runs need a Forgejo API client
        and this deployment has none; an empty run list would be indistinguishable
        from a forge with no runs.
      operationId: get_platform_ci
      summary: Continuous integration (not wired)
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/environments:
    get:
      description: |-
        Returns your deploy targets, and what is running on each.

        It returns the org's environments — the distinct deploy targets its applications
        name, `production` for anything that names none — each aggregating the apps that
        target it, a rolled-up status and when it last changed.

        An environment is DERIVED, not stored: there is nothing to create or delete here,
        and an environment exists exactly as long as an app points at it. Requires a
        validated principal; 403 without one.
      operationId: get_platform_environments
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/environmentBoard'
          description: ok
      summary: Returns your deploy targets, and what is running on each.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/fleet:
    get:
      description: |-
        Returns the platform's own service tier, and where it has drifted.

        It returns the board for the services the PLATFORM itself runs — iam, kms,
        gateway and the rest — as `{apps, summary}`: per service its environment, health,
        phase, the image tag its CR DECLARES, the tag actually running, and the drift
        between them, plus a summary counting the board green, yellow and red.

        This is not a customer surface. `/v1/platform/projects/:project/apps` is a
        tenant's apps; this is the tier those tenants run ON, which is why the two are
        named differently rather than sharing a prefix.

        Admission is scoped at the SCAN, before any CR is read: a platform SuperAdmin
        observes the whole fleet, an org admin observes only their own org's namespaces,
        and an org that owns none gets an empty board — a non-super caller never even
        lists another org's services. Narrow further with `env`, `health`, `org`, or
        `drift=1` for only what has drifted.

        It degrades honestly rather than failing whole: a namespace that does not exist
        is skipped, and a running-state read the caller cannot make leaves the running
        tag empty — an unknown, never a guess — while the declared, health and phase
        columns still render.
      operationId: get_platform_fleet
      parameters:
      - description: 'Env narrows to one lifecycle env: main, test or dev.'
        in: query
        name: env
        required: false
        schema:
          type: string
      - description: 'Health narrows to one health colour: green, yellow or red.'
        in: query
        name: health
        required: false
        schema:
          type: string
      - description: Org narrows to one image namespace.
        in: query
        name: org
        required: false
        schema:
          type: string
      - description: |-
          Drift is `1` or `true` to show only rows that have actually drifted. It is
          a STRING and not a bool because those two spellings are exactly what the
          board has always accepted, and a bool would silently widen that to `?drift`
          alone and to `TRUE` — a behaviour change wearing a type change's clothes.
        in: query
        name: drift
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/driftBoard'
          description: ok
      summary: Returns the platform's own service tier, and where it has drifted.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/fleet/{app}:
    get:
      description: |-
        Returns one platform service, resolved to production by default.

        It returns a single platform service by its CR name, with the same
        declared-versus-running and drift facts the board carries. The name must be a
        DNS-1123 label; anything else is 400.

        Namespaces are scanned in lifecycle order — main, then test, then dev — and the
        first match wins, so a bare name resolves to PRODUCTION. The scan covers only the
        namespaces the caller is authorized for, so an org admin can never read a service
        outside their own org, and a name found in none of them is 404 rather than a leak.
      operationId: get_platform_fleet_by_app
      parameters:
      - description: App is the service's CR name, from the path. It must be a DNS-1123
          label.
        in: path
        name: app
        required: true
        schema:
          type: string
      - description: |-
          Env narrows the scan to one lifecycle env: main, test or dev. Omitted, the
          namespaces are scanned in lifecycle order and the first match wins, so a
          bare name resolves to PRODUCTION.
        in: query
        name: env
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AppView'
          description: ok
      summary: Returns one platform service, resolved to production by default.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/fleet/{app}/deploy:
    post:
      description: |-
        Rolls a platform service's pods, in a named environment.

        It triggers a rolling restart of one platform service's Deployment by stamping a
        fresh restart annotation, and answers 202 with the app, the namespace, the
        environment and the timestamp. It restarts pods; it does NOT change the image — a
        version change is the release path, not this.

        SuperAdmin ONLY, and deliberately narrower than the read gate beside it. The only
        namespaces this board touches are the platform's own tier, so a restart here
        recycles a SHARED service every tenant depends on. A brand-org admin is a
        customer-org admin, not a platform operator: observing the board is bounded and
        audited, and restarting production identity is not.

        `?env=main|test|dev` is REQUIRED — a bare call does not default to production,
        which is what closes the fat-finger and confused-deputy hazard — and any other
        value is 400. A service with no Deployment to restart in that environment is 404.
      operationId: post_platform_fleet_by_app_deploy
      parameters:
      - description: App is the service's CR name, from the path. It must be a DNS-1123
          label.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/restartRef'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/restarted'
          description: accepted
      summary: Rolls a platform service's pods, in a named environment.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/health:
    get:
      description: |-
        Reports whether this control plane can actually deploy anything.

        A real probe, not a status page. It answers 200 only when the metadata store is
        open AND the cluster is genuinely reachable — proved by LISTING the operator App
        CRD, which settles reachability and CRD presence in one bounded call, and which
        is the exact question every deploy depends on. Anything else is 503 carrying the
        real reason and whether the CRD was found.

        A constructed cluster client proves nothing — it is built from a kubeconfig, not
        from a reachable apiserver — so this deliberately spends a round trip rather than
        reporting `ok` while every deploy fails. Not admin-gated: liveness has to be
        probe-able without a credential.
      operationId: get_platform_health
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/readiness'
          description: ok
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/readiness'
          description: service unavailable
      summary: Reports whether this control plane can actually deploy anything.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/hook:
    post:
      description: |-
        The forge's push-to-deploy door. git.hanzo.ai runs as a separate server, so its pushes never reach this fleet's own receive-pack; without this a push to the host we call canonical builds nothing. A verified push is handed to the SAME two seams a native push travels — the single-registrant deploy trigger, and the many-subscriber lifecycle stream that notifies and indexes — and the build decision itself stays downstream in the one place that knows what a push means.

        PUBLIC at the JWT layer, because the forge carries no Hanzo session: AUTHENTICATION IS THE SIGNATURE. The HMAC covers the raw bytes and is verified BEFORE the payload is parsed, so an unauthenticated body is never decoded. The secret is read from KMS; a deployment that cannot read it answers 503 and processes nothing, rather than trusting a delivery it could not check. The body is read UNCOMPRESSED — a request declaring a Content-Encoding is refused 415 before it is touched, because decoding one is unbounded work bought with a few bytes and no credential. A bad signature is 401, a payload over 8 MiB is 413, and a malformed one 400.

        A verified push that reaches both seams answers 200 with fired true and the NUMBER OF BUILDS it launched — zero is ordinary, since most pushes track no application, and it is the answer 'fired' cannot give. A push that could not be dispatched answers 500: the delivery page shows it red, and the Replay that prompts reaches a fresh attempt rather than being declined as already landed.

        The deliveries deliberately ignored answer 200 with a reason and nothing else: a payload that is not a push, a ref DELETE (a zero `after` has no commit to build), a BOT-authored push (release automation pushes as the forge's own Actions user, and a release must never rebuild itself), a push from a forge namespace that maps to no org, and a redelivery of a push already fired. Branches and tags both reach the build trigger, because releases are cut by tag and filtering here would silently stop publishing.
      operationId: post_platform_hook
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/push'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/verdict'
          description: Success
      summary: Receive a push from the forge and trigger its build
      tags:
      - platform
      x-app: platform
      x-public: true
  /v1/platform/pipelines:
    get:
      description: |-
        Returns one build-and-deploy pipeline per app, with its latest run.

        It returns one pipeline per application in the caller's org — its repo or image
        source, its current status, and when its most recent deployment ran and how long
        it took. A pipeline is a PROJECTION of an app plus its newest deployment, not a
        separate record: it comes into existence with the app and is triggered only
        through /deploy, never here. Requires a validated principal; 403 without one.
      operationId: get_platform_pipelines
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pipelineBoard'
          description: ok
      summary: Returns one build-and-deploy pipeline per app, with its latest run.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects:
    get:
      description: |-
        Returns your org's projects, each with how many apps live under it.

        It lists the caller org's projects with the number of platform applications in
        each. A project is IAM's resource — it is created and deleted at
        /v1/iam/projects, never here — so this is the ONE projection IAM cannot serve:
        the project plus what the platform has put under it.

        Requires a validated principal; 403 without one, and the org comes from that
        validated identity rather than a request header. This is the console's first
        authenticated read, so a project store that is not yet initialised degrades to
        an EMPTY list rather than a 500 — a new org genuinely has zero projects — and
        the real cause is surfaced to operators instead of to the caller.
      operationId: get_platform_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/projectView'
                type: array
          description: ok
      summary: Returns your org's projects, each with how many apps live under it.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}:
    get:
      description: |-
        Returns one project and its app count.

        It returns a single project of the caller's org with the number of platform
        applications under it. A project this org does not have is 404, which is also
        what another tenant's project looks like from here. Requires a validated
        principal; 403 without one.
      operationId: get_platform_projects_by_project
      parameters:
      - description: Project is the project's name, from the path.
        in: path
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectView'
          description: ok
      summary: Returns one project and its app count.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps:
    get:
      description: |-
        Returns the applications in one project, with what the cluster says
        about them.

        It lists the caller org's applications under one project. Each row carries the
        stored record and, for an app that is live or deploying, the LIVE phase and
        health read from its operator Service CR; an app with sealed env also carries
        its secret-sync state. Those cluster reads are best-effort — an unreachable
        cluster leaves those fields empty and never blocks the listing.

        The project must exist in IAM for this org, or the answer is 404; the `default`
        project is implicit and always accepted, because it is part of what an org IS.
        Requires a validated principal; 403 without one.
      operationId: get_platform_projects_by_project_apps
      parameters:
      - description: Project is the project's name, from the path.
        in: path
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/appView'
                type: array
          description: ok
      summary: Returns the applications in one project, with what the cluster says
        about them.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates an application from a git repo or a container image.

        It registers a new application under one of the caller org's projects and
        answers 201 with it. Creating does NOT deploy: the app lands in `draft` and
        nothing reaches the cluster until /deploy.

        `source` is `git` — which requires `repo.url` — or `image`, which requires
        `image.repository`; anything else is 400. A git app builds with zero-config
        `pack` by default and may opt into `dockerfile`; an image app never builds. The
        repo URL and Dockerfile path are validated here against the SAME allowlist the
        privileged build enforces, so an unsafe source is refused before it is ever
        persisted.

        The `slug` is the app's identity in the cluster: given or derived from `name`,
        it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, and a slug already used in
        this project is 409. `replicas` and `storageGb` are clamped to the deployment's
        limits rather than refused.

        Env keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A variable marked `secret: true`
        is SEALED into KMS and its plaintext is never written to the database — and if
        KMS is unavailable the create fails 503 rather than falling back to storing a
        secret in the clear.

        The app is seeded with its canonical default host, so it has a working HTTPS URL
        the moment it deploys. A bare custom domain cannot be attached here — it has to
        go through add-domain and DNS verification first. Requires a validated
        principal; 403 without one, and every cluster object it will later create lands
        in that org's own `tenant-<org>` namespace.
      operationId: post_platform_projects_by_project_apps
      parameters:
      - description: Project is the project to create the application under, from
          the path.
        in: path
        name: project
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/createAppReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appView'
          description: created
      summary: Creates an application from a git repo or a container image.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}:
    delete:
      description: |-
        Deletes an application and tears down what it runs.

        It removes the application record and tears down what it owns in the org's
        tenant namespace — its operator Service CR and its KMSSecret — then answers 204.
        An app this org and project do not have is 404, never a silent success.

        Teardown is best-effort by design: a cluster that refuses or is unreachable does
        not block the delete, so the record cannot be left orphaned behind a broken
        cluster; the failure is logged for operators and the orphan reaper reconciles
        it. Requires a validated principal; 403 without one.
      operationId: delete_platform_projects_by_project_apps_by_app
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes an application and tears down what it runs.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one application, with its live phase, health and secret sync.

        It returns a single application of the caller's org together with what the
        cluster currently reports for it: the operator Service CR's phase and health,
        and whether its sealed env has synced. An app this org and project do not have
        is 404. Requires a validated principal; 403 without one.
      operationId: get_platform_projects_by_project_apps_by_app
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appView'
          description: ok
      summary: Returns one application, with its live phase, health and secret sync.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/deploy:
    post:
      description: |-
        Deploys the app — building it first if it comes from git.

        It starts a new, monotonically versioned deployment of the app and answers 202
        with the deployment record. A 202 is an ACCEPTED deployment, not a live one.

        An IMAGE app deploys the tag you name (falling back to the app's tag, then
        `latest`) by writing its operator Service CR; the operator reconciles it to
        running. A GIT app launches an in-cluster BuildKit Job at `commit` — or the app's
        branch — and comes back in `building`; the Service CR is applied later, by the
        reconciler, once the Job succeeds. The reconciler is restart-safe, so a build in
        flight survives a cloud restart.

        Deploys are bounded per org: over the concurrent-deploy cap is 429 and NOTHING is
        recorded, so a rejected deploy leaves no phantom in the history. An unreachable
        cluster is 503 but still records an honest `error` deployment, because a deploy
        that was attempted and failed must not be indistinguishable from one never made.
        Every other failure is likewise recorded in its real terminal state.

        This is metered work: a git build is billed to the org's ledger in wall-clock
        build minutes once the Job finishes, and the running deployment is billed for its
        compute per tick for as long as it stays live. Requires a validated principal; 403
        without one, and everything is written into that org's own `tenant-<org>`
        namespace.
      operationId: post_platform_projects_by_project_apps_by_app_deploy
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/deployReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deploymentView'
          description: accepted
      summary: Deploys the app — building it first if it comes from git.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/deployments:
    get:
      description: |-
        Returns an app's deployment history.

        It lists every deployment recorded for one of the caller org's applications,
        newest version first, each with its version, status, source, commit and image.
        Failed and superseded attempts are included — that is the point of a history.
        Requires a validated principal; 403 without one.
      operationId: get_platform_projects_by_project_apps_by_app_deployments
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/deploymentView'
                type: array
          description: ok
      summary: Returns an app's deployment history.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/deployments/{id}:
    get:
      description: |-
        Returns one deployment of one app.

        It returns a single deployment by id, scoped to the named application of the
        caller's org — so an id belonging to another app or another tenant is 404, not a
        read. Requires a validated principal; 403 without one.
      operationId: get_platform_projects_by_project_apps_by_app_deployments_by_id
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      - description: ID is the deployment's id, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deploymentView'
          description: ok
      summary: Returns one deployment of one app.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/deployments/{id}/logs:
    get:
      description: |-
        Returns real logs for a deployment — the build's, then the app's.

        It returns the deployment's recorded status timeline together with LIVE pod logs
        pulled from the cluster: the build pod's output while a git build is running, and
        the running app's output once it is deployed. The `source` field says which of
        the two the body is — `build`, `app` or `none` — so a console can label the pane
        honestly.

        It never fabricates log content. When no pod exists yet, or the cluster is
        unreachable, it degrades to the recorded timeline and says so. Every cluster read
        is confined to the caller org's own namespaces and time-boxed. Requires a
        validated principal; 403 without one.
      operationId: get_platform_projects_by_project_apps_by_app_deployments_by_id_logs
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      - description: ID is the deployment's id, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deployLogs'
          description: ok
      summary: Returns real logs for a deployment — the build's, then the app's.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/domains:
    get:
      description: |-
        Returns every hostname this app answers on.

        It lists the app's hosts: the permanent default host it was born with, any
        org-subtree hosts attached to it, and every custom host claimed for it with its
        verification state and, while pending, the DNS challenge records to publish. Live
        endpoint status for each host is observed from the cluster. Requires a validated
        principal; 403 without one.
      operationId: get_platform_projects_by_project_apps_by_app_domains
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/domainView'
                type: array
          description: ok
      summary: Returns every hostname this app answers on.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
    post:
      description: |-
        Attaches a hostname — instantly if you already own it, otherwise with a
        DNS challenge.

        It attaches `host` to the app, and which of two things happens depends on who
        owns the name. A host inside the caller org's own subtree is structurally owned,
        so it goes ACTIVE immediately and answers 201. A bring-your-own host is claimed
        as PENDING and answers the DNS challenge records to publish; it is NOT rendered
        into the app's ingress until /verify passes.

        Claims are globally unique. A host already claimed by another organization is
        409, and so is one claimed by a different app in your own; re-adding this app's
        OWN claim is idempotent and answers its current state at 200. The default host is
        always attached and re-adding it is 409. A host under the platform's shared apex
        that is not the caller's own subtree is 403 — it belongs to whoever owns that
        subtree and can never be grabbed through the custom path.

        `host` must be a valid DNS hostname; anything else is 400. Requires a validated
        principal; 403 without one.
      operationId: post_platform_projects_by_project_apps_by_app_domains
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/addDomainReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/domainView'
          description: ok
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/domainView'
          description: created
      summary: Attaches a hostname — instantly if you already own it, otherwise with
        a DNS challenge.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/domains/{host}:
    delete:
      description: |-
        Detaches a hostname and releases the claim.

        It drops the host from the app's ingress and releases any custom claim on it, so
        the name becomes claimable again — by this org or any other. Answers 204.

        The default host is permanent and cannot be removed: that is 400, not 404. A host
        that is neither attached nor claimed here is 404. Requires a validated principal;
        403 without one.
      operationId: delete_platform_projects_by_project_apps_by_app_domains_by_host
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      - description: Host is the hostname, from the path.
        in: path
        name: host
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Detaches a hostname and releases the claim.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/domains/{host}/verify:
    post:
      description: |-
        Checks a custom domain's DNS and turns it on if it passes.

        It runs the DNS challenge check for a pending custom host and, when it passes,
        marks the host verified and renders it into the app's ingress so it starts
        serving.

        A check that RAN and did not pass is not an error: it answers 200 with the host
        still pending and the reason in `detail`, so a console can show the operator what
        DNS is actually returning. An already-verified host answers as-is without
        re-checking. A host not claimed by this app is 404. Requires a validated
        principal; 403 without one.
      operationId: post_platform_projects_by_project_apps_by_app_domains_by_host_verify
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      - description: Host is the hostname, from the path.
        in: path
        name: host
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/domainView'
          description: ok
      summary: Checks a custom domain's DNS and turns it on if it passes.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/env:
    put:
      description: |-
        Replaces an app's environment variables.

        It writes the app's whole environment set and answers the updated application.
        This is the one post-create write path for env, and it REPLACES rather than
        merges: a variable absent from the body is gone, and a secret dropped from the
        set leaves the app's Secret on its next deploy.

        Keys must match `^[A-Za-z_][A-Za-z0-9_]*$`. A value marked `secret: true` is
        sealed into KMS and blanked in the database, so plaintext is never persisted —
        and the write fails 503 if KMS is unavailable rather than storing one in the
        clear.

        The rule worth knowing: this does not restart anything. Once the app has been
        deployed the secret sync is re-declared immediately so the operator
        re-materialises the Secret, but RUNNING pods keep the environment they started
        with until their next deploy or restart. Requires a validated principal; 403
        without one.
      operationId: put_platform_projects_by_project_apps_by_app_env
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/setEnvReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appView'
          description: ok
      summary: Replaces an app's environment variables.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/preview:
    post:
      description: |-
        Puts a branch on its own URL.

        It deploys an already-built `image` to a per-branch preview and answers its URL,
        the branch, the preview's slug and the deployment. The preview is a FIRST-CLASS
        application named `<app>-<branch>` in the same project and tenant namespace, with
        its own default host — so it is completely isolated from production while reusing
        the same deploy mechanic. Re-previewing a branch converges that same target in
        place rather than stacking another one.

        It carries NO environment variables, deliberately: a preview never inherits
        production's secrets. It also does not build — `image` is required and must
        already exist, and `branch` defaults to the parent app's. A branch that does not
        resolve to a valid slug distinct from the parent's is 400. Requires a validated
        principal; 403 without one.
      operationId: post_platform_projects_by_project_apps_by_app_preview
      parameters:
      - description: Project is the project the parent application lives under, from
          the path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the parent application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/previewReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/previewView'
          description: accepted
      summary: Puts a branch on its own URL.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/promote:
    post:
      description: |-
        Promotes an already-built release to the app.

        It redeploys an image that already exists — named either by `deploymentId`, which
        promotes that deployment's exact built image, or by `tag`, resolved the same way
        a deploy resolves one. One of the two is required; neither is 400.

        Promotion never builds. A deployment that carries no built image cannot be
        promoted and is 400, and a deployment id outside this app is 404. It runs through
        the same deploy core as everything else, so it takes a NEW version number and is
        subject to the same per-org concurrency cap. Requires a validated principal; 403
        without one.
      operationId: post_platform_projects_by_project_apps_by_app_promote
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/promoteReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deploymentView'
          description: accepted
      summary: Promotes an already-built release to the app.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/rollback:
    post:
      description: |-
        Goes back to the previous release.

        It redeploys a prior image: the one named by `deploymentId`, or — with no body —
        the newest earlier deployment that carries a real built image and did not error,
        skipping the release currently live. An app with nothing earlier to return to is
        400.

        A rollback is a deploy of an old image, not a rewind: it takes a NEW version
        number and appends to the history rather than erasing what came after. Both
        lookups are scoped to this app and org, so another tenant's image can never be
        rolled in. Requires a validated principal; 403 without one.
      operationId: post_platform_projects_by_project_apps_by_app_rollback
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/rollbackReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deploymentView'
          description: accepted
      summary: Goes back to the previous release.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/start:
    post:
      description: |-
        Starts a stopped app back up.

        It scales the app's Service back to its configured replica count and marks it
        live, answering the updated application. It does not redeploy: the image already
        on the Service CR is what comes back.

        The billing watermark is reset to now as part of starting, so the org is charged
        for THIS live span and never for the gap the app spent stopped. An app with no
        Service CR is 404, an unreachable cluster is 503, and a cluster that refuses the
        scale is 502. Requires a validated principal; 403 without one.
      operationId: post_platform_projects_by_project_apps_by_app_start
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appView'
          description: ok
      summary: Starts a stopped app back up.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/projects/{project}/apps/{app}/stop:
    post:
      description: |-
        Stops an app without deleting it.

        It scales the app's Service to zero replicas and marks it stopped, answering the
        updated application. Nothing else is removed — the record, its env, its domains
        and its deployment history all survive, and /start brings it back at the same
        replica count.

        An app that is not deployed has no Service CR to scale and is 404. An
        unreachable cluster is 503 and a cluster that refuses the scale is 502. Because
        the pods stop, so does the compute metering. Requires a validated principal; 403
        without one.
      operationId: post_platform_projects_by_project_apps_by_app_stop
      parameters:
      - description: Project is the project the application lives under, from the
          path.
        in: path
        name: project
        required: true
        schema:
          type: string
      - description: App is the application's slug, from the path.
        in: path
        name: app
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/appView'
          description: ok
      summary: Stops an app without deleting it.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/releases:
    get:
      description: |-
        Returns the versions that actually reached the cluster.

        It lists the org's releases: the deployments that were genuinely applied to the
        cluster, with the app they belong to, their version, environment, status and when
        they were released. A deployment that failed or is still building is NOT a
        release and is excluded — reaching the cluster is what makes one. Requires a
        validated principal; 403 without one.
      operationId: get_platform_releases
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/releaseBoard'
          description: ok
      summary: Returns the versions that actually reached the cluster.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/run:
    post:
      description: |-
        Runs a container image and gives back a URL.

        The one-call shortcut over project → app → deploy: give it a `name` and an
        `image` and it creates or updates an image-source application in your org's
        DEFAULT project, deploys it through the same operator Service-CR writer
        everything else uses, and answers its id, name, live URL, status and shape.
        Re-running the same name UPDATES it in place, so the call is idempotent by name.

        What it produces is a first-class application, not a special object: it is
        listable, stoppable and redeployable through the /v1/platform routes like any
        other app.

        `minScale` is the replica floor. `maxScale` above it declares an autoscaling
        ceiling; `maxScale: 0` means no autoscaler at all — a fixed run at the floor.
        Both are clamped to the deployment's limits. `runtime` and `shape` are accepted
        for the client contract and echoed back: the image is the runtime unit and sizing
        is the operator's default.

        It is BILLING-GATED before it touches the cluster: a flat per-run fee is
        authorized against the org's own prepaid balance first, so an org that cannot pay
        is refused without anything being created. An unreachable cluster is 503 — a run
        never reports a URL it did not create. Secret env is sealed into KMS and fails
        closed without it.

        Requires a validated principal; 403 without one. The org is resolved from that
        validated identity and is what both pays and owns the namespace — it is never
        read from the body.
      operationId: post_platform_run
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/runReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runView'
          description: accepted
      summary: Runs a container image and gives back a URL.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/platform/runner:
    post:
      description: |-
        Triggers a native build — an image, or the binaries a repo declares.

        The fabric's own build trigger, and what `hanzo build` and git-push-to-deploy
        call. It answers 202 with the build job id: a queued build, not a pushed
        artifact.

        Two lanes, and a build is exactly one of them. The IMAGE lane takes `repo` and
        the output `image` and launches a BuildKit Job that pushes it. The ARTIFACT lane
        takes `binaries` — the same recipe the repo's hanzo.yml declares — and publishes
        to object storage instead; it must carry no `image`, because a build produces
        binaries or an image, never both.

        PRIVILEGED, with exactly two credentials and never a third: the shared
        build-callback token compared in constant time — the machine path, which a user
        never holds — or a validated IAM principal who is an ADMIN of their org, which is
        the `hanzo build` user path and means one IAM login authorizes a build with no
        separate build token. A plain member is refused.

        Both paths are bounded the same way: the output must push to a registry the
        fabric owns, and on the IAM path the image's registry namespace must MATCH the
        caller's own validated org — so an org admin can only publish into their own
        brand and can never overwrite another's through the shared push credential. The
        same confinement applies to the artifact lane's repo owner.

        The output image is parsed and validated as a single well-formed OCI ref before
        any authorization decision reads it, so a crafted ref cannot smuggle a
        build-exporter attribute past the check.
      operationId: post_platform_runner
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/runnerBuildReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/runnerBuildResp'
          description: accepted
      summary: Triggers a native build — an image, or the binaries a repo declares.
      tags:
      - platform
      x-app: platform
      x-public: true
      x-tool: true
  /v1/prefs:
    get:
      description: |-
        Returns the signed-in caller's OWN preference document — the theme,
        density and pinned nav that follow them across every Hanzo surface. There is no
        path to another user's preferences: not for an org admin, not for a platform
        SuperAdmin, because the subject is built from the validated credential and is the
        mandatory predicate on the read. A caller who has never saved anything gets an
        empty document at 200, never a 404, so the user menu always renders.
      operationId: get_prefs
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/prefsView'
          description: ok
      summary: Returns the signed-in caller's OWN preference document — the theme,
        density and pinned nav that follow them across every Hanzo surface.
      tags:
      - prefs
      x-app: prefs
      x-public: true
      x-tool: true
    patch:
      description: 'Merges a JSON object key-wise into the signed-in caller''s OWN
        preference document and answers with the whole document after the merge, so
        a surface saves `theme` without having to send back the `density` another
        surface owns. The merge is SHALLOW and the key space is open: an unnamed key
        is left untouched, a named key is replaced whole, and a key sent with a `null`
        value is DELETED. The subject is the `<owner>/<name>` identity built from
        the validated credential and is the mandatory predicate on the write, so there
        is no path to another user''s preferences — not for an org admin, not for
        a platform SuperAdmin. Fails closed: no validated principal is 403; an empty
        body or a literal `null` is 400; and a patch or a resulting document over
        16 KiB or 128 keys is 413.'
      operationId: patch_prefs
      summary: Save the preference keys your surface owns, leaving every other key
        alone
      tags:
      - prefs
      x-app: prefs
      x-public: true
  /v1/pricing:
    get:
      description: |-
        Returns the whole pricing catalog in one document: Zen and
        third-party models, providers, model families, the free-model list, plan and
        infrastructure pricing. Every model and provider it names is filtered to what
        the caller's org may see — the same gate the leaf routes apply, so this can
        never be an un-gated second source for what they hide.
      operationId: get_pricing
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the whole pricing catalog in one document: Zen and third-party
        models, providers, model families, the free-model list, plan and infrastructure
        pricing.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/base:
    get:
      description: |-
        Returns the Hanzo Base plans — the managed-instance tiers,
        each with its monthly and annual price, storage and request allowances and
        feature list.
      operationId: get_pricing_base
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: Returns the Hanzo Base plans — the managed-instance tiers, each with
        its monthly and annual price, storage and request allowances and feature list.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/blockchain:
    get:
      description: |-
        Returns the blockchain access plans — the RPC and node
        tiers, each with its monthly price, compute-unit allowance and feature list.
      operationId: get_pricing_blockchain
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: Returns the blockchain access plans — the RPC and node tiers, each
        with its monthly price, compute-unit allowance and feature list.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/cloud:
    get:
      description: |-
        Returns the public cloud section of the catalog in one
        document: its instance plans, its regions and its block-storage prices. The
        section's internal half — the provider costs Hanzo pays and the plan-to-
        provider routing table — is stripped before it is served, so this is what a
        customer may see and nothing more.
      operationId: get_pricing_cloud
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the public cloud section of the catalog in one document: its
        instance plans, its regions and its block-storage prices.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/cloud/plans:
    get:
      description: |-
        Returns just the cloud instance plans — each with its vCPU,
        memory, disk, CPU type, VM allowance, feature list and monthly and hourly
        price. It is the plans of the cloud section on their own.
      operationId: get_pricing_cloud_plans
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: Returns just the cloud instance plans — each with its vCPU, memory,
        disk, CPU type, VM allowance, feature list and monthly and hourly price.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/cloud/regions:
    get:
      description: |-
        Returns the regions a cloud instance can be placed in, each
        with its id, display name and physical location. It is the regions of the
        cloud section on their own.
      operationId: get_pricing_cloud_regions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingRegionList'
          description: ok
      summary: Returns the regions a cloud instance can be placed in, each with its
        id, display name and physical location.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/cloud/storage:
    get:
      description: |-
        Returns the block-storage prices of the cloud
        section: the per-GB monthly rate and the volume size bounds a caller may ask
        for.
      operationId: get_pricing_cloud_storage
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the block-storage prices of the cloud section: the per-GB
        monthly rate and the volume size bounds a caller may ask for.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/compute:
    get:
      description: |-
        Returns the compute section of the catalog: the cloud
        provider and region the prices are quoted for, the monthly markup applied to
        them, the full instance-size tier list and the named presets. It is the
        whole section as the pricing source records it, un-gated — no model or
        provider identity appears in it.
      operationId: get_pricing_compute
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the compute section of the catalog: the cloud provider and
        region the prices are quoted for, the monthly markup applied to them, the
        full instance-size tier list and the named presets.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/compute/presets:
    get:
      description: |-
        Returns just the named compute sizes — the short,
        human-labelled list ("Starter", "Pro") a size picker renders, each carrying
        its provider slug, vCPU, memory, disk and price. It is the presets of the
        compute section on their own, for a caller that does not need the full tier
        table.
      operationId: get_pricing_compute_presets
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPresetList'
          description: ok
      summary: Returns just the named compute sizes — the short, human-labelled list
        ("Starter", "Pro") a size picker renders, each carrying its provider slug,
        vCPU, memory, disk and price.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/datastore:
    get:
      description: |-
        Returns the Hanzo Datastore rate card: the tier list, the
        per-GB storage and egress usage rates, the annual discount and the trial. It is
        the section as authored, un-gated — no provider identity appears in it.

        The route was missing while the data existed, so this 404d and every visitor to
        hanzo.ai's Infrastructure tab was told pricing was "temporarily unavailable".
      operationId: get_pricing_datastore
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the Hanzo Datastore rate card: the tier list, the per-GB storage
        and egress usage rates, the annual discount and the trial.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/enablement:
    get:
      description: |-
        Returns what the caller's org can actually use: every managed
        item with its global state, whether it is effective here, whether this org is
        already opted into its beta, and whether it may still opt in. Read-only and
        safe for any caller — one without a validated principal simply sees the
        generally-available items and no opt-in affordance, never another org's state.
      operationId: get_pricing_enablement
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/enablementBoard'
          description: ok
      summary: 'Returns what the caller''s org can actually use: every managed item
        with its global state, whether it is effective here, whether this org is already
        opted into its beta, and whether it may still opt in.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/enablement/optin:
    post:
      description: |-
        Opts the caller's OWN org into a beta item. The org is the
        caller's validated one, so this can never target another org, and the registry
        refuses anything not in beta — so it can neither re-open an item an operator
        turned off nor touch one that is already generally available. Requires a
        signed-in caller with an org.
      operationId: post_pricing_enablement_optin
      requestBody:
        content:
          application/json:
            example:
              id: labs
              kind: feature
            schema:
              $ref: '#/components/schemas/enablementOptRef'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/userEnablementItem'
          description: ok
      summary: Opts the caller's OWN org into a beta item.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/enablement/optout:
    post:
      description: |-
        Removes the caller's OWN org from a beta item's grant list, the
        reverse of OptIntoBeta and idempotent. The org is the caller's validated one,
        so this can never revoke another org's grant. Requires a signed-in caller with
        an org.
      operationId: post_pricing_enablement_optout
      requestBody:
        content:
          application/json:
            example:
              id: labs
              kind: feature
            schema:
              $ref: '#/components/schemas/enablementOptRef'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/userEnablementItem'
          description: ok
      summary: Removes the caller's OWN org from a beta item's grant list, the reverse
        of OptIntoBeta and idempotent.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/featured:
    get:
      description: |-
        Returns the models the catalog highlights, filtered to what
        the caller's org may see. It is the same catalog as ListModels narrowed to
        entries the pricing source marks featured.
      operationId: get_pricing_featured
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingModelList'
          description: ok
      summary: Returns the models the catalog highlights, filtered to what the caller's
        org may see.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/free:
    get:
      description: |-
        Returns the models that cost nothing to call, filtered to what
        the caller's org may see. It is the same catalog as ListModels narrowed to
        entries the pricing source marks free.
      operationId: get_pricing_free
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingModelList'
          description: ok
      summary: Returns the models that cost nothing to call, filtered to what the
        caller's org may see.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/gpu:
    get:
      description: |-
        ListGPUTiers returns the rentable GPU configurations, each with its
        accelerator count and model, VRAM, vCPU, host memory and hourly price.
      operationId: get_pricing_gpu
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingTierList'
          description: ok
      summary: ListGPUTiers returns the rentable GPU configurations, each with its
        accelerator count and model, VRAM, vCPU, host memory and hourly price.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/health:
    get:
      description: |-
        Health reports that the pricing subsystem is mounted and serving. It answers
        from the process itself and consults neither the catalog bundle nor the
        enablement store, so it stays "ok" while either is degraded.
      operationId: get_pricing_health
      responses:
        "200":
          content:
            application/json:
              example:
                service: pricing
                status: ok
              schema:
                $ref: '#/components/schemas/pricingHealth'
          description: ok
      summary: Health reports that the pricing subsystem is mounted and serving.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/iam:
    get:
      description: |-
        ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each with its
        monthly and annual price, monthly-active-user allowance and feature list.
      operationId: get_pricing_iam
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: ListIAMPlans returns the identity plans — the Hanzo IAM tiers, each
        with its monthly and annual price, monthly-active-user allowance and feature
        list.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/model/{name}:
    get:
      description: |-
        Returns one model's catalog entry — its pricing, context window and
        capabilities as the pricing source records them. A model hidden for the
        caller's org answers the same 404 an unknown name does, so a disabled model
        gets no existence oracle.
      operationId: get_pricing_model_by_name
      parameters:
      - description: |-
          Name is the model's name or its slugged id ("zen4",
          "acme/some-model-1"), matched case-insensitively. It comes from
          the path: the URL is the addressing authority.
        example: zen4
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: Returns one model's catalog entry — its pricing, context window and
        capabilities as the pricing source records them.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/models:
    get:
      description: |-
        Returns the whole model catalog — every model the gateway serves,
        Zen and third-party alike — filtered to what the caller's org may see. A
        model an admin has disabled is absent; one in beta appears only for an org
        granted it. A SuperAdmin sees every model, each annotated with its
        enablement state.
      operationId: get_pricing_models
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingModelList'
          description: ok
      summary: Returns the whole model catalog — every model the gateway serves, Zen
        and third-party alike — filtered to what the caller's org may see.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/paas:
    get:
      description: |-
        ListPaaSPlans returns the application-hosting plans — the deploy-and-host
        tiers, each with its monthly and annual price, app and memory allowances and
        feature list.
      operationId: get_pricing_paas
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: ListPaaSPlans returns the application-hosting plans — the deploy-and-host
        tiers, each with its monthly and annual price, app and memory allowances and
        feature list.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/policy:
    get:
      description: |-
        Returns the pricing policy document: the revenue-sharing
        terms (the idle-resale share and the open-source share, each with its
        percentage and who is eligible) and the commitments Hanzo makes about how it
        bills — no hidden fees, no egress charges, no surprise bills.
      operationId: get_pricing_policy
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: 'Returns the pricing policy document: the revenue-sharing terms (the
        idle-resale share and the open-source share, each with its percentage and
        who is eligible) and the commitments Hanzo makes about how it bills — no hidden
        fees, no egress charges, no surprise bills.'
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/providers:
    get:
      description: |-
        Returns the model providers the catalog knows, each with its
        info object, filtered to what the caller's org may see. A provider an admin
        has disabled is absent — and so are its models everywhere else on this
        surface, because a provider's state cascades to what it serves.
      operationId: get_pricing_providers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingProviderList'
          description: ok
      summary: Returns the model providers the catalog knows, each with its info object,
        filtered to what the caller's org may see.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/services:
    get:
      description: |-
        Returns the managed-service rate cards — Search, Crawl,
        Vector, Console and Managed Services — each with its own tiers, and some with
        usage rates or a comparison table. It is the section as authored, un-gated.

        These are DISPLAY rate cards: what a product costs, not what a plan grants. No
        entitlement or limit fields ride here, so nothing can bill off them.
      operationId: get_pricing_services
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: Returns the managed-service rate cards — Search, Crawl, Vector, Console
        and Managed Services — each with its own tiers, and some with usage rates
        or a comparison table.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/subscriptions:
    get:
      description: |-
        Returns the API subscription plans — the account-level
        tiers a customer subscribes to, each with its monthly and annual price,
        included credit, rate limits and feature list.
      operationId: get_pricing_subscriptions
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingPlanList'
          description: ok
      summary: Returns the API subscription plans — the account-level tiers a customer
        subscribes to, each with its monthly and annual price, included credit, rate
        limits and feature list.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/summary:
    get:
      description: |-
        Returns the catalog's headline statistics — model counts by
        family and the provider directory. The provider sub-object is filtered to what
        the caller's org may see, so a disabled provider's name never leaks; the
        aggregate counts are the catalog's own, over everything it holds.
      operationId: get_pricing_summary
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  type: object
                type: object
          description: ok
      summary: Returns the catalog's headline statistics — model counts by family
        and the provider directory.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/sync:
    post:
      description: |-
        Refreshes the third-party section of the catalog from its upstream
        listings and returns the time the refreshed catalog was stamped with. The
        fetch runs in Go and the markup transform in the pricing bundle. SuperAdmin
        only; every other caller is refused.
      operationId: post_pricing_sync
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingSyncOut'
          description: ok
      summary: Refreshes the third-party section of the catalog from its upstream
        listings and returns the time the refreshed catalog was stamped with.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/pricing/tools:
    get:
      description: |-
        Returns the per-use tool prices — web search, code
        interpreter, file storage, image generation, speech-to-text and
        text-to-speech — each with the unit it is billed by and its price in that
        unit.
      operationId: get_pricing_tools
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pricingToolList'
          description: ok
      summary: Returns the per-use tool prices — web search, code interpreter, file
        storage, image generation, speech-to-text and text-to-speech — each with the
        unit it is billed by and its price in that unit.
      tags:
      - pricing
      x-app: pricing
      x-public: true
      x-tool: true
  /v1/projects:
    get:
      description: |-
        Returns every project your org owns.

        Each row carries the slug, name, framework, visibility, status and live URL —
        the same rows console and the builder render, because there is only one store
        behind both. It requires a validated principal (403 without one) and is keyed
        by that principal's org, so it never contains another tenant's project.
      operationId: get_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/projectsProject'
                type: array
          description: ok
      summary: Returns every project your org owns.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a project — the handle a site is deployed and served
        under — and answers 201 with it in `draft`.

        `name` is required; `slug` is derived from the name when omitted and is the
        identifier that matters — it becomes the S3 key segment, the public host
        `<slug>.hanzo.app`, and the handle every later call addresses, so it must
        match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$` and may not be a reserved label
        such as `api` or `admin`. `framework` is a build hint from a closed set,
        defaulting to `static`; it never gates a deploy, it only tells CI how to build
        a linked repo.

        Two defaults are worth knowing: the analytics beacon is ON unless `analytics`
        is explicitly false, and `visibility` is `public` unless asked otherwise.
        Publishing publicly is free; PRIVATE is the paid feature, and an unfunded org
        asking for it is refused rather than quietly published as public. Creation
        also provisions the project's data space and a canonical git repo, both
        best-effort — neither can fail the create.

        Scope: a validated principal is required (403 without one) and the project is
        created in THAT principal's org. The slug is unique per org, so a slug already
        used in the caller's own org is a 409 while the same slug in another org is
        irrelevant.
      operationId: post_projects
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsCreate'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsProject'
          description: created
      summary: Creates a project — the handle a site is deployed and served under
        — and answers 201 with it in `draft`.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}:
    delete:
      description: |-
        Deletes a project and takes its site off the internet.

        The metadata delete is authoritative and everything after it is best-effort,
        in this order: the public `<slug>` subdomain binding is released so the slug is
        free to reclaim, the release rows are dropped so a reclaimed slug never
        inherits the previous owner's rollback menu, the git source is retired on
        every copy it has so a reclaimed slug never adopts a repository left behind
        (visibility.go), the S3 origin is purged under BOTH `<org>/<slug>/` and the
        site's sibling release space, and the edge cache-tag is flushed. A failure in
        any of those is logged and the delete still answers 204 — resurrecting a
        project because a purge missed would be worse than a leaked prefix.

        Scope: a validated principal is required (403 without one) and the project is
        resolved within that principal's org, so another tenant's slug is a 404 and
        nothing of theirs is touched.
      operationId: delete_projects_by_slug
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes a project and takes its site off the internet.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one project of yours by slug — its settings, its live URL
        and the deployment currently serving it.

        Scope: a validated principal is required (403 without one) and the lookup is
        keyed by (org, slug), so another tenant's slug is a 404 exactly like a
        nonexistent one.
      operationId: get_projects_by_slug
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsProject'
          description: ok
      summary: Returns one project of yours by slug — its settings, its live URL and
        the deployment currently serving it.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    patch:
      description: |-
        Changes a project's settings, and only the settings you send.

        Every field is optional and absent means "leave it": `name` may not be blanked,
        `framework` must stay a known build hint, and `cacheControl` is capped at 256
        characters with no newlines (it becomes a response header). `visibility` flips
        public/private under the same rule as create — public is free, private needs a
        funded org. `upstream` and `license` are free-text credit for third-party work,
        and sending "" clears one. Changing anything reconciles the project's canonical
        git repo, so a visibility change reaches the source and not just the listing.

        `hidden`/`hiddenReason` are platform MODERATION and are ignored unless the
        caller is a platform admin; they remove a project from the public catalogue
        without touching the publisher's own visibility choice, so un-hiding restores
        exactly what they asked for.

        Scope: a validated principal is required (403 without one) and the project is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: patch_projects_by_slug
      parameters:
      - description: |-
          Slug is the project to update, from the path. The URL is the addressing
          authority — a `slug` in the body cannot move the write to another project.
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsUpdate'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsProject'
          description: ok
      summary: Changes a project's settings, and only the settings you send.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/deploy:
    post:
      description: |-
        Takes a built site live at `https://<slug>.hanzo.app` in one call. The body is the site itself — a `zip` or `tar.gz` holding `index.html` at its root (or a single wrapper directory that does), sent raw or as a multipart file part. It is unpacked to the site's own storage prefix and served immediately, answering the finished deployment.

        It is bounded by the edge body limit (16 MiB by default), and that bound is the whole reason the other path exists: an oversized POST is refused by the server BEFORE any handler runs and surfaces as an opaque `400 Error when parsing request` that reads like a malformed payload rather than a size cap. A site too large for one archive opens a deployment with `POST /v1/projects/{slug}/deployments` instead and writes its files straight to storage against the scoped grant that answers with — no body limit, and no bytes through this API at all.

        Billing is fail-closed and fails FIRST: the hosting gate runs before anything is parsed or uploaded, so an unfunded org is 402 and an unreachable commerce is 503 with nothing written. The debit lands only on success — a failed upload is never billed and never flips the live site — and a redeploy answers the SAME URL, because slug and apex are stable.

        Scope: a validated principal is required (403 without one) and the site is resolved within that principal's org, so another tenant's slug is a 404. Object storage must be configured (503); an archive that does not walk is a 400 and one over the size cap is a 413.
      operationId: post_projects_by_slug_deploy
      parameters:
      - in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/octet-stream:
            schema:
              format: binary
              type: string
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDeployment'
          description: Success
      summary: Upload a built site as one archive and serve it
      tags:
      - projects
      x-app: projects
      x-public: true
  /v1/projects/{slug}/deployments:
    get:
      description: |-
        Returns a project's deploy history, newest version first.

        Every deploy of the project is a row — uploads, generated sites, and git/CI
        builds alike — carrying its version, status, source, commit, live URL, file
        count and byte count. The short-lived upload grant a queued git deployment was
        handed is NOT replayed here: it exists only on the 202 that minted it, so a
        grant cannot outlive its build by being fetched again.

        Scope: a validated principal is required (403 without one) and the project is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: get_projects_by_slug_deployments
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/projectsDeployment'
                type: array
          description: ok
      summary: Returns a project's deploy history, newest version first.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    post:
      description: |-
        Opens a deployment and hands back a short-lived, prefix-scoped
        grant to write its bytes straight to object storage. Answers 202.

        This is the path for a site too large to send as one archive: a real export is
        hundreds of megabytes against a 16 MiB body limit, so the bytes deliberately do
        NOT pass through the API. The answer carries `bucket`, `prefix` and `upload` —
        a presigned POST policy that S3 itself confines to this site's prefix
        (starts-with `<org>/<slug>/`), expires in 30 minutes and bounds each object.
        So a build writes its own files and holds no standing bucket credential; there
        is nothing to rotate and nothing that leaks between tenants. Never guess the
        prefix — it is server-derived, and a guessed one lands where nothing is served.

        The deployment is `queued` until POST .../deployments/{id}/complete flips it
        live (or error). That completion is also where DELETION happens: the grant
        authorizes writes only, so a build cannot remove a file, and cloud reconciles
        the prefix against the `keys` manifest the completion carries. A build that
        dies before completing leaves the deployment queued rather than a half-live
        site.

        The grant is on the 202 and NOWHERE else — it is never stored and never
        replayed on a later read, so it cannot outlive the build it was minted for. A
        deployment whose grant could not be minted is still created and still
        completable; it simply carries no `upload`, and a caller with no other way to
        write should treat that as the failure it is.

        Billing: the hosting gate runs BEFORE anything is created (402 unfunded, 503
        commerce unreachable), and the debit lands on the completion that goes live —
        never on a queued or failed build.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_deployments
      parameters:
      - description: Slug is the site to deploy, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsDeployStart'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDeployment'
          description: accepted
      summary: Opens a deployment and hands back a short-lived, prefix-scoped grant
        to write its bytes straight to object storage.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/deployments/{id}:
    get:
      description: |-
        Returns one deployment of a project by id.

        It is how a console follows a build: the status (`queued`, `uploading`,
        `live`, `error`), the message a failure left, and the URL and prefix it went
        live at. Like the history, it never replays the upload grant.

        Scope: a validated principal is required (403 without one). Both the project
        and the deployment are resolved within that principal's org, so a deployment
        of another project — or of another tenant — is a 404.
      operationId: get_projects_by_slug_deployments_by_id
      parameters:
      - description: Slug is the project the deployment belongs to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      - description: |-
          ID is the deployment id, from the path. A deployment of another project —
          or of another tenant's project — is not found.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDeployment'
          description: ok
      summary: Returns one deployment of a project by id.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/deployments/{id}/complete:
    post:
      description: |-
        CompleteDeployment is the CI completion hook that flips a queued git
        deployment to live (or error) once CI has synced the built site to S3.

        `status` must be `live` or `error`. On a LIVE completion the public host is
        claimed FIRST, so the deployment reports the URL it actually OWNS — a
        CI-supplied `liveUrl` is a hint that can refine that URL but can never assert
        a subdomain another tenant holds. `keys` is the manifest CI just uploaded,
        relative to the deployment prefix: cloud reconciles the prefix against it so a
        page deleted from the build actually stops serving. Omit `keys` and nothing is
        deleted — the prefix only grows. Reconciliation runs only on a live completion
        (pruning against a failed build's manifest would delete the site the last good
        build is still serving) and is best-effort, so a stale leftover never turns a
        successful deploy into a 500. A live completion is also the one billable
        moment on the git path; an error completion bills nothing.

        Scope: a validated principal is required (403 without one). CI authenticates
        with an org-scoped token through the gateway, so the deployment is resolved
        within that principal's org and another tenant's slug or deployment id is a
        404.
      operationId: post_projects_by_slug_deployments_by_id_complete
      parameters:
      - description: Slug is the project the deployment belongs to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      - description: ID is the queued deployment to complete, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsComplete'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDeployment'
          description: ok
      summary: CompleteDeployment is the CI completion hook that flips a queued git
        deployment to live (or error) once CI has synced the built site to S3.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/domains:
    get:
      description: |-
        Returns every custom hostname this site holds: the live ones, plus
        any pending claim with the DNS records it still owes.

        `domains` is the routing answer — the hosts that are verified right now —
        while `claims` is the full panel, one row per host, each saying whether it is
        live or pending and, if pending, exactly what to publish.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: get_projects_by_slug_domains
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDomains'
          description: ok
      summary: 'Returns every custom hostname this site holds: the live ones, plus
        any pending claim with the DNS records it still owes.'
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    post:
      description: |-
        Attaches one or more CUSTOM public hostnames to this org's site.

        Binding a host you do not own would let you shadow it at the edge, so which
        outcome you get depends on whether ownership is already established: a SuperAdmin
        vouches (the operator manages the customer's DNS, so its bind IS the proof) and
        binds VERIFIED immediately; every other caller, INCLUDING an admin of the
        deployment's own brand org, has the host CLAIMED as pending and gets the DNS
        challenge back in `bound[].records`. A pending claim HOLDS the name so nobody
        else can take it, but it does not route until POST .../domains/{host}/verify
        proves control.

        A hostname we operate is refused to a non-vouched caller (those are assigned
        by the platform, never claimed), a host another site already holds is a 409,
        and a name the platform holds is a 400 for EVERY caller — a vouch skips the
        ownership proof, never the host table's own invariant. Claims and binds are
        idempotent for the same
        (org, slug), and re-claiming returns the SAME token rather than invalidating a
        record the customer has already published. The edge cache-tag is flushed
        afterwards so a newly-verified host serves the current build immediately.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_domains
      parameters:
      - description: Slug is the site the hosts attach to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsDomainsBind'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsBoundDomains'
          description: ok
      summary: Attaches one or more CUSTOM public hostnames to this org's site.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/domains/{host}:
    delete:
      description: |-
        Gives a custom hostname back, so the name is free to reuse.

        A claim is FIRST-COME and global, so an add-only surface was not ownership but
        a leak: a customer who mistyped a domain, or claimed one they later moved
        elsewhere, could neither reuse it nor let anyone else. This is the third
        writer that closes it. The release is scoped to (host, org, slug), so it can
        only ever drop THIS tenant's own claim, and it is IDEMPOTENT: releasing a host
        we do not hold is a clean 204, never a 404 that would let a caller probe which
        hosts other tenants hold. The edge cache-tag is flushed, since the host stops
        routing here.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: delete_projects_by_slug_domains_by_host
      parameters:
      - description: Slug is the project the host is attached to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      - description: |-
          Host is the custom hostname, from the path. It is cleaned to its canonical
          form (lowercased, trailing dot dropped) before anything is looked up.
        in: path
        name: host
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Gives a custom hostname back, so the name is free to reuse.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/domains/{host}/verify:
    post:
      description: |-
        Checks the DNS challenge for a pending custom hostname and, when
        it passes, promotes the host so it begins routing at the edge.

        It answers 200 either way, with the host's honest current state: verified once
        the TXT record is found, still pending — with the records to publish and the
        resolver's own explanation in `detail` — when it is not. A not-yet is not an
        error: the check ran, DNS simply has not propagated, and the customer retries.
        An already-verified host is returned unchanged without re-resolving. On a
        successful promotion the edge cache-tag is flushed, since the host routes as
        of that moment.

        Scope: a validated principal is required (403 without one). Both the site and
        the claim are resolved within that principal's org, so a host claimed by
        another tenant is "not claimed by this site".
      operationId: post_projects_by_slug_domains_by_host_verify
      parameters:
      - description: Slug is the project the host is attached to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      - description: |-
          Host is the custom hostname, from the path. It is cleaned to its canonical
          form (lowercased, trailing dot dropped) before anything is looked up.
        in: path
        name: host
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsDomain'
          description: ok
      summary: Checks the DNS challenge for a pending custom hostname and, when it
        passes, promotes the host so it begins routing at the edge.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/publish:
    post:
      description: |-
        Promotes a build output into a new release AND goes live with it —
        create+activate in one call, which is the 99% path.

        It is exactly the two halves in sequence with no extra semantics, so the
        staged flow and the one-shot flow can never drift apart: `source` is promoted
        under the same org-relative rule and the same guards CreateRelease applies,
        then the site's pointer is flipped to it, the public host is claimed and the
        edge is purged. Idempotent on unchanged bytes — same manifest, same release id,
        no copy — and billed once, after the release exists.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_publish
      parameters:
      - description: Slug is the site to publish, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsPublish'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsRelease'
          description: ok
      summary: Promotes a build output into a new release AND goes live with it —
        create+activate in one call, which is the 99% path.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/purge:
    post:
      description: |-
        Flushes the site's edge cache without redeploying anything.

        It invalidates the edge cache-tag `site-<org>-<slug>` and stamps `lastPurgeAt`
        (unix seconds), and it NEVER writes or deletes the S3 origin — the live build
        keeps serving; only stale copies held at the edge drop, so the next request
        re-fetches the current artifact from origin. Idempotent, and an edge that is
        unconfigured or failing is not fatal: `lastPurgeAt` is still stamped and the
        answer is still the updated project.

        Scope: a validated principal is required (403 without one) and the project is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_purge
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsProject'
          description: ok
      summary: Flushes the site's edge cache without redeploying anything.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/releases:
    get:
      description: |-
        Returns a site's releases newest-first, marking the active one —
        the rollback menu.

        Each row carries the release id to activate, the source it was promoted from,
        its object and byte counts, and the URL if it is the one serving. Retention
        bounds the list, so it is the set that can actually still be rolled back to,
        not a full history.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: get_projects_by_slug_releases
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/projectsRelease'
                type: array
          description: ok
      summary: Returns a site's releases newest-first, marking the active one — the
        rollback menu.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    post:
      description: |-
        Promotes a build output into a new immutable release WITHOUT
        serving it — the staged half of publishing, for when you want to check a
        release before it goes live. Answers 201.

        `source` is a path RELATIVE to your org's own storage space: the org segment
        is prepended server-side from the validated principal and the bucket is never
        in the request at all, so a server-side copy can only ever reach bytes your
        org already owns. The prefix is listed, content-addressed (SHA-256 over the
        sorted manifest of key/size/etag), and copied into an immutable
        `<org>/.releases/<slug>/<id>/` prefix; the row is written LAST, so a partial
        copy is unreachable rather than merely unlikely. Re-publishing an unchanged
        source is idempotent BY CONSTRUCTION — same bytes, same id, no copy at all.

        The source must contain index.html at its root and stay under the same file
        and byte caps an artifact deploy does (413 past them); a source that changes
        mid-copy is a 409 and the release is abandoned. Each publish also reclaims
        releases past the retention depth, so a site's release space stays bounded.
        This is the billable half — the hosting gate runs before any copy, and the
        debit lands once the release exists.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_releases
      parameters:
      - description: Slug is the site to publish, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsPublish'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsRelease'
          description: created
      summary: Promotes a build output into a new immutable release WITHOUT serving
        it — the staged half of publishing, for when you want to check a release before
        it goes live.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/{slug}/releases/{release}/activate:
    post:
      description: |-
        Points the site at an existing release — the go-live, and
        equally the ROLLBACK.

        Aim it at an older release and the site serves that one again: releases are
        immutable and retained to the retention depth, so nothing is rebuilt or
        re-copied and the flip is one atomic statement. Before the flip, two
        conditions run in the order that gives each its own honest answer — the ROW
        says whether this release exists for this tenant at all (404, with no signal
        about a foreign id), and only then do the BYTES say whether it can still serve
        (410 GONE when retention has reclaimed them; that rollback target is not
        coming back, so publish again). Going live also claims the public host and
        purges the edge, so the release is reachable and no cached predecessor is
        served. NOT billed: no new content is produced, only a pointer moved.

        Scope: a validated principal is required (403 without one) and the site is
        resolved within that principal's org, so another tenant's slug is a 404.
      operationId: post_projects_by_slug_releases_by_release_activate
      parameters:
      - description: Slug is the site the release belongs to, from the path.
        in: path
        name: slug
        required: true
        schema:
          type: string
      - description: |-
          Release is the content-addressed release id ("rel_" + 32 hex chars), from
          the path. Anything that is not that shape is not found, rather than being
          interpolated into a storage prefix.
        in: path
        name: release
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsRelease'
          description: ok
      summary: Points the site at an existing release — the go-live, and equally the
        ROLLBACK.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/edge:
    get:
      description: |-
        health reports whether a publish reaches readers, rather than whether it was
        accepted. Those are different questions and only the second one was ever
        visible.

        It asks the edge and nothing else. There is no live call to the provider here:
        Configured is a local fact, it is the fact that was missing, and a health check
        that spends a third-party API call is one an operator learns not to run.
      operationId: get_projects_edge
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/edgeState'
          description: ok
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/edgeState'
          description: service unavailable
      summary: health reports whether a publish reaches readers, rather than whether
        it was accepted.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/fork:
    post:
      description: |-
        Creates a project seeded from a PUBLISHED EXAMPLE — either a
        starter-kit template from the ONE embedded gallery catalog, or any live
        project on the platform (an example a seeded creator published, or another
        org's app serving at <slug>.hanzo.app). Answers 201 with the new project.

        `slug` names the PARENT to fork and is required. Templates resolve first, and
        the caller org's own private templates ahead of the public gallery, so a
        curated template slug keeps meaning the same thing even if someone later
        publishes a live project under it; `variant` picks that template's
        format/page/theme. If no template matches, the slug resolves to the UNIQUE
        live project that owns it across all orgs — the same resolution the site edge
        uses to serve <slug>.hanzo.app, so what you can browse is what you can fork.

        `name` and `target` override the derived project name and slug; everything
        else is inherited from the parent. A live parent contributes its REPO, so the
        child builds from the same source — the parent's deployed bytes are never
        copied, because releases are per-tenant by design and the fork publishes its
        own. The parent it actually resolved is stamped on the child as `forkedFrom`,
        so attribution is a fact recorded at fork time rather than a claim
        reconstructed later.

        It funnels through the SAME create path POST /v1/projects uses, so slug
        validation, org scoping, ID minting and the 409 on a slug the caller's own org
        already uses are identical.

        Scope: a validated principal is required (403 without one) and the child is
        created in THAT principal's org.
      operationId: post_projects_fork
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsFork'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsProject'
          description: created
      summary: Creates a project seeded from a PUBLISHED EXAMPLE — either a starter-kit
        template from the ONE embedded gallery catalog, or any live project on the
        platform (an example a seeded creator published, or another org's app serving
        at <slug>.hanzo.app).
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/sites:
    get:
      description: |-
        Returns the org's deployed sites at the pretty URLs they serve at.

        It reads the SAME org-scoped store as /v1/projects and keeps only the projects
        that are actually `live`, so a draft or a failed build is not advertised as a
        site.

        Scope: a validated principal is required (403 without one) and the list is
        keyed by that principal's org.
      operationId: get_projects_sites
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/projectsSite'
                type: array
          description: ok
      summary: Returns the org's deployed sites at the pretty URLs they serve at.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
    post:
      description: |-
        Generates a self-contained, mobile-responsive static site from a
        natural-language brief and deploys it live in one call.

        One inference call turns `brief` (capped at 8 KiB) into a file manifest, which
        then runs through the SAME validation, guards and viewport guarantee as a
        hand-supplied manifest: index.html required at the root, absolute and
        traversal paths rejected, per-file and total size capped, and a mobile
        viewport meta tag injected into every HTML document that lacks one. The
        generated site is fully inline — no CDNs, no remote fonts or images — so it is
        CSP-safe. `slug` and `name` are optional: the model's own title is preferred,
        and a slug is derived or minted when none is given.

        It writes into the SAME org-scoped store as /v1/projects — it ensures a
        project (framework `static`) for the resolved slug and records a deployment —
        so this is a second door onto one publish pipeline, not a second copy of
        project state. Ordering is the billing contract: the hosting gate runs BEFORE
        any inference or upload, so a denied gate generates and uploads NOTHING, and
        the debit lands once, only after the site is actually live. The tokens are
        billed to the same ledger the hosting fee was reserved against.

        Answers 503 when object storage or inference is unconfigured, and 400 when the
        model's manifest cannot be parsed or fails the guards.

        Scope: a validated principal is required (403 without one) and the site is
        published into THAT principal's org.
      operationId: post_projects_sites
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsBuildSite'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsSiteDeploy'
          description: ok
      summary: Generates a self-contained, mobile-responsive static site from a natural-language
        brief and deploys it live in one call.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/sites/{slug}:
    get:
      description: |-
        Returns one site — the same row ListSites carries, for one slug.

        Every sub-resource under a site already answered: deployments, releases,
        publish. The site itself did not, and a route that is never registered
        answers 404 for a LIVE site exactly as it does for one that was never
        created. So the one call a client makes to ask "is it there yet?" could only
        ever say no, and a CI lane watching for its own publish would wait forever on
        a success it had already achieved.

        The org is the caller's, never a path segment. A slug is unique within an org
        and two orgs may both own `tel`; taking the org from the validated principal
        instead of the URL means a caller cannot read another org's site by editing a
        path, and it is the same scope ListProjects and ListSites already use.

        A site that exists but is not live is NOT found here, matching ListSites,
        which keeps only `live` rows so a draft or a failed build is never advertised
        as a site. One definition of "is a site", used by both.
      operationId: get_projects_sites_by_slug
      parameters:
      - description: |-
          Slug is the project to act on, from the path. It is unique within the
          caller's org and nowhere else, so another tenant's slug is a 404.
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsSite'
          description: ok
      summary: Returns one site — the same row ListSites carries, for one slug.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/sites/deploy:
    post:
      description: |-
        Deploys a caller-supplied file manifest — the deploy_site
        capability an agent calls — and answers with where it went live.

        `files` is a list of {path, content} pairs, the same shape the brief build
        emits, and it runs through the SAME guards: index.html required at the root,
        absolute and traversal paths rejected, per-file and total size capped, and a
        mobile viewport meta tag injected into every HTML document that lacks one — so
        a hand-built site is exactly as safe and as responsive as a generated one.
        `slug` and `name` are optional; a slug is derived from the name or minted.

        It writes into the SAME org-scoped store as /v1/projects, ensuring a project
        (framework `static`) for the resolved slug and recording a deployment. The
        hosting gate runs before the upload and the debit lands once, after the site
        is live — a failed upload is never billed. Answers 503 when object storage is
        unconfigured.

        Scope: a validated principal is required (403 without one) and the site is
        published into THAT principal's org.
      operationId: post_projects_sites_deploy
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/projectsDeploySite'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/projectsSiteDeploy'
          description: ok
      summary: Deploys a caller-supplied file manifest — the deploy_site capability
        an agent calls — and answers with where it went live.
      tags:
      - projects
      x-app: projects
      x-public: true
      x-tool: true
  /v1/projects/tags:
    get:
      description: 'Returns the client-side pixels the SITE has connected (GA/Meta/TikTok/X)
        with their NON-SECRET ids, so the hosted tag injects them first-party and
        stamps each browser event with the same event_id the server-side Conversions
        API uses — deduping the two. Resolved per site: by the publishable key on
        ?key= when it names a project, else by the request host, so hanzo.ai and hanzo.chat
        carry different tags under one org. WITHOUT a resolvable site it answers an
        empty set at 200 — a page never breaks on its tag config.'
      operationId: get_projects_tags
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/tagConfig'
          description: Success
      summary: The site's browser tag set for the hosted tag — which pixels to inject,
        by publishable key
      tags:
      - projects
      x-app: projects
      x-public: true
  /v1/prompts:
    get:
      description: |-
        List returns the caller org's prompt library as one row per prompt: its name,
        type, every version number it has, its taxonomy and when it last changed. The
        template bodies are deliberately absent — fetch one prompt to read its text.
      operationId: get_prompts
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/promptList'
          description: ok
      summary: 'List returns the caller org''s prompt library as one row per prompt:
        its name, type, every version number it has, its taxonomy and when it last
        changed.'
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
    post:
      description: |-
        Create records a prompt for the caller's org and answers 201 with it. A name the
        org already uses is NOT an error and NOT an overwrite: it appends a new version,
        so the library keeps real, inspectable history and the response carries the whole
        version list. The name is also the URL segment the prompt is fetched by, which is
        why its shape is constrained and a handful of names are reserved.
      operationId: post_prompts
      requestBody:
        content:
          application/json:
            example:
              name: greeting
              prompt: You are a helpful assistant.
              tags:
              - support
            schema:
              $ref: '#/components/schemas/promptReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/promptDetail'
          description: created
      summary: Create records a prompt for the caller's org and answers 201 with it.
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
  /v1/prompts/{name}:
    delete:
      description: |-
        Delete removes one of the caller org's prompts and every version of it, answering
        204. It is scoped to the caller's org, so a name another tenant owns is the same
        404 an unknown name gives. There is no undo: the version history goes with it.
      operationId: delete_prompts_by_name
      parameters:
      - description: Name is the prompt to act on, from the path.
        example: greeting
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Delete removes one of the caller org's prompts and every version of
        it, answering 204.
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
    get:
      description: |-
        Get returns one of the caller org's prompts: its CURRENT template text plus the
        metadata of every version it has had. The history carries version numbers, types
        and timestamps only — not each version's body — so a long history cannot inflate
        this response. A name the caller's org does not own is 404, whoever owns it.
      operationId: get_prompts_by_name
      parameters:
      - description: Name is the prompt to act on, from the path.
        example: greeting
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/promptDetail'
          description: ok
      summary: 'Get returns one of the caller org''s prompts: its CURRENT template
        text plus the metadata of every version it has had.'
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
  /v1/prompts/catalog:
    get:
      description: |-
        Catalog returns the read-only starter prompt library shipped with the binary —
        reference content every tenant sees the same, NOT the caller's own prompts and
        never mixed into them. An org's library stays honestly empty until someone
        explicitly imports a starter, which is an ordinary POST /v1/prompts. Entries that
        would fail the create guards are dropped, so everything offered here can actually
        be imported.
      operationId: get_prompts_catalog
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/catalogList'
          description: ok
      summary: Catalog returns the read-only starter prompt library shipped with the
        binary — reference content every tenant sees the same, NOT the caller's own
        prompts and never mixed into them.
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
  /v1/prompts/metrics:
    get:
      description: |-
        Metrics returns real per-prompt statistics for the caller's org: how many versions
        each prompt has, which one is current, and when it was created and last changed.
        Every number is counted from the store — nothing here is estimated or fabricated.
      operationId: get_prompts_metrics
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/metricList'
          description: ok
      summary: 'Metrics returns real per-prompt statistics for the caller''s org:
        how many versions each prompt has, which one is current, and when it was created
        and last changed.'
      tags:
      - prompts
      x-app: prompts
      x-public: true
      x-tool: true
  /v1/provisioning/datastore:
    get:
      description: |-
        Lists the caller org's Hanzo Datastore warehouses. Each one is
        a DEDICATED analytical instance the org alone runs, so the host is that
        instance's own in-cluster Service and the port is its HTTP port, 8123.
      operationId: get_provisioning_datastore
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: Lists the caller org's Hanzo Datastore warehouses.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Launches your org's OWN Hanzo Datastore instance and answers with its `datastore://` connection string. The instance is yours alone: a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_datastore
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a Hanzo Datastore instance for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/datastore/{name}:
    delete:
      description: |-
        Deprovisions one Hanzo Datastore warehouse. It reverts any app
        instance bound to it back to Base BEFORE tearing down the org's dedicated
        instance, then deletes the sealed credential and removes the metadata row.
        Answers 204 with no body; a second call is a 404.
      operationId: delete_provisioning_datastore_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: warehouse
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deprovisions one Hanzo Datastore warehouse.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one Hanzo Datastore warehouse's metadata. It carries the
        warehouse's status, its instance address and the admin user the instance
        booted with — never the password. A still-booting instance reads
        "provisioning", reconciled from the operator's live view rather than the row.
      operationId: get_provisioning_datastore_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: warehouse
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: Returns one Hanzo Datastore warehouse's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/docdb:
    get:
      description: |-
        ListDocDB lists the caller org's Hanzo DocDB document databases. Each one is
        a DEDICATED FerretDB instance the org alone runs, speaking the MongoDB wire
        protocol, so the host is that instance's own in-cluster Service and the port
        is 27017.
      operationId: get_provisioning_docdb
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: ListDocDB lists the caller org's Hanzo DocDB document databases.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Launches your org's OWN document-database instance — it speaks the MongoDB wire protocol, so existing MongoDB drivers connect unchanged — and answers with its `mongodb://` connection string. The instance is yours alone: a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_docdb
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a document database for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/docdb/{name}:
    delete:
      description: |-
        DropDocDB deprovisions one Hanzo DocDB database. It reverts any app instance
        bound to it back to Base BEFORE tearing down the org's dedicated FerretDB
        instance, then deletes the sealed credential and removes the metadata row.
        Answers 204 with no body; a second call is a 404.
      operationId: delete_provisioning_docdb_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: sessions
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: DropDocDB deprovisions one Hanzo DocDB database.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        GetDocDB returns one Hanzo DocDB database's metadata. It carries the
        database's status, its instance address and the SCRAM user the instance was
        set up with — never the password. A still-booting instance reads
        "provisioning", reconciled from the operator's live view.
      operationId: get_provisioning_docdb_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: sessions
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: GetDocDB returns one Hanzo DocDB database's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/kv:
    get:
      description: |-
        ListKV lists the caller org's Hanzo KV stores. Each one is a DEDICATED Valkey
        instance the org alone runs, so the host is that instance's own in-cluster
        Service and the port is 6379.
      operationId: get_provisioning_kv
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: ListKV lists the caller org's Hanzo KV stores.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Launches your org's OWN key-value instance and answers with its `kv://` connection string. The instance is yours alone: a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_kv
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a key-value store for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/kv/{name}:
    delete:
      description: |-
        DropKV deprovisions one Hanzo KV store. It reverts any app instance bound to
        it back to Base BEFORE tearing down the org's dedicated Valkey instance, then
        deletes the sealed credential and removes the metadata row. Answers 204 with
        no body; a second call is a 404.
      operationId: delete_provisioning_kv_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: sessions
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: DropKV deprovisions one Hanzo KV store.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        GetKV returns one Hanzo KV store's metadata. It carries the store's status,
        its instance address and the Valkey user it authenticates as ("default", the
        only user a requirepass instance has) — never the password. A still-booting
        instance reads "provisioning", reconciled from the operator's live view.
      operationId: get_provisioning_kv_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: sessions
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: GetKV returns one Hanzo KV store's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/s3:
    get:
      description: |-
        Lists the caller org's object-storage buckets. A bucket lives in an
        already-live shared object store and is reached through the public gateway.
        The names here are the friendly ones the org provisioned; the physical bucket
        is org-namespaced underneath, which is what keeps two tenants' buckets
        distinct.
      operationId: get_provisioning_s3
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: Lists the caller org's object-storage buckets.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates an S3-compatible bucket inside the already-running shared object store and answers with the endpoint that reaches it.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_s3
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision an object storage bucket for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/s3/{name}:
    delete:
      description: |-
        Deletes one bucket from the shared object store and removes its
        metadata row. Answers 204 with no body; a second call is a 404.
      operationId: delete_provisioning_s3_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: uploads
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one bucket from the shared object store and removes its metadata
        row.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one bucket's metadata. It carries the bucket's status and the
        gateway address it is reached at, and no username: the object store
        authenticates with a shared, out-of-band key rather than a per-bucket
        credential.
      operationId: get_provisioning_s3_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: uploads
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: Returns one bucket's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/search:
    get:
      description: |-
        Lists the caller org's search indexes. An index is a logical
        resource inside an already-live shared backend, so every one of them is
        reached through the public gateway rather than at an instance of its own.
      operationId: get_provisioning_search
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: Lists the caller org's search indexes.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a search index inside the already-running shared search backend and answers with the endpoint that reaches it.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_search
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a search index for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/search/{name}:
    delete:
      description: |-
        Deletes one search index from the shared backend and removes its
        metadata row. Answers 204 with no body; a second call is a 404.
      operationId: delete_provisioning_search_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: products
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one search index from the shared backend and removes its metadata
        row.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one search index's metadata. It carries the index's status
        and the gateway address it is reached at, and no username: the backend
        authenticates with a shared, out-of-band key rather than a per-index
        credential.
      operationId: get_provisioning_search_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: products
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: Returns one search index's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/sql:
    get:
      description: |-
        ListSQL lists the caller org's Hanzo SQL databases. Each one is a DEDICATED
        PostgreSQL instance the org alone runs, so the host is that instance's own
        in-cluster Service and the port is 5432.
      operationId: get_provisioning_sql
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: ListSQL lists the caller org's Hanzo SQL databases.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Launches your org's OWN PostgreSQL instance and answers with its `postgres://` connection string. The instance is yours alone: a deployment in your own tenant namespace, so its admin credential is naturally scoped to you and no other tenant shares the process. Off-cluster, where there is no orchestrator to launch one, this fails closed with 503 rather than handing back a shared one.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_sql
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a PostgreSQL database for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/sql/{name}:
    delete:
      description: |-
        DropSQL deprovisions one Hanzo SQL database. It reverts any app instance
        bound to it back to Base BEFORE tearing down the org's dedicated Postgres
        instance — never a live app pointed at a deleted backend — then deletes the
        sealed credential and removes the metadata row. Answers 204 with no body; a
        second call is a 404, not a second delete.
      operationId: delete_provisioning_sql_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: orders
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: DropSQL deprovisions one Hanzo SQL database.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        GetSQL returns one Hanzo SQL database's metadata. It carries the database's
        status, its instance address and the admin user Postgres booted with — never
        the password, which is returned once at create and otherwise lives only in
        Hanzo KMS. A still-booting instance reads "provisioning", reconciled from the
        operator's live view rather than from the row.
      operationId: get_provisioning_sql_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: orders
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: GetSQL returns one Hanzo SQL database's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/provisioning/vector:
    get:
      description: |-
        Lists the caller org's vector collections. A collection is a
        logical resource inside an already-live shared backend, so every one of them
        is reached through the public gateway rather than at an instance of its own.
      operationId: get_provisioning_vector
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/provisionedSummary'
                type: array
          description: ok
      summary: Lists the caller org's vector collections.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a vector collection inside the already-running shared vector backend and answers with the endpoint that reaches it.

        `name` is the org-unique slug every physical name derives from, and must match ^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$. `instance` optionally BINDS the add-on to one of your app instances: the DSN is injected into that instance's addons secret as <KIND>_URL, switching the app off its built-in store and onto this one. Omit it and the connection string is yours to wire.

        THE CREDENTIAL COMES BACK ONCE. The connection string and password are in this response and nowhere else — every read beside it omits the password — so a caller that does not keep them has to provision again. Where KMS is configured the password is sealed there and only a reference is persisted; where it is not, it is returned this once and stored nowhere. It is never held in plaintext.

        Scoped to the caller's validated org (403 without one), which also namespaces the physical resource under a fixed-width hash, so two tenants can never fold onto one backend resource — a residual collision fails closed with 409 rather than silently sharing. A name already taken in your org is 409; an invalid name or instance slug is 400; a backend that refuses the create is 502. Where a later step fails after the backend resource already exists, it is torn back down rather than left orphaned.

        Billing is gated BEFORE anything is created: an unfunded org — or, in the fail-closed default, an unreachable meter — gets the fleet-wide 402/503 and nothing is provisioned. The fee is per-kind and set by the deployment.
      operationId: post_provisioning_vector
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/provisionRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionResult'
          description: Success
      summary: Provision a vector collection for your org
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
  /v1/provisioning/vector/{name}:
    delete:
      description: |-
        Deletes one vector collection from the shared backend and removes
        its metadata row. Answers 204 with no body; a second call is a 404.
      operationId: delete_provisioning_vector_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: embeddings
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one vector collection from the shared backend and removes its
        metadata row.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one vector collection's metadata. It carries the
        collection's status and the gateway address it is reached at, and no username:
        the backend authenticates with a shared, out-of-band key rather than a
        per-collection credential, so there is no per-resource user to report.
      operationId: get_provisioning_vector_by_name
      parameters:
      - description: |-
          Name is the resource's org-unique slug, from the path. Lower-cased and
          trimmed before lookup, exactly as it was at create.
        example: embeddings
        in: path
        name: name
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/provisionedResource'
          description: ok
      summary: Returns one vector collection's metadata.
      tags:
      - provisioning
      x-app: provisioning
      x-public: true
      x-tool: true
  /v1/pubsub/kv/{bucket}:
    delete:
      description: |-
        Removes one bucket of the caller's org — every key and every
        revision with it — and answers 204 with no body. 404 when the org has no
        bucket of that name.
      operationId: delete_pubsub_kv_by_bucket
      parameters:
      - description: Bucket is the bucket's name, from the path.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one bucket of the caller's org — every key and every revision
        with it — and answers 204 with no body.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a KV bucket and returns it. A bucket is keyed state on
        the same durable plane as the streams: each key holds up to History
        revisions, entries can expire by TTL, and watchers on the NATS port see every
        write. 409 when the org already has a bucket of that name.
      operationId: post_pubsub_kv_by_bucket
      parameters:
      - description: |-
          Bucket is the bucket's name within the org, from the path: 1–64 of
          [A-Za-z0-9_], no dash.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              history: 5
              ttl: 3600
            schema:
              $ref: '#/components/schemas/bucketWrite'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/bucketRecord'
          description: created
      summary: Creates a KV bucket and returns it.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
  /v1/pubsub/kv/{bucket}/{key}:
    delete:
      description: |-
        Delete removes one key — a delete marker in the key's history, so watchers
        see it and Get answers 404 — and answers 204 with no body. 404 when the
        bucket does not exist.
      operationId: delete_pubsub_kv_by_bucket_by_key
      parameters:
      - description: Bucket is the bucket, from the path.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      - description: Key is the key, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Delete removes one key — a delete marker in the key's history, so watchers
        see it and Get answers 404 — and answers 204 with no body.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
    get:
      description: |-
        Get returns one key's current value and revision. 404 when the bucket does
        not exist, the key was never written, or its latest revision is a delete.
      operationId: get_pubsub_kv_by_bucket_by_key
      parameters:
      - description: Bucket is the bucket, from the path.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      - description: Key is the key, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kvEntry'
          description: ok
      summary: Get returns one key's current value and revision.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
    put:
      description: |-
        Put sets one key to one value and returns the revision the write created.
        Writes are versioned: each put is a new revision and the bucket retains up to
        its History of them per key.
      operationId: put_pubsub_kv_by_bucket_by_key
      parameters:
      - description: Bucket is the bucket, from the path.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      - description: Key is the key, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              value: '{"theme":"dark"}'
            schema:
              $ref: '#/components/schemas/kvWrite'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kvAck'
          description: ok
      summary: Put sets one key to one value and returns the revision the write created.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
  /v1/pubsub/kv/{bucket}/{key}/history:
    get:
      description: |-
        History returns one key's retained revisions, oldest first — every put and
        every delete marker up to the bucket's History depth. 404 when the bucket
        does not exist or the key was never written.
      operationId: get_pubsub_kv_by_bucket_by_key_history
      parameters:
      - description: Bucket is the bucket, from the path.
        in: path
        name: bucket
        required: true
        schema:
          type: string
      - description: Key is the key, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kvPage'
          description: ok
      summary: History returns one key's retained revisions, oldest first — every
        put and every delete marker up to the bucket's History depth.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
  /v1/pubsub/publish:
    post:
      description: |-
        Publish puts one message on the org's bus. When a stream captures the subject
        the write is DURABLE — the receipt names the stream and sequence only after
        JetStream has it on storage, and a repeated Nats-Msg-Id header within the
        dedup window answers duplicate instead of storing twice. When nothing
        captures it, the message goes out core NATS: delivered to current
        subscribers, receipt {ok}, nothing retained.
      operationId: post_pubsub_publish
      requestBody:
        content:
          application/json:
            example:
              data: '{"id":"o_1"}'
              headers:
                Nats-Msg-Id: o_1
              subject: orders.created
            schema:
              $ref: '#/components/schemas/busPublish'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/busAck'
          description: ok
      summary: Publish puts one message on the org's bus.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
  /v1/pubsub/request:
    post:
      description: |-
        Request sends one request on the org's bus and waits for one reply — the
        synchronous half of pub/sub, for callers speaking to a responder subscribed
        on the NATS port. 404 when nobody is listening on the subject; 408 when a
        responder exists but no reply arrived within the timeout.
      operationId: post_pubsub_request
      requestBody:
        content:
          application/json:
            example:
              data: '{"sku":"gpu_1"}'
              subject: billing.quote
              timeoutMs: 2000
            schema:
              $ref: '#/components/schemas/busRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/busMessage'
          description: ok
      summary: Request sends one request on the org's bus and waits for one reply
        — the synchronous half of pub/sub, for callers speaking to a responder subscribed
        on the NATS port.
      tags:
      - pubsub
      x-app: pubsub
      x-public: true
      x-tool: true
  /v1/registry/images:
    get:
      description: |-
        Images lists the org's container repositories, read live from the OCI
        catalog and filtered server-side to the org's namespace — the page can only
        ever hold the caller's own images.
      operationId: get_registry_images
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryImageList'
          description: ok
      summary: Images lists the org's container repositories, read live from the OCI
        catalog and filtered server-side to the org's namespace — the page can only
        ever hold the caller's own images.
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/registry/packages:
    get:
      description: |-
        Packages lists the org's npm packages — `<org>` and `@<org>/…` — from the
        npm registry's search index, optionally narrowed by a query within that
        scope. The org boundary is applied server-side after the search, so a query
        can never widen it.
      operationId: get_registry_packages
      parameters:
      - description: |-
          Query narrows the listing within the org's scope when present; the org
          boundary itself is never widened by it. It rides the query string.
        in: query
        name: query
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryPackageList'
          description: ok
      summary: Packages lists the org's npm packages — `<org>` and `@<org>/…` — from
        the npm registry's search index, optionally narrowed by a query within that
        scope.
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/registry/projects:
    get:
      description: |-
        Projects lists the namespaces the caller can see with what each holds: the
        org's slug, its repository count on the OCI catalog, and its package count
        on the npm registry. Today that is exactly one row — the caller's org.
      operationId: get_registry_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryProjectList'
          description: ok
      summary: 'Projects lists the namespaces the caller can see with what each holds:
        the org''s slug, its repository count on the OCI catalog, and its package
        count on the npm registry.'
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/registry/status:
    get:
      description: |-
        Status reports whether the OCI and npm registries are reachable and, when
        the OCI half is auth-gated, which token realm its challenge advertises — an
        honest lens for "is the registry plane up", never a fabricated ok.
      operationId: get_registry_status
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryStatus'
          description: ok
      summary: Status reports whether the OCI and npm registries are reachable and,
        when the OCI half is auth-gated, which token realm its challenge advertises
        — an honest lens for "is the registry plane up", never a fabricated ok.
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/registry/tags:
    get:
      description: |-
        Tags lists one org-owned repository's tags, read live from the OCI registry.
        The repository is addressed inside the org's namespace — a name outside it
        cannot be expressed, and an unknown one answers 404.
      operationId: get_registry_tags
      parameters:
      - description: |-
          Image is the repository name inside the org's namespace, as returned by
          the images op. It rides the query string.
        in: query
        name: image
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryTagList'
          description: ok
      summary: Tags lists one org-owned repository's tags, read live from the OCI
        registry.
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/registry/token:
    post:
      description: |-
        Token mints a short-lived, pull-only registry token for exactly one of the
        org's images, through the same IAM realm the docker CLI authenticates
        against. The scope is pinned server-side to `<org>/<image>` with the `pull`
        action — no field exists to name another org's image or ask for push. Use it
        as `Authorization: Bearer …` on the OCI wire; it expires in minutes.
      operationId: post_registry_token
      requestBody:
        content:
          application/json:
            example:
              image: cloud
            schema:
              $ref: '#/components/schemas/registryMint'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/registryToken'
          description: ok
      summary: Token mints a short-lived, pull-only registry token for exactly one
        of the org's images, through the same IAM realm the docker CLI authenticates
        against.
      tags:
      - registry
      x-app: registry
      x-public: true
      x-tool: true
  /v1/replay:
    post:
      description: |-
        Accepts a batch of rrweb events from a browser recorder and hands it to the session-replay pipeline, which stores the recording and derives the session summary a player reads back.

        ONE REQUEST IS ONE BATCH, and it is all-or-nothing: the recording is made durable before this answers, so a 200 {"accepted":1} means stored and never "buffered somewhere". There is no partial count, because a half-written recording is not a recording.

        `sessionId` is REQUIRED and bounded — at most 70 characters of ASCII letters, digits or '-'. It is the key every batch of one visit is grouped and ordered by, so an id outside that grammar is refused 400 here rather than accepted and dropped further down. `windowId` separates two tabs of one session and `distinctId` attributes the recording to a person; both are optional. `events` is the rrweb batch, each element a raw eventWithTime object, carried VERBATIM — the summary (click, keypress and mouse-activity counts, size) is derived downstream from exactly these bytes, so nothing is re-encoded or dropped.

        THE CALLER'S CREDENTIAL DECIDES THE TENANT, and the body never does: the recording lands in the org the presented credential resolves to. It takes the SAME credentials as /v1/event — a validated bearer, an org API key, or a publishable pk- key on Authorization: Bearer, x-hanzo-ingest-key or ?ingest_key= — so a browser bundle already holding a pk- for events needs nothing new to record. A caller that presents nothing is 401 `ingest_key_required`; one whose key resolves to no project is 403 `ingest_key_unknown`; a reduced principal (a Hanzo Team workspace token) is 403 `insufficient_capability`, because a full-fidelity screen recording has no projected form that is safe for a guest to write into a host org.

        BOUNDS: 413 over 512 KiB of body, and that is the only bound on one batch — a recorder is expected to chunk a long session rather than send it whole, and the cap is the size one message can carry rather than an arbitrary number. 503 when the pipeline cannot take the batch: honest unavailability the caller can retry, never a 200 over a discarded recording.
      operationId: post_replay
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/replayBody'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CaptureResult'
          description: Success
      summary: Record a session-replay snapshot batch
      tags:
      - analytics
      x-app: analytics
      x-public: true
  /v1/rerank:
    post:
      description: "Implements POST /v1/rerank (Cohere/Jina-compatible).\n\nBody:
        {\"model\": \"...\", \"query\": \"...\", \"documents\": [\"...\", ...]|[{\"text\":\"...\"}],\n\n\t\"top_n\"?:
        int, \"return_documents\"?: bool}\n\nResponse: {\"object\":\"list\",\"model\":...,\"results\":[{\"index\",\"relevance_score\",\"document\"?}],\"usage\":{...}}\n\nBackend
        selection is provider-driven (one endpoint, one contract):\n  - If the model
        routes to a native rerank provider (Jina/Cohere/Voyage) the\n    request is
        proxied to that provider's /rerank endpoint.\n  - Otherwise scores are computed
        as a real bi-encoder ranking: embed the\n    query and documents through the
        resolved embedding model and rank by\n    cosine similarity. No rerank-specific
        key required."
      operationId: post_rerank
      summary: Implements POST /v1/rerank (Cohere/Jina-compatible).
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/responses:
    post:
      description: |-
        Implements POST /v1/responses. The converted request is completed by
        the chat path, which is handed a sink saying where the answer goes: a stream is
        translated as it is produced, a whole body is translated entire.
      operationId: post_responses
      summary: Implements POST /v1/responses.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/s3/buckets:
    get:
      description: |-
        Returns the caller's own buckets under the friendly names they were created with, each with its creation time.

        Another tenant's bucket is not refused, it is INVISIBLE — a bucket outside the caller's namespace is skipped during the listing rather than reported, so the operation cannot be used to discover that a name is taken elsewhere.

        A validated principal is required, and every bucket and key is resolved inside the caller's own org: physical bucket names are derived from the org, so a tenant cannot name another's storage. The operation is billed per call — the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit happens only after the work succeeds. Object storage that is not configured answers 503 under this subsystem's own name rather than falling through to another.
      operationId: get_s3_buckets
      summary: List your org's buckets
      tags:
      - storage
      x-app: storage
      x-public: true
    post:
      description: |-
        Creates a new bucket in the caller's own namespace and answers 201 with its friendly name and creation time.

        The name is validated exactly as sent and never quietly normalised: it must match `^[a-z0-9]([a-z0-9-]{0,38}[a-z0-9])?$`, so a mixed-case name is a clean 400 rather than a bucket created as `photos` that the caller keeps asking for as `Photos`. A name already in use in the caller's own namespace is 409.

        A validated principal is required, and every bucket and key is resolved inside the caller's own org: physical bucket names are derived from the org, so a tenant cannot name another's storage. The operation is billed per call — the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit happens only after the work succeeds. Object storage that is not configured answers 503 under this subsystem's own name rather than falling through to another.
      operationId: post_s3_buckets
      summary: Create a bucket in your org
      tags:
      - storage
      x-app: storage
      x-public: true
  /v1/s3/buckets/{bucket}:
    delete:
      description: |-
        Removes one of the caller's buckets, and only when it is already EMPTY — a bucket with objects in it answers 409 instead.

        That refusal is deliberate rather than a limitation: this API does not cascade a delete of a tenant's objects behind a single bucket call, so emptying the bucket stays an explicit act. A bucket that does not exist is 404, and a successful delete answers 204 with no body.

        A validated principal is required, and every bucket and key is resolved inside the caller's own org: physical bucket names are derived from the org, so a tenant cannot name another's storage. The operation is billed per call — the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit happens only after the work succeeds. Object storage that is not configured answers 503 under this subsystem's own name rather than falling through to another.
      operationId: delete_s3_buckets_by_bucket
      parameters:
      - in: path
        name: bucket
        required: true
        schema:
          type: string
      summary: Delete an empty bucket
      tags:
      - storage
      x-app: storage
      x-public: true
  /v1/s3/buckets/{bucket}/objects:
    get:
      description: |-
        Lists one folder level of a bucket: each entry's key, whether it is a folder, its size, last-modified time and ETag. `prefix` scopes the read to a sub-folder.

        Keys come back RELATIVE to the requested prefix, not absolute, which is what lets a client render a breadcrumb without re-deriving it. The default is the folder view — sub-prefixes are returned as directory entries — and `recursive=true` flattens it to every key beneath the prefix instead.

        The listing is bounded at 1000 entries so a large bucket cannot exhaust memory; treat a full page as "there may be more" rather than as the whole bucket.

        A validated principal is required, and every bucket and key is resolved inside the caller's own org: physical bucket names are derived from the org, so a tenant cannot name another's storage. The operation is billed per call — the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit happens only after the work succeeds. Object storage that is not configured answers 503 under this subsystem's own name rather than falling through to another.
      operationId: get_s3_buckets_by_bucket_objects
      parameters:
      - in: path
        name: bucket
        required: true
        schema:
          type: string
      summary: Browse one level of a bucket
      tags:
      - storage
      x-app: storage
      x-public: true
    post:
      description: |-
        Returns a short-lived presigned PUT URL, with the method, the cleaned key and the seconds until it expires. The client uploads to that URL DIRECTLY — the bytes never pass through this API, and the storage credential never leaves the server.

        The URL is signed against the public storage host and scoped to exactly one bucket and key, and it expires five minutes after it is issued. The key is path-cleaned before signing, so a traversal cannot escape the bucket. A deployment with no public storage endpoint answers 503, because there is no host to sign a browser-followable URL against.

        A validated principal is required, and every bucket and key is resolved inside the caller's own org: physical bucket names are derived from the org, so a tenant cannot name another's storage. The operation is billed per call — the balance is checked BEFORE anything is touched, so an unfunded org is refused with nothing done, and the debit happens only after the work succeeds. Object storage that is not configured answers 503 under this subsystem's own name rather than falling through to another.
      operationId: post_s3_buckets_by_bucket_objects
      parameters:
      - in: path
        name: bucket
        required: true
        schema:
          type: string
      summary: Get a URL to upload one object directly
      tags:
      - storage
      x-app: storage
      x-public: true
  /v1/s3/health:
    get:
      description: |-
        A real readiness probe rather than a liveness stub: 200 only when the storage credentials are present, and it additionally reports whether presigning is available — the capability the two URL-issuing operations need and refuse without.

        An unconfigured deployment answers 503 with `ready:false` and the reason, which is the same state in which every data-plane operation here refuses. Not token-gated, so the platform can probe it without a credential, and it carries no credential, bucket or tenant detail.
      operationId: get_s3_health
      summary: Whether object storage is usable here
      tags:
      - storage
      x-app: storage
      x-public: true
  /v1/sandboxes:
    get:
      description: |-
        Lists the caller org's sandboxes, newest first. `project` and `status` narrow it, and both are read from the QUERY STRING.

        It answers from the org's own store rather than from the cluster, so a sandbox whose pod has since died still appears, carrying the status it was last known to have. That is deliberate: a lease you are being charged for should not vanish from the list because the thing behind it fell over.
      operationId: get_sandboxes
      summary: The sandboxes this org holds
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
    post:
      description: |-
        Creates a sandbox and returns it. `class` is one of `exec`, `dev`, `desktop` or `android`; `dev` and `desktop` are attached to a `project`, which is required for them and names the volume the work persists on. `ttlSec` bounds the lease, and `image` overrides the class default.

        This is the ONLY path that creates cluster objects. The isolation boundary is the pod's runtime class, one field, so what a sandbox is confined by is a deployment decision rather than anything this operation negotiates.
      operationId: post_sandboxes
      summary: Lease a sandbox
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}:
    delete:
      description: |-
        Stops the sandbox's pod and drops the lease. The VOLUME survives by default, so a `dev` or `desktop` sandbox can be leased again over the same project and find its checkout where it left it.

        `purge=1` deletes the volume too. It is opt-in because it is the one part of this that cannot be undone.
      operationId: delete_sandboxes_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: End a sandbox
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
    get:
      description: Returns one of the caller org's sandboxes. An id belonging to another
        org answers 404 and not 403 — a 403 would confirm the id exists, and whether
        a given sandbox exists is itself a cross-tenant fact.
      operationId: get_sandboxes_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: One sandbox
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/exec:
    post:
      description: |-
        Runs a command inside the sandbox and returns its exit code, stdout and stderr. A non-zero exit is a SUCCESSFUL call carrying a failed program — the HTTP status stays 200, because "the tests failed" and "the sandbox is broken" are different facts.

        NOTHING RUNS IN cloud. The command is streamed to the Kubernetes exec subresource of the sandbox's pod, which runs under the gVisor runtime class. The sandbox is addressed by pod NAME through the apiserver, never by address.
      operationId: post_sandboxes_by_id_exec
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Run a command in a sandbox
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/fs:
    get:
      description: Reads one file from the sandbox's project directory as text, or
        lists the entries when the path names a directory. Paths resolve under the
        project root and a path that climbs out is refused rather than rewritten.
      operationId: get_sandboxes_by_id_fs
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Read a file, or list a directory
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
    post:
      description: Writes the request body to one file in the sandbox's project directory,
        creating parent directories. Same confinement as the read above.
      operationId: post_sandboxes_by_id_fs
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Write a file
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/screen:
    get:
      description: |-
        A complete, self-contained desktop — noVNC inline, no other origin — that opens its own socket and draws this sandbox's display. Embed it in an iframe and there is nothing else to build.

        `ticket` is the credential from the POST above, carried through to the socket. The page is NOT gated: it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything. `frame-ancestors` admits our own brands' hosts and nothing further.

        It is served for every class, not only for `desktop`. The class is a fact about the image, and a sandbox with no VNC server already fails exactly — the connection is refused and the page says so — where a check here would be a second opinion about what is running inside a pod, formed from a label rather than from the pod.
      operationId: get_sandboxes_by_id_screen
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: The screen, as a page
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/screen/ticket:
    post:
      description: |-
        Mints a SINGLE-USE ticket for this sandbox's DISPLAY and returns `{ticket, expiresIn, url}`, where url is the desktop PAGE with the ticket already on it. The same ticket as the terminal's, minted for a different door.

        A ticket says which org and which sandbox, and the terminal and the screen are two views of one machine: a caller who may type in a sandbox may look at it. What the door decides is the URL handed back, which is the only part that differs.

        Mint one per screen, and mint a fresh one to reconnect.
      operationId: post_sandboxes_by_id_screen_ticket
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Open a screen
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/screen/ws:
    get:
      description: |-
        Upgrades to a WebSocket carrying RFB — the VNC wire protocol — from the sandbox's display, for a host that brings its own client. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.

        THE WIRE IS RFB, in BINARY frames both ways, and it is not interpreted here: this is a pipe between the caller's client and the server inside the pod.

        THE PIXELS COME OUT THROUGH THE EXEC CHANNEL. The display binds 127.0.0.1 only and deliberately nothing else, so there is no address to dial — `socat` joins the stream to that loopback port over the same Kubernetes exec subresource every other call into a sandbox uses. One way in, one thing to authorize, nothing further exposed.

        The window size is ignored. A browser pane is not the X server's geometry, and the client scales what it is given rather than asking a server with no RandR to resize itself.
      operationId: get_sandboxes_by_id_screen_ws
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: The screen, as a socket
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/terminal:
    get:
      description: |-
        A complete, self-contained terminal — xterm inline, no other origin — that opens its own socket and runs a shell in this sandbox. Embed it in an iframe and there is nothing else to build.

        `ticket` is the credential from the POST above and `arg` names the session (see the socket below); both are simply carried through to the socket. The page is NOT gated — it is inert markup and does not redeem the ticket, because a ticket is spent once and a page that spent it would hold a credential that no longer opens anything.

        When the terminal is up it posts `{source:"hanzo-term", ready:true}` to its parent frame, so a host can tell a live terminal from a page that failed into something else. `frame-ancestors` admits our own brands' hosts and nothing further.
      operationId: get_sandboxes_by_id_terminal
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: The terminal, as a page
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/terminal/ticket:
    post:
      description: |-
        Mints a SINGLE-USE ticket for one interactive terminal in this sandbox and returns `{ticket, expiresIn, url}`, where url is the terminal PAGE with the ticket already on it.

        It exists because a browser carries no Authorization header into a WebSocket or an iframe, so a terminal cannot be authenticated the way every other route here is. The ticket is a credential MINTED for that one terminal: bound to this org and this sandbox, valid for thirty seconds, and gone the first time it is presented. A long-lived bearer in a query string would instead be written into every access log on the path.

        Mint one per terminal, and mint a fresh one to reconnect.
      operationId: post_sandboxes_by_id_terminal_ticket
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Open a terminal
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/{id}/terminal/ws:
    get:
      description: |-
        Upgrades to a WebSocket carrying a login shell on a pseudo-terminal inside the sandbox — for a host that brings its own emulator. Requires `ticket`; a missing, expired or already-spent one answers 401 without upgrading.

        THE WIRE. A text frame is stdin, unless it is the one control object `{"resize":{"cols":N,"rows":M}}`; a binary frame is always stdin. Output comes back as BINARY frames, because a pty emits arbitrary bytes cut at arbitrary offsets and a text frame carrying half a rune is one the browser closes the connection over.

        `arg` names a SESSION: the shell runs under `tmux new -A -s <arg>`, which attaches to that session if it exists and creates it if it does not — so one sandbox holds as many terminals as a caller has names for. It is 1-64 characters of letters, digits, `-` or `_` and may not begin with `-`; anything else is 400. Without `arg` the shell is unnamed and unmultiplexed.

        The shell is `zsh -l`, falling back to `bash -l` and then to `sh -l`, and to the plain shell again when the image has no tmux. Every step is a preference and none is a requirement: whatever else the image carries — the hanzo CLI included — is a command to type, never a condition for getting a prompt.
      operationId: get_sandboxes_by_id_terminal_ws
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: The terminal, as a socket
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
  /v1/sandboxes/end:
    post:
      description: |-
        Ends the caller's sandbox lease: the pod goes, and the volume goes only
        when the caller asked for that too.
      operationId: end_sandbox
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/EndIn'
        required: true
      responses:
        "204":
          description: no content
      summary: End a sandbox and release it
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/sandboxes/lease:
    post:
      description: |-
        Leases the caller's sandbox, or returns the one it named if that
        lease is still running.

        What comes back is a real computer: a pod under a runtime boundary with a
        toolchain already in it, its own filesystem, and a lease that ends it. Every
        other op here acts on the one this returns.
      operationId: lease_sandbox
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/LeaseIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Leased'
          description: ok
      summary: Lease a sandbox — a real computer — or resume one you hold
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/sandboxes/read:
    post:
      description: |-
        Reads one path in the caller's sandbox: a file's bytes, or a
        directory's entries when the path names one.
      operationId: read_sandbox_file
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PathIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Blob'
          description: ok
      summary: Read a file from a sandbox you hold
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/sandboxes/run:
    post:
      description: |-
        Runs one command inside the caller's sandbox and answers its exit code,
        stdout and stderr. A non-zero exit is a successful call carrying a failed
        program, so it comes back as data and not as an error.

        Name a `session` and the command NARRATES INTO IT: its output is appended to
        that session's live log as the program produces it, so anything watching the
        session — GET /v1/agents/sessions/stream, scoped to one run with ?root= —
        watches the work happen rather than waiting for the verdict. Without it the
        call is what it always was: silent until it returns, which for an agentic run
        is twenty-five minutes of blank screen.

        The session is named; the TENANT is not. It is the org the caller already
        proved, so a session belonging to somebody else is absent from the org this
        call acts for and the append is refused there.
      operationId: run_in_sandbox
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/RunIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Ran'
          description: ok
      summary: Run a command in a sandbox you hold and read its output
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/sandboxes/stop:
    post:
      description: |-
        Interrupts whatever the caller's sandbox is running and answers how
        many commands it ended. The sandbox stays leased — stop ends the WORK, end ends
        the RESOURCE — so whoever stopped a run can still read what it left behind.
      operationId: stop_run
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/StopIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Stopped'
          description: ok
      summary: Stop what a sandbox is running, and keep the sandbox
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/sandboxes/write:
    post:
      description: |-
        Writes bytes to one path in the caller's sandbox, creating parents,
        and answers the resolved path.
      operationId: write_sandbox_file
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WriteIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Wrote'
          description: ok
      summary: Write a file into a sandbox you hold
      tags:
      - sandboxes
      x-app: sandboxes
      x-public: true
      x-tool: true
  /v1/search:
    post:
      description: |-
        Is the typed op behind POST /v1/search. It does exactly two things the
        in-process entry point must not do: resolve the tenant from the validated
        principal, and refuse when there is none. Everything else is ForOrg.
      operationId: search
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Request'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Response'
          description: ok
      summary: Hybrid search over the org's own corpora
      tags:
      - search
      x-app: search
      x-public: true
      x-tool: true
  /v1/search/indexes:
    get:
      description: |-
        Lists the search indexes with their document counts and timestamps.

        It reads the in-cluster Meilisearch service and reshapes its /stats and
        /indexes replies into the rows the console's Search panel renders. The read is
        degrade-friendly by design: an unreachable Meilisearch answers 200 with an
        EMPTY list, so the panel shows an honest empty state instead of an error.
        createdAt falls back to now and lastIndexedAt to null when the index list is
        unavailable.
      operationId: get_search_indexes
      parameters:
      - description: |-
          Authorization carries the surface's bearer key (`Bearer <key>`); the bare
          key is accepted too. It is not `validate:"required"` on purpose: requireKey
          answers absence itself, so an unconfigured surface 503s and a missing bearer
          401s — a validation refusal would rewrite both statuses.
        in: header
        name: Authorization
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/searchIndexList'
          description: ok
      summary: Lists the search indexes with their document counts and timestamps.
      tags:
      - search
      x-app: search
      x-public: true
      x-tool: true
  /v1/search/stats:
    get:
      description: |-
        Totals the documents across every search index.

        totalDocuments is summed from Meilisearch's own per-index counts. The other
        three fields are structurally zero rather than estimated: Meilisearch keeps no
        query-history counters, so searches, sessions and the per-day series are not
        derivable from the index and this surface reports the honest zero instead of a
        fabricated number. An unreachable Meilisearch answers 200 with all zeros.
      operationId: get_search_stats
      parameters:
      - description: |-
          Authorization carries the surface's bearer key (`Bearer <key>`); the bare
          key is accepted too. It is not `validate:"required"` on purpose: requireKey
          answers absence itself, so an unconfigured surface 503s and a missing bearer
          401s — a validation refusal would rewrite both statuses.
        in: header
        name: Authorization
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/searchStats'
          description: ok
      summary: Totals the documents across every search index.
      tags:
      - search
      x-app: search
      x-public: true
      x-tool: true
  /v1/sentinel/discover:
    post:
      description: |-
        Aggregates a project's captured errors into a table — the caller
        names the filters, the groupings and the aggregations, and gets back the
        columns and rows they asked for.

        The project is mandatory and is checked against the caller's own org before it
        scopes anything, so a project id belonging to someone else reads as absent
        rather than as data.
      operationId: post_sentinel_discover
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11yDiscoverIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yDiscoverOut'
          description: ok
      summary: Aggregates a project's captured errors into a table — the caller names
        the filters, the groupings and the aggregations, and gets back the columns
        and rows they asked for.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/events/{id}:
    get:
      description: |-
        Returns one captured error event of a project, by its id.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_events_by_id
      parameters:
      - description: ID is the event id.
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Project is the project the event belongs to, by its id. Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryEventOut'
          description: ok
      summary: Returns one captured error event of a project, by its id.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/issues:
    get:
      description: |-
        Lists the caller's org's grouped error issues, optionally
        narrowed to one project and one time window, and filtered by status, level,
        environment, service, a free-text query and a sort.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_issues
      parameters:
      - description: 'Status narrows to one lifecycle state: unresolved, resolved
          or ignored.'
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: Level narrows to one severity, e.g. error, warning, info.
        in: query
        name: level
        required: false
        schema:
          type: string
      - description: Environment narrows to one deployment environment.
        in: query
        name: environment
        required: false
        schema:
          type: string
      - description: ServiceName narrows to one reporting service.
        in: query
        name: serviceName
        required: false
        schema:
          type: string
      - description: Query narrows to issues whose text contains it.
        in: query
        name: query
        required: false
        schema:
          type: string
      - description: Sort orders the page, e.g. lastSeen, firstSeen, count.
        in: query
        name: sort
        required: false
        schema:
          type: string
      - description: Offset is how many issues to skip. Zero starts at the first.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      - description: Limit caps how many issues come back. Zero means the default.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Project narrows the org's issues to one project, by its id.
        in: query
        name: project
        required: false
        schema:
          type: string
      - description: Period is the window to read, relative to now — 1h, 24h, 7d,
          14d, 30d.
        in: query
        name: period
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorIssuesOut'
          description: ok
      summary: Lists the caller's org's grouped error issues, optionally narrowed
        to one project and one time window, and filtered by status, level, environment,
        service, a free-text query and a sort.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/issues/{id}:
    get:
      description: |-
        Returns one grouped issue of the caller's org with its latest
        occurrence sample.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_issues_by_id
      parameters:
      - description: ID is the issue id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorGettableIssueOut'
          description: ok
      summary: Returns one grouped issue of the caller's org with its latest occurrence
        sample.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    put:
      description: |-
        Changes an issue's lifecycle — resolve, ignore, reopen or
        assign — and returns the updated issue. Fields left unset are left unchanged.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: put_sentinel_issues_by_id
      parameters:
      - description: ID is the issue id.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySentryUpdateIssueIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yErrorIssueOut'
          description: ok
      summary: Changes an issue's lifecycle — resolve, ignore, reopen or assign —
        and returns the updated issue.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/issues/{id}/events:
    get:
      description: |-
        Lists one issue's captured occurrences, scoped to a project
        — a project is an isolation unit, so the caller declares which project's
        occurrences to read.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_issues_by_id_events
      parameters:
      - description: ID is the issue id.
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Project is the project whose occurrences to read, by its id.
          Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      - description: Limit caps how many occurrences come back. Zero means the default.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryIssueEventsOut'
          description: ok
      summary: Lists one issue's captured occurrences, scoped to a project — a project
        is an isolation unit, so the caller declares which project's occurrences to
        read.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/logs:
    get:
      description: |-
        Lists a project's captured error events, newest first, optionally
        narrowed to those whose message or exception text contains a search string.
      operationId: get_sentinel_logs
      parameters:
      - description: Project is the project to read, as its id. Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      - description: Query narrows the page to events whose text contains it.
        in: query
        name: query
        required: false
        schema:
          type: string
      - description: Period is the window to read, relative to now — 1h, 24h, 7d,
          14d, 30d.
        in: query
        name: period
        required: false
        schema:
          type: string
      - description: Limit caps how many events come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yLogsOut'
          description: ok
      summary: Lists a project's captured error events, newest first, optionally narrowed
        to those whose message or exception text contains a search string.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/projects:
    get:
      description: |-
        Lists the caller's org's Sentry projects, each with its
        freshly-derived DSN.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryProjectsOut'
          description: ok
      summary: Lists the caller's org's Sentry projects, each with its freshly-derived
        DSN.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a Sentry project under the caller's org and
        returns it, DSN included. Only the name, and optionally a slug and platform,
        are the caller's to set; the org, id and key are server-assigned.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_sentinel_projects
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/o11y.O11ySentryPostableProject'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryProjectOut'
          description: ok
      summary: Creates a Sentry project under the caller's org and returns it, DSN
        included.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/projects/{id}:
    delete:
      description: |-
        Deletes one Sentry project of the caller's org. Its DSN
        stops resolving immediately, so ingest for that id fails closed exactly as an
        unknown project does; retained events are not touched. Answers 204.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: delete_sentinel_projects_by_id
      parameters:
      - description: ID is the project id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes one Sentry project of the caller's org.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one Sentry project of the caller's org, DSN included.

        Callers need the viewer role; the runtime's own gate enforces it.
      operationId: get_sentinel_projects_by_id
      parameters:
      - description: ID is the project id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryProjectOut'
          description: ok
      summary: Returns one Sentry project of the caller's org, DSN included.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/projects/{id}/keys/rotate:
    post:
      description: |-
        Rotates a project's DSN key — bumping its rotation
        watermark so keys below it stop verifying — and returns the project with its
        new DSN.

        Callers need the editor role; the runtime's own gate enforces it.
      operationId: post_sentinel_projects_by_id_keys_rotate
      parameters:
      - description: ID is the project id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11ySentryProjectOut'
          description: ok
      summary: Rotates a project's DSN key — bumping its rotation watermark so keys
        below it stop verifying — and returns the project with its new DSN.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/stats:
    get:
      description: |-
        Returns a project's event-rate timeseries: one bucket per interval over
        the requested period, counting the events in it.
      operationId: get_sentinel_stats
      parameters:
      - description: Project is the project to read, as its id. Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      - description: Field is the dimension to count over. Empty counts all events.
        in: query
        name: field
        required: false
        schema:
          type: string
      - description: Period is the window to read, relative to now — 1h, 24h, 7d,
          14d, 30d.
        in: query
        name: period
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yStatsOut'
          description: ok
      summary: 'Returns a project''s event-rate timeseries: one bucket per interval
        over the requested period, counting the events in it.'
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/traces:
    get:
      description: |-
        Lists the traces a project's captured errors reference, each with how
        many errors landed on it, when they started and stopped, and the latest
        message seen — the entry point for "which requests are failing".
      operationId: get_sentinel_traces
      parameters:
      - description: Project is the project to read, as its id. Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      - description: Period is the window to read, relative to now — 1h, 24h, 7d,
          14d, 30d.
        in: query
        name: period
        required: false
        schema:
          type: string
      - description: Limit caps how many traces come back.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTracesOut'
          description: ok
      summary: Lists the traces a project's captured errors reference, each with how
        many errors landed on it, when they started and stopped, and the latest message
        seen — the entry point for "which requests are failing".
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/sentinel/traces/{id}:
    get:
      description: |-
        Returns one trace's captured errors for a project — every error event
        that carried the trace id, in the order the events plane holds them.
      operationId: get_sentinel_traces_by_id
      parameters:
      - description: ID is the trace id.
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: Project is the project the trace's errors belong to. Required.
        in: query
        name: project
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/o11y.O11yTraceOut'
          description: ok
      summary: Returns one trace's captured errors for a project — every error event
        that carried the trace id, in the order the events plane holds them.
      tags:
      - o11y
      x-app: o11y
      x-public: true
      x-tool: true
  /v1/settings/{product}:
    get:
      description: |-
        Reads the caller org's configuration for one product, with every
        secret field MASKED — only the names of the set secrets come back, never their
        values, which live in KMS. A product the org has never configured is not a 404:
        it answers 200 with an empty config object, so the console's Settings tab always
        renders and merges its own display defaults on top.
      operationId: get_settings_by_product
      parameters:
      - description: Product is the catalog slug, from the path. Must match ^[a-z0-9][a-z0-9._-]{0,62}$.
        in: path
        name: product
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/settingsView'
          description: ok
      summary: Reads the caller org's configuration for one product, with every secret
        field MASKED — only the names of the set secrets come back, never their values,
        which live in KMS.
      tags:
      - settings
      x-app: settings
      x-public: true
      x-tool: true
    put:
      description: |-
        Writes the caller org's configuration for one product and answers the
        stored result, secrets masked. Secret VALUES are sealed into KMS under
        orgs/{org}/settings/{product}/{key} and never touch this deployment's database;
        with no KMS configured a write that carries any secret is refused whole (503)
        rather than dropping it or persisting it in the clear. A secret the body omits
        keeps its stored value, so a partial write never silently clears one.
      operationId: put_settings_by_product
      parameters:
      - description: |-
          Product is the catalog slug, from the PATH. zip binds the path last, so the
          URL names the product being written whatever a body field claims.
        in: path
        name: product
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/settingsReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/settingsView'
          description: ok
      summary: Writes the caller org's configuration for one product and answers the
        stored result, secrets masked.
      tags:
      - settings
      x-app: settings
      x-public: true
      x-tool: true
  /v1/sync:
    get:
      description: |-
        List returns every sync link the caller's org has, each with its two endpoints, its
        direction and trigger policy, and the time it last reconciled. Scoped to the
        caller's own org — another tenant's links are structurally unreachable.
      operationId: get_sync
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/syncList'
          description: ok
      summary: List returns every sync link the caller's org has, each with its two
        endpoints, its direction and trigger policy, and the time it last reconciled.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
    post:
      description: |-
        Create declares a sync between two endpoints and returns it. It is an UPSERT:
        re-declaring the same source and target updates that link rather than piling up
        duplicates, so a console that re-submits is safe. The org comes from the validated
        principal, never from the request, so a sync can only ever bind endpoints inside
        the caller's own org. A git source must be an https clone URL on the provider's own
        host with no embedded credentials; a target left empty is derived as a native
        repository named after the source. With run=true the first reconcile is queued in
        the background, so a large initial import never blocks this response.
      operationId: post_sync
      requestBody:
        content:
          application/json:
            example:
              run: true
              source:
                locator: https://github.com/acme/site
                provider: github
            schema:
              $ref: '#/components/schemas/syncReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/syncView'
          description: ok
      summary: Create declares a sync between two endpoints and returns it.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
  /v1/sync/{id}:
    delete:
      description: |-
        Delete removes one sync and tears down the outbound mirror it derived, answering
        204. The teardown is the point: without it an unsynced repository would keep
        force-pushing to the upstream it is no longer linked to. Org-scoped, so another
        tenant's id is the same 404 an unknown id gives.
      operationId: delete_sync_by_id
      parameters:
      - description: ID is the sync to act on, from the path.
        example: sync_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Delete removes one sync and tears down the outbound mirror it derived,
        answering 204.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
    get:
      description: |-
        Get returns one sync by id. It is org-scoped: an id belonging to another tenant is
        the same 404 an unknown id gives, so a probe learns nothing about what exists.
      operationId: get_sync_by_id
      parameters:
      - description: ID is the sync to act on, from the path.
        example: sync_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/syncView'
          description: ok
      summary: Get returns one sync by id.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
    patch:
      description: |-
        Patch updates one sync's mutable policy — direction, trigger and actor — in place.
        The endpoints and the kind are immutable: re-pointing a sync is a delete and a
        create, so a link can never silently start syncing somewhere else. A field the
        request omits is left as it was. Changing the direction immediately reconciles the
        derived outbound mirror, so turning push off stops the upstream being written to
        rather than merely recording the intent.
      operationId: patch_sync_by_id
      parameters:
      - description: ID is the sync to update, from the path.
        example: sync_1
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              direction: pull
              id: sync_1
            schema:
              $ref: '#/components/schemas/patchSyncIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/syncView'
          description: ok
      summary: Patch updates one sync's mutable policy — direction, trigger and actor
        — in place.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
  /v1/sync/{id}/run:
    post:
      description: |-
        Run reconciles one sync now — the manual re-sync, and the initial import for a link
        created without run=true. The work is handed to a bounded background worker and the
        call answers 202 immediately, so a large mirror-in never holds the request open;
        queued=true means accepted, not finished.
      operationId: post_sync_by_id_run
      parameters:
      - description: ID is the sync to act on, from the path.
        example: sync_1
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/syncQueued'
          description: accepted
      summary: Run reconciles one sync now — the manual re-sync, and the initial import
        for a link created without run=true.
      tags:
      - sync
      x-app: sync
      x-public: true
      x-tool: true
  /v1/tasks:
    delete:
      description: |-
        Answers 307 with Location /v1/tasks/ — this address serves nothing itself. The redirect is a routing fact derived from the engine's subtree, decided before any handler runs, so it is the same on every method.

        A 307 preserves both the method and the body, so a client that follows redirects re-sends the request unchanged to /v1/tasks/ and nothing is lost. A client that does NOT follow redirects sees only the 307 and performs no work — address /v1/tasks/ directly and the hop disappears.
      operationId: delete_tasks
      summary: Redirect to the tasks API root
      tags:
      - tasks
      x-app: tasks
      x-public: true
    get:
      description: |-
        Answers 307 with Location /v1/tasks/ — this address serves nothing itself. The redirect is a routing fact derived from the engine's subtree, decided before any handler runs, so it is the same on every method.

        A 307 preserves both the method and the body, so a client that follows redirects re-sends the request unchanged to /v1/tasks/ and nothing is lost. A client that does NOT follow redirects sees only the 307 and performs no work — address /v1/tasks/ directly and the hop disappears.
      operationId: get_tasks
      summary: Redirect to the tasks API root
      tags:
      - tasks
      x-app: tasks
      x-public: true
    patch:
      description: |-
        Answers 307 with Location /v1/tasks/ — this address serves nothing itself. The redirect is a routing fact derived from the engine's subtree, decided before any handler runs, so it is the same on every method.

        A 307 preserves both the method and the body, so a client that follows redirects re-sends the request unchanged to /v1/tasks/ and nothing is lost. A client that does NOT follow redirects sees only the 307 and performs no work — address /v1/tasks/ directly and the hop disappears.
      operationId: patch_tasks
      summary: Redirect to the tasks API root
      tags:
      - tasks
      x-app: tasks
      x-public: true
    post:
      description: |-
        Answers 307 with Location /v1/tasks/ — this address serves nothing itself. The redirect is a routing fact derived from the engine's subtree, decided before any handler runs, so it is the same on every method.

        A 307 preserves both the method and the body, so a client that follows redirects re-sends the request unchanged to /v1/tasks/ and nothing is lost. A client that does NOT follow redirects sees only the 307 and performs no work — address /v1/tasks/ directly and the hop disappears.
      operationId: post_tasks
      summary: Redirect to the tasks API root
      tags:
      - tasks
      x-app: tasks
      x-public: true
    put:
      description: |-
        Answers 307 with Location /v1/tasks/ — this address serves nothing itself. The redirect is a routing fact derived from the engine's subtree, decided before any handler runs, so it is the same on every method.

        A 307 preserves both the method and the body, so a client that follows redirects re-sends the request unchanged to /v1/tasks/ and nothing is lost. A client that does NOT follow redirects sees only the 307 and performs no work — address /v1/tasks/ directly and the hop disappears.
      operationId: put_tasks
      summary: Redirect to the tasks API root
      tags:
      - tasks
      x-app: tasks
      x-public: true
  /v1/team/account:
    post:
      description: |-
        The account control plane the Team client speaks: one POST carries a `method` verb and its `params`, and answers {"result": …}. The verbs are the session's own reads and the workspace switch — getLoginInfoByToken, getUserWorkspaces, selectWorkspace, getWorkspaceInfo, getMemberships, getPerson, getSocialIds, getRegionInfo, isReadOnlyGuest — plus sendInvite, which adds a member to a workspace and is refused for a caller who is not its owner or admin.

        A REFUSAL IS HTTP 200 carrying {"error": {severity, code, params}} — the platform Status the client translates — not a 4xx. An unreadable body, an unauthorized session and an unknown verb all arrive that way, so a caller that reads only the status code reads every failure here as a success.

        NO CREDENTIAL IS EVER HANDLED HERE. login, signUp, the OTP verbs, password change and reset, join and the guest-token exchange each answer Unauthorized with "sign in at hanzo.id" — a stated policy, not an unknown method, so the door being shut is a fact a test can pin. Sessions come from the OAuth pair under /account/auth.

        Auth is the team session token: Authorization: Bearer, else the HttpOnly account-token cookie. The tenant is that token's SIGNED org claim, never a header, and selectWorkspace resolves only among the orgs the token proves membership of. It also demands an explicit workspaceUrl — it never falls back to a first workspace, and a slug that resolves in two of the caller's orgs answers Ambiguous rather than picking one.
      operationId: post_team_account
      summary: Read the caller's account and switch workspace
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/account/auth/{provider}:
    get:
      description: |-
        STARTS the OAuth hop: answers 302 to hanzo.id's authorize endpoint and sets the short-lived HttpOnly state cookie that binds the flow to this browser. NO TOKEN COMES BACK FROM THIS CALL — the session is minted by the callback below, and a client that expects JSON here gets a redirect with no body.

        A browser is the intended caller. Anything else must follow the Location AND keep the Set-Cookie, because the callback refuses a flow whose state it cannot match. That cookie carries the random nonce plus the client's navigateUrl, so the round trip needs no second channel, and it lives ten minutes — the whole budget for the hop.

        The provider segment only picks a hint: the redirect_uri is ALWAYS the canonical openid callback, the one IAM has registered. Measured end to end, hanzo.id strips that hint today, so /auth/google and /auth/openid land on the same Hanzo sign-in page — the federation shortcut is an upstream fix, not a second door here.
      operationId: get_team_account_auth_by_provider
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: Start a sign-in at hanzo.id
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/account/auth/{provider}/callback:
    get:
      description: |-
        COMPLETES the OAuth hop: hanzo.id redirects the browser here with ?code and ?state, and the answer is another 302 — back to the client's login route carrying the freshly minted team session token in the query. Never JSON, and never a token in this response's own body.

        THE STATE IS CHECKED FIRST, before the code is even looked at: the flow cookie is read and cleared one-shot, and a callback whose ?state does not equal the nonce it held is bounced with error=state_mismatch and NEVER exchanged. That is what makes a forged or replayed callback inert. Only then is the code exchanged server-side — team is a confidential client with a client_secret, so there is no PKCE and the code never passes through the browser's JS.

        The tenant is derived from the IAM access token VERIFIED RS256 against the JWKS, the same trust anchor the identity boundary uses; a token whose owner claim is empty fails closed with no login at all. Every org that token proves gets a workspace ensured, so a member of two orgs is a counted seat in both. The IAM access token is also parked in an HttpOnly cookie for the same-origin agents proxy — page JS never reads it.

        EVERY failure is a redirect, not a status: a denied consent, a missing code, a failed exchange, an unreadable userinfo, an unverifiable org and a token-mint failure each bounce to the login page with an ?error code naming the step.
      operationId: get_team_account_auth_by_provider_callback
      parameters:
      - in: path
        name: provider
        required: true
        schema:
          type: string
      summary: Complete a sign-in and hand the browser its session
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/account/cookie:
    delete:
      description: |-
        Signs this browser out of team by expiring the HttpOnly
        account-token cookie the OAuth callback set. It is the counterpart of the
        cookie PUT, it takes nothing — the cookie it clears is named by this service,
        never by the caller — and it is unconditional: a caller with no cookie, an
        expired one or a forged one all get the same acknowledgement, because clearing
        something that is not there is the same outcome as clearing something that is.

        It clears ONLY the team session cookie. The IAM access-token cookie the same
        callback set is a different credential with a different lifetime and is left
        alone, so this is a team sign-out, not a platform one.
      operationId: delete_team_account_cookie
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/cookieAck'
          description: ok
      summary: Signs this browser out of team by expiring the HttpOnly account-token
        cookie the OAuth callback set.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
    put:
      description: |-
        Writes the team session token into the HttpOnly `account-token` cookie — Secure, SameSite=Lax, whole-origin scope, thirty days — and answers {"result": true}. This is how the client turns the token it caught off the OAuth bounce into a credential page JS can no longer read, which IS the security property: script that cannot see the cookie cannot exfiltrate it, and every later call on the files, billing and collaborator planes authenticates from it when no bearer is sent.

        The token is VERIFIED — signature and expiry, against this service's own signing secret — BEFORE it is stored. Anything this service did not sign is 401 and nothing is written; persisting a caller-supplied value unchecked would be a session-fixation door, where an attacker pins a cookie the victim's browser then presents as its own.

        The token may arrive as `token` in the JSON body or, when the body is absent or unparseable, from the Authorization bearer — an unreadable body is NOT an error here. The sibling DELETE clears this same cookie and signs the browser out of team only: the IAM cookie set alongside it is a different credential with its own lifetime and is left alone.
      operationId: put_team_account_cookie
      summary: Store the session token as this browser's cookie
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/account/providers:
    get:
      description: |-
        Returns the identity providers this deployment starts a login
        with. It is always exactly one — hanzo.id. Which identities that door accepts
        (Google, GitHub, passkey, password) is IAM's question, answered on IAM's own
        page next to the identity check and the training-data consent that must
        precede a first session; listing them here would be a second place holding
        that answer, and the two drift the moment IAM gains or drops one.
      operationId: get_team_account_providers
      responses:
        "200":
          content:
            application/json:
              example:
              - displayName: Hanzo
                name: openid
              schema:
                items:
                  $ref: '#/components/schemas/ProviderInfo'
                type: array
          description: ok
      summary: Returns the identity providers this deployment starts a login with.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/billing/plan:
    get:
      description: |-
        Returns the plan and seat counts for the caller's OWN org, resolved
        from the VERIFIED team session token — never a client header. Seats and guests
        are the org's distinct active human members (a bot member is not a seat); the
        plan comes from the licensing entitlement and is empty when that read is
        unavailable, so the page shows an honest dash rather than a fabricated tier. A
        caller with no verified session gets 401, and a real seat-read failure is a
        502 rather than a false "0 members".
      operationId: get_team_billing_plan
      responses:
        "200":
          content:
            application/json:
              example:
                active: true
                guestLimit: 3
                guests: 1
                plan: pro
                seats: 3
                upgradeUrl: https://billing.hanzo.ai
              schema:
                $ref: '#/components/schemas/planInfo'
          description: ok
      summary: Returns the plan and seat counts for the caller's OWN org, resolved
        from the VERIFIED team session token — never a client header.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/billing/ui:
    get:
      description: |-
        Serves the usage-and-wallet page the Team front links to — HTML, not JSON. It is a static React build compiled into this binary, so there is no upstream to be down and no build step at request time.

        SESSION-GATED: without a verified team session token — bearer, else the HttpOnly account cookie — the caller gets 401 and not one byte of the page, so an anonymous browser meets a refusal rather than a shell that then fails to load anything.

        The page is markup only. It reads money SAME-ORIGIN from cloud's own balance and usage endpoints, which pin the org from the IAM cookie the team OAuth callback set — nothing under this path proxies a money read, so there is no second auth mechanism here to get wrong. A deployment whose page was never built answers 503 naming the missing bundle, never a blank 200.
      operationId: get_team_billing_ui
      summary: Open the wallet page
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/bots:
    get:
      description: |-
        Returns the caller org's bot members — the org's agents projected as
        the workspace Employees they become, each with the member account uuid and
        Person reference the roster addresses it by. An agents subsystem that is not
        mounted answers an empty list, never an error.
      operationId: get_team_bots
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/botRoster'
          description: ok
      summary: Returns the caller org's bot members — the org's agents projected as
        the workspace Employees they become, each with the member account uuid and
        Person reference the roster addresses it by.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/bots/sync:
    post:
      description: |-
        SyncBots re-projects the caller org's agents as workspace members into EVERY
        workspace of the org, and removes the ones whose agent is gone. It is
        idempotent, and admin only: mutating a workspace's roster requires the
        gateway-minted admin flag, which a client can never forge. It answers how many
        roster entries the reconcile touched.
      operationId: post_team_bots_sync
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/botSync'
          description: ok
      summary: SyncBots re-projects the caller org's agents as workspace members into
        EVERY workspace of the org, and removes the ones whose agent is gone.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/collaborator:
    get:
      description: |-
        Upgrades to the hocuspocus WebSocket the Team editor syncs its Y.js documents over: binary frames of document name, message type and payload, with ONE socket multiplexing every document a tab has open. The server is a relay and an ordered update log, not a CRDT engine — it replays the log to each joining peer and broadcasts every update to the rest, which converges because Y.js updates are commutative and idempotent. There is no body; the response is a protocol upgrade.

        BOTH LANES SHARE ONE ROOT. The client derives them from one configured URL — this socket at its root, the markup-snapshot RPC one segment in — so pointing the editor at this service is one value, and the two lanes cannot drift apart.

        AUTH IS IN-BAND, PER DOCUMENT, NOT ON THE UPGRADE. The handshake gates only on browser Origin (403 outside the team surfaces; no Origin at all is admitted, which is what a non-browser sends), and then the first frame for a document must be an Auth message carrying the same session or workspace token every other team route verifies — a browser WebSocket cannot set an Authorization header, which is why the token rides inside the protocol. Anything else on an unauthenticated document is answered with one permission denial and nothing further.

        Every document is authorized on its own: the document's workspace must be the token's workspace when the token pins one, and the caller must be a member of it. A mismatch, an unknown workspace and a non-member deny alike with "document not found". Rooms are keyed by org and workspace and the persisted log's key embeds both, so a foreign document id can neither join a room nor read a blob.

        The server pings every twenty seconds and drops a socket silent for sixty, so a backgrounded tab — whose JS timers are throttled but whose network stack still auto-pongs — stays connected instead of dying into a reconnect loop.
      operationId: get_team_collaborator
      summary: Open the live collaborative-editing socket
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/collaborator/rpc/{documentId}:
    post:
      description: |-
        CollabRPC is the collaborative-markup snapshot plane the Team front's editor
        speaks: createContent stores a document field's markup at a fresh, immutable
        blob ref and returns it, updateContent stores a new snapshot and answers
        nothing, and getContent reads back the exact snapshot a ref names.

        createContent ALSO seeds the live-editing update log from the front-supplied
        Y.js update, so a dialog-authored description is visible in the collaborative
        editor — which replays that log — and not only in snapshot reads.
        updateContent never touches that log: peers may be live-editing the document,
        and their edits are not this call's to overwrite.

        Every call is scoped to the caller's VERIFIED session or workspace token: the
        documentId's workspace must be the token's workspace when the token names one,
        and the caller must be a member of it. An unknown workspace, another tenant's
        workspace and a workspace the caller is not in all answer the same 404, so a
        probe learns nothing about what exists.
      operationId: post_team_collaborator_rpc_by_documentid
      parameters:
      - description: |-
          DocumentID addresses the document field, as
          "<workspaceUuid>|<objectClass>|<objectId>|<objectAttr>" — the
          collaborator-client encodeDocumentId shape, from the path.
        example: 6579…|tracker:class:Issue|issue-1|description
        in: path
        name: documentId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              documentId: 6579…|tracker:class:Issue|issue-1|description
              method: getContent
              payload:
                source: issue-1-description-1730000000000
            schema:
              $ref: '#/components/schemas/collabRequest'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/collabResult'
          description: ok
      summary: 'CollabRPC is the collaborative-markup snapshot plane the Team front''s
        editor speaks: createContent stores a document field''s markup at a fresh,
        immutable blob ref and returns it, updateContent stores a new snapshot and
        answers nothing, and getContent reads back the exact snapshot a ref names.'
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/files/{workspace}:
    post:
      description: |-
        Stores one file in a workspace's blob store and answers the blob id it is addressable by, as plain text — the front discards that body, it is there for a caller driving this by hand.

        The body is a multipart form with a `file` part, and THAT PART'S FILENAME IS THE BLOB ID: the client mints it (a uuid v4) and the server stores under it, so a part whose filename is not a uuid is refused rather than assigned one. A file over 100 MiB is 413 and an empty one is 400.

        The caller must hold a verified session or workspace token AND be a member of the workspace; an unknown workspace, another tenant's workspace and a workspace the caller is not in all answer the same 404, so a probe learns nothing about what exists. The stored key embeds the verified org and the workspace, so an upload cannot land in another tenant's box whatever id it names. A storage backend that is unavailable fails closed with 502 rather than reporting a write it never made.
      operationId: post_team_files_by_workspace
      parameters:
      - in: path
        name: workspace
        required: true
        schema:
          type: string
      summary: Upload a file into a workspace
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/files/{workspace}/{filename}:
    delete:
      description: |-
        Removes one blob from a workspace's file store. The caller must
        hold a verified session AND be a member of the workspace; anything else — an
        unknown workspace, another tenant's workspace, a workspace the caller is not
        in — answers the same 404, so a probe learns nothing about what exists.

        It is IDEMPOTENT: deleting a present or an absent blob both answer 204, so a
        delete never confirms a blob's existence and a foreign blob id (a physical key
        the caller can never name into another tenant's box) is a harmless no-op. A
        storage backend that is unavailable fails closed with 502 rather than lying
        about success.
      operationId: delete_team_files_by_workspace_by_filename
      parameters:
      - description: Workspace is the workspace uuid the blob belongs to, from the
          path.
        example: 6579…
        in: path
        name: workspace
        required: true
        schema:
          type: string
      - description: |-
          Filename is the last path segment, which the front sets to the blob id
          when it sends no explicit `file`.
        in: path
        name: filename
        required: true
        schema:
          type: string
      - description: File is the blob id, and wins over the path segment when both
          are present.
        example: 0d4f…
        in: query
        name: file
        required: false
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one blob from a workspace's file store.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
    get:
      description: |-
        Streams one blob's raw BYTES back — this is the read side of the workspace file store, not a JSON envelope around it.

        THE BLOB IS NAMED BY THE `file` QUERY PARAMETER, NOT BY :filename. The path segment is only the name a browser saves the download under; a request without ?file= is a 400 no matter what the path says.

        The Content-Type is derived from the STORED BYTES, never from the name: only png, jpeg, gif and webp, recognized by their magic bytes, are served inline under their true type, and everything else is served inert as application/octet-stream with an attachment disposition. Every response carries nosniff, so a file uploaded under an .svg or .html name cannot be talked into executing in a viewer's origin. Blobs are immutable, so a hit caches privately for a year.

        Same gate as the upload: verified token, membership of the workspace. A genuine miss, another tenant's workspace, a workspace the caller is not in, and a blob id belonging to a different workspace are ONE answer — 404 — because the physical key is org- and workspace-scoped and a foreign id is simply a key that does not exist. An unavailable backend is a 502, never an empty 200.
      operationId: get_team_files_by_workspace_by_filename
      parameters:
      - in: path
        name: workspace
        required: true
        schema:
          type: string
      - in: path
        name: filename
        required: true
        schema:
          type: string
      summary: Download a workspace file
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/transactor/{token}:
    get:
      description: |-
        Upgrades to the WebSocket the Team client runs an entire workspace over: every frame is a ZAP envelope wrapping one JSON-RPC message — findAll/findOne reads against the workspace's documents, tx writes that broadcast to the other live sessions, hello negotiating JSON rather than msgpack. The response is a protocol upgrade, so there is no body to read.

        THE PATH SEGMENT IS THE CREDENTIAL. It is the workspace token selectWorkspace minted — bearer-equivalent, and sitting in a URL that proxies and access logs record, which is exactly why it expires in twelve hours and is re-minted on demand rather than being long-lived like the session token. It is decoded and verified (signature and expiry) BEFORE the upgrade, so a bad one is a 401 and never a socket that is accepted and then dropped, and it must carry both an account and a workspace claim. Nothing ambient authorizes this socket: a WebSocket is exempt from CORS, so a cookie-borne credential would make the Origin check the only access control on the whole data plane.

        The tenant is the token's SIGNED org claim and it keys every store path, so no header can name another workspace's data. The upgrade ALSO refuses a browser Origin outside the team surfaces with 403 — otherwise any page could open an authenticated socket with a token it lured out of a logged-in browser — while a request with no Origin at all is admitted, because that is what a non-browser client sends.

        On connect the workspace's system spaces are seeded once and the roster is reconciled every time, so the org's human members and its bots are present as workspace people without a separate sync call.
      operationId: get_team_transactor_by_token
      parameters:
      - in: path
        name: token
        required: true
        schema:
          type: string
      summary: Open the workspace data-plane socket
      tags:
      - team
      x-app: team
      x-public: true
  /v1/team/transactor/api/v1/statistics:
    get:
      description: |-
        Statistics returns the transactor's live sessions for the workspace the caller's
        credential names — the endpoint the front's workspace switcher and server panel
        poll on the transactor base. `token` carries the same two lanes the socket's path
        segment does: a workspace UUID names the workspace and is authorized against the
        membership rows, an HS256 workspace token names it in its signed claims.
        activeSessions carries ONLY that one workspace, never another tenant's sessions.
        An unverifiable credential, or one the caller is no member under, is 401.
      operationId: get_team_transactor_api_v1_statistics
      parameters:
      - description: Token is the workspace token minted by selectWorkspace.
        example: eyJhbGciOiJIUzI1NiJ9…
        in: query
        name: token
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/statsOut'
          description: ok
      summary: Statistics returns the transactor's live sessions for the workspace
        the caller's credential names — the endpoint the front's workspace switcher
        and server panel poll on the transactor base.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/team/transactor/statistics:
    get:
      description: |-
        Statistics returns the transactor's live sessions for the workspace the caller's
        credential names — the endpoint the front's workspace switcher and server panel
        poll on the transactor base. `token` carries the same two lanes the socket's path
        segment does: a workspace UUID names the workspace and is authorized against the
        membership rows, an HS256 workspace token names it in its signed claims.
        activeSessions carries ONLY that one workspace, never another tenant's sessions.
        An unverifiable credential, or one the caller is no member under, is 401.
      operationId: get_team_transactor_statistics
      parameters:
      - description: Token is the workspace token minted by selectWorkspace.
        example: eyJhbGciOiJIUzI1NiJ9…
        in: query
        name: token
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/statsOut'
          description: ok
      summary: Statistics returns the transactor's live sessions for the workspace
        the caller's credential names — the endpoint the front's workspace switcher
        and server panel poll on the transactor base.
      tags:
      - team
      x-app: team
      x-public: true
      x-tool: true
  /v1/tel/calls:
    get:
      description: |-
        Lists the calls this org has placed or received, newest first. Like the
        message list beside it, these are our own records rather than the carrier's.
      operationId: get_tel_calls
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/callList'
          description: ok
      summary: Lists the calls this org has placed or received, newest first.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
    post:
      description: |-
        Dials. An `agent` names a Hanzo assistant to answer it; the call is
        refused up front when no assistant plane is configured, because a call that
        connects to silence has already cost the person who answered it.
      operationId: post_tel_calls
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/callInput'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Call'
          description: created
      summary: Dials.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/calls/{id}:
    delete:
      description: |-
        Ends a call this org placed. The holding is read for THIS org before the
        carrier is asked, for the reason releaseNumber gives one surface up: an id
        belonging to another tenant would otherwise be hung up by whoever guessed it.
      operationId: delete_tel_calls_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/noInput'
          description: ok
      summary: Ends a call this org placed.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/messages:
    get:
      description: |-
        Lists the messages this org has sent or received, newest first. Records from
        our own store, not the carrier's — so it is what this platform did on the
        org's behalf, which is the set an audit or a bill has to agree with.
      operationId: get_tel_messages
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/messageList'
          description: ok
      summary: Lists the messages this org has sent or received, newest first.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
    post:
      description: |-
        Sends a message from one of this org's own numbers.

        `from` must be a number the org HOLDS, checked against the store rather than
        taken on trust — a caller that could send from any number could impersonate
        one, and the carrier would deliver it. `to` is required, and the body needs
        text or media, because a message with neither is delivered as nothing and
        billed as something.
      operationId: post_tel_messages
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/messageInput'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SMS'
          description: created
      summary: Sends a message from one of this org's own numbers.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/numbers:
    get:
      description: |-
        Lists the phone numbers this org HOLDS — the ones it has bought and not
        released. Distinct from the availability search one path down
        (`/numbers/available`), which asks the carrier what could be bought: this
        answers only from our own store, so it is what an org owns rather than what
        it could own.
      operationId: get_tel_numbers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/numberList'
          description: ok
      summary: Lists the phone numbers this org HOLDS — the ones it has bought and
        not released.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
    post:
      description: |-
        Provisions with the carrier FIRST and records second. The other order
        records a holding that may not exist, and a number the platform believes it owns
        but cannot use is worse than one it failed to buy.
      operationId: post_tel_numbers
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/buyInput'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Number'
          description: created
      summary: Provisions with the carrier FIRST and records second.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/numbers/{id}:
    delete:
      description: |-
        Checks the holding is THIS org's before it reaches the carrier.
        Without that read, an id belonging to another tenant would be released by
        whoever guessed it.
      operationId: delete_tel_numbers_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/noInput'
          description: ok
      summary: Checks the holding is THIS org's before it reaches the carrier.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/numbers/available:
    get:
      description: |-
        Asks the carrier what is available to buy. Nothing is recorded —
        a search is not a holding, and treating it as one is how inventory leaks.
      operationId: get_tel_numbers_available
      parameters:
      - in: query
        name: Country
        required: false
        schema:
          type: string
      - in: query
        name: Area
        required: false
        schema:
          type: string
      - in: query
        name: Type
        required: false
        schema:
          type: string
      - in: query
        name: Limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/numberList'
          description: ok
      summary: Asks the carrier what is available to buy.
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/tel/summary:
    get:
      description: |-
        Counts what this org holds on the telephony plane: its numbers, its calls and
        its messages. The one read a dashboard makes before it asks for any list, so
        it answers three totals and no rows.
      operationId: get_tel_summary
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/summary'
          description: ok
      summary: 'Counts what this org holds on the telephony plane: its numbers, its
        calls and its messages.'
      tags:
      - tel
      x-app: tel
      x-public: true
      x-tool: true
  /v1/templates:
    get:
      description: |-
        Lists the public starter-kit catalog plus, for a validated caller, that
        org's own private kits. No request field can widen the scope: the org comes
        from the validated principal, so an anonymous or cross-org caller structurally
        sees the public catalog only.
      operationId: get_templates
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/kitList'
          description: ok
      summary: Lists the public starter-kit catalog plus, for a validated caller,
        that org's own private kits.
      tags:
      - templates
      x-app: templates
      x-public: true
      x-tool: true
    post:
      description: |-
        Creates a starter kit PRIVATE to the caller's org and answers 201 with
        the stored kit. The owner is stamped by the server, so a body "org" is never
        trusted; publishing over a public-catalog slug is 409, so a slug still names
        exactly one kit.
      operationId: post_templates
      requestBody:
        content:
          application/json:
            example:
              framework: Next.js 14
              slug: acme-portal
              title: Acme Internal Portal
            schema:
              $ref: '#/components/schemas/publishKitIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StarterKit'
          description: created
      summary: Creates a starter kit PRIVATE to the caller's org and answers 201 with
        the stored kit.
      tags:
      - templates
      x-app: templates
      x-public: true
      x-tool: true
  /v1/templates/{slug}:
    delete:
      description: |-
        Deletes the caller org's OWN starter kit. A slug they do not own is a
        404, never a delete: the DELETE binds org.
      operationId: delete_templates_by_slug
      parameters:
      - description: Slug is the starter kit to act on, from the path.
        example: acme-portal
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deletes the caller org's OWN starter kit.
      tags:
      - templates
      x-app: templates
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one starter kit: the caller org's own by that slug, else the public
        catalog's. A slug another org owns reads as not found.
      operationId: get_templates_by_slug
      parameters:
      - description: Slug is the starter kit to act on, from the path.
        example: folio
        in: path
        name: slug
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StarterKit'
          description: ok
      summary: 'Returns one starter kit: the caller org''s own by that slug, else
        the public catalog''s.'
      tags:
      - templates
      x-app: templates
      x-public: true
      x-tool: true
    put:
      description: |-
        Overwrites the caller org's OWN starter kit at the path slug, answering
        the stored kit. A slug they do not own is 404, never a create: the UPDATE binds
        org, so a PUT can never reach another org's kit.
      operationId: put_templates_by_slug
      parameters:
      - description: Slug is the kit to replace, from the path.
        example: acme-portal
        in: path
        name: slug
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              slug: acme-portal
              title: Acme Internal Portal v2
            schema:
              $ref: '#/components/schemas/replaceKitIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StarterKit'
          description: ok
      summary: Overwrites the caller org's OWN starter kit at the path slug, answering
        the stored kit.
      tags:
      - templates
      x-app: templates
      x-public: true
      x-tool: true
  /v1/todo/board:
    get:
      description: |-
        Returns a board's issues — work items with their column, priority,
        assignee, labels and schedule.

        WHICH board is a filter, not an address. Bound to a repository (the key from
        the path) it is that project's board; left unbound it is the org's whole
        board; narrowed by label it is a board smaller than any repository — which is
        the only way an app that lives as a directory inside a shared repository can
        have one. Every combination is the same rows through the same projection, so
        no two boards can disagree about what a column means.

        The column is a LABEL on the forge, so the board and the forge web UI are the
        same object seen twice: relabelling in either moves the card in both. A closed
        issue reads as done whatever its labels say.
      operationId: get_todo_board
      parameters:
      - description: |-
          Key is the project whose issues to list, from the path. EMPTY means every
          project in the org — the global board. It is a filter like the rest of
          this struct rather than an address, which is what lets one op answer both
          "this board" and "all the work" without a second surface disagreeing with
          the first about what a column is.
        in: query
        name: key
        required: false
        schema:
          type: string
      - description: |-
          Status keeps only issues in that board column: backlog, todo, in_progress,
          done or canceled. An unknown value is refused with 400.
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: |-
          Kind keeps only work items of that shape: issue, pr or epic. An unknown
          value is refused with 400.
        in: query
        name: kind
        required: false
        schema:
          type: string
      - description: Repo keeps only issues bound to that git repository.
        in: query
        name: repo
        required: false
        schema:
          type: string
      - description: |-
          Label keeps only issues carrying that label, compared case-insensitively.

          This is how a board narrows to something SMALLER than a repository — the
          one mechanism for it. An estate whose apps are directories inside one
          repository (hanzoai/cloud carries ~140 of them) has no repository per app
          to address, so the app is a label: `label=app/meet` is the meet board.
          Nothing is provisioned to make one exist; a board is the query.
        in: query
        name: label
        required: false
        schema:
          type: string
      - description: |-
          Source keeps only issues opened from that surface: team, git, crm,
          helpdesk, cms or agent. An unknown value is refused with 400.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: |-
          Scheduled keeps only issues that carry a date — a start, a due date or
          both. This is the timeline's slice of the board: pass scheduled=true to
          get exactly the rows a gantt has somewhere to draw, instead of fetching
          every issue and discarding the undated ones client-side.
        in: query
        name: scheduled
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/issueView'
                type: array
          description: ok
      summary: Returns a board's issues — work items with their column, priority,
        assignee, labels and schedule.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
  /v1/todo/issues:
    get:
      description: |-
        Answers across every project in the org.

        The org comes from the validated principal and never from the request: a
        caller able to name the org could read another tenant's backlog, and a search
        is exactly the shape that would quietly return it.
      operationId: get_todo_issues
      parameters:
      - description: |-
          Q matches an issue's title or description. A word from the issue, which is
          what someone remembers — not its number, which is what they are looking up.
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: |-
          Project narrows to one team key; "" searches every project in the org,
          which is the point of this op.
        in: query
        name: project
        required: false
        schema:
          type: string
      - description: 'Status keeps one board column: backlog, todo, in_progress, done,
          canceled.'
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: 'Kind keeps one shape: issue, pr, epic.'
        in: query
        name: kind
        required: false
        schema:
          type: string
      - description: Repo keeps issues bound to one git repository.
        in: query
        name: repo
        required: false
        schema:
          type: string
      - description: |-
          Source keeps one origin: team, git, crm, helpdesk, cms, agent. "git" is
          how you ask for the mirrored GitHub issues specifically.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: Assignee keeps issues held by one person. Pass "me" for yourself.
        in: query
        name: assignee
        required: false
        schema:
          type: string
      - description: |-
          Limit caps the answer; 0 means the default, and anything above the ceiling
          is clamped rather than refused — a search that errors on being too broad
          teaches people to guess.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/issueHits'
          description: ok
      summary: Answers across every project in the org.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
  /v1/todo/projects:
    get:
      description: |-
        Returns the boards of your org — the places your work actually
        is. The key addresses the board's issues.

        A BOARD IS A PLACE WORK IS, not an object somebody provisioned. So the list is
        assembled from the work itself: the repositories your org has filed issues on,
        plus the boards the index holds. A repository with nothing on it is not in the
        list and is still perfectly addressable — GET /projects/<name> reads it and a
        create files into it — so nothing is lost by leaving it out.

        Measured, which is why: reading the forge's whole repository inventory put 745
        boards here, of which all but a handful were vendored forks and mirrors
        (.github, .profile, DOMPurify, BoatAttack) that will never carry this org's
        work. A list that long is not a list — the estate's real roadmap was in it
        somewhere and no one could see it.

        The forge half is the FORGE's answer for your own account, so two people in
        one org can legitimately see different boards.
      operationId: get_todo_projects
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/todoProject'
                type: array
          description: ok
      summary: Returns the boards of your org — the places your work actually is.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
    post:
      description: |-
        Answers 405. A todo board IS a repository on this deployment's forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.

        Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: "not this service's job" and "no such thing" are different facts, and the body names the forge so a caller knows where the job IS done.

        What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
      operationId: post_todo_projects
      summary: Refused — a board is a repository on the forge
      tags:
      - todo
      x-app: todo
      x-public: true
  /v1/todo/projects/{key}:
    delete:
      description: |-
        Answers 405. A todo board IS a repository on this deployment's forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.

        Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: "not this service's job" and "no such thing" are different facts, and the body names the forge so a caller knows where the job IS done.

        What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
      operationId: delete_todo_projects_by_key
      parameters:
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Refused — a board is a repository on the forge
      tags:
      - todo
      x-app: todo
      x-public: true
    get:
      description: |-
        Returns one board of your org by its key — the repository name.
        404 when your org has no repository under that key, or when your own forge
        account cannot see it.
      operationId: get_todo_projects_by_key
      parameters:
      - description: |-
          Key is the project's org-unique handle: 2-8 uppercase alphanumerics starting
          with a letter ("ENG", "OPS2"). Matched case-insensitively.
        in: path
        name: key
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/todoProject'
          description: ok
      summary: Returns one board of your org by its key — the repository name.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
    patch:
      description: |-
        Answers 405. A todo board IS a repository on this deployment's forge, so creating, renaming and deleting one is a FORGE operation carried out with FORGE permissions.

        Offering it here would put a second door on the same object, guarded by this surface instead of by the forge — a weaker guard on the same thing. So the route exists and refuses, rather than 404ing: "not this service's job" and "no such thing" are different facts, and the body names the forge so a caller knows where the job IS done.

        What this surface DOES own is the work on a board: list the boards you can see, read and file their issues, move a card between columns, and read the schedule a milestone is. Those are the routes beside this one.
      operationId: patch_todo_projects_by_key
      parameters:
      - in: path
        name: key
        required: true
        schema:
          type: string
      summary: Refused — a board is a repository on the forge
      tags:
      - todo
      x-app: todo
      x-public: true
  /v1/todo/projects/{key}/issues:
    get:
      description: |-
        Returns a board's issues — work items with their column, priority,
        assignee, labels and schedule.

        WHICH board is a filter, not an address. Bound to a repository (the key from
        the path) it is that project's board; left unbound it is the org's whole
        board; narrowed by label it is a board smaller than any repository — which is
        the only way an app that lives as a directory inside a shared repository can
        have one. Every combination is the same rows through the same projection, so
        no two boards can disagree about what a column means.

        The column is a LABEL on the forge, so the board and the forge web UI are the
        same object seen twice: relabelling in either moves the card in both. A closed
        issue reads as done whatever its labels say.
      operationId: get_todo_projects_by_key_issues
      parameters:
      - description: |-
          Key is the project whose issues to list, from the path. EMPTY means every
          project in the org — the global board. It is a filter like the rest of
          this struct rather than an address, which is what lets one op answer both
          "this board" and "all the work" without a second surface disagreeing with
          the first about what a column is.
        in: path
        name: key
        required: true
        schema:
          type: string
      - description: |-
          Status keeps only issues in that board column: backlog, todo, in_progress,
          done or canceled. An unknown value is refused with 400.
        in: query
        name: status
        required: false
        schema:
          type: string
      - description: |-
          Kind keeps only work items of that shape: issue, pr or epic. An unknown
          value is refused with 400.
        in: query
        name: kind
        required: false
        schema:
          type: string
      - description: Repo keeps only issues bound to that git repository.
        in: query
        name: repo
        required: false
        schema:
          type: string
      - description: |-
          Label keeps only issues carrying that label, compared case-insensitively.

          This is how a board narrows to something SMALLER than a repository — the
          one mechanism for it. An estate whose apps are directories inside one
          repository (hanzoai/cloud carries ~140 of them) has no repository per app
          to address, so the app is a label: `label=app/meet` is the meet board.
          Nothing is provisioned to make one exist; a board is the query.
        in: query
        name: label
        required: false
        schema:
          type: string
      - description: |-
          Source keeps only issues opened from that surface: team, git, crm,
          helpdesk, cms or agent. An unknown value is refused with 400.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: |-
          Scheduled keeps only issues that carry a date — a start, a due date or
          both. This is the timeline's slice of the board: pass scheduled=true to
          get exactly the rows a gantt has somewhere to draw, instead of fetching
          every issue and discarding the undated ones client-side.
        in: query
        name: scheduled
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/issueView'
                type: array
          description: ok
      summary: Returns a board's issues — work items with their column, priority,
        assignee, labels and schedule.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
    post:
      description: |-
        Opens a work item on the board — an issue on that repository on
        the deployment's forge, filed as YOU.

        The column and priority are written as LABELS, which is what makes the card
        and the forge issue the same object: someone relabelling in the forge web UI
        has moved your card.
      operationId: post_todo_projects_by_key_issues
      parameters:
      - description: Key is the board — the repository name, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/newIssue'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/issueView'
          description: ok
      summary: Opens a work item on the board — an issue on that repository on the
        deployment's forge, filed as YOU.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
  /v1/todo/projects/{key}/issues/{num}:
    get:
      description: |-
        Returns ONE work item in full — its description included.

        The list reads answer a board, and a board is a summary: the description is
        where the actual content of a work item lives — what an issue asks for, what
        an epic's acceptance criteria are — and no read on this surface returned it.
        The address is the one PATCH already accepts, so an item you can move is now
        an item you can read.

        It reads the forge directly rather than filtering the org fan-out, then falls
        back to the index for a board the forge has never heard of — the same order,
        and the same reason, as GetProject: a row is one kind of thing however it came
        to exist, so a caller does not have to know which store it is in to fetch it.
      operationId: get_todo_projects_by_key_issues_by_num
      parameters:
      - description: Key is the board — the repository name, or an index board's key.
        in: path
        name: key
        required: true
        schema:
          type: string
      - description: Num is the issue's number on that board.
        in: path
        name: num
        required: true
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/issueView'
          description: ok
      summary: Returns ONE work item in full — its description included.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
    patch:
      description: |-
        Edits a work item — rename it, rewrite it, move it to another
        column, or re-prioritise it. Absent fields are left alone.

        MOVING A CARD IS A RELABEL. The column lives in the forge's label set, so the
        move replaces that set rather than writing a status column here that a
        forge-side change could contradict. Moving to `done` also CLOSES the issue on
        the forge, because a done card and an open issue are a contradiction.
      operationId: patch_todo_projects_by_key_issues_by_num
      parameters:
      - description: Key is the board — the repository name, from the path.
        in: path
        name: key
        required: true
        schema:
          type: string
      - description: Num is the issue number on that repository, from the path.
        in: path
        name: num
        required: true
        schema:
          type: integer
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/issueEdit'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/issueView'
          description: ok
      summary: Edits a work item — rename it, rewrite it, move it to another column,
        or re-prioritise it.
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
  /v1/todo/projects/{key}/issues/{num}/claim:
    post:
      description: |-
        Takes an issue: it becomes yours and it moves to in_progress.

        The holder is the CALLER, never an argument. "Assign this to someone else" is
        a different act with different authority, and it already exists as a PATCH;
        conflating them would let anyone hand work to anyone by naming them.

        Claiming something already held by someone else is refused rather than
        silently taken — two agents on one issue is the failure this prevents, and a
        claim that quietly wins a race is worse than one that says no.
      operationId: post_todo_projects_by_key_issues_by_num_claim
      parameters:
      - in: path
        name: key
        required: true
        schema:
          type: string
      - in: path
        name: num
        required: true
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/issueHit'
          description: ok
      summary: 'Takes an issue: it becomes yours and it moves to in_progress.'
      tags:
      - todo
      x-app: todo
      x-public: true
      x-tool: true
  /v1/tools:
    get:
      description: |-
        Lists every tool the caller's org and project can reach, from every
        source, each flagged with whether it is activated. This is the discovery
        surface: one flat set of names spanning connector actions, user functions,
        zap-service routes, agents, skills and the org's own external MCP servers,
        deduplicated by name so the highest-precedence source wins a collision. It
        lists; it does not call — dispatch is POST /v1/tools/call.
      operationId: get_tools
      parameters:
      - description: |-
          Source keeps only tools from one source — connector, function, zap-service,
          agent, skill or mcp. Empty keeps every source.
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: |-
          Activated keeps only the tools activated for the caller's org and project,
          and only when it is exactly the string "true".
        in: query
        name: activated
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/toolList'
          description: ok
      summary: Lists every tool the caller's org and project can reach, from every
        source, each flagged with whether it is activated.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/activation:
    get:
      description: |-
        Reports which tools are switched on for the caller's org and
        project. Activation is what makes a tool dispatchable and what makes it visible
        to an agent, so this is the set the MCP tool list is drawn from — every other
        tool in the registry is discoverable but refused at call time.
      operationId: get_tools_activation
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activationSet'
          description: ok
      summary: Reports which tools are switched on for the caller's org and project.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
    put:
      description: |-
        Switches tools on and off for the caller's org and project, and
        answers with the resulting activated set. It is the ONE write path that turns
        skills, plugins and connectors into callable tools — an unactivated tool is
        listed by discovery but refused 403 at dispatch. Activate is applied before
        Deactivate, so a name in both lists ends up off. More than 256 toggles in one
        request is refused 413.
      operationId: put_tools_activation
      requestBody:
        content:
          application/json:
            example:
              activate:
              - cloud_get_ping
              deactivate: []
            schema:
              $ref: '#/components/schemas/activationReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/activationSet'
          description: ok
      summary: Switches tools on and off for the caller's org and project, and answers
        with the resulting activated set.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/call:
    post:
      description: |-
        Runs one of the caller's activated tools and answers with its output.

        This is the door onto the tool plane's DYNAMIC half — the half no build-time
        catalogue can hold, because it is per-tenant: an org's connected connector
        actions, its authored skills, its agents and functions, and the tools of every
        external MCP server it registered. A tool's existence, its price and its
        activation are all rows, not code, so they cannot be known until the caller is.

        One policy, the registry's: resolve by precedence, refuse an unactivated tool
        403, settle a priced one through the x402 seam or fail closed 402, then
        dispatch to the winning source bound to the caller's own (org, project). One
        metered unit, one audit record. A caller can only ever dispatch its own tools.

        Discovery is GET /v1/tools — ?activated=true for the callable set.
      operationId: post_tools_call
      requestBody:
        content:
          application/json:
            example:
              arguments:
                channel: '#general'
                text: hi
              name: slack_post_message
            schema:
              $ref: '#/components/schemas/toolCall'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/toolResult'
          description: ok
      summary: Runs one of the caller's activated tools and answers with its output.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/catalog:
    get:
      description: |-
        Lists the MCP servers the public registries publish, as we hold
        them: our canonical copy of registry.modelcontextprotocol.io, plus what we
        decided about each entry.

        This is the SHELF an org picks from. A listing with a streamable-http endpoint
        can be enabled as-is — POST /v1/tools/mcp/servers with its id — and its tools then
        join the org's tool plane and the fleet's MCP door. A listing that only ships a
        stdio package needs a process to run it, which is why the transports are on
        every entry rather than implied.

        Hidden entries are absent: they are the ones we took off the shelf. A platform
        SuperAdmin sees them, because the same query answers "what is on the shelf" and
        "what is in the catalog" and two queries would drift apart.

        It is PAGED — 50 by default, 200 at most. The public registry publishes tens of
        thousands of servers, so an unbounded answer is a twenty-megabyte response and a
        storefront that renders in a minute. total is the whole match, not the page.
      operationId: get_tools_catalog
      parameters:
      - description: Q matches the name, title or description, case-insensitively.
        in: query
        name: q
        required: false
        schema:
          type: string
      - description: |-
          Featured keeps only the listings we put on the front of the shelf, and only
          when it is exactly the string "true".
        in: query
        name: featured
        required: false
        schema:
          type: string
      - description: |-
          Official keeps only the vendors' OWN servers — not third-party copies of
          them — and only when it is exactly the string "true".
        in: query
        name: official
        required: false
        schema:
          type: string
      - description: |-
          Limit bounds the page: default 50, maximum 200. A value that is not a
          positive integer reads as the default.
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: Offset skips that many listings.
        in: query
        name: offset
        required: false
        schema:
          type: integer
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mcpCatalog'
          description: ok
      summary: 'Lists the MCP servers the public registries publish, as we hold them:
        our canonical copy of registry.modelcontextprotocol.io, plus what we decided
        about each entry.'
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/catalog/{id}:
    get:
      description: |-
        Returns one catalog entry in full: the publisher's description, its
        repository and site, every package form with the runtime that launches it, and
        every hosted endpoint. It is what a branding page renders, and what tells a
        caller whether the listing can be enabled here and now (a streamable-http
        remote) or needs somewhere to run first (a stdio package).

        A HIDDEN listing is not served to an org — a shelf that renders what it does
        not list would be a way around the shelf — but is served to a SuperAdmin, who
        is the one deciding whether to put it back.
      operationId: get_tools_catalog_by_id
      parameters:
      - description: |-
          ID is the listing, from the path. It is the publisher's reverse-DNS name
          with its one slash written as an underscore — "com.stripe_mcp".
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPListing'
          description: ok
      summary: 'Returns one catalog entry in full: the publisher''s description, its
        repository and site, every package form with the runtime that launches it,
        and every hosted endpoint.'
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
    patch:
      description: |-
        Sets what WE say about one catalog entry — hidden, featured,
        official, logo — and answers with the stored listing. SuperAdmin only; every
        other caller is refused.

        Curation is the half of a catalog row a sync cannot write, and this is the only
        thing that writes it. The upstream half is never editable here: a description
        that disagreed with the publisher's would be a fork of their listing, and the
        next sync would silently undo it.
      operationId: patch_tools_catalog_by_id
      parameters:
      - description: ID is the listing to curate, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              featured: true
              official: false
            schema:
              $ref: '#/components/schemas/curateReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPListing'
          description: ok
      summary: Sets what WE say about one catalog entry — hidden, featured, official,
        logo — and answers with the stored listing.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/catalog/sync:
    post:
      description: |-
        Pulls the public MCP registry into our canonical copy and reports
        what changed. SuperAdmin only; every other caller is refused.

        It is IDEMPOTENT: a listing is keyed by the publisher's own reverse-DNS name,
        so a second pass over an unchanged registry rewrites the same rows and reports
        added=0, updated=0. It never deletes — a listing that vanishes upstream may be
        one an org has already enabled, and dropping its description would not drop its
        server. And it never touches CURATION: hidden, featured, an admin-set official
        and a logo survive every sync, because the write does not name those columns.
      operationId: post_tools_catalog_sync
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mcpCatalogSync'
          description: ok
      summary: Pulls the public MCP registry into our canonical copy and reports what
        changed.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/mcp/servers:
    get:
      description: |-
        Lists the external MCP servers the caller's org has registered.
        Each record carries the URL and the name of the header its credential is
        injected into; the credential VALUE lives only in KMS and is never returned,
        so hasSecret is the whole of what this surface says about it.
      operationId: get_tools_mcp_servers
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/mcpServerList'
          description: ok
      summary: Lists the external MCP servers the caller's org has registered.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
    post:
      description: |-
        Gives the caller's org one more external MCP server, so its tools
        join the org's tool plane and the fleet's MCP door. It is the ONE way an org
        gains a server, whether it typed the URL in or enabled a catalog listing: both
        write the SAME record, and `source` says which it was. A second registration
        path would be a second place for a server to exist, and then a second place to
        forget to check the credential.

        The credential VALUE is sealed in KMS under a per-org ref; the row keeps only
        the URL, the header name to inject it into, and a has-secret flag — so a secret
        with no KMS configured is refused 503 rather than stored in the clear. The URL
        is SSRF-validated here and re-checked by the dialer at connect time, which is
        the DNS-rebinding defense.

        Enabling a listing the org already enabled REVISES that server rather than
        adding a near-duplicate beside it, so a retried enable is the same one server.
        Answers 201 with the stored record.
      operationId: post_tools_mcp_servers
      requestBody:
        content:
          application/json:
            example:
              authHeader: Authorization
              listing: com.stripe_mcp
              secret: Bearer …
            schema:
              $ref: '#/components/schemas/createServerReq'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MCPServer'
          description: created
      summary: Gives the caller's org one more external MCP server, so its tools join
        the org's tool plane and the fleet's MCP door.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/mcp/servers/{id}:
    delete:
      description: |-
        Deregisters one of the caller org's external MCP servers, so its
        tools leave the registry. Scoped to the caller's org, so an id belonging to
        another tenant is a 404 and not a delete. Answers 204 with no body; a server
        this org does not have is 404.
      operationId: delete_tools_mcp_servers_by_id
      parameters:
      - description: ID is the server to deregister, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Deregisters one of the caller org's external MCP servers, so its tools
        leave the registry.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/plugins:
    get:
      description: |-
        Reports what this deployment actually mounted: every subsystem the
        composition root declared and whether it is switched on. A plugin here is
        MOUNTED CODE that extends the deployment's own surface — not a tool an agent
        calls — so this is an inventory and not a tool source. It is read off the same
        boot snapshot every traced request resolves its subsystem label against, so it
        cannot drift from what is serving. Enabled-only by default, because a caller
        asking what this deployment can do wants what is running; ?all=true adds the
        configured-but-off ones.
      operationId: get_tools_plugins
      parameters:
      - description: |-
          All includes the configured-but-disabled subsystems too, but only when it is
          exactly the string "true". Otherwise only the running ones are reported.
        in: query
        name: all
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pluginMountList'
          description: ok
      summary: 'Reports what this deployment actually mounted: every subsystem the
        composition root declared and whether it is switched on.'
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/plugins/authored:
    get:
      description: |-
        Lists the plugins the caller's org BUILT, newest first,
        each with the TypeScript as authored. That is a different set with a different
        lifecycle from GET /v1/tools/plugins, which reports the subsystems this deployment
        mounted. The bundled CommonJS the runtime executes is never included, and
        neither is any credential — a plugin names the connectors provider it needs and
        reads the credential from ctx.auth at run time.
      operationId: get_tools_plugins_authored
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/authoredPluginList'
          description: ok
      summary: Lists the plugins the caller's org BUILT, newest first, each with the
        TypeScript as authored.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/plugins/authored/{id}:
    delete:
      description: |-
        Removes one of the caller org's built plugins, so the
        runtime can no longer load it. Scoped to the caller's org, so an id belonging
        to another tenant answers 404 and is not deleted.
      operationId: delete_tools_plugins_authored_by_id
      parameters:
      - description: ID is the plugin to remove, from the path.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/pluginDeleted'
          description: ok
      summary: Removes one of the caller org's built plugins, so the runtime can no
        longer load it.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/plugins/build:
    post:
      description: |-
        Builds one plugin for the caller's org and answers 201 with the bundle's size, whether a model wrote the source, and the plugin as stored. Post `source` to build TypeScript as-is, or `spec` — an OpenAPI document or plain prose describing the endpoints — to have one generated; the generated source comes back in the answer, so a caller reads what will run before it runs. Exactly one of the two, and `name` must be one lowercase path segment; both or neither is 400.

        COMPILING IS THE GATE. The source goes through the same pipeline the committed connectors do — esbuild to one CommonJS program, then compiled in the goja runtime that will actually execute it — and anything that fails is rejected and NEVER stored. So a plugin in the store is one this deployment has already loaded once, not one a model claimed was fine. A failed build answers 422 carrying the diagnostics a caller needs to fix it: the bundler's error, the source that failed, and whether the model wrote it — a body outside the declared success shape.

        CREDENTIALS ARE NOT PART OF A PLUGIN. A plugin names the connectors `provider` it needs and reads that credential from `ctx.auth` at run time, under KMS custody. Source that contains something shaped like a key is REFUSED rather than silently scrubbed, so a caller who pasted one finds out instead of shipping it — register it as a connector instead.

        Requires a validated principal; 403 without one. The plugin is stored under that principal's org and is what `/v1/tools/plugins/authored` lists — never `/v1/tools/plugins`, which is this deployment's mounted-subsystem inventory. Source over 512 KiB or a spec over 256 KiB is refused. Posting a `spec` to a deployment with no AI client configured is 503, and a generation that fails upstream is 502.
      operationId: post_tools_plugins_build
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/buildRequest'
      responses:
        2XX:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/buildOut'
          description: Success
      summary: Build a plugin for your org from TypeScript, or from an API spec a
        model writes it from
      tags:
      - tools
      x-app: tools
      x-public: true
  /v1/tools/skills:
    get:
      description: |-
        Lists the skills the caller's org can reach — the brand's embedded
        catalogue plus the org's own authored ones — with each one's activation flag.
        A skill is discovery and activation metadata attached to an agent, never called
        directly, so every entry here is non-dispatchable. It is GET /v1/tools narrowed
        to one source, not a second store: a name a caller sees here is the same entry,
        with the same activation state, that discovery reports.
      operationId: get_tools_skills
      parameters:
      - description: |-
          Activated keeps only the tools activated for the caller's org and project,
          and only when it is exactly the string "true".
        in: query
        name: activated
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/sourceToolList'
          description: ok
      summary: Lists the skills the caller's org can reach — the brand's embedded
        catalogue plus the org's own authored ones — with each one's activation flag.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
    post:
      description: |-
        Adds or revises one of the caller org's own skills, and answers 201
        with the stored record. The id is derived from the name, so writing the same
        name again REVISES that skill rather than accumulating near-duplicates that
        would then collide in the registry. An org's skills are private to it by
        construction — they live in a different store from the brand's embedded
        catalogue and have no path into the public gallery — and a brand skill always
        wins a name collision against an org's.
      operationId: post_tools_skills
      requestBody:
        content:
          application/json:
            example:
              content: |-
                # Triage
                …
              description: how we triage
              name: triage
            schema:
              $ref: '#/components/schemas/skillIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/skillWritten'
          description: created
      summary: Adds or revises one of the caller org's own skills, and answers 201
        with the stored record.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/skills/{id}:
    delete:
      description: |-
        Removes one of the caller org's authored skills. Scoped to the
        caller's org, so an id belonging to another tenant is never reached. Removing
        what is not there is not an error — the caller's intent is "gone", and it is.
      operationId: delete_tools_skills_by_id
      parameters:
      - description: ID is the skill to remove, from the path. It is the skill's name.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/skillDeleted'
          description: ok
      summary: Removes one of the caller org's authored skills.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/tools/skills/authored:
    get:
      description: |-
        Lists the caller org's OWN skills with their SKILL.md
        bodies. GET /v1/tools/skills is the registry view — the brand's catalogue plus this
        org's, with activation flags and no bodies; this is the EDITABLE set, so it
        carries the content that view omits and nothing the org did not write.
      operationId: get_tools_skills_authored
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/authoredSkillList'
          description: ok
      summary: Lists the caller org's OWN skills with their SKILL.md bodies.
      tags:
      - tools
      x-app: tools
      x-public: true
      x-tool: true
  /v1/traces/health:
    get:
      description: |-
        Reports the native trace store's live state for the calling tenant: the subsystem version and `spans`, the count actually held right now. Not a dependency probe — the store is in-process, so this answers 200 whenever the process is up.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`.
      operationId: get_traces_health
      summary: How many spans this deployment holds for your org
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/traces/query:
    get:
      description: |-
        Answers `{count, spans}`, newest first, filtered on each span's START time. `start` and `end` are nanosecond bounds where 0 — which is what an absent, empty or unparseable value becomes — means UNBOUNDED, so a malformed bound widens the listing instead of failing it. `limit` defaults to 100 when absent or non-positive.

        It lists SPANS, not traces: several spans of one trace each count separately and each take a slot against `limit`. Assembling one trace is /v1/traces/trace. The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`.
      operationId: get_traces_query
      summary: Recent spans for your org over a time range
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/traces/trace:
    get:
      description: |-
        Answers `{spans}`: every span the org holds for the trace id in `id`, in the order they were appended, which is what a waterfall view renders. Unlike the other reads there is no count, no time range and no limit — a trace is addressed by id or not at all.

        An id with no spans answers an EMPTY list, never a 404: the store cannot tell a trace that never existed from one whose spans retention has already dropped, so it does not pretend to. The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`, and a trace id belonging to another org is simply not in this org's store.
      operationId: get_traces_trace
      summary: Every span of one trace — the waterfall
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/traces/write:
    post:
      description: |-
        Takes `{spans:[{traceId, spanId, parentId, name, startNs, endNs, attrs}]}`, appends each, and answers `{written}` — the number of spans sent. Every span is indexed by its trace id as it lands, which is what makes the waterfall read possible without a second store.

        Times are NANOSECONDS since the Unix epoch. Retention is a bounded ring of 1048576 spans per org: past that the OLDEST are evicted to keep the newest 1048576, and the trace index is rebuilt — so a long-lived trace can lose its early spans while its later ones survive, and a waterfall read is best-effort against retention, not a guarantee.

        The tenant is the gateway-minted `X-Org-Id` header, falling back to the deployment brand and then `default`. A body that does not decode is 400.
      operationId: post_traces_write
      summary: Append spans for your org
      tags:
      - metrics
      x-app: metrics
      x-public: true
  /v1/treasury:
    get:
      description: |-
        Returns the reserve fund's health and the current revenue-share
        policy for any validated caller. It is a TRANSPARENCY view — a partner or
        author can see that the pool backing their payouts is solvent — and NOT per-org
        money, which is the customer's own commerce balance at /v1/billing/balance. The
        policy is read-only here; only a SuperAdmin sets it.
      operationId: get_treasury
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TreasuryReport'
          description: ok
      summary: Returns the reserve fund's health and the current revenue-share policy
        for any validated caller.
      tags:
      - treasury
      x-app: treasury
      x-public: true
      x-tool: true
  /v1/treasury/accounts:
    get:
      description: |-
        Returns the ledger accounts the caller may see, with their
        balances. It is tenant-isolated SERVER-SIDE: an ordinary caller sees ONLY
        accounts under its own "org:<tenant>:" prefix, never house accounts and never
        another tenant's. A SuperAdmin may widen with ?scope=house (the reserve,
        revenue and payout house accounts) or ?org=<tenant> — the only way to cross the
        tenant boundary, and only for platform sudo. The answer is honestly empty until
        a tenant has ledger postings.
      operationId: get_treasury_accounts
      parameters:
      - description: Scope is "house" to read the reserve/revenue/payout house accounts.
          SuperAdmin only.
        in: query
        name: scope
        required: false
        schema:
          type: string
      - description: Org names another tenant to read. SuperAdmin only; ignored when
          scope=house.
        in: query
        name: org
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/accountsOut'
          description: ok
      summary: Returns the ledger accounts the caller may see, with their balances.
      tags:
      - treasury
      x-app: treasury
      x-public: true
      x-tool: true
  /v1/usage:
    post:
      description: |-
        Ingests a batch of account-usage samples — what a developer's OWN AI
        accounts have consumed of their OWN plans, metered from each provider's own
        login — and appends them to the warehouse series. Answers 202.

        Send either a `samples` array or one sample's fields at the top level. Every
        sample needs a provider, a machine and a known window class; an unknown window or
        kind is refused rather than silently rewritten, because a dash filled with a class
        nobody reported is worse than an error. There is no timestamp field: the server
        owns the observation clock, and a sample says which window it measured with
        windowStart or resetsAt.

        It is FAIL-SOFT on storage: a warehouse outage costs a poll of history
        (stored:false), never a failed request. It records usage ONLY — the link registry
        is refreshed separately via POST /v1/link, so there is one and only one way to
        update an account row.
      operationId: post_usage
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/reportReq'
        required: true
      responses:
        "202":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/reportResp'
          description: accepted
      summary: Ingests a batch of account-usage samples — what a developer's OWN AI
        accounts have consumed of their OWN plans, metered from each provider's own
        login — and appends them to the warehouse series.
      tags:
      - usage
      x-app: usage
      x-public: true
      x-tool: true
  /v1/usage/analytics:
    get:
      description: |-
        Is the entitlement-GATED per-provider breakdown of the caller org's LLM
        usage — the paid lens over the same warehouse ledger GET /v1/usage/summary reads
        its totals from. Basic own-org usage stays ungated at /v1/usage/summary.

        A plan that does not grant the analytics datastore is refused with 402, and an
        unresolvable plan fails closed to the free floor, which does not grant it. The
        window is clamped forward to the plan's retention entitlement, so a tenant can
        never read older than its plan allows even with a custom start. The response is
        marked no-store.

        INTERIM (mirrors apps/world's limits echo): no org→plan resolver exists in cloud
        yet — the subscription lookup is owned by the billing plane and the gateway
        principal carries no plan claim — so the caller passes the plan and the gate
        resolves THAT plan's access.
      operationId: get_usage_analytics
      parameters:
      - description: End is the exclusive window end, RFC3339. Read only when Range
          is custom.
        in: query
        name: end
        required: false
        schema:
          type: string
      - description: |-
          Plan is the plan id whose entitlement decides access and retention. INTERIM:
          cloud has no org-to-plan resolver yet, so the caller names the plan; when
          that resolver lands this becomes the caller org's own plan.
        in: query
        name: plan
        required: false
        schema:
          type: string
      - description: |-
          Range is the window: a count and a unit — 24h, 7d, 90d, any <N>h or <N>d —
          or day, week, month, all, custom. Empty means 24h. The window is then
          clamped forward to the plan's retention entitlement.
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: |-
          Start is the inclusive window start, RFC3339. Read only when Range is
          custom, and clamped forward to the plan's retention floor.
        in: query
        name: start
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/usageAnalyticsView'
          description: ok
      summary: Is the entitlement-GATED per-provider breakdown of the caller org's
        LLM usage — the paid lens over the same warehouse ledger GET /v1/usage/summary
        reads its totals from.
      tags:
      - usage
      x-app: usage
      x-public: true
      x-tool: true
  /v1/usage/analytics/access:
    get:
      description: |-
        Echoes a plan's resolved analytics entitlement so a dashboard can
        configure itself against the LIVE catalog instead of hardcoding tier numbers. An
        empty plan resolves the free floor, and a catalog resolution failure serves that
        same floor rather than erroring — so this always answers 200. It is a read-only
        contract echo and carries no tenant data.
      operationId: get_usage_analytics_access
      parameters:
      - description: |-
          Plan is a plan id from the live @hanzo/plans catalog. Empty resolves the
          free floor, and so does an id the catalog does not know — this never fails
          on an unknown plan.
        in: query
        name: plan
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/usageAnalyticsAccess'
          description: ok
      summary: Echoes a plan's resolved analytics entitlement so a dashboard can configure
        itself against the LIVE catalog instead of hardcoding tier numbers.
      tags:
      - usage
      x-app: usage
      x-public: true
      x-tool: true
  /v1/usage/samples:
    get:
      description: |-
        Is the PER-PROVIDER view: one connected account's own consumption of its
        own plan — "my plan is 47% through its 6h window, resets at 14:20".

        `current` is the newest instance of each lane (the headline); `windows` is the
        history behind it. Both come from ONE deduped read, so they can never disagree.
        The rows are the caller's OWN linked accounts, scoped to the validated principal
        and its subject — never another user's, and never another org's.
      operationId: get_usage_samples
      parameters:
      - description: |-
          Account narrows to ONE linked account of that provider. Empty covers every
          account the caller has linked there.
        in: query
        name: account
        required: false
        schema:
          type: string
      - description: Provider is the upstream to read, e.g. anthropic. Required.
        in: query
        name: provider
        required: false
        schema:
          type: string
      - description: |-
          Range is the window to read: a count and a unit — 1h, 24h, 90d, any <N>h or
          <N>d — or day, week, month, all. Empty means 24h. A label that is not a
          count, or one reaching past the 730-day horizon, is refused rather than
          silently replaced.
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: |-
          Window narrows to ONE window class: 6h, day, week or month. Empty covers
          every class.
        in: query
        name: window
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/dashResp'
          description: ok
      summary: 'Is the PER-PROVIDER view: one connected account''s own consumption
        of its own plan — "my plan is 47% through its 6h window, resets at 14:20".'
      tags:
      - usage
      x-app: usage
      x-public: true
      x-tool: true
  /v1/usage/summary:
    get:
      description: |-
        Answers GET /v1/usage/summary: the caller's own usage footprint over one
        window — the categorized spend roll-up from the commerce ledger, the org's LLM
        usage totals from the warehouse, and the caller's OWN linked provider accounts
        beside the org's Hanzo-routed usage.

        Every source degrades INDEPENDENTLY to honest zeros and says so in `sources` and
        in its own `available` flag, so a partial deploy reports "no data" rather than
        fabricating spend. The account rows and the Hanzo rows are concatenated and never
        summed: a plan's percent is not money.

        The response is org-scoped from the validated principal and marked no-store — a
        signed-out caller is refused.
      operationId: get_usage_summary
      parameters:
      - description: |-
          Range is the window: a count and a unit — 24h, 7d, 90d, any <N>h or <N>d —
          or day, week, month, all, custom. Empty means 24h. A label this surface
          does not know, or one reaching past the 730-day horizon, is refused rather
          than silently replaced.
        in: query
        name: range
        required: false
        schema:
          type: string
      - description: |-
          Start is the inclusive window start, RFC3339. Read only when Range is
          custom.
        in: query
        name: start
        required: false
        schema:
          type: string
      - description: End is the exclusive window end, RFC3339. Read only when Range
          is custom.
        in: query
        name: end
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/usageSummary'
          description: ok
      summary: 'Answers GET /v1/usage/summary: the caller''s own usage footprint over
        one window — the categorized spend roll-up from the commerce ledger, the org''s
        LLM usage totals from the warehouse, and the caller''s OWN linked provider
        accounts beside the org''s Hanzo-routed usage.'
      tags:
      - usage
      x-app: usage
      x-public: true
      x-tool: true
  /v1/videos/{id}:
    get:
      description: |-
        Implements GET /v1/videos/{id} — poll a job's status.

        It authenticates the caller, verifies they OWN the job (the caller's billing
        subject must equal the job's), performs ONE upstream status poll, and — the
        first time the job is observed completed — settles the reservation with the
        actual cost and records the billable usage event (exactly once). Returns the
        OpenAI-shaped video object.
      operationId: get_videos_by_id
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Implements GET /v1/videos/{id} — poll a job's status.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/videos/{id}/content:
    get:
      description: |-
        Implements GET /v1/videos/{id}/content — download the finished MP4.

        It authenticates + ownership-checks the caller, then proxies the upstream
        /content endpoint (bounded by the download concurrency ceiling) and streams the
        raw video bytes back inline. A successful download also bills the job once (for
        the client that downloads without first polling to completion) — idempotent
        with the poll path via job.markCompleted.
      operationId: get_videos_by_id_content
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      summary: Implements GET /v1/videos/{id}/content — download the finished MP4.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/videos/generations:
    post:
      description: |-
        Implements POST /v1/videos/generations — the ASYNC create.

        Body: {"model": "...", "prompt": "...", "size"?: "1280x720", "seconds"?: int}

        It authenticates the caller, resolves the model to its upstream provider via
        the shared routing table (zen3-video* / wan2-2-t2v-a14b → the spark-video
        backend), reserves the per-video budget (the balance gate), creates ONE
        upstream job, registers it in the in-pod store, and returns the OpenAI-shaped
        video object with status "queued" IMMEDIATELY. The client then polls
        GET /v1/videos/{id} and downloads GET /v1/videos/{id}/content. Nothing is
        billed here — the debit lands on completion.
      operationId: post_videos_generations
      summary: Implements POST /v1/videos/generations — the ASYNC create.
      tags:
      - ai
      x-app: ai
      x-public: true
  /v1/visor/clusters:
    get:
      description: |-
        Returns the caller org's clusters from both sources: the managed
        clusters projected from Visor's node pools, and the BYO clusters attached to the
        caller's project. A Visor outage costs the managed half only — the BYO half
        still lists, because a page that 502s on an optional provider is worse than a
        page that shows what it can.
      operationId: listClusters
      responses:
        "200":
          content:
            application/json:
              example:
                clusters:
                - doksClusterId: cl-1
                  kind: managed
                  name: prod
                  nodeCount: 2
                  nodePools:
                  - count: 2
                    name: gpu
                    poolId: p-1
                    size: gpu-h100x8-640gb
                  nodeSize: gpu-h100x8-640gb
                  status: running
              schema:
                $ref: '#/components/schemas/clusterList'
          description: ok
      summary: 'Returns the caller org''s clusters from both sources: the managed
        clusters projected from Visor''s node pools, and the BYO clusters attached
        to the caller''s project.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    post:
      description: |-
        Attaches a BYO cluster to the caller's org — the kubeconfig is
        validated, KMS-sealed and added to the fleet — and answers 201 with the cluster
        as it now appears on GET /v1/visor/clusters. Billed the nominal management fee: the
        customer brings the compute, Hanzo meters the management plane.
      operationId: attachCluster
      requestBody:
        content:
          application/json:
            example:
              default: false
              kubeconfig: |-
                apiVersion: v1
                kind: Config
                ...
              name: lab
              provider: on-prem
            schema:
              $ref: '#/components/schemas/clusterAttach'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                kind: byo
                name: lab
                nodeCount: 3
                nodePools: []
                nvidiaGpu: 2
                region: on-prem
                status: attached
              schema:
                $ref: '#/components/schemas/clusterView'
          description: ok
      summary: Attaches a BYO cluster to the caller's org — the kubeconfig is validated,
        KMS-sealed and added to the fleet — and answers 201 with the cluster as it
        now appears on GET /v1/visor/clusters.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/clusters/{clusterId}/pools:
    post:
      description: |-
        Adds a node pool to one of the caller org's clusters and answers 201
        with the created pool. Only the CreateNodePoolSpec fields are forwarded;
        owner/provider/clusterId ride in the query exactly as Visor expects them.
      operationId: createNodePool
      parameters:
      - description: ClusterID is the cluster to add the pool to, from the URL path.
        in: path
        name: clusterId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              autoScale: false
              count: 2
              name: gpu
              provider: digitalocean
              size: gpu-h100x8-640gb
            schema:
              $ref: '#/components/schemas/poolCreate'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                count: 2
                name: gpu
                poolId: p-1
                size: gpu-h100x8-640gb
              schema:
                $ref: '#/components/schemas/nodePoolView'
          description: ok
      summary: Adds a node pool to one of the caller org's clusters and answers 201
        with the created pool.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/clusters/{clusterId}/pools/{poolId}:
    delete:
      description: |-
        Removes a node pool from one of the caller org's clusters. The owner
        scopes the delete to the caller's tenant; provider+clusterId drive the
        provider-side removal. Answers 204.
      operationId: deleteNodePool
      parameters:
      - description: ClusterID and PoolID address the pool, from the URL path.
        in: path
        name: clusterId
        required: true
        schema:
          type: string
      - in: path
        name: poolId
        required: true
        schema:
          type: string
      - description: Provider is the cloud the cluster lives on, from ?provider=.
          Required.
        in: query
        name: provider
        required: false
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes a node pool from one of the caller org's clusters.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/clusters/{clusterId}/pools/{poolId}/scale:
    post:
      description: |-
        Resizes a node pool to an absolute node count and returns the pool as
        Visor reports it after the change.
      operationId: scaleNodePool
      parameters:
      - description: ClusterID and PoolID address the pool, from the URL path.
        in: path
        name: clusterId
        required: true
        schema:
          type: string
      - in: path
        name: poolId
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              count: 4
              provider: digitalocean
            schema:
              $ref: '#/components/schemas/poolScale'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                count: 4
                name: gpu
                poolId: p-1
                size: gpu-h100x8-640gb
              schema:
                $ref: '#/components/schemas/nodePoolView'
          description: ok
      summary: Resizes a node pool to an absolute node count and returns the pool
        as Visor reports it after the change.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/clusters/{id}:
    delete:
      description: |-
        Removes a BYO cluster from the caller org's fleet. It only ever
        touches BYO clusters — a managed cluster's nodes are removed through the node-pool
        routes — and answers 404 when the name is not in this org's fleet.
      operationId: detachCluster
      parameters:
      - description: |-
          ID is the cluster's fleet name (the `name` it was attached under), matched
          lower-cased.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                detached: lab
              schema:
                $ref: '#/components/schemas/clusterDetached'
          description: ok
      summary: Removes a BYO cluster from the caller org's fleet.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/compute/bots:
    get:
      description: |-
        Returns the caller org's bot machines — the kind=bot machines — each
        joined with the agent binding that says which cloud Agent it runs.

        The bindings are read ONCE and joined by machine id, so the list is O(1) upstream
        calls, not N+1. A bindings read that fails only costs the reconciled status: a bot
        still lists without it.
      operationId: listBots
      responses:
        "200":
          content:
            application/json:
              example:
                bots:
                - agent: bot-a
                  binding:
                    agentName: bot-a
                    machineId: drop-a
                    status: running
                  id: drop-a
                  name: bot-a
                  status: running
              schema:
                $ref: '#/components/schemas/botList'
          description: ok
      summary: Returns the caller org's bot machines — the kind=bot machines — each
        joined with the agent binding that says which cloud Agent it runs.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/compute/bots/{id}:
    delete:
      description: |-
        Tears down both halves of a bot: it unbinds the agent (best-effort — a
        bot with no binding still deletes), then terminates the machine. Answers 204.
      operationId: deleteBot
      parameters:
      - description: |-
          ID is the bot machine's id — the same id the machines surface addresses it
          by. Scoped to the caller's org upstream, so another tenant's id is 404.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: 'Tears down both halves of a bot: it unbinds the agent (best-effort
        — a bot with no binding still deletes), then terminates the machine.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one of the caller org's bot machines with its agent binding.

        A machine counts as a Bot if it carries the hanzo-kind:bot tag OR has an agent
        binding — either signal is authoritative, so a bot resolves even before its
        cloud-init has stamped every tag. A machine that is neither is 404: this route
        answers for bots, not for machines.
      operationId: getBot
      parameters:
      - description: |-
          ID is the bot machine's id — the same id the machines surface addresses it
          by. Scoped to the caller's org upstream, so another tenant's id is 404.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                agent: bot-a
                binding:
                  agentName: bot-a
                  machineId: drop-a
                  status: running
                id: drop-a
                name: bot-a
                status: running
              schema:
                $ref: '#/components/schemas/botView'
          description: ok
      summary: Returns one of the caller org's bot machines with its agent binding.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/compute/bots/{id}/{action}:
    post:
      description: |-
        Dispatches one verb against a bot the caller's org owns. `message` runs the bot's bound agent with the request body as the message and streams the agent's answer back VERBATIM — the upstream body, its content type and its status — so a message is a real agent run, recorded, billed and traced exactly like any other, under the caller's own identity rather than a fabricated one. `stop` and `pause` are the same single honest capability: they halt the runtime by unbinding the agent while LEAVING THE MACHINE UP, so the bot stops answering but keeps costing — rebind to resume, or delete the bot to tear it down. Stopping is idempotent; a bot with no binding still reports stopped.

        Org-scoped and fails closed: a validated principal is required (403 without one) and the bot is addressed under the caller's OWN org, so another tenant's id is not reachable. An unknown action is a clean 400 naming the three it accepts, never a silent no-op, and messaging a bot with no bound agent is a 400.
      operationId: post_visor_compute_bots_by_id_by_action
      parameters:
      - in: path
        name: id
        required: true
        schema:
          type: string
      - in: path
        name: action
        required: true
        schema:
          type: string
      summary: Message a bot, or stop it, by naming the action in the path
      tags:
      - visor
      x-app: visor
      x-public: true
  /v1/visor/compute/bots/launch:
    post:
      description: |-
        Creates BOTH halves of a bot in one call and answers 201 with the bot: the cloud agent it runs, then a bot-kind machine bootstrapped with the bot runtime, then the binding between them, so a launched bot is immediately messageable. Send `dryRun: true` for a price quote instead — 200 with the upstream quote verbatim, no agent created, no machine launched, nothing spent.

        The agent is created FIRST and on purpose: it is create-if-absent (an agent that already exists is reused, so a relaunch is fine and several bots may share one explicit `agent`), and doing it before the machine means a bad request — a model that is not in the catalog, say — fails with the real reason BEFORE any metered machine is provisioned. `agent` defaults to the bot's name and an empty `model` takes the deployment default.

        Org-scoped and fails closed: a validated principal is required (403 without one), the owning org is that principal's and never a body field, `size` is required (400), and `name` is required for a real launch though not for a quote.
      operationId: post_visor_compute_bots_launch
      summary: Launch a bot machine — an agent plus the machine that runs it — or
        price one
      tags:
      - visor
      x-app: visor
      x-public: true
  /v1/visor/compute/regions:
    get:
      description: 'Lists the launch regions the compute catalog offers, passed through
        verbatim from the provider so the shape stays the provider''s single source
        of truth. The catalog is GLOBAL, not per-tenant: no owner is forwarded and
        every org sees the same list. It is still gated — a validated principal is
        required, 403 without one — because the catalog is what backs the launch drawer,
        not public marketing copy.'
      operationId: get_visor_compute_regions
      summary: The regions a machine or GPU can be launched into
      tags:
      - visor
      x-app: visor
      x-public: true
  /v1/visor/compute/sizes:
    get:
      description: 'Lists the instance sizes the compute catalog offers, passed through
        verbatim from the provider so the shape stays the provider''s single source
        of truth. These are the values `size` accepts on a launch. The catalog is
        GLOBAL, not per-tenant: no owner is forwarded and every org sees the same
        list. It is still gated — a validated principal is required, 403 without one.'
      operationId: get_visor_compute_sizes
      summary: The machine and GPU sizes that can be launched
      tags:
      - visor
      x-app: visor
      x-public: true
  /v1/visor/fleet:
    get:
      description: |-
        Returns every compute unit the caller's org has, from every source, each
        carrying its latest utilization: agent run-targets, the BYO machines that dialed
        in, attached BYO clusters and Visor-provisioned machines.

        A unit with a live snapshot of its own keeps it; the rest are overlaid from the
        utilization series, and only when the sample agrees about the SOURCE — two planes
        could mint the same unit id, and a board must never show one machine's load on
        another's row. BYO GPU units also carry their gpu-jobs queue depth. Every source
        is folded in independently: a broken one costs its own rows and nothing else.
      operationId: listFleet
      responses:
        "200":
          content:
            application/json:
              example:
                units:
                - host: spark
                  kind: worker
                  label: spark
                  metrics:
                    at: "2026-07-27T09:00:00Z"
                    gpuUtil: 0.42
                  queued: 2
                  running: 1
                  sessions: 0
                  source: byo
                  spec:
                    arch: arm64
                    cpus: 20
                    gpuModel: NVIDIA GB10
                    gpus: 1
                    os: linux
                  status: online
                  unit: spark
              schema:
                $ref: '#/components/schemas/fleetBoard'
          description: ok
      summary: 'Returns every compute unit the caller''s org has, from every source,
        each carrying its latest utilization: agent run-targets, the BYO machines
        that dialed in, attached BYO clusters and Visor-provisioned machines.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/fleet/jobs:
    get:
      description: |-
        Returns the caller org's gpu-jobs render queue, each row tagged with
        the GPU it targets (empty = the shared any-GPU lane) and the node claiming it,
        optionally narrowed to one GPU's queue and/or one status.

        A job whose worker died — STARTED with an elapsed lease and not yet reclaimed —
        reads "stalled", not "running". Fail-soft: an unavailable tasks engine yields an
        empty queue rather than an error.
      operationId: listFleetJobs
      parameters:
      - description: |-
          GPU selects one node's lane: jobs TARGETED at it (gpu:<node>) or CLAIMED by
          it. The literal "shared" selects the any-GPU lane — no target, no claimant.
          Matched case-insensitively.
        in: query
        name: gpu
        required: false
        schema:
          type: string
      - description: |-
          Status selects one lifecycle state: queued, running, stalled, completed,
          failed or canceled.
        in: query
        name: status
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                jobs:
                - attempt: 1
                  gpu: spark
                  id: job-1
                  label: hero
                  runId: job-1
                  status: running
                  type: studio.render
                  worker: spark
              schema:
                $ref: '#/components/schemas/jobList'
          description: ok
      summary: Returns the caller org's gpu-jobs render queue, each row tagged with
        the GPU it targets (empty = the shared any-GPU lane) and the node claiming
        it, optionally narrowed to one GPU's queue and/or one status.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/fleet/jobs/{id}/cancel:
    post:
      description: |-
        Cancels a queued or running render in the caller's org. The engine
        cancel is org-scoped, so a tenant can only ever cancel its OWN job: a job in
        another tenant's shard is 404, exactly like one that never existed. An
        already-finished job is 409.
      operationId: cancelFleetJob
      parameters:
      - description: ID is the job (activity) id, from the URL path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              reason: superseded
            schema:
              $ref: '#/components/schemas/jobCancel'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                canceled: job-1
                run: job-1
              schema:
                $ref: '#/components/schemas/jobCanceled'
          description: ok
      summary: Cancels a queued or running render in the caller's org.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/fleet/samples:
    get:
      description: |-
        Returns the caller org's utilization series, oldest first.

        A rejected narrower is a 400 carrying its own reason (the vocabulary is ours and
        safe to echo); a warehouse failure is logged and answered 503 "unavailable",
        because a chart that silently reads "no load" when the truth is "we cannot tell"
        is worse than one that says so. An ABSENT warehouse is different again: it returns
        an empty series, which renders honestly as "no samples yet".
      operationId: listFleetSamples
      parameters:
      - description: Unit selects one compute unit's series by its source-local id.
        in: query
        name: unit
        required: false
        schema:
          type: string
      - description: 'Source selects one plane: "agent", "byo" or "visor".'
        in: query
        name: source
        required: false
        schema:
          type: string
      - description: |-
          Range is the lookback window (e.g. "1h", "24h", "7d"); empty takes the
          warehouse default.
        in: query
        name: range
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                samples:
                - at: "2026-07-27T09:00:00Z"
                  gpuModel: GB10
                  gpuUtil: 0.42
                  gpus: 1
                  host: spark
                  kind: worker
                  source: byo
                  unit: spark
              schema:
                $ref: '#/components/schemas/sampleList'
          description: ok
      summary: Returns the caller org's utilization series, oldest first.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    post:
      description: |-
        Records a BYO worker's live GPU utilization into the SAME series the
        fleet board overlays. The org is the validated principal and source/kind are fixed
        server-side, so a worker names only its own metrics — never another tenant or
        another source. Answers 202: the warehouse write is DETACHED (its own bounded
        context, never in the response path), so a slow or absent warehouse cannot stall a
        heartbeat.
      operationId: recordFleetSample
      requestBody:
        content:
          application/json:
            example:
              gpuModel: GB10
              gpuUtil: 0.42
              gpus: 1
              host: spark
              memFree: 200
              memUsed: 100
              unit: spark
            schema:
              $ref: '#/components/schemas/sampleIngest'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                recorded: true
              schema:
                $ref: '#/components/schemas/sampleAccepted'
          description: ok
      summary: Records a BYO worker's live GPU utilization into the SAME series the
        fleet board overlays.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/fleet/workers:
    get:
      description: |-
        Returns the caller org's BYO machines — the ones that dialed in
        via `hanzo link` — with everything each host reported about itself. The Machines
        and GPUs pages fold the same data into their normalized shapes; this is the
        canonical raw list a fleet view (or the CLI's `status`) reads.
      operationId: listFleetWorkers
      responses:
        "200":
          content:
            application/json:
              example:
                workers:
                - arch: arm64
                  cpus: 20
                  gpus:
                  - memoryTotal: 122880 MiB
                    name: NVIDIA GB10
                  hostname: spark
                  id: spark
                  location: on-prem
                  provider: byo
                  status: online
              schema:
                $ref: '#/components/schemas/workerList'
          description: ok
      summary: Returns the caller org's BYO machines — the ones that dialed in via
        `hanzo link` — with everything each host reported about itself.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/gpus:
    get:
      description: |-
        Returns one row per physical accelerator the caller's org has, derived
        from its real GPU machines (the size slug says how many cards a node holds) and
        from the accelerators BYO workers report through nvidia-smi.

        Live telemetry is absent on Visor rows because Visor's machine object carries
        none — an honest omission the console renders as "—", never a fabricated 0.
      operationId: listGpus
      responses:
        "200":
          content:
            application/json:
              example:
                gpus:
                - id: gpu-1#0
                  machine: gpu-1
                  model: H100
                  name: gpu-1
                  provider: digitalocean
                  region: nyc2
                  status: running
              schema:
                $ref: '#/components/schemas/gpuList'
          description: ok
      summary: Returns one row per physical accelerator the caller's org has, derived
        from its real GPU machines (the size slug says how many cards a node holds)
        and from the accelerators BYO workers report through nvidia-smi.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/gpus/alerts:
    get:
      description: |-
        Is an HONEST empty surface: Visor exposes no GPU alert inventory, so
        this returns [] rather than fabricating alerts. It stays a real, tenant-gated
        route so the console's alerts fetch resolves (200 [], not a 404) — an honest
        "no alerts", the same discipline the rest of the surface follows.
      operationId: listGpuAlerts
      responses:
        "200":
          content:
            application/json:
              example:
                alerts: []
              schema:
                $ref: '#/components/schemas/gpuAlertList'
          description: ok
      summary: 'Is an HONEST empty surface: Visor exposes no GPU alert inventory,
        so this returns [] rather than fabricating alerts.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/k8s/clusters:
    get:
      description: |-
        Lists the org's DOKS clusters (Visor, house account) folded with
        the org's BYO clusters — ONE fleet cluster view under the unified k8s noun. A Visor
        outage is logged and skipped so a down optional provider never hides the BYO list.
      operationId: listKubernetesClusters
      responses:
        "200":
          content:
            application/json:
              example:
                clusters:
                - doClusterId: cl-1
                  doksClusterId: cl-1
                  kind: managed
                  name: prod
                  nodeCount: 0
                  nodePools: []
                  region: nyc3
                  status: running
              schema:
                $ref: '#/components/schemas/clusterList'
          description: ok
      summary: Lists the org's DOKS clusters (Visor, house account) folded with the
        org's BYO clusters — ONE fleet cluster view under the unified k8s noun.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    post:
      description: |-
        Provisions a DOKS cluster for the caller's org and answers 201.
        ADMIN-GATED — a SuperAdmin, or an OrgAdmin of the caller's own org — because
        provisioning spends real infrastructure on the house account. The request is
        validated at this boundary, then Visor owns provisioning and the hanzo-org
        ownership tag.
      operationId: createKubernetesCluster
      requestBody:
        content:
          application/json:
            example:
              name: prod
              nodePool:
                count: 2
                name: gpu
                size: gpu-h100x8-640gb
              region: nyc3
            schema:
              $ref: '#/components/schemas/createClusterReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                doClusterId: cl-1
                doksClusterId: cl-1
                kind: managed
                name: prod
                nodeCount: 0
                nodePools: []
                region: nyc3
                status: provisioning
              schema:
                $ref: '#/components/schemas/clusterView'
          description: ok
      summary: Provisions a DOKS cluster for the caller's org and answers 201.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/k8s/clusters/{id}:
    delete:
      description: |-
        Destroys a DOKS cluster by id and answers 204. ADMIN-GATED, like
        create. Visor scopes the delete to the org (refuses a foreign id), so this can
        only ever remove the caller org's own cluster.
      operationId: deleteKubernetesCluster
      parameters:
      - description: |-
          ID is the provider's DOKS cluster id. Visor scopes the lookup to the caller's
          org, so another tenant's id resolves to not-found rather than their cluster.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Destroys a DOKS cluster by id and answers 204.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one cluster's detail: node pools + worker nodes. Visor scopes
        the lookup to the org (a foreign or missing id resolves to not-found), so a tenant
        can never read another tenant's cluster by guessing an id.
      operationId: getKubernetesCluster
      parameters:
      - description: |-
          ID is the provider's DOKS cluster id. Visor scopes the lookup to the caller's
          org, so another tenant's id resolves to not-found rather than their cluster.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                doksClusterId: cl-1
                kind: managed
                name: prod
                nodeCount: 1
                nodePools:
                - count: 1
                  name: gpu
                  poolId: p-1
                  size: gpu-h100x8-640gb
                nodeSize: gpu-h100x8-640gb
                nodes:
                - id: node-1
                  name: node-1
                  status: active
                region: nyc3
                status: running
              schema:
                $ref: '#/components/schemas/clusterDetailView'
          description: ok
      summary: 'Returns one cluster''s detail: node pools + worker nodes.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/k8s/nodes:
    get:
      description: |-
        Returns every DOKS worker node in the org's clusters as a machine —
        the SAME set the fleet folds in (managedMachines), exposed directly under the k8s
        noun. House account (hanzo-org cluster tag) + BYOC, deduped by Visor.
      operationId: listKubernetesNodes
      responses:
        "200":
          content:
            application/json:
              example:
                nodes:
                - id: node-1
                  name: node-1
                  region: nyc3
                  status: active
                  type: s-4vcpu-8gb
                  vcpu: 4
              schema:
                $ref: '#/components/schemas/nodeList'
          description: ok
      summary: Returns every DOKS worker node in the org's clusters as a machine —
        the SAME set the fleet folds in (managedMachines), exposed directly under
        the k8s noun.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/machines:
    get:
      description: |-
        Returns every machine the caller's org has — Visor's registry, the
        live DigitalOcean droplets and the DOKS worker nodes (deduped into one union),
        plus the BYO machines that dialed in via `hanzo link` (provider "byo").

        A source Visor cannot answer for is logged and skipped, never an error: one
        wedged upstream must not hide the machines the other sources can see.
      operationId: listMachines
      responses:
        "200":
          content:
            application/json:
              example:
                machines:
                - id: web-1
                  mem: 4 GB
                  name: Web 1
                  provider: digitalocean
                  publicIp: 1.2.3.4
                  region: sfo3
                  status: running
                  type: s-2vcpu-4gb
                  vcpu: 2
              schema:
                $ref: '#/components/schemas/machineList'
          description: ok
      summary: Returns every machine the caller's org has — Visor's registry, the
        live DigitalOcean droplets and the DOKS worker nodes (deduped into one union),
        plus the BYO machines that dialed in via `hanzo link` (provider "byo").
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    post:
      description: |-
        Provisions a machine owned by the caller's org and answers 201 with the machine. Send `dryRun: true` to get a PRICE QUOTE instead: 200 with the upstream quote passed through verbatim, nothing launched and nothing spent. Two response shapes on one address is the rule to know, and it is why this is not a typed op.

        Metering is not this plane's: the launch fronts the compute provider's resell endpoint, which owns the balance gate and the per-hour meter, and cloud only forwards the tenant. Ownership is the validated principal's org and is never read from the body, so a launch always lands in the caller's OWN tenant and the machine it creates is only ever visible to that tenant. Fails closed: a validated principal is required (403 without one) and `size` (or its `instanceType` alias) is required (400).
      operationId: post_visor_machines
      summary: Launch a metered machine for your org, or price one first with dryRun
      tags:
      - visor
      x-app: visor
      x-public: true
  /v1/visor/machines/{id}:
    delete:
      description: |-
        Terminates one of the caller org's machines. Visor takes the
        machine identity as owner+name, and the owner is the validated principal, so a
        caller can only ever terminate its own tenant's machine. Answers 204.
      operationId: deleteMachine
      parameters:
      - description: |-
          ID is the machine's org-scoped NAME — the stable key Visor addresses a
          machine by (owner/name), not the ephemeral provider id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Terminates one of the caller org's machines.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one of the caller org's machines by its org-scoped name.
        Visor keys the lookup by owner/name, so an id belonging to another tenant
        resolves to not-found rather than another org's machine.
      operationId: getMachine
      parameters:
      - description: |-
          ID is the machine's org-scoped NAME — the stable key Visor addresses a
          machine by (owner/name), not the ephemeral provider id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                id: web-1
                name: Web 1
                publicIp: 1.2.3.4
                region: sfo3
                status: running
                type: s-2vcpu-4gb
                vcpu: 2
              schema:
                $ref: '#/components/schemas/machineView'
          description: ok
      summary: Returns one of the caller org's machines by its org-scoped name.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/machines/{id}/agent:
    delete:
      description: |-
        Detaches the agent runtime from one of the caller org's
        machines. The machine stays — this halts the bot, it does not terminate the
        compute. Answers 204.
      operationId: unbindMachineAgent
      parameters:
      - description: |-
          ID is the machine's org-scoped NAME — the stable key Visor addresses a
          machine by (owner/name), not the ephemeral provider id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Detaches the agent runtime from one of the caller org's machines.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns the agent binding of one of the caller org's
        machines, or 404 when the machine runs no bot runtime.
      operationId: getMachineAgent
      parameters:
      - description: |-
          ID is the machine's org-scoped NAME — the stable key Visor addresses a
          machine by (owner/name), not the ephemeral provider id.
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              example:
                agentName: bot-a
                botVersion: 1.4.0
                machineId: drop-a
                status: running
              schema:
                $ref: '#/components/schemas/agentBinding'
          description: ok
      summary: Returns the agent binding of one of the caller org's machines, or 404
        when the machine runs no bot runtime.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
    put:
      description: |-
        Binds a cloud Agent to one of the caller org's machines: the
        machine is recorded as running that Agent's @hanzo/bot runtime. The owning org is
        the validated tenant, never a client field.
      operationId: bindMachineAgent
      parameters:
      - description: ID is the machine to bind, from the URL path.
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              agentName: bot-a
              botVersion: 1.4.0
            schema:
              $ref: '#/components/schemas/bindAgentReq'
        required: true
      responses:
        "200":
          content:
            application/json:
              example:
                agentName: bot-a
                machineId: drop-a
                status: binding
              schema:
                $ref: '#/components/schemas/agentBinding'
          description: ok
      summary: 'Binds a cloud Agent to one of the caller org''s machines: the machine
        is recorded as running that Agent''s @hanzo/bot runtime.'
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/visor/machines/agents:
    get:
      description: |-
        Returns every agent↔machine binding in the caller's org — which
        machines are running which cloud Agent, with vm's own reconciled status.
      operationId: listMachineAgents
      responses:
        "200":
          content:
            application/json:
              example:
                agentBindings:
                - agentName: bot-a
                  machineId: drop-a
                  publicIp: 1.2.3.4
                  status: running
              schema:
                $ref: '#/components/schemas/bindingList'
          description: ok
      summary: Returns every agent↔machine binding in the caller's org — which machines
        are running which cloud Agent, with vm's own reconciled status.
      tags:
      - visor
      x-app: visor
      x-public: true
      x-tool: true
  /v1/webhooks:
    get:
      description: |-
        Returns every webhook endpoint the caller's org has registered,
        newest first, each with its 7-day delivery and failure counts. Signing secrets
        are redacted here — a secret leaves the server only on create and on rotate.
        The listing is physically org-scoped, so another tenant's endpoints are not
        reachable from this route at all.
      operationId: get_webhooks
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/endpointList'
          description: ok
      summary: Returns every webhook endpoint the caller's org has registered, newest
        first, each with its 7-day delivery and failure counts.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
    post:
      description: |-
        Registers a new webhook subscription for the caller's org and
        answers 201 with the endpoint INCLUDING its freshly minted signing secret.
        This is one of only two responses that ever carry that secret (the other is
        rotate) — store it now, because no later read returns it. The org is stamped by
        the server from the validated principal, so a body can never register an
        endpoint in another tenant.
      operationId: post_webhooks
      requestBody:
        content:
          application/json:
            example:
              description: order pipeline
              events:
              - commerce.order.>
              url: https://acme.example/hooks/hanzo
            schema:
              $ref: '#/components/schemas/createEndpointIn'
        required: true
      responses:
        "201":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
          description: created
      summary: Registers a new webhook subscription for the caller's org and answers
        201 with the endpoint INCLUDING its freshly minted signing secret.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
  /v1/webhooks/{id}:
    delete:
      description: |-
        Removes one of the caller org's webhook endpoints and answers
        204 with no body. Delivery stops immediately and the endpoint's signing secret
        is gone with it; its recorded delivery history goes too. An id another org owns
        reads as not found.
      operationId: delete_webhooks_by_id
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "204":
          description: no content
      summary: Removes one of the caller org's webhook endpoints and answers 204 with
        no body.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
    get:
      description: |-
        Returns one of the caller org's webhook endpoints with its 7-day
        delivery and failure counts, signing secret redacted. An id another org owns
        reads as not found, so the response cannot confirm that it exists.
      operationId: get_webhooks_by_id
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
          description: ok
      summary: Returns one of the caller org's webhook endpoints with its 7-day delivery
        and failure counts, signing secret redacted.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
    put:
      description: |-
        Replaces the editable fields of one of the caller org's
        endpoints — url, events, status and description — and answers the stored row
        with its secret redacted. It is a full replace, not a patch: an omitted field
        is written as its empty value, and an omitted or empty events list resubscribes
        the endpoint to EVERY event. The signing secret and the creation time are
        immutable here; rotate the secret with POST /v1/webhooks/{id}/secret.
      operationId: put_webhooks_by_id
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            example:
              events:
              - commerce.order.paid
              id: wh_9f8c1d2e
              status: disabled
              url: https://acme.example/hooks/v2
            schema:
              $ref: '#/components/schemas/updateEndpointIn'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
          description: ok
      summary: Replaces the editable fields of one of the caller org's endpoints —
        url, events, status and description — and answers the stored row with its
        secret redacted.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
  /v1/webhooks/{id}/deliveries:
    get:
      description: |-
        Returns one endpoint's per-attempt delivery log, newest first —
        the record of what was sent, what the subscriber answered, and how long it
        took. One event that retried three times appears as three rows sharing a
        delivery id. It is org-scoped exactly like every other route here: the endpoint
        lookup only ever finds THIS org's endpoint, so another org's id is a 404 and
        never a window onto its logs.
      operationId: get_webhooks_by_id_deliveries
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      - description: |-
          Limit caps how many attempts come back: default 50, maximum 200. A value
          that is not a positive integer reads as the default.
        example: 100
        in: query
        name: limit
        required: false
        schema:
          type: integer
      - description: |-
          Status narrows the log to one outcome: "ok", "retrying" or "failed".
          Empty returns every attempt.
        example: failed
        in: query
        name: status
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/deliveryList'
          description: ok
      summary: Returns one endpoint's per-attempt delivery log, newest first — the
        record of what was sent, what the subscriber answered, and how long it took.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
  /v1/webhooks/{id}/secret:
    post:
      description: |-
        Mints a NEW HMAC signing secret for the endpoint and answers the
        endpoint WITH it — the only other response besides create that ever carries a
        secret. The old secret stops working the instant this returns: every subsequent
        delivery signs with the new one, with no overlap window. Call it when the
        subscriber is ready to swap the value on its side, not before.
      operationId: post_webhooks_by_id_secret
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Endpoint'
          description: ok
      summary: Mints a NEW HMAC signing secret for the endpoint and answers the endpoint
        WITH it — the only other response besides create that ever carries a secret.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
  /v1/webhooks/{id}/test:
    post:
      description: |-
        Sends ONE signed test event to the endpoint right now and answers
        the outcome inline, so the console can show whether the subscriber is reachable
        without waiting for real traffic. It takes the same attempt path the bus
        dispatcher takes — one attempt, 10s timeout, no retry ladder — and records the
        result in the endpoint's delivery log. It works on a DISABLED endpoint too:
        validating one you have paused is the whole point.
      operationId: post_webhooks_by_id_test
      parameters:
      - example: wh_9f8c1d2e
        in: path
        name: id
        required: true
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/testResult'
          description: ok
      summary: Sends ONE signed test event to the endpoint right now and answers the
        outcome inline, so the console can show whether the subscriber is reachable
        without waiting for real traffic.
      tags:
      - webhooks
      x-app: webhooks
      x-public: true
      x-tool: true
  /v1/websearch:
    post:
      description: |-
        Searches the live web and answers with ranked results.

        This is the fleet's path to what is happening RIGHT NOW — today's weather, an
        outage, a release that postdates any model's training. `q` is the query and
        `language` narrows it to a locale. The answer is `{query, number_of_results,
        results:[{url, title, content, engine}]}`, where `content` is the ENGINE's
        snippet and not the page: read a page with POST /v1/crawl.

        It is served in-process by a Go meta-search over keyless public engines — never
        a third-party search API and never a search key. The enabled engines run
        concurrently and their hits are merged, deduplicated by normalised URL (host
        and path, trailing slash and fragment dropped, query kept, so distinct queries
        stay distinct results) and capped at 30. Ranking is deterministic rather than
        scored: the first configured engine's hits lead.

        It fails SOFT on the engines. One that errors, times out or is served a
        bot-challenge page contributes zero results and never fails the call, so an
        empty `results` is a real answer — nothing was found — and not an outage. The
        array is always present, never null.

        A VALIDATED PRINCIPAL IS REQUIRED, and there is no tenant beyond that: the
        results are public web pages, identical for every caller, so nothing here is
        scoped and nothing here can leak across orgs. A typed op is also an MCP tool
        and a CLI command, and tools/call invokes it with no route and therefore no
        middleware — so the gate is in the handler, where every door reaches it, rather
        than in a middleware only one door passes through.
      operationId: search_web
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/webSearchQuery'
        required: true
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/webSearchResults'
          description: ok
      summary: Search the live web
      tags:
      - websearch
      x-app: websearch
      x-public: true
      x-tool: true
  /v1/websearch/scrape:
    post:
      description: |-
        Takes {url} and answers {success, data:{markdown, metadata}} — the exact contract a firecrawl client decodes. The fetch, extraction and optional browser render run in-process; there is no crawler pod to be down.

        The shared service key is required as an Authorization Bearer, compared in constant time: unset on the deployment is 503, missing or wrong is 401. Unlike search, a validated principal does NOT substitute for it — this is the service-to-service door.

        A page is archived under the caller's own org and project, taken from the verified principal when there is one, so a scrape lands in the same corpus /v1/crawl fills and a URL already read under that scope is answered from the archive without touching the network. A service caller carrying no principal shares the unscoped prefix.

        The URL is caller-supplied and fetched from INSIDE the cluster, which makes this a request-forgery primitive by construction: in-namespace service DNS and a cloud metadata endpoint that hands credentials to anyone who asks are both a resolution away. Only http and https are accepted, and every address actually dialled must be public unicast — loopback, link-local, private and multicast are refused. The check lives in the DIALER rather than on the hostname, because resolving a name to validate it and then letting the transport resolve it again is a gap DNS rebinding walks straight through; redirects re-enter the same dialer, so a public URL that bounces to the metadata address is refused at the hop that matters.

        The one thing to get right: FAILURE IS 200. A missing or unparseable url, a body over the 1 MiB read cap, and a fetch that could not be completed all answer HTTP 200 with success:false and a reason — a firecrawl client reads data.success, not the status line. Only the two auth refusals use a status code, so a caller that branches on HTTP status alone will read every failed scrape as a success.
      operationId: post_websearch_scrape
      summary: Fetch one page and get its extracted markdown, in the firecrawl envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
  /v1/websearch/search:
    delete:
      description: |-
        Answers {query, number_of_results, results:[{url, title, content, engine}]} — the exact /search?format=json contract a SearXNG client decodes, so an agent tool configured against SearXNG reaches this with no change. `q` is the query and `language` narrows it; both are read from the QUERY STRING.

        Served in-process by a Go meta-search over keyless public engines, never a third-party search API and never a search key. The enabled engines run concurrently and their hits are merged, deduplicated by normalised URL (host and path, trailing slash and fragment dropped, query kept — distinct queries are distinct results) and capped at 30. Ranking is deterministic rather than scored: the first configured engine's hits lead.

        TWO WAYS IN, one-way equivalent, and no third: a validated principal — the same gate the whole data plane uses — passes straight through, and a caller without one must present the shared service key as X-API-Key, compared in constant time. A deployment with no key configured answers 503 rather than opening the surface to everyone, and a missing or wrong key is 401. It is never an open proxy. There is no tenant scoping beyond that gate, and there is nothing to scope: the results are public web pages, identical for every caller.

        It fails SOFT on the engines and closed only on the gate. An engine that errors or is served a bot-challenge page contributes zero results and never fails the request, so an empty `results` is a real answer — nothing was found — and not an outage. The array is always present, never null.

        The one thing to get right: every method answers identically. This is one handler registered for all of them, and it reads only the query string, so a body sent on the write verbs is ignored rather than refused.
      operationId: delete_websearch_search
      summary: Keyless web meta-search, in the SearXNG JSON envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
    get:
      description: |-
        Answers {query, number_of_results, results:[{url, title, content, engine}]} — the exact /search?format=json contract a SearXNG client decodes, so an agent tool configured against SearXNG reaches this with no change. `q` is the query and `language` narrows it; both are read from the QUERY STRING.

        Served in-process by a Go meta-search over keyless public engines, never a third-party search API and never a search key. The enabled engines run concurrently and their hits are merged, deduplicated by normalised URL (host and path, trailing slash and fragment dropped, query kept — distinct queries are distinct results) and capped at 30. Ranking is deterministic rather than scored: the first configured engine's hits lead.

        TWO WAYS IN, one-way equivalent, and no third: a validated principal — the same gate the whole data plane uses — passes straight through, and a caller without one must present the shared service key as X-API-Key, compared in constant time. A deployment with no key configured answers 503 rather than opening the surface to everyone, and a missing or wrong key is 401. It is never an open proxy. There is no tenant scoping beyond that gate, and there is nothing to scope: the results are public web pages, identical for every caller.

        It fails SOFT on the engines and closed only on the gate. An engine that errors or is served a bot-challenge page contributes zero results and never fails the request, so an empty `results` is a real answer — nothing was found — and not an outage. The array is always present, never null.

        The one thing to get right: every method answers identically. This is one handler registered for all of them, and it reads only the query string, so a body sent on the write verbs is ignored rather than refused.
      operationId: get_websearch_search
      summary: Keyless web meta-search, in the SearXNG JSON envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
    patch:
      description: |-
        Answers {query, number_of_results, results:[{url, title, content, engine}]} — the exact /search?format=json contract a SearXNG client decodes, so an agent tool configured against SearXNG reaches this with no change. `q` is the query and `language` narrows it; both are read from the QUERY STRING.

        Served in-process by a Go meta-search over keyless public engines, never a third-party search API and never a search key. The enabled engines run concurrently and their hits are merged, deduplicated by normalised URL (host and path, trailing slash and fragment dropped, query kept — distinct queries are distinct results) and capped at 30. Ranking is deterministic rather than scored: the first configured engine's hits lead.

        TWO WAYS IN, one-way equivalent, and no third: a validated principal — the same gate the whole data plane uses — passes straight through, and a caller without one must present the shared service key as X-API-Key, compared in constant time. A deployment with no key configured answers 503 rather than opening the surface to everyone, and a missing or wrong key is 401. It is never an open proxy. There is no tenant scoping beyond that gate, and there is nothing to scope: the results are public web pages, identical for every caller.

        It fails SOFT on the engines and closed only on the gate. An engine that errors or is served a bot-challenge page contributes zero results and never fails the request, so an empty `results` is a real answer — nothing was found — and not an outage. The array is always present, never null.

        The one thing to get right: every method answers identically. This is one handler registered for all of them, and it reads only the query string, so a body sent on the write verbs is ignored rather than refused.
      operationId: patch_websearch_search
      summary: Keyless web meta-search, in the SearXNG JSON envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
    post:
      description: |-
        Answers {query, number_of_results, results:[{url, title, content, engine}]} — the exact /search?format=json contract a SearXNG client decodes, so an agent tool configured against SearXNG reaches this with no change. `q` is the query and `language` narrows it; both are read from the QUERY STRING.

        Served in-process by a Go meta-search over keyless public engines, never a third-party search API and never a search key. The enabled engines run concurrently and their hits are merged, deduplicated by normalised URL (host and path, trailing slash and fragment dropped, query kept — distinct queries are distinct results) and capped at 30. Ranking is deterministic rather than scored: the first configured engine's hits lead.

        TWO WAYS IN, one-way equivalent, and no third: a validated principal — the same gate the whole data plane uses — passes straight through, and a caller without one must present the shared service key as X-API-Key, compared in constant time. A deployment with no key configured answers 503 rather than opening the surface to everyone, and a missing or wrong key is 401. It is never an open proxy. There is no tenant scoping beyond that gate, and there is nothing to scope: the results are public web pages, identical for every caller.

        It fails SOFT on the engines and closed only on the gate. An engine that errors or is served a bot-challenge page contributes zero results and never fails the request, so an empty `results` is a real answer — nothing was found — and not an outage. The array is always present, never null.

        The one thing to get right: every method answers identically. This is one handler registered for all of them, and it reads only the query string, so a body sent on the write verbs is ignored rather than refused.
      operationId: post_websearch_search
      summary: Keyless web meta-search, in the SearXNG JSON envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
    put:
      description: |-
        Answers {query, number_of_results, results:[{url, title, content, engine}]} — the exact /search?format=json contract a SearXNG client decodes, so an agent tool configured against SearXNG reaches this with no change. `q` is the query and `language` narrows it; both are read from the QUERY STRING.

        Served in-process by a Go meta-search over keyless public engines, never a third-party search API and never a search key. The enabled engines run concurrently and their hits are merged, deduplicated by normalised URL (host and path, trailing slash and fragment dropped, query kept — distinct queries are distinct results) and capped at 30. Ranking is deterministic rather than scored: the first configured engine's hits lead.

        TWO WAYS IN, one-way equivalent, and no third: a validated principal — the same gate the whole data plane uses — passes straight through, and a caller without one must present the shared service key as X-API-Key, compared in constant time. A deployment with no key configured answers 503 rather than opening the surface to everyone, and a missing or wrong key is 401. It is never an open proxy. There is no tenant scoping beyond that gate, and there is nothing to scope: the results are public web pages, identical for every caller.

        It fails SOFT on the engines and closed only on the gate. An engine that errors or is served a bot-challenge page contributes zero results and never fails the request, so an empty `results` is a real answer — nothing was found — and not an outage. The array is always present, never null.

        The one thing to get right: every method answers identically. This is one handler registered for all of them, and it reads only the query string, so a body sent on the write verbs is ignored rather than refused.
      operationId: put_websearch_search
      summary: Keyless web meta-search, in the SearXNG JSON envelope.
      tags:
      - websearch
      x-app: websearch
      x-public: true
security:
- bearer: []
servers:
- url: https://api.hanzo.ai
tags:
- description: 'Package account is your own account: API keys you mint and revoke,
    and org onboarding.'
  name: account
- description: Package admission is the launch-control GATE for Hanzo's hosted services
    — the COMPLETE waitlist feature, COMPOSING the ONE flag engine (apps/flags) one-way.
  name: admission
- description: 'Package agents is autonomous agents for your org: define them, run
    them, keep every run.'
  name: agents
- description: Package ai is Hanzo AI — the model API on /v1 (/v1/chat/completions,
    /v1/messages, /v1/models and the rest of hanzoai/ai's surface) — mounted into
    a cloud binary with the money, ingest and telemetry callbacks cloud BUILDS but
    cannot INSTALL.
  name: ai
- description: Package allowance is how much a plan lets you do without paying, and
    how much of it you have left today.
  name: allowance
- description: 'Package analytics is product analytics: send an event, read back who
    did what.'
  name: analytics
- description: Package ask is a plain-language question about your business, answered
    with real numbers.
  name: ask
- description: 'Package auditlog is your org''s tamper-evident audit trail: every
    security-relevant event, hash-chained and readable.'
  name: audit
- name: authz
- description: Package automations is workflows that run themselves, on a schedule
    or a webhook.
  name: automations
- description: 'Package base is managed Hanzo Base: a hosted backend for your app
    — collections, records, access rules and sign-in.'
  name: base
- description: Package billing is your org's balance, what it has spent, and the cards
    it pays with.
  name: billing
- description: Package blueprint is what a template costs to run, worked out before
    you deploy.
  name: blueprint
- description: Package catalog is one place to browse every project, app and site
    built here.
  name: catalog
- description: Package channels is one inbox for the chat apps you connect — Discord,
    Slack, Teams, Telegram.
  name: channels
- description: 'Package cloudflare is your Cloudflare account, managed from Hanzo:
    zones, Pages, Workers, Workers AI, R2, KV and D1.'
  name: cloudflare
- description: Package code is search and symbols across your repos, for you and your
    agents.
  name: code
- description: 'Package commerce is selling: checkout, subscriptions, invoices, spend
    alerts, payment webhooks and the storefront catalog.'
  name: commerce
- description: Package crawl is any web page turned into clean markdown a model can
    read.
  name: crawl
- description: 'Package deploy is Hanzo CD: see what each app is running, sync it,
    and roll back a bad release.'
  name: deploy
- description: Package destinations is your events forwarded to the ad and analytics
    tools you use.
  name: destinations
- description: 'Package domain is Hanzo Domains: search a name, see the price, buy
    it from your prepaid wallet.'
  name: domain
- description: 'Package engine is Hanzo Engine: which models the serving runtime has
    loaded, and the GPUs under it.'
  name: engine
- description: 'Package entitlements is what your org may run: what the plan grants,
    and which of those products are switched on.'
  name: entitlements
- description: Package eval is scoring a model on your own data, with a judge you
    choose.
  name: evals
- description: 'Package exec is the code interpreter: run a snippet in a sandbox,
    and move files in and out of the session that sandbox IS.'
  name: exec
- description: 'Package flags is feature flags: ship it dark, then turn it on for
    who you pick.'
  name: flags
- description: 'Package flow is Hanzo Flow: build an agent workflow on a visual canvas,
    run it, and read every run.'
  name: flow
- description: 'Package functions is your serverless code: publish it, call it over
    HTTP, watch every run and what it cost.'
  name: functions
- description: 'Package gateway is live control of the policy your API applies to
    every incoming request: CORS, rate limits, cache TTL and allowed methods, changed
    without a redeploy.'
  name: gateway
- description: 'Package git is Git hosting for your org: create repos, clone, push,
    and see what they cost.'
  name: git
- description: 'Package help is a support desk: customers file tickets, your team
    answers them.'
  name: help
- description: 'Package iam is Hanzo''s identity provider: users, organizations, applications,
    and the OIDC/OAuth2 endpoints every Hanzo service authenticates against.'
  name: iam
- description: Package index is fast full-text search over your own data, typos forgiven.
  name: index
- description: 'Package ingress is your front door: automatic TLS certificates and
    hostname routing to any backend, changed live.'
  name: ingress
- description: Package integrations is how your org connects third-party accounts
    like Slack, and revokes them.
  name: integrations
- description: 'Package kms is secret custody: your org''s secrets sealed at rest,
    plus threshold signing.'
  name: kms
- description: Package knowledge is your team's wiki and your agents' memory, searchable
    by meaning.
  name: knowledge
- description: Package leaderboard is the ranking of who uses AI most, in your org
    and globally.
  name: leaderboard
- description: Package lsp is live semantic code intelligence — definitions, references,
    types, hover, outline and diagnostics — over a repository AND its resolved dependencies,
    with no toolchain on the caller's machine.
  name: lsp
- name: metrics
- description: 'Package mq is queue and stream admin for your org: create them, watch
    them drain, ack what you pulled.'
  name: mq
- description: Package notify is transactional email and SMS, sent through your org's
    own provider credential.
  name: notify
- description: 'Package o11y is your logs, metrics and traces: ship them in, query
    them, chart them.'
  name: o11y
- description: 'The served contract: the OpenAPI document every client, tool list
    and command group is generated from, its command projection, and the agent door
    that offers the same operations over MCP.'
  name: openapi
- description: 'Package plan is the plan catalog: every tier you can buy, what it
    costs, and what it grants.'
  name: plan
- description: 'Package platform is Hanzo PaaS: deploy containers to your own tenant
    namespace — builds, releases, environments, logs, custom domains.'
  name: platform
- description: Package prefs is your own settings — theme, density, pinned nav — following
    you across every Hanzo app.
  name: prefs
- description: 'Package pricing is the price list: what every model, provider, GPU
    tier, tool and hosting plan costs.'
  name: pricing
- description: 'Package projects is where your sites live: create one, deploy a build,
    roll back to any release.'
  name: projects
- description: Package prompts is your prompt library, versioned, so nothing changes
    silently.
  name: prompts
- description: 'Package provisioning is one-click data add-ons: a SQL, key-value,
    document, vector, search or object store, wired straight into your app.'
  name: provisioning
- description: 'Package pubsub is your message bus: publish, subscribe, and durable
    streams your apps read at their own pace.'
  name: pubsub
- description: 'Package registry is your container and package registry: push images,
    pull them back, see what you store.'
  name: registry
- description: 'Package sandbox is the ONE compute primitive: a sandbox is a gVisor
    pod that runs somebody else''s code, and every lifetime is the same object.'
  name: sandboxes
- description: Package search is one ranked result set over everything your org has
    stored.
  name: search
- description: Package settings is how an org configures each product it uses, secret
    fields included.
  name: settings
- description: 'Package storage is object storage: your buckets and the files in them,
    with signed URLs for upload and download.'
  name: storage
- description: 'Package sync is data sync: link two endpoints and keep them in step,
    on a webhook, on a schedule, or on demand.'
  name: sync
- description: 'Package tasks is Hanzo Tasks: durable workflows that survive a crash,
    with every run visible and replayable.'
  name: tasks
- description: 'Package team is your org''s shared workspace: documents edited together,
    files, seats, and agents as teammates.'
  name: team
- description: 'Package tel is the telecommunications surface: phone numbers, calls
    and messages, on whatever carrier the deployment is configured for.'
  name: tel
- description: Package templates is a gallery of starter kits you can deploy as they
    come.
  name: templates
- description: 'Package todo is Hanzo Todo: boards, the work items on them, and the
    filters that make a board.'
  name: todo
- description: 'Package tools is everything your org can call, in one list: connector
    actions, functions, agents, skills and your own MCP servers.'
  name: tools
- description: 'Package treasury is the reserve fund behind every payout: real capital,
    held and accounted for.'
  name: treasury
- description: Package usage is what your org ran and what it cost, broken down per
    account.
  name: usage
- description: 'Package visor is the compute you rent from Hanzo: machines, GPUs and
    clusters — launch one, resize it, tear it down.'
  name: visor
- description: 'Package webhooks is how your app hears about events: register an endpoint,
    pick the events, get each one delivered and signed.'
  name: webhooks
- description: Package websearch is a web search and a page fetch your agents can
    call.
  name: websearch
- description: 'Package zt mounts the Hanzo Cloud NETWORKING surface: the tenant''s
    Hanzo Zero Trust footprint — overlay networks, their routers and mesh services
    — served as clean, org-scoped REST off the unified cloud binary and fronting the
    Hanzo Zero Trust controller (hanzoai/zt, an OpenZiti-based fabric).'
  name: zt
