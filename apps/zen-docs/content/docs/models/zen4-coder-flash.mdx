---
title: zen4-coder-flash
description: Fast code model built on Qwen3-Coder-30B-A3B by Alibaba Qwen. 30B (3B active) MoE with 262K context.
---

# zen4-coder-flash

**Fast Code**

Lightweight code model optimized for speed and inline completions. Built on Qwen3-Coder-30B-A3B by Alibaba Qwen -- 30B total parameters with only 3B active per token.

## Specifications

| Property | Value |
|----------|-------|
| **Model ID** | `zen4-coder-flash` |
| **Upstream** | Qwen3-Coder-30B-A3B by Alibaba Qwen |
| **Parameters** | 30B (3B active) |
| **Architecture** | MoE |
| **Context Window** | 262K tokens |
| **Tier** | pro max |
| **Input Price** | $1.50 / 1M tokens |
| **Output Price** | $1.50 / 1M tokens |

## Capabilities

- Fast code generation for rapid iteration
- 262K context for codebase-level understanding
- Inline completions and code review
- Efficient MoE inference with only 3B active parameters
- All major programming languages

## API Usage

```bash
curl https://api.hanzo.ai/v1/chat/completions \
  -H "Authorization: Bearer $HANZO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zen4-coder-flash",
    "messages": [{"role": "user", "content": "Write a Python function for binary search."}]
  }'
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.hanzo.ai/v1",
    api_key="hk-your-api-key",
)

response = client.chat.completions.create(
    model="zen4-coder-flash",
    messages=[{"role": "user", "content": "Write a Python function for binary search."}],
)
print(response.choices[0].message.content)
```

## Upstream

zen4-coder-flash is built on open-source weights from **Qwen3-Coder-30B-A3B** by Alibaba Qwen. At $1.50/MTok for both input and output, it is the most cost-effective code model in the Zen lineup. Pricing is set at 3x upstream inference cost.

## See Also

- [zen4-coder](/docs/models/zen4-coder) -- Larger 480B MoE code model
- [zen4-coder-pro](/docs/models/zen4-coder-pro) -- Full BF16 precision
- [Pricing](/docs/api/pricing) -- Full pricing table
