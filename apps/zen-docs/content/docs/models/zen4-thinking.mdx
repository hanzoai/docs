---
title: zen4-thinking
description: Deep reasoning model built on Qwen3-Next-80B-A3B (thinking) by Alibaba Qwen. 80B (3B active) MoE + CoT with 131K context.
---

# zen4-thinking

**Deep Reasoning**

Dedicated reasoning model with explicit chain-of-thought. Built on Qwen3-Next-80B-A3B (thinking mode) by Alibaba Qwen -- same architecture as zen4-pro with CoT reasoning enabled.

## Specifications

| Property | Value |
|----------|-------|
| **Model ID** | `zen4-thinking` |
| **Upstream** | Qwen3-Next-80B-A3B (thinking) by Alibaba Qwen |
| **Parameters** | 80B (3B active) |
| **Architecture** | MoE + CoT |
| **Context Window** | 131K tokens |
| **Tier** | pro max |
| **Input Price** | $2.70 / 1M tokens |
| **Output Price** | $2.70 / 1M tokens |

## Capabilities

- Explicit chain-of-thought reasoning
- Mathematical and logical problem solving
- Step-by-step analysis and deduction
- Complex multi-step planning
- 131K context window

## API Usage

```bash
curl https://api.hanzo.ai/v1/chat/completions \
  -H "Authorization: Bearer $HANZO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zen4-thinking",
    "messages": [{"role": "user", "content": "Solve: If 2^x = 1024, what is x?"}]
  }'
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.hanzo.ai/v1",
    api_key="hk-your-api-key",
)

response = client.chat.completions.create(
    model="zen4-thinking",
    messages=[{"role": "user", "content": "Solve: If 2^x = 1024, what is x?"}],
)
print(response.choices[0].message.content)
```

## Upstream

zen4-thinking is built on open-source weights from **Qwen3-Next-80B-A3B (thinking)** by Alibaba Qwen. The thinking variant enables explicit chain-of-thought reasoning, showing its work for complex problems. Pricing is set at 3x upstream inference cost.

## See Also

- [zen4-pro](/docs/models/zen4-pro) -- Same base without thinking mode
- [zen4-ultra](/docs/models/zen4-ultra) -- GLM-5 based maximum reasoning
- [Pricing](/docs/api/pricing) -- Full pricing table
