---
title: zen3-vl
description: Vision-language model built on Qwen3-VL-30B-A3B by Alibaba Qwen. 30B (3B active) MoE Vision-Language with 131K context.
---

# zen3-vl

**Vision-Language**

Vision-language model for image understanding and visual reasoning. Built on Qwen3-VL-30B-A3B by Alibaba Qwen -- 30B total parameters with 3B active via MoE.

## Specifications

| Property | Value |
|----------|-------|
| **Model ID** | `zen3-vl` |
| **Upstream** | Qwen3-VL-30B-A3B by Alibaba Qwen |
| **Parameters** | 30B (3B active) |
| **Architecture** | MoE Vision-Language |
| **Context Window** | 131K tokens |
| **Modalities** | Text, Vision |
| **Tier** | pro max |
| **Input Price** | $0.45 / 1M tokens |
| **Output Price** | $1.80 / 1M tokens |

## Capabilities

- Image analysis and visual understanding
- OCR across 32+ languages
- Spatial reasoning and object detection
- Document understanding (charts, tables, diagrams)
- Function calling for visual agent workflows
- 131K context window

## API Usage

```bash
curl https://api.hanzo.ai/v1/chat/completions \
  -H "Authorization: Bearer $HANZO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zen3-vl",
    "messages": [{
      "role": "user",
      "content": [
        {"type": "text", "text": "Read the text in this screenshot."},
        {"type": "image_url", "image_url": {"url": "https://example.com/screenshot.png"}}
      ]
    }]
  }'
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.hanzo.ai/v1",
    api_key="hk-your-api-key",
)

response = client.chat.completions.create(
    model="zen3-vl",
    messages=[{
        "role": "user",
        "content": [
            {"type": "text", "text": "What does this chart show?"},
            {"type": "image_url", "image_url": {"url": "https://example.com/chart.png"}},
        ],
    }],
)
print(response.choices[0].message.content)
```

## Upstream

zen3-vl is built on open-source weights from **Qwen3-VL-30B-A3B** by Alibaba Qwen. With only 3B active parameters from 30B total, it delivers strong vision-language capability at very low cost ($0.45 input / $1.80 output per MTok). Pricing is set at 3x upstream inference cost.

## See Also

- [zen3-omni](/docs/models/zen3-omni) -- Full multimodal (text + vision + audio)
- [zen4-mini](/docs/models/zen4-mini) -- Text-only fast model
- [Pricing](/docs/api/pricing) -- Full pricing table
