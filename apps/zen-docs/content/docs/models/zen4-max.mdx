---
title: zen4-max
description: Trillion-parameter frontier MoE model built on Kimi K2-Thinking by Moonshot AI. 1.04T (32B active) with 256K context.
---

# zen4-max

**Maximum Scale**

Trillion-parameter frontier model with deep reasoning capabilities. Built on Kimi K2-Thinking by Moonshot AI -- 1.04T total parameters with 32B active per token.

## Specifications

| Property | Value |
|----------|-------|
| **Model ID** | `zen4-max` |
| **Upstream** | Kimi K2-Thinking by Moonshot AI |
| **Parameters** | 1.04T (32B active) |
| **Architecture** | MoE |
| **Context Window** | 256K tokens |
| **Tier** | ultra max |
| **Input Price** | $3.60 / 1M tokens |
| **Output Price** | $3.60 / 1M tokens |

## Capabilities

- Frontier-scale reasoning with 1.04T parameters
- Deep thinking with chain-of-thought capabilities
- 256K context window for massive document processing
- Strong performance on math, code, and complex reasoning
- Efficient MoE inference with only 32B active parameters

## API Usage

```bash
curl https://api.hanzo.ai/v1/chat/completions \
  -H "Authorization: Bearer $HANZO_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "zen4-max",
    "messages": [{"role": "user", "content": "Summarize this research paper..."}]
  }'
```

```python
from openai import OpenAI

client = OpenAI(
    base_url="https://api.hanzo.ai/v1",
    api_key="hk-your-api-key",
)

response = client.chat.completions.create(
    model="zen4-max",
    messages=[{"role": "user", "content": "Summarize this research paper..."}],
)
print(response.choices[0].message.content)
```

## Upstream

zen4-max is built on open-source weights from **Kimi K2-Thinking** by Moonshot AI. With 1.04T total parameters and 32B active, it is the largest model in the Zen family -- offering frontier-scale reasoning at efficient MoE inference cost. Pricing is set at 3x upstream inference cost.

## See Also

- [zen4](/docs/models/zen4) -- GLM-5 flagship (744B MoE)
- [zen4-ultra](/docs/models/zen4-ultra) -- GLM-5 with deep thinking mode
- [Pricing](/docs/api/pricing) -- Full pricing table
