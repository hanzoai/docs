{
  "extends": "tsconfig/react-library.json",
  "compilerOptions": {
    // process.env is how Next inlines NEXT_PUBLIC_* at the consuming app's build.
    "types": ["node"]
  },
  "exclude": ["node_modules", "dist"]
}
